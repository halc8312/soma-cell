# coding: utf-8
"""Focused validation for SOMA-CELL 0.6.8-GPU A4.1 through A4.9c slices."""
from __future__ import division

import argparse
import copy
import csv
import gc
import hashlib
import inspect
import json
import os
import pickle
import sys
import tempfile
import time
from collections.abc import Mapping

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.abspath(os.path.join(HERE, os.pardir, os.pardir))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import SOMA_CELL_0_6_8_gpu_a4 as a4
import SOMA_CELL_0_6_8_gpu_a4_replication as a44
import SOMA_CELL_0_6_8_gpu_a4_hydrolysis as a47
import SOMA_CELL_0_6_8_gpu_a4_integration as a48
import SOMA_CELL_0_6_8_gpu_a4_translation_integration as a48b
import SOMA_CELL_0_6_8_gpu_a4_replication_integration as a48c
import SOMA_CELL_0_6_8_gpu_a4_replication_completion_integration as a48c2
import SOMA_CELL_0_6_8_gpu_a4_replication_completion_mutation_integration as a48c3
import SOMA_CELL_0_6_8_gpu_a4_replication_start_integration as a48c4
import SOMA_CELL_0_6_8_gpu_a4_replication_start_mutation_free_integration as a48c5
import SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_integration as a48c6
import SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_mutation_integration as a48c7
import SOMA_CELL_0_6_8_gpu_a4_replication_early_noop_integration as a48c8
import SOMA_CELL_0_6_8_gpu_a4_resident_arena as a49a
import SOMA_CELL_0_6_8_gpu_a4_resident_translation_integration as a49b
import SOMA_CELL_0_6_8_gpu_a4_resident_maintenance_foundation as a49c
import SOMA_CELL_0_6_8_gpu_a3_scheduler as a3s
import SOMA_CELL_0_6_8_A3_validation as v3

try:
    import torch
except Exception:  # pragma: no cover
    torch = None

RESULT_JSON = 'SOMA_CELL_0_6_8_GPU_A4_9C_VALIDATION_RESULTS.json'
RESULT_CSV = 'soma_cell_0_6_8_gpu_a4_9c_validation.csv'
RESULT_TXT = 'SOMA_CELL_0_6_8_GPU_A4_9C_VALIDATION_RESULTS.txt'

SOURCE_PATHS = (
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_hydrolysis.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_translation_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_mutation_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_mutation_free_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_mutation_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_early_noop_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_resident_arena.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_resident_translation_integration.py',
    'src/0_6_8/SOMA_CELL_0_6_8_A4_validation.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3.py',
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3_scheduler.py',
    'src/0_6_8/SOMA_CELL_0_6_8_A3_validation.py',
    'src/0_6_6/SOMA_CELL_0_6_6_pythonista.py',
    'src/0_6_5/SOMA_CELL_0_6_5_pythonista.py',
    'src/baseline/SOMA_CELL_0_5_pythonista.py',
    'src/baseline/SOMA_CELL_0_4_pythonista.py',
    'src/baseline/SOMA_CELL_0_3_pythonista.py',
    'src/baseline/SOMA_CELL_0_2_pythonista.py',
    'src/0_6_p0/SOMA_CELL_0_6_P0_pythonista.py',
    'docs/SOMA_CELL_0_6_8_GPU_A4_CONTRACT.md',
    'docs/SOMA_CELL_0_6_8_GPU_A4_SCHEMA.json',
    'planning/SOMA_CELL_0_6_8_GPU_A4_PREREGISTRATION_JA.md',
)

A49C_NEW_MODULE_PATH = (
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_resident_maintenance_foundation.py'
)
A49C_SOURCE_PATHS = SOURCE_PATHS + (A49C_NEW_MODULE_PATH,)

PROMOTED_A3_SHA256 = {
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3.py': (
        '6ca66378702dabd5a388553ca92279376deae5ba8c39bea9bdbe3488aa41b4f6'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3_scheduler.py': (
        '5e207f68fd0d9aa674ab054dc43d62247e84c1f3c61fc6c7b455960ff84413aa'
    ),
}

_REQUIRE_CUDA = False


def _sha256(path):
    digest = hashlib.sha256()
    with open(path, 'rb') as handle:
        for block in iter(lambda: handle.read(1 << 20), b''):
            digest.update(block)
    return digest.hexdigest()


def source_sha256_map():
    result = {}
    for relative in SOURCE_PATHS:
        path = os.path.join(ROOT, *relative.split('/'))
        if not os.path.isfile(path):
            raise AssertionError('source hash input missing: %s' % relative)
        result[relative] = _sha256(path)
    return result


def a49c_source_sha256_map():
    result = {}
    for relative in A49C_SOURCE_PATHS:
        path = os.path.join(ROOT, *relative.split('/'))
        if not os.path.isfile(path):
            raise AssertionError('A4.9c source hash input missing: %s' % relative)
        result[relative] = _sha256(path)
    return result


def _fixture_cells(seed=7401):
    world = v3.make_world(seed=seed, cells=4)
    cells = world.cells
    gene_a = a4.g2.make_gene(
        a4.g2.ROLE_ENERGY, parameter=1, promoter=7, efficiency=6,
    )
    gene_b = a4.g2.make_gene(
        a4.g2.ROLE_GENERIC,
        parameter=a4.g2.REACTION_INTERMEDIATE_TO_WASTE,
        promoter=4, efficiency=5,
    )
    damaged_prefix = np.asarray((4, 3, 2, 1, 7), dtype=np.uint8)
    sequence_a = np.concatenate((gene_a, damaged_prefix, gene_b)).astype(np.uint8)
    sequence_b = np.concatenate((gene_b, gene_a, gene_a)).astype(np.uint8)

    cells[0].genomes = []
    cells[0].genome_lesions = []
    cells[0].replication_template = None
    cells[0].replication_copy = []
    cells[0].replication_template_lesion = 0.0
    cells[0].replication_fractional = 0.0

    cells[1].genomes = [sequence_a.copy()]
    # This shorter-than-genome-count vector is legal until the damage phase.
    cells[1].genome_lesions = []
    # An active replication record must retain even an empty copy sequence.
    cells[1].replication_template = sequence_a.copy()
    cells[1].replication_copy = []
    cells[1].replication_template_lesion = 0.125
    cells[1].replication_fractional = 0.25

    cells[2].genomes = [sequence_b.copy(), sequence_a.copy()]
    cells[2].genome_lesions = [0.25]
    cells[2].replication_template = sequence_b.copy()
    cells[2].replication_copy = [int(value) for value in sequence_b[:11]]
    cells[2].replication_template_lesion = 0.375
    cells[2].replication_fractional = 0.625

    # Frozen damage state can transiently contain three complete polymers;
    # this explicitly guards against the old A1 `[:2]` representation loss.
    cells[3].genomes = [
        sequence_a.copy(), sequence_b.copy(), sequence_a.copy(),
    ]
    cells[3].genome_lesions = [0.1, 0.2, 0.3]
    cells[3].replication_template = None
    cells[3].replication_copy = []
    cells[3].replication_template_lesion = 0.0
    cells[3].replication_fractional = 0.0

    for cell in cells:
        cell._refresh_gene_cache()
    return world, cells


def _structural_state(cell):
    return {
        'cell_id': int(cell.cell_id),
        'genomes': [np.asarray(item, dtype=np.uint8).copy()
                    for item in cell.genomes],
        'genome_lesions': list(float(value) for value in cell.genome_lesions),
        'replication_template': (
            None if cell.replication_template is None
            else np.asarray(cell.replication_template, dtype=np.uint8).copy()
        ),
        'replication_copy': list(int(value) for value in cell.replication_copy),
        'replication_template_lesion': float(cell.replication_template_lesion),
        'replication_fractional': float(cell.replication_fractional),
        'gene_order': list(cell.gene_specs.keys()),
        'gene_specs': copy.deepcopy(cell.gene_specs),
    }


def _assert_structural_equal(expected, actual, label='cell'):
    v3.assert_recursive_close(
        _structural_state(expected), _structural_state(actual),
        atol=0.0, rtol=0.0, path=label,
    )


def _target_cells(seed=7501):
    world = v3.make_world(seed=seed, cells=4)
    return world, world.cells


def _gene_decode_fixture(seed=7601):
    """One-cell fixture fixing malformed-marker and cache-order semantics."""
    world = v3.make_world(seed=seed, cells=1)
    cell = world.cells[0]
    non_generic = a4.g2.make_gene(
        a4.g2.ROLE_TRANSLATOR,
        parameter=5, regulator=2, promoter=7, efficiency=0,
        fidelity=6, localisation=7, spare=(1, 2, 3, 4),
    )
    generic = a4.g2.make_gene(
        a4.g2.ROLE_GENERIC,
        parameter=a4.g2.REACTION_WASTE_TO_INTERMEDIATE,
        regulator=7, promoter=0, efficiency=7,
        fidelity=0, localisation=6, spare=(4, 5, 6, 7),
    )
    grammar = a4.s65.make_grammar_gene(
        a4.s65.GRAMMAR_READINESS,
        value=6, promoter=5, efficiency=4, fidelity=6,
    )
    bad_start = np.asarray([7, 6] + [1] * 12 + [0, 0], dtype=np.uint8)
    bad_stop = np.asarray([7, 7] + [1] * 12 + [0, 1], dtype=np.uint8)
    # Both outer @0 and nested @2 are syntactically valid.  Frozen parsing
    # accepts outer then advances 16, suppressing the nested candidate.
    overlapping_valid = np.concatenate((
        grammar, np.asarray([0, 0], dtype=np.uint8),
    ))
    # Outer @0 has the wrong stop; one-symbol recovery finds generic @2.
    invalid_outer_valid_inner = np.concatenate((
        np.asarray([7, 7], dtype=np.uint8), generic,
    ))
    genome0 = np.concatenate((
        bad_start, np.asarray([3, 4, 5], dtype=np.uint8),
        non_generic, generic,
    )).astype(np.uint8)
    genome1 = np.concatenate((
        generic, np.asarray([1, 2, 3], dtype=np.uint8),
        overlapping_valid, bad_stop,
    )).astype(np.uint8)
    genome2 = np.concatenate((
        np.asarray([1, 2], dtype=np.uint8), non_generic,
        invalid_outer_valid_inner, bad_stop, non_generic[:-1],
    )).astype(np.uint8)
    template_only = a4.g2.make_gene(
        a4.g2.ROLE_MEMBRANE, parameter=0, promoter=6, efficiency=5,
    )
    template = np.concatenate((template_only, non_generic)).astype(np.uint8)

    cell.genomes = [genome0, genome1, genome2]
    cell.genome_lesions = [0.1, 0.2, 0.3]
    cell.replication_template = template
    cell.replication_copy = [int(value) for value in template[:7]]
    cell.replication_template_lesion = 0.25
    cell.replication_fractional = 0.5
    cell._refresh_gene_cache()
    config = a4.GPU068A4Config(
        max_cells=1, max_sequences=5, max_symbols=210,
        max_sequence_symbols=67,
    )
    return world, cell, config


def _assert_gene_specs_equal(expected, actual, label):
    if list(expected.keys()) != list(actual.keys()):
        raise AssertionError('%s insertion order differs' % label)
    v3.assert_recursive_close(
        expected, actual, atol=0.0, rtol=0.0, path=label,
    )


def _assert_raises(kind, fn):
    try:
        fn()
    except kind:
        return
    if isinstance(kind, tuple):
        label = '/'.join(item.__name__ for item in kind)
    else:
        label = kind.__name__
    raise AssertionError('expected %s' % label)


def _paid_translation_fixture(seed=7701):
    """Four Formal066 cells fixing payment, early-gate, and MRO signals."""
    world = v3.make_world(seed=seed, cells=4)
    cells = world.cells
    for ci, cell in enumerate(cells):
        cell.last_receptor_activity = np.linspace(
            0.03 + 0.01 * ci, 0.42 + 0.01 * ci,
            a4.s4.LIGAND_COUNT, dtype=np.float64,
        )
        cell.last_control_vectors = np.asarray([
            (0.01 * (index + 1), -0.006 * (ci + index + 1))
            for index in range(a4.s4.CONTROL_COUNT)
        ], dtype=np.float64)
        cell.last_control_scalars = np.linspace(
            -0.08, 0.11 + 0.01 * ci, a4.s4.CONTROL_COUNT,
            dtype=np.float64,
        )
        cell.last_edna_signal = 0.07 + 0.02 * ci
        cell.last_corpse_signal = 0.05 + 0.01 * ci
        cell.last_necrotoxin_signal = 0.04 + 0.015 * ci
        cell.behavioural_quiescence = 0.18 + 0.03 * ci
        cell.last_translation = 0.41 + 0.01 * ci
        cell.last_quiescence = 0.61 + 0.01 * ci
        cell._refresh_gene_cache()
        cell._sync_protein_pool()
        cell._sync_damage_pool()

    # Formal066 inherits P0ProtoCell.osmolyte(), which includes every neural
    # attachment.  Keep one real attachment in the shared fixture so both the
    # NumPy oracle and resident Torch path exercise that dynamic-MRO input.
    p0 = sys.modules['SOMA_CELL_0_6_P0_pythonista']
    attachment = p0.NeuralAttachmentState(
        'a4-neural-osmolyte',
        stores=np.full((p0.BUDGET_COUNT,), 10.0, dtype=np.float64),
        tissue_material=np.full(
            (p0.TISSUE_MATERIAL_COUNT,), 10.0, dtype=np.float64,
        ),
    )
    cells[0].neural_attachments['a4-neural-osmolyte'] = attachment

    # Reaching the weight gate with exactly the ATP reserve must update
    # quiescence but produce no protein.
    cells[1].pools[a4.a3.POOL_ATP] = 0.042
    # One early gene consumes this tiny mineral budget; later genes see the
    # already depleted material in frozen cache order.
    cells[2].pools[a4.a3.POOL_MINERAL] = 0.36e-6
    # Remove every active translator while retaining its genes.  This returns
    # before quiescence and leaves that observable at its sentinel value.
    translator_keys = [
        fingerprint for fingerprint, spec in cells[3].gene_specs.items()
        if int(spec['role']) == int(a4.a3.ROLE_TRANSLATOR)
    ]
    for fingerprint in translator_keys:
        cells[3].proteins.pop(fingerprint, None)
    cells[3]._sync_protein_pool()
    cells[3]._sync_damage_pool()

    config = a4.GPU068A4Config(
        max_cells=4, max_sequences=16, max_symbols=8192,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    return world, cells, config, 0.1


def _assert_translation_matches_cells(state, cells, label, atol=2e-12):
    if int(state.cell_count) != len(cells):
        raise AssertionError('%s cell count differs' % label)
    for ci, cell in enumerate(cells):
        v3.assert_recursive_close(
            cell.pools, state.pools[ci], atol=atol, rtol=0.0,
            path='%s.cell[%d].pools' % (label, ci),
        )
        v3.assert_recursive_close(
            float(cell.last_translation), float(state.last_translation[ci]),
            atol=atol, rtol=0.0,
            path='%s.cell[%d].last_translation' % (label, ci),
        )
        v3.assert_recursive_close(
            float(cell.last_quiescence), float(state.last_quiescence[ci]),
            atol=atol, rtol=0.0,
            path='%s.cell[%d].last_quiescence' % (label, ci),
        )
        active_count = int(state.active_count[ci])
        damaged_count = int(state.damaged_count[ci])
        active_order = [int(value) for value in
                        state.active_fingerprints[ci, :active_count]]
        damaged_order = [int(value) for value in
                         state.damaged_fingerprints[ci, :damaged_count]]
        if active_order != list(cell.proteins.keys()):
            raise AssertionError('%s cell[%d] active insertion order differs' %
                                 (label, ci))
        if damaged_order != list(cell.damaged_proteins.keys()):
            raise AssertionError('%s cell[%d] damaged insertion order differs' %
                                 (label, ci))
        v3.assert_recursive_close(
            np.asarray(list(cell.proteins.values()), dtype=np.float64),
            state.active_mass[ci, :active_count],
            atol=atol, rtol=0.0,
            path='%s.cell[%d].active' % (label, ci),
        )
        v3.assert_recursive_close(
            np.asarray(list(cell.damaged_proteins.values()), dtype=np.float64),
            state.damaged_mass[ci, :damaged_count],
            atol=atol, rtol=0.0,
            path='%s.cell[%d].damaged' % (label, ci),
        )


def _paid_replication_fixture(seed=7801):
    """Six active Formal066 templates spanning the A4.4a paid gates."""
    world = v3.make_world(seed=seed, cells=6)
    cells = world.cells
    for name, value in (
        ('genome_replication', True),
        ('mutation', False),
        ('proofreading', False),
        ('external_replicase', False),
        ('quiescence', False),
        ('quiescence_effector', False),
    ):
        setattr(world.config, name, value)

    near_one = np.nextafter(np.float64(1.0), np.float64(0.0))
    for ci, cell in enumerate(cells):
        genome = np.asarray(cell.genomes[0], dtype=np.uint8).copy()
        copy_length = 8 + ci
        if len(genome) <= copy_length + 2:
            raise AssertionError('replication fixture is too close to completion')
        cell.genomes = [genome.copy()]
        cell.genome_lesions = [0.025 * (ci + 1)]
        cell.replication_template = genome.copy()
        cell.replication_copy = [int(value) for value in genome[:copy_length]]
        cell.replication_template_lesion = 0.05 * (ci + 1)
        cell.replication_fractional = 0.125 if ci == 1 else near_one
        cell.last_replication_symbols = 71 + ci
        cell.last_effective_error_rate = 0.5 + 0.01 * ci
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
        cell.pools[a4.a3.POOL_ATP] = 0.75
        cell._refresh_gene_cache()
        cell._sync_protein_pool()
        cell._sync_damage_pool()
        if cell.role_activity(a4.g2.ROLE_REPLICASE) <= 1e-6:
            raise AssertionError('replication fixture lacks endogenous replicase')

    atp_gate = float(a4.g2.REPLICATION_ATP_PER_SYMBOL) + 0.022
    monomer_gate = float(a4.g2.MONOMER_MASS)
    cells[2].pools[a4.a3.POOL_ATP] = atp_gate
    cells[3].pools[a4.a3.POOL_ATP] = np.nextafter(atp_gate, 0.0)
    cells[4].pools[a4.a3.POOL_NUCLEOTIDE] = monomer_gate
    cells[5].pools[a4.a3.POOL_NUCLEOTIDE] = np.nextafter(
        monomer_gate, 0.0,
    )
    config = a4.GPU068A4Config(
        max_cells=6, max_sequences=18, max_symbols=16384,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    return world, cells, config, 1e-9


def _replication_model_config(
        base, proofreading=False, external_replicase=False,
        quiescence=False, quiescence_effector=False):
    config = copy.deepcopy(base)
    config.genome_replication = True
    config.mutation = False
    config.proofreading = bool(proofreading)
    config.external_replicase = bool(external_replicase)
    config.quiescence = bool(quiescence)
    config.quiescence_effector = bool(quiescence_effector)
    return config


def _cpu_raw_repair_activity(cell, kind):
    """Literal 0.4 grouping: (mass * efficiency) * promoter."""
    total = 0.0
    for fingerprint, amount in cell.proteins.items():
        spec = cell.gene_specs.get(fingerprint)
        if (
                spec is not None
                and int(spec['role']) == int(a4.a3.ROLE_REGULATOR)
                and int(spec['localisation']) == int(a4.s4.LOC_REPAIR)
                and int(spec['parameter']) % int(a4.a3.REPAIR_COUNT)
                == int(kind)):
            total += (
                float(amount) * float(spec['efficiency'])
            ) * float(spec['promoter'])
    inhibition = 1.0 / (1.0 + 2.6 * cell.aggregate_concentration())
    return float(total / 0.014 * inhibition)


def _paid_replication_b_fixture(seed=7901):
    """A4.4b proof, quiescence, error, and resource-boundary rows."""
    world, cells, config, dt = _paid_replication_fixture(seed=seed)
    world.config = _replication_model_config(
        world.config, proofreading=True, external_replicase=True,
        quiescence=True, quiescence_effector=True,
    )
    cumulative_before = (
        0.375, -0.0, float(2 ** 40), 17.0, 0.125, 2048.5,
    )
    for ci, cell in enumerate(cells):
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
        cell.pools[a4.a3.POOL_ATP] = 0.75
        cell.pools[a4.a3.POOL_REACTIVE] = 0.015 * (ci + 1)
        cell.replication_template_lesion = 0.035 + 0.09 * ci
        cell.behavioural_quiescence = 0.0
        cell.cumulative_proofreading_atp = cumulative_before[ci]

    # Complete-genome damage drives inherited quiescence, while the distinct
    # template lesion above drives replication error telemetry.
    cells[0].genome_lesions = [0.55]
    cells[0].membrane_oxidation[:] = 1.2
    cells[0].current_stress = 0.45
    cells[0].pools[a4.a3.POOL_AGGREGATE] = 0.18
    cells[0].pools[a4.a3.POOL_REACTIVE] = 0.16
    cells[0].behavioural_quiescence = 0.05
    cells[1].behavioural_quiescence = 0.70

    proof = _cpu_raw_repair_activity(
        cells[2], a4.a3.REPAIR_PROOFREADING,
    )
    if proof <= 0.0:
        raise AssertionError('A4.4b fixture lacks proofreading activity')
    proof_fraction = proof / (0.75 + proof)
    atp_per_symbol = (
        float(a4.g2.REPLICATION_ATP_PER_SYMBOL)
        + 0.00075 * proof_fraction
    )
    atp_gate = atp_per_symbol + 0.022
    # Keep the ordinary parity batch outside A4.4b's fp64 comparison guard.
    # Exact/nextafter proof-ATP boundaries are tested separately as explicit
    # CPU-authoritative scope exclusions.
    cells[2].pools[a4.a3.POOL_ATP] = atp_gate + 1e-10
    cells[3].pools[a4.a3.POOL_ATP] = atp_gate - 1e-10
    monomer_gate = float(a4.g2.MONOMER_MASS)
    cells[4].pools[a4.a3.POOL_NUCLEOTIDE] = monomer_gate
    cells[5].pools[a4.a3.POOL_NUCLEOTIDE] = np.nextafter(
        monomer_gate, 0.0,
    )
    return world, cells, config, dt


def _paid_completion_fixture(seed=8301):
    """Two active Formal066 rows completing a previously copied polymer."""
    world, source_cells, _, _ = _paid_replication_b_fixture(seed=seed)
    cells = [copy.deepcopy(source_cells[0]), copy.deepcopy(source_cells[1])]
    near_one = np.nextafter(np.float64(1.0), np.float64(0.0))
    for ci, cell in enumerate(cells):
        template = np.asarray(cell.replication_template, dtype=np.uint8).copy()
        partial = template[:-1].copy()
        # Preserve two substitutions made by an earlier replication call.
        # Completion must append to this paid partial copy, not clone template.
        for position in (5 + ci, len(partial) // 2 + ci):
            position %= len(partial)
            partial[position] = (int(partial[position]) + ci + 1) % a4.ALPHABET_SIZE
        cell.replication_copy = [int(value) for value in partial]
        cell.replication_fractional = near_one
        cell.replication_cycles = 13 + ci
        cell.mutation_events['substitution'] = 4 + ci
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
        cell.pools[a4.a3.POOL_ATP] = 0.75
    config = a4.GPU068A4Config(
        max_cells=2, max_sequences=8, max_symbols=8192,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    return world, cells, config, 1e-9


def _a46b1_completion_mutation_fixture(seed=9911):
    """Three completion rows exposing the complete frozen structural chain."""
    world, source_cells, _, _ = _paid_replication_b_fixture(seed=seed)
    # Exercise NumPy PCG64's cached uint32 path before byte-vector insertion
    # and padding draws.  The tape must preserve the complete state, not only
    # the 128-bit generator state.
    world.rng.integers(0, 7)
    if int(world.rng.bit_generator.state['has_uint32']) != 1:
        raise AssertionError('A4.6b1 fixture did not prime PCG64 uint32 cache')

    config = copy.deepcopy(world.config)
    config.mutation = True
    config.mutation_rate = 0.5
    config.structural_rate = 10.0
    config.variable_length = True
    config.gene_duplication = True
    world.config = config

    root = np.asarray(
        source_cells[0].replication_template, dtype=np.uint8,
    ).copy()
    if (len(root) != 576
            or int(a4.g2.MAX_GENOME_LENGTH) != 640
            or int(a4.g2.MIN_GENOME_LENGTH) != 32):
        raise AssertionError('A4.6b1 frozen genome limits drifted')
    sequences = (
        root.copy(),
        np.concatenate((root, root[:64])).astype(np.uint8),
        root[:8].copy(),
    )
    bases = (source_cells[0], source_cells[1], source_cells[0])
    nucleotide = (
        2.25 * float(a4.g2.MONOMER_MASS), 0.10, 0.10,
    )
    near_one = np.nextafter(np.float64(1.0), np.float64(0.0))
    cells = []
    for ci, (base, sequence) in enumerate(zip(bases, sequences)):
        cell = copy.deepcopy(base)
        cell.cell_id = int(source_cells[ci].cell_id)
        cell.genomes = [sequence.copy()]
        cell.genome_lesions = [0.0]
        cell.replication_template = sequence.copy()
        cell.replication_copy = [int(value) for value in sequence[:-1]]
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = near_one
        cell.pools[a4.a3.POOL_REACTIVE] = 0.0
        cell.pools[a4.a3.POOL_ATP] = 0.75
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = nucleotide[ci]
        cell._refresh_gene_cache()
        cell._sync_protein_pool()
        cells.append(cell)
    capacity = a4.GPU068A4Config(
        max_cells=4, max_sequences=12, max_symbols=16384,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    return world, cells, capacity, 1e-9


def _a46b1_replay_tape_row(tape, before, ci):
    """Independent fixed-order host application used only by validation."""
    alphabet = int(a4.g2.ALPHABET_SIZE)
    candidate = np.asarray(before.replication_copy, dtype=np.uint8).copy()
    template = np.asarray(before.replication_template, dtype=np.uint8)
    append_count = int(tape.append_count[ci])
    for rank in range(append_count):
        symbol = int(template[len(candidate)])
        if bool(tape.replacement_mask[ci, rank]):
            raw = int(tape.replacement_raw[ci, rank])
            symbol = (raw + (1 if raw >= symbol else 0)) % alphabet
        candidate = np.concatenate((
            candidate, np.asarray([symbol], dtype=np.uint8),
        ))
    if len(candidate) != int(tape.pre_structural_lengths[ci]):
        raise AssertionError('A4.6b1 replay pre-structural length differs')

    count = int(tape.insertion_count[ci])
    if count:
        position = int(tape.insertion_position[ci])
        inserted = np.asarray(
            tape.insertion_symbols[ci, :count], dtype=np.uint8,
        )
        candidate = np.concatenate((
            candidate[:position], inserted, candidate[position:],
        ))

    count = int(tape.deletion_count[ci])
    if count:
        position = int(tape.deletion_position[ci])
        candidate = np.concatenate((
            candidate[:position], candidate[position + count:],
        ))

    ordinal = int(tape.duplication_gene_ordinal[ci])
    if ordinal >= 0:
        genes = a4.g2.parse_genes(candidate)
        if ordinal >= len(genes):
            raise AssertionError('A4.6b1 replay duplication ordinal differs')
        start = int(genes[ordinal]['start'])
        if start != int(tape.duplication_source_start[ci]):
            raise AssertionError('A4.6b1 replay duplication start differs')
        fragment = candidate[start:start + int(a4.g2.GENE_SPAN)].copy()
        position = int(tape.duplication_position[ci])
        candidate = np.concatenate((
            candidate[:position], fragment, candidate[position:],
        ))

    left = int(tape.inversion_left[ci])
    right = int(tape.inversion_right[ci])
    if left >= 0:
        candidate[left:right] = candidate[left:right][::-1]

    count = int(tape.transposition_count[ci])
    if count:
        start = int(tape.transposition_start[ci])
        fragment = candidate[start:start + count].copy()
        remainder = np.concatenate((
            candidate[:start], candidate[start + count:],
        ))
        position = int(tape.transposition_position[ci])
        candidate = np.concatenate((
            remainder[:position], fragment, remainder[position:],
        ))

    count = int(tape.padding_count[ci])
    if count:
        candidate = np.concatenate((
            candidate,
            np.asarray(tape.padding_symbols[ci, :count], dtype=np.uint8),
        ))
    if len(candidate) > int(a4.g2.MAX_GENOME_LENGTH):
        candidate = candidate[:int(a4.g2.MAX_GENOME_LENGTH)]
    final_length = int(tape.post_structural_lengths[ci])
    if final_length > len(candidate):
        raise AssertionError('A4.6b1 replay final length exceeds candidate')
    candidate = candidate[:final_length]
    if (len(candidate) - int(tape.pre_structural_lengths[ci])
            != int(tape.material_delta_symbols[ci])):
        raise AssertionError('A4.6b1 replay material delta differs')
    return candidate.astype(np.uint8, copy=False)


def _a47_gene_sequence(role, start, length=48):
    """One isolated frozen gene whose selected deletion breaks decoding."""
    sequence = np.ones((int(length),), dtype=np.uint8)
    gene = a4.g2.make_gene(
        int(role), parameter=int(role), promoter=5, efficiency=4,
    )
    start = int(start)
    if start < 0 or start + len(gene) > len(sequence):
        raise AssertionError('A4.7a fixture gene does not fit its polymer')
    sequence[start:start + len(gene)] = gene
    return sequence


def _a47_hydrolysis_fixture(boundaries=False):
    """One Formal066 post-gain event with a primed PCG64 uint32 cache."""
    world = v3.make_world(seed=9971 if boundaries else 9972, cells=1)
    cell = world.cells[0]
    world.config.endogenous_damage = False
    minimum = int(a4.g2.MIN_GENOME_LENGTH)
    if boundaries:
        cell.genomes = [
            _a47_gene_sequence(a4.g2.ROLE_ENERGY, 0),
            _a47_gene_sequence(
                a4.g2.ROLE_MEMBRANE, 0, length=minimum,
            ),
            _a47_gene_sequence(a4.g2.ROLE_TRANSPORTER, 16),
        ]
        cell.genome_lesions = [0.75, 1.0, 1.0]
        dt = 0.0
    else:
        cell.genomes = [
            _a47_gene_sequence(a4.g2.ROLE_ENERGY, 0),
            _a47_gene_sequence(a4.g2.ROLE_MEMBRANE, 0),
            _a47_gene_sequence(a4.g2.ROLE_TRANSPORTER, 16),
        ]
        lesion = 0.5 / 0.00065
        if (0.00065 * lesion != 0.5
                or len(cell.genomes[0]) != 48):
            raise AssertionError('A4.7a probability fixture drifted')
        cell.genome_lesions = [lesion, lesion, lesion]
        dt = 1.0
    cell.replication_template = None
    cell.replication_copy = []
    cell.replication_template_lesion = 0.0
    cell.replication_fractional = 0.0
    cell.membrane_oxidation[:] = 0.0
    cell.pools[a4.a3.POOL_WASTE] = 0.004
    cell._refresh_gene_cache()
    cell._sync_protein_pool()

    # This one uint8 call leaves a cached uint32.  The hit/miss/hit fixture
    # then consumes that cache in the literal random()/integers() interleave.
    world.rng = np.random.default_rng(2)
    priming_value = int(world.rng.integers(
        0, 8, size=1, dtype=np.uint8,
    )[0])
    state = world.rng.bit_generator.state
    if (priming_value != 6 or int(state['has_uint32']) != 1
            or int(state['uinteger']) != 1123615560):
        raise AssertionError('A4.7a primed PCG64 fixture drifted')

    symbol_count = sum(len(genome) for genome in cell.genomes)
    capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=5, max_symbols=symbol_count,
        max_sequence_symbols=48, max_proteins_per_cell=64,
    )
    return world, cell, capacity, dt


def _a47_manual_apply_tape(cell, tape):
    """Apply only recorded deletion effects on a disposable CPU oracle copy."""
    out = copy.deepcopy(cell)
    for genome_index in range(len(out.genomes)):
        if not bool(tape.hit_mask[genome_index]):
            continue
        sequence = np.asarray(out.genomes[genome_index], dtype=np.uint8)
        position = int(tape.deletion_positions[genome_index])
        if position < 0 or position >= len(sequence):
            raise AssertionError('A4.7a tape deletion position is invalid')
        out.genomes[genome_index] = np.concatenate((
            sequence[:position], sequence[position + 1:],
        ))
        out.pools[a4.a3.POOL_WASTE] += float(a4.g2.MONOMER_MASS)
        out.genome_lesions[genome_index] *= 0.80
        out.genome_damage_events += 1
        out._refresh_gene_cache()
    return out


def _a47b_hydrolysis_fixture(zero_hit=False):
    """A4.7b one-cell source with non-candidate active intermediates."""
    world, cell, _, dt = _a47_hydrolysis_fixture(
        boundaries=bool(zero_hit),
    )
    template = _a47_gene_sequence(a4.g2.ROLE_REPLICASE, 0)
    copy_symbols = template[:7].copy()
    cell.replication_template = template.copy()
    cell.replication_copy = [int(value) for value in copy_symbols]
    cell.replication_template_lesion = 0.125
    cell.replication_fractional = 0.375
    cell._refresh_gene_cache()
    cell._sync_protein_pool()

    sequences = list(cell.genomes) + [template, copy_symbols]
    symbol_count = sum(len(sequence) for sequence in sequences)
    capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=len(sequences),
        max_symbols=symbol_count,
        max_sequence_symbols=max(len(sequence) for sequence in sequences),
        max_proteins_per_cell=64,
    )
    if (not zero_hit
            and (len(sequences) != 5 or symbol_count != 199
                 or sum(len(genome) for genome in cell.genomes)
                 + len(copy_symbols) != 151)):
        raise AssertionError('A4.7b exact-capacity fixture drifted')
    return world, cell, capacity, dt


def _a47b_formal066_oracle(world, cell, dt):
    """Return direct frozen-CPU hydrolysis state and descriptor expectations."""
    before = copy.deepcopy(cell)
    cpu_world = copy.deepcopy(world)
    actual = copy.deepcopy(cell)
    cpu_world.cells = [actual]
    actual._decay_information_and_proteins(cpu_world, dt)

    sequences = [
        np.asarray(genome, dtype=np.uint8).copy()
        for genome in actual.genomes
    ]
    if actual.replication_template is not None:
        sequences.extend((
            np.asarray(actual.replication_template, dtype=np.uint8).copy(),
            np.asarray(actual.replication_copy, dtype=np.uint8).copy(),
        ))
    width = max([len(sequence) for sequence in sequences] or [0])
    fixed = np.zeros((len(sequences), width), dtype=np.uint8)
    lengths = np.asarray(
        [len(sequence) for sequence in sequences], dtype=np.int64,
    )
    for index, sequence in enumerate(sequences):
        fixed[index, :len(sequence)] = sequence
    offsets = np.zeros((len(sequences) + 1,), dtype=np.int64)
    if len(sequences):
        offsets[1:] = np.cumsum(lengths, dtype=np.int64)

    lesion_after = np.asarray(actual.genome_lesions, dtype=np.float64)
    event_delta = (
        int(actual.genome_damage_events)
        - int(before.genome_damage_events)
    )
    source_sequences = list(before.genomes)
    if before.replication_template is not None:
        source_sequences.extend((
            before.replication_template, before.replication_copy,
        ))
    source_symbol_count = sum(len(sequence) for sequence in source_sequences)
    material_after = (
        sum(len(genome) for genome in actual.genomes)
        + len(actual.replication_copy)
    )
    return cpu_world, actual, {
        'final_symbols': fixed,
        'final_lengths': lengths,
        'final_offsets': offsets,
        'final_symbol_count': int(offsets[-1]),
        'pools_after': np.asarray(actual.pools, dtype=np.float64).copy(),
        'genome_lesions_after': lesion_after.copy(),
        'genome_lesion_mean_after': (
            float(np.mean(lesion_after)) if len(lesion_after) else 1.0
        ),
        'genome_damage_event_delta': event_delta,
        'topology_sequence_delta': 0,
        'topology_symbol_delta': int(offsets[-1]) - source_symbol_count,
        'genome_material_symbols_after': material_after,
        'cache_dirty': bool(event_delta),
        'gene_specs_after': copy.deepcopy(actual.gene_specs),
        'rng_after_state': copy.deepcopy(cpu_world.rng.bit_generator.state),
    }


def _a48_binding(world, cell, config):
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells([cell])
    state = a4.pack_a4_translation_state(
        [cell], ragged, world.config, config,
    )
    return a4.bind_a4_translation(ragged, state)


def _a48_begin_direct(scheduler, world, cell, dt):
    """Enter exactly the post-lesion-gain A3 event boundary."""
    scheduler.begin_step(world, [cell])
    metabolism_rank = a3s.WORLD_EVENT_ORDER.index('cell_metabolism_loop')
    for event in a3s.WORLD_EVENT_ORDER[:metabolism_rank]:
        scheduler.claim_world(
            event, status='skipped', metadata={'validation': 'A4.8a'},
        )
    scheduler.claim(
        cell, 'surface_exchange', status='skipped',
        metadata={'validation': 'A4.8a'},
    )
    scheduler.reserve_metabolism_dispatch(world, cell, dt)
    if not scheduler.begin_a3_metabolism(world, cell, dt):
        raise AssertionError('A4.8a direct fixture cell is not alive')
    stop = a3s.CELL_EVENT_ORDER.index('genome_hydrolysis_cpu_rng')
    for event in a3s.CELL_EVENT_ORDER[1:stop]:
        scheduler.claim(
            cell, event, status='skipped',
            metadata={'validation': 'A4.8a'},
        )
    return scheduler


def _a48_hydrolysis_entry(scheduler, cell):
    _, record = scheduler._record(cell)
    entries = [entry for entry in record['events']
               if entry['event'] == 'genome_hydrolysis_cpu_rng']
    if len(entries) != 1:
        raise AssertionError(
            'A4.8a hydrolysis event count is %d' % len(entries)
        )
    return copy.deepcopy(entries[0])


def _a48_abort_direct(scheduler, cell, error=None):
    cell._defer_damage_viability = False
    if scheduler.active:
        return scheduler.abort_step(
            RuntimeError('A4.8a direct validation close')
            if error is None else error
        )
    return None


def _a48_assert_committed_oracle(world, cell, expected_world,
                                 expected_cell, label):
    _assert_structural_equal(expected_cell, cell, label + '.structure')
    v3.assert_recursive_close(
        expected_cell.pools, cell.pools, atol=0.0, rtol=0.0,
        path=label + '.pools',
    )
    if (int(expected_cell.genome_damage_events)
            != int(cell.genome_damage_events)):
        raise AssertionError(label + ' damage-event counter differs')
    if expected_world.rng.bit_generator.state != world.rng.bit_generator.state:
        raise AssertionError(label + ' full PCG64 state differs')


def _a48_assert_plan_equal(expected, actual, label):
    left = expected.state_dict()
    right = actual.state_dict()
    lesion_mean = 'genome_lesion_mean_after'
    left_mean = np.asarray(left.pop(lesion_mean), dtype=np.float64)
    right_mean = np.asarray(right.pop(lesion_mean), dtype=np.float64)
    v3.assert_recursive_close(
        left, right, atol=0.0, rtol=0.0, path=label,
    )
    left_bits = left_mean.view(np.uint64).reshape(-1)
    right_bits = right_mean.view(np.uint64).reshape(-1)
    if (left_mean.shape != right_mean.shape
            or not all(abs(int(a) - int(b)) <= 1
                       for a, b in zip(left_bits, right_bits))):
        raise AssertionError(label + ' lesion mean differs by more than 1 ULP')


def _a48_hybrid_from_state(state, a4_config=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48.Hybrid066WorldA4Hydrolysis(
        world, backend=backend,
        a4_config=a4_config or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48b_begin_translation_direct(scheduler, world, cell, dt):
    """Enter exactly the post-maintenance translation event boundary."""
    scheduler.begin_step(world, [cell])
    metabolism_rank = a3s.WORLD_EVENT_ORDER.index('cell_metabolism_loop')
    for event in a3s.WORLD_EVENT_ORDER[:metabolism_rank]:
        scheduler.claim_world(
            event, status='skipped', metadata={'validation': 'A4.8b'},
        )
    scheduler.claim(
        cell, 'surface_exchange', status='skipped',
        metadata={'validation': 'A4.8b'},
    )
    scheduler.reserve_metabolism_dispatch(world, cell, dt)
    if not scheduler.begin_a3_metabolism(world, cell, dt):
        raise AssertionError('A4.8b direct fixture cell is not alive')
    stop = a3s.CELL_EVENT_ORDER.index('translation_cpu')
    for event in a3s.CELL_EVENT_ORDER[1:stop]:
        scheduler.claim(
            cell, event, status='skipped',
            metadata={'validation': 'A4.8b'},
        )
    return scheduler


def _a48b_translation_entry(scheduler, cell):
    _, record = scheduler._record(cell)
    entries = [entry for entry in record['events']
               if entry['event'] == 'translation_cpu']
    if len(entries) != 1:
        raise AssertionError(
            'A4.8b translation event count is %d' % len(entries)
        )
    return copy.deepcopy(entries[0])


def _a48b_assert_cell_matches(expected, actual, label, atol=2e-12):
    if list(expected.proteins.keys()) != list(actual.proteins.keys()):
        raise AssertionError(label + ' active protein order differs')
    if (list(expected.damaged_proteins.keys())
            != list(actual.damaged_proteins.keys())):
        raise AssertionError(label + ' damaged protein order differs')
    v3.assert_recursive_close(
        expected.state_dict(), actual.state_dict(),
        atol=atol, rtol=0.0, path=label,
    )


def _a48b_hybrid_from_state(state, a4_config=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48b.Hybrid066WorldA4Translation(
        world, backend=backend,
        a4_config=a4_config or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48b_replication_boundary_cases(seed=9971):
    """Post-translation CPU-replication branch controls with real RNG work."""
    cases = []
    world, cells, _, dt = _paid_replication_fixture(seed=seed)
    world.config.mutation = True
    world.config.mutation_rate = 1.0
    for label, index, copied, rng_changed in (
            ('atp_exact', 2, 1, True),
            ('atp_nextbelow', 3, 0, False),
            ('nucleotide_exact', 4, 1, True),
            ('nucleotide_nextbelow', 5, 0, False)):
        cases.append((
            label, v3.pickle_clone(world.state_dict()),
            int(cells[index].cell_id), dt, copied, rng_changed,
        ))

    gate_world, gate_cells, _, gate_dt = _paid_replication_fixture(
        seed=seed + 1,
    )
    gate_world.config.gene_expression = False
    gate_world.config.mutation = True
    gate_world.config.mutation_rate = 1.0
    gate = gate_cells[0]
    gate.genome_lesions = [0.0 for _ in gate.genomes]
    gate.damaged_proteins = {}
    gate.pools[a4.a3.POOL_DAMAGED_PROTEIN] = 0.0
    gate.pools[a4.a3.POOL_AGGREGATE] = 0.0
    replicases = [
        (fingerprint, spec)
        for fingerprint, spec in gate.gene_specs.items()
        if int(spec['role']) == int(a4.a3.ROLE_REPLICASE)
    ]
    if len(replicases) != 1:
        raise AssertionError('replicase boundary fixture is not singular')
    fingerprint, spec = replicases[0]
    gate_mass = 1e-6 * 0.040 / float(spec['efficiency'])
    for label, mass, copied, rng_changed in (
            ('replicase_below', np.nextafter(gate_mass, 0.0), 0, False),
            ('replicase_above', gate_mass, 1, True)):
        candidate_world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(gate_world.state_dict()),
        )
        candidate = candidate_world.cells[0]
        candidate.proteins = {fingerprint: float(mass)}
        candidate._sync_protein_pool()
        activity = candidate.role_activity(a4.a3.ROLE_REPLICASE)
        if ((label.endswith('below') and not activity <= 1e-6)
                or (label.endswith('above') and not activity > 1e-6)):
            raise AssertionError(label + ' did not straddle replicase gate')
        cases.append((
            label, v3.pickle_clone(candidate_world.state_dict()),
            int(candidate.cell_id), gate_dt, copied, rng_changed,
        ))
    return cases


def _assert_completion_cpu_parity(plan, before_cells, cpu_cells, label,
                                  atol=2e-12):
    if int(plan.cell_count) != len(before_cells):
        raise AssertionError('%s cell count differs' % label)
    for ci, (before, actual) in enumerate(zip(before_cells, cpu_cells)):
        if not bool(plan.completion_events[ci]):
            raise AssertionError('%s cell[%d] did not complete' % (label, ci))
        length = int(plan.completed_lengths[ci])
        completed = np.asarray(
            plan.completed_symbols[ci, :length], dtype=np.uint8,
        )
        append_count = int(plan.append_count[ci])
        appended = np.asarray(
            plan.append_symbols[ci, :append_count], dtype=np.uint8,
        )
        partial = np.asarray(before.replication_copy, dtype=np.uint8)
        if (len(actual.genomes) != len(before.genomes) + 1
                or not np.array_equal(completed, actual.genomes[-1])
                or not np.array_equal(completed[:len(partial)], partial)
                or not np.array_equal(completed[len(partial):], appended)):
            raise AssertionError(
                '%s cell[%d] completed payload differs' % (label, ci)
            )
        if (actual.replication_template is not None
                or actual.replication_copy
                or float(actual.replication_template_lesion) != 0.0
                or float(actual.replication_fractional) != 0.0):
            raise AssertionError(
                '%s cell[%d] frozen completion reset differs' % (label, ci)
            )
        if (int(plan.replication_cycle_deltas[ci]) != 1
                or int(actual.replication_cycles)
                != int(before.replication_cycles) + 1
                or int(plan.topology_sequence_deltas[ci]) != -1
                or int(plan.topology_symbol_deltas[ci])
                != append_count - length):
            raise AssertionError(
                '%s cell[%d] completion delta differs' % (label, ci)
            )
        if before.mutation_events != actual.mutation_events:
            raise AssertionError(
                '%s cell[%d] mutation counters changed' % (label, ci)
            )
        v3.assert_recursive_close(
            np.asarray(plan.pools_after[ci]), actual.pools,
            atol=atol, rtol=0.0, path='%s.cell[%d].pools' % (label, ci),
        )
        v3.assert_recursive_close(
            float(plan.new_genome_lesions[ci]),
            float(actual.genome_lesions[-1]),
            atol=atol, rtol=0.0,
            path='%s.cell[%d].new_lesion' % (label, ci),
        )
        v3.assert_recursive_close(
            float(plan.last_effective_error_rate[ci]),
            float(actual.last_effective_error_rate),
            atol=atol, rtol=0.0,
            path='%s.cell[%d].effective_error' % (label, ci),
        )
        v3.assert_recursive_close(
            float(plan.cumulative_proofreading_atp_after[ci]),
            float(actual.cumulative_proofreading_atp),
            atol=atol, rtol=0.0,
            path='%s.cell[%d].proofreading_atp' % (label, ci),
        )
        if (int(plan.last_replication_symbols[ci])
                != int(actual.last_replication_symbols)):
            raise AssertionError(
                '%s cell[%d] last copied count differs' % (label, ci)
            )


def _external_only_replication_cell(cell):
    out = copy.deepcopy(cell)
    keys = [
        fingerprint for fingerprint, spec in out.gene_specs.items()
        if int(spec['role']) == int(a4.a3.ROLE_REPLICASE)
    ]
    for fingerprint in keys:
        out.proteins.pop(fingerprint, None)
    out._sync_protein_pool()
    if out.role_activity(a4.a3.ROLE_REPLICASE) > 1e-6:
        raise AssertionError('external-only fixture retains replicase')
    return out


def _paid_replication_binding(cells, model_config, config):
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells)
    state = a4.pack_a4_translation_state(
        cells, ragged, model_config, config,
    )
    return ragged, state, a4.bind_a4_translation(ragged, state)


def _assert_paid_replication_cpu_parity(plan, before_cells, cpu_cells, label):
    if int(plan.cell_count) != len(before_cells):
        raise AssertionError('%s cell count differs' % label)
    for ci, (before, actual) in enumerate(zip(before_cells, cpu_cells)):
        expected = copy.deepcopy(before)
        if bool(plan.template_start_events[ci]):
            selected = int(plan.selected_template_indices[ci])
            if (before.replication_template is not None or selected != 0
                    or len(before.genomes) != 1):
                raise AssertionError(
                    '%s cell[%d] template-start metadata differs' % (
                        label, ci,
                    )
                )
            expected.replication_template = np.asarray(
                before.genomes[selected], dtype=np.uint8,
            ).copy()
            expected.replication_template_lesion = (
                float(before.genome_lesions[selected])
                if selected < len(before.genome_lesions) else 0.0
            )
            expected.replication_copy = []
            expected.replication_fractional = 0.0
            if (int(plan.template_storage_symbols[ci])
                    != len(expected.replication_template)):
                raise AssertionError(
                    '%s cell[%d] template storage differs' % (label, ci)
                )
        count = int(plan.append_count[ci])
        appended = [int(value) for value in plan.append_symbols[ci, :count]]
        expected.replication_copy.extend(appended)
        expected.pools[:] = np.asarray(plan.pools_after[ci], dtype=np.float64)
        expected.replication_fractional = float(
            plan.replication_fractional_after[ci]
        )
        expected.last_replication_symbols = int(
            plan.last_replication_symbols[ci]
        )
        expected.last_effective_error_rate = float(
            plan.last_effective_error_rate[ci]
        )
        expected.cumulative_proofreading_atp = float(
            plan.cumulative_proofreading_atp_after[ci]
        )
        expected.mutation_events['substitution'] += int(
            plan.substitution_events[ci]
        )
        if actual.replication_copy[len(before.replication_copy):] != appended:
            raise AssertionError('%s cell[%d] copied suffix differs' % (label, ci))
        v3.assert_recursive_close(
            expected.state_dict(), actual.state_dict(),
            atol=2e-12, rtol=0.0,
            path='%s.cell[%d]' % (label, ci),
        )


def _assert_replication_failure_atomic(kind, binding, fn, label):
    ragged_before = binding.ragged.state_dict()
    state_before = binding.state.state_dict()
    cache_before = binding.cache.state_dict()
    _assert_raises(kind, fn)
    v3.assert_recursive_close(
        ragged_before, binding.ragged.state_dict(), atol=0.0, rtol=0.0,
        path='%s.ragged' % label,
    )
    v3.assert_recursive_close(
        state_before, binding.state.state_dict(), atol=0.0, rtol=0.0,
        path='%s.state' % label,
    )
    v3.assert_recursive_close(
        cache_before, binding.cache.state_dict(), atol=0.0, rtol=0.0,
        path='%s.cache' % label,
    )


def test_api_scope():
    required = (
        'GPU068A4Config', 'A4RaggedGenomeBatch',
        'FullFidelityA4GenomeAdapter', 'validate_a4_ragged',
        'pack_a4_cells', 'unpack_a4_cells', 'A4GeneCacheBatch',
        'validate_a4_gene_cache', 'decode_a4_gene_cache_numpy',
        'decode_a4_gene_cache_torch', 'decode_a4_gene_cache',
        'A4TranslationStateBatch', 'FullFidelityA4TranslationAdapter',
        'pack_a4_translation_state', 'bind_a4_translation',
        'paid_translation_plan_numpy', 'paid_translation_plan_torch',
        'paid_translation_plan', 'validate_a4_translation_state',
    )
    missing = [name for name in required if not hasattr(a4, name)]
    if missing:
        raise AssertionError('missing A4 API: %s' % missing)
    replication_required = (
        'A4ReplicationError', 'A4ReplicationScopeError',
        'A4PaidElongationPlan', 'validate_a4_paid_elongation_plan',
        'paid_replication_elongation_numpy',
        'paid_replication_elongation_torch',
        'paid_replication_elongation_plan',
        'paid_replication_completion_plan',
        'A4SubstitutionRngTape', 'validate_a4_substitution_rng_tape',
        'prepare_substitution_rng_tape',
        'paid_replication_substitution_numpy',
        'paid_replication_substitution_torch',
        'paid_replication_substitution_plan',
        'A4CompletionMutationRngTape',
        'prepare_completion_mutation_rng_tape',
        'validate_a4_completion_mutation_rng_tape',
        'A4CompletionMutationPlan',
        'validate_a4_completion_mutation_plan',
        'paid_replication_completion_mutation_numpy',
        'paid_replication_completion_mutation_torch',
        'paid_replication_completion_mutation_plan',
    )
    missing = [name for name in replication_required if not hasattr(a44, name)]
    if missing:
        raise AssertionError('missing A4.6b2 API: %s' % missing)
    hydrolysis_required = (
        'A4HydrolysisError', 'A4HydrolysisScopeError',
        'A4HydrolysisRngTape', 'prepare_genome_hydrolysis_rng_tape',
        'validate_a4_hydrolysis_rng_tape',
        'A4HydrolysisDeletionPlan',
        'validate_a4_hydrolysis_deletion_plan',
        'genome_hydrolysis_deletion_numpy',
        'genome_hydrolysis_deletion_torch',
        'genome_hydrolysis_deletion_plan',
    )
    missing = [name for name in hydrolysis_required if not hasattr(a47, name)]
    if missing:
        raise AssertionError('missing A4.7b API: %s' % missing)
    integration_required = (
        'A4HydrolysisCommitError', 'A4HydrolysisEventScheduler',
        'Hybrid066WorldA4Hydrolysis',
    )
    missing = [name for name in integration_required
               if not hasattr(a48, name)]
    if missing:
        raise AssertionError('missing A4.8a API: %s' % missing)
    translation_integration_required = (
        'A4TranslationCommitError', 'A4TranslationEventScheduler',
        'Hybrid066WorldA4Translation',
    )
    missing = [name for name in translation_integration_required
               if not hasattr(a48b, name)]
    if missing:
        raise AssertionError('missing A4.8b API: %s' % missing)
    replication_integration_required = (
        'A4ReplicationCommitError', 'A4ReplicationEventScheduler',
        'Hybrid066WorldA4Replication',
    )
    missing = [name for name in replication_integration_required
               if not hasattr(a48c, name)]
    if missing:
        raise AssertionError('missing A4.8c1 API: %s' % missing)
    completion_integration_required = (
        'A4ReplicationCompletionCommitError',
        'A4ReplicationCompletionEventScheduler',
        'Hybrid066WorldA4ReplicationCompletion',
    )
    missing = [name for name in completion_integration_required
               if not hasattr(a48c2, name)]
    if missing:
        raise AssertionError('missing A4.8c2 API: %s' % missing)
    if a4.FULL_GPU_WORLD_STEP is not False:
        raise AssertionError('A4.1 must not claim full GPU world-step')
    if hasattr(a4.A4GeneCacheBatch, 'from_state_dict'):
        raise AssertionError('derived cache must not expose a deserialize authority')
    if a4.PORT_STATUS.get('gene_cache_decode') != (
            'a4.2-batched-resident-derived-cache'):
        raise AssertionError('A4.2 gene-cache status is missing')
    if a4.PORT_STATUS.get('translation') != (
            'a4.3-full-formal066-paid-plan-not-integrated-cpu-authoritative'):
        raise AssertionError('A4.3 translation plan status is missing')
    if a4.PORT_STATUS.get('genome_replication') != 'cpu-authoritative-next-a4-slice':
        raise AssertionError('replication authority changed in representation slice')
    if a44.BUILD != 'SOMA-CELL 0.6.8-GPU A4.6b2':
        raise AssertionError('A4.6b2 build identity differs')
    if a44.SCHEMA_VERSION != (
            '0.6.8-GPU-A4.6a-replication-completion-plan'):
        raise AssertionError('A4.6a schema identity differs')
    if a44.RNG_TAPE_SCHEMA_VERSION != (
            '0.6.8-GPU-A4.5b-template-start-rng-tape'):
        raise AssertionError('A4.5b RNG tape schema identity differs')
    if a44.COMPLETION_MUTATION_RNG_TAPE_SCHEMA_VERSION != (
            '0.6.8-GPU-A4.6b1-completion-mutation-rng-tape'):
        raise AssertionError('A4.6b1 RNG tape schema identity differs')
    if a44.COMPLETION_MUTATION_PLAN_SCHEMA_VERSION != (
            '0.6.8-GPU-A4.6b2-completion-mutation-plan'):
        raise AssertionError('A4.6b2 completion plan schema differs')
    if tuple(a44.STRUCTURAL_EVENT_NAMES) != (
            'insertion', 'deletion', 'duplication',
            'inversion', 'transposition'):
        raise AssertionError('A4.6b1 structural event order differs')
    tape_fields = set(a44.A4CompletionMutationRngTape.__dataclass_fields__)
    forbidden_payload = {
        'completed_symbols', 'final_symbols', 'mutated_symbols',
        'completed_genome', 'final_genome',
    }
    if (tape_fields & forbidden_payload
            or len(a44._COMPLETION_TAPE_ARRAY_FIELDS) != 32
            or len(set(a44._COMPLETION_TAPE_ARRAY_FIELDS)) != 32):
        raise AssertionError(
            'A4.6b1 tape exposed a final genome or duplicate field'
        )
    plan_fields = tuple(a44._COMPLETION_PLAN_ARRAY_FIELDS)
    if (len(plan_fields) != len(set(plan_fields))
            or 'final_symbols' not in plan_fields
            or 'genome_material_symbols_after' not in plan_fields):
        raise AssertionError('A4.6b2 completion plan field set differs')
    if a44.FULL_GPU_WORLD_STEP is not False:
        raise AssertionError('A4.6b2 must not claim full GPU world-step')
    if (a47.BUILD != 'SOMA-CELL 0.6.8-GPU A4.7b'
            or a47.SCHEMA_VERSION
            != '0.6.8-GPU-A4.7a-genome-hydrolysis-rng-tape'
            or a47.DELETION_PLAN_SCHEMA_VERSION
            != '0.6.8-GPU-A4.7b-genome-hydrolysis-deletion-plan'
            or a47.FULL_GPU_WORLD_STEP is not False):
        raise AssertionError('A4.7b identity/authority differs')
    hydrolysis_fields = set(
        a47.A4HydrolysisRngTape.__dataclass_fields__
    )
    if (set(a47._TAPE_ARRAY_FIELDS) != {
            'genome_slot_mask', 'draw_mask', 'uniform_draws',
            'hit_mask', 'deletion_positions'}
            or hydrolysis_fields & {
                'final_symbols', 'completed_symbols', 'pools_after',
                'genome_lesions_after'}):
        raise AssertionError('A4.7a tape exposed application state')
    if tuple(a47._DELETION_PLAN_ARRAY_FIELDS) != (
            'scope_valid', 'scope_error_code', 'final_symbols',
            'final_lengths', 'genome_lesions_after', 'pools_after',
            'symbol_count_after', 'topology_symbol_delta',
            'genome_damage_event_delta', 'gene_cache_dirty',
            'gene_cache_refresh_count', 'genome_material_symbols_after',
            'genome_lesion_mean_after'):
        raise AssertionError('A4.7b deletion-plan field order differs')
    if (a47.DELETION_SCOPE_OK != 0
            or a47.DELETION_SCOPE_TAPE_MISMATCH != 1):
        raise AssertionError('A4.7b deletion scope codes differ')
    if (a44.SCOPE_FP64_DISCRETE_BOUNDARY != 6
            or a44.FP64_DISCRETE_GUARD_EPS != 4096.0):
        raise AssertionError('A4.5b fp64 discrete guard contract differs')
    if a44.SCOPE_RNG_TAPE_MISMATCH != 7:
        raise AssertionError('A4.5b RNG tape scope code differs')
    if a44.SCOPE_NONCOMPLETION != 8:
        raise AssertionError('A4.6a noncompletion scope code differs')
    expected_replication = (
        'a4.6b2-pre-existing-active-all-row-completion-combined-pcg64-'
        'substitution-structural-material-fixed-resident-plan-'
        'not-arena-committed-not-integrated-cpu-authoritative'
    )
    expected_material = (
        'a4.6b2-binding-aware-attested-tape-applied-to-pure-resident-'
        'descriptor-not-live-rng-authority-cpu-authoritative'
    )
    if (a44.PORT_STATUS.get('genome_replication') != expected_replication
            or a44.PORT_STATUS.get('material_mutation')
            != expected_material):
        raise AssertionError('A4.6b2 authority status differs')
    expected_hydrolysis = (
        'a4.7b-single-cell-row-padded-deletion-ledger-plan-'
        'not-arena-cache-live-rng-committed-not-integrated-'
        'cpu-authoritative'
    )
    if a47.PORT_STATUS.get('genome_symbol_hydrolysis') != expected_hydrolysis:
        raise AssertionError('A4.7b hydrolysis authority status differs')
    public_names = set(a48.__all__)
    if (a48.BUILD != 'SOMA-CELL 0.6.8-GPU A4.8a'
            or a48.SCHEMA_VERSION
            != '0.6.8-GPU-A4.8a-hydrolysis-atomic-commit'
            or a48.FULL_GPU_WORLD_STEP is not False
            or '_A4HydrolysisCommitCandidate' in public_names):
        raise AssertionError('A4.8a identity/public scope differs')
    signature = inspect.signature(
        a48.A4HydrolysisEventScheduler.cpu_genome_hydrolysis,
    )
    if tuple(signature.parameters) != ('self', 'world', 'cell', 'hazards', 'dt'):
        raise AssertionError('A4.8a bridge accepts external plan authority')
    if not issubclass(
            a48.A4HydrolysisEventScheduler, a3s.A3EventScheduler):
        raise AssertionError('A4.8a scheduler is not an A3 scheduler subtype')
    translation_public_names = set(a48b.__all__)
    if (a48b.BUILD != 'SOMA-CELL 0.6.8-GPU A4.8b'
            or a48b.SCHEMA_VERSION
            != '0.6.8-GPU-A4.8b-translation-atomic-commit'
            or a48b.FULL_GPU_WORLD_STEP is not False
            or '_A4TranslationCommitCandidate' in translation_public_names):
        raise AssertionError('A4.8b identity/public scope differs')
    translation_signature = inspect.signature(
        a48b.A4TranslationEventScheduler.cpu_translation,
    )
    if tuple(translation_signature.parameters) != (
            'self', 'world', 'cell', 'dt', 'config'):
        raise AssertionError('A4.8b bridge accepts external plan authority')
    if (not issubclass(
            a48b.A4TranslationEventScheduler,
            a48.A4HydrolysisEventScheduler)
            or not issubclass(
                a48b.Hybrid066WorldA4Translation,
                a48.Hybrid066WorldA4Hydrolysis)):
        raise AssertionError('A4.8b wrapper did not preserve A4.8a authority')
    replication_public_names = set(a48c.__all__)
    if (a48c.BUILD != 'SOMA-CELL 0.6.8-GPU A4.8c1'
            or a48c.SCHEMA_VERSION
            != ('0.6.8-GPU-A4.8c1-active-noncompletion-'
                'replication-atomic-commit')
            or a48c.FULL_GPU_WORLD_STEP is not False
            or '_A4ReplicationCommitCandidate' in replication_public_names):
        raise AssertionError('A4.8c1 identity/public scope differs')
    replication_signature = inspect.signature(
        a48c.A4ReplicationEventScheduler.cpu_replication,
    )
    if tuple(replication_signature.parameters) != (
            'self', 'world', 'cell', 'dt', 'config'):
        raise AssertionError('A4.8c1 bridge accepts external plan authority')
    if (not issubclass(
            a48c.A4ReplicationEventScheduler,
            a48b.A4TranslationEventScheduler)
            or not issubclass(
                a48c.Hybrid066WorldA4Replication,
                a48b.Hybrid066WorldA4Translation)):
        raise AssertionError('A4.8c1 wrapper did not preserve A4.8b authority')
    completion_public_names = set(a48c2.__all__)
    if (a48c2.BUILD != 'SOMA-CELL 0.6.8-GPU A4.8c2'
            or a48c2.SCHEMA_VERSION
            != ('0.6.8-GPU-A4.8c2-mutation-free-active-completion-'
                'atomic-commit')
            or a48c2.FULL_GPU_WORLD_STEP is not False
            or '_A4ReplicationCompletionCommitCandidate'
            in completion_public_names):
        raise AssertionError('A4.8c2 identity/public scope differs')
    completion_signature = inspect.signature(
        a48c2.A4ReplicationCompletionEventScheduler.cpu_replication,
    )
    if tuple(completion_signature.parameters) != (
            'self', 'world', 'cell', 'dt', 'config'):
        raise AssertionError('A4.8c2 bridge accepts external plan authority')
    if (not issubclass(
            a48c2.A4ReplicationCompletionEventScheduler,
            a48c.A4ReplicationEventScheduler)
            or not issubclass(
                a48c2.Hybrid066WorldA4ReplicationCompletion,
                a48c.Hybrid066WorldA4Replication)):
        raise AssertionError('A4.8c2 wrapper did not preserve A4.8c1 authority')
    return ('%s / %s + %s + %s + %s + %s + %s + %s + %s + %s / '
            'full_gpu=false') % (
        a48c2.BUILD, a4.SCHEMA_VERSION,
        a4.GENE_CACHE_SCHEMA_VERSION + ' + ' + a4.TRANSLATION_SCHEMA_VERSION,
        a44.SCHEMA_VERSION, a47.SCHEMA_VERSION,
        a47.DELETION_PLAN_SCHEMA_VERSION, a48.SCHEMA_VERSION,
        a48b.SCHEMA_VERSION, a48c.SCHEMA_VERSION, a48c2.SCHEMA_VERSION,
    )


def test_source_hash_inputs_present():
    values = source_sha256_map()
    if set(values) != set(SOURCE_PATHS):
        raise AssertionError('source hash identity set differs')
    if any(len(value) != 64 for value in values.values()):
        raise AssertionError('invalid SHA-256')
    return '%d authoritative development inputs hashed' % len(values)


def test_ragged_batch_roundtrip_lossless():
    source_world, cells = _fixture_cells()
    source_before = v3.pickle_clone(source_world.state_dict())
    adapter = a4.FullFidelityA4GenomeAdapter()
    batch = adapter.pack_cells(cells)
    a4.validate_a4_ragged(batch)
    v3.assert_recursive_close(
        source_before, source_world.state_dict(), atol=0.0, rtol=0.0,
        path='pack_source',
    )
    if list(batch.genome_counts[:4]) != [0, 1, 2, 3]:
        raise AssertionError('complete genome counts/order changed')
    if list(batch.replication_active[:4]) != [False, True, True, False]:
        raise AssertionError('replication flags changed')
    if list(batch.lesion_offsets[:5]) != [0, 0, 0, 1, 4]:
        raise AssertionError('transient lesion vector was padded')
    expected_material = np.asarray((0, len(cells[1].genomes[0]),
                                    sum(len(g) for g in cells[2].genomes) +
                                    len(cells[2].replication_copy),
                                    sum(len(g) for g in cells[3].genomes)),
                                   dtype=np.int64)
    if not np.array_equal(batch.material_symbol_counts_host()[:4],
                          expected_material):
        raise AssertionError('template was counted as material or copy omitted')

    _, targets = _target_cells()
    adapter.unpack_cells(batch, targets)
    for index, (expected, actual) in enumerate(zip(cells, targets)):
        _assert_structural_equal(expected, actual, 'cell[%d]' % index)
    return '%d cells / %d sequences / %d symbols exact' % (
        batch.cell_count, batch.sequence_count, batch.symbol_count,
    )


def test_numpy_torch_fp64_roundtrip_and_deep_clone():
    _, cells = _fixture_cells(seed=7402)
    batch = a4.pack_a4_cells(cells)
    clone = batch.clone()
    original = int(batch.symbols[0])
    clone.symbols[0] = (original + 1) % a4.ALPHABET_SIZE
    if int(batch.symbols[0]) != original:
        raise AssertionError('clone aliases source symbol storage')
    restored = a4.A4RaggedGenomeBatch.from_state_dict(batch.state_dict())
    v3.assert_recursive_close(
        batch, restored, atol=0.0, rtol=0.0, path='state_dict',
    )
    if torch is None:
        raise AssertionError('PyTorch is required for A4.1')
    _assert_raises(ValueError, lambda: batch.to_torch(device='auto'))
    _assert_raises(ValueError, lambda: batch.to_torch(device='meta'))
    tensor = batch.to_torch(device='cpu')
    back = tensor.to_numpy()
    v3.assert_recursive_close(
        batch, back, atol=0.0, rtol=0.0, path='numpy_torch_numpy',
    )
    return 'deep clone + NumPy/Torch fp64 exact'


def test_ragged_schema_rejects_corruption_atomically():
    _, cells = _fixture_cells(seed=7403)
    adapter = a4.FullFidelityA4GenomeAdapter()
    batch = adapter.pack_cells(cells)
    corruptions = []

    def add(name, mutate):
        item = batch.clone()
        mutate(item)
        corruptions.append((name, item))

    add('offset_start', lambda x: x.sequence_offsets.__setitem__(0, 1))
    add('offset_nonmonotonic',
        lambda x: x.sequence_offsets.__setitem__(1, -1))
    add('alphabet', lambda x: x.symbols.__setitem__(0, a4.ALPHABET_SIZE))
    add('symbol_tail',
        lambda x: x.symbols.__setitem__(x.symbol_count, 1))
    add('unused_offset_tail',
        lambda x: x.sequence_offsets.__setitem__(x.sequence_count + 1, 0))
    add('cell_sequence_relation',
        lambda x: x.genome_counts.__setitem__(0, 1))
    add('inactive_replication_fractional',
        lambda x: x.replication_fractional.__setitem__(0, 0.25))
    add('dtype', lambda x: setattr(
        x, 'sequence_offsets', x.sequence_offsets.astype(np.int32),
    ))
    add('count_scalar_type', lambda x: setattr(x, 'cell_count', 3.0))

    _, targets = _target_cells(seed=7503)
    target_before = [_structural_state(cell) for cell in targets]
    for name, corrupt in corruptions:
        _assert_raises(a4.A4SchemaError,
                       lambda corrupt=corrupt: adapter.unpack_cells(corrupt, targets))
        for index, cell in enumerate(targets):
            v3.assert_recursive_close(
                target_before[index], _structural_state(cell),
                atol=0.0, rtol=0.0,
                path='atomic.%s.cell[%d]' % (name, index),
            )
    return '%d corruptions rejected before target mutation' % len(corruptions)


def test_capacity_exact_and_plus_one_atomic():
    world, cells = _fixture_cells(seed=7404)
    probe = a4.pack_a4_cells(cells)
    lengths = np.diff(probe.sequence_offsets[:probe.sequence_count + 1])
    exact_values = {
        'max_cells': probe.cell_count,
        'max_sequences': probe.sequence_count,
        'max_symbols': probe.symbol_count,
        'max_sequence_symbols': int(np.max(lengths)),
    }
    exact = a4.GPU068A4Config(**exact_values)
    packed = a4.FullFidelityA4GenomeAdapter(exact).pack_cells(cells)
    if (packed.cell_capacity, packed.sequence_capacity, packed.symbol_capacity) != (
            probe.cell_count, probe.sequence_count, probe.symbol_count):
        raise AssertionError('exact capacity did not remain exact')

    before = v3.pickle_clone(world.state_dict())
    _assert_raises(
        ValueError,
        lambda: a4.GPU068A4Config(max_cells=float(exact_values['max_cells'])),
    )
    _assert_raises(
        ValueError,
        lambda: a4.GPU068A4Config.from_state({'max_cell': exact_values['max_cells']}),
    )
    for key in exact_values:
        if exact_values[key] <= 1:
            raise AssertionError('fixture too small for +1 capacity assay')
        values = dict(exact_values)
        values[key] -= 1
        config = a4.GPU068A4Config(**values)
        _assert_raises(
            a4.A4CapacityError,
            lambda config=config: a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells),
        )
        v3.assert_recursive_close(
            before, world.state_dict(), atol=0.0, rtol=0.0,
            path='capacity_source.%s' % key,
        )
    return '4 exact axes PASS / each capacity+1 rejected atomically'


def test_cell_identity_order_and_duplicate_rejected():
    _, cells = _fixture_cells(seed=7405)
    adapter = a4.FullFidelityA4GenomeAdapter()
    batch = adapter.pack_cells(cells)
    duplicate = copy.deepcopy(cells)
    duplicate[1].cell_id = duplicate[0].cell_id
    _assert_raises(a4.A4SchemaError, lambda: adapter.pack_cells(duplicate))
    missing_genomes = copy.deepcopy(cells)
    del missing_genomes[0].genomes
    _assert_raises(a4.A4SchemaError,
                   lambda: adapter.pack_cells(missing_genomes))
    nonnumeric_lesion = copy.deepcopy(cells)
    nonnumeric_lesion[2].genome_lesions = ['0.25']
    _assert_raises(a4.A4SchemaError,
                   lambda: adapter.pack_cells(nonnumeric_lesion))
    complex_lesion = copy.deepcopy(cells)
    complex_lesion[2].genome_lesions = [0.25 + 0.5j]
    _assert_raises(a4.A4SchemaError,
                   lambda: adapter.pack_cells(complex_lesion))

    _, targets = _target_cells(seed=7505)
    target_before = [_structural_state(cell) for cell in targets]
    reordered = [targets[1], targets[0], targets[2], targets[3]]
    _assert_raises(a4.A4SchemaError,
                   lambda: adapter.unpack_cells(batch, reordered))
    fractional_id = copy.deepcopy(targets)
    fractional_id[0].cell_id = float(batch.cell_ids[0]) + 0.5
    _assert_raises(a4.A4SchemaError,
                   lambda: adapter.unpack_cells(batch, fractional_id))
    for index, cell in enumerate(targets):
        v3.assert_recursive_close(
            target_before[index], _structural_state(cell),
            atol=0.0, rtol=0.0, path='identity.cell[%d]' % index,
        )
    return 'duplicate pack and reordered commit rejected'


def test_explicit_device_residency_checksum_smoke():
    if torch is None:
        raise AssertionError('PyTorch unavailable')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    _, base_cells = _fixture_cells(seed=7406)
    cells = []
    for index in range(32):
        cell = copy.deepcopy(base_cells[2])
        cell.cell_id = index
        cells.append(cell)
    probe = a4.FullFidelityA4GenomeAdapter(a4.GPU068A4Config(
        max_cells=32, max_sequences=32 * 4,
        max_symbols=32 * 2048,
    )).pack_cells(cells)
    expected = int(np.sum(probe.symbols[:probe.symbol_count], dtype=np.int64))
    resident = probe.to_torch(device=device)
    requested_type = torch.device(device).type
    if any(getattr(resident, name).device.type != requested_type
           for name in a4._ARRAY_FIELDS):
        raise AssertionError('not every array is on the requested device')
    pointers_before = resident.data_ptrs()
    warm = resident.resident_symbol_checksum()
    if device == 'cuda':
        torch.cuda.synchronize()
    started = time.perf_counter()
    result = warm
    for _ in range(10):
        result = resident.resident_symbol_checksum()
    if device == 'cuda':
        torch.cuda.synchronize()
    elapsed = time.perf_counter() - started
    pointers_after = resident.data_ptrs()
    if pointers_before != pointers_after:
        raise AssertionError('resident input storage was reallocated')
    # One explicit final readback phase after the resident loop: verify both
    # the reduction output and every packed array.
    resident_checksum = int(result.detach().item())
    back = resident.to_numpy()
    v3.assert_recursive_close(
        probe, back, atol=0.0, rtol=0.0, path='resident_cuda_readback',
    )
    if resident_checksum != expected:
        raise AssertionError('resident checksum differs from host source')
    return '%s canonical dtypes (float fields fp64), 10 resident reads, stable pointers, %.6fs (no speed claim)' % (
        device, elapsed,
    )


def test_a4_gene_decode_frozen_oracle():
    world, cell, config = _gene_decode_fixture()
    before = v3.pickle_clone(world.state_dict())
    packed = a4.FullFidelityA4GenomeAdapter(config).pack_cells([cell])
    cache = a4.decode_a4_gene_cache_numpy(packed)
    if int(cache.entry_count) != 3:
        raise AssertionError('unique gene count differs from frozen cache')
    expected_order = [40116138652, 55715248503, 68679218097]
    if list(cache.fingerprints[:3]) != expected_order:
        raise AssertionError('first-occurrence fingerprint order differs')
    if list(cache.copy_numbers[:3]) != [2, 3, 1]:
        raise AssertionError('duplicate copy counts differ')
    if list(cache.starts[:3]) != [19, 35, 19]:
        raise AssertionError('first record starts differ')
    materialized = cache.materialize_gene_specs_host()[0]
    _assert_gene_specs_equal(cell.gene_specs, materialized, 'gene_specs')
    if 9105062057 in materialized:
        raise AssertionError('replication template was decoded as a genome')
    if int(materialized[40116138652]['copy_number']) != 2:
        raise AssertionError('template duplicate changed complete-genome count')
    grammar = materialized[68679218097]
    if not a4.s65.is_grammar_spec(grammar):
        raise AssertionError('0.6.5 grammar payload was not preserved')
    if tuple(grammar['payload'][8:10]) != (1, 6):
        raise AssertionError('grammar module/value payload was truncated')
    for fingerprint, spec in materialized.items():
        if spec['role'] == a4.g2.ROLE_GENERIC:
            if 'reaction' not in spec or 'reaction_name' not in spec:
                raise AssertionError('generic reaction fields are missing')
        elif 'reaction' in spec or 'reaction_name' in spec:
            raise AssertionError('non-generic cache fabricated reaction fields')
    v3.assert_recursive_close(
        before, world.state_dict(), atol=0.0, rtol=0.0,
        path='gene_decode_source',
    )
    return '6 accepted -> 3 unique; overlap/recovery/order/template exclusion exact'


def test_a4_gene_decode_numpy_torch_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.2')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    _, cell, config = _gene_decode_fixture(seed=7602)
    packed = a4.FullFidelityA4GenomeAdapter(config).pack_cells([cell])
    expected = a4.decode_a4_gene_cache_numpy(packed)
    expected_specs = expected.materialize_gene_specs_host()

    source = '\n'.join(inspect.getsource(item) for item in (
        a4.decode_a4_gene_cache_torch,
        a4._validate_resident_ragged_metadata,
        a4._gene_decode_dimensions,
        a4._validate_gene_backend_and_dtypes,
    ))
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(')
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident decoder contains host/dynamic op: %s' % found)

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    details = []
    for device in devices:
        resident = packed.to_torch(device=device)
        pointers_before = resident.data_ptrs()
        cache = a4.decode_a4_gene_cache_torch(resident)
        if any(getattr(cache, name).device.type != device
               for name in a4._GENE_ARRAY_FIELDS):
            raise AssertionError('%s cache escaped requested device' % device)
        if pointers_before != resident.data_ptrs():
            raise AssertionError('%s decode reallocated source arena' % device)
        if device == 'cuda':
            torch.cuda.synchronize()
        back = cache.to_numpy()
        v3.assert_recursive_close(
            expected.state_dict(), back.state_dict(),
            atol=0.0, rtol=0.0, path='gene_cache.%s' % device,
        )
        actual_specs = back.materialize_gene_specs_host()
        for ci, (left, right) in enumerate(zip(expected_specs, actual_specs)):
            _assert_gene_specs_equal(left, right, '%s.cell[%d]' % (device, ci))
        v3.assert_recursive_close(
            packed.state_dict(), resident.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='resident_source.%s' % device,
        )
        details.append(device)
    return 'NumPy/Torch %s fixed-shape exact; no decoder D2H/dynamic op' % '/'.join(details)


def test_a4_gene_decode_capacity_exact_and_plus_one():
    _assert_raises(
        ValueError,
        lambda: a4.GPU068A4Config(
            max_cells=a4.MAX_CELL_CAPACITY_FOR_FINGERPRINT_KEY + 1,
        ),
    )
    world = v3.make_world(seed=7603, cells=1)
    cell = world.cells[0]
    genes = [a4.g2.make_gene(
        role, parameter=role, regulator=role,
        promoter=role, efficiency=7 - role,
    ) for role in range(6)]
    cell.genomes = [np.concatenate(genes).astype(np.uint8)]
    cell.genome_lesions = [0.0]
    cell.replication_template = None
    cell.replication_copy = []
    cell.replication_template_lesion = 0.0
    cell.replication_fractional = 0.0
    cell._refresh_gene_cache()
    config = a4.GPU068A4Config(
        max_cells=1, max_sequences=1, max_symbols=6 * a4.GENE_SPAN,
        max_sequence_symbols=6 * a4.GENE_SPAN,
    )
    before = v3.pickle_clone(world.state_dict())
    packed = a4.FullFidelityA4GenomeAdapter(config).pack_cells([cell])
    decoded = a4.decode_a4_gene_cache_numpy(packed)
    if decoded.entry_capacity != 6 or int(decoded.entry_count) != 6:
        raise AssertionError('exact derived entry capacity did not pass')
    resident = packed.to_torch(device='cuda' if torch.cuda.is_available() else 'cpu')
    resident_cache = a4.decode_a4_gene_cache_torch(resident).to_numpy()
    if resident_cache.entry_capacity != 6 or int(resident_cache.entry_count) != 6:
        raise AssertionError('Torch exact derived entry capacity did not pass')

    overflow = copy.deepcopy(cell)
    overflow.genomes[0] = np.concatenate((
        overflow.genomes[0],
        a4.g2.make_gene(a4.g2.ROLE_GENERIC, parameter=7, spare=(7, 7, 7, 7)),
    )).astype(np.uint8)
    overflow._refresh_gene_cache()
    _assert_raises(
        a4.A4CapacityError,
        lambda: a4.FullFidelityA4GenomeAdapter(config).pack_cells([overflow]),
    )
    v3.assert_recursive_close(
        before, world.state_dict(), atol=0.0, rtol=0.0,
        path='gene_capacity_source',
    )

    empty = copy.deepcopy(cell)
    empty.genomes = []
    empty.genome_lesions = []
    empty._refresh_gene_cache()
    empty_config = a4.GPU068A4Config(
        max_cells=1, max_sequences=1, max_symbols=1,
        max_sequence_symbols=15,
    )
    empty_batch = a4.FullFidelityA4GenomeAdapter(empty_config).pack_cells([empty])
    empty_cache = a4.decode_a4_gene_cache_numpy(empty_batch)
    if empty_cache.entry_capacity != 0 or int(empty_cache.entry_count) != 0:
        raise AssertionError('zero-gene capacity is not represented exactly')
    return 'key bound + derived capacity 6 exact / seventh fails before pack / zero exact'


def test_a4_gene_cache_schema_and_decode_nonmutation():
    world, cell, config = _gene_decode_fixture(seed=7604)
    world_before = v3.pickle_clone(world.state_dict())
    packed = a4.FullFidelityA4GenomeAdapter(config).pack_cells([cell])
    packed_before = packed.state_dict()
    cache = a4.decode_a4_gene_cache_numpy(packed)
    v3.assert_recursive_close(
        packed_before, packed.state_dict(), atol=0.0, rtol=0.0,
        path='numpy_decode_input',
    )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='numpy_decode_cells',
    )

    corruptions = []
    def add(name, mutate):
        item = cache.clone()
        mutate(item)
        corruptions.append((name, item))
    add('mask', lambda x: x.entry_mask.__setitem__(0, False))
    add('offset', lambda x: x.cell_entry_offsets.__setitem__(1, 0))
    add('fingerprint', lambda x: x.fingerprints.__setitem__(0, 0))
    add('payload', lambda x: x.payloads.__setitem__((0, 0), a4.ALPHABET_SIZE))
    add('copy_number', lambda x: x.copy_numbers.__setitem__(0, 0))
    add('unused_tail', lambda x: x.starts.__setitem__(int(x.entry_count), 0))
    add('frozen_length_limit', lambda x: setattr(
        x, 'max_sequence_symbols', a4.MAX_FROZEN_GENOME_SYMBOLS + 1,
    ))
    for name, item in corruptions:
        _assert_raises(
            a4.A4SchemaError,
            lambda item=item: a4.validate_a4_gene_cache(item),
        )
    resident = packed.to_torch(device='cpu')
    for value in (0, a4.MAX_FROZEN_GENOME_SYMBOLS + 1):
        malformed = resident.clone()
        malformed.max_sequence_symbols = value
        _assert_raises(
            a4.A4SchemaError,
            lambda malformed=malformed: a4.decode_a4_gene_cache_torch(malformed),
        )
    return '%d cache + 2 resident metadata corruptions rejected; source/cells unchanged' % len(corruptions)


def test_a4_paid_translation_cpu_oracle_ledger_and_order():
    world, cells, config, dt = _paid_translation_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    gene_orders = [list(cell.gene_specs.keys()) for cell in cells]
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells)
    state = a4.pack_a4_translation_state(cells, ragged, world.config, config)
    v3.assert_recursive_close(
        float(state.neural_attachment_osmolyte[0]), 29.3,
        atol=4e-15, rtol=0.0, path='neural_attachment_osmolyte',
    )
    state_before = state.state_dict()
    plan = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(ragged, state), dt,
    )
    cpu_cells = copy.deepcopy(cells)
    for cell in cpu_cells:
        cell.translate(dt, world.config)
    _assert_translation_matches_cells(plan, cpu_cells, 'cpu_oracle')

    for ci, (before, after) in enumerate(zip(cells, cpu_cells)):
        if list(after.gene_specs.keys()) != gene_orders[ci]:
            raise AssertionError('translation changed gene-cache order')
        total = float(after.last_translation) * dt
        for pool, coefficient in (
            (a4.a3.POOL_FUEL, 0.64),
            (a4.a3.POOL_MINERAL, 0.36),
            (a4.a3.POOL_ATP, 0.52),
        ):
            v3.assert_recursive_close(
                float(before.pools[pool] - after.pools[pool]),
                coefficient * total, atol=3e-12, rtol=0.0,
                path='ledger.cell[%d].pool[%d]' % (ci, pool),
            )
        v3.assert_recursive_close(
            float(after.pools[a4.a3.POOL_CATALYST]),
            float(sum(after.proteins.values())), atol=2e-12, rtol=0.0,
            path='ledger.cell[%d].active' % ci,
        )
        v3.assert_recursive_close(
            float(after.pools[a4.a3.POOL_DAMAGED_PROTEIN]),
            float(sum(after.damaged_proteins.values())),
            atol=2e-12, rtol=0.0,
            path='ledger.cell[%d].damaged' % ci,
        )
    if cpu_cells[1].last_translation != 0.0:
        raise AssertionError('ATP reserve produced unpaid protein')
    if cpu_cells[1].last_quiescence == cells[1].last_quiescence:
        raise AssertionError('ATP reserve was mistaken for an early return')
    if not (cpu_cells[2].last_translation > 0.0
            and cpu_cells[2].pools[a4.a3.POOL_MINERAL] <= 2e-16):
        raise AssertionError('gene-order mineral exhaustion was not exercised')
    if (cpu_cells[3].last_translation != 0.0
            or cpu_cells[3].last_quiescence != cells[3].last_quiescence):
        raise AssertionError('no-translator early-return observables differ')
    v3.assert_recursive_close(
        state_before, state.state_dict(), atol=0.0, rtol=0.0,
        path='translation_numpy_input',
    )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='translation_numpy_world',
    )
    return '4 Formal066 cells exact; ATP reserve/order/ledgers/dict order fixed'


def test_a4_paid_translation_numpy_torch_devices_nonmutation():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.3')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    world, cells, config, dt = _paid_translation_fixture(seed=7702)
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells)
    state = a4.pack_a4_translation_state(cells, ragged, world.config, config)
    expected = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(ragged, state), dt,
    )
    source = '\n'.join(inspect.getsource(item) for item in (
        a4.paid_translation_plan_torch,
        a4._torch_ordered_row_sum,
        a4._validate_translation_resident_metadata,
        a4._validate_translation_backend_and_dtypes,
    ))
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(')
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident translation contains host/dynamic op: %s' % found)

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        binding = a4.bind_a4_translation(resident_ragged, resident_state)
        cache_ptrs = binding.cache.data_ptrs()
        plan = a4.paid_translation_plan_torch(binding, dt)
        if any(getattr(plan, name).device.type != device
               for name in a4._TRANSLATION_ARRAY_FIELDS):
            raise AssertionError('%s translation output escaped device' % device)
        if device == 'cuda':
            torch.cuda.synchronize()
        back = plan.to_numpy()
        v3.assert_recursive_close(
            expected.state_dict(), back.state_dict(),
            atol=2e-12, rtol=0.0, path='translation.%s' % device,
        )
        if ragged_ptrs != resident_ragged.data_ptrs():
            raise AssertionError('%s translation reallocated ragged input' % device)
        if state_ptrs != resident_state.data_ptrs():
            raise AssertionError('%s translation reallocated physiology input' % device)
        if cache_ptrs != binding.cache.data_ptrs():
            raise AssertionError('%s translation reallocated decoded cache' % device)
        v3.assert_recursive_close(
            ragged.state_dict(), resident_ragged.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='translation_ragged_source.%s' % device,
        )
        v3.assert_recursive_close(
            state.state_dict(), resident_state.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='translation_state_source.%s' % device,
        )

    # External translator raises capacity but must not alter the endogenous
    # translator-protein need used while weights are built.
    external_config = copy.deepcopy(world.config)
    external_config.external_translator = True
    external_state = a4.pack_a4_translation_state(
        cells, ragged, external_config, config,
    )
    external_expected = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(ragged, external_state), dt,
    )
    external_cpu = copy.deepcopy(cells)
    for cell in external_cpu:
        cell.translate(dt, external_config)
    _assert_translation_matches_cells(
        external_expected, external_cpu, 'external_translator_cpu',
    )
    external_device = 'cuda' if torch.cuda.is_available() else 'cpu'
    external_plan = a4.paid_translation_plan_torch(
        a4.bind_a4_translation(
            ragged.to_torch(external_device),
            external_state.to_torch(external_device),
        ), dt,
    ).to_numpy()
    v3.assert_recursive_close(
        external_expected.state_dict(), external_plan.state_dict(),
        atol=2e-12, rtol=0.0, path='external_translator.%s' % external_device,
    )

    # Resource exhaustion at a tiny positive budget is a sensitive fp64
    # boundary: the resident plan must preserve the literal per-gene payment
    # recurrence without producing a schema-invalid negative pool.
    tiny_cells = copy.deepcopy(cells)
    tiny_cells[0].pools[a4.a3.POOL_FUEL] = 0.64e-12
    tiny_cells[0].pools[a4.a3.POOL_MINERAL] = 0.36e-12
    tiny_cells[0].pools[a4.a3.POOL_ATP] = 0.042 + 0.52e-12
    tiny_ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(tiny_cells)
    tiny_state = a4.pack_a4_translation_state(
        tiny_cells, tiny_ragged, external_config, config,
    )
    tiny_dt = 1e-8
    tiny_expected = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(tiny_ragged, tiny_state), tiny_dt,
    )
    tiny_cpu = copy.deepcopy(tiny_cells)
    for cell in tiny_cpu:
        cell.translate(tiny_dt, external_config)
    _assert_translation_matches_cells(
        tiny_expected, tiny_cpu, 'tiny_resource_cpu', atol=2e-12,
    )
    tiny_plan = a4.paid_translation_plan_torch(
        a4.bind_a4_translation(
            tiny_ragged.to_torch(external_device),
            tiny_state.to_torch(external_device),
        ), tiny_dt,
    ).to_numpy()
    v3.assert_recursive_close(
        tiny_expected.state_dict(), tiny_plan.state_dict(),
        atol=2e-12, rtol=0.0, path='tiny_resource.%s' % external_device,
    )

    # This exact fuel boundary makes the frozen Python recurrence retain a
    # signed fp64 residual.  Preserve it as measured evidence for the narrow
    # three-paid-pool ledger tolerance; do not hide it with a general clip.
    residual_cells = copy.deepcopy(cells)
    residual_cells[0].pools[a4.a3.POOL_FUEL] = 9.936293309191388e-08
    residual_ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(
        residual_cells,
    )
    residual_state = a4.pack_a4_translation_state(
        residual_cells, residual_ragged, world.config, config,
    )
    residual_dt = 0.001
    residual_expected = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(residual_ragged, residual_state), residual_dt,
    )
    residual_cpu = copy.deepcopy(residual_cells)
    for cell in residual_cpu:
        cell.translate(residual_dt, world.config)
    _assert_translation_matches_cells(
        residual_expected, residual_cpu, 'signed_payment_residual',
        atol=2e-12,
    )
    observed_residual = float(
        residual_cpu[0].pools[a4.a3.POOL_FUEL]
    )
    if not (-a4.TRANSLATION_LEDGER_ATOL <= observed_residual < 0.0):
        raise AssertionError(
            'fixture did not exercise a signed fp64 payment residual'
        )
    residual_plan = a4.paid_translation_plan_torch(
        a4.bind_a4_translation(
            residual_ragged.to_torch(external_device),
            residual_state.to_torch(external_device),
        ), residual_dt,
    ).to_numpy()
    v3.assert_recursive_close(
        residual_expected.state_dict(), residual_plan.state_dict(),
        atol=2e-12, rtol=0.0,
        path='signed_payment_residual.%s' % external_device,
    )
    return 'NumPy/Torch %s fp64 + external translator parity; fixed output and stable inputs' % '/'.join(devices)


def test_a4_paid_translation_full_mro_signals_and_behavioural_quiescence():
    world, cells, config, dt = _paid_translation_fixture(seed=7703)
    localisations = {
        int(spec['localisation'])
        for spec in cells[0].gene_specs.values()
        if int(spec['role']) == int(a4.a3.ROLE_REGULATOR)
    }
    expected_localisations = {
        int(a4.s4.LOC_REPAIR), int(a4.s4.LOC_SENSOR),
        int(a4.s4.LOC_EFFECTOR), int(a4.s5.LOC_ECOLOGY),
    }
    if localisations != expected_localisations:
        raise AssertionError('fixture does not cover the full regulator MRO')

    model_config = copy.deepcopy(world.config)
    model_config.quiescence = False
    model_config.quiescence_effector = True
    cells[0].behavioural_quiescence = 0.47
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells)
    state = a4.pack_a4_translation_state(
        cells, ragged, model_config, config,
    )
    plan = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(ragged, state), dt,
    )
    cpu_cells = copy.deepcopy(cells)
    for cell in cpu_cells:
        cell.translate(dt, model_config)
    _assert_translation_matches_cells(plan, cpu_cells, 'full_mro')
    v3.assert_recursive_close(
        float(plan.last_quiescence[0]), 0.47,
        atol=2e-12, rtol=0.0, path='behavioural_quiescence',
    )

    zero_signal_cells = copy.deepcopy(cells)
    for cell in zero_signal_cells:
        cell.last_receptor_activity[:] = 0.0
        cell.last_control_vectors[:] = 0.0
        cell.last_control_scalars[:] = 0.0
        cell.last_edna_signal = 0.0
        cell.last_corpse_signal = 0.0
        cell.last_necrotoxin_signal = 0.0
        cell.behavioural_quiescence = 0.0
    zero_ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(zero_signal_cells)
    zero_state = a4.pack_a4_translation_state(
        zero_signal_cells, zero_ragged, model_config, config,
    )
    zero_plan = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(zero_ragged, zero_state), dt,
    )
    if np.allclose(plan.active_mass, zero_plan.active_mass, atol=1e-15, rtol=0.0):
        raise AssertionError('sensor/effector/ecology snapshots did not affect weights')
    return 'repair/sensor/effector/ecology MRO + behavioural quiescence exact'


def test_a4_paid_translation_early_gate_capacity_and_schema():
    world, cells, config, dt = _paid_translation_fixture(seed=7704)
    disabled_config = copy.deepcopy(world.config)
    disabled_config.gene_expression = False
    ragged = a4.FullFidelityA4GenomeAdapter(config).pack_cells(cells)
    state = a4.pack_a4_translation_state(
        cells, ragged, disabled_config, config,
    )
    plan = a4.paid_translation_plan_numpy(
        a4.bind_a4_translation(ragged, state), dt,
    )
    cpu_cells = copy.deepcopy(cells)
    for cell in cpu_cells:
        cell.translate(dt, disabled_config)
    _assert_translation_matches_cells(plan, cpu_cells, 'gene_expression_disabled')
    if np.any(plan.last_translation[:plan.cell_count] != 0.0):
        raise AssertionError('disabled translation did not reset last_translation')
    if not np.array_equal(plan.last_quiescence, state.last_quiescence):
        raise AssertionError('disabled translation changed last_quiescence')

    cache = a4.decode_a4_gene_cache_numpy(ragged)
    specs = cache.materialize_gene_specs_host()
    required = 0
    for cell, cell_specs in zip(cells, specs):
        gene_keys = set(cell_specs.keys())
        required = max(
            required,
            len(set(cell.proteins.keys()).union(gene_keys)),
            len(set(cell.damaged_proteins.keys()).union(gene_keys)),
        )
    exact_config = a4.GPU068A4Config(
        max_cells=config.max_cells, max_sequences=config.max_sequences,
        max_symbols=config.max_symbols,
        max_sequence_symbols=config.max_sequence_symbols,
        max_proteins_per_cell=required,
    )
    exact = a4.pack_a4_translation_state(
        cells, ragged, world.config, exact_config,
    )
    if exact.protein_capacity != required:
        raise AssertionError('exact protein capacity did not remain exact')
    smaller = a4.GPU068A4Config(
        max_cells=config.max_cells, max_sequences=config.max_sequences,
        max_symbols=config.max_symbols,
        max_sequence_symbols=config.max_sequence_symbols,
        max_proteins_per_cell=required - 1,
    )
    _assert_raises(
        a4.A4CapacityError,
        lambda: a4.pack_a4_translation_state(cells, ragged, world.config, smaller),
    )

    corruptions = []
    def add(name, mutate):
        item = state.clone()
        mutate(item)
        corruptions.append((name, item))
    add('negative_pool', lambda x: x.pools.__setitem__((0, 0), -1.0))
    add('active_count', lambda x: x.active_count.__setitem__(0, x.protein_capacity + 1))
    add('float32', lambda x: setattr(x, 'pools', x.pools.astype(np.float32)))
    add('fingerprint_tail', lambda x: x.active_fingerprints.__setitem__(
        (0, int(x.active_count[0])), 0,
    ))
    for _, item in corruptions:
        _assert_raises(a4.A4SchemaError,
                       lambda item=item: a4.validate_a4_translation_state(item))
    signed_roundoff = state.clone()
    signed_roundoff.pools[0, a4.a3.POOL_FUEL] = (
        -0.5 * a4.TRANSLATION_LEDGER_ATOL
    )
    a4.validate_a4_translation_state(signed_roundoff)
    beyond_roundoff = state.clone()
    beyond_roundoff.pools[0, a4.a3.POOL_FUEL] = (
        -2.0 * a4.TRANSLATION_LEDGER_ATOL
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.validate_a4_translation_state(beyond_roundoff),
    )
    nonpayment_negative = state.clone()
    nonpayment_negative.pools[0, a4.a3.POOL_WASTE] = (
        -0.5 * a4.TRANSLATION_LEDGER_ATOL
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.validate_a4_translation_state(nonpayment_negative),
    )
    mismatched = state.clone()
    mismatched.cell_ids[:2] = mismatched.cell_ids[1::-1]
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(ragged, mismatched),
    )
    foreign = ragged.clone()
    foreign.symbols[0] = (int(foreign.symbols[0]) + 1) % a4.ALPHABET_SIZE
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(foreign, state),
    )
    resident_foreign = foreign.to_torch('cuda' if torch.cuda.is_available() else 'cpu')
    resident_state = state.to_torch(
        'cuda' if torch.cuda.is_available() else 'cpu'
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(resident_foreign, resident_state),
    )
    resident_ragged = ragged.to_torch(
        'cuda' if torch.cuda.is_available() else 'cpu'
    )
    trusted_ragged_clone = resident_ragged.clone()
    trusted_state_clone = resident_state.clone()
    a4.bind_a4_translation(trusted_ragged_clone, trusted_state_clone)
    trusted_state_clone.pools[0, a4.a3.POOL_FUEL] += 1e-9
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(
            trusted_ragged_clone, trusted_state_clone,
        ),
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: trusted_state_clone.clone(),
    )
    trusted_ragged_clone.symbols[0] = torch.remainder(
        trusted_ragged_clone.symbols[0].to(torch.int64) + 1,
        a4.ALPHABET_SIZE,
    ).to(torch.uint8)
    _assert_raises(
        a4.A4SchemaError,
        lambda: trusted_ragged_clone.clone(),
    )
    cache = a4.decode_a4_gene_cache_numpy(ragged)
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.A4TranslationBinding(
            ragged=ragged, cache=cache, state=state,
        ),
    )
    forged = object.__new__(a4.A4TranslationBinding)
    object.__setattr__(forged, 'ragged', ragged)
    object.__setattr__(forged, 'cache', cache)
    object.__setattr__(forged, 'state', state)
    object.__setattr__(forged, '_token', object())
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.paid_translation_plan_numpy(forged, dt),
    )
    changed_host_cache = a4.bind_a4_translation(ragged, state)
    changed_host_cache.cache.copy_numbers[0] += 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.paid_translation_plan_numpy(changed_host_cache, dt),
    )
    attest_device = 'cuda' if torch.cuda.is_available() else 'cpu'
    changed_resident_cache = a4.bind_a4_translation(
        ragged.to_torch(attest_device), state.to_torch(attest_device),
    )
    changed_resident_cache.cache.copy_numbers[0] += 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.paid_translation_plan_torch(
            changed_resident_cache, dt,
        ),
    )

    # Scalar metadata is part of the binding trust identity even though it is
    # not tensor storage.  Cover every post-bind component on both backends.
    for component in ('ragged', 'state', 'cache'):
        scalar_binding = a4.bind_a4_translation(
            ragged.clone(), state.clone(),
        )
        if component == 'ragged':
            scalar_binding.ragged.symbol_count -= 1
        elif component == 'state':
            scalar_binding.state.quiescence = (
                not scalar_binding.state.quiescence
            )
        else:
            scalar_binding.cache.cell_count -= 1
        _assert_raises(
            a4.A4SchemaError,
            lambda scalar_binding=scalar_binding: a4.paid_translation_plan_numpy(
                scalar_binding, dt,
            ),
        )
    for component in ('ragged', 'state', 'cache'):
        scalar_binding = a4.bind_a4_translation(
            ragged.to_torch(attest_device),
            state.to_torch(attest_device),
        )
        if component == 'ragged':
            scalar_binding.ragged.symbol_count -= 1
        elif component == 'state':
            scalar_binding.state.quiescence = (
                not scalar_binding.state.quiescence
            )
        else:
            scalar_binding.cache.cell_count -= 1
        _assert_raises(
            a4.A4SchemaError,
            lambda scalar_binding=scalar_binding: a4.paid_translation_plan_torch(
                scalar_binding, dt,
            ),
        )

    # Resident uploads remember their scalar metadata before binding.
    prebind_ragged = ragged.to_torch(attest_device)
    prebind_state = state.to_torch(attest_device)
    prebind_ragged.symbol_count -= 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(prebind_ragged, prebind_state),
    )
    prebind_ragged = ragged.to_torch(attest_device)
    prebind_state = state.to_torch(attest_device)
    prebind_state.quiescence = not prebind_state.quiescence
    _assert_raises(
        a4.A4SchemaError,
        lambda: a4.bind_a4_translation(prebind_ragged, prebind_state),
    )
    if a4.FULL_GPU_WORLD_STEP is not False:
        raise AssertionError('A4.3 must not claim a full GPU world-step')
    if a4.PORT_STATUS['genome_replication'] != 'cpu-authoritative-next-a4-slice':
        raise AssertionError('translation slice changed replication authority')
    return 'early gate + exact P=%d/P+1 atomic + %d schema corruptions rejected' % (
        required, len(corruptions),
    )


def test_a3_focused_regression():
    # Invoke named tests only; never call A3 run_all or overwrite A3 evidence.
    for fn in (
        v3.test_packed_state_roundtrip_and_invariants,
        v3.test_transient_genome_lesion_count_preserved_until_damage,
        v3.test_world_one_step_lockstep,
        v3.test_clone_deterministic_full_state,
        v3.test_save_restore_deterministic_full_state,
    ):
        fn()
    return '5 promoted-A3 focused regressions PASS'


def test_a44_paid_replication_formal066_cpu_oracle_and_nonmutation():
    world, cells, config, dt = _paid_replication_fixture()
    if any(type(cell).__name__ != 'Formal066ProtoCell' for cell in cells):
        raise AssertionError('A4.4 CPU oracle is not Formal066')
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = v3.pickle_clone(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, config,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    plan = a44.paid_replication_elongation_numpy(
        binding, dt, world.config,
    )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a44.numpy_world',
    )
    v3.assert_recursive_close(
        rng_before, world.rng.bit_generator.state, atol=0.0, rtol=0.0,
        path='a44.numpy_rng',
    )
    v3.assert_recursive_close(
        ragged_before, ragged.state_dict(), atol=0.0, rtol=0.0,
        path='a44.numpy_ragged',
    )
    v3.assert_recursive_close(
        state_before, state.state_dict(), atol=0.0, rtol=0.0,
        path='a44.numpy_state',
    )
    v3.assert_recursive_close(
        cache_before, binding.cache.state_dict(), atol=0.0, rtol=0.0,
        path='a44.numpy_cache',
    )

    cpu_cells = copy.deepcopy(cells)
    cpu_before = copy.deepcopy(cpu_cells)
    for cell in cpu_cells:
        cell._replicate_genome(world, dt, world.config)
    _assert_paid_replication_cpu_parity(
        plan, cpu_before, cpu_cells, 'a44.formal066',
    )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a44.cpu_oracle_world',
    )
    v3.assert_recursive_close(
        rng_before, world.rng.bit_generator.state, atol=0.0, rtol=0.0,
        path='a44.cpu_oracle_rng',
    )
    return '6 direct Formal066 rows exact; RNG/world/ragged/state/cache unchanged'


def test_a44_requested_zero_and_exact_resource_boundaries():
    world, cells, config, dt = _paid_replication_fixture(seed=7802)
    _, _, binding = _paid_replication_binding(cells, world.config, config)
    plan = a44.paid_replication_elongation_numpy(
        binding, dt, world.config,
    )
    requested = [int(value) for value in plan.requested_symbols[:6]]
    appended = [int(value) for value in plan.append_count[:6]]
    if requested != [1, 0, 1, 1, 1, 1]:
        raise AssertionError('requested-symbol boundary rows differ: %s' % requested)
    if appended != [1, 0, 1, 0, 1, 0]:
        raise AssertionError('paid resource gates differ: %s' % appended)

    cpu_cells = copy.deepcopy(cells)
    cpu_before = copy.deepcopy(cpu_cells)
    for cell in cpu_cells:
        cell._replicate_genome(world, dt, world.config)
    _assert_paid_replication_cpu_parity(
        plan, cpu_before, cpu_cells, 'a44.boundary_cpu',
    )

    if not np.array_equal(plan.pools_after[1], cells[1].pools):
        raise AssertionError('requested=0 changed a paid pool')
    if not (0.125 < float(plan.replication_fractional_after[1]) < 1.0):
        raise AssertionError('requested=0 fractional continuation was lost')
    atp_index = int(a4.a3.POOL_ATP)
    nucleotide_index = int(a4.a3.POOL_NUCLEOTIDE)
    v3.assert_recursive_close(
        float(plan.pools_after[2, atp_index]), 0.022,
        atol=4e-18, rtol=0.0, path='a44.atp_exact_after',
    )
    if float(plan.pools_after[3, atp_index]) != float(cells[3].pools[atp_index]):
        raise AssertionError('ATP nextafter exhaustion was not atomic')
    v3.assert_recursive_close(
        float(plan.pools_after[4, nucleotide_index]), 0.0,
        atol=0.0, rtol=0.0, path='a44.monomer_exact_after',
    )
    if (float(plan.pools_after[5, nucleotide_index])
            != float(cells[5].pools[nucleotide_index])):
        raise AssertionError('monomer nextafter exhaustion was not atomic')
    return 'requested=0 + ATP/monomer exact pass and nextafter exhaustion exact'


def test_a44_paid_replication_numpy_torch_devices_and_scope_readback():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.4b')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    world, cells, config, dt = _paid_replication_fixture(seed=7803)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, config,
    )
    expected = a44.paid_replication_elongation_numpy(
        binding, dt, world.config,
    )
    source = '\n'.join(inspect.getsource(item) for item in (
        a44.paid_replication_elongation_torch,
        a44._torch_replicase,
        a44._validate_plan_metadata,
    ))
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(')
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident replication contains host/dynamic op: %s' % found)

    inactive = copy.deepcopy(cells[0])
    inactive.replication_template = None
    inactive.replication_copy = []
    inactive.replication_template_lesion = 0.0
    inactive.replication_fractional = 0.0
    inactive_ragged, inactive_state, _ = _paid_replication_binding(
        [inactive], world.config, config,
    )

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        resident_binding = a4.bind_a4_translation(
            resident_ragged, resident_state,
        )
        cache_ptrs = resident_binding.cache.data_ptrs()
        plan = a44.paid_replication_elongation_torch(
            resident_binding, dt, world.config,
        )
        plan_ptrs = plan.data_ptrs()
        if any(getattr(plan, name).device.type != device
               for name in a44._PLAN_ARRAY_FIELDS):
            raise AssertionError('%s replication output escaped device' % device)
        if device == 'cuda':
            torch.cuda.synchronize()
        back = plan.to_numpy()
        v3.assert_recursive_close(
            expected.state_dict(), back.state_dict(),
            atol=2e-12, rtol=0.0, path='a44.plan.%s' % device,
        )
        if ragged_ptrs != resident_ragged.data_ptrs():
            raise AssertionError('%s replication reallocated ragged input' % device)
        if state_ptrs != resident_state.data_ptrs():
            raise AssertionError('%s replication reallocated state input' % device)
        if cache_ptrs != resident_binding.cache.data_ptrs():
            raise AssertionError('%s replication reallocated cache input' % device)
        if plan_ptrs != plan.data_ptrs():
            raise AssertionError('%s plan readback changed resident pointers' % device)
        v3.assert_recursive_close(
            ragged.state_dict(), resident_ragged.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='a44.ragged_source.%s' % device,
        )
        v3.assert_recursive_close(
            state.state_dict(), resident_state.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='a44.state_source.%s' % device,
        )

        invalid_binding = a4.bind_a4_translation(
            inactive_ragged.to_torch(device=device),
            inactive_state.to_torch(device=device),
        )
        invalid_plan = a44.paid_replication_elongation_torch(
            invalid_binding, dt, world.config,
        )
        _assert_raises(a44.A4ReplicationScopeError, invalid_plan.to_numpy)
    return 'NumPy/Torch %s fp64 exact; fixed resident ops/pointers and scope readback rejection' % '/'.join(devices)


def test_a44_capacity_scope_fail_closed_and_a3_authority():
    world, cells, roomy_config, dt = _paid_replication_fixture(seed=7804)
    single = [cells[0]]
    probe = a4.FullFidelityA4GenomeAdapter(roomy_config).pack_cells(single)
    common = {
        'max_cells': 1,
        'max_sequences': int(probe.sequence_count),
        'max_sequence_symbols': int(roomy_config.max_sequence_symbols),
        'max_proteins_per_cell': int(roomy_config.max_proteins_per_cell),
    }
    exact_config = a4.GPU068A4Config(
        max_symbols=int(probe.symbol_count) + 1, **common
    )
    exact_ragged, _, exact_binding = _paid_replication_binding(
        single, world.config, exact_config,
    )
    exact_plan = a44.paid_replication_elongation_numpy(
        exact_binding, dt, world.config,
    )
    if (int(exact_ragged.symbol_capacity) != int(exact_ragged.symbol_count) + 1
            or int(exact_plan.append_count[0]) != 1):
        raise AssertionError('one-future-symbol exact capacity did not pass')

    forged_plan = exact_plan.clone()
    forged_plan.pools_after[0, a4.a3.POOL_NUCLEOTIDE] = -1e-12
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_paid_elongation_plan(forged_plan),
    )

    host_ragged = exact_ragged.clone()
    host_state = exact_binding.state.clone()
    host_ragged_binding = a4.bind_a4_translation(host_ragged, host_state)
    host_ragged.symbols[0] = (
        int(host_ragged.symbols[0]) + 1
    ) % a4.ALPHABET_SIZE
    _assert_replication_failure_atomic(
        a4.A4SchemaError, host_ragged_binding,
        lambda: a44.paid_replication_elongation_numpy(
            host_ragged_binding, dt, world.config,
        ),
        'a44.host_post_bind_ragged',
    )
    host_ragged = exact_ragged.clone()
    host_state = exact_binding.state.clone()
    host_state_binding = a4.bind_a4_translation(host_ragged, host_state)
    host_state.pools[0, a4.a3.POOL_FUEL] += 1e-9
    _assert_replication_failure_atomic(
        a4.A4SchemaError, host_state_binding,
        lambda: a44.paid_replication_elongation_numpy(
            host_state_binding, dt, world.config,
        ),
        'a44.host_post_bind_state',
    )

    trust_device = 'cuda' if torch.cuda.is_available() else 'cpu'
    resident_ragged_binding = a4.bind_a4_translation(
        exact_ragged.to_torch(device=trust_device),
        exact_binding.state.to_torch(device=trust_device),
    )
    resident_ragged_binding.ragged.symbols[0] = torch.remainder(
        resident_ragged_binding.ragged.symbols[0].to(torch.int64) + 1,
        a4.ALPHABET_SIZE,
    ).to(torch.uint8)
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.paid_replication_elongation_torch(
            resident_ragged_binding, dt, world.config,
        ),
    )
    resident_state_binding = a4.bind_a4_translation(
        exact_ragged.to_torch(device=trust_device),
        exact_binding.state.to_torch(device=trust_device),
    )
    resident_state_binding.state.pools[0, a4.a3.POOL_FUEL] += 1e-9
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.paid_replication_elongation_torch(
            resident_state_binding, dt, world.config,
        ),
    )

    full_config = a4.GPU068A4Config(
        max_symbols=int(probe.symbol_count), **common
    )
    full_ragged, full_state, full_binding = _paid_replication_binding(
        single, world.config, full_config,
    )
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = v3.pickle_clone(world.rng.bit_generator.state)
    _assert_replication_failure_atomic(
        a4.A4CapacityError, full_binding,
        lambda: a44.paid_replication_elongation_numpy(
            full_binding, dt, world.config,
        ),
        'a44.capacity_plus_one',
    )
    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    resident_full = a4.bind_a4_translation(
        full_ragged.to_torch(device=device),
        full_state.to_torch(device=device),
    )
    resident_capacity_plan = a44.paid_replication_elongation_torch(
        resident_full, dt, world.config,
    )
    _assert_raises(a4.A4CapacityError, resident_capacity_plan.to_numpy)

    flag_cases = (
        ('genome_replication', False),
        ('mutation', True),
        ('quiescence', True),
        ('quiescence_effector', True),
    )
    for name, value in flag_cases:
        unsupported = copy.deepcopy(world.config)
        setattr(unsupported, name, value)
        _assert_replication_failure_atomic(
            a44.A4ReplicationScopeError, exact_binding,
            lambda unsupported=unsupported: a44.paid_replication_elongation_numpy(
                exact_binding, dt, unsupported,
            ),
            'a44.unsupported.%s' % name,
        )

    inactive = copy.deepcopy(cells[0])
    inactive.replication_template = None
    inactive.replication_copy = []
    inactive.replication_template_lesion = 0.0
    inactive.replication_fractional = 0.0
    _, _, inactive_binding = _paid_replication_binding(
        [inactive], world.config, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, inactive_binding,
        lambda: a44.paid_replication_elongation_numpy(
            inactive_binding, dt, world.config,
        ),
        'a44.inactive',
    )

    completing = copy.deepcopy(cells[0])
    completing.replication_copy = [
        int(value) for value in completing.replication_template[:-1]
    ]
    completing.replication_fractional = np.nextafter(1.0, 0.0)
    _, _, completing_binding = _paid_replication_binding(
        [completing], world.config, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, completing_binding,
        lambda: a44.paid_replication_elongation_numpy(
            completing_binding, dt, world.config,
        ),
        'a44.completion',
    )

    expected_status = (
        'a4.6b2-pre-existing-active-all-row-completion-combined-pcg64-'
        'substitution-structural-material-fixed-resident-plan-'
        'not-arena-committed-not-integrated-cpu-authoritative'
    )
    if a44.PORT_STATUS.get('genome_replication') != expected_status:
        raise AssertionError('A4.6b2 CPU authority status differs')
    if (a44.FULL_GPU_WORLD_STEP is not False
            or a44.PORT_STATUS.get('full_gpu_world_step') is not False):
        raise AssertionError('A4.5b claimed full GPU authority')
    if a4.PORT_STATUS.get('genome_replication') != 'cpu-authoritative-next-a4-slice':
        raise AssertionError('A4 core authority was changed')
    if getattr(v3.a3_module(), 'FULL_GPU_WORLD_STEP', None) is not False:
        raise AssertionError('A3 full-world authority changed')
    event_order = v3._event_order()
    if event_order.count('replication_cpu') != 1:
        raise AssertionError('A3 CPU replication scheduler authority changed')
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a44.fail_closed_world',
    )
    v3.assert_recursive_close(
        rng_before, world.rng.bit_generator.state, atol=0.0, rtol=0.0,
        path='a44.fail_closed_rng',
    )
    return 'future-symbol exact/+1 atomic; trust/plan rejects; 4 flags + inactive/completion; A3 CPU authority'


def test_a44b_feature_matrix_formal066_cpu_oracle():
    world, cells, config, dt = _paid_replication_b_fixture()
    if any(type(cell).__name__ != 'Formal066ProtoCell' for cell in cells):
        raise AssertionError('A4.4b CPU oracle is not Formal066')

    # Fix decoded promoter/efficiency arithmetic and the CPU multiplication
    # grouping used by repair-localised proofreading/quiescence proteins.
    repair_specs = [
        spec for spec in cells[0].gene_specs.values()
        if (int(spec['role']) == int(a4.a3.ROLE_REGULATOR)
            and int(spec['localisation']) == int(a4.s4.LOC_REPAIR))
    ]
    if not repair_specs:
        raise AssertionError('A4.4b fixture lacks repair-localised regulators')
    for spec in repair_specs:
        payload = spec['payload']
        expected_promoter = 0.18 + 1.22 * (payload[3] / 7.0)
        expected_efficiency = 0.52 + 0.96 * (payload[4] / 7.0)
        if (float(spec['promoter']) != expected_promoter
                or float(spec['efficiency']) != expected_efficiency):
            raise AssertionError('gene decode changed base + scale*(byte/7.0)')
    for kind in (a4.a3.REPAIR_PROOFREADING, a4.a3.REPAIR_QUIESCENCE):
        v3.assert_recursive_close(
            _cpu_raw_repair_activity(cells[0], kind),
            cells[0].raw_repair_activity(kind),
            atol=0.0, rtol=0.0, path='a44b.raw_repair.%d' % int(kind),
        )

    plans = {}
    for label, model_config in (
        ('proof_off', _replication_model_config(world.config)),
        ('proof_on', _replication_model_config(
            world.config, proofreading=True,
        )),
    ):
        _, _, binding = _paid_replication_binding(
            cells[:2], model_config, config,
        )
        plan = a44.paid_replication_elongation_numpy(
            binding, dt, model_config,
        )
        cpu_cells = copy.deepcopy(cells[:2])
        cpu_before = copy.deepcopy(cpu_cells)
        for cell in cpu_cells:
            cell._replicate_genome(world, dt, model_config)
        _assert_paid_replication_cpu_parity(
            plan, cpu_before, cpu_cells, 'a44b.%s' % label,
        )
        plans[label] = plan
    if not np.array_equal(
            plans['proof_off'].cumulative_proofreading_atp_after[:2],
            np.asarray([
                cells[0].cumulative_proofreading_atp,
                cells[1].cumulative_proofreading_atp,
            ], dtype=np.float64)):
        raise AssertionError('proofreading-off changed cumulative ATP')
    if not (
            float(plans['proof_on'].cumulative_proofreading_atp_after[0])
            > float(cells[0].cumulative_proofreading_atp)
            and float(plans['proof_on'].last_effective_error_rate[0])
            < float(plans['proof_off'].last_effective_error_rate[0])
            and float(plans['proof_on'].replication_fractional_after[1])
            < float(plans['proof_off'].replication_fractional_after[1])):
        raise AssertionError('proofreading speed/error/payment branches not exercised')

    all_on = _replication_model_config(
        world.config, proofreading=True, external_replicase=True,
        quiescence=True, quiescence_effector=True,
    )
    _, _, binding = _paid_replication_binding(cells[:2], all_on, config)
    combined = a44.paid_replication_elongation_numpy(binding, dt, all_on)
    cpu_cells = copy.deepcopy(cells[:2])
    cpu_before = copy.deepcopy(cpu_cells)
    for cell in cpu_cells:
        cell._replicate_genome(world, dt, all_on)
    _assert_paid_replication_cpu_parity(
        combined, cpu_before, cpu_cells, 'a44b.combined',
    )
    inherited = cells[0].quiescence_level(all_on)
    behavioural = cells[1].quiescence_level(all_on)
    if not (inherited > cells[0].behavioural_quiescence
            and behavioural == cells[1].behavioural_quiescence == 0.70):
        raise AssertionError('inherited/behavioural quiescence dominance missing')

    external = _external_only_replication_cell(cells[0])
    external_config = _replication_model_config(
        world.config, external_replicase=True,
    )
    _, _, external_binding = _paid_replication_binding(
        [external], external_config, config,
    )
    external_plan = a44.paid_replication_elongation_numpy(
        external_binding, dt, external_config,
    )
    external_cpu = [copy.deepcopy(external)]
    external_before = copy.deepcopy(external_cpu)
    external_cpu[0]._replicate_genome(world, dt, external_config)
    _assert_paid_replication_cpu_parity(
        external_plan, external_before, external_cpu, 'a44b.external_only',
    )
    no_external = _replication_model_config(world.config)
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            external_binding, dt, no_external,
        ),
    )
    return 'proof on/off + inherited/behavioural q + external-only direct Formal066 exact'


def test_a44b_proof_ledger_resource_error_and_nonmutation():
    world, cells, config, dt = _paid_replication_b_fixture(seed=7902)
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = v3.pickle_clone(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, config,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    plan = a44.paid_replication_elongation_numpy(
        binding, dt, world.config,
    )
    requested = [int(value) for value in plan.requested_symbols[:6]]
    appended = [int(value) for value in plan.append_count[:6]]
    if requested != [1, 0, 1, 1, 1, 1]:
        raise AssertionError('A4.4b requested rows differ: %s' % requested)
    if appended != [1, 0, 1, 0, 1, 0]:
        raise AssertionError('A4.4b resource gates differ: %s' % appended)

    cpu_cells = copy.deepcopy(cells)
    cpu_before = copy.deepcopy(cpu_cells)
    for cell in cpu_cells:
        cell._replicate_genome(world, dt, world.config)
    _assert_paid_replication_cpu_parity(
        plan, cpu_before, cpu_cells, 'a44b.ledger_cpu',
    )
    for ci, (before, after) in enumerate(zip(cells, cpu_cells)):
        v3.assert_recursive_close(
            float(plan.cumulative_proofreading_atp_after[ci]),
            float(after.cumulative_proofreading_atp),
            atol=0.0, rtol=0.0,
            path='a44b.cumulative_after[%d]' % ci,
        )
        if appended[ci] == 0 and (
                float(after.cumulative_proofreading_atp)
                != float(before.cumulative_proofreading_atp)):
            raise AssertionError('unaccepted row paid proofreading ATP')

    atp = int(a4.a3.POOL_ATP)
    nucleotide = int(a4.a3.POOL_NUCLEOTIDE)
    proof = _cpu_raw_repair_activity(
        cells[2], a4.a3.REPAIR_PROOFREADING,
    )
    proof_fraction = proof / (0.75 + proof)
    expected_atp_after = (
        float(cells[2].pools[atp])
        - (float(a4.g2.REPLICATION_ATP_PER_SYMBOL)
           + 0.00075 * proof_fraction)
    )
    v3.assert_recursive_close(
        float(plan.pools_after[2, atp]), expected_atp_after,
        atol=4e-18, rtol=0.0, path='a44b.proof_atp_above_guard',
    )
    if float(plan.pools_after[3, atp]) != float(cells[3].pools[atp]):
        raise AssertionError('proof ATP nextafter gate was not atomic')
    if float(plan.pools_after[4, nucleotide]) != 0.0:
        raise AssertionError('monomer exact gate did not pay to zero')
    if (float(plan.pools_after[5, nucleotide])
            != float(cells[5].pools[nucleotide])):
        raise AssertionError('monomer nextafter gate was not atomic')
    if (not np.array_equal(plan.pools_after[1], cells[1].pools)
            or float(plan.cumulative_proofreading_atp_after[1])
            != float(cells[1].cumulative_proofreading_atp)):
        raise AssertionError('requested=0 changed a paid ledger')
    if (not np.signbit(plan.cumulative_proofreading_atp_after[1])
            or not np.signbit(cpu_cells[1].cumulative_proofreading_atp)):
        raise AssertionError('requested=0 lost frozen negative-zero telemetry')

    for ci in (0, 1):
        proof = _cpu_raw_repair_activity(
            cells[ci], a4.a3.REPAIR_PROOFREADING,
        )
        proof_fraction = proof / (0.75 + proof)
        volume = max(
            0.20, (cells[ci].radius / float(a4.s5.BASE_RADIUS)) ** 2,
        )
        reactive = cells[ci].pools[a4.a3.POOL_REACTIVE] / volume
        raw_error = max(
            0.0, float(world.config.mutation_rate)
            + 0.0012 * cells[ci].replication_template_lesion
            + 0.0010 * reactive,
        )
        expected_error = raw_error * (1.0 - 0.82 * proof_fraction)
        v3.assert_recursive_close(
            float(plan.last_effective_error_rate[ci]), expected_error,
            atol=2e-15, rtol=0.0, path='a44b.error[%d]' % ci,
        )

    for label, expected, actual in (
        ('world', world_before, world.state_dict()),
        ('rng', rng_before, world.rng.bit_generator.state),
        ('ragged', ragged_before, ragged.state_dict()),
        ('state', state_before, state.state_dict()),
        ('cache', cache_before, binding.cache.state_dict()),
    ):
        v3.assert_recursive_close(
            expected, actual, atol=0.0, rtol=0.0,
            path='a44b.nonmutation.%s' % label,
        )
    return ('proof after-state + guard-separated ATP ledgers + exact monomer '
            'boundaries + lesion/reactive error; RNG/world/input exact')


def test_a44b_numpy_torch_deterministic_devices_and_pointers():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.4b')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    world, cells, config, dt = _paid_replication_b_fixture(seed=7903)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, config,
    )
    expected = a44.paid_replication_elongation_numpy(
        binding, dt, world.config,
    )
    expected_translation = a4.paid_translation_plan_numpy(binding, dt)
    boundary_dt = float.fromhex('0x1.c46204851f076p-4')
    boundary_ragged, boundary_state, boundary_binding = (
        _paid_replication_binding([cells[0]], world.config, config)
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            boundary_binding, boundary_dt, world.config,
        ),
    )
    control_dt = boundary_dt + 1e-10
    control_expected = a44.paid_replication_elongation_numpy(
        boundary_binding, control_dt, world.config,
    )
    if int(control_expected.requested_symbols[0]) != 5:
        raise AssertionError('A4.4b nonambiguous boundary control drifted')
    division_cell = copy.deepcopy(cells[0])
    division_cell.radius = float.fromhex('0x1.5eaf9b4659ac2p-5')
    division_dt = float.fromhex('0x1.c96cc2eb79450p-4')
    division_ragged, division_state, division_binding = (
        _paid_replication_binding([division_cell], world.config, config)
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            division_binding, division_dt, world.config,
        ),
    )
    if (not a44._numpy_fp64_comparison_boundary(1e-6, 1e-6)
            or a44._numpy_fp64_comparison_boundary(
                1e-6 + 1e-10, 1e-6,
            )
            or not a44._numpy_fp64_integer_boundary(
                float('nan'), float('nan'),
            )):
        raise AssertionError('NumPy derived comparison guard differs')
    proof = _cpu_raw_repair_activity(
        cells[2], a4.a3.REPAIR_PROOFREADING,
    )
    proof_fraction = proof / (0.75 + proof)
    proof_atp_gate = (
        float(a4.g2.REPLICATION_ATP_PER_SYMBOL)
        + 0.00075 * proof_fraction + 0.022
    )
    payment_boundaries = []
    for atp_value in (
            proof_atp_gate, np.nextafter(proof_atp_gate, 0.0)):
        payment_cell = copy.deepcopy(cells[2])
        payment_cell.pools[a4.a3.POOL_ATP] = atp_value
        payment_ragged, payment_state, payment_binding = (
            _paid_replication_binding(
                [payment_cell], world.config, config,
            )
        )
        _assert_raises(
            a44.A4ReplicationScopeError,
            lambda payment_binding=payment_binding:
                a44.paid_replication_elongation_numpy(
                    payment_binding, dt, world.config,
                ),
        )
        payment_boundaries.append((payment_ragged, payment_state))

    # Construct an actual plan row on the derived replicase gate while
    # preserving the active-protein pool ledger.  This pins code 6 at the
    # public plan boundary rather than testing only the comparison helper.
    replicase_cell = copy.deepcopy(cells[0])
    replicase_ragged, replicase_state, initial_replicase_binding = (
        _paid_replication_binding(
            [replicase_cell], world.config, config,
        )
    )
    replicase_specs = (
        initial_replicase_binding.cache.materialize_gene_specs_host()[0]
    )
    replicase_positions = []
    nonreplicase_positions = []
    for position in range(int(replicase_state.active_count[0])):
        fingerprint = int(
            replicase_state.active_fingerprints[0, position]
        )
        spec = replicase_specs.get(fingerprint)
        if (spec is not None
                and int(spec['role']) == int(a4.a3.ROLE_REPLICASE)):
            replicase_positions.append(position)
        else:
            nonreplicase_positions.append(position)
    if len(replicase_positions) != 1 or not nonreplicase_positions:
        raise AssertionError('replicase boundary fixture shape drifted')
    rep_position = replicase_positions[0]
    donor_position = nonreplicase_positions[0]
    rep_fingerprint = int(
        replicase_state.active_fingerprints[0, rep_position]
    )
    efficiency = float(replicase_specs[rep_fingerprint]['efficiency'])
    pools = replicase_state.pools[0]
    volume = max(
        0.20,
        (float(replicase_state.radius[0])
         / float(a4.s5.BASE_RADIUS)) ** 2,
    )
    aggregate_concentration = float(
        pools[a4.a3.POOL_AGGREGATE] / volume
    )
    active = max(0.0, float(pools[a4.a3.POOL_CATALYST]))
    damaged = float(pools[a4.a3.POOL_DAMAGED_PROTEIN])
    aggregate_pool = float(pools[a4.a3.POOL_AGGREGATE])
    functional = active / max(
        1e-9, active + damaged + aggregate_pool,
    )
    toxicity = 1.0 / (1.0 + 3.6 * aggregate_concentration)
    proteostasis = float(np.clip(functional * toxicity, 0.02, 1.0))
    genome_factor = 1.0 / (
        1.0 + 0.85 * float(replicase_state.genome_lesion_mean[0])
    )
    target_mass = (
        (1e-6 * 0.040 / (proteostasis * genome_factor)) / efficiency
    )
    old_mass = float(replicase_state.active_mass[0, rep_position])
    transfer = old_mass - target_mass
    replicase_state.active_mass[0, rep_position] = target_mass
    replicase_state.active_mass[0, donor_position] += transfer
    a4.validate_a4_translation_state(replicase_state)
    replicase_boundary_binding = a4.bind_a4_translation(
        replicase_ragged, replicase_state,
    )
    replicase_boundary_config = _replication_model_config(
        world.config, proofreading=True, external_replicase=False,
        quiescence=True, quiescence_effector=True,
    )
    observed_replicase = a44._numpy_replicase(
        replicase_boundary_binding, 0, replicase_specs,
    )
    if not a44._numpy_fp64_comparison_boundary(
            observed_replicase, 1e-6):
        raise AssertionError('actual replicase row missed comparison guard')
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            replicase_boundary_binding, dt, replicase_boundary_config,
        ),
    )

    overflow_ragged, overflow_state, initial_overflow_binding = (
        _paid_replication_binding(
            [copy.deepcopy(cells[0])], world.config, config,
        )
    )
    overflow_specs = (
        initial_overflow_binding.cache.materialize_gene_specs_host()[0]
    )
    proof_positions = []
    for position in range(int(overflow_state.active_count[0])):
        fingerprint = int(overflow_state.active_fingerprints[0, position])
        spec = overflow_specs.get(fingerprint)
        if (spec is not None
                and int(spec['role']) == int(a4.a3.ROLE_REGULATOR)
                and int(spec['localisation']) == int(a4.s4.LOC_REPAIR)
                and int(spec['parameter']) % int(a4.a3.REPAIR_COUNT)
                == int(a4.a3.REPAIR_PROOFREADING)):
            proof_positions.append(position)
    if not proof_positions:
        raise AssertionError('overflow fixture lacks proof regulator')
    overflow_state.active_mass[0, proof_positions[0]] = 1e308
    ordered_active_total = 0.0
    for position in range(int(overflow_state.active_count[0])):
        ordered_active_total += float(overflow_state.active_mass[0, position])
    overflow_state.pools[0, a4.a3.POOL_CATALYST] = ordered_active_total
    a4.validate_a4_translation_state(overflow_state)
    overflow_binding = a4.bind_a4_translation(
        overflow_ragged, overflow_state,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            overflow_binding, dt, world.config,
        ),
    )

    # Pin the late proofreading-payment boundary that motivated the 4096-eps
    # comparison band.  The ambiguous comparison occurs after 405 accepted
    # symbols: narrower 8/512/2048-eps bands allowed the CUDA row to continue
    # while NumPy/Torch CPU failed closed.  Keep this as an actual public plan
    # row, not only a helper-level comparison.
    late_payment_cell = copy.deepcopy(cells[0])
    late_proof_fingerprint = 66246424745
    late_proof_found = False
    for fingerprint, spec in late_payment_cell.gene_specs.items():
        if (int(spec['role']) == int(a4.a3.ROLE_REGULATOR)
                and int(spec['localisation']) == int(a4.s4.LOC_REPAIR)
                and int(spec['parameter']) % int(a4.a3.REPAIR_COUNT)
                == int(a4.a3.REPAIR_PROOFREADING)):
            late_payment_cell.proteins[fingerprint] = 0.0
            if int(fingerprint) == late_proof_fingerprint:
                late_proof_found = True
    if not late_proof_found:
        raise AssertionError('late payment proof fingerprint drifted')
    late_payment_cell.proteins[late_proof_fingerprint] = float.fromhex(
        '0x1.a027936981d40p-3'
    )
    late_payment_cell._sync_protein_pool()
    late_payment_cell.radius = float.fromhex('0x1.3c3336c953ab2p-6')
    late_payment_cell.pools[a4.a3.POOL_AGGREGATE] = float.fromhex(
        '0x1.c6be302cd832cp-3'
    )
    late_payment_cell.pools[a4.a3.POOL_ATP] = float.fromhex(
        '0x1.851a9a4931017p-1'
    )
    late_payment_cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
    late_payment_cell.replication_fractional = 0.2718281828459045
    if (len(late_payment_cell.replication_template) != 576
            or len(late_payment_cell.replication_copy) != 8):
        raise AssertionError('late payment template/copy shape drifted')
    late_ragged, late_state, late_binding = _paid_replication_binding(
        [late_payment_cell], world.config, config,
    )
    late_dt = 16.0
    late_specs = late_binding.cache.materialize_gene_specs_host()[0]
    late_proof = _cpu_raw_repair_activity(
        late_payment_cell, a4.a3.REPAIR_PROOFREADING,
    )
    late_fraction = late_proof / (0.75 + late_proof)
    late_gate = (
        float(a4.g2.REPLICATION_ATP_PER_SYMBOL)
        + 0.00075 * late_fraction + 0.022
    )
    if (late_gate.hex() != '0x1.863a7e0fc1aeap-6'
            or late_specs[late_proof_fingerprint]['copy_number'] < 1):
        raise AssertionError('late payment arithmetic fixture drifted')
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            late_binding, late_dt, world.config,
        ),
    )
    world_before = v3.pickle_clone(world.state_dict())
    source = '\n'.join(inspect.getsource(item) for item in (
        a44.paid_replication_elongation_torch,
        a44._torch_replicase,
        a44._torch_numpy_pairwise_sum_rows,
        a44._torch_fp64_integer_boundary,
        a44._torch_fp64_comparison_boundary,
        a4._torch_ordered_row_sum,
    ))
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(', 'cumsum(')
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident A4.4b contains host/dynamic op: %s' % found)

    deterministic_before = torch.are_deterministic_algorithms_enabled()
    warn_before = torch.is_deterministic_algorithms_warn_only_enabled()
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    try:
        torch.use_deterministic_algorithms(True)
        for device in devices:
            resident_ragged = ragged.to_torch(device=device)
            resident_state = state.to_torch(device=device)
            ragged_ptrs = resident_ragged.data_ptrs()
            state_ptrs = resident_state.data_ptrs()
            # Direct bind exercises deterministic resident gene decode.
            resident_binding = a4.bind_a4_translation(
                resident_ragged, resident_state,
            )
            cache_ptrs = resident_binding.cache.data_ptrs()
            translation = a4.paid_translation_plan_torch(
                resident_binding, dt,
            )
            plan = a44.paid_replication_elongation_torch(
                resident_binding, dt, world.config,
            )
            boundary_resident = a4.bind_a4_translation(
                boundary_ragged.to_torch(device=device),
                boundary_state.to_torch(device=device),
            )
            boundary_plan = a44.paid_replication_elongation_torch(
                boundary_resident, boundary_dt, world.config,
            )
            control_plan = a44.paid_replication_elongation_torch(
                boundary_resident, control_dt, world.config,
            ).to_numpy()
            division_resident = a4.bind_a4_translation(
                division_ragged.to_torch(device=device),
                division_state.to_torch(device=device),
            )
            division_plan = a44.paid_replication_elongation_torch(
                division_resident, division_dt, world.config,
            )
            payment_plans = []
            for payment_ragged, payment_state in payment_boundaries:
                payment_resident = a4.bind_a4_translation(
                    payment_ragged.to_torch(device=device),
                    payment_state.to_torch(device=device),
                )
                payment_plans.append(
                    a44.paid_replication_elongation_torch(
                        payment_resident, dt, world.config,
                    )
                )
            replicase_resident = a4.bind_a4_translation(
                replicase_ragged.to_torch(device=device),
                replicase_state.to_torch(device=device),
            )
            replicase_boundary_plan = a44.paid_replication_elongation_torch(
                replicase_resident, dt, replicase_boundary_config,
            )
            overflow_resident = a4.bind_a4_translation(
                overflow_ragged.to_torch(device=device),
                overflow_state.to_torch(device=device),
            )
            overflow_plan = a44.paid_replication_elongation_torch(
                overflow_resident, dt, world.config,
            )
            late_resident = a4.bind_a4_translation(
                late_ragged.to_torch(device=device),
                late_state.to_torch(device=device),
            )
            late_plan = a44.paid_replication_elongation_torch(
                late_resident, late_dt, world.config,
            )
            if not torch.are_deterministic_algorithms_enabled():
                raise AssertionError('A4.4b disabled deterministic algorithms')
            if any(getattr(plan, name).device.type != device
                   for name in a44._PLAN_ARRAY_FIELDS):
                raise AssertionError('%s A4.4b plan escaped device' % device)
            plan_ptrs = plan.data_ptrs()
            if device == 'cuda':
                torch.cuda.synchronize()
            v3.assert_recursive_close(
                expected_translation.state_dict(),
                translation.to_numpy().state_dict(),
                atol=2e-12, rtol=0.0,
                path='a44b.translation.%s' % device,
            )
            v3.assert_recursive_close(
                expected.state_dict(), plan.to_numpy().state_dict(),
                atol=2e-12, rtol=0.0, path='a44b.plan.%s' % device,
            )
            boundary_code = int(
                boundary_plan.scope_error_code.detach().cpu().numpy()[0]
            )
            division_code = int(
                division_plan.scope_error_code.detach().cpu().numpy()[0]
            )
            if (boundary_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY
                    or division_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY):
                raise AssertionError(
                    '%s fp64 integer guard codes differ: %d/%d' % (
                        device, boundary_code, division_code,
                    )
                )
            _assert_raises(
                a44.A4ReplicationScopeError, boundary_plan.to_numpy,
            )
            _assert_raises(
                a44.A4ReplicationScopeError, division_plan.to_numpy,
            )
            for payment_plan in payment_plans:
                payment_code = int(
                    payment_plan.scope_error_code.detach().cpu().numpy()[0]
                )
                if payment_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY:
                    raise AssertionError(
                        '%s proof ATP guard code differs: %d' % (
                            device, payment_code,
                        )
                    )
                _assert_raises(
                    a44.A4ReplicationScopeError, payment_plan.to_numpy,
                )
            replicase_code = int(
                replicase_boundary_plan.scope_error_code.detach().cpu()
                .numpy()[0]
            )
            if replicase_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY:
                raise AssertionError(
                    '%s actual replicase guard code differs: %d' % (
                        device, replicase_code,
                    )
                )
            if (int(replicase_boundary_plan.requested_symbols.detach().cpu()
                    .numpy()[0]) != 0
                    or int(replicase_boundary_plan.append_count.detach().cpu()
                           .numpy()[0]) != 0
                    or float(
                        replicase_boundary_plan.replication_fractional_after
                        .detach().cpu().numpy()[0]
                    ) != 0.0):
                raise AssertionError(
                    '%s replicase-boundary telemetry was speculative' % device
                )
            _assert_raises(
                a44.A4ReplicationScopeError,
                replicase_boundary_plan.to_numpy,
            )
            overflow_code = int(
                overflow_plan.scope_error_code.detach().cpu().numpy()[0]
            )
            if overflow_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY:
                raise AssertionError(
                    '%s nonfinite kinetics guard code differs: %d' % (
                        device, overflow_code,
                    )
                )
            if (int(overflow_plan.requested_symbols.detach().cpu()
                    .numpy()[0]) != 0
                    or int(overflow_plan.append_count.detach().cpu()
                           .numpy()[0]) != 0
                    or float(
                        overflow_plan.replication_fractional_after
                        .detach().cpu().numpy()[0]
                    ) != 0.0):
                raise AssertionError(
                    '%s nonfinite kinetics telemetry was speculative' % device
                )
            _assert_raises(
                a44.A4ReplicationScopeError, overflow_plan.to_numpy,
            )
            late_code = int(
                late_plan.scope_error_code.detach().cpu().numpy()[0]
            )
            if late_code != a44.SCOPE_FP64_DISCRETE_BOUNDARY:
                raise AssertionError(
                    '%s late proof-payment guard code differs: %d' % (
                        device, late_code,
                    )
                )
            if (int(late_plan.requested_symbols.detach().cpu().numpy()[0])
                    != 0
                    or int(late_plan.append_count.detach().cpu().numpy()[0])
                    != 0
                    or float(
                        late_plan.replication_fractional_after.detach().cpu()
                        .numpy()[0]
                    ) != 0.0):
                raise AssertionError(
                    '%s late proof-payment telemetry was speculative' % device
                )
            _assert_raises(
                a44.A4ReplicationScopeError, late_plan.to_numpy,
            )
            v3.assert_recursive_close(
                control_expected.state_dict(), control_plan.state_dict(),
                atol=2e-12, rtol=0.0,
                path='a44b.nonambiguous_control.%s' % device,
            )
            pairwise_values = (
                np.clip(
                    np.asarray(boundary_state.membrane_oxidation[:1]),
                    0.0, 2.5,
                )
                * np.maximum(
                    np.asarray(boundary_state.membrane[:1]), 1e-9,
                )
            )
            pairwise_expected = np.sum(
                pairwise_values, axis=1, dtype=np.float64,
            )
            pairwise_actual = a44._torch_numpy_pairwise_sum_rows(
                torch.as_tensor(
                    pairwise_values, dtype=torch.float64, device=device,
                )
            ).detach().cpu().numpy()
            if not np.array_equal(
                    pairwise_expected.view(np.uint64),
                    pairwise_actual.view(np.uint64)):
                raise AssertionError(
                    '%s NumPy pairwise fp64 fold differs' % device
                )
            comparison_probe = torch.as_tensor(
                [1e-6, 1e-6 + 1e-10],
                dtype=torch.float64, device=device,
            )
            comparison_guard = a44._torch_fp64_comparison_boundary(
                comparison_probe, 1e-6,
            ).detach().cpu().numpy()
            if not np.array_equal(
                    comparison_guard, np.asarray([True, False])):
                raise AssertionError(
                    '%s derived comparison guard differs' % device
                )
            nonfinite_guard = a44._torch_fp64_integer_boundary(
                torch.as_tensor(
                    [float('nan'), 0.25],
                    dtype=torch.float64, device=device,
                ),
                torch.as_tensor(
                    [float('nan'), 0.0],
                    dtype=torch.float64, device=device,
                ),
            ).detach().cpu().numpy()
            if not np.array_equal(
                    nonfinite_guard, np.asarray([True, False])):
                raise AssertionError(
                    '%s nonfinite progress guard differs' % device
                )
            if (ragged_ptrs != resident_ragged.data_ptrs()
                    or state_ptrs != resident_state.data_ptrs()
                    or cache_ptrs != resident_binding.cache.data_ptrs()
                    or plan_ptrs != plan.data_ptrs()):
                raise AssertionError('%s resident pointer changed' % device)
            v3.assert_recursive_close(
                ragged.state_dict(), resident_ragged.to_numpy().state_dict(),
                atol=0.0, rtol=0.0,
                path='a44b.ragged_source.%s' % device,
            )
            v3.assert_recursive_close(
                state.state_dict(), resident_state.to_numpy().state_dict(),
                atol=0.0, rtol=0.0,
                path='a44b.state_source.%s' % device,
            )
    finally:
        torch.use_deterministic_algorithms(
            deterministic_before, warn_only=warn_before,
        )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a44b.device_world',
    )
    return ('deterministic=True direct gene decode/translation/replication '
            'NumPy/Torch %s; pairwise fold exact + two fp64 integer '
            'boundaries plus first/late proof-ATP, replicase, and nonfinite '
            'comparisons '
            'fail closed + control/pointers exact') % '/'.join(devices)


def test_a44b_capacity_scope_and_a3_authority():
    world, cells, roomy_config, dt = _paid_replication_b_fixture(seed=7904)
    single = [cells[0]]
    probe = a4.FullFidelityA4GenomeAdapter(roomy_config).pack_cells(single)
    common = {
        'max_cells': 1,
        'max_sequences': int(probe.sequence_count),
        'max_sequence_symbols': int(roomy_config.max_sequence_symbols),
        'max_proteins_per_cell': int(roomy_config.max_proteins_per_cell),
    }
    exact_config = a4.GPU068A4Config(
        max_symbols=int(probe.symbol_count) + 1, **common
    )
    exact_ragged, _, exact_binding = _paid_replication_binding(
        single, world.config, exact_config,
    )
    exact_plan = a44.paid_replication_elongation_numpy(
        exact_binding, dt, world.config,
    )
    if (int(exact_ragged.symbol_capacity) != int(exact_ragged.symbol_count) + 1
            or int(exact_plan.append_count[0]) != 1):
        raise AssertionError('A4.4b exact future capacity did not pass')
    full_config = a4.GPU068A4Config(
        max_symbols=int(probe.symbol_count), **common
    )
    _, _, full_binding = _paid_replication_binding(
        single, world.config, full_config,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, full_binding,
        lambda: a44.paid_replication_elongation_numpy(
            full_binding, dt, world.config,
        ),
        'a44b.capacity_plus_one',
    )

    negative_atp = copy.deepcopy(cells[0])
    negative_atp.pools[a4.a3.POOL_ATP] = -1e-12
    negative_atp.replication_fractional = 0.5
    negative_dt = 1e6
    _, negative_state, negative_binding = _paid_replication_binding(
        [negative_atp], world.config, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, negative_binding,
        lambda: a44.paid_replication_elongation_numpy(
            negative_binding, negative_dt, world.config,
        ),
        'a44b.negative_atp',
    )
    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    negative_ragged = negative_binding.ragged.to_torch(device)
    negative_resident = a4.bind_a4_translation(
        negative_ragged, negative_state.to_torch(device),
    )
    negative_plan = a44.paid_replication_elongation_torch(
        negative_resident, negative_dt, world.config,
    )
    if (int(negative_plan.scope_error_code[0])
            != int(a44.SCOPE_NEGATIVE_ATP)
            or int(negative_plan.requested_symbols[0]) != 0
            or float(negative_plan.replication_fractional_after[0]) != 0.0):
        raise AssertionError('negative ATP entered resident rate telemetry')
    _assert_raises(a44.A4ReplicationScopeError, negative_plan.to_numpy)

    zero_genome = copy.deepcopy(cells[0])
    zero_genome.genomes = []
    zero_genome.genome_lesions = []
    zero_genome._refresh_gene_cache()
    _, _, zero_binding = _paid_replication_binding(
        [zero_genome], world.config, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, zero_binding,
        lambda: a44.paid_replication_elongation_numpy(
            zero_binding, dt, world.config,
        ),
        'a44b.zero_genome_active_template',
    )

    mismatched = copy.deepcopy(world.config)
    mismatched.quiescence = not mismatched.quiescence
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, exact_binding,
        lambda: a44.paid_replication_elongation_numpy(
            exact_binding, dt, mismatched,
        ),
        'a44b.quiescence_metadata_mismatch',
    )
    mutation = copy.deepcopy(world.config)
    mutation.mutation = True
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, exact_binding,
        lambda: a44.paid_replication_elongation_numpy(
            exact_binding, dt, mutation,
        ),
        'a44b.mutation_scope',
    )
    external_only = _external_only_replication_cell(cells[0])
    no_external = _replication_model_config(world.config)
    _, _, no_external_binding = _paid_replication_binding(
        [external_only], no_external, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, no_external_binding,
        lambda: a44.paid_replication_elongation_numpy(
            no_external_binding, dt, no_external,
        ),
        'a44b.replicase_gate',
    )

    completing = copy.deepcopy(cells[0])
    completing.replication_copy = [
        int(value) for value in completing.replication_template[:-1]
    ]
    completing.replication_fractional = np.nextafter(1.0, 0.0)
    _, _, completing_binding = _paid_replication_binding(
        [completing], world.config, roomy_config,
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, completing_binding,
        lambda: a44.paid_replication_elongation_numpy(
            completing_binding, dt, world.config,
        ),
        'a44b.completion',
    )

    completion_probe = a4.FullFidelityA4GenomeAdapter(
        roomy_config,
    ).pack_cells([completing])
    completion_capacity = a4.GPU068A4Config(
        max_cells=1,
        max_sequences=int(completion_probe.sequence_count),
        max_symbols=int(completion_probe.symbol_count),
        max_sequence_symbols=int(roomy_config.max_sequence_symbols),
        max_proteins_per_cell=int(roomy_config.max_proteins_per_cell),
    )
    completion_ragged, completion_state, completion_capacity_binding = (
        _paid_replication_binding(
            [completing], world.config, completion_capacity,
        )
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, completion_capacity_binding,
        lambda: a44.paid_replication_elongation_numpy(
            completion_capacity_binding, dt, world.config,
        ),
        'a44b.completion_capacity_priority',
    )
    completion_resident = a4.bind_a4_translation(
        completion_ragged.to_torch(device),
        completion_state.to_torch(device),
    )
    completion_capacity_plan = a44.paid_replication_elongation_torch(
        completion_resident, dt, world.config,
    )
    _assert_raises(
        a4.A4CapacityError, completion_capacity_plan.to_numpy,
    )

    forged = exact_plan.clone()
    forged.cumulative_proofreading_atp_after[0] = -1e-12
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_paid_elongation_plan(forged),
    )
    forged_atp = exact_plan.clone()
    forged_atp.pools_after[0, a4.a3.POOL_ATP] = -1e-12
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_paid_elongation_plan(forged_atp),
    )
    expected_status = (
        'a4.6b2-pre-existing-active-all-row-completion-combined-pcg64-'
        'substitution-structural-material-fixed-resident-plan-'
        'not-arena-committed-not-integrated-cpu-authoritative'
    )
    if (a44.PORT_STATUS.get('genome_replication') != expected_status
            or a44.FULL_GPU_WORLD_STEP is not False
            or a44.PORT_STATUS.get('full_gpu_world_step') is not False):
        raise AssertionError('A4.6b2 CPU authority status differs')
    if (a4.PORT_STATUS.get('genome_replication')
            != 'cpu-authoritative-next-a4-slice'):
        raise AssertionError('A4 core replication authority changed')
    if (getattr(v3.a3_module(), 'FULL_GPU_WORLD_STEP', None) is not False
            or v3._event_order().count('replication_cpu') != 1):
        raise AssertionError('A3 CPU replication authority changed')
    return 'exact/+1 and completion+capacity priority; negative ATP telemetry/zero-genome/config/mutation/replicase/completion fail closed; A3 authority'


def _a45_substitution_fixture(seed=8002):
    world, cells, capacity, _ = _paid_replication_b_fixture(seed=seed)
    world.config.mutation = True
    world.config.mutation_rate = 0.5
    return world, cells[:3], capacity, 0.5


def _a45b_template_start_fixture(seed=8201, prime_uint32=True):
    """Two inactive starts around one active row, all non-completing."""
    world, cells, capacity, _ = _paid_replication_b_fixture(seed=seed)
    world.config.mutation = True
    world.config.mutation_rate = 0.45
    selected = [copy.deepcopy(cell) for cell in cells[:3]]
    for ci in (0, 2):
        cell = selected[ci]
        if len(cell.genomes) != 1 or len(cell.genomes[0]) == 0:
            raise AssertionError('A4.5b start fixture needs one nonempty genome')
        cell.replication_template = None
        cell.replication_copy = []
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = 0.0
        cell.last_replication_symbols = 81 + ci
        cell.last_effective_error_rate = 0.75 + 0.01 * ci
    # The second start fixes the frozen missing-lesion fallback to exactly 0.
    selected[2].genome_lesions = []
    # Exercise the full PCG64 state, including a pre-existing cached uint32.
    if prime_uint32:
        world.rng.integers(0, 7)
    expected_cache = 1 if prime_uint32 else 0
    if int(world.rng.bit_generator.state['has_uint32']) != expected_cache:
        raise AssertionError('A4.5b fixture PCG64 uint32 cache differs')
    return world, selected, capacity, 0.5


def test_a45_substitution_rng_tape_formal066_oracle():
    world, cells, capacity, dt = _a45_substitution_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a44.prepare_substitution_rng_tape(
        binding, dt, world.config, rng_before,
    )
    plan = a44.paid_replication_substitution_numpy(
        binding, dt, world.config, tape,
    )

    cpu_world = copy.deepcopy(world)
    cpu_before_cells = [copy.deepcopy(cell) for cell in cpu_world.cells[:3]]
    for cell in cpu_world.cells[:3]:
        cell._replicate_genome(cpu_world, dt, cpu_world.config)
    _assert_paid_replication_cpu_parity(
        plan, cpu_before_cells, cpu_world.cells[:3], 'a45.cpu_oracle',
    )
    if tape.rng_after_state != cpu_world.rng.bit_generator.state:
        raise AssertionError('A4.5a PCG64 after-state differs from Formal066')
    if not np.array_equal(tape.draw_count, plan.append_count):
        raise AssertionError('A4.5a threshold draw count differs from append')
    if (int(np.sum(tape.substitution_count)) <= 0
            or int(np.sum(tape.substitution_count))
            >= int(np.sum(tape.draw_count))):
        raise AssertionError('A4.5a fixture lacks mixed hit/miss draws')
    if not np.any(plan.requested_symbols[:3] > plan.append_count[:3]):
        raise AssertionError('A4.5a fixture lacks resource-stopped requests')
    for ci, before in enumerate(cpu_before_cells):
        offset = len(before.replication_copy)
        count = int(plan.append_count[ci])
        template = np.asarray(before.replication_template, dtype=np.uint8)
        for rank in range(count):
            old = int(template[offset + rank])
            new = int(plan.append_symbols[ci, rank])
            if bool(tape.replacement_mask[ci, rank]) != (new != old):
                raise AssertionError('A4.5a exclude-old mapping differs')

    # mutation_rate=0 still consumes one scalar random() per accepted symbol,
    # but never consumes the conditional bounded integer draw.
    zero_world, zero_cells, zero_capacity, zero_dt = _paid_replication_fixture(
        seed=8003,
    )
    zero_world.config.mutation = True
    zero_world.config.mutation_rate = 0.0
    zero_cells[0].replication_template_lesion = 0.0
    zero_cells[0].pools[a4.a3.POOL_REACTIVE] = 0.0
    _, _, zero_binding = _paid_replication_binding(
        [zero_cells[0]], zero_world.config, zero_capacity,
    )
    zero_before = copy.deepcopy(zero_world.rng.bit_generator.state)
    zero_tape = a44.prepare_substitution_rng_tape(
        zero_binding, zero_dt, zero_world.config, zero_before,
    )
    if (int(zero_tape.draw_count[0]) <= 0
            or int(zero_tape.substitution_count[0]) != 0
            or np.any(zero_tape.replacement_mask)):
        raise AssertionError('zero-error RNG consumption differs')
    replay = np.random.Generator(np.random.PCG64())
    replay.bit_generator.state = copy.deepcopy(zero_before)
    for _ in range(int(zero_tape.draw_count[0])):
        replay.random()
    if replay.bit_generator.state != zero_tape.rng_after_state:
        raise AssertionError('zero-error threshold-only RNG state differs')

    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.5a changed the live world RNG')
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a45.source_world',
    )
    for label, expected, actual in (
        ('ragged', ragged_before, ragged.state_dict()),
        ('state', state_before, state.state_dict()),
        ('cache', cache_before, binding.cache.state_dict()),
    ):
        v3.assert_recursive_close(
            expected, actual, atol=0.0, rtol=0.0,
            path='a45.source_%s' % label,
        )
    return ('3 Formal066 rows: scalar random per paid symbol, conditional '
            'integer draws, suffix/events/PCG64 after-state exact')


def test_a45_substitution_rng_tape_numpy_torch_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.5a')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    world, cells, capacity, dt = _a45_substitution_fixture(seed=8004)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    tape = a44.prepare_substitution_rng_tape(
        binding, dt, world.config, rng_before,
    )
    expected = a44.paid_replication_substitution_numpy(
        binding, dt, world.config, tape,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    source = inspect.getsource(a44.paid_replication_substitution_torch)
    forbidden = (
        '.item(', '.cpu(', '.numpy(', '.tolist(', 'nonzero(',
        'masked_select(', 'unique(',
    )
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident substitution contains host op: %s' % found)
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        resident = a4.bind_a4_translation(resident_ragged, resident_state)
        resident_tape = tape.to_torch(device=device)
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        cache_ptrs = resident.cache.data_ptrs()
        tape_ptrs = resident_tape.data_ptrs()
        plan = a44.paid_replication_substitution_torch(
            resident, dt, world.config, resident_tape,
        )
        if any(getattr(plan, name).device.type != device
               for name in a44._PLAN_ARRAY_FIELDS):
            raise AssertionError('%s A4.5a output escaped device' % device)
        v3.assert_recursive_close(
            expected.state_dict(), plan.to_numpy().state_dict(),
            atol=2e-12, rtol=0.0, path='a45.plan.%s' % device,
        )
        v3.assert_recursive_close(
            tape.state_dict(), resident_tape.to_numpy().state_dict(),
            atol=0.0, rtol=0.0, path='a45.tape.%s' % device,
        )
        if (ragged_ptrs != resident_ragged.data_ptrs()
                or state_ptrs != resident_state.data_ptrs()
                or cache_ptrs != resident.cache.data_ptrs()
                or tape_ptrs != resident_tape.data_ptrs()):
            raise AssertionError('%s A4.5a resident pointer changed' % device)
    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('device parity changed the live RNG')
    for label, before, after in (
        ('ragged', ragged_before, ragged.state_dict()),
        ('state', state_before, state.state_dict()),
        ('cache', cache_before, binding.cache.state_dict()),
    ):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a45.device_source.%s' % label,
        )
    return 'NumPy/Torch %s fixed-tape suffix/events exact; source/RNG/pointers stable' % '/'.join(devices)


def test_a45_substitution_rng_tape_fail_closed_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.5a trust checks')
    world, cells, capacity, dt = _a45_substitution_fixture(seed=8005)
    ragged, state, binding = _paid_replication_binding(
        [cells[0]], world.config, capacity,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    tape = a44.prepare_substitution_rng_tape(
        binding, dt, world.config, rng_before,
    )

    forged = tape.clone()
    forged._factory_token = None
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_substitution_rng_tape(forged),
    )
    bad_after = tape.clone()
    bad_after.rng_after_state['state']['state'] ^= 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_substitution_rng_tape(bad_after),
    )
    bad_tail = tape.clone()
    bad_tail.uniform_draws[0, int(bad_tail.draw_count[0])] = 0.25
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_substitution_rng_tape(bad_tail),
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_substitution_numpy(
            binding, dt + 1e-6, world.config, tape,
        ),
    )
    disabled = copy.deepcopy(world.config)
    disabled.mutation = False
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_substitution_rng_tape(
            binding, dt, disabled, rng_before,
        ),
    )
    noncanonical = copy.deepcopy(rng_before)
    noncanonical['bit_generator'] = 'PCG64DXSM'
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.prepare_substitution_rng_tape(
            binding, dt, world.config, noncanonical,
        ),
    )

    # An exact threshold comparison is deliberately CPU-authoritative.
    boundary_world, boundary_cells, boundary_capacity, boundary_dt = (
        _paid_replication_fixture(seed=8006)
    )
    boundary_world.config.mutation = True
    boundary_world.config.proofreading = False
    boundary_world.config.quiescence = False
    boundary_world.config.quiescence_effector = False
    boundary_cells[0].replication_template_lesion = 0.0
    boundary_cells[0].pools[a4.a3.POOL_REACTIVE] = 0.0
    boundary_state_before = copy.deepcopy(
        boundary_world.rng.bit_generator.state,
    )
    probe = np.random.Generator(np.random.PCG64())
    probe.bit_generator.state = copy.deepcopy(boundary_state_before)
    boundary_world.config.mutation_rate = float(probe.random())
    _, _, boundary_binding = _paid_replication_binding(
        [boundary_cells[0]], boundary_world.config, boundary_capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_substitution_rng_tape(
            boundary_binding, boundary_dt, boundary_world.config,
            boundary_state_before,
        ),
    )
    if boundary_world.rng.bit_generator.state != boundary_state_before:
        raise AssertionError('ambiguous tape preparation changed live RNG')

    completing = copy.deepcopy(cells[0])
    completing.replication_copy = [
        int(value) for value in completing.replication_template[:-1]
    ]
    completing.replication_fractional = np.nextafter(1.0, 0.0)
    _, _, completing_binding = _paid_replication_binding(
        [completing], world.config, capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_substitution_rng_tape(
            completing_binding, dt, world.config, rng_before,
        ),
    )

    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    resident = a4.bind_a4_translation(
        ragged.to_torch(device=device), state.to_torch(device=device),
    )
    resident_tape = tape.to_torch(device=device)
    resident_tape.uniform_draws[0, 0] += 0.125
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.paid_replication_substitution_torch(
            resident, dt, world.config, resident_tape,
        ),
    )

    # Scalar metadata and the nested PCG64 states are part of the resident
    # attestation, not caller-editable labels around otherwise trusted arrays.
    for component in (
            'source_provenance', 'config_sha256', 'schedule_sha256',
            'rng_before_state', 'rng_after_state'):
        changed = tape.to_torch(device=device)
        if component in (
                'source_provenance', 'config_sha256', 'schedule_sha256'):
            setattr(
                changed, component,
                ('0' if getattr(changed, component)[0] != '0' else '1')
                + getattr(changed, component)[1:],
            )
        else:
            getattr(changed, component)['state']['state'] ^= 1
        _assert_raises(
            a4.A4SchemaError,
            lambda changed=changed: a44.paid_replication_substitution_torch(
                resident, dt, world.config, changed,
            ),
        )

    # A tape from the same ragged genome but a different physiology snapshot
    # must not be reusable merely because its append count happens to match.
    foreign_world, foreign_cells, foreign_capacity, foreign_dt = (
        _a45_substitution_fixture(seed=8123)
    )
    original = copy.deepcopy(foreign_cells[2])
    altered = copy.deepcopy(original)
    altered.pools[a4.a3.POOL_REACTIVE] = 1000.0
    original_ragged, original_state, _ = _paid_replication_binding(
        [original], foreign_world.config, foreign_capacity,
    )
    _, _, altered_binding = _paid_replication_binding(
        [altered], foreign_world.config, foreign_capacity,
    )
    foreign_tape = a44.prepare_substitution_rng_tape(
        altered_binding, foreign_dt, foreign_world.config,
        copy.deepcopy(foreign_world.rng.bit_generator.state),
    )
    original_binding = a4.bind_a4_translation(
        original_ragged.to_torch(device=device),
        original_state.to_torch(device=device),
    )
    foreign_result = a44.paid_replication_substitution_torch(
        original_binding, foreign_dt, foreign_world.config,
        foreign_tape.to_torch(device=device),
    )
    foreign_back = {
        'code': foreign_result.scope_error_code.detach().cpu().numpy(),
        'valid': foreign_result.scope_valid.detach().cpu().numpy(),
        'requested': foreign_result.requested_symbols.detach().cpu().numpy(),
        'append': foreign_result.append_count.detach().cpu().numpy(),
        'events': foreign_result.substitution_events.detach().cpu().numpy(),
    }
    if (int(foreign_back['code'][0])
            != int(a44.SCOPE_RNG_TAPE_MISMATCH)
            or bool(foreign_back['valid'][0])
            or any(int(foreign_back[name][0]) != 0
                   for name in ('requested', 'append', 'events'))):
        raise AssertionError('foreign physiology tape did not rollback as code 7')
    _assert_raises(a44.A4ReplicationScopeError, foreign_result.to_numpy)

    # A schedule mismatch on a row that has become independently out of scope
    # still invalidates later rows in the shared PCG64 chain.  The cause row
    # retains its primary code; otherwise-valid followers roll back as code 7.
    chain_world, chain_cells, chain_capacity, chain_dt = (
        _a45_substitution_fixture(seed=8124)
    )
    chain_ragged, chain_state, chain_binding = _paid_replication_binding(
        chain_cells[:2], chain_world.config, chain_capacity,
    )
    chain_tape = a44.prepare_substitution_rng_tape(
        chain_binding, chain_dt, chain_world.config,
        copy.deepcopy(chain_world.rng.bit_generator.state),
    )
    invalid_chain_state = chain_state.clone()
    invalid_chain_state.pools[0, int(a4.a3.POOL_ATP)] = -1e-12
    invalid_chain = a4.bind_a4_translation(
        chain_ragged.to_torch(device=device),
        invalid_chain_state.to_torch(device=device),
    )
    chain_result = a44.paid_replication_substitution_torch(
        invalid_chain, chain_dt, chain_world.config,
        chain_tape.to_torch(device=device),
    )
    chain_code = chain_result.scope_error_code.detach().cpu().numpy()
    chain_valid = chain_result.scope_valid.detach().cpu().numpy()
    chain_append = chain_result.append_count.detach().cpu().numpy()
    if (chain_code[:2].tolist() != [
            int(a44.SCOPE_NEGATIVE_ATP),
            int(a44.SCOPE_RNG_TAPE_MISMATCH),
            ] or np.any(chain_valid[:2]) or np.any(chain_append[:2] != 0)):
        raise AssertionError('shared RNG chain mismatch was not batch-atomic')
    _assert_raises(a44.A4ReplicationScopeError, chain_result.to_numpy)

    # If the current device error lies inside the registered comparison band
    # around a recorded draw, ambiguity (code 6) takes precedence over the
    # ordinary schedule mismatch and invalidates the complete RNG event.
    deterministic, _ = a44._substitution_config(world.config)
    base_plan = a44.paid_replication_elongation_numpy(
        binding, dt, deterministic,
    )
    ranks = [
        rank for rank in range(int(tape.draw_count[0]))
        if float(tape.uniform_draws[0, rank])
        > float(base_plan.last_effective_error_rate[0])
    ]
    if not ranks:
        raise AssertionError('A4.5a fixture lacks a tunable comparison draw')
    target = float(tape.uniform_draws[0, ranks[0]])
    tuned_state = state.clone()
    reactive_index = int(a4.a3.POOL_REACTIVE)
    original_reactive = float(tuned_state.pools[0, reactive_index])
    probe_state = state.clone()
    probe_state.pools[0, reactive_index] = original_reactive + 1.0
    probe_binding = a4.bind_a4_translation(ragged, probe_state)
    probe_error = float(a44.paid_replication_elongation_numpy(
        probe_binding, dt, deterministic,
    ).last_effective_error_rate[0])
    base_error = float(base_plan.last_effective_error_rate[0])
    slope = probe_error - base_error
    if not slope > 0.0:
        raise AssertionError('reactive/error tuning slope is not positive')
    tuned_state.pools[0, reactive_index] = (
        original_reactive + (target - base_error) / slope
    )
    tuned_binding = a4.bind_a4_translation(ragged, tuned_state)
    tuned_error = float(a44.paid_replication_elongation_numpy(
        tuned_binding, dt, deterministic,
    ).last_effective_error_rate[0])
    if not a44._numpy_fp64_comparison_boundary(target, tuned_error):
        raise AssertionError('failed to construct device comparison boundary')
    resident_tuned = a4.bind_a4_translation(
        ragged.to_torch(device=device), tuned_state.to_torch(device=device),
    )
    boundary_result = a44.paid_replication_substitution_torch(
        resident_tuned, dt, world.config, tape.to_torch(device=device),
    )
    boundary_back = {
        'code': boundary_result.scope_error_code.detach().cpu().numpy(),
        'valid': boundary_result.scope_valid.detach().cpu().numpy(),
        'requested': boundary_result.requested_symbols.detach().cpu().numpy(),
        'append': boundary_result.append_count.detach().cpu().numpy(),
        'events': boundary_result.substitution_events.detach().cpu().numpy(),
    }
    if (int(boundary_back['code'][0])
            != int(a44.SCOPE_FP64_DISCRETE_BOUNDARY)
            or bool(boundary_back['valid'][0])
            or any(int(boundary_back[name][0]) != 0
                   for name in ('requested', 'append', 'events'))):
        raise AssertionError('device decision boundary did not rollback as code 6')
    _assert_raises(a44.A4ReplicationScopeError, boundary_result.to_numpy)
    if (world.rng.bit_generator.state != rng_before
            or a44.FULL_GPU_WORLD_STEP is not False
            or a44.PORT_STATUS.get('full_gpu_world_step') is not False
            or v3._event_order().count('replication_cpu') != 1):
        raise AssertionError('A4.5a changed RNG or A3 CPU authority')
    return ('forged/state/tail/dt/config/PCG64/scalar-attestation/'
            'foreign-state-code7/device-boundary-code6/completion/resident '
            'mutation rejected; A3 replication_cpu authority retained')


def test_a45b_template_start_formal066_oracle_and_rng_order():
    world, cells, capacity, dt = _a45b_template_start_fixture()
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    source_before = [v3.pickle_clone(cell.state_dict()) for cell in cells]
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a44.prepare_substitution_rng_tape(
        binding, dt, world.config, rng_before,
    )
    plan = a44.paid_replication_substitution_numpy(
        binding, dt, world.config, tape,
    )
    expected_start = np.asarray((True, False, True), dtype=bool)
    expected_index = np.asarray((0, -1, 0), dtype=np.int64)
    if (not np.array_equal(plan.template_start_events[:3], expected_start)
            or not np.array_equal(tape.template_start_mask[:3], expected_start)
            or not np.array_equal(
                plan.selected_template_indices[:3], expected_index,
            )
            or not np.array_equal(
                tape.template_selection_indices[:3], expected_index,
            )):
        raise AssertionError('A4.5b template-start schedule differs')
    for ci in (0, 2):
        if (int(plan.template_storage_symbols[ci]) != len(cells[ci].genomes[0])
                or int(plan.append_count[ci]) <= 0
                or int(plan.append_count[ci]) >= len(cells[ci].genomes[0])):
            raise AssertionError('A4.5b start topology is not non-completing')

    cpu_world = copy.deepcopy(world)
    cpu_world.cells = [copy.deepcopy(cell) for cell in cells]
    cpu_before_cells = [copy.deepcopy(cell) for cell in cpu_world.cells]
    for cell in cpu_world.cells:
        cell._replicate_genome(cpu_world, dt, cpu_world.config)
    _assert_paid_replication_cpu_parity(
        plan, cpu_before_cells, cpu_world.cells, 'a45b.cpu_oracle',
    )
    if (float(cpu_world.cells[0].replication_template_lesion)
            != float(cells[0].genome_lesions[0])
            or float(cpu_world.cells[2].replication_template_lesion) != 0.0):
        raise AssertionError('A4.5b selected-lesion/fallback differs')
    if tape.rng_after_state != cpu_world.rng.bit_generator.state:
        raise AssertionError('A4.5b PCG64 after-state differs from Formal066')

    # Independently replay each cell's literal high-level call order.  The
    # index-zero selection is state-neutral on this NumPy/PCG64, including the
    # primed uint32 cache, but remains an explicit event in the tape.
    replay = np.random.Generator(np.random.PCG64())
    replay.bit_generator.state = copy.deepcopy(rng_before)
    for ci in range(3):
        if bool(tape.template_start_mask[ci]):
            selection_before = copy.deepcopy(replay.bit_generator.state)
            selected = int(replay.integers(0, 1))
            if (selected != 0
                    or replay.bit_generator.state != selection_before):
                raise AssertionError('A4.5b index-zero selection replay differs')
        for rank in range(int(tape.draw_count[ci])):
            uniform = float(replay.random())
            if (np.float64(uniform).view(np.uint64)
                    != np.float64(tape.uniform_draws[ci, rank]).view(np.uint64)):
                raise AssertionError('A4.5b threshold interleave differs')
            if bool(tape.replacement_mask[ci, rank]):
                raw = int(replay.integers(0, 7))
                if raw != int(tape.replacement_raw[ci, rank]):
                    raise AssertionError('A4.5b integer interleave differs')
    if replay.bit_generator.state != tape.rng_after_state:
        raise AssertionError('A4.5b explicit replay after-state differs')

    # Repeat one start from an ordinary PCG64 state with no cached uint32, so
    # both canonical cache states are bound by the direct Formal066 oracle.
    ordinary_world, ordinary_cells, ordinary_capacity, ordinary_dt = (
        _a45b_template_start_fixture(seed=8204, prime_uint32=False)
    )
    ordinary_before_rng = copy.deepcopy(
        ordinary_world.rng.bit_generator.state,
    )
    _, _, ordinary_binding = _paid_replication_binding(
        [ordinary_cells[0]], ordinary_world.config, ordinary_capacity,
    )
    ordinary_tape = a44.prepare_substitution_rng_tape(
        ordinary_binding, ordinary_dt, ordinary_world.config,
        ordinary_before_rng,
    )
    ordinary_plan = a44.paid_replication_substitution_numpy(
        ordinary_binding, ordinary_dt, ordinary_world.config, ordinary_tape,
    )
    ordinary_cpu = copy.deepcopy(ordinary_world)
    ordinary_cpu.cells = [copy.deepcopy(ordinary_cells[0])]
    ordinary_cpu_before = [copy.deepcopy(ordinary_cpu.cells[0])]
    ordinary_cpu.cells[0]._replicate_genome(
        ordinary_cpu, ordinary_dt, ordinary_cpu.config,
    )
    _assert_paid_replication_cpu_parity(
        ordinary_plan, ordinary_cpu_before, ordinary_cpu.cells,
        'a45b.ordinary_cache_cpu_oracle',
    )
    if (ordinary_tape.rng_after_state
            != ordinary_cpu.rng.bit_generator.state):
        raise AssertionError('A4.5b ordinary-cache PCG64 after-state differs')
    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.5b changed the live world RNG')
    for ci, (before, cell) in enumerate(zip(source_before, cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), atol=0.0, rtol=0.0,
            path='a45b.source_cell[%d]' % ci,
        )
    for label, before, after in (
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a45b.source_%s' % label,
        )
    return ('inactive/active/inactive cell order: index-zero start -> scalar '
            'threshold -> conditional integer; Formal066 state and PCG64 exact')


def test_a45b_template_start_exact_capacity_and_zero_dt():
    world, cells, roomy, _ = _a45b_template_start_fixture(seed=8202)
    cell = copy.deepcopy(cells[0])
    rng_before = copy.deepcopy(world.rng.bit_generator.state)

    # Even dt=0 starts the frozen template and needs two sequence slots plus a
    # byte-exact template arena copy, while drawing no substitution threshold.
    ragged, _, binding = _paid_replication_binding(
        [cell], world.config, roomy,
    )
    zero_tape = a44.prepare_substitution_rng_tape(
        binding, 0.0, world.config, rng_before,
    )
    zero_plan = a44.paid_replication_substitution_numpy(
        binding, 0.0, world.config, zero_tape,
    )
    zero_q = int(ragged.sequence_count) + 2
    zero_s = int(ragged.symbol_count) + len(cell.genomes[0])
    if (not bool(zero_plan.template_start_events[0])
            or int(zero_plan.append_count[0]) != 0
            or int(zero_tape.draw_count[0]) != 0
            or zero_tape.rng_after_state != rng_before):
        raise AssertionError('A4.5b dt=0 start semantics differ')

    def capacity_config(sequences, symbols):
        return a4.GPU068A4Config(
            max_cells=1, max_sequences=int(sequences),
            max_symbols=int(symbols),
            max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
            max_proteins_per_cell=64,
        )

    exact_zero = capacity_config(zero_q, zero_s)
    exact_zero_ragged, _, exact_zero_binding = _paid_replication_binding(
        [cell], world.config, exact_zero,
    )
    exact_zero_plan = a44.paid_replication_substitution_numpy(
        exact_zero_binding, 0.0, world.config,
        a44.prepare_substitution_rng_tape(
            exact_zero_binding, 0.0, world.config, rng_before,
        ),
    )
    if (int(exact_zero_ragged.sequence_capacity) != zero_q
            or int(exact_zero_ragged.symbol_capacity) != zero_s
            or not bool(exact_zero_plan.template_start_events[0])):
        raise AssertionError('A4.5b dt=0 exact capacity was not accepted')
    short_q = capacity_config(zero_q - 1, zero_s)
    short_q_ragged, short_q_state, short_q_binding = _paid_replication_binding(
        [cell], world.config, short_q,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, short_q_binding,
        lambda: a44.prepare_substitution_rng_tape(
            short_q_binding, 0.0, world.config, rng_before,
        ),
        'a45b.sequence_capacity_short',
    )

    # A nonzero call additionally accounts for every paid copy symbol.
    paid_tape = a44.prepare_substitution_rng_tape(
        binding, 0.5, world.config, rng_before,
    )
    paid_plan = a44.paid_replication_substitution_numpy(
        binding, 0.5, world.config, paid_tape,
    )
    if int(paid_plan.append_count[0]) <= 0:
        raise AssertionError('A4.5b capacity fixture produced no paid append')
    paid_q = int(ragged.sequence_count) + 2
    paid_s = (
        int(ragged.symbol_count) + len(cell.genomes[0])
        + int(paid_plan.append_count[0])
    )
    exact_paid = capacity_config(paid_q, paid_s)
    exact_paid_ragged, exact_paid_state, exact_paid_binding = _paid_replication_binding(
        [cell], world.config, exact_paid,
    )
    exact_paid_tape = a44.prepare_substitution_rng_tape(
        exact_paid_binding, 0.5, world.config, rng_before,
    )
    exact_paid_plan = a44.paid_replication_substitution_numpy(
        exact_paid_binding, 0.5, world.config,
        exact_paid_tape,
    )
    if (int(exact_paid_ragged.symbol_capacity) != paid_s
            or int(exact_paid_plan.append_count[0])
            != int(paid_plan.append_count[0])):
        raise AssertionError('A4.5b paid exact capacity differs')
    short_s = capacity_config(paid_q, paid_s - 1)
    short_s_ragged, short_s_state, short_s_binding = _paid_replication_binding(
        [cell], world.config, short_s,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, short_s_binding,
        lambda: a44.prepare_substitution_rng_tape(
            short_s_binding, 0.5, world.config, rng_before,
        ),
        'a45b.symbol_capacity_short',
    )
    if torch is None:
        raise AssertionError('PyTorch is required for A4.5b capacity checks')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    deterministic, _ = a44._substitution_config(world.config)
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        exact_resident = a4.bind_a4_translation(
            exact_paid_ragged.to_torch(device=device),
            exact_paid_state.to_torch(device=device),
        )
        exact_result = a44.paid_replication_substitution_torch(
            exact_resident, 0.5, world.config,
            exact_paid_tape.to_torch(device=device),
        )
        v3.assert_recursive_close(
            exact_paid_plan.state_dict(), exact_result.to_numpy().state_dict(),
            atol=2e-12, rtol=0.0,
            path='a45b.exact_capacity.%s' % device,
        )
        for label, short_ragged, short_state, case_dt in (
                ('sequence', short_q_ragged, short_q_state, 0.0),
                ('symbol', short_s_ragged, short_s_state, 0.5)):
            short_resident = a4.bind_a4_translation(
                short_ragged.to_torch(device=device),
                short_state.to_torch(device=device),
            )
            short_plan = a44._paid_replication_elongation_torch(
                short_resident, case_dt, deterministic,
                allow_template_start=True,
            )
            code = short_plan.scope_error_code.detach().cpu().numpy()
            valid = short_plan.scope_valid.detach().cpu().numpy()
            starts = short_plan.template_start_events.detach().cpu().numpy()
            indices = short_plan.selected_template_indices.detach().cpu().numpy()
            storage = short_plan.template_storage_symbols.detach().cpu().numpy()
            if (int(code[0]) != int(a44.SCOPE_CAPACITY)
                    or bool(valid[0]) or bool(starts[0])
                    or int(indices[0]) != -1 or int(storage[0]) != 0):
                raise AssertionError(
                    '%s %s capacity did not fail closed as code 4' % (
                        device, label,
                    )
                )
            _assert_raises(a4.A4CapacityError, short_plan.to_numpy)
    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.5b capacity checks changed live RNG')
    return ('dt=0 +2 sequences/template bytes exact; paid template+append '
            'symbols exact; each one-short capacity fails atomically')


def test_a45b_template_start_numpy_torch_trust_and_scope():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.5b')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    world, cells, capacity, dt = _a45b_template_start_fixture(seed=8203)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    tape = a44.prepare_substitution_rng_tape(
        binding, dt, world.config, rng_before,
    )
    expected = a44.paid_replication_substitution_numpy(
        binding, dt, world.config, tape,
    )
    deterministic, _ = a44._substitution_config(world.config)
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            binding, dt, deterministic,
        ),
    )
    _assert_raises(
        TypeError,
        lambda: a44.paid_replication_elongation_numpy(
            binding, dt, deterministic, allow_template_start=True,
        ),
    )
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        resident = a4.bind_a4_translation(resident_ragged, resident_state)
        resident_tape = tape.to_torch(device=device)
        pointers = (
            resident_ragged.data_ptrs(), resident_state.data_ptrs(),
            resident.cache.data_ptrs(), resident_tape.data_ptrs(),
        )
        result = a44.paid_replication_substitution_torch(
            resident, dt, world.config, resident_tape,
        )
        public_base = a44.paid_replication_elongation_torch(
            resident, dt, deterministic,
        )
        public_code = public_base.scope_error_code.detach().cpu().numpy()
        public_start = public_base.template_start_events.detach().cpu().numpy()
        if (int(public_code[0]) != int(a44.SCOPE_INACTIVE_TEMPLATE)
                or bool(public_start[0])):
            raise AssertionError(
                '%s public deterministic path minted a template start' % device
            )
        _assert_raises(a44.A4ReplicationScopeError, public_base.to_numpy)
        v3.assert_recursive_close(
            expected.state_dict(), result.to_numpy().state_dict(),
            atol=2e-12, rtol=0.0, path='a45b.device.%s' % device,
        )
        if pointers != (
                resident_ragged.data_ptrs(), resident_state.data_ptrs(),
                resident.cache.data_ptrs(), resident_tape.data_ptrs()):
            raise AssertionError('%s A4.5b resident pointer changed' % device)
        changed = tape.to_torch(device=device)
        changed.template_selection_indices[0] = 1
        _assert_raises(
            a4.A4SchemaError,
            lambda changed=changed: a44.paid_replication_substitution_torch(
                resident, dt, world.config, changed,
            ),
        )

    changed_host = tape.clone()
    changed_host.template_start_mask[0] = False
    changed_host.template_selection_indices[0] = -1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_substitution_rng_tape(changed_host),
    )

    # Removing an index-zero selection call remains a fully replayable PCG64
    # tape on the recorded NumPy implementation because that high-level call is
    # state-neutral.  It is nevertheless the wrong biological schedule and
    # must become a batch-wide code 7 on every resident backend.
    alternate_values = tape.state_dict()
    alternate_values['template_start_mask'][0] = False
    alternate_values['template_selection_indices'][0] = -1
    alternate_values['schedule_sha256'] = '0' * 64
    alternate_draft = a44._make_rng_tape(**alternate_values)
    alternate_values['schedule_sha256'] = a44._rng_schedule_digest(
        alternate_draft,
    )
    alternate = a44.validate_a4_substitution_rng_tape(
        a44._make_rng_tape(**alternate_values),
    )
    for device in devices:
        alternate_resident = a4.bind_a4_translation(
            ragged.to_torch(device=device), state.to_torch(device=device),
        )
        alternate_result = a44.paid_replication_substitution_torch(
            alternate_resident, dt, world.config,
            alternate.to_torch(device=device),
        )
        code = alternate_result.scope_error_code.detach().cpu().numpy()
        valid = alternate_result.scope_valid.detach().cpu().numpy()
        requested = alternate_result.requested_symbols.detach().cpu().numpy()
        appended = alternate_result.append_count.detach().cpu().numpy()
        starts = alternate_result.template_start_events.detach().cpu().numpy()
        indices = (
            alternate_result.selected_template_indices.detach().cpu().numpy()
        )
        storage = (
            alternate_result.template_storage_symbols.detach().cpu().numpy()
        )
        if (code[:3].tolist() != [int(a44.SCOPE_RNG_TAPE_MISMATCH)] * 3
                or np.any(valid[:3]) or np.any(requested[:3] != 0)
                or np.any(appended[:3] != 0) or np.any(starts[:3])
                or np.any(indices[:3] != -1) or np.any(storage[:3] != 0)):
            raise AssertionError(
                '%s A4.5b alternate start schedule did not rollback' % device
            )
        _assert_raises(
            a44.A4ReplicationScopeError, alternate_result.to_numpy,
        )

    # Broader ordinary no-op batching and mutation-free starts remain explicit
    # CPU authority until prior telemetry and topology commit are represented.
    scope_cells = []
    zero = copy.deepcopy(cells[0])
    zero.genomes = []
    zero.genome_lesions = []
    zero.replication_template = None
    zero.replication_copy = []
    zero.replication_template_lesion = 0.0
    zero.replication_fractional = 0.0
    zero._refresh_gene_cache()
    scope_cells.append(('zero_genomes', zero, world.config, dt))
    two = copy.deepcopy(cells[0])
    two.replication_template = None
    two.replication_copy = []
    two.replication_template_lesion = 0.0
    two.replication_fractional = 0.0
    two.genomes = [
        np.asarray(two.genomes[0], dtype=np.uint8).copy(),
        np.asarray(two.genomes[0], dtype=np.uint8).copy(),
    ]
    two.genome_lesions = [0.1, 0.2]
    two._refresh_gene_cache()
    scope_cells.append(('two_genomes', two, world.config, dt))
    gated = _external_only_replication_cell(cells[0])
    gated.replication_template = None
    gated.replication_copy = []
    gated.replication_template_lesion = 0.0
    gated.replication_fractional = 0.0
    gated_config = copy.deepcopy(world.config)
    gated_config.external_replicase = False
    scope_cells.append(('replicase_gate', gated, gated_config, dt))
    for label, cell, model_config, case_dt in scope_cells:
        _, _, case_binding = _paid_replication_binding(
            [cell], model_config, capacity,
        )
        _assert_replication_failure_atomic(
            a44.A4ReplicationScopeError, case_binding,
            lambda case_binding=case_binding, model_config=model_config,
                    case_dt=case_dt: a44.prepare_substitution_rng_tape(
                        case_binding, case_dt, model_config, rng_before,
                    ),
            'a45b.scope.%s' % label,
        )
    mutation_off = copy.deepcopy(world.config)
    mutation_off.mutation = False
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_substitution_rng_tape(
            binding, dt, mutation_off, rng_before,
        ),
    )

    completing = copy.deepcopy(cells[0])
    completing.genomes = [np.asarray((3,), dtype=np.uint8)]
    completing.genome_lesions = [0.2]
    completing.replication_template = None
    completing.replication_copy = []
    completing.replication_template_lesion = 0.0
    completing.replication_fractional = 0.0
    completing._refresh_gene_cache()
    completing_config = copy.deepcopy(world.config)
    completing_config.external_replicase = True
    completion_ragged, completion_state, completion_binding = (
        _paid_replication_binding(
        [completing], completing_config, capacity,
        )
    )
    _assert_replication_failure_atomic(
        a44.A4ReplicationScopeError, completion_binding,
        lambda: a44.prepare_substitution_rng_tape(
            completion_binding, 100.0, completing_config, rng_before,
        ),
        'a45b.start_completion',
    )
    completion_deterministic, _ = a44._substitution_config(completing_config)
    for device in devices:
        completion_resident = a4.bind_a4_translation(
            completion_ragged.to_torch(device=device),
            completion_state.to_torch(device=device),
        )
        completion_plan = a44._paid_replication_elongation_torch(
            completion_resident, 100.0, completion_deterministic,
            allow_template_start=True,
        )
        code = completion_plan.scope_error_code.detach().cpu().numpy()
        valid = completion_plan.scope_valid.detach().cpu().numpy()
        starts = completion_plan.template_start_events.detach().cpu().numpy()
        indices = (
            completion_plan.selected_template_indices.detach().cpu().numpy()
        )
        storage = (
            completion_plan.template_storage_symbols.detach().cpu().numpy()
        )
        if (int(code[0]) != int(a44.SCOPE_COMPLETION)
                or bool(valid[0]) or bool(starts[0])
                or int(indices[0]) != -1 or int(storage[0]) != 0):
            raise AssertionError(
                '%s A4.5b start completion did not remain code 3' % device
            )
        _assert_raises(
            a44.A4ReplicationScopeError, completion_plan.to_numpy,
        )
    if (world.rng.bit_generator.state != rng_before
            or a44.FULL_GPU_WORLD_STEP is not False
            or a44.PORT_STATUS.get('full_gpu_world_step') is not False
            or v3._event_order().count('replication_cpu') != 1):
        raise AssertionError('A4.5b changed RNG or A3 CPU authority')
    return ('NumPy/Torch %s topology+tape exact; start trust tampering, '
            'len0/len2/gate/mutation-off/completion rejected; A3 authority' %
            '/'.join(devices))


def test_a46a_completion_formal066_oracle_and_conceptual_poststate():
    world, cells, capacity, dt = _paid_completion_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = v3.pickle_clone(world.rng.bit_generator.state)
    source_before = [v3.pickle_clone(cell.state_dict()) for cell in cells]
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    plan = a44.paid_replication_completion_plan(
        binding, dt, world.config,
    )

    cpu_cells = copy.deepcopy(cells)
    cpu_before = copy.deepcopy(cpu_cells)
    for cell in cpu_cells:
        cell._replicate_genome(world, dt, world.config)
    _assert_completion_cpu_parity(
        plan, cpu_before, cpu_cells, 'a46a.formal066', atol=0.0,
    )

    for ci, before in enumerate(cells):
        proof = _cpu_raw_repair_activity(
            before, a4.a3.REPAIR_PROOFREADING,
        )
        proof_fraction = proof / (0.75 + proof)
        length = int(plan.completed_lengths[ci])
        expected_lesion = float(before.replication_template_lesion) * (
            0.28 + 0.22 * (1.0 - proof_fraction)
        ) + float(plan.last_effective_error_rate[ci]) * length * 0.06
        v3.assert_recursive_close(
            expected_lesion, float(plan.new_genome_lesions[ci]),
            atol=0.0, rtol=0.0,
            path='a46a.lesion_grouping[%d]' % ci,
        )

    # Treat the direct CPU result only as a conceptual post-state.  The A4.6a
    # plan itself must not mutate/re-attest the authoritative resident arena.
    post_ragged = a4.FullFidelityA4GenomeAdapter(capacity).pack_cells(cpu_cells)
    if (int(post_ragged.sequence_count)
            != int(ragged.sequence_count)
            + int(np.sum(plan.topology_sequence_deltas[:2], dtype=np.int64))
            or int(post_ragged.symbol_count)
            != int(ragged.symbol_count)
            + int(np.sum(plan.topology_symbol_deltas[:2], dtype=np.int64))
            or int(post_ragged.lesion_count)
            != int(ragged.lesion_count) + 2):
        raise AssertionError('A4.6a conceptual topology delta differs')
    post_cache = a4.decode_a4_gene_cache_numpy(post_ragged)
    materialized = post_cache.materialize_gene_specs_host()
    for ci, cell in enumerate(cpu_cells):
        _assert_gene_specs_equal(
            cell.gene_specs, materialized[ci],
            'a46a.conceptual_cache[%d]' % ci,
        )

    for ci, (before, cell) in enumerate(zip(source_before, cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), atol=0.0, rtol=0.0,
            path='a46a.source_cell[%d]' % ci,
        )
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('rng', rng_before, world.rng.bit_generator.state),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a46a.nonmutation.%s' % label,
        )
    return ('2 Formal066 completions: paid partial+suffix, lesion/cycle/reset, '
            'conceptual topology/cache exact; source and RNG unchanged')


def test_a46a_completion_numpy_torch_devices_and_nonmutation():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.6a')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    world, cells, capacity, dt = _paid_completion_fixture(seed=8302)
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = v3.pickle_clone(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    expected = a44.paid_replication_completion_plan(
        binding, dt, world.config,
    )
    source = '\n'.join(inspect.getsource(item) for item in (
        a44.paid_replication_completion_plan,
        a44._paid_replication_completion_torch,
        a44._paid_replication_elongation_torch,
        a44._torch_replicase,
        a44._validate_plan_metadata,
    ))
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(')
    found = [token for token in forbidden if token in source]
    if found:
        raise AssertionError('resident A4.6a contains host/dynamic op: %s' % found)

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        resident_binding = a4.bind_a4_translation(
            resident_ragged, resident_state,
        )
        cache_ptrs = resident_binding.cache.data_ptrs()
        plan = a44.paid_replication_completion_plan(
            resident_binding, dt, world.config,
        )
        plan_ptrs = plan.data_ptrs()
        if any(getattr(plan, name).device.type != device
               for name in a44._PLAN_ARRAY_FIELDS):
            raise AssertionError('%s A4.6a output escaped device' % device)
        if device == 'cuda':
            torch.cuda.synchronize()
        back = plan.to_numpy()
        v3.assert_recursive_close(
            expected.state_dict(), back.state_dict(),
            atol=2e-12, rtol=0.0, path='a46a.plan.%s' % device,
        )
        if (ragged_ptrs != resident_ragged.data_ptrs()
                or state_ptrs != resident_state.data_ptrs()
                or cache_ptrs != resident_binding.cache.data_ptrs()
                or plan_ptrs != plan.data_ptrs()):
            raise AssertionError('%s A4.6a resident pointer changed' % device)
        v3.assert_recursive_close(
            ragged.state_dict(), resident_ragged.to_numpy().state_dict(),
            atol=0.0, rtol=0.0,
            path='a46a.ragged_source.%s' % device,
        )
        v3.assert_recursive_close(
            state.state_dict(), resident_state.to_numpy().state_dict(),
            atol=0.0, rtol=0.0,
            path='a46a.state_source.%s' % device,
        )
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a46a.device_world',
    )
    v3.assert_recursive_close(
        rng_before, world.rng.bit_generator.state, atol=0.0, rtol=0.0,
        path='a46a.device_rng',
    )
    return ('NumPy/Torch %s fp64 completion descriptor exact; fixed device '
            'outputs, pointers, inputs, world, and RNG unchanged' %
            '/'.join(devices))


def test_a46a_completion_scope_schema_and_a3_authority():
    world, cells, capacity, dt = _paid_completion_fixture(seed=8303)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    valid = a44.paid_replication_completion_plan(
        binding, dt, world.config,
    )

    # Legacy public paths must continue to route completion to CPU authority.
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_elongation_numpy(
            binding, dt, world.config,
        ),
    )
    device = 'cuda' if torch is not None and torch.cuda.is_available() else 'cpu'
    resident = a4.bind_a4_translation(
        ragged.to_torch(device=device), state.to_torch(device=device),
    )
    legacy = a44.paid_replication_elongation_torch(
        resident, dt, world.config,
    )
    legacy_codes = legacy.scope_error_code.detach().cpu().numpy()[:2]
    if not np.all(legacy_codes == int(a44.SCOPE_COMPLETION)):
        raise AssertionError('legacy completion did not remain code 3')
    _assert_raises(a44.A4ReplicationScopeError, legacy.to_numpy)

    # A mixed completion/noncompletion batch is one atomic descriptor event.
    _, ordinary, _, ordinary_dt = _paid_replication_b_fixture(seed=8304)
    mixed_cells = [copy.deepcopy(cells[0]), copy.deepcopy(ordinary[1])]
    mixed_ragged, mixed_state, mixed_binding = _paid_replication_binding(
        mixed_cells, world.config, capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_completion_plan(
            mixed_binding, ordinary_dt, world.config,
        ),
    )
    mixed_resident = a4.bind_a4_translation(
        mixed_ragged.to_torch(device=device),
        mixed_state.to_torch(device=device),
    )
    mixed_plan = a44.paid_replication_completion_plan(
        mixed_resident, ordinary_dt, world.config,
    )
    mixed_code = mixed_plan.scope_error_code.detach().cpu().numpy()[:2]
    mixed_valid = mixed_plan.scope_valid.detach().cpu().numpy()[:2]
    if (np.any(mixed_valid)
            or mixed_code.tolist() != [
                int(a44.SCOPE_NONCOMPLETION),
                int(a44.SCOPE_NONCOMPLETION),
            ]
            or bool(torch.any(mixed_plan.completion_events).detach().cpu())
            or int(torch.sum(mixed_plan.append_count).detach().cpu()) != 0
            or int(torch.sum(mixed_plan.completed_lengths).detach().cpu()) != 0):
        raise AssertionError('mixed completion batch did not roll back atomically')
    _assert_raises(a44.A4ReplicationScopeError, mixed_plan.to_numpy)

    mutation = copy.deepcopy(world.config)
    mutation.mutation = True
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_completion_plan(binding, dt, mutation),
    )
    inactive = copy.deepcopy(cells[0])
    inactive.replication_template = None
    inactive.replication_copy = []
    inactive.replication_template_lesion = 0.0
    inactive.replication_fractional = 0.0
    _, _, inactive_binding = _paid_replication_binding(
        [inactive], world.config, capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.paid_replication_completion_plan(
            inactive_binding, dt, world.config,
        ),
    )

    corruptions = []
    def add(name, mutate):
        item = valid.clone()
        mutate(item)
        corruptions.append((name, item))

    add('completion_event', lambda x: x.completion_events.__setitem__(0, False))
    add('completed_length', lambda x: x.completed_lengths.__setitem__(0, 0))
    add('completed_tail', lambda x: x.completed_symbols.__setitem__(
        (0, int(x.completed_lengths[0])), 1,
    ))
    add('completed_suffix', lambda x: x.completed_symbols.__setitem__(
        (0, int(x.completed_lengths[0]) - 1),
        (int(x.completed_symbols[0, int(x.completed_lengths[0]) - 1]) + 1)
        % a4.ALPHABET_SIZE,
    ))
    add('new_lesion_negative', lambda x: x.new_genome_lesions.__setitem__(0, -1.0))
    add('new_lesion_nonfinite', lambda x: x.new_genome_lesions.__setitem__(0, np.nan))
    add('cycle_delta', lambda x: x.replication_cycle_deltas.__setitem__(0, 0))
    add('sequence_delta', lambda x: x.topology_sequence_deltas.__setitem__(0, 0))
    add('symbol_delta', lambda x: x.topology_symbol_deltas.__setitem__(
        0, int(x.topology_symbol_deltas[0]) + 1,
    ))
    add('fractional_reset', lambda x: x.replication_fractional_after.__setitem__(0, 0.5))
    for name, forged in corruptions:
        _assert_raises(
            a4.A4SchemaError,
            lambda forged=forged: a44.validate_a4_paid_elongation_plan(forged),
        )

    # A short empty-copy completion replaces template storage one-for-one.
    # Exact current symbol capacity must pass; no append may be double-counted.
    exact_cell = copy.deepcopy(cells[0])
    short = np.asarray(exact_cell.genomes[0][:64], dtype=np.uint8).copy()
    exact_cell.genomes = [short.copy()]
    exact_cell.genome_lesions = [0.125]
    exact_cell.replication_template = short.copy()
    exact_cell.replication_copy = []
    exact_cell.replication_template_lesion = 0.125
    exact_cell.replication_fractional = 0.0
    exact_cell.pools[a4.a3.POOL_NUCLEOTIDE] = 1.0
    exact_cell.pools[a4.a3.POOL_ATP] = 1.0
    exact_cell._refresh_gene_cache()
    exact_cell._sync_protein_pool()
    exact_model = _replication_model_config(
        world.config, external_replicase=True,
    )
    roomy = a4.GPU068A4Config(
        max_cells=1, max_sequences=3, max_symbols=256,
        max_sequence_symbols=64, max_proteins_per_cell=64,
    )
    probe = a4.FullFidelityA4GenomeAdapter(roomy).pack_cells([exact_cell])
    exact_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=int(probe.sequence_count),
        max_symbols=int(probe.symbol_count), max_sequence_symbols=64,
        max_proteins_per_cell=64,
    )
    exact_ragged, _, exact_binding = _paid_replication_binding(
        [exact_cell], exact_model, exact_capacity,
    )
    exact_plan = a44.paid_replication_completion_plan(
        exact_binding, 20.0, exact_model,
    )
    if (int(exact_plan.completed_lengths[0]) != 64
            or int(exact_plan.topology_symbol_deltas[0]) != 0
            or int(exact_ragged.symbol_capacity)
            != int(exact_ragged.symbol_count)):
        raise AssertionError('exact current completion capacity did not pass')

    if (a44.FULL_GPU_WORLD_STEP is not False
            or a44.PORT_STATUS.get('full_gpu_world_step') is not False
            or 'not-integrated' not in a44.PORT_STATUS.get(
                'genome_replication', '')
            or v3._event_order().count('replication_cpu') != 1):
        raise AssertionError('A4.6a changed A3 replication authority')
    return ('legacy code3; mixed/noncompletion/mutation/start rollback; '
            '%d structural/schema corruptions rejected; exact current capacity; '
            'A3 replication_cpu retained' % len(corruptions))


def test_a46b1_completion_mutation_rng_tape_formal066_oracle():
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    source_before = [v3.pickle_clone(cell.state_dict()) for cell in cells]
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )
    if a44.validate_a4_completion_mutation_rng_tape(
            tape, binding, dt, world.config) is not tape:
        raise AssertionError('A4.6b1 validator did not return its tape')

    expected_events = np.asarray([
        [4, 2, 16, 1, 1],
        [0, 2, 0, 1, 1],
        [3, 0, 0, 1, 1],
    ], dtype=np.int64)
    for label, actual, expected in (
            ('append_count', tape.append_count[:3], [1, 1, 1]),
            ('substitution_count', tape.substitution_count[:3], [0, 0, 1]),
            ('pre_length', tape.pre_structural_lengths[:3], [576, 640, 8]),
            ('post_length', tape.post_structural_lengths[:3], [577, 638, 32]),
            ('material_delta', tape.material_delta_symbols[:3], [1, -2, 24]),
            ('insertion_count', tape.insertion_count[:3], [4, 0, 3]),
            ('deletion_count', tape.deletion_count[:3], [2, 2, 0]),
            ('padding_count', tape.padding_count[:3], [0, 0, 21])):
        if not np.array_equal(
                np.asarray(actual), np.asarray(expected, dtype=np.int64)):
            raise AssertionError('A4.6b1 %s fixture drifted' % label)
    mass = float(a4.g2.MONOMER_MASS)
    expected_budget = np.asarray([
        min(
            int((
                float(cell.pools[a4.a3.POOL_NUCLEOTIDE])
                - int(tape.append_count[ci]) * mass
            ) / mass),
            int(a4.g2.MAX_GENOME_LENGTH),
        )
        for ci, cell in enumerate(cells)
    ], dtype=np.int64)
    if not np.array_equal(
            tape.nucleotide_budget_symbols[:3], expected_budget):
        raise AssertionError('A4.6b1 effective material budget differs')
    if not np.array_equal(
            tape.structural_event_counts[:3], expected_events):
        raise AssertionError('A4.6b1 structural event fixture drifted')
    if (not np.all(tape.append_draw_mask[:3, 0])
            or not np.all(tape.threshold_draw_mask[:3])
            or not np.all(tape.threshold_hit_mask[:3])
            or int(tape.duplication_gene_ordinal[0]) < 0
            or np.any(tape.duplication_gene_ordinal[1:3] != -1)):
        raise AssertionError('A4.6b1 conditional draw schedule drifted')
    if (int(tape.cell_count) != 3
            or bool(tape.cell_mask[3])
            or np.any(tape.structural_event_counts[3])
            or np.any(tape.append_draw_mask[3])
            or np.any(tape.threshold_draw_mask[3])
            or np.any(tape.padding_symbols[3])):
        raise AssertionError('A4.6b1 unused tape tail is not canonical')
    if (int(tape.rng_before_state['has_uint32']) != 1
            or int(tape.rng_before_state['uinteger']) != 3034332626):
        raise AssertionError('A4.6b1 primed PCG64 before-state drifted')

    cpu_world = copy.deepcopy(world)
    cpu_before = copy.deepcopy(cells)
    cpu_cells = copy.deepcopy(cells)
    for cell in cpu_cells:
        cell._replicate_genome(cpu_world, dt, cpu_world.config)
    if tape.rng_after_state != cpu_world.rng.bit_generator.state:
        raise AssertionError('A4.6b1 PCG64 after-state differs from Formal066')
    if (int(tape.rng_after_state['has_uint32']) != 1
            or int(tape.rng_after_state['uinteger']) != 2698505109):
        raise AssertionError('A4.6b1 PCG64 byte-vector cache drifted')

    for ci, (before, actual) in enumerate(zip(cpu_before, cpu_cells)):
        replayed = _a46b1_replay_tape_row(tape, before, ci)
        if (len(actual.genomes) != len(before.genomes) + 1
                or not np.array_equal(replayed, actual.genomes[-1])):
            raise AssertionError(
                'A4.6b1 cell[%d] final polymer differs' % ci
            )
        structural = np.asarray([
            int(actual.mutation_events[name])
            - int(before.mutation_events[name])
            for name in a44.STRUCTURAL_EVENT_NAMES
        ], dtype=np.int64)
        substitution = (
            int(actual.mutation_events['substitution'])
            - int(before.mutation_events['substitution'])
        )
        if (not np.array_equal(
                structural, tape.structural_event_counts[ci])
                or substitution != int(tape.substitution_count[ci])):
            raise AssertionError(
                'A4.6b1 cell[%d] mutation counters differ' % ci
            )
        expected_nucleotide = (
            float(before.pools[a4.a3.POOL_NUCLEOTIDE])
            - int(tape.append_count[ci]) * mass
            - int(tape.material_delta_symbols[ci]) * mass
        )
        v3.assert_recursive_close(
            expected_nucleotide,
            float(actual.pools[a4.a3.POOL_NUCLEOTIDE]),
            atol=2e-12, rtol=0.0,
            path='a46b1.material[%d]' % ci,
        )

    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.6b1 preparation advanced the live RNG')
    for ci, (before, cell) in enumerate(zip(source_before, cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), atol=0.0, rtol=0.0,
            path='a46b1.source_cell[%d]' % ci,
        )
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a46b1.nonmutation.%s' % label,
        )
    return ('3 cell-interleaved Formal066 completions: substitution hit/miss, '
            'five structural operations, padding, material trim/refund, and '
            'full PCG64 uint32 cache exact')


def test_a46b1_completion_mutation_rng_tape_torch_roundtrip():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.6b1')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; CPU fallback forbidden')
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture(
        seed=9912,
    )
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    for device in devices:
        resident = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        if any(
                getattr(resident, name).device.type != device
                for name in a44._COMPLETION_TAPE_ARRAY_FIELDS):
            raise AssertionError('%s A4.6b1 tape escaped device' % device)
        pointers = resident.data_ptrs()
        if device == 'cuda':
            torch.cuda.synchronize()
        back = resident.to_numpy()
        a44.validate_a4_completion_mutation_rng_tape(
            back, binding, dt, world.config,
        )
        v3.assert_recursive_close(
            tape.state_dict(), back.state_dict(), atol=0.0, rtol=0.0,
            path='a46b1.tape_roundtrip.%s' % device,
        )
        if pointers != resident.data_ptrs():
            raise AssertionError('%s A4.6b1 tape pointer changed' % device)

        changed = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        changed.append_uniform_draws[0, 0] += 0.125
        _assert_raises(a4.A4SchemaError, changed.to_numpy)
        stealth = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        version_before = int(stealth.append_uniform_draws._version)
        stealth.append_uniform_draws.data[0, 0] = (
            stealth.append_uniform_draws.data[0, 0] + 0.25
        )
        if int(stealth.append_uniform_draws._version) != version_before:
            raise AssertionError(
                '%s stealth fixture unexpectedly changed Tensor version' %
                device
            )
        stealth_clone = stealth.clone()
        _assert_raises(a4.A4SchemaError, stealth.to_numpy)
        _assert_raises(a4.A4SchemaError, stealth_clone.to_numpy)
        changed_scalar = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        changed_scalar.dt_hex = float(dt + 1e-12).hex()
        _assert_raises(a4.A4SchemaError, changed_scalar.to_numpy)

    if (world.rng.bit_generator.state != rng_before
            or a44.FULL_GPU_WORLD_STEP is not False):
        raise AssertionError('A4.6b1 upload changed RNG or authority')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a46b1.roundtrip_nonmutation.%s' % label,
        )
    return ('NumPy/Torch %s fixed-shape tape roundtrip exact; resident '
            'scalar/tensor/content/pointer attestation and source/RNG '
            'nonmutation' %
            '/'.join(devices))


def test_a46b1_completion_mutation_rng_tape_fail_closed_and_authority():
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture(
        seed=9913,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    _, _, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )

    corruptions = []
    def add(name, mutate):
        values = tape.state_dict()
        mutate(values)
        corruptions.append((
            name, a44._make_completion_mutation_rng_tape(**values),
        ))

    add('append_uniform', lambda x: x['append_uniform_draws'].__setitem__(
        (0, 0), np.nextafter(x['append_uniform_draws'][0, 0], 1.0),
    ))
    add('replacement_raw', lambda x: x['replacement_raw'].__setitem__(
        (2, 0), (int(x['replacement_raw'][2, 0]) + 1) % 7,
    ))
    add('insertion_payload', lambda x: x['insertion_symbols'].__setitem__(
        (0, 0), (int(x['insertion_symbols'][0, 0]) + 1) % 8,
    ))
    add('deletion_position', lambda x: x['deletion_position'].__setitem__(
        0, int(x['deletion_position'][0]) + 1,
    ))
    add('duplication_start', lambda x: x['duplication_source_start'].__setitem__(
        0, int(x['duplication_source_start'][0]) + 1,
    ))
    add('inversion_right', lambda x: x['inversion_right'].__setitem__(
        0, int(x['inversion_right'][0]) - 1,
    ))
    add('transposition_position', lambda x: x['transposition_position'].__setitem__(
        0, int(x['transposition_position'][0]) + 1,
    ))
    add('padding_payload', lambda x: x['padding_symbols'].__setitem__(
        (2, 0), (int(x['padding_symbols'][2, 0]) + 1) % 8,
    ))
    add('material_delta', lambda x: x['material_delta_symbols'].__setitem__(
        0, int(x['material_delta_symbols'][0]) + 1,
    ))
    def change_after(values):
        nested = values['rng_after_state']['state']
        nested['state'] = (int(nested['state']) + 1) % (1 << 128)
    add('rng_after', change_after)
    add('schedule_digest', lambda x: x.__setitem__('schedule_sha256', 'f' * 64))
    for name, forged in corruptions:
        _assert_raises(
            a4.A4SchemaError,
            lambda forged=forged: a44.validate_a4_completion_mutation_rng_tape(
                forged, binding, dt, world.config,
            ),
        )

    changed_host = tape.clone()
    changed_host.padding_symbols[2, 0] ^= np.uint8(1)
    _assert_raises(
        a4.A4SchemaError,
        lambda: a44.validate_a4_completion_mutation_rng_tape(
            changed_host, binding, dt, world.config,
        ),
    )
    mutation_off = copy.deepcopy(world.config)
    mutation_off.mutation = False
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_completion_mutation_rng_tape(
            binding, dt, mutation_off, rng_before,
        ),
    )
    nonfinite = copy.deepcopy(world.config)
    nonfinite.structural_rate = float('nan')
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_completion_mutation_rng_tape(
            binding, dt, nonfinite, rng_before,
        ),
    )
    changed_config = copy.deepcopy(world.config)
    changed_config.structural_rate = 9.0
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.validate_a4_completion_mutation_rng_tape(
            tape, binding, dt, changed_config,
        ),
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.validate_a4_completion_mutation_rng_tape(
            tape, binding, dt + 1e-12, world.config,
        ),
    )

    foreign_cells = copy.deepcopy(cells)
    foreign_cells[0].pools[a4.a3.POOL_NUCLEOTIDE] += 0.01
    _, _, foreign_binding = _paid_replication_binding(
        foreign_cells, world.config, capacity,
    )
    _assert_raises(
        a4.A4Error,
        lambda: a44.validate_a4_completion_mutation_rng_tape(
            tape, foreign_binding, dt, world.config,
        ),
    )

    # A large but CPU-convertible budget is outcome-equivalent to F and is
    # bounded in the fixed tape.  A finite pool whose raw quotient overflows to
    # infinity remains outside scope, matching the frozen CPU conversion
    # failure instead of inventing an A4-only successful state.
    bounded = copy.deepcopy(cells[0])
    bounded.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
    huge_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=4, max_symbols=4096,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    _, _, bounded_binding = _paid_replication_binding(
        [bounded], world.config, huge_capacity,
    )
    bounded_tape = a44.prepare_completion_mutation_rng_tape(
        bounded_binding, dt, world.config, rng_before,
    )
    if int(bounded_tape.nucleotide_budget_symbols[0]) != int(
            a4.g2.MAX_GENOME_LENGTH):
        raise AssertionError('A4.6b1 large finite budget was not bounded to F')
    huge = copy.deepcopy(cells[0])
    huge.pools[a4.a3.POOL_NUCLEOTIDE] = 1e308
    _, _, huge_binding = _paid_replication_binding(
        [huge], world.config, huge_capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_completion_mutation_rng_tape(
            huge_binding, dt, world.config, rng_before,
        ),
    )
    inactive = copy.deepcopy(cells[0])
    inactive.replication_template = None
    inactive.replication_copy = []
    inactive.replication_template_lesion = 0.0
    inactive.replication_fractional = 0.0
    _, _, inactive_binding = _paid_replication_binding(
        [inactive], world.config, capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_completion_mutation_rng_tape(
            inactive_binding, dt, world.config, rng_before,
        ),
    )
    _, ordinary_cells, _, ordinary_dt = _paid_replication_b_fixture(
        seed=9914,
    )
    mixed = [copy.deepcopy(cells[0]), copy.deepcopy(ordinary_cells[1])]
    _, _, mixed_binding = _paid_replication_binding(
        mixed, world.config, capacity,
    )
    _assert_raises(
        a44.A4ReplicationScopeError,
        lambda: a44.prepare_completion_mutation_rng_tape(
            mixed_binding, ordinary_dt, world.config, rng_before,
        ),
    )

    # The tape carries no final genome, but preparation already knows the
    # frozen post-mutation length.  Per-sequence and aggregate future capacity
    # must fail closed; material-budget trimming is never reused as an arena
    # overflow clip.
    short = copy.deepcopy(cells[2])
    common = {
        'max_cells': 1, 'max_sequences': 3,
        'max_proteins_per_cell': 64,
    }
    current_capacity = a4.GPU068A4Config(
        max_symbols=23, max_sequence_symbols=32, **common
    )
    _, _, current_binding = _paid_replication_binding(
        [short], world.config, current_capacity,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, current_binding,
        lambda: a44.prepare_completion_mutation_rng_tape(
            current_binding, dt, world.config, rng_before,
        ),
        'a46b1.current_symbol_capacity',
    )
    exact_capacity = a4.GPU068A4Config(
        max_symbols=40, max_sequence_symbols=32, **common
    )
    exact_ragged, _, exact_binding = _paid_replication_binding(
        [short], world.config, exact_capacity,
    )
    exact = a44.prepare_completion_mutation_rng_tape(
        exact_binding, dt, world.config, rng_before,
    )
    if (int(exact_ragged.symbol_count) != 23
            or int(exact_ragged.symbol_capacity) != 40
            or int(exact.append_capacity) != 32
            or int(exact.post_structural_lengths[0]) != 32
            or int(exact.material_delta_symbols[0]) != 24):
        raise AssertionError('A4.6b1 exact future capacity differs')
    one_short_capacity = a4.GPU068A4Config(
        max_symbols=39, max_sequence_symbols=32, **common
    )
    _, _, one_short_binding = _paid_replication_binding(
        [short], world.config, one_short_capacity,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, one_short_binding,
        lambda: a44.prepare_completion_mutation_rng_tape(
            one_short_binding, dt, world.config, rng_before,
        ),
        'a46b1.one_short_symbol_capacity',
    )
    narrow_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=3, max_symbols=64,
        max_sequence_symbols=31, max_proteins_per_cell=64,
    )
    _, _, narrow_binding = _paid_replication_binding(
        [short], world.config, narrow_capacity,
    )
    _assert_replication_failure_atomic(
        a4.A4CapacityError, narrow_binding,
        lambda: a44.prepare_completion_mutation_rng_tape(
            narrow_binding, dt, world.config, rng_before,
        ),
        'a46b1.sequence_capacity',
    )

    expected_replication = (
        'a4.6b2-pre-existing-active-all-row-completion-combined-pcg64-'
        'substitution-structural-material-fixed-resident-plan-'
        'not-arena-committed-not-integrated-cpu-authoritative'
    )
    expected_material = (
        'a4.6b2-binding-aware-attested-tape-applied-to-pure-resident-'
        'descriptor-not-live-rng-authority-cpu-authoritative'
    )
    if (a44.PORT_STATUS.get('genome_replication') != expected_replication
            or a44.PORT_STATUS.get('material_mutation') != expected_material
            or a44.FULL_GPU_WORLD_STEP is not False
            or v3._event_order().count('replication_cpu') != 1
            or world.rng.bit_generator.state != rng_before):
        raise AssertionError('A4.6b1 CPU/RNG/scheduler authority changed')
    return ('%d replay/attestation corruptions plus config/dt/source/inactive/'
            'mixed scope rejected; current/exact/one-short aggregate and '
            'per-sequence capacities fail closed; apply remains A4.6b2' %
            len(corruptions))


def test_a46b2_completion_mutation_apply_formal066_oracle():
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    source_before = [v3.pickle_clone(cell.state_dict()) for cell in cells]
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )
    plan = a44.paid_replication_completion_mutation_numpy(
        binding, dt, world.config, tape,
    )

    cpu_world = copy.deepcopy(world)
    cpu_before = copy.deepcopy(cells)
    cpu_cells = copy.deepcopy(cells)
    for cell in cpu_cells:
        cell._replicate_genome(cpu_world, dt, cpu_world.config)
    if cpu_world.rng.bit_generator.state != tape.rng_after_state:
        raise AssertionError('A4.6b2 CPU RNG after-state differs from tape')

    post_ragged = a4.FullFidelityA4GenomeAdapter(capacity).pack_cells(
        cpu_cells,
    )
    post_state = a4.pack_a4_translation_state(
        cpu_cells, post_ragged, cpu_world.config, capacity,
    )
    for ci, (before, actual) in enumerate(zip(cpu_before, cpu_cells)):
        length = int(plan.final_lengths[ci])
        final = np.asarray(plan.final_symbols[ci, :length], dtype=np.uint8)
        if (len(actual.genomes) != len(before.genomes) + 1
                or not np.array_equal(final, actual.genomes[-1])):
            raise AssertionError(
                'A4.6b2 cell[%d] final polymer differs' % ci
            )
        v3.assert_recursive_close(
            np.asarray(plan.pools_after[ci], dtype=np.float64),
            np.asarray(actual.pools, dtype=np.float64),
            atol=2e-12, rtol=0.0,
            path='a46b2.cell[%d].pools' % ci,
        )
        v3.assert_recursive_close(
            float(plan.new_genome_lesions[ci]),
            float(actual.genome_lesions[-1]),
            atol=2e-12, rtol=0.0,
            path='a46b2.cell[%d].lesion' % ci,
        )
        for label, planned, observed in (
                ('last_symbols', plan.last_replication_symbols[ci],
                 actual.last_replication_symbols),
                ('cycle_delta', plan.replication_cycle_deltas[ci],
                 actual.replication_cycles - before.replication_cycles),
                ('genome_count', plan.genome_count_after[ci],
                 len(actual.genomes)),
                ('material_symbols', plan.genome_material_symbols_after[ci],
                 post_state.genome_material_symbols[ci])):
            if int(planned) != int(observed):
                raise AssertionError(
                    'A4.6b2 cell[%d] %s differs' % (ci, label)
                )
        for label, planned, observed in (
                ('effective_error', plan.last_effective_error_rate[ci],
                 actual.last_effective_error_rate),
                ('proof_atp', plan.cumulative_proofreading_atp_after[ci],
                 actual.cumulative_proofreading_atp),
                ('lesion_mean', plan.genome_lesion_mean_after[ci],
                 post_state.genome_lesion_mean[ci])):
            v3.assert_recursive_close(
                float(planned), float(observed), atol=2e-12, rtol=0.0,
                path='a46b2.cell[%d].%s' % (ci, label),
            )
        structural = np.asarray([
            int(actual.mutation_events[name])
            - int(before.mutation_events[name])
            for name in a44.STRUCTURAL_EVENT_NAMES
        ], dtype=np.int64)
        substitution = (
            int(actual.mutation_events['substitution'])
            - int(before.mutation_events['substitution'])
        )
        if (not np.array_equal(
                structural, plan.structural_event_counts[ci])
                or substitution != int(plan.substitution_events[ci])
                or bool(plan.replication_active_after[ci])
                or float(plan.replication_template_lesions_after[ci]) != 0.0
                or float(plan.replication_fractional_after[ci]) != 0.0):
            raise AssertionError(
                'A4.6b2 cell[%d] event/reset descriptor differs' % ci
            )
        pre_first = int(ragged.cell_sequence_offsets[ci])
        pre_last = int(ragged.cell_sequence_offsets[ci + 1])
        post_first = int(post_ragged.cell_sequence_offsets[ci])
        post_last = int(post_ragged.cell_sequence_offsets[ci + 1])
        pre_symbols = int(
            ragged.sequence_offsets[pre_last]
            - ragged.sequence_offsets[pre_first]
        )
        post_symbols = int(
            post_ragged.sequence_offsets[post_last]
            - post_ragged.sequence_offsets[post_first]
        )
        if (int(plan.topology_sequence_deltas[ci])
                != (post_last - post_first) - (pre_last - pre_first)
                or int(plan.topology_symbol_deltas[ci])
                != post_symbols - pre_symbols):
            raise AssertionError(
                'A4.6b2 cell[%d] topology delta differs' % ci
            )

    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.6b2 changed source world or live RNG')
    v3.assert_recursive_close(
        world_before, world.state_dict(), atol=0.0, rtol=0.0,
        path='a46b2.source_world',
    )
    for ci, (before, cell) in enumerate(zip(source_before, cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), atol=0.0, rtol=0.0,
            path='a46b2.source_cell[%d]' % ci,
        )
    for label, before, after in (
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a46b2.nonmutation.%s' % label,
        )

    # A short completed polymer may be padded by the frozen mutator and then
    # trimmed back to its pre-structural length by a zero material budget.
    # MIN_GENOME_LENGTH is therefore not an unconditional post-mutation floor.
    short_world = copy.deepcopy(world)
    short_cell = copy.deepcopy(cells[2])
    short_cell.pools[a4.a3.POOL_NUCLEOTIDE] = float(a4.g2.MONOMER_MASS)
    short_rng_before = copy.deepcopy(short_world.rng.bit_generator.state)
    short_ragged, short_state, short_binding = _paid_replication_binding(
        [short_cell], short_world.config, capacity,
    )
    short_tape = a44.prepare_completion_mutation_rng_tape(
        short_binding, dt, short_world.config, short_rng_before,
    )
    short_plan = a44.paid_replication_completion_mutation_numpy(
        short_binding, dt, short_world.config, short_tape,
    )
    short_cpu_world = copy.deepcopy(short_world)
    short_cpu = copy.deepcopy(short_cell)
    short_cpu._replicate_genome(
        short_cpu_world, dt, short_cpu_world.config,
    )
    short_length = int(short_plan.final_lengths[0])
    if (int(short_tape.pre_structural_lengths[0]) != 8
            or int(short_tape.nucleotide_budget_symbols[0]) != 0
            or int(short_tape.padding_count[0]) <= 0
            or int(short_plan.material_delta_symbols[0]) != 0
            or short_length != 8
            or not np.array_equal(
                short_plan.final_symbols[0, :short_length],
                short_cpu.genomes[-1],
            )
            or short_cpu_world.rng.bit_generator.state
            != short_tape.rng_after_state):
        raise AssertionError(
            'A4.6b2 material-starved short completion differs'
        )
    short_devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        short_devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in short_devices:
        short_resident_ragged = short_ragged.to_torch(device=device)
        short_resident_state = short_state.to_torch(device=device)
        short_resident_binding = a4.bind_a4_translation(
            short_resident_ragged, short_resident_state,
        )
        short_resident_tape = short_tape.to_torch(
            short_binding, dt, short_world.config, device=device,
        )
        short_back = a44.paid_replication_completion_mutation_torch(
            short_resident_binding, dt, short_world.config,
            short_resident_tape,
        ).to_numpy()
        v3.assert_recursive_close(
            short_plan.state_dict(), short_back.state_dict(),
            atol=2e-12, rtol=0.0,
            path='a46b2.short_material.%s' % device,
        )

    # The host PCG64 tape owns Python's float64 division/int budget.  CUDA may
    # evaluate an exact CPU integer quotient one ulp below the boundary; it
    # must validate the attested bucket rather than re-truncate to 18.
    boundary_world = copy.deepcopy(world)
    boundary_cell = copy.deepcopy(cells[0])
    boundary_cell.pools[a4.a3.POOL_NUCLEOTIDE] = (
        20.0 * float(a4.g2.MONOMER_MASS)
    )
    boundary_rng_before = copy.deepcopy(
        boundary_world.rng.bit_generator.state,
    )
    boundary_ragged, boundary_state, boundary_binding = (
        _paid_replication_binding(
            [boundary_cell], boundary_world.config, capacity,
        )
    )
    boundary_tape = a44.prepare_completion_mutation_rng_tape(
        boundary_binding, dt, boundary_world.config, boundary_rng_before,
    )
    if int(boundary_tape.nucleotide_budget_symbols[0]) != 19:
        raise AssertionError('A4.6b2 exact material budget fixture drifted')
    boundary_plan = a44.paid_replication_completion_mutation_numpy(
        boundary_binding, dt, boundary_world.config, boundary_tape,
    )
    boundary_cpu_world = copy.deepcopy(boundary_world)
    boundary_cpu = copy.deepcopy(boundary_cell)
    boundary_cpu._replicate_genome(
        boundary_cpu_world, dt, boundary_cpu_world.config,
    )
    boundary_length = int(boundary_plan.final_lengths[0])
    if (not np.array_equal(
            boundary_plan.final_symbols[0, :boundary_length],
            boundary_cpu.genomes[-1],
        ) or boundary_cpu_world.rng.bit_generator.state
            != boundary_tape.rng_after_state):
        raise AssertionError('A4.6b2 exact material budget differs')
    for device in short_devices:
        boundary_resident_ragged = boundary_ragged.to_torch(device=device)
        boundary_resident_state = boundary_state.to_torch(device=device)
        boundary_resident_binding = a4.bind_a4_translation(
            boundary_resident_ragged, boundary_resident_state,
        )
        boundary_resident_tape = boundary_tape.to_torch(
            boundary_binding, dt, boundary_world.config, device=device,
        )
        boundary_back = a44.paid_replication_completion_mutation_torch(
            boundary_resident_binding, dt, boundary_world.config,
            boundary_resident_tape,
        ).to_numpy()
        v3.assert_recursive_close(
            boundary_plan.state_dict(), boundary_back.state_dict(),
            atol=2e-12, rtol=0.0,
            path='a46b2.exact_material.%s' % device,
        )

    # Seven old lesions plus the new lesion cross NumPy's eight-element
    # reduction boundary.  The post-state mean must reduce the combined prefix
    # once; summing seven and then adding the eighth changes one fp64 bit here.
    lesion_world, lesion_cells, _, lesion_dt = (
        _a46b1_completion_mutation_fixture(seed=16001)
    )
    lesion_cell = copy.deepcopy(lesion_cells[0])
    lesion_root = np.asarray(lesion_cell.genomes[0], dtype=np.uint8).copy()
    lesion_cell.genomes = [lesion_root.copy() for _ in range(7)]
    lesion_cell.genome_lesions = [float.fromhex(value) for value in (
        '0x1.3f6d854147672p+18',
        '0x1.f8bc5d71f2247p-11',
        '0x1.8758cfd9c680cp+38',
        '0x1.36d1c260cd486p-29',
        '0x1.b70768dd2cda4p+38',
        '0x1.f4c3db9ec2057p+23',
        '0x1.521441496b18bp-22',
    )]
    lesion_cell.replication_template = lesion_root.copy()
    lesion_cell.replication_copy = [int(value) for value in lesion_root[:-1]]
    lesion_cell.replication_template_lesion = float.fromhex(
        '0x1.dc5dfd02182dbp-9'
    )
    lesion_cell._refresh_gene_cache()
    lesion_cell._sync_protein_pool()
    lesion_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=10, max_symbols=8192,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    lesion_rng_before = copy.deepcopy(
        lesion_world.rng.bit_generator.state,
    )
    lesion_ragged, lesion_state, lesion_binding = _paid_replication_binding(
        [lesion_cell], lesion_world.config, lesion_capacity,
    )
    lesion_tape = a44.prepare_completion_mutation_rng_tape(
        lesion_binding, lesion_dt, lesion_world.config, lesion_rng_before,
    )
    lesion_plan = a44.paid_replication_completion_mutation_numpy(
        lesion_binding, lesion_dt, lesion_world.config, lesion_tape,
    )
    lesion_cpu_world = copy.deepcopy(lesion_world)
    lesion_cpu = copy.deepcopy(lesion_cell)
    lesion_cpu._replicate_genome(
        lesion_cpu_world, lesion_dt, lesion_cpu_world.config,
    )
    cpu_lesion_mean = float(np.mean(
        np.asarray(lesion_cpu.genome_lesions, dtype=np.float64),
        dtype=np.float64,
    ))
    if (float(lesion_plan.genome_lesion_mean_after[0]).hex()
            != cpu_lesion_mean.hex()):
        raise AssertionError('A4.6b2 combined lesion grouping differs')
    for device in short_devices:
        lesion_resident_ragged = lesion_ragged.to_torch(device=device)
        lesion_resident_state = lesion_state.to_torch(device=device)
        lesion_resident_binding = a4.bind_a4_translation(
            lesion_resident_ragged, lesion_resident_state,
        )
        lesion_resident_tape = lesion_tape.to_torch(
            lesion_binding, lesion_dt, lesion_world.config, device=device,
        )
        lesion_back = a44.paid_replication_completion_mutation_torch(
            lesion_resident_binding, lesion_dt, lesion_world.config,
            lesion_resident_tape,
        ).to_numpy()
        if (float(lesion_back.genome_lesion_mean_after[0]).hex()
                != cpu_lesion_mean.hex()):
            raise AssertionError(
                'A4.6b2 combined lesion grouping differs on %s' % device
            )

    # Recovering the inherited component by subtracting the A4.6a error term
    # loses low bits for large finite signals.  Recompute the frozen
    # template/proofreading term directly and preserve its grouping.
    cancel_world, cancel_cells, cancel_capacity, cancel_dt = (
        _a46b1_completion_mutation_fixture(seed=18031)
    )
    cancel_world.config.mutation_rate = 1.0088277385404213e-07
    cancel_cell = copy.deepcopy(cancel_cells[0])
    cancel_cell.replication_template_lesion = 312534361.6960746
    cancel_cell.genome_lesions = [1.0]
    cancel_cell.pools[a4.a3.POOL_REACTIVE] = 2311896.74665122
    cancel_rng_before = copy.deepcopy(
        cancel_world.rng.bit_generator.state,
    )
    cancel_ragged, cancel_state, cancel_binding = _paid_replication_binding(
        [cancel_cell], cancel_world.config, cancel_capacity,
    )
    cancel_tape = a44.prepare_completion_mutation_rng_tape(
        cancel_binding, cancel_dt, cancel_world.config, cancel_rng_before,
    )
    cancel_plan = a44.paid_replication_completion_mutation_numpy(
        cancel_binding, cancel_dt, cancel_world.config, cancel_tape,
    )
    cancel_cpu_world = copy.deepcopy(cancel_world)
    cancel_cpu = copy.deepcopy(cancel_cell)
    cancel_cpu._replicate_genome(
        cancel_cpu_world, cancel_dt, cancel_cpu_world.config,
    )
    cpu_new_lesion = float(cancel_cpu.genome_lesions[-1])
    if float(cancel_plan.new_genome_lesions[0]).hex() != cpu_new_lesion.hex():
        raise AssertionError('A4.6b2 inherited lesion grouping differs')
    for device in short_devices:
        cancel_resident_ragged = cancel_ragged.to_torch(device=device)
        cancel_resident_state = cancel_state.to_torch(device=device)
        cancel_resident_binding = a4.bind_a4_translation(
            cancel_resident_ragged, cancel_resident_state,
        )
        cancel_resident_tape = cancel_tape.to_torch(
            cancel_binding, cancel_dt, cancel_world.config, device=device,
        )
        cancel_back = a44.paid_replication_completion_mutation_torch(
            cancel_resident_binding, cancel_dt, cancel_world.config,
            cancel_resident_tape,
        ).to_numpy()
        v3.assert_recursive_close(
            float(cancel_back.new_genome_lesions[0]), cpu_new_lesion,
            atol=2e-12, rtol=0.0,
            path='a46b2.inherited_lesion.%s' % device,
        )
    return ('3 Formal066 completions plus material-starved short padding/trim '
            'exact integer budget, seven-to-eight lesion grouping, and large '
            'inherited-lesion cancellation: final polymer, paid/refunded '
            'material, lesion, counters, reset, derived physiology and '
            'topology exact')


def test_a46b2_completion_mutation_numpy_torch_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.6b2')
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture(
        seed=9912,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    source_before = [v3.pickle_clone(cell.state_dict()) for cell in cells]
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )
    expected = a44.paid_replication_completion_mutation_numpy(
        binding, dt, world.config, tape,
    )
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        resident_binding = a4.bind_a4_translation(
            resident_ragged, resident_state,
        )
        resident_tape = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        cache_ptrs = resident_binding.cache.data_ptrs()
        tape_ptrs = resident_tape.data_ptrs()
        plan = a44.paid_replication_completion_mutation_torch(
            resident_binding, dt, world.config, resident_tape,
        )
        for name in a44._COMPLETION_PLAN_ARRAY_FIELDS:
            value = getattr(plan, name)
            if (not isinstance(value, torch.Tensor)
                    or value.device.type != device):
                raise AssertionError(
                    '%s A4.6b2 %s escaped device' % (device, name)
                )
        readback = plan.to_numpy()
        v3.assert_recursive_close(
            expected.state_dict(), readback.state_dict(),
            atol=2e-12, rtol=0.0,
            path='a46b2.device.%s' % device,
        )
        if (resident_ragged.data_ptrs() != ragged_ptrs
                or resident_state.data_ptrs() != state_ptrs
                or resident_binding.cache.data_ptrs() != cache_ptrs
                or resident_tape.data_ptrs() != tape_ptrs):
            raise AssertionError('%s A4.6b2 input pointer changed' % device)
    forbidden = ('.item(', '.cpu(', '.numpy(', '.tolist(',
                 'nonzero(', 'masked_select(', 'unique(')
    source = inspect.getsource(
        a44.paid_replication_completion_mutation_torch,
    )
    hits = [token for token in forbidden if token in source]
    if hits:
        raise AssertionError('A4.6b2 Torch path contains host op: %s' % hits)
    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.6b2 device apply advanced live RNG')
    for ci, (before, cell) in enumerate(zip(source_before, cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), atol=0.0, rtol=0.0,
            path='a46b2.device_source[%d]' % ci,
        )
    return 'NumPy/Torch %s fixed-shape final polymer+ledger parity' % '/'.join(
        devices,
    )


def test_a46b2_completion_mutation_fail_closed_and_authority():
    world, cells, capacity, dt = _a46b1_completion_mutation_fixture(
        seed=9913,
    )
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        cells, world.config, capacity,
    )
    tape = a44.prepare_completion_mutation_rng_tape(
        binding, dt, world.config, rng_before,
    )
    plan = a44.paid_replication_completion_mutation_numpy(
        binding, dt, world.config, tape,
    )
    corruptions = []
    bad = plan.clone()
    bad.final_symbols[0, int(bad.final_lengths[0])] = 1
    corruptions.append(('final_tail', bad))
    bad = plan.clone()
    bad.final_lengths[0] = 0
    corruptions.append(('final_length', bad))
    bad = plan.clone()
    bad.material_delta_symbols[0] += 1
    corruptions.append(('material_delta', bad))
    bad = plan.clone()
    bad.new_genome_lesions[0] = -1.0
    corruptions.append(('lesion', bad))
    bad = plan.clone()
    bad.topology_symbol_deltas[0] += 1
    corruptions.append(('topology', bad))
    bad = plan.clone()
    bad.replication_active_after[0] = True
    corruptions.append(('active_reset', bad))
    bad = plan.clone()
    bad.genome_count_after[0] = 0
    corruptions.append(('genome_count', bad))
    bad = plan.clone()
    bad.genome_material_symbols_after[0] = 0
    corruptions.append(('genome_material', bad))
    bad = plan.clone()
    bad.genome_lesion_mean_after[0] = np.nan
    corruptions.append(('lesion_mean', bad))
    for label, corrupt in corruptions:
        _assert_raises(
            a4.A4SchemaError,
            lambda corrupt=corrupt: a44.validate_a4_completion_mutation_plan(
                corrupt,
            ),
        )

    if torch is None:
        raise AssertionError('PyTorch is required for A4.6b2 trust test')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in devices:
        resident_binding = a4.bind_a4_translation(
            ragged.to_torch(device=device),
            state.to_torch(device=device),
        )
        resident_tape = tape.to_torch(
            binding, dt, world.config, device=device,
        )
        before_version = int(resident_tape.insertion_symbols._version)
        resident_tape.insertion_symbols.data[0, 0] = (
            resident_tape.insertion_symbols.data[0, 0] + 1
        ) % int(a4.g2.ALPHABET_SIZE)
        if int(resident_tape.insertion_symbols._version) != before_version:
            raise AssertionError('%s .data unexpectedly changed version' % device)
        failed = a44.paid_replication_completion_mutation_torch(
            resident_binding, dt, world.config, resident_tape,
        )
        codes = failed.scope_error_code.detach().cpu().numpy()
        valid = failed.scope_valid.detach().cpu().numpy()
        final = failed.final_symbols.detach().cpu().numpy()
        cycles = failed.replication_cycle_deltas.detach().cpu().numpy()
        if (not np.all(codes[:len(cells)] == a44.SCOPE_RNG_TAPE_MISMATCH)
                or np.any(valid[:len(cells)])
                or np.any(final[:len(cells)])
                or np.any(cycles[:len(cells)])):
            raise AssertionError(
                '%s A4.6b2 hidden tape mutation did not roll back batch' %
                device
            )
        _assert_raises(a44.A4ReplicationScopeError, failed.to_numpy)
    if (world.rng.bit_generator.state != rng_before
            or a44.FULL_GPU_WORLD_STEP is not False
            or v3._event_order().count('replication_cpu') != 1):
        raise AssertionError('A4.6b2 CPU/RNG/scheduler authority changed')
    return ('%d plan corruptions and CPU/CUDA hidden tape mutation rejected; '
            'batch rollback, live RNG and A3 replication authority preserved' %
            len(corruptions))


def test_a47a_hydrolysis_literal_boundaries_and_zero_probability_draw():
    world, cell, capacity, dt = _a47_hydrolysis_fixture(boundaries=True)
    world_before = v3.pickle_clone(world.state_dict())
    cell_before = v3.pickle_clone(cell.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()

    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )
    if a47.validate_a4_hydrolysis_rng_tape(
            tape, binding, dt) is not tape:
        raise AssertionError('A4.7a validator did not return its tape')
    expected_slots = np.asarray(
        [True, True, True, False, False], dtype=bool,
    )
    expected_draws = np.asarray(
        [False, False, True, False, False], dtype=bool,
    )
    if (not np.array_equal(tape.genome_slot_mask, expected_slots)
            or not np.array_equal(tape.draw_mask, expected_draws)
            or np.any(tape.hit_mask)
            or np.any(tape.deletion_positions != -1)
            or int(tape.draw_count) != 1
            or int(tape.hit_count) != 0):
        raise AssertionError('A4.7a literal boundary schedule differs')

    replay_bitgen = np.random.PCG64()
    replay_bitgen.state = copy.deepcopy(rng_before)
    replay_rng = np.random.Generator(replay_bitgen)
    expected_uniform = float(replay_rng.random())
    if (float(tape.uniform_draws[2]).hex() != expected_uniform.hex()
            or np.any(tape.uniform_draws[[0, 1, 3, 4]] != 0.0)
            or tape.rng_after_state != replay_rng.bit_generator.state):
        raise AssertionError('A4.7a zero-probability RNG call differs')
    if (tape.rng_after_state != {
            'bit_generator': 'PCG64',
            'state': {
                'state': 126889499338173139678050600050083445318,
                'inc': 121863417007658695389390353187995180015,
            },
            'has_uint32': 1,
            'uinteger': 1123615560,
            }):
        raise AssertionError('A4.7a zero-dt PCG64 cache fixture drifted')

    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.7a boundary preparation advanced live RNG')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('cell', cell_before, cell.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a47a.boundary.%s' % label,
        )
    return ('lesion==0.75 and length==MIN skip draws; eligible dt=0 '
            'consumes exactly one random() with no integer/hit')


def test_a47a_hydrolysis_formal066_hit_miss_hit_oracle_and_purity():
    world, cell, capacity, dt = _a47_hydrolysis_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    cell_before = v3.pickle_clone(cell.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )

    expected_uniforms = np.asarray([
        0.2984911434141233,
        0.8142257405942803,
        0.0919159421350969,
    ], dtype=np.float64)
    expected_hits = np.asarray(
        [True, False, True, False, False], dtype=bool,
    )
    expected_positions = np.asarray(
        [12, -1, 16, -1, -1], dtype=np.int64,
    )
    if (int(tape.draw_count) != 3 or int(tape.hit_count) != 2
            or not np.all(tape.draw_mask[:3])
            or np.any(tape.draw_mask[3:])
            or not np.array_equal(tape.hit_mask, expected_hits)
            or not np.array_equal(
                tape.deletion_positions, expected_positions,
            )
            or not np.array_equal(
                tape.uniform_draws[:3], expected_uniforms,
            )
            or np.any(tape.uniform_draws[3:] != 0.0)):
        raise AssertionError('A4.7a hit/miss/hit call schedule differs')
    expected_after = {
        'bit_generator': 'PCG64',
        'state': {
            'state': 94939001618465750405315260005713465823,
            'inc': 121863417007658695389390353187995180015,
        },
        'has_uint32': 1,
        'uinteger': 2577412133,
    }
    if tape.rng_after_state != expected_after:
        raise AssertionError('A4.7a conditional integer/cache order differs')

    manual = _a47_manual_apply_tape(cell, tape)
    cpu_world = copy.deepcopy(world)
    cpu_cell = copy.deepcopy(cell)
    cpu_world.cells = [cpu_cell]
    cpu_cell._decay_information_and_proteins(cpu_world, dt)
    if cpu_world.rng.bit_generator.state != tape.rng_after_state:
        raise AssertionError('A4.7a after-state differs from Formal066')
    if ([len(genome) for genome in cpu_cell.genomes] != [47, 48, 47]
            or int(cpu_cell.genome_damage_events)
            - int(cell.genome_damage_events) != 2):
        raise AssertionError('A4.7a Formal066 deletion topology differs')
    for genome_index, (expected, actual) in enumerate(zip(
            manual.genomes, cpu_cell.genomes)):
        if not np.array_equal(expected, actual):
            raise AssertionError(
                'A4.7a genome[%d] differs from Formal066' % genome_index
            )
    if ([float(value).hex() for value in cpu_cell.genome_lesions] != [
            '0x1.33b13b13b13b2p+9',
            '0x1.809d89d89d89ep+9',
            '0x1.33b13b13b13b2p+9',
            ]
            or [float(value).hex() for value in manual.genome_lesions]
            != [float(value).hex() for value in cpu_cell.genome_lesions]):
        raise AssertionError('A4.7a lesion attenuation differs')

    waste_index = int(a4.a3.POOL_WASTE)
    cpu_waste = float(cpu_cell.pools[waste_index])
    manual_waste = float(manual.pools[waste_index])
    naive_waste = 0.004 + 2.0 * float(a4.g2.MONOMER_MASS)
    if (cpu_waste.hex() != '0x1.719f7f8ca8198p-8'
            or manual_waste.hex() != cpu_waste.hex()
            or naive_waste.hex() == cpu_waste.hex()):
        raise AssertionError('A4.7a ordered waste ledger differs')
    _assert_gene_specs_equal(
        cpu_cell.gene_specs, manual.gene_specs, 'a47a.gene_cache',
    )
    remaining_roles = [
        int(spec['role']) for spec in cpu_cell.gene_specs.values()
    ]
    if remaining_roles != [int(a4.g2.ROLE_MEMBRANE)]:
        raise AssertionError('A4.7a final gene cache refresh differs')

    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.7a preparation advanced the live RNG')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('cell', cell_before, cell.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a47a.oracle_nonmutation.%s' % label,
        )
    return ('one-cell hit/miss/hit random->conditional-integer order, full '
            'PCG64 cache, polymer/waste/lesion/event/cache exact')


def test_a47a_hydrolysis_torch_trust_scope_and_a3_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.7a')
    world, cell, capacity, dt = _a47_hydrolysis_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    cell_before = v3.pickle_clone(cell.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )

    forged = a47.A4HydrolysisRngTape(
        _factory_token=None, **tape.state_dict()
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            forged, binding, dt,
        ),
    )
    scalar_bad = tape.clone()
    scalar_bad.cell_id += 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            scalar_bad, binding, dt,
        ),
    )
    dict_bad = tape.clone()
    dict_bad.rng_after_state['state']['state'] ^= 1
    _assert_raises(
        a4.A4SchemaError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            dict_bad, binding, dt,
        ),
    )
    array_bad = tape.clone()
    array_bad.uniform_draws[0] = np.nextafter(
        array_bad.uniform_draws[0], np.float64(1.0),
    )
    _assert_raises(
        a4.A4SchemaError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            array_bad, binding, dt,
        ),
    )
    tail_bad = tape.clone()
    tail_index = int(tape.sequence_count)
    if tail_index >= int(tape.sequence_capacity):
        raise AssertionError('A4.7a trust fixture lacks an unused tail')
    tail_bad.deletion_positions[tail_index] = 0
    _assert_raises(
        a4.A4SchemaError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            tail_bad, binding, dt,
        ),
    )
    wrong_dt = float(np.nextafter(
        np.float64(dt), np.float64(np.inf),
    ))
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            tape, binding, wrong_dt,
        ),
    )

    stale_cell = copy.deepcopy(cell)
    stale_cell.genome_lesions[0] = np.nextafter(
        stale_cell.genome_lesions[0], np.float64(np.inf),
    )
    _, _, stale_binding = _paid_replication_binding(
        [stale_cell], world.config, capacity,
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_rng_tape(
            tape, stale_binding, dt,
        ),
    )

    second = copy.deepcopy(cell)
    second.cell_id = int(cell.cell_id) + 1000000
    multi_capacity = a4.GPU068A4Config(
        max_cells=2, max_sequences=10,
        max_symbols=2 * sum(len(genome) for genome in cell.genomes),
        max_sequence_symbols=48, max_proteins_per_cell=64,
    )
    _, _, multi_binding = _paid_replication_binding(
        [cell, second], world.config, multi_capacity,
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.prepare_genome_hydrolysis_rng_tape(
            multi_binding, dt, rng_before,
        ),
    )

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in devices:
        resident = tape.to_torch(binding, dt, device=device)
        pointers = resident.data_ptrs()
        for name in a47._TAPE_ARRAY_FIELDS:
            value = getattr(resident, name)
            if (not isinstance(value, torch.Tensor)
                    or value.device.type != device):
                raise AssertionError(
                    '%s A4.7a %s escaped device' % (device, name)
                )
        readback = resident.to_numpy()
        if a47.validate_a4_hydrolysis_rng_tape(
                readback, binding, dt) is not readback:
            raise AssertionError('%s A4.7a readback was not validated' % device)
        for name, expected in tape.state_dict().items():
            actual = getattr(readback, name)
            if isinstance(expected, np.ndarray):
                equal = np.array_equal(expected, actual)
            else:
                equal = expected == actual
            if not equal:
                raise AssertionError(
                    '%s A4.7a %s roundtrip differs' % (device, name)
                )
        if resident.data_ptrs() != pointers:
            raise AssertionError('%s A4.7a tape pointer changed' % device)

        hidden = tape.to_torch(binding, dt, device=device)
        version = int(hidden.uniform_draws._version)
        hidden.uniform_draws.data[0] = 0.0
        if int(hidden.uniform_draws._version) != version:
            raise AssertionError('%s .data unexpectedly changed version' % device)
        _assert_raises(a4.A4SchemaError, hidden.to_numpy)

        hidden_expected = tape.to_torch(binding, dt, device=device)
        expected_tensor = hidden_expected._resident_expected_arrays[
            'uniform_draws'
        ]
        expected_version = int(expected_tensor._version)
        expected_tensor.data[0] = 0.0
        if int(expected_tensor._version) != expected_version:
            raise AssertionError(
                '%s expected .data unexpectedly changed version' % device
            )
        _assert_raises(a4.A4SchemaError, hidden_expected.to_numpy)

    expected_status = (
        'a4.7a-single-cell-post-gain-literal-pcg64-event-tape-'
        'not-applied-not-live-rng-committed-not-integrated-cpu-authoritative'
    )
    if (world.rng.bit_generator.state != rng_before
            or a47.FULL_GPU_WORLD_STEP is not False
            or a47.PORT_STATUS.get('full_gpu_world_step') is not False
            or a47.PORT_STATUS.get('genome_symbol_hydrolysis_rng')
            != expected_status
            or a4.a3.PORT_STATUS.get('genome_symbol_hydrolysis_rng')
            != 'cpu-authoritative-explicit-hazard-plan'
            or v3._event_order().count('genome_hydrolysis_cpu_rng') != 1):
        raise AssertionError('A4.7a changed A3 hydrolysis authority')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('cell', cell_before, cell.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a47a.trust_nonmutation.%s' % label,
        )
    return ('NumPy/Torch %s explicit readback; factory/scalar/dict/array/'
            'tail/dt/stale/multicell/.data trust rejection; A3 authority retained' %
            '/'.join(devices))


def test_a47b_hydrolysis_apply_formal066_oracle_and_noop():
    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )
    plan = a47.genome_hydrolysis_deletion_numpy(
        binding, dt, tape,
    )
    if a47.validate_a4_hydrolysis_deletion_plan(
            plan, binding, dt, tape) is not plan:
        raise AssertionError('A4.7b validator did not return its plan')
    cpu_world, actual, expected = _a47b_formal066_oracle(
        world, cell, dt,
    )
    if (not bool(plan.scope_valid[0])
            or int(plan.scope_error_code[0]) != 0
            or not np.array_equal(
                plan.final_symbols[:5], expected['final_symbols'],
            )
            or not np.array_equal(
                plan.final_lengths[:5], expected['final_lengths'],
            )
            or int(plan.symbol_count_after[0])
            != expected['final_symbol_count']
            or int(plan.topology_symbol_delta[0])
            != expected['topology_symbol_delta']
            or int(plan.genome_damage_event_delta[0])
            != expected['genome_damage_event_delta']
            or int(plan.genome_material_symbols_after[0])
            != expected['genome_material_symbols_after']
            or bool(plan.gene_cache_dirty[0]) is not True
            or int(plan.gene_cache_refresh_count[0]) != 2):
        raise AssertionError('A4.7b Formal066 discrete descriptor differs')
    if (not np.array_equal(
            plan.genome_lesions_after[:3],
            expected['genome_lesions_after'],
            )
            or np.any(plan.genome_lesions_after[3:] != 0.0)
            or not np.array_equal(
                plan.pools_after[0], expected['pools_after'],
            )
            or float(plan.genome_lesion_mean_after[0]).hex()
            != float(expected['genome_lesion_mean_after']).hex()):
        raise AssertionError('A4.7b Formal066 float descriptor differs')
    if ([int(value) for value in plan.final_lengths[:5]]
            != [47, 48, 47, 48, 7]
            or expected['final_offsets'].tolist()
            != [0, 47, 95, 142, 190, 197]
            or float(plan.pools_after[0, a4.a3.POOL_WASTE]).hex()
            != '0x1.719f7f8ca8198p-8'):
        raise AssertionError('A4.7b topology/ordered waste fixture drifted')

    template = np.asarray(cell.replication_template, dtype=np.uint8)
    copy_symbols = np.asarray(cell.replication_copy, dtype=np.uint8)
    if (not np.array_equal(plan.final_symbols[3, :48], template)
            or not np.array_equal(plan.final_symbols[4, :7], copy_symbols)):
        raise AssertionError('A4.7b touched active template/copy rows')
    final_genomes = [
        np.asarray(plan.final_symbols[index, :int(plan.final_lengths[index])],
                   dtype=np.uint8).copy()
        for index in range(3)
    ]
    conceptual_cache = a4._gene_cache_from_genomes(final_genomes)
    _assert_gene_specs_equal(
        actual.gene_specs, conceptual_cache, 'a47b.conceptual_cache',
    )
    if cpu_world.rng.bit_generator.state != tape.rng_after_state:
        raise AssertionError('A4.7b direct CPU RNG differs from tape')

    zero_world, zero_cell, zero_capacity, zero_dt = (
        _a47b_hydrolysis_fixture(zero_hit=True)
    )
    zero_ragged, _, zero_binding = _paid_replication_binding(
        [zero_cell], zero_world.config, zero_capacity,
    )
    zero_tape = a47.prepare_genome_hydrolysis_rng_tape(
        zero_binding, zero_dt,
        copy.deepcopy(zero_world.rng.bit_generator.state),
    )
    zero_plan = a47.genome_hydrolysis_deletion_numpy(
        zero_binding, zero_dt, zero_tape,
    )
    _, _, zero_expected = _a47b_formal066_oracle(
        zero_world, zero_cell, zero_dt,
    )
    source_lengths = np.diff(zero_ragged.sequence_offsets[:6])
    if (not np.array_equal(zero_plan.final_lengths[:5], source_lengths)
            or not np.array_equal(
                zero_plan.pools_after[0], zero_expected['pools_after'],
            )
            or not np.array_equal(
                zero_plan.genome_lesions_after[:3],
                zero_expected['genome_lesions_after'],
            )
            or int(zero_plan.symbol_count_after[0])
            != int(zero_ragged.symbol_count)
            or int(zero_plan.topology_symbol_delta[0]) != 0
            or int(zero_plan.genome_damage_event_delta[0]) != 0
            or bool(zero_plan.gene_cache_dirty[0])
            or int(zero_plan.gene_cache_refresh_count[0]) != 0):
        raise AssertionError('A4.7b zero-hit descriptor is not an exact no-op')
    return ('hit/miss/hit Formal066 deletion, sequential waste, lesion/event/'
            'topology/cache-dirty exact; active rows preserved; dt=0 no-op')


def test_a47b_hydrolysis_numpy_torch_devices_and_purity():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.7b')
    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    cell_before = v3.pickle_clone(cell.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )
    tape_before = tape.state_dict()
    expected = a47.genome_hydrolysis_deletion_numpy(
        binding, dt, tape,
    )
    expected_state = expected.state_dict()
    array_names = tuple(
        name for name, value in expected_state.items()
        if isinstance(value, np.ndarray)
    )

    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in devices:
        resident_ragged = ragged.to_torch(device=device)
        resident_state = state.to_torch(device=device)
        resident_binding = a4.bind_a4_translation(
            resident_ragged, resident_state,
        )
        resident_tape = tape.to_torch(
            binding, dt, device=device,
        )
        ragged_ptrs = resident_ragged.data_ptrs()
        state_ptrs = resident_state.data_ptrs()
        cache_ptrs = resident_binding.cache.data_ptrs()
        tape_ptrs = resident_tape.data_ptrs()
        resident_plan = a47.genome_hydrolysis_deletion_torch(
            resident_binding, dt, resident_tape,
        )
        for name in array_names:
            value = getattr(resident_plan, name)
            if (not isinstance(value, torch.Tensor)
                    or value.device.type != device):
                raise AssertionError(
                    '%s A4.7b %s escaped device' % (device, name)
                )
        readback = resident_plan.to_numpy()
        if a47.validate_a4_hydrolysis_deletion_plan(
                readback, binding, dt, tape) is not readback:
            raise AssertionError('%s A4.7b readback was not validated' % device)
        v3.assert_recursive_close(
            expected_state, readback.state_dict(), atol=0.0, rtol=0.0,
            path='a47b.device.%s' % device,
        )
        if (resident_ragged.data_ptrs() != ragged_ptrs
                or resident_state.data_ptrs() != state_ptrs
                or resident_binding.cache.data_ptrs() != cache_ptrs
                or resident_tape.data_ptrs() != tape_ptrs):
            raise AssertionError('%s A4.7b input pointer changed' % device)

    # Eight final lesions cross the NumPy reduction boundary.  Reducing the
    # first seven and then appending the eighth changes one fp64 bit, so the
    # plan must derive the mean from the conceptual final vector in one pass.
    mean_world, mean_cell, _, mean_dt = _a47_hydrolysis_fixture()
    mean_root = np.asarray(mean_cell.genomes[0], dtype=np.uint8).copy()
    mean_cell.genomes = [mean_root.copy() for _ in range(8)]
    mean_cell.genome_lesions = [float.fromhex(value) for value in (
        '0x1.ad76af3009b08p+10',
        '0x1.8b8030d222309p+13',
        '0x1.22aabaa71530cp+12',
        '0x1.f8640be991f30p+12',
        '0x1.2380e7928077cp+11',
        '0x1.3df56805c6afbp+11',
        '0x1.de49d4ca52714p+11',
        '0x1.32a89effea992p+11',
    )]
    mean_cell._refresh_gene_cache()
    mean_cell._sync_protein_pool()
    mean_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=8, max_symbols=384,
        max_sequence_symbols=48, max_proteins_per_cell=64,
    )
    mean_ragged, mean_state, mean_binding = _paid_replication_binding(
        [mean_cell], mean_world.config, mean_capacity,
    )
    mean_tape = a47.prepare_genome_hydrolysis_rng_tape(
        mean_binding, mean_dt,
        copy.deepcopy(mean_world.rng.bit_generator.state),
    )
    mean_expected = a47.genome_hydrolysis_deletion_numpy(
        mean_binding, mean_dt, mean_tape,
    )
    _, _, mean_oracle = _a47b_formal066_oracle(
        mean_world, mean_cell, mean_dt,
    )
    final_lesions = mean_oracle['genome_lesions_after']
    cpu_mean = float(np.mean(final_lesions, dtype=np.float64))
    old_grouping = (
        float(np.sum(final_lesions[:7], dtype=np.float64))
        + float(final_lesions[7])
    ) / 8.0
    if (int(mean_expected.genome_damage_event_delta[0]) != 8
            or cpu_mean.hex() != '0x1.de203df070020p+11'
            or old_grouping.hex() != '0x1.de203df070021p+11'
            or float(mean_expected.genome_lesion_mean_after[0]).hex()
            != cpu_mean.hex()):
        raise AssertionError('A4.7b eight-lesion reduction fixture differs')
    for device in devices:
        mean_resident_binding = a4.bind_a4_translation(
            mean_ragged.to_torch(device=device),
            mean_state.to_torch(device=device),
        )
        mean_resident_tape = mean_tape.to_torch(
            mean_binding, mean_dt, device=device,
        )
        mean_back = a47.genome_hydrolysis_deletion_torch(
            mean_resident_binding, mean_dt, mean_resident_tape,
        ).to_numpy()
        v3.assert_recursive_close(
            float(mean_back.genome_lesion_mean_after[0]), cpu_mean,
            atol=2e-12, rtol=0.0,
            path='a47b.eight_lesion_mean.%s' % device,
        )

    # The final division for three extremely uneven but finite lesions is one
    # ULP lower on RTX CUDA than NumPy.  The pairwise numerator and every
    # non-mean field remain bit-exact; only this final quotient may be either
    # adjacent fp64 neighbour of the direct Formal066 mean.
    division_world = v3.make_world(seed=33003, cells=1)
    division_cell = division_world.cells[0]
    division_world.config.endogenous_damage = False
    division_cell.genomes = [
        _a47_gene_sequence(a4.g2.ROLE_ENERGY, 0, length=33),
        _a47_gene_sequence(a4.g2.ROLE_MEMBRANE, 0, length=34),
        _a47_gene_sequence(a4.g2.ROLE_TRANSPORTER, 16, length=35),
    ]
    division_cell.genome_lesions = [float.fromhex(value) for value in (
        '0x1.8000000000001p-1',
        '0x1.249ad2594c37dp+332',
        '0x1.249ad2594c37dp+333',
    )]
    division_cell.replication_template = None
    division_cell.replication_copy = []
    division_cell.replication_template_lesion = 0.0
    division_cell.replication_fractional = 0.0
    division_cell.membrane_oxidation[:] = 0.0
    division_cell._refresh_gene_cache()
    division_cell._sync_protein_pool()
    division_dt = 1000.0
    division_capacity = a4.GPU068A4Config(
        max_cells=1, max_sequences=3, max_symbols=102,
        max_sequence_symbols=35, max_proteins_per_cell=64,
    )
    division_ragged, division_state, division_binding = (
        _paid_replication_binding(
            [division_cell], division_world.config, division_capacity,
        )
    )
    division_tape = a47.prepare_genome_hydrolysis_rng_tape(
        division_binding, division_dt,
        copy.deepcopy(division_world.rng.bit_generator.state),
    )
    division_expected = a47.genome_hydrolysis_deletion_numpy(
        division_binding, division_dt, division_tape,
    )
    _, _, division_oracle = _a47b_formal066_oracle(
        division_world, division_cell, division_dt,
    )
    division_post = division_oracle['genome_lesions_after']
    division_mean = float(np.mean(division_post, dtype=np.float64))
    if (division_tape.hit_mask[:3].tolist() != [False, True, True]
            or [float(value).hex() for value in division_post] != [
                '0x1.8000000000001p-1',
                '0x1.d42aea2879f2fp+331',
                '0x1.d42aea2879f2fp+332',
            ]
            or float(np.sum(division_post, dtype=np.float64)).hex()
            != '0x1.5f202f9e5b763p+333'
            or division_mean.hex() != '0x1.d42aea2879f2fp+331'
            or float(division_expected.genome_lesion_mean_after[0]).hex()
            != division_mean.hex()):
        raise AssertionError('A4.7b three-lesion division fixture differs')
    division_expected_state = division_expected.state_dict()
    for device in devices:
        division_resident_binding = a4.bind_a4_translation(
            division_ragged.to_torch(device=device),
            division_state.to_torch(device=device),
        )
        division_resident_tape = division_tape.to_torch(
            division_binding, division_dt, device=device,
        )
        division_back = a47.genome_hydrolysis_deletion_torch(
            division_resident_binding, division_dt,
            division_resident_tape,
        ).to_numpy()
        if a47.validate_a4_hydrolysis_deletion_plan(
                division_back, division_binding, division_dt,
                division_tape) is not division_back:
            raise AssertionError(
                '%s A4.7b ULP readback was not validated' % device
            )
        division_actual_state = division_back.state_dict()
        for name, expected_value in division_expected_state.items():
            if name == 'genome_lesion_mean_after':
                continue
            v3.assert_recursive_close(
                expected_value, division_actual_state[name],
                atol=0.0, rtol=0.0,
                path='a47b.division_nonmean.%s.%s' % (device, name),
            )
        actual_mean = float(division_back.genome_lesion_mean_after[0])
        expected_bits = int(np.asarray(
            division_mean, dtype=np.float64,
        ).view(np.uint64))
        actual_bits = int(np.asarray(
            actual_mean, dtype=np.float64,
        ).view(np.uint64))
        if abs(actual_bits - expected_bits) > 1:
            raise AssertionError(
                '%s A4.7b lesion mean exceeds one ULP: %s vs %s' % (
                    device, actual_mean.hex(), division_mean.hex(),
                )
            )

    forbidden = (
        '.item(', '.cpu(', '.numpy(', '.tolist(',
        'nonzero(', 'masked_select(', 'unique(',
    )
    source = inspect.getsource(a47.genome_hydrolysis_deletion_torch)
    hits = [token for token in forbidden if token in source]
    if hits:
        raise AssertionError('A4.7b Torch path contains host op: %s' % hits)
    if world.rng.bit_generator.state != rng_before:
        raise AssertionError('A4.7b apply advanced live RNG')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('cell', cell_before, cell.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict()),
            ('tape', tape_before, tape.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a47b.nonmutation.%s' % label,
        )
    return ('NumPy/Torch %s final deletion descriptor exact; all outputs '
            'resident and source/tape/world/live RNG pure' % '/'.join(devices))


def test_a47b_hydrolysis_capacity_trust_rollback_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.7b trust test')
    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    world_before = v3.pickle_clone(world.state_dict())
    cell_before = v3.pickle_clone(cell.state_dict())
    rng_before = copy.deepcopy(world.rng.bit_generator.state)
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    ragged_before = ragged.state_dict()
    state_before = state.state_dict()
    cache_before = binding.cache.state_dict()
    tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, rng_before,
    )
    tape_before = tape.state_dict()
    plan = a47.genome_hydrolysis_deletion_numpy(
        binding, dt, tape,
    )

    if (int(ragged.cell_capacity) != 1
            or int(ragged.sequence_capacity) != 5
            or int(ragged.symbol_capacity) != 199
            or int(ragged.max_sequence_symbols) != 48):
        raise AssertionError('A4.7b exact source capacity drifted')
    exact_values = {
        'max_cells': 1,
        'max_sequences': 5,
        'max_symbols': 199,
        'max_sequence_symbols': 48,
        'max_proteins_per_cell': 64,
    }
    for label, field in (
            ('sequence', 'max_sequences'),
            ('symbol', 'max_symbols'),
            ('row_width', 'max_sequence_symbols')):
        values = dict(exact_values)
        values[field] -= 1
        short = a4.GPU068A4Config(**values)
        _assert_raises(
            a4.A4CapacityError,
            lambda short=short: a4.FullFidelityA4GenomeAdapter(
                short,
            ).pack_cells([cell]),
        )

    corruptions = []
    bad = plan.clone()
    bad.final_symbols[0, 0] = (
        int(bad.final_symbols[0, 0]) + 1
    ) % int(a4.g2.ALPHABET_SIZE)
    corruptions.append(('used_symbol', bad))
    bad = plan.clone()
    tail = int(bad.final_lengths[0])
    if tail >= int(bad.final_symbols.shape[1]):
        raise AssertionError('A4.7b trust fixture lacks final-symbol tail')
    bad.final_symbols[0, tail] = 1
    corruptions.append(('symbol_tail', bad))
    bad = plan.clone()
    bad.final_lengths[0] -= 1
    corruptions.append(('length', bad))
    bad = plan.clone()
    bad.genome_lesions_after[0] = np.nextafter(
        bad.genome_lesions_after[0], np.float64(np.inf),
    )
    corruptions.append(('lesion', bad))
    bad = plan.clone()
    bad.pools_after[0, a4.a3.POOL_WASTE] = np.nextafter(
        bad.pools_after[0, a4.a3.POOL_WASTE], np.float64(np.inf),
    )
    corruptions.append(('pool', bad))
    for label, field in (
            ('symbol_count', 'symbol_count_after'),
            ('topology', 'topology_symbol_delta'),
            ('damage_event', 'genome_damage_event_delta'),
            ('material', 'genome_material_symbols_after'),
            ('refresh_count', 'gene_cache_refresh_count')):
        bad = plan.clone()
        getattr(bad, field)[0] += 1
        corruptions.append((label, bad))
    bad = plan.clone()
    bad.genome_lesion_mean_after[0] = np.nextafter(
        np.nextafter(
            bad.genome_lesion_mean_after[0], np.float64(np.inf),
        ),
        np.float64(np.inf),
    )
    corruptions.append(('lesion_mean', bad))
    bad = plan.clone()
    bad.gene_cache_dirty[0] = False
    corruptions.append(('cache_dirty', bad))
    for label, corrupt in corruptions:
        _assert_raises(
            a4.A4SchemaError,
            lambda corrupt=corrupt: (
                a47.validate_a4_hydrolysis_deletion_plan(
                    corrupt, binding, dt, tape,
                )
            ),
        )

    wrong_dt = float(np.nextafter(
        np.float64(dt), np.float64(np.inf),
    ))
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_deletion_plan(
            plan, binding, wrong_dt, tape,
        ),
    )
    stale_cell = copy.deepcopy(cell)
    stale_cell.genome_lesions[0] = np.nextafter(
        stale_cell.genome_lesions[0], np.float64(np.inf),
    )
    _, _, stale_binding = _paid_replication_binding(
        [stale_cell], world.config, capacity,
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_deletion_plan(
            plan, stale_binding, dt, tape,
        ),
    )
    foreign_state = copy.deepcopy(
        np.random.default_rng(10147).bit_generator.state,
    )
    foreign_tape = a47.prepare_genome_hydrolysis_rng_tape(
        binding, dt, foreign_state,
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_deletion_plan(
            plan, binding, dt, foreign_tape,
        ),
    )

    second = copy.deepcopy(cell)
    second.cell_id = int(cell.cell_id) + 1000000
    multi_capacity = a4.GPU068A4Config(
        max_cells=2, max_sequences=10, max_symbols=398,
        max_sequence_symbols=48, max_proteins_per_cell=64,
    )
    _, _, multi_binding = _paid_replication_binding(
        [cell, second], world.config, multi_capacity,
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.prepare_genome_hydrolysis_rng_tape(
            multi_binding, dt, rng_before,
        ),
    )
    _assert_raises(
        a47.A4HydrolysisScopeError,
        lambda: a47.validate_a4_hydrolysis_deletion_plan(
            plan, multi_binding, dt, tape,
        ),
    )

    source_lengths = np.diff(
        ragged.sequence_offsets[:int(ragged.sequence_count) + 1]
    ).astype(np.int64, copy=False)
    source_rows = np.zeros(
        (int(ragged.sequence_capacity), int(ragged.max_sequence_symbols)),
        dtype=np.uint8,
    )
    for sequence_index, length in enumerate(source_lengths):
        start = int(ragged.sequence_offsets[sequence_index])
        source_rows[sequence_index, :int(length)] = (
            ragged.symbols[start:start + int(length)]
        )
    rollback_expected = {
        'final_symbols': source_rows,
        'final_lengths': source_lengths,
        'genome_lesions_after': ragged.genome_lesions.copy(),
        'pools_after': state.pools[:1].copy(),
        'symbol_count_after': np.asarray(
            [ragged.symbol_count], dtype=np.int64,
        ),
        'topology_symbol_delta': np.zeros((1,), dtype=np.int64),
        'genome_damage_event_delta': np.zeros((1,), dtype=np.int64),
        'genome_material_symbols_after': (
            state.genome_material_symbols[:1].copy()
        ),
        'genome_lesion_mean_after': state.genome_lesion_mean[:1].copy(),
        'gene_cache_dirty': np.zeros((1,), dtype=bool),
        'gene_cache_refresh_count': np.zeros((1,), dtype=np.int64),
    }
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable')
    for device in devices:
        for target in ('actual', 'expected'):
            resident_binding = a4.bind_a4_translation(
                ragged.to_torch(device=device),
                state.to_torch(device=device),
            )
            resident_tape = tape.to_torch(
                binding, dt, device=device,
            )
            if target == 'actual':
                tensor = resident_tape.uniform_draws
            else:
                tensor = resident_tape._resident_expected_arrays[
                    'uniform_draws'
                ]
            version = int(tensor._version)
            tensor.data[0] = torch.remainder(
                tensor.data[0] + 0.125, 1.0,
            )
            if int(tensor._version) != version:
                raise AssertionError(
                    '%s %s .data unexpectedly changed version' % (
                        device, target,
                    )
                )
            failed = a47.genome_hydrolysis_deletion_torch(
                resident_binding, dt, resident_tape,
            )
            scope_valid = failed.scope_valid.detach().cpu().numpy()
            error_code = failed.scope_error_code.detach().cpu().numpy()
            if np.any(scope_valid) or not np.all(error_code != 0):
                raise AssertionError(
                    '%s %s tape tamper did not scope-fail' % (
                        device, target,
                    )
                )
            for name, expected_values in rollback_expected.items():
                values = getattr(failed, name).detach().cpu().numpy()
                v3.assert_recursive_close(
                    expected_values, values, atol=0.0, rtol=0.0,
                    path='a47b.rollback.%s.%s.%s' % (
                        device, target, name,
                    ),
                )
            _assert_raises(a47.A4HydrolysisScopeError, failed.to_numpy)

        resident_binding = a4.bind_a4_translation(
            ragged.to_torch(device=device),
            state.to_torch(device=device),
        )
        resident_tape = tape.to_torch(binding, dt, device=device)
        resident_plan = a47.genome_hydrolysis_deletion_torch(
            resident_binding, dt, resident_tape,
        )
        version = int(resident_plan.final_symbols._version)
        resident_plan.final_symbols.data[0, 0] = torch.remainder(
            resident_plan.final_symbols.data[0, 0].to(torch.int64) + 1,
            int(a4.g2.ALPHABET_SIZE),
        ).to(resident_plan.final_symbols.dtype)
        if int(resident_plan.final_symbols._version) != version:
            raise AssertionError(
                '%s plan .data unexpectedly changed version' % device
            )
        changed_readback = resident_plan.to_numpy()
        _assert_raises(
            a4.A4SchemaError,
            lambda changed_readback=changed_readback: (
                a47.validate_a4_hydrolysis_deletion_plan(
                    changed_readback, binding, dt, tape,
                )
            ),
        )

    if (world.rng.bit_generator.state != rng_before
            or a47.FULL_GPU_WORLD_STEP is not False
            or a47.PORT_STATUS.get('full_gpu_world_step') is not False
            or a4.a3.PORT_STATUS.get('genome_symbol_hydrolysis_rng')
            != 'cpu-authoritative-explicit-hazard-plan'
            or v3._event_order().count('genome_hydrolysis_cpu_rng') != 1):
        raise AssertionError('A4.7b changed A3 hydrolysis authority')
    for label, before, after in (
            ('world', world_before, world.state_dict()),
            ('cell', cell_before, cell.state_dict()),
            ('ragged', ragged_before, ragged.state_dict()),
            ('state', state_before, state.state_dict()),
            ('cache', cache_before, binding.cache.state_dict()),
            ('tape', tape_before, tape.state_dict())):
        v3.assert_recursive_close(
            before, after, atol=0.0, rtol=0.0,
            path='a47b.trust_nonmutation.%s' % label,
        )
    return ('exact Q/S/W and each one-short; %d descriptor fields/'
            'dt/source/tape/multicell trust rejection; CPU/CUDA public/private '
            'tape rollback plus binding-aware plan .data rejection; '
            'A3 authority retained' %
            len(corruptions))


def test_a48a_atomic_commit_formal066_hit_nohit_dt0():
    cases = []
    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    cases.append(('hit_miss_hit', world, cell, capacity, dt, 3, 2))

    world, cell, capacity, _ = _a47b_hydrolysis_fixture(zero_hit=True)
    cell.genome_lesions = [0.751, 1.0, 0.751]
    cell._refresh_gene_cache()
    cases.append(('no_hit', world, cell, capacity, 1.0, 2, 0))

    world, cell, capacity, dt = _a47b_hydrolysis_fixture(zero_hit=True)
    cases.append(('eligible_dt0', world, cell, capacity, dt, 1, 0))

    summaries = []
    for (label, world, cell, capacity, dt,
         expected_draws, expected_hits) in cases:
        source = _a48_binding(world, cell, capacity)
        rng_before = copy.deepcopy(world.rng.bit_generator.state)
        tape = a47.prepare_genome_hydrolysis_rng_tape(
            source, dt, rng_before,
        )
        expected_world, expected_cell, expected = _a47b_formal066_oracle(
            world, cell, dt,
        )
        if (int(tape.draw_count) != expected_draws
                or int(tape.hit_count) != expected_hits):
            raise AssertionError(label + ' fixture draw/hit schedule drifted')

        scheduler = a48.A4HydrolysisEventScheduler(capacity, 'cpu')
        _a48_begin_direct(scheduler, world, cell, dt)
        try:
            changed = scheduler.cpu_genome_hydrolysis(
                world, cell, (), dt,
            )
            entry = _a48_hydrolysis_entry(scheduler, cell)
            fresh = _a48_binding(world, cell, capacity)
            a4._require_translation_binding(fresh)
            sequence_count = len(expected['final_lengths'])
            if (int(fresh.ragged.sequence_count) != sequence_count
                    or int(fresh.ragged.symbol_count)
                    != int(expected['final_symbol_count'])
                    or not np.array_equal(
                        fresh.ragged.sequence_offsets[:sequence_count + 1],
                        expected['final_offsets'],
                    )):
                raise AssertionError(label + ' compact offsets differ')
            _a48_assert_committed_oracle(
                world, cell, expected_world, expected_cell,
                'a48a.' + label,
            )
            metadata = entry['metadata']
            expected_metadata = {
                'authority': (
                    'A4.8a-resident-plan-atomic-cpu-rng-commit'
                ),
                'enabled': True,
                'device': 'cpu',
                'legacy_hazard_count': 0,
                'eligible_genome_count': expected_draws,
                'draw_count': expected_draws,
                'hit_count': expected_hits,
                'source_provenance': source.state.source_provenance,
                'final_provenance': fresh.state.source_provenance,
                'mutation_count': expected_hits,
                'rng_draw_count': expected_draws + expected_hits,
                'gene_cache_refresh_count': expected_hits,
                'work_performed': expected_hits > 0,
            }
            if metadata != expected_metadata:
                raise AssertionError(label + ' receipt metadata differs')
            if entry['status'] != 'executed' or bool(changed) != (expected_hits > 0):
                raise AssertionError(label + ' executed/change status differs')
            if expected_hits:
                if source.state.source_provenance == fresh.state.source_provenance:
                    raise AssertionError(label + ' old binding remained current')
            elif source.state.source_provenance != fresh.state.source_provenance:
                raise AssertionError(label + ' no-hit source provenance changed')
            summaries.append('%s=%d/%d' % (
                label, expected_draws, expected_hits,
            ))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('Formal066 exact compact/lesion/waste/cache/counter/full-PCG64; '
            + ', '.join(summaries))


def test_a48a_candidate_cpu_cuda_purity_fresh_binding_and_one_shot():
    if torch is None:
        raise AssertionError('PyTorch unavailable')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')

    reference = None
    summaries = []
    for device in devices:
        world, cell, capacity, dt = _a47b_hydrolysis_fixture()
        scheduler = a48.A4HydrolysisEventScheduler(capacity, device)
        _a48_begin_direct(scheduler, world, cell, dt)
        before_cell = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        try:
            candidate = scheduler._prepare_candidate(world, cell, dt)
            v3.assert_recursive_close(
                before_cell, cell.state_dict(), atol=0.0, rtol=0.0,
                path='a48a.%s.prepare_cell_purity' % device,
            )
            if world.rng.bit_generator.state != before_rng:
                raise AssertionError(device + ' candidate advanced live RNG')
            for owner in (
                    candidate.resident_binding.ragged,
                    candidate.resident_binding.state,
                    candidate.resident_binding.cache,
                    candidate.resident_tape, candidate.resident_plan):
                pointers = owner.data_ptrs()
                if (not pointers
                        or any(getattr(owner, name).device.type != device
                               for name in pointers)):
                    raise AssertionError(device + ' resident device differs')
            a4._require_translation_binding(candidate.fresh_binding)
            if (candidate.source_binding.state.source_provenance
                    == candidate.fresh_binding.state.source_provenance):
                raise AssertionError(device + ' hit candidate is not fresh')

            if reference is None:
                reference = (
                    candidate.tape.clone(), candidate.plan.clone(),
                    v3.pickle_clone(candidate.candidate_cell.state_dict()),
                )
            else:
                v3.assert_recursive_close(
                    reference[0].state_dict(), candidate.tape.state_dict(),
                    atol=0.0, rtol=0.0,
                    path='a48a.%s.tape_parity' % device,
                )
                _a48_assert_plan_equal(
                    reference[1], candidate.plan,
                    'a48a.%s.plan_parity' % device,
                )
                v3.assert_recursive_close(
                    reference[2], candidate.candidate_cell.state_dict(),
                    atol=0.0, rtol=0.0,
                    path='a48a.%s.candidate_cell_parity' % device,
                )

            scheduler._commit_candidate(
                world, cell, dt, (), candidate,
            )
            final_binding = _a48_binding(world, cell, capacity)
            if (a48._binding_identity(final_binding)
                    != a48._binding_identity(candidate.fresh_binding)):
                raise AssertionError(device + ' committed binding is not fresh')
            committed_cell = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48.A4HydrolysisCommitError,
                lambda: scheduler._commit_candidate(
                    world, cell, dt, (), candidate,
                ),
            )
            v3.assert_recursive_close(
                committed_cell, cell.state_dict(), atol=0.0, rtol=0.0,
                path='a48a.%s.one_shot_cell' % device,
            )
            if world.rng.bit_generator.state != committed_rng:
                raise AssertionError(device + ' one-shot retry changed RNG')
            summaries.append(device)
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('/'.join(summaries) +
            ' candidate exact parity, prepare purity, resident device, '
            'fresh binding and one-shot rejection')


def test_a48a_fail_closed_capacity_trust_order_and_publish_rollback():
    one_short = (
        ('Q', dict(max_sequences=4, max_symbols=199,
                   max_sequence_symbols=48)),
        ('S', dict(max_sequences=5, max_symbols=198,
                   max_sequence_symbols=48)),
        ('W', dict(max_sequences=5, max_symbols=199,
                   max_sequence_symbols=47)),
    )
    for label, values in one_short:
        world, cell, _, dt = _a47b_hydrolysis_fixture()
        capacity = a4.GPU068A4Config(
            max_cells=1, max_proteins_per_cell=64, **values
        )
        scheduler = a48.A4HydrolysisEventScheduler(capacity, 'cpu')
        _a48_begin_direct(scheduler, world, cell, dt)
        before_cell = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        try:
            _assert_raises(
                a4.A4CapacityError,
                lambda: scheduler.cpu_genome_hydrolysis(
                    world, cell, (), dt,
                ),
            )
            v3.assert_recursive_close(
                before_cell, cell.state_dict(), 0.0, 0.0,
                'a48a.capacity_' + label,
            )
            if (world.rng.bit_generator.state != before_rng
                    or any(entry['event'] == 'genome_hydrolysis_cpu_rng'
                           for entry in scheduler._record(cell)[1]['events'])):
                raise AssertionError(label + ' preclaim failure was not atomic')
        finally:
            _a48_abort_direct(scheduler, cell)

    class TamperResidentPlan(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            candidate.resident_plan.final_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = TamperResidentPlan(capacity, 'cpu')
    _a48_begin_direct(scheduler, world, cell, dt)
    before_cell = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        _assert_raises(
            (a48.A4HydrolysisCommitError, a4.A4SchemaError),
            lambda: scheduler.cpu_genome_hydrolysis(
                world, cell, (), dt,
            ),
        )
        v3.assert_recursive_close(
            before_cell, cell.state_dict(), 0.0, 0.0,
            'a48a.resident_data_tamper',
        )
        if (world.rng.bit_generator.state != before_rng
                or any(entry['event'] == 'genome_hydrolysis_cpu_rng'
                       for entry in scheduler._record(cell)[1]['events'])):
            raise AssertionError('resident .data tamper crossed claim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = a48.A4HydrolysisEventScheduler(capacity, 'cpu')
    _a48_begin_direct(scheduler, world, cell, dt)
    before_cell = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3SchedulerProtocolError,
        lambda: scheduler.cpu_genome_hydrolysis(
            world, cell, (), np.nextafter(dt, np.inf),
        ),
    )
    foreign_cell = copy.deepcopy(cell)
    _assert_raises(
        a3s.A3SchedulerProtocolError,
        lambda: scheduler.cpu_genome_hydrolysis(
            world, foreign_cell, (), dt,
        ),
    )
    v3.assert_recursive_close(
        before_cell, cell.state_dict(), 0.0, 0.0,
        'a48a.wrong_cell_dt',
    )
    if world.rng.bit_generator.state != before_rng:
        raise AssertionError('wrong cell/dt changed RNG')
    _a48_abort_direct(scheduler, cell)

    class TamperResidentTape(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            candidate.resident_tape.uniform_draws.data[0].add_(0.125)
            return candidate

    class TamperHostSource(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            candidate.source_binding.ragged.symbols[0] ^= np.uint8(1)
            return candidate

    class TamperSchedulerSettings(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=4, max_symbols=199,
                max_sequence_symbols=48, max_proteins_per_cell=64,
            )
            self.a4_device = 'cuda'
            return candidate

    class TamperCandidateBeforeState(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            bit_generator = np.random.PCG64()
            bit_generator.state = copy.deepcopy(candidate.rng_before_state)
            generator = np.random.Generator(bit_generator)
            generator.random()
            candidate.rng_before_state = copy.deepcopy(
                generator.bit_generator.state
            )
            return candidate

    for scheduler_type, label in (
            (TamperResidentTape, 'resident_tape_data'),
            (TamperHostSource, 'host_source'),
            (TamperSchedulerSettings, 'scheduler_config_device'),
            (TamperCandidateBeforeState, 'candidate_rng_before')):
        world, cell, capacity, dt = _a47b_hydrolysis_fixture()
        scheduler = scheduler_type(capacity, 'cpu')
        _a48_begin_direct(scheduler, world, cell, dt)
        before_cell = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        try:
            _assert_raises(
                (a48.A4HydrolysisCommitError, a4.A4SchemaError),
                lambda: scheduler.cpu_genome_hydrolysis(
                    world, cell, (), dt,
                ),
            )
            v3.assert_recursive_close(
                before_cell, cell.state_dict(), 0.0, 0.0,
                'a48a.' + label,
            )
            if (world.rng.bit_generator.state != before_rng
                    or any(entry['event'] == 'genome_hydrolysis_cpu_rng'
                           for entry in scheduler._record(cell)[1]['events'])):
                raise AssertionError(label + ' crossed claim boundary')
        finally:
            _a48_abort_direct(scheduler, cell)

    class TamperCandidateAfterState(a48.A4HydrolysisEventScheduler):
        def _candidate_ready(self, world, cell, dt, candidate):
            bit_generator = np.random.PCG64()
            bit_generator.state = copy.deepcopy(candidate.rng_after_state)
            generator = np.random.Generator(bit_generator)
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state
            )
            return candidate

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = TamperCandidateAfterState(capacity, 'cpu')
    _a48_begin_direct(scheduler, world, cell, dt)
    before_cell = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        _assert_raises(
            a48.A4HydrolysisCommitError,
            lambda: scheduler.cpu_genome_hydrolysis(
                world, cell, (), dt,
            ),
        )
        v3.assert_recursive_close(
            before_cell, cell.state_dict(), 0.0, 0.0,
            'a48a.candidate_rng_after_tamper',
        )
        if (world.rng.bit_generator.state != before_rng
                or any(entry['event'] == 'genome_hydrolysis_cpu_rng'
                       for entry in scheduler._record(cell)[1]['events'])):
            raise AssertionError(
                'candidate RNG after-state tamper crossed claim boundary'
            )
    finally:
        _a48_abort_direct(scheduler, cell)

    class FailAfterPublish(a48.A4HydrolysisEventScheduler):
        def _publish_candidate(self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_candidate(
                world, cell, candidate,
            )
            cell.genomes[0][0] ^= np.uint8(1)
            raise RuntimeError('injected A4.8a publish failure')

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = FailAfterPublish(capacity, 'cpu')
    _a48_begin_direct(scheduler, world, cell, dt)
    before_cell = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    object_ids = (
        id(cell.genomes), id(cell.genome_lesions), id(cell.pools),
        id(cell.gene_specs), id(world.rng),
    )
    caught = None
    try:
        scheduler.cpu_genome_hydrolysis(world, cell, (), dt)
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected publish failure did not escape')
    v3.assert_recursive_close(
        before_cell, cell.state_dict(), 0.0, 0.0,
        'a48a.publish_rollback_cell',
    )
    if (world.rng.bit_generator.state != before_rng
            or object_ids != (
                id(cell.genomes), id(cell.genome_lesions), id(cell.pools),
                id(cell.gene_specs), id(world.rng),
            )):
        raise AssertionError('publish rollback did not restore identity/state')
    entry = _a48_hydrolysis_entry(scheduler, cell)
    receipt = _a48_abort_direct(scheduler, cell, caught)
    if (receipt['status'] != 'aborted'
            or entry['event'] != 'genome_hydrolysis_cpu_rng'):
        raise AssertionError('postclaim failure did not leave aborted receipt')

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = a48.A4HydrolysisEventScheduler(capacity, 'cpu')
    _a48_begin_direct(scheduler, world, cell, dt)
    scheduler.cpu_genome_hydrolysis(world, cell, (), dt)
    committed = v3.pickle_clone(cell.state_dict())
    committed_rng = copy.deepcopy(world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: scheduler.cpu_genome_hydrolysis(world, cell, (), dt),
    )
    v3.assert_recursive_close(
        committed, cell.state_dict(), 0.0, 0.0,
        'a48a.duplicate_atomic',
    )
    if world.rng.bit_generator.state != committed_rng:
        raise AssertionError('duplicate attempt changed RNG')
    _a48_abort_direct(scheduler, cell)

    world, cell, capacity, dt = _a47b_hydrolysis_fixture()
    scheduler = a48.A4HydrolysisEventScheduler(capacity, 'cpu')
    scheduler.begin_step(world, [cell])
    for event in a3s.WORLD_EVENT_ORDER[:5]:
        scheduler.claim_world(event, status='skipped')
    scheduler.claim(cell, 'surface_exchange', status='skipped')
    scheduler.reserve_metabolism_dispatch(world, cell, dt)
    scheduler.begin_a3_metabolism(world, cell, dt)
    before_cell = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: scheduler.cpu_genome_hydrolysis(world, cell, (), dt),
    )
    v3.assert_recursive_close(
        before_cell, cell.state_dict(), 0.0, 0.0,
        'a48a.out_of_order_atomic',
    )
    if world.rng.bit_generator.state != before_rng:
        raise AssertionError('out-of-order attempt changed RNG')
    _a48_abort_direct(scheduler, cell)
    return ('Q/S/W one-short; wrong cell/dt/source/config/device/PCG64; '
            'resident tape+plan .data preclaim atomic; postclaim object/'
            'state/RNG rollback + aborted receipt; duplicate/out-of-order '
            'exact-once')


def test_a48a_world_interleave_lockstep_save_clone_and_a3_authority():
    capacity = a4.GPU068A4Config()
    for steps, seed in ((1, 9881), (10, 9882)):
        source = v3.make_world(seed=seed, cells=2)
        state = v3.pickle_clone(source.state_dict())
        cpu = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        hybrid = _a48_hybrid_from_state(state, capacity, 'cpu')
        for _ in range(steps):
            cpu.step(0.1)
            hybrid.step(0.1)
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48a.lockstep_%d' % steps,
        )

    stressed = v3.make_world(seed=9886, cells=2)
    for cell in stressed.cells:
        cell.current_stress = 1.5
        cell.damage_trace[:] = 0.9
        cell.membrane_oxidation[:] = np.linspace(
            0.3, 1.8, len(cell.membrane_oxidation),
        )
        cell.pools[a4.a3.POOL_REACTIVE] += 0.16
        cell.pools[a4.a3.POOL_ATP] += 0.4
        cell._sync_damage_pool()
    state = v3.pickle_clone(stressed.state_dict())
    cpu = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    hybrid = _a48_hybrid_from_state(state, capacity, 'cpu')
    for _ in range(3):
        cpu.step(0.1)
        hybrid.step(0.1)
    v3._assert_world_pair(
        cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL,
        label='a48a.repair_heavy_stress_3',
    )

    source = v3.make_world(seed=9883, cells=2)
    source.config.endogenous_damage = False
    for cell in source.cells:
        cell.genome_lesions = [
            0.5 / 0.00065 for _ in cell.genomes
        ]
        cell._refresh_gene_cache()
    state = v3.pickle_clone(source.state_dict())
    cpu = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    hybrid = _a48_hybrid_from_state(state, capacity, 'cpu')
    cpu.step(0.1)
    original_bridge = a3s.A3EventScheduler.cpu_genome_hydrolysis

    def legacy_bridge_bomb(*args, **kwargs):
        raise AssertionError('legacy A3 hydrolysis bridge was called')

    a3s.A3EventScheduler.cpu_genome_hydrolysis = legacy_bridge_bomb
    try:
        receipt = hybrid.step(0.1)
    finally:
        a3s.A3EventScheduler.cpu_genome_hydrolysis = original_bridge
    v3._assert_world_pair(
        cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL,
        label='a48a.two_cell_interleave',
    )
    previous_motion = None
    for record in receipt['cells']:
        by_name = {entry['event']: entry for entry in record['events']}
        names = [entry['event'] for entry in record['events']]
        if names.count('genome_hydrolysis_cpu_rng') != 1:
            raise AssertionError('A4.8a integrated hydrolysis count differs')
        replication = by_name['replication_cpu']['ordinal']
        hydrolysis = by_name['genome_hydrolysis_cpu_rng']['ordinal']
        motion = by_name['motion']['ordinal']
        if not replication < hydrolysis < motion:
            raise AssertionError('per-cell RNG event order differs')
        if previous_motion is not None and previous_motion >= replication:
            raise AssertionError('two-cell RNG interleave was batched/reordered')
        previous_motion = motion
        if by_name['genome_hydrolysis_cpu_rng']['metadata'][
                'eligible_genome_count'] < 1:
            raise AssertionError('two-cell hydrolysis fixture drew no RNG')

    hybrid = _a48_hybrid_from_state(
        v3.make_world(seed=9884, cells=2).state_dict(), capacity, 'cpu',
    )
    for _ in range(2):
        hybrid.step(0.1)
    twin = hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48a.pkl')
        hybrid.save(path)
        restored = a48.Hybrid066WorldA4Hydrolysis.load(path)
        for value in (twin, restored):
            if (type(value) is not a48.Hybrid066WorldA4Hydrolysis
                    or type(value.scheduler)
                    is not a48.A4HydrolysisEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != capacity):
                raise AssertionError('A4.8a save/clone authority was lost')
        for _ in range(3):
            hybrid.step(0.1)
            twin.step(0.1)
            restored.step(0.1)
        v3.assert_recursive_close(
            hybrid.world.state_dict(), twin.world.state_dict(),
            0.0, 0.0, 'a48a.clone_continuation',
        )
        v3.assert_recursive_close(
            hybrid.world.state_dict(), restored.world.state_dict(),
            0.0, 0.0, 'a48a.save_continuation',
        )

    active = _a48_hybrid_from_state(
        v3.make_world(seed=9885, cells=1).state_dict(), capacity, 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected active-save rejection'))
    active.scheduler._a4_hydrolysis_commit_active = True
    try:
        _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
    finally:
        active.scheduler._a4_hydrolysis_commit_active = False

    if (a4.a3.PORT_STATUS.get('genome_symbol_hydrolysis_rng')
            != 'cpu-authoritative-explicit-hazard-plan'
            or a48.FULL_GPU_WORLD_STEP is not False):
        raise AssertionError('A4.8a changed promoted A3/full-GPU authority')
    for relative, expected in PROMOTED_A3_SHA256.items():
        path = os.path.join(ROOT, *relative.split('/'))
        if _sha256(path) != expected:
            raise AssertionError(relative + ' changed from promoted A3 bytes')
    return ('1/10-step + repair-heavy 3-step + 2-cell hydrolysis-stress '
            'replication/hydrolysis/motion PCG64 lockstep; '
            'legacy bridge bomb; save/load/clone continuation; active reject; '
            'A3 baseline authority unchanged')


def test_a48b_translation_atomic_commit_formal066_oracle_and_gates():
    source_world, source_cells, capacity, source_dt = _paid_translation_fixture(
        seed=9981,
    )
    source_state = v3.pickle_clone(source_world.state_dict())
    cases = []
    for label, index in (
            ('rich', 0), ('atp_reserve', 1),
            ('mineral_exhaustion', 2), ('no_translator', 3)):
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        cases.append((label, world, world.cells[index], source_dt))

    disabled = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    disabled.config.gene_expression = False
    cases.append(('disabled', disabled, disabled.cells[0], source_dt))

    no_genome = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    no_genome_cell = no_genome.cells[0]
    no_genome_cell.genomes = []
    no_genome_cell.genome_lesions = []
    no_genome_cell.replication_template = None
    no_genome_cell.replication_copy = []
    no_genome_cell.replication_template_lesion = 0.0
    no_genome_cell.replication_fractional = 0.0
    no_genome_cell._refresh_gene_cache()
    cases.append(('no_genome', no_genome, no_genome_cell, source_dt))

    zero_dt = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    cases.append(('eligible_dt0', zero_dt, zero_dt.cells[0], 0.0))

    residual = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    residual.cells[0].pools[a4.a3.POOL_FUEL] = 9.936293309191388e-08
    cases.append(('signed_residual', residual, residual.cells[0], 0.001))

    details = []
    for label, world, cell, dt in cases:
        expected = copy.deepcopy(cell)
        expected_pool_id = id(expected.pools)
        expected_active_id = id(expected.proteins)
        expected_damaged_id = id(expected.damaged_proteins)
        expected.translate(dt, world.config)
        expected_sync = (
            id(expected.proteins) != expected_active_id
            and id(expected.damaged_proteins) != expected_damaged_id
        )
        if id(expected.pools) != expected_pool_id:
            raise AssertionError(label + ' CPU oracle replaced pools')

        scheduler = a48b.A4TranslationEventScheduler(capacity, 'cpu')
        _a48b_begin_translation_direct(scheduler, world, cell, dt)
        source = _a48_binding(world, cell, capacity)
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        pool_id = id(cell.pools)
        active_id = id(cell.proteins)
        damaged_id = id(cell.damaged_proteins)
        try:
            result = scheduler.cpu_translation(
                world, cell, dt, world.config,
            )
            if result is not None:
                raise AssertionError(label + ' translation return differs')
            _a48b_assert_cell_matches(
                expected, cell, 'a48b.formal066.' + label,
            )
            if id(cell.pools) != pool_id:
                raise AssertionError(label + ' replaced pools ndarray')
            actual_sync = (
                id(cell.proteins) != active_id
                and id(cell.damaged_proteins) != damaged_id
            )
            if actual_sync != expected_sync:
                raise AssertionError(label + ' dictionary identity branch differs')
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng):
                raise AssertionError(label + ' translation changed PCG64')

            final = _a48_binding(world, cell, capacity)
            entry = _a48b_translation_entry(scheduler, cell)
            metadata = entry['metadata']
            expected_metadata = {
                'authority': (
                    'A4.8b-resident-plan-atomic-cpu-cell-commit'
                ),
                'enabled': bool(world.config.gene_expression),
                'device': 'cpu',
                'genome_count': len(cell.genomes),
                'source_provenance': a4._translation_state_provenance(
                    source.state,
                ),
                'final_provenance': a4._translation_state_provenance(
                    final.state,
                ),
                'sync_performed': expected_sync,
                'amount': float(cell.last_translation),
                'work_performed': float(cell.last_translation) > 0.0,
                'rng_draw_count': 0,
            }
            if entry['status'] != 'executed' or metadata != expected_metadata:
                raise AssertionError(label + ' receipt metadata differs')
            details.append('%s:%s' % (
                label, 'sync' if expected_sync else 'early',
            ))
        finally:
            _a48_abort_direct(scheduler, cell)

    residual_pool = cases[-1][2].pools[a4.a3.POOL_FUEL]
    if not (-a4.TRANSLATION_LEDGER_ATOL <= residual_pool < 0.0):
        raise AssertionError('integrated signed payment residual was clipped')
    return ('8 direct Formal066 cases; discrete/order/genotype/PCG64 exact, '
            'fp64 <=2e-12, pools identity and early/sync dict identity; ' +
            ', '.join(details))


def test_a48b_translation_candidate_cpu_cuda_commit_purity_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch unavailable')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable')
    public_names = set(a48b.__all__)
    signature = inspect.signature(a48b.A4TranslationEventScheduler.cpu_translation)
    if (a48b.BUILD != 'SOMA-CELL 0.6.8-GPU A4.8b'
            or a48b.SCHEMA_VERSION
            != '0.6.8-GPU-A4.8b-translation-atomic-commit'
            or a48b.FULL_GPU_WORLD_STEP is not False
            or '_A4TranslationCommitCandidate' in public_names
            or tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or not issubclass(
                a48b.A4TranslationEventScheduler,
                a48.A4HydrolysisEventScheduler,
            )):
        raise AssertionError('A4.8b identity/public authority differs')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')

    source_world, _, capacity, dt = _paid_translation_fixture(seed=9982)
    source_state = v3.pickle_clone(source_world.state_dict())
    reference_plan = None
    reference_cell = None
    details = []
    for device in devices:
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        cell = world.cells[0]
        expected = copy.deepcopy(cell)
        expected.translate(dt, world.config)
        scheduler = a48b.A4TranslationEventScheduler(capacity, device)
        _a48b_begin_translation_direct(scheduler, world, cell, dt)
        before_cell = v3.pickle_clone(cell.state_dict())
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        pool_id = id(cell.pools)
        active_id = id(cell.proteins)
        damaged_id = id(cell.damaged_proteins)
        try:
            candidate = scheduler._prepare_translation_candidate(
                world, cell, dt, world.config,
            )
            v3.assert_recursive_close(
                before_cell, cell.state_dict(), 0.0, 0.0,
                'a48b.%s.prepare_purity' % device,
            )
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng):
                raise AssertionError(device + ' prepare changed PCG64')
            for owner in (
                    candidate.resident_binding.ragged,
                    candidate.resident_binding.state,
                    candidate.resident_binding.cache,
                    candidate.resident_plan):
                pointers = owner.data_ptrs()
                if (not pointers
                        or any(getattr(owner, name).device.type != device
                               for name in pointers)):
                    raise AssertionError(device + ' resident device differs')
            _a48b_assert_cell_matches(
                expected, candidate.candidate_cell,
                'a48b.%s.candidate' % device,
            )
            _assert_translation_matches_cells(
                candidate.plan, [expected],
                'a48b.%s.resident_authority' % device,
            )
            v3.assert_recursive_close(
                candidate.host_replay.state_dict(),
                candidate.plan.state_dict(),
                atol=a48b.TRANSLATION_ORACLE_ATOL, rtol=0.0,
                path='a48b.%s.independent_replay' % device,
            )
            source_identity = a48._binding_identity(
                candidate.source_binding,
            )
            fresh_identity = a48._binding_identity(candidate.fresh_binding)
            if (source_identity[0] != fresh_identity[0]
                    or source_identity[2] != fresh_identity[2]
                    or source_identity[1] == fresh_identity[1]):
                raise AssertionError(
                    device + ' fresh physiology/genome/cache provenance differs'
                )
            if not candidate.sync_performed:
                raise AssertionError(device + ' rich fixture missed sync gate')

            if reference_plan is None:
                reference_plan = candidate.plan.clone()
                reference_cell = v3.pickle_clone(
                    candidate.candidate_cell.state_dict(),
                )
            else:
                v3.assert_recursive_close(
                    reference_plan.state_dict(), candidate.plan.state_dict(),
                    atol=a48b.TRANSLATION_ORACLE_ATOL, rtol=0.0,
                    path='a48b.%s.device_plan' % device,
                )
                v3.assert_recursive_close(
                    reference_cell, candidate.candidate_cell.state_dict(),
                    atol=a48b.TRANSLATION_ORACLE_ATOL, rtol=0.0,
                    path='a48b.%s.device_candidate' % device,
                )

            scheduler._commit_translation_candidate(
                world, cell, dt, world.config, candidate,
            )
            _a48b_assert_cell_matches(
                expected, cell, 'a48b.%s.commit' % device,
            )
            if (id(cell.pools) != pool_id
                    or id(cell.proteins) == active_id
                    or id(cell.damaged_proteins) == damaged_id):
                raise AssertionError(device + ' success object identity differs')
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng):
                raise AssertionError(device + ' commit changed PCG64')
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48b.A4TranslationCommitError,
                lambda: scheduler._commit_translation_candidate(
                    world, cell, dt, world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48b.%s.one_shot' % device,
            )
            if world.rng.bit_generator.state != committed_rng:
                raise AssertionError(device + ' one-shot changed PCG64')
            details.append(device)
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('/'.join(details) +
            ' resident authority + independent replay, prepare purity, '
            'fresh binding, CPU identity branch, no RNG and one-shot')


def test_a48b_translation_capacity_trust_rollback_and_exact_once():
    source_world, _, roomy, dt = _paid_translation_fixture(seed=9983)
    source_state = v3.pickle_clone(source_world.state_dict())
    source_cell = source_world.cells[0]
    probe = a4.FullFidelityA4GenomeAdapter(roomy).pack_cells([source_cell])
    lengths = np.diff(
        probe.sequence_offsets[:int(probe.sequence_count) + 1],
    )
    specs = a4.decode_a4_gene_cache_numpy(probe).materialize_gene_specs_host()[0]
    protein_capacity = max(
        len(set(source_cell.proteins).union(specs)),
        len(set(source_cell.damaged_proteins).union(specs)),
    )
    exact_values = {
        'max_cells': 1,
        'max_sequences': int(probe.sequence_count),
        'max_symbols': int(probe.symbol_count),
        'max_sequence_symbols': int(np.max(lengths)),
        'max_proteins_per_cell': int(protein_capacity),
    }
    if any(exact_values[name] <= 1 for name in (
            'max_sequences', 'max_symbols', 'max_sequence_symbols',
            'max_proteins_per_cell')):
        raise AssertionError('A4.8b capacity fixture is too small')

    exact = a4.GPU068A4Config(**exact_values)
    exact_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    exact_cell = exact_world.cells[0]
    exact_scheduler = a48b.A4TranslationEventScheduler(exact, 'cpu')
    _a48b_begin_translation_direct(
        exact_scheduler, exact_world, exact_cell, dt,
    )
    try:
        exact_scheduler.cpu_translation(
            exact_world, exact_cell, dt, exact_world.config,
        )
    finally:
        _a48_abort_direct(exact_scheduler, exact_cell)

    for axis in (
            'max_sequences', 'max_symbols', 'max_sequence_symbols',
            'max_proteins_per_cell'):
        values = dict(exact_values)
        values[axis] -= 1
        capacity = a4.GPU068A4Config(**values)
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        cell = world.cells[0]
        scheduler = a48b.A4TranslationEventScheduler(capacity, 'cpu')
        _a48b_begin_translation_direct(scheduler, world, cell, dt)
        before = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        try:
            _assert_raises(
                a4.A4CapacityError,
                lambda: scheduler.cpu_translation(
                    world, cell, dt, world.config,
                ),
            )
            v3.assert_recursive_close(
                before, cell.state_dict(), 0.0, 0.0,
                'a48b.capacity_' + axis,
            )
            _, record = scheduler._record(cell)
            if (world.rng.bit_generator.state != before_rng
                    or any(entry['event'] == 'translation_cpu'
                           for entry in record['events'])):
                raise AssertionError(axis + ' one-short crossed claim')
        finally:
            _a48_abort_direct(scheduler, cell)

    class TamperHostSource(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.source_binding.state.pools[0, a4.a3.POOL_FUEL] += 1e-8
            return candidate

    class TamperHostReplay(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.last_translation[0] += 1e-8
            return candidate

    class TamperResidentRagged(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentState(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_FUEL
            ].add_(1e-8)
            return candidate

    class TamperResidentCache(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class SwapResidentRaggedClone(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'ragged',
                candidate.resident_binding.ragged.clone(),
            )
            return candidate

    class SwapResidentStateClone(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'state',
                candidate.resident_binding.state.clone(),
            )
            return candidate

    class SwapResidentCacheClone(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'cache',
                candidate.resident_binding.cache.clone(),
            )
            return candidate

    class TamperResidentPlan(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.last_translation.data[0].add_(1e-8)
            return candidate

    class SwapResidentPlanAuthority(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan = candidate.plan
            return candidate

    class TamperSettings(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = 'cuda'
            return candidate

    class TamperCandidateCell(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.last_translation += 1e-8
            return candidate

    class TamperCandidateMembrane(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.membrane[0] = np.nextafter(
                candidate.candidate_cell.membrane[0], np.inf,
            )
            return candidate

    class TamperCandidateGeneSpecs(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            fingerprint = next(iter(candidate.candidate_cell.gene_specs))
            candidate.candidate_cell.gene_specs[fingerprint]['promoter'] += 0.125
            return candidate

    class MutateUnpackedCandidate(a48b.A4TranslationEventScheduler):
        isolated = False

        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.isolated = (
                candidate.candidate_cell.last_repair_flux
                is not cell.last_repair_flux
            )
            candidate.candidate_cell.last_repair_flux[0] += 0.125
            return candidate

    class TamperFreshBinding(a48b.A4TranslationEventScheduler):
        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.state.pools[0, a4.a3.POOL_FUEL] += 1e-8
            return candidate

    for scheduler_type, label in (
            (TamperHostSource, 'host_source'),
            (TamperHostReplay, 'host_replay'),
            (TamperResidentRagged, 'resident_ragged_data'),
            (TamperResidentState, 'resident_state_data'),
            (TamperResidentCache, 'resident_cache_data'),
            (SwapResidentRaggedClone, 'resident_ragged_member_clone'),
            (SwapResidentStateClone, 'resident_state_member_clone'),
            (SwapResidentCacheClone, 'resident_cache_member_clone'),
            (TamperResidentPlan, 'resident_plan_data'),
            (SwapResidentPlanAuthority, 'resident_plan_authority_swap'),
            (TamperSettings, 'config_device'),
            (TamperCandidateCell, 'candidate_cell'),
            (TamperCandidateMembrane, 'candidate_membrane'),
            (TamperCandidateGeneSpecs, 'candidate_gene_specs'),
            (TamperFreshBinding, 'fresh_binding')):
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        cell = world.cells[0]
        scheduler = scheduler_type(roomy, 'cpu')
        _a48b_begin_translation_direct(scheduler, world, cell, dt)
        before = v3.pickle_clone(cell.state_dict())
        before_world = v3.pickle_clone(world.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_objects = (
            id(cell.pools), id(cell.proteins),
            id(cell.damaged_proteins), id(cell.gene_specs), id(world.rng),
        )
        before_receipt_count = len(scheduler._receipts)
        try:
            _assert_raises(
                (a48b.A4TranslationCommitError, a4.A4SchemaError),
                lambda: scheduler.cpu_translation(
                    world, cell, dt, world.config,
                ),
            )
            v3.assert_recursive_close(
                before, cell.state_dict(), 0.0, 0.0,
                'a48b.tamper_' + label,
            )
            v3.assert_recursive_close(
                before_world, world.state_dict(), 0.0, 0.0,
                'a48b.tamper_world_' + label,
            )
            _, record = scheduler._record(cell)
            if (world.rng.bit_generator.state != before_rng
                    or before_objects != (
                        id(cell.pools), id(cell.proteins),
                        id(cell.damaged_proteins), id(cell.gene_specs),
                        id(world.rng),
                    )
                    or len(scheduler._receipts) != before_receipt_count
                    or any(entry['event'] == 'translation_cpu'
                           for entry in record['events'])):
                raise AssertionError(label + ' crossed preclaim boundary')
        finally:
            _a48_abort_direct(scheduler, cell)

    if torch is not None and torch.cuda.is_available():
        for scheduler_type, label in (
                (TamperCandidateMembrane, 'candidate_membrane'),
                (SwapResidentRaggedClone, 'resident_ragged_member_clone'),
                (SwapResidentStateClone, 'resident_state_member_clone'),
                (SwapResidentCacheClone, 'resident_cache_member_clone'),
                (SwapResidentPlanAuthority, 'resident_plan_authority_swap')):
            cuda_world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(source_state),
            )
            cuda_cell = cuda_world.cells[0]
            cuda_scheduler = scheduler_type(roomy, 'cuda')
            _a48b_begin_translation_direct(
                cuda_scheduler, cuda_world, cuda_cell, dt,
            )
            cuda_before = v3.pickle_clone(cuda_cell.state_dict())
            cuda_world_before = v3.pickle_clone(cuda_world.state_dict())
            cuda_rng = copy.deepcopy(cuda_world.rng.bit_generator.state)
            cuda_objects = (
                id(cuda_cell.pools), id(cuda_cell.proteins),
                id(cuda_cell.damaged_proteins), id(cuda_cell.gene_specs),
                id(cuda_world.rng),
            )
            cuda_receipt_count = len(cuda_scheduler._receipts)
            try:
                _assert_raises(
                    (a48b.A4TranslationCommitError, a4.A4SchemaError),
                    lambda: cuda_scheduler.cpu_translation(
                        cuda_world, cuda_cell, dt, cuda_world.config,
                    ),
                )
                v3.assert_recursive_close(
                    cuda_before, cuda_cell.state_dict(), 0.0, 0.0,
                    'a48b.cuda_' + label,
                )
                v3.assert_recursive_close(
                    cuda_world_before, cuda_world.state_dict(), 0.0, 0.0,
                    'a48b.cuda_world_' + label,
                )
                _, cuda_record = cuda_scheduler._record(cuda_cell)
                if (cuda_world.rng.bit_generator.state != cuda_rng
                        or cuda_objects != (
                            id(cuda_cell.pools), id(cuda_cell.proteins),
                            id(cuda_cell.damaged_proteins),
                            id(cuda_cell.gene_specs), id(cuda_world.rng),
                        )
                        or len(cuda_scheduler._receipts)
                        != cuda_receipt_count
                        or any(entry['event'] == 'translation_cpu'
                               for entry in cuda_record['events'])):
                    raise AssertionError(
                        'CUDA %s tamper crossed preclaim boundary' % label
                    )
            finally:
                _a48_abort_direct(cuda_scheduler, cuda_cell)
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required for A4.8b trust regression')

    isolation_devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        isolation_devices.append('cuda')
    for device in isolation_devices:
        isolation_world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        isolation_cell = isolation_world.cells[0]
        isolation_before = v3.pickle_clone(isolation_cell.state_dict())
        isolation_world_before = v3.pickle_clone(
            isolation_world.state_dict(),
        )
        repair_flux_object = isolation_cell.last_repair_flux
        repair_flux_before = np.asarray(
            isolation_cell.last_repair_flux, dtype=np.float64,
        ).copy()
        isolation_rng = copy.deepcopy(
            isolation_world.rng.bit_generator.state,
        )
        isolation_objects = (
            id(isolation_cell.pools), id(isolation_cell.proteins),
            id(isolation_cell.damaged_proteins),
            id(isolation_cell.gene_specs), id(isolation_world.rng),
        )
        isolation_scheduler = MutateUnpackedCandidate(roomy, device)
        _a48b_begin_translation_direct(
            isolation_scheduler, isolation_world, isolation_cell, dt,
        )
        isolation_receipt_count = len(isolation_scheduler._receipts)
        try:
            _assert_raises(
                (a48b.A4TranslationCommitError, a4.A4SchemaError),
                lambda: isolation_scheduler.cpu_translation(
                    isolation_world, isolation_cell, dt,
                    isolation_world.config,
                ),
            )
            if not isolation_scheduler.isolated:
                raise AssertionError(
                    device + ' unpacked candidate array aliases live state'
                )
            v3.assert_recursive_close(
                isolation_before, isolation_cell.state_dict(), 0.0, 0.0,
                'a48b.%s.unpacked_candidate_reject' % device,
            )
            v3.assert_recursive_close(
                isolation_world_before, isolation_world.state_dict(),
                0.0, 0.0,
                'a48b.%s.unpacked_candidate_world' % device,
            )
            if (isolation_cell.last_repair_flux is not repair_flux_object
                    or not np.array_equal(
                        isolation_cell.last_repair_flux, repair_flux_before,
                    )
                    or isolation_world.rng.bit_generator.state
                    != isolation_rng
                    or isolation_objects != (
                        id(isolation_cell.pools),
                        id(isolation_cell.proteins),
                        id(isolation_cell.damaged_proteins),
                        id(isolation_cell.gene_specs),
                        id(isolation_world.rng),
                    )
                    or len(isolation_scheduler._receipts)
                    != isolation_receipt_count):
                raise AssertionError(
                    device + ' unpacked candidate mutation escaped isolation'
                )
            _, isolation_record = isolation_scheduler._record(
                isolation_cell,
            )
            if any(entry['event'] == 'translation_cpu'
                   for entry in isolation_record['events']):
                raise AssertionError(
                    device + ' unpacked candidate tamper crossed claim'
                )
        finally:
            _a48_abort_direct(isolation_scheduler, isolation_cell)

    stale_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    stale_cell = stale_world.cells[0]
    stale_fingerprint = next(iter(stale_cell.gene_specs))
    stale_cell.gene_specs[stale_fingerprint]['promoter'] += 0.125
    stale_scheduler = a48b.A4TranslationEventScheduler(roomy, 'cpu')
    _a48b_begin_translation_direct(
        stale_scheduler, stale_world, stale_cell, dt,
    )
    stale_before = v3.pickle_clone(stale_cell.state_dict())
    stale_rng = copy.deepcopy(stale_world.rng.bit_generator.state)
    try:
        _assert_raises(
            (a48b.A4TranslationCommitError, a4.A4SchemaError),
            lambda: stale_scheduler.cpu_translation(
                stale_world, stale_cell, dt, stale_world.config,
            ),
        )
        v3.assert_recursive_close(
            stale_before, stale_cell.state_dict(), 0.0, 0.0,
            'a48b.preexisting_stale_gene_specs',
        )
        _, stale_record = stale_scheduler._record(stale_cell)
        if (stale_world.rng.bit_generator.state != stale_rng
                or any(entry['event'] == 'translation_cpu'
                       for entry in stale_record['events'])):
            raise AssertionError('stale gene_specs crossed claim boundary')
    finally:
        _a48_abort_direct(stale_scheduler, stale_cell)

    class TamperLiveGeneSpecs(a48b.A4TranslationEventScheduler):
        tampered_state = None

        def _translation_candidate_ready(
                self, world, cell, dt, config, candidate):
            fingerprint = next(iter(cell.gene_specs))
            cell.gene_specs[fingerprint]['promoter'] += 0.125
            self.tampered_state = v3.pickle_clone(cell.state_dict())
            return candidate

    live_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    live_cell = live_world.cells[0]
    live_scheduler = TamperLiveGeneSpecs(roomy, 'cpu')
    _a48b_begin_translation_direct(live_scheduler, live_world, live_cell, dt)
    live_rng = copy.deepcopy(live_world.rng.bit_generator.state)
    try:
        _assert_raises(
            (a48b.A4TranslationCommitError, a4.A4SchemaError),
            lambda: live_scheduler.cpu_translation(
                live_world, live_cell, dt, live_world.config,
            ),
        )
        if live_scheduler.tampered_state is None:
            raise AssertionError('live gene_specs seam did not run')
        v3.assert_recursive_close(
            live_scheduler.tampered_state, live_cell.state_dict(),
            0.0, 0.0, 'a48b.live_gene_specs_no_extra_mutation',
        )
        _, live_record = live_scheduler._record(live_cell)
        if (live_world.rng.bit_generator.state != live_rng
                or any(entry['event'] == 'translation_cpu'
                       for entry in live_record['events'])):
            raise AssertionError('live gene_specs tamper crossed claim boundary')
    finally:
        _a48_abort_direct(live_scheduler, live_cell)

    wrong_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    wrong_cell = wrong_world.cells[0]
    wrong_scheduler = a48b.A4TranslationEventScheduler(roomy, 'cpu')
    _a48b_begin_translation_direct(
        wrong_scheduler, wrong_world, wrong_cell, dt,
    )
    wrong_before = v3.pickle_clone(wrong_cell.state_dict())
    wrong_rng = copy.deepcopy(wrong_world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3SchedulerProtocolError,
        lambda: wrong_scheduler.cpu_translation(
            wrong_world, wrong_cell, np.nextafter(dt, np.inf),
            wrong_world.config,
        ),
    )
    foreign = copy.deepcopy(wrong_cell)
    _assert_raises(
        a3s.A3SchedulerProtocolError,
        lambda: wrong_scheduler.cpu_translation(
            wrong_world, foreign, dt, wrong_world.config,
        ),
    )
    v3.assert_recursive_close(
        wrong_before, wrong_cell.state_dict(), 0.0, 0.0,
        'a48b.wrong_cell_dt',
    )
    if wrong_world.rng.bit_generator.state != wrong_rng:
        raise AssertionError('wrong cell/dt changed PCG64')
    _a48_abort_direct(wrong_scheduler, wrong_cell)

    class FailAfterPublishNoGeneTouch(a48b.A4TranslationEventScheduler):
        def _publish_translation_candidate(self, world, cell, candidate):
            super(FailAfterPublishNoGeneTouch, self)._publish_translation_candidate(
                world, cell, candidate,
            )
            cell.pools[a4.a3.POOL_FUEL] += 1.0
            cell.proteins[999999] = 1.0
            cell.damaged_proteins[999998] = 1.0
            cell.last_translation += 1.0
            raise RuntimeError('injected A4.8b publish failure without gene touch')

    class FailAfterPublishGeneReplace(a48b.A4TranslationEventScheduler):
        def _publish_translation_candidate(self, world, cell, candidate):
            super(FailAfterPublishGeneReplace, self)._publish_translation_candidate(
                world, cell, candidate,
            )
            cell.pools[a4.a3.POOL_FUEL] += 1.0
            cell.proteins[999999] = 1.0
            cell.damaged_proteins[999998] = 1.0
            cell.last_translation += 1.0
            fingerprint = next(iter(cell.gene_specs))
            cell.gene_specs[fingerprint]['promoter'] += 0.125
            cell.gene_specs = copy.deepcopy(cell.gene_specs)
            raise RuntimeError('injected A4.8b publish failure')

    rollback_devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        rollback_devices.append('cuda')

    def run_publish_rollback(failure_type, failure_label, device):
        rollback_world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        rollback_cell = rollback_world.cells[0]
        rollback_scheduler = failure_type(roomy, device)
        _a48b_begin_translation_direct(
            rollback_scheduler, rollback_world, rollback_cell, dt,
        )
        rollback_before = v3.pickle_clone(rollback_cell.state_dict())
        rollback_rng_object = rollback_world.rng
        rollback_rng = copy.deepcopy(rollback_world.rng.bit_generator.state)
        rollback_ids = (
            id(rollback_cell.pools), id(rollback_cell.proteins),
            id(rollback_cell.damaged_proteins),
            id(rollback_cell.gene_specs), id(rollback_world.rng),
        )
        rollback_nested_spec_ids = {
            int(fingerprint): id(spec)
            for fingerprint, spec in rollback_cell.gene_specs.items()
        }
        rollback_gene_order = list(rollback_cell.gene_specs)
        caught = None
        try:
            rollback_scheduler.cpu_translation(
                rollback_world, rollback_cell, dt, rollback_world.config,
            )
        except RuntimeError as error:
            caught = error
        if caught is None:
            raise AssertionError(
                '%s/%s injected translation publish failure escaped' % (
                    device, failure_label,
                )
            )
        v3.assert_recursive_close(
            rollback_before, rollback_cell.state_dict(), 0.0, 0.0,
            'a48b.%s.%s.publish_rollback' % (device, failure_label),
        )
        actual_nested_spec_ids = {
            int(fingerprint): id(spec)
            for fingerprint, spec in rollback_cell.gene_specs.items()
        }
        if (rollback_world.rng is not rollback_rng_object
                or rollback_world.rng.bit_generator.state != rollback_rng
                or rollback_ids != (
                    id(rollback_cell.pools), id(rollback_cell.proteins),
                    id(rollback_cell.damaged_proteins),
                    id(rollback_cell.gene_specs), id(rollback_world.rng),
                )
                or actual_nested_spec_ids != rollback_nested_spec_ids
                or list(rollback_cell.gene_specs) != rollback_gene_order):
            raise AssertionError(
                '%s/%s translation rollback identity/state differs' % (
                    device, failure_label,
                )
            )
        _a48b_translation_entry(rollback_scheduler, rollback_cell)
        receipt = _a48_abort_direct(
            rollback_scheduler, rollback_cell, caught,
        )
        if receipt['status'] != 'aborted':
            raise AssertionError(
                '%s/%s postclaim translation failure was not aborted' % (
                    device, failure_label,
                )
            )

    for failure_type, failure_label in (
            (FailAfterPublishNoGeneTouch, 'no_gene_touch'),
            (FailAfterPublishGeneReplace, 'nested_and_outer_gene_replace')):
        for device in rollback_devices:
            run_publish_rollback(failure_type, failure_label, device)

    duplicate_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    duplicate_cell = duplicate_world.cells[0]
    duplicate_scheduler = a48b.A4TranslationEventScheduler(roomy, 'cpu')
    _a48b_begin_translation_direct(
        duplicate_scheduler, duplicate_world, duplicate_cell, dt,
    )
    duplicate_scheduler.cpu_translation(
        duplicate_world, duplicate_cell, dt, duplicate_world.config,
    )
    duplicate_before = v3.pickle_clone(duplicate_cell.state_dict())
    duplicate_rng = copy.deepcopy(duplicate_world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_translation(
            duplicate_world, duplicate_cell, dt, duplicate_world.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate_cell.state_dict(), 0.0, 0.0,
        'a48b.duplicate',
    )
    if duplicate_world.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate translation changed PCG64')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    order_cell = order_world.cells[0]
    order_scheduler = a48b.A4TranslationEventScheduler(roomy, 'cpu')
    order_scheduler.begin_step(order_world, [order_cell])
    metabolism_rank = a3s.WORLD_EVENT_ORDER.index('cell_metabolism_loop')
    for event in a3s.WORLD_EVENT_ORDER[:metabolism_rank]:
        order_scheduler.claim_world(event, status='skipped')
    order_scheduler.claim(order_cell, 'surface_exchange', status='skipped')
    order_scheduler.reserve_metabolism_dispatch(order_world, order_cell, dt)
    order_scheduler.begin_a3_metabolism(order_world, order_cell, dt)
    order_scheduler.claim(order_cell, 'gene_refresh', status='skipped')
    order_before = v3.pickle_clone(order_cell.state_dict())
    order_rng = copy.deepcopy(order_world.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_translation(
            order_world, order_cell, dt, order_world.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order_cell.state_dict(), 0.0, 0.0,
        'a48b.out_of_order',
    )
    if order_world.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order translation changed PCG64')
    _a48_abort_direct(order_scheduler, order_cell)
    return ('Q/S/W/P exact + each one-short; host/resident member, storage, '
            'device, authority and full-candidate trust rejection; packed/'
            'unpacked non-alias; wrong cell/dt; preclaim atomic; postclaim '
            'nested gene-spec/value/object/RNG rollback; duplicate/out-of-order')


def test_a48b_world_lockstep_stress_interleave_save_clone_and_a3_authority():
    capacity = a4.GPU068A4Config()
    for steps, seed in ((1, 9991), (10, 9992)):
        source = v3.make_world(seed=seed, cells=1)
        state = v3.pickle_clone(source.state_dict())
        cpu = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        hybrid = _a48b_hybrid_from_state(state, capacity, 'cpu')
        for _ in range(steps):
            cpu.step(0.1)
            hybrid.step(0.1)
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48b.lockstep_%d' % steps,
        )

    stressed = v3.make_world(seed=9993, cells=1)
    stressed.config.external_translator = True
    stressed.config.protein_repair = True
    stressed.config.quiescence = True
    stressed.config.quiescence_effector = True
    for ci, cell in enumerate(stressed.cells):
        cell.current_stress = 1.4 + 0.1 * ci
        cell.damage_trace[:] = 0.8 + 0.05 * ci
        cell.membrane_oxidation[:] = np.linspace(
            0.25, 1.75, len(cell.membrane_oxidation),
        )
        cell.pools[a4.a3.POOL_REACTIVE] += 0.14 + 0.01 * ci
        cell.pools[a4.a3.POOL_AGGREGATE] += 0.06
        cell.pools[a4.a3.POOL_ATP] += 0.45
        cell.behavioural_quiescence = 0.55 + 0.05 * ci
        cell._sync_damage_pool()
    stress_state = v3.pickle_clone(stressed.state_dict())
    stress_cpu = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(stress_state),
    )
    stress_hybrid = _a48b_hybrid_from_state(
        stress_state, capacity, 'cpu',
    )
    for _ in range(3):
        stress_cpu.step(0.1)
        stress_hybrid.step(0.1)
    v3._assert_world_pair(
        stress_cpu, stress_hybrid, state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL,
        label='a48b.translation_heavy_stress_3',
    )

    interleave_source = v3.make_world(seed=9994, cells=2)
    interleave_source.config.endogenous_damage = False
    for cell in interleave_source.cells:
        cell.genome_lesions = [
            0.5 / 0.00065 for _ in cell.genomes
        ]
        cell._refresh_gene_cache()
    interleave_state = v3.pickle_clone(interleave_source.state_dict())
    interleave_cpu = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(interleave_state),
    )
    interleave_hybrid = _a48b_hybrid_from_state(
        interleave_state, capacity, 'cpu',
    )
    interleave_cpu.step(0.1)
    original_translation = a48.A4HydrolysisEventScheduler.cpu_translation

    def legacy_translation_bomb(*args, **kwargs):
        raise AssertionError('legacy CPU translation bridge was called')

    a48.A4HydrolysisEventScheduler.cpu_translation = legacy_translation_bomb
    try:
        receipt = interleave_hybrid.step(0.1)
    finally:
        a48.A4HydrolysisEventScheduler.cpu_translation = original_translation
    v3._assert_world_pair(
        interleave_cpu, interleave_hybrid,
        state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL,
        label='a48b.two_cell_interleave',
    )
    previous_motion = None
    for record in receipt['cells']:
        by_name = {entry['event']: entry for entry in record['events']}
        names = [entry['event'] for entry in record['events']]
        if (names.count('translation_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1):
            raise AssertionError('A4.8b integrated event count differs')
        translation = by_name['translation_cpu']['ordinal']
        replication = by_name['replication_cpu']['ordinal']
        hydrolysis = by_name['genome_hydrolysis_cpu_rng']['ordinal']
        motion = by_name['motion']['ordinal']
        if not translation < replication < hydrolysis < motion:
            raise AssertionError('A4.8b per-cell event order differs')
        if previous_motion is not None and previous_motion >= translation:
            raise AssertionError('A4.8b two-cell chain was batched/reordered')
        previous_motion = motion
        if (by_name['translation_cpu']['metadata']['authority']
                != 'A4.8b-resident-plan-atomic-cpu-cell-commit'):
            raise AssertionError('A4.8b translation authority differs')
        if by_name['genome_hydrolysis_cpu_rng']['metadata'][
                'eligible_genome_count'] < 1:
            raise AssertionError('interleave fixture drew no hydrolysis RNG')

    persisted = _a48b_hybrid_from_state(
        v3.make_world(seed=9995, cells=1).state_dict(), capacity, 'cpu',
    )
    persisted.step(0.1)
    twin = persisted.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48b.pkl')
        persisted.save(path)
        restored = a48b.Hybrid066WorldA4Translation.load(path)
        for value in (twin, restored):
            if (type(value) is not a48b.Hybrid066WorldA4Translation
                    or type(value.scheduler)
                    is not a48b.A4TranslationEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != capacity):
                raise AssertionError('A4.8b save/clone authority was lost')
        persisted.step(0.1)
        twin.step(0.1)
        restored.step(0.1)
        v3.assert_recursive_close(
            persisted.world.state_dict(), twin.world.state_dict(),
            0.0, 0.0, 'a48b.clone_continuation',
        )
        v3.assert_recursive_close(
            persisted.world.state_dict(), restored.world.state_dict(),
            0.0, 0.0, 'a48b.save_continuation',
        )

    active = _a48b_hybrid_from_state(
        v3.make_world(seed=9996, cells=1).state_dict(), capacity, 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected active-save rejection'))
    active.scheduler._a4_translation_commit_active = True
    try:
        _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
    finally:
        active.scheduler._a4_translation_commit_active = False

    boundary_details = []
    devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required for A4.8b boundary authority')
    for device in devices:
        for (label, state, cell_id, dt,
             expected_symbols, expected_rng_change) in (
                _a48b_replication_boundary_cases(seed=10001)
        ):
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            integrated_hybrid = _a48b_hybrid_from_state(
                state, capacity, device,
            )
            integrated = integrated_hybrid.world
            direct_cell = next(
                cell for cell in direct.cells if int(cell.cell_id) == cell_id
            )
            integrated_cell = next(
                cell for cell in integrated.cells
                if int(cell.cell_id) == cell_id
            )
            direct_before_rng = copy.deepcopy(
                direct.rng.bit_generator.state,
            )
            integrated_before_rng = copy.deepcopy(
                integrated.rng.bit_generator.state,
            )
            direct_cell.translate(dt, direct.config)
            direct_cell._replicate_genome(direct, dt, direct.config)

            scheduler = integrated_hybrid.scheduler
            _a48b_begin_translation_direct(
                scheduler, integrated, integrated_cell, dt,
            )
            scheduler.cpu_translation(
                integrated, integrated_cell, dt, integrated.config,
            )
            if integrated.rng.bit_generator.state != integrated_before_rng:
                raise AssertionError(
                    '%s/%s translation consumed replication RNG' % (
                        device, label,
                    )
                )
            scheduler.cpu_replication(
                integrated, integrated_cell, dt, integrated.config,
            )
            _a48b_assert_cell_matches(
                direct_cell, integrated_cell,
                'a48b.boundary.%s.%s' % (device, label),
                atol=v3.WORLD_FP64_ATOL,
            )
            if direct.rng.bit_generator.state != integrated.rng.bit_generator.state:
                raise AssertionError(
                    '%s/%s post-translation replication PCG64 differs' % (
                        device, label,
                    )
                )
            if (int(integrated_cell.last_replication_symbols)
                    != expected_symbols):
                raise AssertionError(
                    '%s/%s discrete replication gate differs' % (
                        device, label,
                    )
                )
            actual_rng_change = (
                integrated.rng.bit_generator.state != integrated_before_rng
            )
            direct_rng_change = (
                direct.rng.bit_generator.state != direct_before_rng
            )
            if (actual_rng_change != expected_rng_change
                    or direct_rng_change != expected_rng_change):
                raise AssertionError(
                    '%s/%s RNG branch control differs' % (device, label)
                )
            replication_entry = [
                entry for entry in scheduler._record(integrated_cell)[1]['events']
                if entry['event'] == 'replication_cpu'
            ]
            if (len(replication_entry) != 1
                    or replication_entry[0]['metadata']['authority']
                    != 'frozen-0.6.6-wrapper-to-0.4'):
                raise AssertionError('A3 CPU replication authority differs')
            _a48_abort_direct(scheduler, integrated_cell)
            boundary_details.append(device + ':' + label)

    if (a48b.FULL_GPU_WORLD_STEP is not False
            or a48b.PORT_STATUS.get('genome_replication')
            != 'cpu-authoritative-scheduler-bridged'
            or a3s.PORT_STATUS.get('translation_replication_division')
            != 'CPU-authoritative-scheduler-bridged'):
        raise AssertionError('A4.8b changed A3/full-GPU authority')
    for relative, expected in PROMOTED_A3_SHA256.items():
        path = os.path.join(ROOT, *relative.split('/'))
        if _sha256(path) != expected:
            raise AssertionError(relative + ' changed from promoted A3 bytes')
    return ('1/10-step + translation-heavy stress + 2-cell nonbatched '
            'translation/replication/hydrolysis/motion lockstep; legacy '
            'translation bomb; save/load/clone + active reject; immediate '
            'A3 CPU replication boundaries and PCG64 exact: ' +
            ', '.join(boundary_details))


def _a48c_begin_replication_direct(scheduler, world, cell, dt):
    """Enter exactly the post-translation replication event boundary."""
    _a48b_begin_translation_direct(scheduler, world, cell, dt)
    scheduler.claim(
        cell, 'translation_cpu', status='skipped',
        metadata={'validation': 'A4.8c1'},
    )
    return scheduler


def _a48c_replication_entry(scheduler, cell):
    _, record = scheduler._record(cell)
    entries = [entry for entry in record['events']
               if entry['event'] == 'replication_cpu']
    if len(entries) != 1:
        raise AssertionError(
            'A4.8c1 replication event count is %d' % len(entries)
        )
    return copy.deepcopy(entries[0])


def _a48c_hybrid_from_state(state, a4_config=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c.Hybrid066WorldA4Replication(
        world, backend=backend,
        a4_config=a4_config or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48c_reduce_world(world, cells):
    world.cells = list(cells)
    world.initial_total_material = world.total_material()
    world.last_step_material_residual = 0.0
    return world


def _a48c_identity_snapshot(world, cell):
    return {
        'rng': id(world.rng),
        'pools': id(cell.pools),
        'genomes': id(cell.genomes),
        'genome_items': tuple(id(value) for value in cell.genomes),
        'lesions': id(cell.genome_lesions),
        'template': id(cell.replication_template),
        'copy': id(cell.replication_copy),
        'events': id(cell.mutation_events),
        'proteins': id(cell.proteins),
        'damaged': id(cell.damaged_proteins),
        'gene_specs': id(cell.gene_specs),
        'gene_spec_items': tuple(
            (int(key), id(value)) for key, value in cell.gene_specs.items()
        ),
    }


def _a48c_assert_oracle(expected_world, expected_cell, actual_world,
                        actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state):
        raise AssertionError(label + ' full PCG64 state differs')
    v3.assert_recursive_close(
        float(expected_world.dissipated_energy),
        float(actual_world.dissipated_energy),
        atol=0.0, rtol=0.0, path=label + '.dissipated_energy',
    )
    if (expected_cell.replication_template is None
            or actual_cell.replication_template is None
            or len(expected_cell.replication_copy)
            >= len(expected_cell.replication_template)
            or len(actual_cell.replication_copy)
            >= len(actual_cell.replication_template)):
        raise AssertionError(label + ' escaped active noncompletion scope')


def _a48c_oracle_cases(seed=10101):
    """Fresh one-cell states spanning both supported c1 branches."""
    cases = []

    world, cells, _, dt = _paid_replication_b_fixture(seed=seed)
    _a48c_reduce_world(world, [cells[0]])
    cases.append((
        'deterministic-rich', v3.pickle_clone(world.state_dict()),
        int(cells[0].cell_id), dt, False, 'ordinary',
    ))

    world, cells, _, dt = _paid_replication_b_fixture(seed=seed + 5)
    cell = cells[1]
    _a48c_reduce_world(world, [cell])
    cases.append((
        'requested-zero', v3.pickle_clone(world.state_dict()),
        int(cell.cell_id), dt, False, 'requested-zero',
    ))

    for offset, label, mutation_rate, mode in (
            (1, 'substitution-miss', 0.0, 'miss'),
            (2, 'substitution-hit', 1.0, 'hit')):
        world, cells, _, dt = _a45_substitution_fixture(
            seed=seed + offset,
        )
        cell = cells[0]
        world.config.proofreading = False
        world.config.mutation_rate = float(mutation_rate)
        cell.replication_template_lesion = 0.0
        cell.pools[a4.a3.POOL_REACTIVE] = 0.0
        cell.genome_lesions = [0.0 for _ in cell.genomes]
        cell._refresh_gene_cache()
        _a48c_reduce_world(world, [cell])
        cases.append((
            label, v3.pickle_clone(world.state_dict()),
            int(cell.cell_id), dt, True, mode,
        ))

    world, cells, _, _ = _a45_substitution_fixture(seed=seed + 3)
    cell = cells[0]
    cell.replication_fractional = 0.375
    _a48c_reduce_world(world, [cell])
    cases.append((
        'dt-zero', v3.pickle_clone(world.state_dict()),
        int(cell.cell_id), 0.0, True, 'zero',
    ))

    world, cells, _, dt = _paid_replication_fixture(seed=seed + 4)
    world.config.mutation = True
    world.config.mutation_rate = 1.0
    cell = cells[3]
    _a48c_reduce_world(world, [cell])
    cases.append((
        'resource-stop', v3.pickle_clone(world.state_dict()),
        int(cell.cell_id), dt, True, 'zero',
    ))
    return cases


def test_a48c1_active_noncompletion_atomic_commit_formal066_oracle():
    if (_REQUIRE_CUDA
            and (torch is None or not torch.cuda.is_available())):
        raise AssertionError('CUDA required for A4.8c1 requested-zero')
    capacity = a4.GPU068A4Config()
    details = []

    class ObserveReplicationPlan(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            plan = candidate.plan
            self.plan_observation = {
                'requested_symbols': int(plan.requested_symbols[0]),
                'append_count': int(plan.append_count[0]),
                'replication_fractional_after': float(
                    plan.replication_fractional_after[0]
                ),
                'last_replication_symbols': int(
                    plan.last_replication_symbols[0]
                ),
                'last_effective_error_rate': float(
                    plan.last_effective_error_rate[0]
                ),
                'cumulative_proofreading_atp_after': float(
                    plan.cumulative_proofreading_atp_after[0]
                ),
            }
            return candidate

    for label, state, cell_id, dt, mutation_enabled, mode in (
            _a48c_oracle_cases()):
        devices = ['cpu']
        if (label == 'requested-zero' and torch is not None
                and torch.cuda.is_available()):
            devices.append('cuda')
        for device in devices:
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            integrated = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct_cell = next(
                cell for cell in direct.cells if int(cell.cell_id) == cell_id
            )
            cell = next(
                cell for cell in integrated.cells if int(cell.cell_id) == cell_id
            )
            direct_ids = _a48c_identity_snapshot(direct, direct_cell)
            integrated_ids = _a48c_identity_snapshot(integrated, cell)
            direct_rng_before = copy.deepcopy(
                direct.rng.bit_generator.state,
            )
            integrated_rng_before = copy.deepcopy(
                integrated.rng.bit_generator.state,
            )
            direct_events_before = int(
                direct_cell.mutation_events.get('substitution', 0)
            )
            direct_energy = float(direct.dissipated_energy)
            fractional_before = float(direct_cell.replication_fractional)
            last_before = int(direct_cell.last_replication_symbols)
            error_before = float(direct_cell.last_effective_error_rate)
            proof_before = float(direct_cell.cumulative_proofreading_atp)
            copy_before = list(direct_cell.replication_copy)
            pools_before = np.asarray(
                direct_cell.pools, dtype=np.float64,
            ).copy()
            direct_cell._replicate_genome(direct, dt, direct.config)
            appended = int(direct_cell.last_replication_symbols)
            substitution_delta = int(
                direct_cell.mutation_events.get('substitution', 0)
            ) - direct_events_before
            if mode == 'miss' and (
                    appended <= 0 or substitution_delta != 0):
                raise AssertionError(
                    label + ' did not consume miss thresholds'
                )
            if mode == 'hit' and (
                    appended <= 0 or substitution_delta != appended):
                raise AssertionError(
                    label + ' did not force every substitution'
                )
            if mode == 'zero' and appended != 0:
                raise AssertionError(label + ' unexpectedly copied a symbol')
            if mode == 'requested-zero':
                if (not float(dt) > 0.0
                        or appended != 0
                        or substitution_delta != 0
                        or direct_cell.replication_copy != copy_before
                        or not np.array_equal(
                            np.asarray(
                                direct_cell.pools, dtype=np.float64,
                            ).view(np.uint64),
                            pools_before.view(np.uint64),
                        )
                        or float(direct_cell.replication_fractional).hex()
                        == fractional_before.hex()
                        or last_before == 0
                        or error_before.hex()
                        == float(
                            direct_cell.last_effective_error_rate
                        ).hex()
                        or proof_before.hex()
                        != float(
                            direct_cell.cumulative_proofreading_atp
                        ).hex()):
                    raise AssertionError(
                        label + ' did not execute active-gate zero work'
                    )
            if float(direct.dissipated_energy) != direct_energy:
                raise AssertionError(
                    label + ' charged world energy for replication'
                )
            if _a48c_identity_snapshot(direct, direct_cell) != direct_ids:
                raise AssertionError(
                    label + ' frozen CPU object identity changed'
                )

            scheduler = ObserveReplicationPlan(capacity, device)
            _a48c_begin_replication_direct(
                scheduler, integrated, cell, dt,
            )
            try:
                scheduler.cpu_replication(
                    integrated, cell, dt, integrated.config,
                )
                _a48c_assert_oracle(
                    direct, direct_cell, integrated, cell,
                    'a48c1.oracle.%s.%s' % (device, label),
                )
                if (_a48c_identity_snapshot(integrated, cell)
                        != integrated_ids):
                    raise AssertionError(
                        label + ' integrated object identity changed'
                    )
                observation = scheduler.plan_observation
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                expected_branch = (
                    'active-noncompletion-substitution'
                    if mutation_enabled else
                    'active-noncompletion-deterministic'
                )
                expected_rng_calls = (
                    appended + substitution_delta
                    if mutation_enabled else 0
                )
                if (metadata.get('branch') != expected_branch
                        or bool(metadata.get('mutation_enabled'))
                        != mutation_enabled
                        or metadata.get('device') != device
                        or int(metadata.get('requested_symbols', -1))
                        != observation['requested_symbols']
                        or int(metadata.get('amount', -1)) != appended
                        or observation['append_count'] != appended
                        or bool(metadata.get('work_performed'))
                        != (appended > 0)
                        or int(metadata.get('substitution_events', -1))
                        != substitution_delta
                        or int(metadata.get('rng_call_count', -1))
                        != expected_rng_calls):
                    raise AssertionError(
                        '%s/%s receipt/plan metadata differs' % (
                            device, label,
                        )
                    )
                if mode == 'requested-zero':
                    if (observation['requested_symbols'] != 0
                            or observation['append_count'] != 0
                            or int(metadata['requested_symbols']) != 0
                            or int(metadata['amount']) != 0
                            or bool(metadata['work_performed'])):
                        raise AssertionError(
                            device + ' requested-zero plan/receipt differs'
                        )
                    v3.assert_recursive_close(
                        [
                            float(direct_cell.replication_fractional),
                            int(direct_cell.last_replication_symbols),
                            float(direct_cell.last_effective_error_rate),
                            float(direct_cell.cumulative_proofreading_atp),
                        ],
                        [
                            observation['replication_fractional_after'],
                            observation['last_replication_symbols'],
                            observation['last_effective_error_rate'],
                            observation[
                                'cumulative_proofreading_atp_after'
                            ],
                        ],
                        atol=a48c.REPLICATION_ORACLE_ATOL, rtol=0.0,
                        path='a48c1.%s.requested_zero_telemetry' % device,
                    )
                rng_changed = (
                    integrated.rng.bit_generator.state
                    != integrated_rng_before
                )
                expected_rng_change = mutation_enabled and appended > 0
                if rng_changed != expected_rng_change:
                    raise AssertionError(
                        label + ' live PCG64 branch differs'
                    )
                if ((not mutation_enabled)
                        and direct.rng.bit_generator.state
                        != direct_rng_before):
                    raise AssertionError(
                        label + ' mutation-off CPU changed RNG'
                    )
                details.append('%s@%s:%d/%d/%d' % (
                    label, device, observation['requested_symbols'],
                    appended, substitution_delta,
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 active/noncompletion resident commits preserve copy, '
            'paid ledger, object identity and full PCG64: ' +
            ', '.join(details))


def test_a48c1_active_noncompletion_candidate_cpu_cuda_purity_and_rng():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c1')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    selected = [
        case for case in _a48c_oracle_cases(seed=10121)
        if case[0] in ('deterministic-rich', 'substitution-hit')
    ]
    details = []
    for device in devices:
        for label, state, cell_id, dt, mutation_enabled, _ in selected:
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct_cell = next(
                cell for cell in direct.cells if int(cell.cell_id) == cell_id
            )
            cell = next(
                cell for cell in world.cells if int(cell.cell_id) == cell_id
            )
            direct_cell._replicate_genome(direct, dt, direct.config)
            scheduler = a48c.A4ReplicationEventScheduler(
                a4.GPU068A4Config(), device,
            )
            _a48c_begin_replication_direct(scheduler, world, cell, dt)
            before_cell = v3.pickle_clone(cell.state_dict())
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_energy = float(world.dissipated_energy)
            try:
                candidate = scheduler._prepare_replication_candidate(
                    world, cell, dt, world.config,
                )
                v3.assert_recursive_close(
                    before_cell, cell.state_dict(), 0.0, 0.0,
                    'a48c1.%s.%s.prepare_cell' % (device, label),
                )
                if (world.rng.bit_generator.state != before_rng
                        or float(world.dissipated_energy) != before_energy
                        or any(entry['event'] == 'replication_cpu'
                               for entry in scheduler._record(cell)[1]['events'])):
                    raise AssertionError(
                        '%s/%s prepare crossed claim' % (device, label)
                    )
                expected_device = torch.device(device)
                if (candidate.resident_plan.pools_after.device.type
                        != expected_device.type
                        or candidate.resident_binding.state.pools.device.type
                        != expected_device.type
                        or candidate.resident_binding.ragged.symbols.device.type
                        != expected_device.type):
                    raise AssertionError(
                        '%s/%s resident artifact moved device' % (
                            device, label,
                        )
                    )
                if (candidate.candidate_cell is cell
                        or candidate.candidate_cell.pools is cell.pools
                        or candidate.candidate_cell.replication_copy
                        is cell.replication_copy):
                    raise AssertionError(
                        '%s/%s candidate aliases live biology' % (
                            device, label,
                        )
                    )
                a48c._plan_semantic_match(
                    candidate.plan, candidate.host_replay,
                )
                if mutation_enabled:
                    if (candidate.tape is None
                            or candidate.resident_tape is None
                            or candidate.rng_before_state != before_rng
                            or candidate.rng_after_state
                            != candidate.tape.rng_after_state):
                        raise AssertionError(
                            '%s/%s substitution tape/RNG differs' % (
                                device, label,
                            )
                        )
                elif (candidate.tape is not None
                        or candidate.resident_tape is not None
                        or candidate.rng_after_state != before_rng):
                    raise AssertionError(
                        '%s/%s deterministic candidate owns RNG work' % (
                            device, label,
                        )
                    )
                scheduler._commit_replication_candidate(
                    world, cell, dt, world.config, candidate,
                )
                _a48c_assert_oracle(
                    direct, direct_cell, world, cell,
                    'a48c1.%s.%s.commit' % (device, label),
                )
                committed = v3.pickle_clone(cell.state_dict())
                committed_rng = copy.deepcopy(world.rng.bit_generator.state)
                _assert_raises(
                    a48c.A4ReplicationCommitError,
                    lambda: scheduler._commit_replication_candidate(
                        world, cell, dt, world.config, candidate,
                    ),
                )
                v3.assert_recursive_close(
                    committed, cell.state_dict(), 0.0, 0.0,
                    'a48c1.%s.%s.one_shot' % (device, label),
                )
                if world.rng.bit_generator.state != committed_rng:
                    raise AssertionError(
                        '%s/%s one-shot retry changed RNG' % (device, label)
                    )
                details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('/'.join(details) +
            ' prepare-pure resident candidate, explicit readback/NumPy '
            'association, fresh binding, full PCG64 and one-shot PASS')


def _a48c_capacity_for_cell(cell):
    sequences = list(cell.genomes) + [
        cell.replication_template, cell.replication_copy,
    ]
    q_value = len(sequences)
    s_value = sum(len(value) for value in sequences)
    w_value = max(len(value) for value in sequences)
    gene_keys = set(int(value) for value in cell.gene_specs)
    p_value = max(
        len(gene_keys.union(int(value) for value in cell.proteins)),
        len(gene_keys.union(
            int(value) for value in cell.damaged_proteins
        )),
    )
    return {
        'max_cells': 1,
        'max_sequences': q_value,
        'max_symbols': s_value,
        'max_sequence_symbols': w_value,
        'max_proteins_per_cell': p_value,
    }


def _a48c_assert_preclaim_failure(state, cell_id, dt, capacity,
                                  scheduler_type=None, label='scope'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(
        item for item in world.cells if int(item.cell_id) == int(cell_id)
    )
    scheduler_type = scheduler_type or a48c.A4ReplicationEventScheduler
    scheduler = scheduler_type(capacity, 'cpu')
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    try:
        _assert_raises(
            (a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, dt, world.config,
            ),
        )
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0,
            'a48c1.preclaim.' + label,
        )
        if (world.rng.bit_generator.state != before_rng
                or float(world.dissipated_energy) != before_energy
                or any(entry['event'] == 'replication_cpu'
                       for entry in scheduler._record(cell)[1]['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c1_active_noncompletion_capacity_trust_rollback_scope_and_exact_once():
    base_case = _a48c_oracle_cases(seed=10141)[0]
    _, state, cell_id, dt, _, _ = base_case
    direct = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    direct_cell = next(
        cell for cell in direct.cells if int(cell.cell_id) == cell_id
    )
    direct_cell._replicate_genome(direct, dt, direct.config)
    exact_values = _a48c_capacity_for_cell(direct_cell)
    exact = a4.GPU068A4Config(**exact_values)

    # The exact final arena is accepted. S-1 is specifically post-append
    # failure; Q/W/P one-short freeze every other fixed dimension.
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(item for item in world.cells if int(item.cell_id) == cell_id)
    scheduler = a48c.A4ReplicationEventScheduler(exact, 'cpu')
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    try:
        scheduler.cpu_replication(world, cell, dt, world.config)
        _a48c_assert_oracle(
            direct, direct_cell, world, cell, 'a48c1.capacity_exact',
        )
    finally:
        _a48_abort_direct(scheduler, cell)

    for label, key in (
            ('Q', 'max_sequences'), ('S', 'max_symbols'),
            ('W', 'max_sequence_symbols'),
            ('P', 'max_proteins_per_cell')):
        values = dict(exact_values)
        values[key] -= 1
        _a48c_assert_preclaim_failure(
            state, cell_id, dt, a4.GPU068A4Config(**values),
            label='capacity_' + label,
        )

    mutation_case = next(
        value for value in _a48c_oracle_cases(seed=10151)
        if value[0] == 'substitution-hit'
    )
    _, mutation_state, mutation_cell_id, mutation_dt, _, _ = mutation_case

    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c1 trust tests')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required for A4.8c1 trust tests')
    resident_devices = ['cpu']
    if torch.cuda.is_available():
        resident_devices.append('cuda')
    trust_details = []

    def assert_candidate_tamper_failure(
            case_state, case_cell_id, case_dt, scheduler_type, label,
            devices=('cpu',), require_isolation=False):
        """A4.8b-style full preclaim audit for a protected c1 seam."""
        for device in devices:
            tamper_world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(case_state),
            )
            tamper_cell = next(
                item for item in tamper_world.cells
                if int(item.cell_id) == int(case_cell_id)
            )
            tamper_scheduler = scheduler_type(
                a4.GPU068A4Config(), device,
            )
            _a48c_begin_replication_direct(
                tamper_scheduler, tamper_world, tamper_cell, case_dt,
            )
            before_world = v3.pickle_clone(tamper_world.state_dict())
            before_rng = copy.deepcopy(
                tamper_world.rng.bit_generator.state,
            )
            before_objects = _a48c_identity_snapshot(
                tamper_world, tamper_cell,
            )
            before_config_object = id(tamper_world.config)
            before_receipts = len(tamper_scheduler._receipts)
            try:
                _assert_raises(
                    (a48c.A4ReplicationCommitError,
                     a4.A4SchemaError, a3s.A3SchedulerError),
                    lambda: tamper_scheduler.cpu_replication(
                        tamper_world, tamper_cell, case_dt,
                        tamper_world.config,
                    ),
                )
                expected_world = getattr(
                    tamper_scheduler, 'expected_world_state',
                    before_world,
                )
                expected_rng = getattr(
                    tamper_scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, tamper_world.state_dict(), 0.0, 0.0,
                    'a48c1.%s.%s.preclaim_world' % (device, label),
                )
                _, record = tamper_scheduler._record(tamper_cell)
                if (tamper_world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(
                            tamper_world, tamper_cell,
                        ) != before_objects
                        or id(tamper_world.config) != before_config_object
                        or len(tamper_scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim boundary' % (
                            device, label,
                        )
                    )
                if (require_isolation
                        and not bool(getattr(
                            tamper_scheduler, 'isolated', False,
                        ))):
                    raise AssertionError(
                        '%s/%s retained artifact aliases live state' % (
                            device, label,
                        )
                    )
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(tamper_scheduler, tamper_cell)

    class TamperPackedHostSource(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.source_binding.state.pools
            self.isolated = (
                retained is not cell.pools
                and not np.shares_memory(retained, cell.pools)
            )
            retained[0, a4.a3.POOL_NUCLEOTIDE] += 1e-8
            return candidate

    class TamperHostOracle(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.last_replication_symbols[0] += 1
            return candidate

    class TamperResidentRaggedValue(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentStateValue(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_NUCLEOTIDE
            ].add_(1e-8)
            return candidate

    class TamperResidentCacheValue(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class TamperResidentPlanValue(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.append_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    class SwapResidentTensorPointer(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools = (
                candidate.resident_binding.state.pools.clone()
            )
            return candidate

    class SwapResidentWholeMember(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'ragged',
                candidate.resident_binding.ragged.clone(),
            )
            return candidate

    class TamperResidentPrivateSnapshot(
            a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            name = next(iter(
                candidate.resident_artifacts['data_ptrs']['plan'],
            ))
            candidate.resident_artifacts['data_ptrs']['plan'][name] += 1
            return candidate

    class TamperSchedulerCapacityDevice(
            a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = (
                'cuda' if self.a4_device == 'cpu' else 'cpu'
            )
            return candidate

    class MoveResidentTensorDevice(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            tensor = candidate.resident_binding.ragged.symbols
            target = 'cuda' if tensor.device.type == 'cpu' else 'cpu'
            candidate.resident_binding.ragged.symbols = tensor.to(target)
            return candidate

    class TamperCandidateCell(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_copy[-1] ^= 1
            return candidate

    class TamperFreshBinding(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class MutateUnpackedCandidate(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.candidate_cell.last_repair_flux
            self.isolated = (
                retained is not cell.last_repair_flux
                and not np.shares_memory(retained, cell.last_repair_flux)
            )
            retained[0] += 0.125
            return candidate

    class TamperLiveConfig(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                float(world.config.mutation_rate), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(
                world.state_dict(),
            )
            return candidate

    class TamperLiveSource(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.replication_fractional = float(np.nextafter(
                float(cell.replication_fractional), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(
                world.state_dict(),
            )
            return candidate

    class TamperLiveRng(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(
                world.state_dict(),
            )
            return candidate

    deterministic_cases = (
        (TamperPackedHostSource, 'host_source_packed_alias', True),
        (TamperHostOracle, 'host_oracle', False),
        (TamperResidentRaggedValue, 'resident_ragged_data_value', False),
        (TamperResidentStateValue, 'resident_state_data_value', False),
        (TamperResidentCacheValue, 'resident_cache_data_value', False),
        (TamperResidentPlanValue, 'resident_plan_data_value', False),
        (SwapResidentTensorPointer, 'resident_tensor_pointer', False),
        (SwapResidentWholeMember, 'resident_whole_member', False),
        (TamperResidentPrivateSnapshot, 'resident_private_snapshot', False),
        (MutateUnpackedCandidate, 'unpacked_candidate_alias', True),
    )
    for scheduler_type, label, require_isolation in deterministic_cases:
        assert_candidate_tamper_failure(
            state, cell_id, dt, scheduler_type, label,
            devices=resident_devices,
            require_isolation=require_isolation,
        )
    for scheduler_type, label in (
            (TamperSchedulerCapacityDevice, 'scheduler_capacity_device'),
            (TamperCandidateCell, 'candidate_cell'),
            (TamperFreshBinding, 'fresh_binding'),
            (TamperLiveConfig, 'live_config'),
            (TamperLiveSource, 'live_source')):
        assert_candidate_tamper_failure(
            state, cell_id, dt, scheduler_type, label,
        )
    if torch.cuda.is_available():
        assert_candidate_tamper_failure(
            state, cell_id, dt, MoveResidentTensorDevice,
            'resident_tensor_device', devices=resident_devices,
        )

    class TamperHostTape(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.tape.uniform_draws[0, 0] = np.nextafter(
                candidate.tape.uniform_draws[0, 0], np.inf,
            )
            return candidate

    class TamperCandidateRng(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_state,
            )
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    class TamperResidentTapeValue(a48c.A4ReplicationEventScheduler):
        def _replication_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_tape.uniform_draws.data[0, 0].add_(0.125)
            return candidate

    for scheduler_type, label, devices in (
            (TamperHostTape, 'host_tape', ('cpu',)),
            (TamperCandidateRng, 'candidate_rng', ('cpu',)),
            (TamperLiveRng, 'live_rng', ('cpu',)),
            (TamperResidentTapeValue, 'resident_tape_data_value',
             resident_devices)):
        assert_candidate_tamper_failure(
            mutation_state, mutation_cell_id, mutation_dt,
            scheduler_type, label, devices=devices,
        )

    # Wrong cell/dt/config arguments are rejected against one prepared
    # private candidate without consuming it or crossing the claim.
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(item for item in world.cells if int(item.cell_id) == cell_id)
    scheduler = a48c.A4ReplicationEventScheduler(
        a4.GPU068A4Config(), 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(cell.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = scheduler._prepare_replication_candidate(
            world, cell, dt, world.config,
        )
        wrong_inputs = (
            ('dt', cell, np.nextafter(dt, np.inf), world.config),
            ('cell', copy.deepcopy(cell), dt, world.config),
            ('config', cell, dt, copy.deepcopy(world.config)),
        )
        for wrong_label, wrong_cell, wrong_dt, wrong_config in wrong_inputs:
            _assert_raises(
                a48c.A4ReplicationCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler._commit_replication_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError(
                    'wrong %s consumed the private candidate' % wrong_label
                )
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0,
            'a48c1.wrong_cell_dt_config',
        )
        if (world.rng.bit_generator.state != before_rng
                or any(entry['event'] == 'replication_cpu'
                       for entry in scheduler._record(cell)[1]['events'])):
            raise AssertionError(
                'wrong cell/dt/config crossed the claim boundary'
            )
    finally:
        _a48_abort_direct(scheduler, cell)

    # Scope exclusions must never invoke the inherited CPU bridge.
    scope_cases = []
    start_world, start_cells, _, start_dt = _a45b_template_start_fixture(
        seed=10161,
    )
    _a48c_reduce_world(start_world, [start_cells[0]])
    scope_cases.append((
        'inactive_start', start_world.state_dict(),
        int(start_cells[0].cell_id), start_dt,
    ))
    completion_world, completion_cells, _, completion_dt = (
        _paid_completion_fixture(seed=10162)
    )
    _a48c_reduce_world(completion_world, [completion_cells[0]])
    scope_cases.append((
        'completion_off', completion_world.state_dict(),
        int(completion_cells[0].cell_id), completion_dt,
    ))
    mutation_world, mutation_cells, _, mutation_completion_dt = (
        _a46b1_completion_mutation_fixture(seed=10163)
    )
    _a48c_reduce_world(mutation_world, [mutation_cells[0]])
    scope_cases.append((
        'completion_on', mutation_world.state_dict(),
        int(mutation_cells[0].cell_id), mutation_completion_dt,
    ))
    disabled = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    disabled.config.genome_replication = False
    scope_cases.append(('disabled', disabled.state_dict(), cell_id, dt))
    no_genome = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    empty = no_genome.cells[0]
    empty.genomes = []
    empty.genome_lesions = []
    empty.replication_template = None
    empty.replication_copy = []
    empty.replication_template_lesion = 0.0
    empty.replication_fractional = 0.0
    empty._refresh_gene_cache()
    scope_cases.append(('no_genome', no_genome.state_dict(), cell_id, dt))
    gate = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    gate.config.external_replicase = False
    gate_cell = gate.cells[0]
    for fingerprint, spec in list(gate_cell.gene_specs.items()):
        if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
            gate_cell.proteins.pop(fingerprint, None)
    gate_cell._sync_protein_pool()
    scope_cases.append(('replicase_gate', gate.state_dict(), cell_id, dt))
    negative = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    negative.cells[0].pools[a4.a3.POOL_ATP] = -1e-12
    scope_cases.append(('negative_atp', negative.state_dict(), cell_id, dt))
    boundary = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    boundary_dt = float.fromhex('0x1.c46204851f076p-4')
    scope_cases.append(('fp64_code6', boundary.state_dict(), cell_id, boundary_dt))

    legacy_called = []
    original_cpu_replication = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_replication_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_replication_bomb
    try:
        for label, scope_state, scope_cell_id, scope_dt in scope_cases:
            _a48c_assert_preclaim_failure(
                scope_state, scope_cell_id, scope_dt,
                a4.GPU068A4Config(), label='scope_' + label,
            )
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = (
            original_cpu_replication
        )
    if legacy_called:
        raise AssertionError('an excluded scope used CPU fallback')

    # Duplicate and out-of-order calls remain atomic under the inherited
    # scheduler protocol.
    duplicate = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = a48c.A4ReplicationEventScheduler(
        a4.GPU068A4Config(), 'cpu',
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, dt,
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, dt, duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate_cell.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, dt, duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate_cell.state_dict(), 0.0, 0.0,
        'a48c1.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate replication changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    order_cell = order.cells[0]
    order_scheduler = a48c.A4ReplicationEventScheduler(
        a4.GPU068A4Config(), 'cpu',
    )
    _a48b_begin_translation_direct(order_scheduler, order, order_cell, dt)
    order_before = v3.pickle_clone(order_cell.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, dt, order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order_cell.state_dict(), 0.0, 0.0,
        'a48c1.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order replication changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(a48c.A4ReplicationEventScheduler):
        def _publish_replication_candidate(self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_candidate(
                world, cell, candidate,
            )
            cell.genomes[0][0] ^= np.uint8(1)
            cell.replication_template[0] ^= np.uint8(1)
            first = next(iter(cell.gene_specs))
            cell.gene_specs[first]['promoter'] += 0.25
            cell.proteins[next(iter(cell.proteins))] += 0.125
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c1 publish failure')

    rollback = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(mutation_state),
    )
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(
        a4.GPU068A4Config(), 'cpu',
    )
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, mutation_dt,
    )
    rollback_before = v3.pickle_clone(rollback_cell.state_dict())
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    rollback_energy = float(rollback.dissipated_energy)
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, mutation_dt, rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected replication publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback_cell.state_dict(), 0.0, 0.0,
        'a48c1.publish_rollback',
    )
    if (rollback.rng.bit_generator.state != rollback_rng
            or float(rollback.dissipated_energy) != rollback_energy
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids):
        raise AssertionError('postclaim rollback lost identity/state')
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if (entry['event'] != 'replication_cpu'
            or receipt['status'] != 'aborted'):
        raise AssertionError('postclaim failure did not leave aborted receipt')
    return ('final Q/S/W/P exact + each one-short; %d explicit %s '
            'source/config/RNG/oracle/resident/pointer/device/alias trust '
            'checks; wrong cell/dt/config; excluded start/completion/no-op/'
            'code6 without fallback; duplicate/order; postclaim nested '
            'identity/RNG rollback' % (
                len(trust_details), '/'.join(resident_devices),
            ))


def test_a48c1_world_lockstep_interleave_save_clone_successor_and_authority():
    capacity = a4.GPU068A4Config()
    dt = None
    for steps, seed in ((1, 10181), (10, 10182)):
        source, cells, _, dt = _paid_replication_b_fixture(seed=seed)
        _a48c_reduce_world(source, [cells[0]])
        state = v3.pickle_clone(source.state_dict())
        cpu = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        hybrid = _a48c_hybrid_from_state(state, capacity, 'cpu')
        for index in range(steps):
            for label, cell in (
                    ('cpu', cpu.cells[0]),
                    ('hybrid', hybrid.world.cells[0])):
                if (cell.replication_template is None
                        or len(cell.replication_copy)
                        >= len(cell.replication_template)):
                    raise AssertionError(
                        '%s left c1 scope before step %d' % (label, index)
                    )
            cpu.step(dt)
            hybrid.step(dt)
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c1.lockstep_%d' % steps,
        )

    interleave_source, interleave_cells, _, interleave_dt = (
        _a45_substitution_fixture(seed=10183)
    )
    interleave_source.config.mutation_rate = 1.0
    interleave_source.config.endogenous_damage = False
    for cell in interleave_cells[:2]:
        cell.genome_lesions = [
            0.5 / 0.00065 for _ in cell.genomes
        ]
        cell._refresh_gene_cache()
    _a48c_reduce_world(interleave_source, interleave_cells[:2])
    interleave_state = v3.pickle_clone(interleave_source.state_dict())
    devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required for A4.8c1 world interleave')
    interleave_details = []
    original_cpu_replication = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_replication_bomb(*args, **kwargs):
        raise AssertionError('legacy CPU replication bridge was called')

    for device in devices:
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(interleave_state),
        )
        hybrid = _a48c_hybrid_from_state(
            interleave_state, capacity, device,
        )
        cpu.step(interleave_dt)
        a48b.A4TranslationEventScheduler.cpu_replication = (
            legacy_replication_bomb
        )
        try:
            receipt = hybrid.step(interleave_dt)
        finally:
            a48b.A4TranslationEventScheduler.cpu_replication = (
                original_cpu_replication
            )
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c1.interleave.' + device,
        )
        previous_motion = None
        for record in receipt['cells']:
            by_name = {entry['event']: entry for entry in record['events']}
            names = [entry['event'] for entry in record['events']]
            if (names.count('translation_cpu') != 1
                    or names.count('replication_cpu') != 1
                    or names.count('genome_hydrolysis_cpu_rng') != 1):
                raise AssertionError(
                    device + ' integrated event count differs'
                )
            translation = by_name['translation_cpu']['ordinal']
            replication = by_name['replication_cpu']['ordinal']
            surface = by_name['surface_assembly']['ordinal']
            hydrolysis = by_name['genome_hydrolysis_cpu_rng']['ordinal']
            motion = by_name['motion']['ordinal']
            if not translation < replication < surface < hydrolysis < motion:
                raise AssertionError(device + ' per-cell event order differs')
            if previous_motion is not None and previous_motion >= translation:
                raise AssertionError(device + ' cells were batched/reordered')
            previous_motion = motion
            metadata = by_name['replication_cpu']['metadata']
            if (metadata.get('authority')
                    != 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit'
                    or metadata.get('branch')
                    != 'active-noncompletion-substitution'
                    or int(metadata.get('amount', 0)) <= 0
                    or int(metadata.get('rng_call_count', 0)) <= 0
                    or by_name['genome_hydrolysis_cpu_rng']['metadata'].get(
                        'eligible_genome_count', 0
                    ) < 1):
                raise AssertionError(device + ' interleave work differs')
        interleave_details.append(device)

    persisted_source, persisted_cells, _, persisted_dt = (
        _paid_replication_b_fixture(seed=10184)
    )
    _a48c_reduce_world(persisted_source, [persisted_cells[0]])
    persisted = _a48c_hybrid_from_state(
        persisted_source.state_dict(), capacity, 'cpu',
    )
    persisted.step(persisted_dt)
    if (persisted.world.cells[0].replication_template is None
            or len(persisted.world.cells[0].replication_copy)
            >= len(persisted.world.cells[0].replication_template)):
        raise AssertionError('persisted c1 fixture completed unexpectedly')
    twin = persisted.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c1.pkl')
        persisted.save(path)
        restored = a48c.Hybrid066WorldA4Replication.load(path)
        for value in (twin, restored):
            if (type(value) is not a48c.Hybrid066WorldA4Replication
                    or type(value.scheduler)
                    is not a48c.A4ReplicationEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != capacity):
                raise AssertionError('A4.8c1 save/clone authority was lost')
        persisted.step(persisted_dt)
        twin.step(persisted_dt)
        restored.step(persisted_dt)
        v3.assert_recursive_close(
            persisted.world.state_dict(), twin.world.state_dict(),
            v3.WORLD_FP64_ATOL, 0.0, 'a48c1.clone_continuation',
        )
        v3.assert_recursive_close(
            persisted.world.state_dict(), restored.world.state_dict(),
            v3.WORLD_FP64_ATOL, 0.0, 'a48c1.save_continuation',
        )

    active_source, active_cells, _, _ = _paid_replication_b_fixture(
        seed=10185,
    )
    _a48c_reduce_world(active_source, [active_cells[0]])
    active = _a48c_hybrid_from_state(
        active_source.state_dict(), capacity, 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected active-save rejection'))
    active.scheduler._a4_replication_commit_active = True
    try:
        _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
    finally:
        active.scheduler._a4_replication_commit_active = False

    # Freeze the immediate frozen successor gate. One paid symbol leaves ATP
    # at exact 0.045, its immediate neighbours, then surface assembly must take
    # the same zero/positive branch after CPU or CUDA resident replication.
    surface_details = []
    surface_targets = (
        ('nextbelow', np.nextafter(np.float64(0.045), 0.0), False),
        ('exact', np.float64(0.045), False),
        ('nextabove', np.nextafter(np.float64(0.045), np.inf), True),
    )
    atp_cost = float(a4.g2.REPLICATION_ATP_PER_SYMBOL)
    for device in devices:
        for label, target, expected_work in surface_targets:
            source, cells, _, surface_dt = _paid_replication_fixture(
                seed=10190,
            )
            cell = cells[0]
            cell.pools[a4.a3.POOL_ATP] = float(target + atp_cost)
            cell.pools[a4.a3.POOL_MEM_PRECURSOR] = 0.1
            cell.pools[a4.a3.POOL_TRANSPORTER_PRECURSOR] = 0.0
            _a48c_reduce_world(source, [cell])
            state = v3.pickle_clone(source.state_dict())
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            integrated = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct_cell = direct.cells[0]
            integrated_cell = integrated.cells[0]
            direct_cell._replicate_genome(
                direct, surface_dt, direct.config,
            )
            if (int(direct_cell.last_replication_symbols) != 1
                    or np.float64(
                        direct_cell.pools[a4.a3.POOL_ATP]
                    ).view(np.uint64) != target.view(np.uint64)):
                raise AssertionError(label + ' frozen ATP seam drifted')
            scheduler = a48c.A4ReplicationEventScheduler(capacity, device)
            _a48c_begin_replication_direct(
                scheduler, integrated, integrated_cell, surface_dt,
            )
            try:
                scheduler.cpu_replication(
                    integrated, integrated_cell, surface_dt,
                    integrated.config,
                )
                _a48c_assert_oracle(
                    direct, direct_cell, integrated, integrated_cell,
                    'a48c1.surface_pre.%s.%s' % (device, label),
                )
                scheduler.claim(
                    integrated_cell, 'surface_assembly',
                    metadata={'validation': 'A4.8c1-successor'},
                )
                direct_cell._assemble_surface(surface_dt, direct.config)
                integrated_cell._assemble_surface(
                    surface_dt, integrated.config,
                )
                _a48b_assert_cell_matches(
                    direct_cell, integrated_cell,
                    'a48c1.surface_post.%s.%s' % (device, label),
                    atol=a48c.REPLICATION_ORACLE_ATOL,
                )
                actual_work = float(integrated_cell.last_assembly) > 0.0
                if actual_work != expected_work:
                    raise AssertionError(
                        '%s/%s successor ATP branch differs' % (
                            device, label,
                        )
                    )
                surface_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, integrated_cell)

    signature = inspect.signature(
        a48c.A4ReplicationEventScheduler.cpu_replication,
    )
    expected_status = (
        'a4.8c1-pre-existing-active-noncompletion-mutation-off-on-'
        'resident-plan-cpu-cell-pcg64-atomic-commit-'
        'inactive-completion-early-noop-fail-closed'
    )
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationCommitCandidate' in set(a48c.__all__)
            or not issubclass(
                a48c.A4ReplicationEventScheduler,
                a48b.A4TranslationEventScheduler,
            )
            or not issubclass(
                a48c.Hybrid066WorldA4Replication,
                a48b.Hybrid066WorldA4Translation,
            )
            or a48c.PORT_STATUS.get('genome_replication') != expected_status
            or a48c.FULL_GPU_WORLD_STEP is not False
            or a48c.PORT_STATUS.get('full_gpu_world_step') is not False):
        raise AssertionError('A4.8c1 API/scope/authority differs')
    for relative, expected in PROMOTED_A3_SHA256.items():
        path = os.path.join(ROOT, *relative.split('/'))
        if _sha256(path) != expected:
            raise AssertionError(relative + ' changed from promoted A3 bytes')
    return ('1/10-step active lockstep; %s two-cell nonbatched '
            'translation/replication/surface/hydrolysis/motion PCG64 order; '
            'legacy bridge bomb; save/load/clone + active reject; successor '
            'ATP gates %s; promoted A3 and full_gpu=false' % (
                '/'.join(interleave_details), '/'.join(surface_details),
            ))


def _a48c2_completion_case(seed=11101, row=0):
    world, cells, _, dt = _paid_completion_fixture(seed=seed)
    cell = cells[int(row)]
    world.config.mutation = False
    _a48c_reduce_world(world, [cell])
    return (
        v3.pickle_clone(world.state_dict()), int(cell.cell_id), float(dt),
    )


def _a48c2_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c2.Hybrid066WorldA4ReplicationCompletion(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48c2_assert_completion_oracle(expected_world, expected_cell,
                                     actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c2.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or actual_cell.replication_template is not None
            or actual_cell.replication_copy
            or float(actual_cell.replication_template_lesion) != 0.0
            or float(actual_cell.replication_fractional) != 0.0):
        raise AssertionError(label + ' completion/RNG state differs')
    v3.assert_recursive_close(
        float(expected_world.dissipated_energy),
        float(actual_world.dissipated_energy),
        atol=0.0, rtol=0.0, path=label + '.dissipated_energy',
    )


def _a48c2_capacity_for_transition(before, after):
    def dimensions(cell):
        sequences = list(cell.genomes)
        if cell.replication_template is not None:
            sequences.extend((
                cell.replication_template, cell.replication_copy,
            ))
        q_value = len(sequences)
        s_value = sum(len(value) for value in sequences)
        w_value = max([len(value) for value in sequences] or [1])
        gene_keys = set(int(value) for value in cell.gene_specs)
        p_value = max(
            len(gene_keys.union(int(value) for value in cell.proteins)),
            len(gene_keys.union(
                int(value) for value in cell.damaged_proteins
            )),
            1,
        )
        return q_value, s_value, w_value, p_value

    left = dimensions(before)
    right = dimensions(after)
    return {
        'max_cells': 1,
        'max_sequences': max(left[0], right[0]),
        'max_symbols': max(left[1], right[1]),
        'max_sequence_symbols': max(left[2], right[2]),
        'max_proteins_per_cell': max(left[3], right[3]),
    }


def test_a48c2_mutation_free_completion_atomic_commit_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c2')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveCompletion(a48c2.A4ReplicationCompletionEventScheduler):
        def _replication_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_plan = candidate.plan.clone()
            return candidate

    details = []
    for device in devices:
        for row in (0, 1):
            state, cell_id, dt = _a48c2_completion_case(
                seed=11101 + row, row=row,
            )
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct_cell = direct.cells[0]
            cell = world.cells[0]
            partial = np.asarray(cell.replication_copy, dtype=np.uint8).copy()
            template = np.asarray(
                cell.replication_template, dtype=np.uint8,
            ).copy()
            cycles_before = int(cell.replication_cycles)
            rng_before = copy.deepcopy(world.rng.bit_generator.state)
            identities = _a48c_identity_snapshot(world, cell)

            direct_cell._replicate_genome(direct, dt, direct.config)
            if (direct_cell.replication_template is not None
                    or len(direct_cell.genomes) != 2):
                raise AssertionError('Formal066 completion fixture drifted')

            scheduler = ObserveCompletion(a4.GPU068A4Config(), device)
            _a48c_begin_replication_direct(scheduler, world, cell, dt)
            try:
                scheduler.cpu_replication(world, cell, dt, world.config)
                _a48c2_assert_completion_oracle(
                    direct, direct_cell, world, cell,
                    'a48c2.oracle.%s.%d' % (device, row),
                )
                plan = scheduler.observed_plan
                length = int(plan.completed_lengths[0])
                appended = int(plan.append_count[0])
                completed = np.asarray(
                    plan.completed_symbols[0, :length], dtype=np.uint8,
                )
                suffix = np.asarray(
                    plan.append_symbols[0, :appended], dtype=np.uint8,
                )
                if (appended <= 0
                        or not np.array_equal(completed[:len(partial)], partial)
                        or not np.array_equal(completed[len(partial):], suffix)
                        or not np.array_equal(suffix, template[len(partial):])
                        or int(cell.replication_cycles) != cycles_before + 1
                        or world.rng.bit_generator.state != rng_before):
                    raise AssertionError(
                        '%s row %d completion payload/ledger differs' % (
                            device, row,
                        )
                    )
                current_ids = _a48c_identity_snapshot(world, cell)
                for key in ('rng', 'pools', 'genomes', 'lesions', 'copy',
                            'events', 'proteins', 'damaged', 'gene_specs'):
                    if current_ids[key] != identities[key]:
                        raise AssertionError(
                            '%s row %d changed %s object identity' % (
                                device, row, key,
                            )
                        )
                if current_ids['genome_items'][:len(
                        identities['genome_items'])] != identities['genome_items']:
                    raise AssertionError('existing genome identity changed')
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                if (metadata.get('authority') != (
                        'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit')
                        or metadata.get('branch')
                        != 'active-completion-deterministic'
                        or int(metadata.get('completion_events', 0)) != 1
                        or int(metadata.get('rng_call_count', -1)) != 0
                        or int(metadata.get('amount', 0)) != appended):
                    raise AssertionError('A4.8c2 completion receipt differs')
                details.append('%s:%d:%d' % (device, row, appended))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 mutation-free completion payload/lesion/cycle/reset/'
            'cache/novel-path, object identity and PCG64 no-advance exact: ' +
            ', '.join(details))


def test_a48c2_completion_candidate_cpu_cuda_purity_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c2')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    details = []
    for device in devices:
        state, cell_id, dt = _a48c2_completion_case(seed=11121)
        world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        cell = world.cells[0]
        scheduler = a48c2.A4ReplicationCompletionEventScheduler(
            a4.GPU068A4Config(), device,
        )
        _a48c_begin_replication_direct(scheduler, world, cell, dt)
        before = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = scheduler._prepare_completion_candidate(
                world, cell, dt, world.config,
            )
            v3.assert_recursive_close(
                before, cell.state_dict(), 0.0, 0.0,
                'a48c2.prepare_purity.' + device,
            )
            if (world.rng.bit_generator.state != before_rng
                    or float(world.dissipated_energy) != before_energy
                    or candidate.candidate_cell is cell
                    or candidate.plan is candidate.host_replay
                    or candidate.resident_plan.pools_after.device.type != device
                    or candidate.resident_binding.ragged.symbols.device.type
                    != device):
                raise AssertionError(device + ' candidate authority/purity differs')
            a4._require_translation_binding(candidate.fresh_binding)
            a48c._plan_semantic_match(
                candidate.plan, candidate.host_replay,
            )
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct.cells[0]._replicate_genome(direct, dt, direct.config)
            _a48b_assert_cell_matches(
                direct.cells[0], candidate.candidate_cell,
                'a48c2.candidate.' + device,
                atol=a48c2.REPLICATION_ORACLE_ATOL,
            )
            scheduler._commit_completion_candidate(
                world, cell, dt, world.config, candidate,
            )
            _a48c2_assert_completion_oracle(
                direct, direct.cells[0], world, cell,
                'a48c2.commit.' + device,
            )
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                (a4.A4Error, a3s.A3SchedulerError),
                lambda: scheduler._commit_completion_candidate(
                    world, cell, dt, world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48c2.one_shot.' + device,
            )
            if world.rng.bit_generator.state != committed_rng:
                raise AssertionError(device + ' reused candidate changed RNG')
            details.append(device)
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('CPU/CUDA resident authority, independent NumPy oracle, prepare '
            'purity, fresh binding and one-shot candidate PASS: ' +
            '/'.join(details))


def test_a48c2_completion_capacity_trust_rollback_scope_and_exact_once():
    state, cell_id, dt = _a48c2_completion_case(seed=11141)
    source = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    direct = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    direct.cells[0]._replicate_genome(direct, dt, direct.config)
    exact_values = _a48c2_capacity_for_transition(
        source.cells[0], direct.cells[0],
    )
    exact = a4.GPU068A4Config(**exact_values)

    for device in (['cpu', 'cuda'] if torch is not None
                   and torch.cuda.is_available() else ['cpu']):
        world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        cell = world.cells[0]
        scheduler = a48c2.A4ReplicationCompletionEventScheduler(exact, device)
        _a48c_begin_replication_direct(scheduler, world, cell, dt)
        try:
            scheduler.cpu_replication(world, cell, dt, world.config)
            _a48c2_assert_completion_oracle(
                direct, direct.cells[0], world, cell,
                'a48c2.capacity_exact.' + device,
            )
        finally:
            _a48_abort_direct(scheduler, cell)

    for label, key in (
            ('Q', 'max_sequences'), ('S', 'max_symbols'),
            ('W', 'max_sequence_symbols'), ('P', 'max_proteins_per_cell')):
        values = dict(exact_values)
        values[key] -= 1
        _a48c_assert_preclaim_failure(
            state, cell_id, dt, a4.GPU068A4Config(**values),
            scheduler_type=a48c2.A4ReplicationCompletionEventScheduler,
            label='a48c2.capacity_' + label,
        )

    class TamperResidentPlan(a48c2.A4ReplicationCompletionEventScheduler):
        def _replication_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.completed_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    class TamperHostReplay(a48c2.A4ReplicationCompletionEventScheduler):
        def _replication_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.completed_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperCandidate(a48c2.A4ReplicationCompletionEventScheduler):
        def _replication_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.membrane[0] += 0.125
            return candidate

    class SwapResidentCache(a48c2.A4ReplicationCompletionEventScheduler):
        def _replication_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'cache',
                candidate.resident_binding.cache.clone(),
            )
            return candidate

    trust_types = (
        TamperResidentPlan, TamperHostReplay, TamperCandidate,
        SwapResidentCache,
    )
    for scheduler_type in trust_types:
        _a48c_assert_preclaim_failure(
            state, cell_id, dt, exact,
            scheduler_type=scheduler_type,
            label='a48c2.trust.' + scheduler_type.__name__,
        )

    # Wrong dt is checked against the prepared one-shot candidate itself.
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = world.cells[0]
    scheduler = a48c2.A4ReplicationCompletionEventScheduler(exact, 'cpu')
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    try:
        candidate = scheduler._prepare_completion_candidate(
            world, cell, dt, world.config,
        )
        before = v3.pickle_clone(cell.state_dict())
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        _assert_raises(
            (a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler._commit_completion_candidate(
                world, cell, np.nextafter(dt, np.inf),
                world.config, candidate,
            ),
        )
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0, 'a48c2.wrong_dt',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong-dt rejection changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)

    class FailingPublish(a48c2.A4ReplicationCompletionEventScheduler):
        def _publish_replication_completion_candidate(
                self, world, cell, candidate):
            super(FailingPublish, self)._publish_replication_completion_candidate(
                world, cell, candidate,
            )
            cell.age += 9.0
            world.rng.random()
            raise RuntimeError('injected A4.8c2 publish failure')

    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = world.cells[0]
    scheduler = FailingPublish(exact, 'cpu')
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(cell.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        _assert_raises(
            RuntimeError,
            lambda: scheduler.cpu_replication(world, cell, dt, world.config),
        )
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0, 'a48c2.rollback',
        )
        after_ids = _a48c_identity_snapshot(world, cell)
        if after_ids != before_ids or world.rng.bit_generator.state != before_rng:
            raise AssertionError('A4.8c2 publish rollback identity/RNG differs')
    finally:
        _a48_abort_direct(scheduler, cell)

    for label, mutate in (
            ('mutation_on', lambda world, cell: setattr(
                world.config, 'mutation', True)),
            ('inactive', lambda world, cell: (
                setattr(cell, 'replication_template', None),
                setattr(cell, 'replication_copy', []))),
            ('disabled', lambda world, cell: setattr(
                world.config, 'genome_replication', False))):
        scoped = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scoped_state = v3.pickle_clone(scoped.state_dict())
        _a48c_assert_preclaim_failure(
            scoped_state, int(scoped_cell.cell_id), dt, exact,
            scheduler_type=a48c2.A4ReplicationCompletionEventScheduler,
            label='a48c2.scope.' + label,
        )
    return ('Q/S/W/P exact and one-short; resident/host/candidate trust; '
            'wrong-dt; declared identity/RNG rollback; mutation/inactive/disabled '
            'scope fail-closed PASS')


def test_a48c2_inherited_c1_save_clone_successor_and_authority():
    devices = ['cpu']
    if torch is not None and torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    # Noncompletion remains explicitly delegated to the inherited c1 bridge.
    source, cells, _, dt = _paid_replication_b_fixture(seed=11161)
    _a48c_reduce_world(source, [cells[0]])
    noncompletion_state = v3.pickle_clone(source.state_dict())
    delegated = []
    for device in devices:
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(noncompletion_state),
        )
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(noncompletion_state),
        )
        direct.cells[0]._replicate_genome(direct, dt, direct.config)
        scheduler = a48c2.A4ReplicationCompletionEventScheduler(
            a4.GPU068A4Config(), device,
        )
        _a48c_begin_replication_direct(scheduler, world, world.cells[0], dt)
        try:
            scheduler.cpu_replication(
                world, world.cells[0], dt, world.config,
            )
            _a48c_assert_oracle(
                direct, direct.cells[0], world, world.cells[0],
                'a48c2.delegate.' + device,
            )
            entry = _a48c_replication_entry(scheduler, world.cells[0])
            if entry['metadata'].get('authority') != (
                    'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit'):
                raise AssertionError(device + ' did not delegate to A4.8c1')
            delegated.append(device)
        finally:
            _a48_abort_direct(scheduler, world.cells[0])

    # Freeze wrapper/scheduler persistence before the one supported completion.
    completion_state, _, completion_dt = _a48c2_completion_case(seed=11162)
    capacity = a4.GPU068A4Config()
    hybrid = _a48c2_hybrid_from_state(completion_state, capacity, 'cpu')
    twin = hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c2.pkl')
        hybrid.save(path)
        restored = a48c2.Hybrid066WorldA4ReplicationCompletion.load(path)
        for value in (hybrid, twin, restored):
            if (type(value) is not a48c2.Hybrid066WorldA4ReplicationCompletion
                    or type(value.scheduler)
                    is not a48c2.A4ReplicationCompletionEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != capacity):
                raise AssertionError('A4.8c2 save/clone authority was lost')
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(completion_state),
        )
        cpu.step(completion_dt)
        receipts = []
        for value in (hybrid, twin, restored):
            receipts.append(value.step(completion_dt))
            v3._assert_world_pair(
                cpu, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c2.persisted_completion',
            )
        for receipt in receipts:
            record = receipt['cells'][0]
            names = [entry['event'] for entry in record['events']]
            by_name = {entry['event']: entry for entry in record['events']}
            if (names.count('replication_cpu') != 1
                    or not names.index('translation_cpu')
                    < names.index('replication_cpu')
                    < names.index('surface_assembly')
                    or by_name['replication_cpu']['metadata'].get('authority')
                    != ('A4.8c2-resident-completion-plan-atomic-'
                        'cpu-cell-commit')):
                raise AssertionError('A4.8c2 successor order/authority differs')

    active = _a48c2_hybrid_from_state(completion_state, capacity, 'cpu')
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected active-save rejection'))
    active.scheduler._a4_replication_completion_commit_active = True
    try:
        _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
    finally:
        active.scheduler._a4_replication_completion_commit_active = False

    expected_status = (
        'a4.8c2-mutation-free-pre-existing-active-completion-'
        'resident-descriptor-cpu-cell-atomic-commit-'
        'a4.8c1-active-noncompletion-inherited-'
        'mutation-completion-inactive-early-noop-fail-closed'
    )
    if (a48c2.PORT_STATUS.get('genome_replication') != expected_status
            or a48c2.FULL_GPU_WORLD_STEP is not False
            or not issubclass(
                a48c2.A4ReplicationCompletionEventScheduler,
                a48c.A4ReplicationEventScheduler,
            )
            or not issubclass(
                a48c2.Hybrid066WorldA4ReplicationCompletion,
                a48c.Hybrid066WorldA4Replication,
            )):
        raise AssertionError('A4.8c2 scope/API/full-GPU authority differs')
    for relative, expected in PROMOTED_A3_SHA256.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected:
            raise AssertionError(relative + ' changed from promoted A3 bytes')
    return ('inherited c1 delegation %s; completion save/load/clone type; '
            'translation<replication<surface successor order; active save '
            'reject; promoted A3 byte authority/full_gpu=false PASS' %
            '/'.join(delegated))


_A48C3_EXPECTED_ROWS = (
    {
        'row': 0,
        'rng_before_cache': (1, 3034332626),
        'rng_after_cache': (1, 1123080652),
        'final_length': 577,
        'structural_event_counts': (4, 2, 16, 1, 1),
        'substitution_events': 0,
        'material_delta_symbols': 1,
        'novel_path_first_age': 81.5,
        'capacity': (1, 3, 1727, 577, 36),
        'rng_call_count': 18,
    },
    {
        'row': 1,
        'rng_before_cache': (1, 1123080652),
        'rng_after_cache': (0, 2037901165),
        'final_length': 638,
        'structural_event_counts': (0, 2, 0, 1, 1),
        'substitution_events': 0,
        'material_delta_symbols': -2,
        'novel_path_first_age': 81.5,
        'capacity': (1, 3, 1919, 640, 37),
        'rng_call_count': 13,
    },
    {
        'row': 2,
        'rng_before_cache': (0, 2037901165),
        'rng_after_cache': (1, 2698505109),
        'final_length': 32,
        'structural_event_counts': (3, 0, 0, 1, 1),
        'substitution_events': 1,
        'material_delta_symbols': 24,
        'novel_path_first_age': None,
        'capacity': (1, 3, 40, 32, 31),
        'rng_call_count': 16,
    },
)

_A48C3_MUTATION_LEDGER_SENTINELS = (
    ('substitution', 11),
    ('insertion', 23),
    ('deletion', 37),
    ('duplication', 41),
    ('inversion', 53),
    ('transposition', 67),
    ('a48c3_extra_sentinel', 79),
)

_A48C3_PRIOR_CORE_SHA256 = {
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_integration.py': (
        'fa9787a6dfc59a7767fcc00095ad052f7afe77877eef78b6e02e992015b2bbef'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_integration.py': (
        '6bf4c979b524c176c2e72b1630b14f630638cdd7eb883bdeeb48208e9ba5b416'
    ),
}

_A48C2_TEST_NAMES_SHA256 = (
    'c1f8d35cf72bad0f89977e968e47ec62719d47b2e5ff3429e90c922fe28827fa'
)


def _a48c3_completion_mutation_rng_call_count(tape):
    """Count frozen high-level NumPy Generator method invocations."""
    used = slice(0, int(tape.cell_count))
    return int(
        np.sum(tape.append_draw_mask[used], dtype=np.int64)
        + np.sum(tape.replacement_mask[used], dtype=np.int64)
        + np.sum(tape.threshold_draw_mask[used], dtype=np.int64)
        + 3 * np.count_nonzero(tape.insertion_count[used] > 0)
        + 2 * np.count_nonzero(tape.deletion_count[used] > 0)
        + 2 * np.count_nonzero(
            tape.duplication_gene_ordinal[used] >= 0
        )
        + 2 * np.count_nonzero(tape.inversion_left[used] >= 0)
        + 3 * np.count_nonzero(tape.transposition_count[used] > 0)
        + np.count_nonzero(tape.padding_count[used] > 0)
    )


def _a48c3_completion_mutation_cases(seed=9911):
    """Return three frozen one-cell states in Formal066 cell-event order."""
    if int(seed) != 9911:
        raise AssertionError('A4.8c3 fixed fixture seed must remain 9911')
    world, source_cells, roomy, dt = (
        _a46b1_completion_mutation_fixture(seed=seed)
    )
    cases = []
    structural_names = tuple(a44.STRUCTURAL_EVENT_NAMES)
    for expected, source_cell in zip(_A48C3_EXPECTED_ROWS, source_cells):
        cell = copy.deepcopy(source_cell)
        for name, value in _A48C3_MUTATION_LEDGER_SENTINELS:
            cell.mutation_events[name] = int(value + expected['row'])
        world.cells = [cell]
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
        state = v3.pickle_clone(world.state_dict())

        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(state),
        )
        direct_cell = direct.cells[0]
        mutation_before = copy.deepcopy(direct_cell.mutation_events)
        mutation_object = direct_cell.mutation_events
        mutation_order = tuple(direct_cell.mutation_events)
        pools_before = np.asarray(
            direct_cell.pools, dtype=np.float64,
        ).copy()
        genomes_object = direct_cell.genomes
        genome_objects = tuple(direct_cell.genomes)
        rng_before = copy.deepcopy(direct.rng.bit_generator.state)

        _, _, binding = _paid_replication_binding(
            [cell], world.config, roomy,
        )
        tape = a44.prepare_completion_mutation_rng_tape(
            binding, dt, world.config, rng_before,
        )
        plan = a44.paid_replication_completion_mutation_numpy(
            binding, dt, world.config, tape,
        )
        direct_cell._replicate_genome(direct, dt, direct.config)

        structural = tuple(
            int(direct_cell.mutation_events[name])
            - int(mutation_before[name])
            for name in structural_names
        )
        substitution = (
            int(direct_cell.mutation_events['substitution'])
            - int(mutation_before['substitution'])
        )
        final_length = len(direct_cell.genomes[-1])
        material_delta = (
            final_length - len(source_cell.replication_template)
        )
        capacity = _a48c2_capacity_for_transition(
            source_cell, direct_cell,
        )
        capacity_tuple = (
            int(capacity['max_cells']),
            int(capacity['max_sequences']),
            int(capacity['max_symbols']),
            int(capacity['max_sequence_symbols']),
            int(capacity['max_proteins_per_cell']),
        )
        before_cache = (
            int(rng_before['has_uint32']), int(rng_before['uinteger']),
        )
        rng_after = direct.rng.bit_generator.state
        after_cache = (
            int(rng_after['has_uint32']), int(rng_after['uinteger']),
        )
        plan_length = int(plan.final_lengths[0])
        plan_polymer = np.asarray(
            plan.final_symbols[0, :plan_length], dtype=np.uint8,
        )
        expected_mutation = copy.deepcopy(mutation_before)
        expected_mutation['substitution'] += substitution
        for name, delta in zip(structural_names, structural):
            expected_mutation[name] += int(delta)
        paid = {
            int(a4.a3.POOL_NUCLEOTIDE), int(a4.a3.POOL_ATP),
        }
        unpaid = [
            index for index in range(int(a4.a3.POOL_COUNT))
            if index not in paid
        ]
        rng_call_count = _a48c3_completion_mutation_rng_call_count(tape)
        if (int(expected['row']) != len(cases)
                or before_cache != expected['rng_before_cache']
                or after_cache != expected['rng_after_cache']
                or tape.rng_before_state != rng_before
                or tape.rng_after_state != rng_after
                or final_length != int(expected['final_length'])
                or plan_length != final_length
                or not np.array_equal(
                    plan_polymer, direct_cell.genomes[-1],
                )
                or structural != expected['structural_event_counts']
                or tuple(int(value) for value in
                         plan.structural_event_counts[0]) != structural
                or substitution != int(expected['substitution_events'])
                or int(plan.substitution_events[0]) != substitution
                or material_delta != int(expected['material_delta_symbols'])
                or int(plan.material_delta_symbols[0]) != material_delta
                or direct_cell.novel_path_first_age
                != expected['novel_path_first_age']
                or capacity_tuple != expected['capacity']
                or rng_call_count != int(expected['rng_call_count'])
                or direct_cell.mutation_events is not mutation_object
                or tuple(direct_cell.mutation_events) != mutation_order
                or direct_cell.mutation_events != expected_mutation
                or direct_cell.mutation_events['a48c3_extra_sentinel']
                != mutation_before['a48c3_extra_sentinel']
                or direct_cell.genomes is not genomes_object
                or any(
                    current is not original for current, original in zip(
                        direct_cell.genomes[:len(genome_objects)],
                        genome_objects,
                    )
                )
                or not np.array_equal(
                    np.asarray(
                        direct_cell.pools[unpaid], dtype=np.float64,
                    ).view(np.uint64),
                    pools_before[unpaid].view(np.uint64),
                )):
            raise AssertionError(
                'A4.8c3 fixed row %d drifted' % expected['row']
            )

        cases.append({
            'label': 'row%d' % expected['row'],
            'state': state,
            'cell_id': int(cell.cell_id),
            'dt': float(dt),
            'integration_capacity': copy.deepcopy(roomy),
            'expected': copy.deepcopy(expected),
            'rng_before_state': rng_before,
            'rng_after_state': copy.deepcopy(rng_after),
            'tape_state': tape.state_dict(),
            'plan_state': plan.state_dict(),
            'rng_schedule_sha256': str(tape.schedule_sha256),
            'mutation_items_before': tuple(mutation_before.items()),
            'mutation_items_after': tuple(
                direct_cell.mutation_events.items()
            ),
        })

        # Advance only the fixture RNG.  The next isolated cell must begin at
        # the same PCG64 point as the next A4.6b1 batch row.
        advance = copy.deepcopy(source_cell)
        advance._replicate_genome(world, dt, world.config)
    return cases


def _a48c3_existing_novel_case(cases=None):
    """Freeze the non-None novel-path age preservation branch."""
    cases = (
        _a48c3_completion_mutation_cases()
        if cases is None else cases
    )
    result = copy.deepcopy(cases[0])
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(result['state']),
    )
    world.cells[0].novel_path_first_age = 7.25
    world.initial_total_material = world.total_material()
    world.last_step_material_residual = 0.0
    result['label'] = 'row0-existing-novel-age'
    result['state'] = v3.pickle_clone(world.state_dict())
    result['expected']['novel_path_first_age'] = 7.25
    direct = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(result['state']),
    )
    direct.cells[0]._replicate_genome(
        direct, result['dt'], direct.config,
    )
    if direct.cells[0].novel_path_first_age != 7.25:
        raise AssertionError('A4.8c3 overwrote an existing novel-path age')
    result['rng_after_state'] = copy.deepcopy(
        direct.rng.bit_generator.state,
    )
    return result


def _a48c3_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c3.Hybrid066WorldA4ReplicationCompletionMutation(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48c3_assert_completion_oracle(expected_world, expected_cell,
                                     actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c3.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or actual_cell.replication_template is not None
            or actual_cell.replication_copy
            or float(actual_cell.replication_template_lesion) != 0.0
            or float(actual_cell.replication_fractional) != 0.0):
        raise AssertionError(label + ' completion/full PCG64 state differs')
    v3.assert_recursive_close(
        float(expected_world.dissipated_energy),
        float(actual_world.dissipated_energy),
        atol=0.0, rtol=0.0, path=label + '.dissipated_energy',
    )


def _a48c3_success_identity_snapshot(world, cell):
    return {
        'rng': id(world.rng),
        'pools': id(cell.pools),
        'genomes': id(cell.genomes),
        'genome_items': tuple(id(value) for value in cell.genomes),
        'lesions': id(cell.genome_lesions),
        'copy': id(cell.replication_copy),
        'events': id(cell.mutation_events),
        'event_order': tuple(cell.mutation_events),
        'proteins': id(cell.proteins),
        'damaged': id(cell.damaged_proteins),
        'gene_specs': id(cell.gene_specs),
    }


def test_a48c3_completion_mutation_atomic_commit_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c3')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveCompletionMutation(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_candidate = candidate
            return candidate

    cases = _a48c3_completion_mutation_cases()
    details = []
    for device in devices:
        selected = list(cases)
        if device == 'cpu':
            selected.append(_a48c3_existing_novel_case(cases))
        for case in selected:
            state = case['state']
            dt = case['dt']
            expected = case['expected']
            direct = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(state),
            )
            direct_cell = direct.cells[0]
            cell = world.cells[0]
            before_ids = _a48c3_success_identity_snapshot(world, cell)
            pools_before = np.asarray(cell.pools, np.float64).copy()
            genome_count_before = len(cell.genomes)
            lesion_count_before = len(cell.genome_lesions)
            cycles_before = int(cell.replication_cycles)
            rng_before = copy.deepcopy(world.rng.bit_generator.state)
            direct_cell._replicate_genome(direct, dt, direct.config)

            scheduler = ObserveCompletionMutation(
                case['integration_capacity'], device,
            )
            _a48c_begin_replication_direct(scheduler, world, cell, dt)
            try:
                scheduler.cpu_replication(world, cell, dt, world.config)
                candidate = scheduler.observed_candidate
                plan = candidate.plan
                tape = candidate.tape
                label = 'a48c3.oracle.%s.%s' % (
                    device, case['label'],
                )
                _a48c3_assert_completion_oracle(
                    direct, direct_cell, world, cell, label,
                )
                after_ids = _a48c3_success_identity_snapshot(world, cell)
                for key in (
                        'rng', 'pools', 'genomes', 'lesions', 'copy',
                        'events', 'event_order', 'proteins', 'damaged',
                        'gene_specs'):
                    if after_ids[key] != before_ids[key]:
                        raise AssertionError(
                            '%s changed %s identity/order' % (label, key)
                        )
                if (after_ids['genome_items'][:genome_count_before]
                        != before_ids['genome_items']
                        or len(cell.genomes) != genome_count_before + 1
                        or len(cell.genome_lesions) != lesion_count_before + 1
                        or int(cell.replication_cycles) != cycles_before + 1):
                    raise AssertionError(label + ' topology/old polymer differs')
                if tuple(cell.mutation_events.items()) != tuple(
                        case['mutation_items_after']):
                    raise AssertionError(label + ' additive mutation ledger differs')
                if (cell.mutation_events['a48c3_extra_sentinel']
                        != dict(case['mutation_items_before'])[
                            'a48c3_extra_sentinel'
                        ]):
                    raise AssertionError(label + ' extra mutation key was lost')

                final_length = int(plan.final_lengths[0])
                structural = tuple(
                    int(value) for value in
                    np.asarray(plan.structural_event_counts[0], np.int64)
                )
                before_cache = (
                    int(rng_before['has_uint32']),
                    int(rng_before['uinteger']),
                )
                after_rng = world.rng.bit_generator.state
                after_cache = (
                    int(after_rng['has_uint32']),
                    int(after_rng['uinteger']),
                )
                if (before_cache != expected['rng_before_cache']
                        or after_cache != expected['rng_after_cache']
                        or candidate.rng_before_state != rng_before
                        or candidate.rng_after_state != after_rng
                        or tape.rng_before_state != rng_before
                        or tape.rng_after_state != after_rng
                        or final_length != int(expected['final_length'])
                        or structural
                        != tuple(expected['structural_event_counts'])
                        or int(plan.substitution_events[0])
                        != int(expected['substitution_events'])
                        or int(plan.material_delta_symbols[0])
                        != int(expected['material_delta_symbols'])
                        or _a48c3_completion_mutation_rng_call_count(tape)
                        != int(expected['rng_call_count'])
                        or not np.array_equal(
                            np.asarray(
                                plan.final_symbols[0, :final_length],
                                np.uint8,
                            ),
                            np.asarray(cell.genomes[-1], np.uint8),
                        )
                        or abs(
                            float(plan.new_genome_lesions[0])
                            - float(cell.genome_lesions[-1])
                        ) > a48c3.REPLICATION_ORACLE_ATOL
                        or int(plan.completion_events[0]) != 1
                        or int(plan.replication_cycle_deltas[0]) != 1
                        or int(plan.genome_count_after[0])
                        != len(cell.genomes)
                        or cell.novel_path_first_age
                        != expected['novel_path_first_age']
                        or list(direct_cell.gene_specs)
                        != list(cell.gene_specs)):
                    raise AssertionError(label + ' plan/RNG/topology/cache differs')
                v3.assert_recursive_close(
                    np.asarray(plan.pools_after[0], np.float64),
                    np.asarray(cell.pools, np.float64),
                    atol=a48c3.REPLICATION_ORACLE_ATOL, rtol=0.0,
                    path=label + '.plan_pools_after',
                )
                a48c3._completion_mutation_plan_semantic_match(
                    plan, candidate.host_replay,
                )
                if not a48._state_arrays_bit_exact(
                        tape.state_dict(), case['tape_state']):
                    raise AssertionError(label + ' host tape differs')

                if int(expected['row']) == 1:
                    changed = {
                        index for index in range(int(a4.a3.POOL_COUNT))
                        if np.float64(pools_before[index]).view(np.uint64)
                        != np.float64(cell.pools[index]).view(np.uint64)
                    }
                    paid = {
                        int(a4.a3.POOL_NUCLEOTIDE), int(a4.a3.POOL_ATP),
                    }
                    if changed != paid:
                        raise AssertionError(
                            label + ' refund changed a non-paid pool'
                        )

                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                expected_metadata = {
                    'authority': (
                        'A4.8c3-resident-completion-mutation-plan-atomic-'
                        'cpu-cell-rng-commit'
                    ),
                    'branch': 'active-completion-mutation',
                    'mutation_enabled': True,
                    'device': device,
                    'amount': int(plan.last_replication_symbols[0]),
                    'requested_symbols': int(plan.requested_symbols[0]),
                    'completion_events': 1,
                    'substitution_events': int(
                        expected['substitution_events']
                    ),
                    'structural_event_counts': list(
                        expected['structural_event_counts']
                    ),
                    'material_delta_symbols': int(
                        expected['material_delta_symbols']
                    ),
                    'final_length': int(expected['final_length']),
                    'rng_call_count': int(expected['rng_call_count']),
                    'rng_schedule_sha256': case['rng_schedule_sha256'],
                }
                for key, value in expected_metadata.items():
                    if metadata.get(key) != value:
                        raise AssertionError(
                            '%s receipt %s differs' % (label, key)
                        )
                details.append('%s:%s:%d/%d' % (
                    device, case['label'], final_length,
                    expected['rng_call_count'],
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('3-row Formal066 structural/material/lesion/cache/PCG64 exact; '
            'sentinel ledger additive identity/order + extra key; existing '
            'novel age preserved: ' + ', '.join(details))


def test_a48c3_completion_mutation_candidate_cpu_cuda_purity_rng_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c3')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    case = _a48c3_completion_mutation_cases()[0]
    reference_plan = None
    reference_state = None
    reference_rng = None
    details = []
    for device in devices:
        state = case['state']
        dt = case['dt']
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(state),
        )
        direct.cells[0]._replicate_genome(direct, dt, direct.config)
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(state),
        )
        cell = world.cells[0]
        scheduler = a48c3.A4ReplicationCompletionMutationEventScheduler(
            case['integration_capacity'], device,
        )
        _a48c_begin_replication_direct(scheduler, world, cell, dt)
        before_world = v3.pickle_clone(world.state_dict())
        before_ids = _a48c_identity_snapshot(world, cell)
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = scheduler._prepare_completion_mutation_candidate(
                world, cell, dt, world.config,
            )
            v3.assert_recursive_close(
                before_world, world.state_dict(), 0.0, 0.0,
                'a48c3.prepare_purity.' + device,
            )
            _, record = scheduler._record(cell)
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng
                    or float(world.dissipated_energy) != before_energy
                    or _a48c_identity_snapshot(world, cell) != before_ids
                    or any(entry['event'] == 'replication_cpu'
                           for entry in record['events'])):
                raise AssertionError(device + ' prepare crossed claim/purity')
            if (candidate.candidate_cell is cell
                    or candidate.candidate_cell.pools is cell.pools
                    or candidate.candidate_cell.genomes is cell.genomes
                    or candidate.candidate_cell.replication_copy
                    is cell.replication_copy
                    or candidate.candidate_cell.mutation_events
                    is cell.mutation_events
                    or candidate.candidate_cell.gene_specs
                    is cell.gene_specs
                    or np.shares_memory(
                        candidate.candidate_cell.pools, cell.pools,
                    )
                    or np.shares_memory(
                        candidate.source_binding.state.pools, cell.pools,
                    )):
                raise AssertionError(device + ' candidate/source aliases live biology')
            if (candidate.plan is candidate.host_replay
                    or candidate.plan is candidate.resident_plan
                    or candidate.tape is candidate.resident_tape
                    or candidate.source_binding is candidate.fresh_binding
                    or candidate.live_rng is not world.rng
                    or candidate.rng_before_state != before_rng
                    or candidate.rng_after_state
                    != candidate.tape.rng_after_state):
                raise AssertionError(device + ' candidate authority/RNG differs')

            actual_artifacts = (
                a48c3._resident_completion_mutation_artifacts(
                    candidate.resident_binding,
                    candidate.resident_tape,
                    candidate.resident_plan,
                    device,
                )
            )
            if actual_artifacts != candidate.resident_artifacts:
                raise AssertionError(device + ' resident pointer/version differs')
            if (a48c3._host_completion_mutation_artifacts(
                    candidate.tape, candidate.host_replay, candidate.plan,
                    ) != candidate.host_artifacts
                    or not a48._state_arrays_bit_exact(
                        candidate.tape.state_dict(), case['tape_state'],
                    )
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_tape.to_numpy().state_dict(),
                        candidate.tape.state_dict(),
                    )):
                raise AssertionError(device + ' host/resident tape association differs')
            a48c3._completion_mutation_plan_semantic_match(
                candidate.plan, candidate.host_replay,
            )
            oracle_mean = float(
                candidate.host_replay.genome_lesion_mean_after[0]
            )
            plan_one_ulp = candidate.plan.clone()
            plan_one_ulp.genome_lesion_mean_after[0] = np.nextafter(
                np.float64(oracle_mean), np.float64(np.inf),
            )
            a48c3._completion_mutation_plan_semantic_match(
                plan_one_ulp, candidate.host_replay,
            )
            plan_two_ulp = candidate.plan.clone()
            plan_two_ulp.genome_lesion_mean_after[0] = np.nextafter(
                np.nextafter(
                    np.float64(oracle_mean), np.float64(np.inf),
                ),
                np.float64(np.inf),
            )
            _assert_raises(
                a48c3.A4ReplicationCompletionMutationCommitError,
                lambda: a48c3._completion_mutation_plan_semantic_match(
                    plan_two_ulp, candidate.host_replay,
                ),
            )
            fresh_mean = float(
                candidate.fresh_binding.state.genome_lesion_mean[0]
            )
            fresh_one_ulp = candidate.plan.clone()
            fresh_one_ulp.genome_lesion_mean_after[0] = np.nextafter(
                np.float64(fresh_mean), np.float64(np.inf),
            )
            scheduler._validate_fresh_completion_mutation_candidate(
                cell, candidate.candidate_cell, candidate.tape,
                fresh_one_ulp, candidate.source_binding,
                candidate.fresh_binding, world.config,
            )
            fresh_two_ulp = candidate.plan.clone()
            fresh_two_ulp.genome_lesion_mean_after[0] = np.nextafter(
                np.nextafter(
                    np.float64(fresh_mean), np.float64(np.inf),
                ),
                np.float64(np.inf),
            )
            _assert_raises(
                a48c3.A4ReplicationCompletionMutationCommitError,
                lambda: scheduler._validate_fresh_completion_mutation_candidate(
                    cell, candidate.candidate_cell, candidate.tape,
                    fresh_two_ulp, candidate.source_binding,
                    candidate.fresh_binding, world.config,
                ),
            )
            if not a48._state_arrays_bit_exact(
                    candidate.resident_plan.to_numpy().state_dict(),
                    candidate.plan.state_dict(),
            ):
                raise AssertionError(device + ' explicit plan readback differs')
            _a48b_assert_cell_matches(
                direct.cells[0], candidate.candidate_cell,
                'a48c3.candidate.' + device,
                atol=a48c3.REPLICATION_ORACLE_ATOL,
            )
            a4._require_translation_binding(candidate.fresh_binding)
            if (not isinstance(
                    candidate.fresh_binding.ragged.symbols, np.ndarray)
                    or not isinstance(
                        candidate.fresh_binding.cache.fingerprints,
                        np.ndarray,
                    )
                    or np.shares_memory(
                        candidate.fresh_binding.ragged.symbols,
                        candidate.source_binding.ragged.symbols,
                    )):
                raise AssertionError(device + ' fresh host binding/cache differs')

            scheduler._commit_completion_mutation_candidate(
                world, cell, dt, world.config, candidate,
            )
            _a48c3_assert_completion_oracle(
                direct, direct.cells[0], world, cell,
                'a48c3.commit.' + device,
            )
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48c3.A4ReplicationCompletionMutationCommitError,
                lambda: scheduler._commit_completion_mutation_candidate(
                    world, cell, dt, world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48c3.one_shot.' + device,
            )
            if (not candidate.consumed
                    or world.rng is not before_rng_object
                    or world.rng.bit_generator.state != committed_rng):
                raise AssertionError(device + ' one-shot/live Generator differs')

            if reference_plan is None:
                reference_plan = candidate.plan.clone()
                reference_state = v3.pickle_clone(cell.state_dict())
                reference_rng = copy.deepcopy(committed_rng)
            else:
                a48c3._completion_mutation_plan_semantic_match(
                    candidate.plan, reference_plan,
                )
                v3.assert_recursive_close(
                    reference_state, cell.state_dict(),
                    a48c3.REPLICATION_ORACLE_ATOL, 0.0,
                    'a48c3.cpu_cuda_state',
                )
                if committed_rng != reference_rng:
                    raise AssertionError('CPU/CUDA committed PCG64 differs')
            pointer_count = sum(
                len(values) for values in
                candidate.resident_artifacts['data_ptrs'].values()
            )
            details.append('%s:%d-pointers' % (device, pointer_count))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('CPU/CUDA prepare purity, resident device/pointer/version/content, '
            'host tape/NumPy oracle/readback plan, plan/fresh lesion mean '
            '1-ULP accept + 2-ULP reject, deep candidate/fresh cache, same '
            'live PCG64 and one-shot PASS: ' + '/'.join(details))


def _a48c3_assert_preclaim_failure(state, cell_id, dt, capacity,
                                    scheduler_type=None, device='cpu',
                                    label='scope'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(
        item for item in world.cells if int(item.cell_id) == int(cell_id)
    )
    scheduler_type = (
        scheduler_type
        or a48c3.A4ReplicationCompletionMutationEventScheduler
    )
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(world.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng_object = world.rng
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    before_receipts = len(scheduler._receipts)
    try:
        _assert_raises(
            (a48c3.A4ReplicationCompletionMutationCommitError,
             a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, dt, world.config,
            ),
        )
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c3.preclaim.' + label,
        )
        _, record = scheduler._record(cell)
        if (world.rng is not before_rng_object
                or world.rng.bit_generator.state != before_rng
                or float(world.dissipated_energy) != before_energy
                or _a48c_identity_snapshot(world, cell) != before_ids
                or len(scheduler._receipts) != before_receipts
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c3_completion_mutation_capacity_trust_rollback_scope_and_exact_once():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c3 trust tests')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    resident_devices = ['cpu']
    if torch.cuda.is_available():
        resident_devices.append('cuda')
    cases = _a48c3_completion_mutation_cases()

    # Each one-short is tied to the row where that arena axis is authoritative:
    # row0 W grows 576->577, row2 S grows 23->40, row1 P grows 36->37.
    axes = (
        ('Q', 'max_sequences', 0),
        ('S', 'max_symbols', 2),
        ('W', 'max_sequence_symbols', 0),
        ('P', 'max_proteins_per_cell', 1),
    )
    capacity_details = []
    capacity_keys = (
        'max_cells', 'max_sequences', 'max_symbols',
        'max_sequence_symbols', 'max_proteins_per_cell',
    )
    for label, key, row in axes:
        case = cases[row]
        exact_values = dict(zip(
            capacity_keys, case['expected']['capacity'],
        ))
        exact = a4.GPU068A4Config(**exact_values)
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(case['state']),
        )
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        for device in resident_devices:
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(case['state']),
            )
            cell = world.cells[0]
            scheduler = (
                a48c3.A4ReplicationCompletionMutationEventScheduler(
                    exact, device,
                )
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                _a48c3_assert_completion_oracle(
                    direct, direct.cells[0], world, cell,
                    'a48c3.capacity.%s.%s' % (label, device),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append('%s@%s' % (label, device))
        short = dict(exact_values)
        short[key] -= 1
        _a48c3_assert_preclaim_failure(
            case['state'], case['cell_id'], case['dt'],
            a4.GPU068A4Config(**short),
            label='capacity_%s_row%d_one_short' % (label, row),
        )
    del scheduler, world, cell, direct
    gc.collect()

    # The lesion vector may legally lag the genome vector on entry.  This is
    # still an in-scope c3 completion, on both resident devices.
    missing_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(cases[0]['state']),
    )
    missing_world.cells[0].genome_lesions = []
    missing_world.initial_total_material = missing_world.total_material()
    missing_state = v3.pickle_clone(missing_world.state_dict())
    missing_direct = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(missing_state),
    )
    missing_direct.cells[0]._replicate_genome(
        missing_direct, cases[0]['dt'], missing_direct.config,
    )
    for device in resident_devices:
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(missing_state),
        )
        cell = world.cells[0]
        scheduler = a48c3.A4ReplicationCompletionMutationEventScheduler(
            cases[0]['integration_capacity'], device,
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, cases[0]['dt'],
        )
        try:
            scheduler.cpu_replication(
                world, cell, cases[0]['dt'], world.config,
            )
            _a48c3_assert_completion_oracle(
                missing_direct, missing_direct.cells[0], world, cell,
                'a48c3.missing_lesion.' + device,
            )
        finally:
            _a48_abort_direct(scheduler, cell)
    del scheduler, world, cell, missing_world, missing_direct
    gc.collect()

    base = cases[0]
    trust_capacity = a4.GPU068A4Config(**dict(zip(
        capacity_keys, base['expected']['capacity'],
    )))
    trust_details = []

    def assert_tamper_failure(scheduler_type, label, devices=('cpu',),
                              require_isolation=False):
        for device in devices:
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(base['state']),
            )
            cell = world.cells[0]
            scheduler = scheduler_type(
                trust_capacity, device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            before_world = v3.pickle_clone(world.state_dict())
            before_objects = _a48c_identity_snapshot(world, cell)
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_config_object = id(world.config)
            before_receipts = len(scheduler._receipts)
            try:
                _assert_raises(
                    (a48c3.A4ReplicationCompletionMutationCommitError,
                     a4.A4Error, a3s.A3SchedulerError),
                    lambda: scheduler.cpu_replication(
                        world, cell, base['dt'], world.config,
                    ),
                )
                expected_world = getattr(
                    scheduler, 'expected_world_state', before_world,
                )
                expected_rng = getattr(
                    scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, world.state_dict(), 0.0, 0.0,
                    'a48c3.trust.%s.%s' % (device, label),
                )
                _, record = scheduler._record(cell)
                if (world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(world, cell)
                        != before_objects
                        or id(world.config) != before_config_object
                        or len(scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim' % (device, label)
                    )
                if (require_isolation
                        and not bool(getattr(scheduler, 'isolated', False))):
                    raise AssertionError(label + ' retained alias was not isolated')
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
            del scheduler, cell, world
            gc.collect()

    class TamperPackedSource(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.source_binding.state.pools
            self.isolated = (
                retained is not cell.pools
                and not np.shares_memory(retained, cell.pools)
            )
            retained[0, a4.a3.POOL_NUCLEOTIDE] += 1e-8
            return candidate

    class TamperHostTape(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.tape.append_uniform_draws[0, 0] = np.nextafter(
                candidate.tape.append_uniform_draws[0, 0], np.inf,
            )
            return candidate

    class TamperHostOracle(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.final_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperReadbackPlan(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.final_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperResidentRaggedData(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentStateData(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_NUCLEOTIDE
            ].add_(1e-8)
            return candidate

    class TamperResidentCacheData(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class TamperResidentTapeData(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_tape.append_uniform_draws.data[0, 0].add_(0.125)
            return candidate

    class TamperResidentPlanData(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.final_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    class SwapResidentTensorPointer(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.final_symbols = (
                candidate.resident_plan.final_symbols.clone()
            )
            return candidate

    class SwapResidentWholeMember(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'ragged',
                candidate.resident_binding.ragged.clone(),
            )
            return candidate

    class TamperResidentPrivateSnapshot(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_artifacts['data_ptrs']['plan'][
                'final_symbols'
            ] += 1
            return candidate

    class TamperHostPrivateSnapshot(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_artifacts['readback_plan']['data_ptrs'][
                'final_symbols'
            ] += 1
            return candidate

    class MoveResidentTensorDevice(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            tensor = candidate.resident_binding.ragged.symbols
            target = 'cuda' if tensor.device.type == 'cpu' else 'cpu'
            candidate.resident_binding.ragged.symbols = tensor.to(target)
            return candidate

    class TamperCandidateCell(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.genomes[-1][0] ^= np.uint8(1)
            return candidate

    class MutateUnpackedCandidate(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.candidate_cell.last_repair_flux
            self.isolated = (
                retained is not cell.last_repair_flux
                and not np.shares_memory(retained, cell.last_repair_flux)
            )
            retained[0] += 0.125
            return candidate

    class TamperFreshBinding(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class AliasReadbackToOracle(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.final_symbols = candidate.host_replay.final_symbols
            return candidate

    class TamperSchedulerCapacityDevice(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = 'cuda' if self.a4_device == 'cpu' else 'cpu'
            return candidate

    class TamperLiveConfig(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                float(world.config.mutation_rate), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveSource(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.replication_fractional = float(np.nextafter(
                float(cell.replication_fractional), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveRng(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperCandidateRng(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _replication_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_state,
            )
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    for scheduler_type, label, isolation in (
            (TamperPackedSource, 'packed_source_alias', True),
            (TamperHostTape, 'host_tape', False),
            (TamperHostOracle, 'host_oracle', False),
            (TamperReadbackPlan, 'readback_plan', False),
            (SwapResidentTensorPointer, 'resident_pointer', False),
            (SwapResidentWholeMember, 'resident_whole_member', False),
            (TamperResidentPrivateSnapshot, 'resident_private', False),
            (TamperHostPrivateSnapshot, 'host_private', False),
            (TamperCandidateCell, 'candidate_cell', False),
            (MutateUnpackedCandidate, 'unpacked_candidate_alias', True),
            (TamperFreshBinding, 'fresh_binding', False),
            (AliasReadbackToOracle, 'packed_unpacked_plan_alias', False),
            (TamperSchedulerCapacityDevice, 'scheduler_config_device', False),
            (TamperLiveConfig, 'live_config', False),
            (TamperLiveSource, 'live_source', False),
            (TamperLiveRng, 'live_rng', False),
            (TamperCandidateRng, 'candidate_rng', False)):
        assert_tamper_failure(
            scheduler_type, label, require_isolation=isolation,
        )
    for scheduler_type, label in (
            (TamperResidentRaggedData, 'resident_ragged_data'),
            (TamperResidentStateData, 'resident_state_data'),
            (TamperResidentCacheData, 'resident_cache_data'),
            (TamperResidentTapeData, 'resident_tape_data'),
            (TamperResidentPlanData, 'resident_plan_data')):
        assert_tamper_failure(
            scheduler_type, label, devices=resident_devices,
        )
    if torch.cuda.is_available():
        assert_tamper_failure(
            MoveResidentTensorDevice, 'resident_device',
            devices=resident_devices,
        )

    # Wrong cell/dt/config must not consume one private candidate.
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    cell = world.cells[0]
    scheduler = a48c3.A4ReplicationCompletionMutationEventScheduler(
        trust_capacity, 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, cell, base['dt'])
    before = v3.pickle_clone(world.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = scheduler._prepare_completion_mutation_candidate(
            world, cell, base['dt'], world.config,
        )
        wrong_inputs = (
            ('cell', copy.deepcopy(cell), base['dt'], world.config),
            ('dt', cell, np.nextafter(base['dt'], np.inf), world.config),
            ('config', cell, base['dt'], copy.deepcopy(world.config)),
        )
        for label, wrong_cell, wrong_dt, wrong_config in wrong_inputs:
            _assert_raises(
                a48c3.A4ReplicationCompletionMutationCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler._commit_completion_mutation_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError('wrong ' + label + ' consumed candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c3.wrong_cell_dt_config',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong inputs changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)
    del candidate, scheduler, world, cell
    gc.collect()

    # Inactive start and pre-active early returns are outside c3 and must not
    # call the inherited frozen CPU replication bridge.
    scope_cases = []
    start_world, start_cells, _, start_dt = _a45b_template_start_fixture(
        seed=11961,
    )
    start_world.config.mutation = True
    _a48c_reduce_world(start_world, [start_cells[0]])
    scope_cases.append((
        'inactive_start', start_world.state_dict(),
        int(start_cells[0].cell_id), start_dt,
    ))
    for label, mutate in (
            ('disabled', lambda world, cell: setattr(
                world.config, 'genome_replication', False)),
            ('no_genome', lambda world, cell: (
                setattr(cell, 'genomes', []),
                setattr(cell, 'genome_lesions', []),
                setattr(cell, 'replication_template', None),
                setattr(cell, 'replication_copy', []),
                cell._refresh_gene_cache())),
            ('negative_atp', lambda world, cell: cell.pools.__setitem__(
                a4.a3.POOL_ATP, -1e-12))):
        scoped = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(base['state']),
        )
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scope_cases.append((
            label, scoped.state_dict(), int(scoped_cell.cell_id), base['dt'],
        ))
    gate = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    gate.config.external_replicase = False
    gate_cell = gate.cells[0]
    for fingerprint, spec in list(gate_cell.gene_specs.items()):
        if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
            gate_cell.proteins.pop(fingerprint, None)
    gate_cell._sync_protein_pool()
    gate.initial_total_material = gate.total_material()
    scope_cases.append((
        'replicase_gate', gate.state_dict(),
        int(gate_cell.cell_id), base['dt'],
    ))
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for label, state, cell_id, dt in scope_cases:
            _a48c3_assert_preclaim_failure(
                state, cell_id, dt, trust_capacity,
                label='scope_' + label,
            )
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('excluded c3 scope used CPU fallback')
    del start_world, start_cells, gate, gate_cell, scoped, scoped_cell
    gc.collect()

    # Duplicate and out-of-order calls remain exact-once scheduler errors.
    duplicate = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = (
        a48c3.A4ReplicationCompletionMutationEventScheduler(
            trust_capacity, 'cpu',
        )
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, base['dt'],
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, base['dt'], duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, base['dt'], duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate.state_dict(), 0.0, 0.0,
        'a48c3.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate completion changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)
    del duplicate_scheduler, duplicate_cell, duplicate
    gc.collect()

    order = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    order_cell = order.cells[0]
    order_scheduler = a48c3.A4ReplicationCompletionMutationEventScheduler(
        trust_capacity, 'cpu',
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, base['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, base['dt'], order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order.state_dict(), 0.0, 0.0,
        'a48c3.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order completion changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)
    gc.collect()

    class FailAfterPublish(
            a48c3.A4ReplicationCompletionMutationEventScheduler):
        def _publish_replication_completion_mutation_candidate(
                self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_completion_mutation_candidate(
                world, cell, candidate,
            )
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.genomes[0][0] ^= np.uint8(1)
            cell.genome_lesions[0] += 0.25
            cell.mutation_events['a48c3_extra_sentinel'] += 1
            first = next(iter(cell.gene_specs))
            cell.gene_specs[first]['promoter'] += 0.25
            cell.replication_cycles += 3
            cell.last_replication_symbols += 2
            cell.last_effective_error_rate += 0.125
            cell.cumulative_proofreading_atp += 0.5
            cell.novel_path_first_age = -9.0
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c3 publish failure')

    rollback = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(
        trust_capacity, 'cpu',
    )
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, base['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    rollback_spec_ids = tuple(
        (key, id(spec)) for key, spec in rollback_cell.gene_specs.items()
    )
    rollback_rng_object = rollback.rng
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    rollback_cache = (
        int(rollback_rng['has_uint32']), int(rollback_rng['uinteger']),
    )
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, base['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c3 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c3.publish_rollback',
    )
    restored_rng = rollback.rng.bit_generator.state
    if (rollback.rng is not rollback_rng_object
            or restored_rng != rollback_rng
            or (int(restored_rng['has_uint32']), int(restored_rng['uinteger']))
            != rollback_cache
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids
            or tuple(
                (key, id(spec))
                for key, spec in rollback_cell.gene_specs.items()
            ) != rollback_spec_ids):
        raise AssertionError(
            'publish rollback lost outer/nested identity or PCG64 cache'
        )
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if entry['event'] != 'replication_cpu' or receipt['status'] != 'aborted':
        raise AssertionError('publish failure did not leave aborted receipt')
    return ('transition-specific Q/S/W/P exact CPU/CUDA + one-short; '
            'missing-lesion CPU/CUDA; %d host/resident/public/private/.data/'
            'pointer/device/candidate/live/alias trust checks; wrong inputs; '
            '%d no-fallback scopes; duplicate/order; nested identity + PCG64 '
            'cache rollback PASS (%s)' % (
                len(trust_details), len(scope_cases),
                '/'.join(capacity_details),
            ))


def test_a48c3_inherited_c2_c1_save_clone_successor_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c3')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    delegate_cases = []
    world, cells, capacity, dt = _paid_replication_b_fixture(seed=11981)
    _a48c_reduce_world(world, [cells[0]])
    delegate_cases.append((
        'c1-off-noncompletion', v3.pickle_clone(world.state_dict()), dt,
        capacity,
        'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'active-noncompletion-deterministic',
    ))
    world, cells, capacity, dt = _a45_substitution_fixture(seed=11982)
    world.config.mutation_rate = 1.0
    _a48c_reduce_world(world, [cells[0]])
    delegate_cases.append((
        'c1-on-noncompletion', v3.pickle_clone(world.state_dict()), dt,
        capacity,
        'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'active-noncompletion-substitution',
    ))
    requested = next(
        case for case in _a48c_oracle_cases(seed=11983)
        if case[0] == 'requested-zero'
    )
    delegate_cases.append((
        'c1-requested-zero', requested[1], requested[3],
        a4.GPU068A4Config(),
        'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'active-noncompletion-deterministic',
    ))
    completion_state, _, completion_dt = _a48c2_completion_case(
        seed=11984,
    )
    delegate_cases.append((
        'c2-mutation-free-completion', completion_state, completion_dt,
        a4.GPU068A4Config(),
        'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit',
        'active-completion-deterministic',
    ))

    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy replication bridge was called')

    delegated = []
    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for device in devices:
            for (label, state, case_dt, case_capacity,
                 authority, branch) in delegate_cases:
                direct = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(state),
                )
                direct.cells[0]._replicate_genome(
                    direct, case_dt, direct.config,
                )
                integrated = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(state),
                )
                cell = integrated.cells[0]
                scheduler = (
                    a48c3.A4ReplicationCompletionMutationEventScheduler(
                        case_capacity, device,
                    )
                )
                _a48c_begin_replication_direct(
                    scheduler, integrated, cell, case_dt,
                )
                try:
                    scheduler.cpu_replication(
                        integrated, cell, case_dt, integrated.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], cell,
                        'a48c3.delegate.%s.%s' % (device, label),
                        atol=a48c3.REPLICATION_ORACLE_ATOL,
                    )
                    if (integrated.rng.bit_generator.state
                            != direct.rng.bit_generator.state):
                        raise AssertionError(label + ' delegated RNG differs')
                    metadata = _a48c_replication_entry(
                        scheduler, cell,
                    )['metadata']
                    if (metadata.get('authority') != authority
                            or metadata.get('branch') != branch):
                        raise AssertionError(
                            '%s/%s delegate authority differs' % (
                                device, label,
                            )
                        )
                    if (label == 'c1-requested-zero'
                            and (int(metadata.get('requested_symbols', -1)) != 0
                                 or int(metadata.get('amount', -1)) != 0)):
                        raise AssertionError('c1 requested-zero delegate differs')
                    delegated.append(device + ':' + label)
                finally:
                    _a48_abort_direct(scheduler, cell)
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('a c1/c2 delegate used frozen CPU fallback')

    # The c3-owned branch must not reach c2's public dispatch method.
    own_case = _a48c3_completion_mutation_cases()[0]
    original_c2 = a48c2.A4ReplicationCompletionEventScheduler.cpu_replication

    def c2_dispatch_bomb(*args, **kwargs):
        raise AssertionError('c3-owned completion delegated to c2')

    a48c2.A4ReplicationCompletionEventScheduler.cpu_replication = (
        c2_dispatch_bomb
    )
    try:
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(own_case['state']),
        )
        direct.cells[0]._replicate_genome(
            direct, own_case['dt'], direct.config,
        )
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(own_case['state']),
        )
        scheduler = a48c3.A4ReplicationCompletionMutationEventScheduler(
            own_case['integration_capacity'], 'cpu',
        )
        _a48c_begin_replication_direct(
            scheduler, world, world.cells[0], own_case['dt'],
        )
        try:
            scheduler.cpu_replication(
                world, world.cells[0], own_case['dt'], world.config,
            )
            _a48c3_assert_completion_oracle(
                direct, direct.cells[0], world, world.cells[0],
                'a48c3.owned_no_c2_dispatch',
            )
        finally:
            _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48c2.A4ReplicationCompletionEventScheduler.cpu_replication = (
            original_c2
        )

    # Full world step freezes the post-completion successor RNG/event order.
    successor_details = []
    for device in devices:
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(own_case['state']),
        )
        hybrid = _a48c3_hybrid_from_state(
            own_case['state'], own_case['integration_capacity'], device,
        )
        cpu.step(own_case['dt'])
        original_legacy = a48b.A4TranslationEventScheduler.cpu_replication
        a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
        try:
            receipt = hybrid.step(own_case['dt'])
        finally:
            a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c3.successor.' + device,
        )
        record = receipt['cells'][0]
        names = [entry['event'] for entry in record['events']]
        by_name = {entry['event']: entry for entry in record['events']}
        if (names.count('replication_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1
                or not names.index('translation_cpu')
                < names.index('replication_cpu')
                < names.index('surface_assembly')
                < names.index('genome_hydrolysis_cpu_rng')
                < names.index('motion')
                or by_name['replication_cpu']['metadata'].get('authority')
                != ('A4.8c3-resident-completion-mutation-plan-atomic-'
                    'cpu-cell-rng-commit')):
            raise AssertionError(device + ' c3 successor order differs')
        successor_details.append(device)

    # Save/load/clone must preserve the c3 type and all inherited settings.
    capacity = own_case['integration_capacity']
    hybrid = _a48c3_hybrid_from_state(own_case['state'], capacity, 'cpu')
    twin = hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c3.pkl')
        hybrid.save(path)
        restored = (
            a48c3.Hybrid066WorldA4ReplicationCompletionMutation.load(path)
        )
        for value in (hybrid, twin, restored):
            if (type(value)
                    is not a48c3.Hybrid066WorldA4ReplicationCompletionMutation
                    or type(value.scheduler) is not (
                        a48c3.A4ReplicationCompletionMutationEventScheduler
                    )
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != capacity
                    or value.scheduler.completion_mutation_integration_state()
                    != {
                        'schema': a48c3.SCHEMA_VERSION,
                        'config': a48._config_state(capacity),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c3 save/clone authority was lost')
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(own_case['state']),
        )
        cpu.step(own_case['dt'])
        for value in (hybrid, twin, restored):
            value.step(own_case['dt'])
            v3._assert_world_pair(
                cpu, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c3.persisted_successor',
            )

    active = _a48c3_hybrid_from_state(
        own_case['state'], capacity, 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    expected_status = (
        'a4.8c3-mutation-enabled-pre-existing-active-completion-'
        'resident-final-plan-cpu-cell-pcg64-atomic-commit-'
        'a4.8c2-completion-and-a4.8c1-noncompletion-inherited-'
        'inactive-start-early-noop-fail-closed'
    )
    signature = inspect.signature(
        a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication,
    )
    prior_names = '\n'.join(fn.__name__ for fn in TESTS[:62])
    c3_names = tuple(fn.__name__ for fn in TESTS[62:66])
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationCompletionMutationCommitCandidate'
            in set(a48c3.__all__)
            or not issubclass(
                a48c3.A4ReplicationCompletionMutationEventScheduler,
                a48c2.A4ReplicationCompletionEventScheduler,
            )
            or not issubclass(
                a48c3.Hybrid066WorldA4ReplicationCompletionMutation,
                a48c2.Hybrid066WorldA4ReplicationCompletion,
            )
            or a48c3.PORT_STATUS.get('genome_replication') != expected_status
            or a48c3.FULL_GPU_WORLD_STEP is not False
            or a48c3.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) < 66
            or c3_names != (
                'test_a48c3_completion_mutation_atomic_commit_formal066_oracle',
                'test_a48c3_completion_mutation_candidate_cpu_cuda_purity_rng_and_fresh_binding',
                'test_a48c3_completion_mutation_capacity_trust_rollback_scope_and_exact_once',
                'test_a48c3_inherited_c2_c1_save_clone_successor_and_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A48C2_TEST_NAMES_SHA256):
        raise AssertionError('A4.8c3 API/scope/62-test authority differs')
    expected_hashes = dict(PROMOTED_A3_SHA256)
    expected_hashes.update(_A48C3_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_completion_mutation_integration.py'
    ] = '9e34a63322e717f646f225588abd3f908e335c5527bc3068bd357d438f73057c'
    for relative, expected in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('c1 off/on/requested-zero + c2 completion delegates %s; c3 owns '
            'completion without c2/frozen fallback; save/load/clone and c1/c2/'
            'c3 active/pending reject; translation<replication<surface<'
            'hydrolysis<motion full PCG64 successor %s; first62/A3/c1/c2/c3 '
            'hash/full_gpu=false PASS' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A48C4_TEST_NAMES_SHA256 = (
    'a57acfa15244e5147d4d325181d606c20d1cdf1292d690462aab4daf272e3a34'
)

_A48C4_PRIOR_CORE_SHA256 = {
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication.py': (
        'c4e27520b8665ba036322e9b1e23ccec280b40ad5d19016ba523bd4bf3dd1e0a'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_integration.py': (
        'fa9787a6dfc59a7767fcc00095ad052f7afe77877eef78b6e02e992015b2bbef'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_integration.py': (
        '6bf4c979b524c176c2e72b1630b14f630638cdd7eb883bdeeb48208e9ba5b416'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_mutation_integration.py': (
        '9e34a63322e717f646f225588abd3f908e335c5527bc3068bd357d438f73057c'
    ),
}

_A48C4_CORE_SHA256 = (
    '10f3e357e1b2466245ba10add7b22599b4a2853abf2c7bb5cd1f247dc022fd3d'
)

_A48C4_EXPECTED_CASES = {
    'row0-normal': (18, 18, 3, 576, 3, 1170, 576, 36),
    'row2-missing-lesion': (5, 1, 0, 496, 3, 993, 496, 31),
    'dt-zero': (0, 0, 0, 576, 3, 1152, 576, 36),
    'requested-zero': (0, 0, 0, 576, 3, 1152, 576, 36),
    'resource-stop': (512, 0, 0, 576, 3, 1152, 576, 36),
}


def _a48c4_success_stable_identity(world, cell):
    """Objects which frozen template start mutates in place or not at all."""
    return {
        'rng': id(world.rng),
        'pools': id(cell.pools),
        'genomes': id(cell.genomes),
        'genome_items': tuple(id(value) for value in cell.genomes),
        'lesions': id(cell.genome_lesions),
        'events': id(cell.mutation_events),
        'proteins': id(cell.proteins),
        'damaged': id(cell.damaged_proteins),
        'gene_specs': id(cell.gene_specs),
        'gene_spec_items': tuple(
            (int(key), id(value)) for key, value in cell.gene_specs.items()
        ),
    }


def _a48c4_assert_start_oracle(expected_world, expected_cell,
                                actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c4.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or float(expected_world.dissipated_energy)
            != float(actual_world.dissipated_energy)
            or len(actual_cell.genomes) != 1
            or actual_cell.replication_template is None
            or len(actual_cell.replication_copy)
            >= len(actual_cell.replication_template)
            or not np.array_equal(
                actual_cell.replication_template,
                actual_cell.genomes[0],
            )
            or actual_cell.replication_template is actual_cell.genomes[0]
            or np.shares_memory(
                actual_cell.replication_template, actual_cell.genomes[0],
            )):
        raise AssertionError(label + ' start topology/PCG64 differs')


def _a48c4_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c4.Hybrid066WorldA4ReplicationStart(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48c4_world_for_case(case):
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(case['state']),
    )
    if bool(case.get('drop_lesions', False)):
        # Formal066 save restoration normalizes a lagging lesion vector.  This
        # live-only fixture deliberately reinstates the legal transient source
        # so the frozen selected-lesion fallback remains directly exercised.
        world.cells[0].genome_lesions = []
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
    return world


def _a48c4_cases(seed=12101):
    """Fixed isolated start rows, including all three zero-append modes."""
    if int(seed) != 12101:
        raise AssertionError('A4.8c4 fixed fixture seed must remain 12101')
    definitions = (
        ('row0-normal', 0, 0.5, {}),
        ('row2-missing-lesion', 2, 0.5, {}),
        ('dt-zero', 0, 0.0, {}),
        ('requested-zero', 0, 1e-12, {}),
        ('resource-stop', 0, 100.0, {
            int(a4.a3.POOL_ATP): 0.022,
            int(a4.a3.POOL_NUCLEOTIDE): 0.1,
        }),
    )
    cases = []
    for label, row, dt, pool_overrides in definitions:
        world, source_cells, _, _ = _a45b_template_start_fixture(
            seed=seed,
        )
        cell = copy.deepcopy(source_cells[row])
        for index, name in enumerate(tuple(cell.mutation_events)):
            cell.mutation_events[name] = (
                int(cell.mutation_events[name]) + 17 + index + row
            )
        cell.mutation_events['a48c4_extra_sentinel'] = 101 + row
        for pool_index, value in pool_overrides.items():
            cell.pools[int(pool_index)] = float(value)
        _a48c_reduce_world(world, [cell])
        state = v3.pickle_clone(world.state_dict())

        provisional = {'state': state, 'drop_lesions': row == 2}
        direct = _a48c4_world_for_case(provisional)
        before = copy.deepcopy(direct.cells[0])
        rng_before = copy.deepcopy(direct.rng.bit_generator.state)
        direct.cells[0]._replicate_genome(direct, dt, direct.config)
        after = direct.cells[0]
        capacity_values = _a48c2_capacity_for_transition(before, after)
        capacity = a4.GPU068A4Config(**capacity_values)

        source = _a48c4_world_for_case(provisional)
        ragged, _, binding = _paid_replication_binding(
            [source.cells[0]], source.config, capacity,
        )
        tape = a44.prepare_substitution_rng_tape(
            binding, dt, source.config, rng_before,
        )
        plan = a44.paid_replication_substitution_numpy(
            binding, dt, source.config, tape,
        )
        _assert_paid_replication_cpu_parity(
            plan, [before], [after], 'a48c4.fixture.' + label,
        )
        template_length = len(before.genomes[0])
        values = (
            int(plan.requested_symbols[0]),
            int(plan.append_count[0]),
            int(plan.substitution_events[0]),
            template_length,
            int(capacity.max_sequences),
            int(capacity.max_symbols),
            int(capacity.max_sequence_symbols),
            int(capacity.max_proteins_per_cell),
        )
        if (values != _A48C4_EXPECTED_CASES[label]
                or int(ragged.sequence_count) != 1
                or int(ragged.symbol_count) != template_length
                or not bool(plan.template_start_events[0])
                or bool(plan.completion_events[0])
                or int(plan.selected_template_indices[0]) != 0
                or int(plan.template_storage_symbols[0]) != template_length
                or int(plan.topology_sequence_deltas[0]) != 0
                or int(plan.topology_symbol_deltas[0]) != 0
                or tape.rng_before_state != rng_before
                or tape.rng_after_state
                != direct.rng.bit_generator.state):
            raise AssertionError(label + ' fixed start fixture drifted')
        if (label == 'row2-missing-lesion'
                and (before.genome_lesions
                     or float(after.replication_template_lesion) != 0.0)):
            raise AssertionError('A4.8c4 missing-lesion fallback drifted')
        if label in ('dt-zero', 'requested-zero', 'resource-stop'):
            if (int(plan.append_count[0]) != 0
                    or tape.rng_after_state != rng_before
                    or after.replication_template is None):
                raise AssertionError(label + ' zero-append start drifted')
        cases.append({
            'label': label,
            'state': state,
            'drop_lesions': row == 2,
            'cell_id': int(cell.cell_id),
            'dt': float(dt),
            'capacity': capacity,
            'capacity_values': capacity_values,
            'template_length': template_length,
            'requested': int(plan.requested_symbols[0]),
            'append': int(plan.append_count[0]),
            'substitution': int(plan.substitution_events[0]),
            'rng_call_count': (
                1 + int(plan.append_count[0])
                + int(plan.substitution_events[0])
            ),
            'rng_before_state': rng_before,
            'rng_after_state': copy.deepcopy(
                direct.rng.bit_generator.state,
            ),
            'tape_state': tape.state_dict(),
            'plan_state': plan.state_dict(),
            'mutation_before': tuple(before.mutation_events.items()),
            'mutation_after': tuple(after.mutation_events.items()),
        })
    return cases


def test_a48c4_inactive_start_noncompletion_atomic_commit_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c4')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveStart(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_candidate = candidate
            return candidate

    details = []
    for case in _a48c4_cases():
        devices = ['cpu']
        if (case['label'] in ('row0-normal', 'row2-missing-lesion')
                and torch.cuda.is_available()):
            devices.append('cuda')
        for device in devices:
            direct = _a48c4_world_for_case(case)
            direct_cell = direct.cells[0]
            direct_copy_before = direct_cell.replication_copy
            direct_stable = _a48c4_success_stable_identity(
                direct, direct_cell,
            )
            direct_cell._replicate_genome(
                direct, case['dt'], direct.config,
            )
            if (_a48c4_success_stable_identity(direct, direct_cell)
                    != direct_stable
                    or direct_cell.replication_copy is direct_copy_before):
                raise AssertionError(
                    case['label'] + ' frozen start identity drifted'
                )

            world = _a48c4_world_for_case(case)
            cell = world.cells[0]
            stable_before = _a48c4_success_stable_identity(world, cell)
            old_copy = cell.replication_copy
            old_template = cell.replication_template
            pools_before = np.asarray(cell.pools, np.float64).copy()
            scheduler = ObserveStart(case['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                candidate = scheduler.observed_candidate
                _a48c4_assert_start_oracle(
                    direct, direct_cell, world, cell,
                    'a48c4.oracle.%s.%s' % (device, case['label']),
                )
                plan = candidate.plan
                tape = candidate.tape
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                if (_a48c4_success_stable_identity(world, cell)
                        != stable_before
                        or old_template is not None
                        or cell.replication_copy is old_copy
                        or cell.replication_template
                        is candidate.candidate_cell.replication_template
                        or np.shares_memory(
                            cell.replication_template,
                            candidate.candidate_cell.replication_template,
                        )
                        or cell.replication_copy
                        is candidate.candidate_cell.replication_copy
                        or not np.array_equal(
                            cell.replication_template,
                            candidate.candidate_cell.replication_template,
                        )
                        or cell.replication_copy
                        != candidate.candidate_cell.replication_copy):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' success identity/nonalias differs'
                    )
                paid = {
                    int(a4.a3.POOL_NUCLEOTIDE), int(a4.a3.POOL_ATP),
                }
                unchanged = [
                    index for index in range(int(a4.a3.POOL_COUNT))
                    if index not in paid
                ]
                if (not np.array_equal(
                        np.asarray(cell.pools[unchanged], np.float64).view(
                            np.uint64),
                        pools_before[unchanged].view(np.uint64),
                        )
                        or tuple(cell.mutation_events.items())
                        != case['mutation_after']
                        or tuple(candidate.candidate_cell.mutation_events.items())
                        != case['mutation_after']
                        or not candidate.consumed
                        or int(plan.requested_symbols[0]) != case['requested']
                        or int(plan.append_count[0]) != case['append']
                        or int(plan.substitution_events[0])
                        != case['substitution']
                        or int(plan.template_storage_symbols[0])
                        != case['template_length']
                        or tape.rng_before_state
                        != case['rng_before_state']
                        or tape.rng_after_state
                        != case['rng_after_state']
                        or metadata.get('authority') != (
                            'A4.8c4-resident-template-start-plan-atomic-'
                            'cpu-cell-pcg64-commit'
                        )
                        or metadata.get('branch')
                        != 'inactive-template-start-substitution'
                        or metadata.get('device') != device
                        or metadata.get('mutation_enabled') is not True
                        or int(metadata.get('amount', -1)) != case['append']
                        or int(metadata.get('requested_symbols', -1))
                        != case['requested']
                        or int(metadata.get('substitution_events', -1))
                        != case['substitution']
                        or int(metadata.get('rng_call_count', -1))
                        != case['rng_call_count']
                        or metadata.get('work_performed') is not True):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' plan/tape/receipt/ledger differs'
                    )
                fresh = candidate.fresh_binding
                if (int(fresh.ragged.sequence_count) != 3
                        or int(fresh.ragged.symbol_count)
                        != case['capacity_values']['max_symbols']
                        or int(fresh.ragged.genome_counts[0]) != 1
                        or not bool(fresh.ragged.replication_active[0])
                        or int(fresh.state.genome_count[0]) != 1):
                    raise AssertionError(
                        case['label'] + ' fresh Q/S/genome topology differs'
                    )
                details.append('%s:%s:%d/%d/%d' % (
                    device, case['label'], case['requested'],
                    case['append'], case['substitution'],
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 selection->append, new template/copy nonalias, lesion/'
            'fallback, paid pools, additive ledger, full PCG64, Q+2/S+T+A; '
            'dt0/requested0/resource-stop remain work=true: '
            + ', '.join(details))


def test_a48c4_candidate_cpu_cuda_purity_rng_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c4')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    case = _a48c4_cases()[0]
    reference_plan = None
    reference_state = None
    reference_rng = None
    details = []
    for device in devices:
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(case['state']),
        )
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(case['state']),
        )
        cell = world.cells[0]
        old_copy = cell.replication_copy
        scheduler = a48c4.A4ReplicationStartEventScheduler(
            case['capacity'], device,
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, case['dt'],
        )
        before_world = v3.pickle_clone(world.state_dict())
        before_ids = _a48c_identity_snapshot(world, cell)
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = scheduler._prepare_replication_start_candidate(
                world, cell, case['dt'], world.config,
            )
            v3.assert_recursive_close(
                before_world, world.state_dict(), 0.0, 0.0,
                'a48c4.prepare_purity.' + device,
            )
            _, record = scheduler._record(cell)
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng
                    or float(world.dissipated_energy) != before_energy
                    or _a48c_identity_snapshot(world, cell) != before_ids
                    or any(entry['event'] == 'replication_cpu'
                           for entry in record['events'])):
                raise AssertionError(device + ' prepare crossed claim/purity')
            prepared = candidate.candidate_cell
            if (prepared is cell
                    or prepared.pools is cell.pools
                    or prepared.genomes is cell.genomes
                    or prepared.replication_template is None
                    or prepared.replication_template is prepared.genomes[0]
                    or np.shares_memory(
                        prepared.replication_template, prepared.genomes[0],
                    )
                    or prepared.replication_copy is old_copy
                    or prepared.mutation_events is cell.mutation_events
                    or candidate.source_binding is candidate.fresh_binding
                    or candidate.plan is candidate.host_replay
                    or candidate.plan is candidate.resident_plan
                    or candidate.tape is candidate.resident_tape
                    or candidate.live_rng is not world.rng
                    or candidate.rng_before_state != before_rng
                    or candidate.rng_after_state
                    != candidate.tape.rng_after_state):
                raise AssertionError(device + ' candidate authority/alias differs')
            if (a48c4._resident_start_artifacts(
                    candidate.resident_binding, candidate.resident_tape,
                    candidate.resident_plan, device,
                    ) != candidate.resident_artifacts
                    or a48c4._host_start_artifacts(
                        candidate.tape, candidate.host_replay, candidate.plan,
                    ) != candidate.host_artifacts
                    or not a48._state_arrays_bit_exact(
                        candidate.tape.state_dict(), case['tape_state'],
                    )
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_tape.to_numpy().state_dict(),
                        candidate.tape.state_dict(),
                    )
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_plan.to_numpy().state_dict(),
                        candidate.plan.state_dict(),
                    )):
                raise AssertionError(device + ' host/resident association differs')
            a48c._plan_semantic_match(
                candidate.plan, candidate.host_replay,
            )
            a48c4._start_plan_scope(
                candidate.source_binding, candidate.tape, candidate.plan,
                case['dt'], world.config,
            )
            _a48b_assert_cell_matches(
                direct.cells[0], prepared,
                'a48c4.candidate.' + device,
                atol=a48c4.REPLICATION_ORACLE_ATOL,
            )
            a4._require_translation_binding(candidate.fresh_binding)
            if (not isinstance(
                    candidate.fresh_binding.ragged.symbols, np.ndarray)
                    or not isinstance(
                        candidate.fresh_binding.cache.fingerprints,
                        np.ndarray,
                    )
                    or np.shares_memory(
                        candidate.fresh_binding.ragged.symbols,
                        candidate.source_binding.ragged.symbols,
                    )):
                raise AssertionError(device + ' fresh binding/cache differs')

            private_template = prepared.replication_template
            private_copy = prepared.replication_copy
            scheduler._commit_replication_start_candidate(
                world, cell, case['dt'], world.config, candidate,
            )
            _a48c4_assert_start_oracle(
                direct, direct.cells[0], world, cell,
                'a48c4.commit.' + device,
            )
            if (cell.replication_template is private_template
                    or np.shares_memory(
                        cell.replication_template, private_template,
                    )
                    or cell.replication_copy is private_copy
                    or not np.array_equal(
                        cell.replication_template, private_template,
                    )
                    or cell.replication_copy != private_copy):
                raise AssertionError(device + ' publish aliases private candidate')
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48c4.A4ReplicationStartCommitError,
                lambda: scheduler._commit_replication_start_candidate(
                    world, cell, case['dt'], world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48c4.one_shot.' + device,
            )
            if (not candidate.consumed
                    or world.rng is not before_rng_object
                    or world.rng.bit_generator.state != committed_rng):
                raise AssertionError(device + ' one-shot/live Generator differs')
            if reference_plan is None:
                reference_plan = candidate.plan.clone()
                reference_state = v3.pickle_clone(cell.state_dict())
                reference_rng = copy.deepcopy(committed_rng)
            else:
                a48c._plan_semantic_match(candidate.plan, reference_plan)
                v3.assert_recursive_close(
                    reference_state, cell.state_dict(),
                    a48c4.REPLICATION_ORACLE_ATOL, 0.0,
                    'a48c4.cpu_cuda_state',
                )
                if committed_rng != reference_rng:
                    raise AssertionError('CPU/CUDA committed PCG64 differs')
            pointer_count = sum(
                len(values) for values in
                candidate.resident_artifacts['data_ptrs'].values()
            )
            details.append('%s:%d-pointers' % (device, pointer_count))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('CPU/CUDA prepare purity, resident device/pointer/version/content, '
            'host tape/NumPy oracle/readback, deep candidate/fresh cache, '
            'private/live nonalias, same live PCG64 and one-shot PASS: '
            + '/'.join(details))


def _a48c4_assert_preclaim_failure(state, cell_id, dt, capacity,
                                    scheduler_type=None, device='cpu',
                                    label='scope'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(
        item for item in world.cells if int(item.cell_id) == int(cell_id)
    )
    scheduler_type = scheduler_type or a48c4.A4ReplicationStartEventScheduler
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(world.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng_object = world.rng
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    before_receipts = len(scheduler._receipts)
    try:
        _assert_raises(
            (a48c4.A4ReplicationStartCommitError,
             a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, dt, world.config,
            ),
        )
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c4.preclaim.' + label,
        )
        _, record = scheduler._record(cell)
        if (world.rng is not before_rng_object
                or world.rng.bit_generator.state != before_rng
                or float(world.dissipated_energy) != before_energy
                or _a48c_identity_snapshot(world, cell) != before_ids
                or len(scheduler._receipts) != before_receipts
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c4_capacity_trust_rollback_scope_and_exact_once():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c4 trust tests')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    resident_devices = ['cpu']
    if torch.cuda.is_available():
        resident_devices.append('cuda')
    base = _a48c4_cases()[0]

    # Q/S are transition capacity; W/P bind the complete source and cache.
    capacity_details = []
    for label, key in (
            ('Q', 'max_sequences'),
            ('S', 'max_symbols'),
            ('W', 'max_sequence_symbols'),
            ('P', 'max_proteins_per_cell')):
        exact = a4.GPU068A4Config(**base['capacity_values'])
        direct = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(base['state']),
        )
        direct.cells[0]._replicate_genome(
            direct, base['dt'], direct.config,
        )
        for device in resident_devices:
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(base['state']),
            )
            cell = world.cells[0]
            scheduler = a48c4.A4ReplicationStartEventScheduler(
                exact, device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, base['dt'], world.config,
                )
                _a48c4_assert_start_oracle(
                    direct, direct.cells[0], world, cell,
                    'a48c4.capacity.%s.%s' % (label, device),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append('%s@%s' % (label, device))
        short = dict(base['capacity_values'])
        short[key] -= 1
        _a48c4_assert_preclaim_failure(
            base['state'], base['cell_id'], base['dt'],
            a4.GPU068A4Config(**short),
            label='capacity_' + label + '_one_short',
        )
    del scheduler, world, cell, direct
    gc.collect()

    trust_details = []

    def assert_tamper_failure(scheduler_type, label, devices=('cpu',),
                              require_isolation=False):
        for device in devices:
            world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(base['state']),
            )
            cell = world.cells[0]
            scheduler = scheduler_type(base['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            before_world = v3.pickle_clone(world.state_dict())
            before_objects = _a48c_identity_snapshot(world, cell)
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_config_object = id(world.config)
            before_receipts = len(scheduler._receipts)
            try:
                _assert_raises(
                    (a48c4.A4ReplicationStartCommitError,
                     a4.A4Error, a3s.A3SchedulerError),
                    lambda: scheduler.cpu_replication(
                        world, cell, base['dt'], world.config,
                    ),
                )
                expected_world = getattr(
                    scheduler, 'expected_world_state', before_world,
                )
                expected_rng = getattr(
                    scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, world.state_dict(), 0.0, 0.0,
                    'a48c4.trust.%s.%s' % (device, label),
                )
                _, record = scheduler._record(cell)
                if (world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(world, cell)
                        != before_objects
                        or id(world.config) != before_config_object
                        or len(scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim' % (device, label)
                    )
                if (require_isolation
                        and not bool(getattr(scheduler, 'isolated', False))):
                    raise AssertionError(label + ' retained alias was not isolated')
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
            del scheduler, cell, world
            gc.collect()

    class TamperPackedSource(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.source_binding.ragged.symbols
            self.isolated = (
                retained is not cell.genomes[0]
                and not np.shares_memory(retained, cell.genomes[0])
            )
            retained[0] ^= np.uint8(1)
            return candidate

    class TamperSourceLesion(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.source_binding.ragged.genome_lesions[0] = np.nextafter(
                candidate.source_binding.ragged.genome_lesions[0], np.inf,
            )
            return candidate

    class TamperHostTape(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.tape.template_start_mask[0] = False
            candidate.tape.template_selection_indices[0] = -1
            return candidate

    class TamperHostOracle(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.template_storage_symbols[0] += 1
            return candidate

    class TamperReadbackPlan(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.template_start_events[0] = False
            return candidate

    class TamperResidentRaggedData(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentStateData(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_NUCLEOTIDE
            ].add_(1e-8)
            return candidate

    class TamperResidentCacheData(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class TamperResidentTapeData(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_tape.template_selection_indices.data[0].add_(1)
            return candidate

    class TamperResidentPlanData(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.template_storage_symbols.data[0].add_(1)
            return candidate

    class SwapResidentTensorPointer(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.template_storage_symbols = (
                candidate.resident_plan.template_storage_symbols.clone()
            )
            return candidate

    class SwapResidentWholeMember(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'ragged',
                candidate.resident_binding.ragged.clone(),
            )
            return candidate

    class TamperResidentPrivateSnapshot(
            a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_artifacts['data_ptrs']['plan'][
                'template_storage_symbols'
            ] += 1
            return candidate

    class TamperHostPrivateSnapshot(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_artifacts['plan']['data_ptrs'][
                'template_storage_symbols'
            ] += 1
            return candidate

    class MoveResidentTensorDevice(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            tensor = candidate.resident_binding.ragged.symbols
            target = 'cuda' if tensor.device.type == 'cpu' else 'cpu'
            candidate.resident_binding.ragged.symbols = tensor.to(target)
            return candidate

    class TamperCandidateTemplate(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_template[0] ^= np.uint8(1)
            return candidate

    class AliasCandidateTemplate(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_template = (
                candidate.candidate_cell.genomes[0]
            )
            return candidate

    class SwapCandidateCopyOuter(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_copy = list(
                candidate.candidate_cell.replication_copy
            )
            return candidate

    class MutateUnpackedCandidate(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.candidate_cell.last_repair_flux
            self.isolated = (
                retained is not cell.last_repair_flux
                and not np.shares_memory(retained, cell.last_repair_flux)
            )
            retained[0] += 0.125
            return candidate

    class TamperFreshBinding(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class TamperSchedulerCapacityDevice(
            a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = 'cuda' if self.a4_device == 'cpu' else 'cpu'
            return candidate

    class TamperLiveConfig(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                float(world.config.mutation_rate), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveSource(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.genomes[0][0] ^= np.uint8(1)
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveRng(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperCandidateRng(a48c4.A4ReplicationStartEventScheduler):
        def _replication_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_state,
            )
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    for scheduler_type, label, isolation in (
            (TamperPackedSource, 'packed_source', True),
            (TamperSourceLesion, 'source_lesion', False),
            (TamperHostTape, 'host_tape_start_schedule', False),
            (TamperHostOracle, 'host_oracle_start_topology', False),
            (TamperReadbackPlan, 'readback_plan_start_flag', False),
            (SwapResidentTensorPointer, 'resident_pointer', False),
            (SwapResidentWholeMember, 'resident_whole_member', False),
            (TamperResidentPrivateSnapshot, 'resident_private', False),
            (TamperHostPrivateSnapshot, 'host_private', False),
            (TamperCandidateTemplate, 'candidate_template', False),
            (AliasCandidateTemplate, 'candidate_template_alias', False),
            (SwapCandidateCopyOuter, 'candidate_copy_outer', False),
            (MutateUnpackedCandidate, 'candidate_deep_array', True),
            (TamperFreshBinding, 'fresh_binding', False),
            (TamperSchedulerCapacityDevice, 'scheduler_config_device', False),
            (TamperLiveConfig, 'live_config', False),
            (TamperLiveSource, 'live_source_genome', False),
            (TamperLiveRng, 'live_rng', False),
            (TamperCandidateRng, 'candidate_rng', False)):
        assert_tamper_failure(
            scheduler_type, label, require_isolation=isolation,
        )
    for scheduler_type, label in (
            (TamperResidentRaggedData, 'resident_ragged_data'),
            (TamperResidentStateData, 'resident_state_data'),
            (TamperResidentCacheData, 'resident_cache_data'),
            (TamperResidentTapeData, 'resident_tape_data'),
            (TamperResidentPlanData, 'resident_plan_data')):
        assert_tamper_failure(
            scheduler_type, label, devices=resident_devices,
        )
    if torch.cuda.is_available():
        assert_tamper_failure(
            MoveResidentTensorDevice, 'resident_device',
            devices=resident_devices,
        )

    # Wrong arguments must not consume a private candidate or claim the event.
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    cell = world.cells[0]
    scheduler = a48c4.A4ReplicationStartEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, cell, base['dt'])
    before = v3.pickle_clone(world.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = scheduler._prepare_replication_start_candidate(
            world, cell, base['dt'], world.config,
        )
        wrong_inputs = (
            ('cell', copy.deepcopy(cell), base['dt'], world.config),
            ('dt', cell, np.nextafter(base['dt'], np.inf), world.config),
            ('config', cell, base['dt'], copy.deepcopy(world.config)),
        )
        for label, wrong_cell, wrong_dt, wrong_config in wrong_inputs:
            _assert_raises(
                a48c4.A4ReplicationStartCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler._commit_replication_start_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError('wrong ' + label + ' consumed candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c4.wrong_cell_dt_config',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong inputs changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)
    del candidate, scheduler, world, cell
    gc.collect()

    # Unsupported inactive and pre-active branches fail without CPU fallback.
    scope_cases = []

    def add_scope(label, mutate, dt=base['dt']):
        scoped = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(base['state']),
        )
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scoped.last_step_material_residual = 0.0
        scope_cases.append((
            label, v3.pickle_clone(scoped.state_dict()),
            int(scoped_cell.cell_id), float(dt),
        ))

    add_scope('mutation_off', lambda world, cell: setattr(
        world.config, 'mutation', False))
    add_scope('disabled', lambda world, cell: setattr(
        world.config, 'genome_replication', False))
    add_scope('negative_atp', lambda world, cell: cell.pools.__setitem__(
        a4.a3.POOL_ATP, -1e-12))

    def make_no_genome(world, cell):
        cell.genomes = []
        cell.genome_lesions = []
        cell.replication_template = None
        cell.replication_copy = []
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = 0.0
        cell._refresh_gene_cache()

    add_scope('no_genome', make_no_genome)

    def make_two_genomes(world, cell):
        cell.genomes = [
            np.asarray(cell.genomes[0], np.uint8).copy(),
            np.asarray(cell.genomes[0], np.uint8).copy(),
        ]
        cell.genome_lesions = [0.1, 0.2]
        cell._refresh_gene_cache()

    add_scope('two_genomes', make_two_genomes)

    def remove_replicase(world, cell):
        world.config.external_replicase = False
        for fingerprint, spec in list(cell.gene_specs.items()):
            if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
                cell.proteins.pop(fingerprint, None)
        cell._sync_protein_pool()

    add_scope('replicase_gate', remove_replicase)

    def make_start_completion(world, cell):
        cell.genomes = [np.asarray((3,), dtype=np.uint8)]
        cell.genome_lesions = [0.2]
        cell.replication_template = None
        cell.replication_copy = []
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = 0.0
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
        cell.pools[a4.a3.POOL_ATP] = 0.75
        cell._refresh_gene_cache()

    add_scope('same_call_completion', make_start_completion, dt=100.0)
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for label, state, cell_id, dt in scope_cases:
            _a48c4_assert_preclaim_failure(
                state, cell_id, dt, a4.GPU068A4Config(),
                label='scope_' + label,
            )
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('excluded c4 scope used CPU fallback')

    # Duplicate must be detected before the successful start is reclassified
    # as an inherited active event; order failure stays preclaim atomic.
    duplicate = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = a48c4.A4ReplicationStartEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, base['dt'],
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, base['dt'], duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, base['dt'], duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate.state_dict(), 0.0, 0.0,
        'a48c4.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate start changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    order_cell = order.cells[0]
    order_scheduler = a48c4.A4ReplicationStartEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, base['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, base['dt'], order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order.state_dict(), 0.0, 0.0,
        'a48c4.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order start changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(a48c4.A4ReplicationStartEventScheduler):
        def _publish_replication_start_candidate(
                self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_start_candidate(
                world, cell, candidate,
            )
            if cell.replication_template is None:
                raise AssertionError('publish seam ran before template creation')
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.replication_template[0] ^= np.uint8(1)
            cell.replication_copy.append(7)
            cell.replication_copy = list(cell.replication_copy)
            cell.replication_template_lesion += 0.25
            cell.replication_fractional += 0.5
            cell.last_replication_symbols += 2
            cell.last_effective_error_rate += 0.125
            cell.cumulative_proofreading_atp += 0.5
            cell.mutation_events = copy.deepcopy(cell.mutation_events)
            cell.mutation_events['a48c4_extra_sentinel'] += 1
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c4 publish failure')

    rollback = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(base['state']),
    )
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(base['capacity'], 'cpu')
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, base['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    old_copy_object = rollback_cell.replication_copy
    old_pools_object = rollback_cell.pools
    old_events_object = rollback_cell.mutation_events
    rollback_rng_object = rollback.rng
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, base['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c4 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c4.publish_rollback',
    )
    if (rollback_cell.replication_template is not None
            or rollback_cell.replication_copy is not old_copy_object
            or rollback_cell.replication_copy != []
            or rollback_cell.pools is not old_pools_object
            or rollback_cell.mutation_events is not old_events_object
            or rollback.rng is not rollback_rng_object
            or rollback.rng.bit_generator.state != rollback_rng
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids):
        raise AssertionError(
            'None-aware start rollback lost source identity/state'
        )
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if entry['event'] != 'replication_cpu' or receipt['status'] != 'aborted':
        raise AssertionError('publish failure did not leave aborted receipt')
    return ('Q/S/W/P exact CPU/CUDA + each one-short; %d source/tape/plan/'
            'pointer/device/.data/candidate/fresh/live trust checks; wrong '
            'inputs; %d no-fallback scopes; duplicate-before-reclass/order; '
            'None-template + old-copy/pools/ledger/RNG identity rollback PASS '
            '(%s)' % (
                len(trust_details), len(scope_cases),
                '/'.join(capacity_details),
            ))


def test_a48c4_active_delegate_save_clone_successor_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c4')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    delegated_cases = []
    c1_world, c1_cells, _, c1_dt = _a45_substitution_fixture(seed=12201)
    _a48c_reduce_world(c1_world, [c1_cells[0]])
    delegated_cases.append({
        'label': 'c1-active-noncompletion',
        'state': v3.pickle_clone(c1_world.state_dict()),
        'dt': float(c1_dt),
        'capacity': a4.GPU068A4Config(),
        'authority': 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
    })

    c2_state, _, c2_dt = _a48c2_completion_case(seed=12202, row=0)
    c2_before_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_before = copy.deepcopy(c2_before_world.cells[0])
    c2_after_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after_world.cells[0]._replicate_genome(
        c2_after_world, c2_dt, c2_after_world.config,
    )
    c2_capacity = a4.GPU068A4Config(**_a48c2_capacity_for_transition(
        c2_before, c2_after_world.cells[0],
    ))
    delegated_cases.append({
        'label': 'c2-mutation-free-completion',
        'state': c2_state, 'dt': float(c2_dt),
        'capacity': c2_capacity,
        'authority': (
            'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit'
        ),
    })

    c3_case = _a48c3_completion_mutation_cases()[0]
    delegated_cases.append({
        'label': 'c3-mutation-completion',
        'state': c3_case['state'], 'dt': float(c3_case['dt']),
        'capacity': c3_case['integration_capacity'],
        'authority': (
            'A4.8c3-resident-completion-mutation-plan-atomic-'
            'cpu-cell-rng-commit'
        ),
    })

    delegated = []
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in delegated_cases:
            for device in devices:
                direct = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(case['state']),
                )
                world = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(case['state']),
                )
                direct.cells[0]._replicate_genome(
                    direct, case['dt'], direct.config,
                )
                scheduler = a48c4.A4ReplicationStartEventScheduler(
                    case['capacity'], device,
                )
                _a48c_begin_replication_direct(
                    scheduler, world, world.cells[0], case['dt'],
                )
                try:
                    scheduler.cpu_replication(
                        world, world.cells[0], case['dt'], world.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], world.cells[0],
                        'a48c4.delegate.%s.%s' % (
                            device, case['label'],
                        ),
                        atol=a48c4.REPLICATION_ORACLE_ATOL,
                    )
                    entry = _a48c_replication_entry(
                        scheduler, world.cells[0],
                    )
                    if (direct.rng.bit_generator.state
                            != world.rng.bit_generator.state
                            or float(direct.dissipated_energy)
                            != float(world.dissipated_energy)
                            or entry['metadata'].get('authority')
                            != case['authority']):
                        raise AssertionError(
                            device + '/' + case['label']
                            + ' inherited authority differs'
                        )
                    delegated.append(device + ':' + case['label'])
                finally:
                    _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('an active delegate used frozen CPU fallback')

    # The owned inactive branch must not dispatch to c3 merely because c3 is
    # the superclass; it claims c4 directly.
    own = _a48c4_cases()[0]
    direct = _a48c4_world_for_case(own)
    direct.cells[0]._replicate_genome(direct, own['dt'], direct.config)
    world = _a48c4_world_for_case(own)
    scheduler = a48c4.A4ReplicationStartEventScheduler(
        own['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, world.cells[0], own['dt'])
    original_c3 = (
        a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication
    )

    def c3_dispatch_bomb(*args, **kwargs):
        raise AssertionError('owned c4 start delegated to c3')

    a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication = (
        c3_dispatch_bomb
    )
    try:
        scheduler.cpu_replication(
            world, world.cells[0], own['dt'], world.config,
        )
        _a48c4_assert_start_oracle(
            direct, direct.cells[0], world, world.cells[0],
            'a48c4.owned_no_c3_dispatch',
        )
    finally:
        a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication = (
            original_c3
        )
        _a48_abort_direct(scheduler, world.cells[0])

    # Full world-step successor freezes event order and c4 persistence.  Use
    # room beyond the fixed transition capacity because preceding metabolism
    # and translation are intentionally live in this integrated check.
    roomy = a4.GPU068A4Config(
        max_cells=1, max_sequences=4, max_symbols=2048,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    successor_details = []
    persisted = None
    persisted_cpu = None
    for device in devices:
        cpu = _a48c4_world_for_case(own)
        hybrid = _a48c4_hybrid_from_state(own['state'], roomy, device)
        cpu.step(own['dt'])
        original_c3 = (
            a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication
        )
        a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication = (
            c3_dispatch_bomb
        )
        try:
            receipt = hybrid.step(own['dt'])
        finally:
            a48c3.A4ReplicationCompletionMutationEventScheduler.cpu_replication = (
                original_c3
            )
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c4.successor.' + device,
        )
        record = receipt['cells'][0]
        names = [entry['event'] for entry in record['events']]
        by_name = {entry['event']: entry for entry in record['events']}
        if (names.count('replication_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1
                or not names.index('translation_cpu')
                < names.index('replication_cpu')
                < names.index('surface_assembly')
                < names.index('genome_hydrolysis_cpu_rng')
                < names.index('motion')
                or by_name['replication_cpu']['metadata'].get('authority')
                != ('A4.8c4-resident-template-start-plan-atomic-'
                    'cpu-cell-pcg64-commit')
                or by_name['replication_cpu']['metadata'].get(
                    'work_performed') is not True):
            raise AssertionError(device + ' c4 successor order differs')
        successor_details.append(device)
        if device == 'cpu':
            persisted = hybrid
            persisted_cpu = cpu

    twin = persisted.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c4.pkl')
        persisted.save(path)
        restored = a48c4.Hybrid066WorldA4ReplicationStart.load(path)
        for value in (persisted, twin, restored):
            if (type(value) is not a48c4.Hybrid066WorldA4ReplicationStart
                    or type(value.scheduler)
                    is not a48c4.A4ReplicationStartEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != roomy
                    or value.scheduler.replication_start_integration_state()
                    != {
                        'schema': a48c4.SCHEMA_VERSION,
                        'config': a48._config_state(roomy),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c4 save/clone authority was lost')
        expected = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(persisted_cpu.state_dict()),
        )
        expected.step(own['dt'])
        for value in (persisted, twin, restored):
            value.step(own['dt'])
            v3._assert_world_pair(
                expected, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c4.persisted_successor',
            )

    active = _a48c4_hybrid_from_state(own['state'], roomy, 'cpu')
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active',
            '_a4_replication_start_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    expected_status = (
        'a4.8c4-mutation-enabled-inactive-one-genome-template-start-'
        'noncompletion-resident-plan-cpu-cell-pcg64-atomic-commit-'
        'a4.8c3-c2-c1-active-branches-inherited-other-inactive-'
        'early-noop-fail-closed'
    )
    signature = inspect.signature(
        a48c4.A4ReplicationStartEventScheduler.cpu_replication,
    )
    prior_names = '\n'.join(fn.__name__ for fn in TESTS[:66])
    c4_names = tuple(fn.__name__ for fn in TESTS[66:70])
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationStartCommitCandidate' in set(a48c4.__all__)
            or not issubclass(
                a48c4.A4ReplicationStartEventScheduler,
                a48c3.A4ReplicationCompletionMutationEventScheduler,
            )
            or not issubclass(
                a48c4.Hybrid066WorldA4ReplicationStart,
                a48c3.Hybrid066WorldA4ReplicationCompletionMutation,
            )
            or a48c4.PORT_STATUS.get('genome_replication') != expected_status
            or a48c4.FULL_GPU_WORLD_STEP is not False
            or a48c4.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) < 70
            or c4_names != (
                'test_a48c4_inactive_start_noncompletion_atomic_commit_formal066_oracle',
                'test_a48c4_candidate_cpu_cuda_purity_rng_and_fresh_binding',
                'test_a48c4_capacity_trust_rollback_scope_and_exact_once',
                'test_a48c4_active_delegate_save_clone_successor_and_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A48C4_TEST_NAMES_SHA256):
        raise AssertionError('A4.8c4 API/scope/66-test authority differs')
    expected_hashes = dict(PROMOTED_A3_SHA256)
    expected_hashes.update(_A48C4_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_start_integration.py'
    ] = _A48C4_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('active c1/c2/c3 CPU/CUDA delegates %s; owned start no c3/'
            'frozen fallback; save/load/clone + c1/c2/c3/c4 active reject; '
            'translation<replication<surface<hydrolysis<motion successor %s; '
            'first66/A3/A4.5b/c1/c2/c3/c4 hash/full_gpu=false PASS' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A48C5_TEST_NAMES_SHA256 = (
    '707b82f408f3de2cee2acc925126c195ad74316b7dd9ffd5845d107880a5f355'
)

_A48C5_PRIOR_CORE_SHA256 = dict(_A48C4_PRIOR_CORE_SHA256)
_A48C5_PRIOR_CORE_SHA256[
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_integration.py'
] = _A48C4_CORE_SHA256

# Filled from the final core bytes before canonical validator insertion.
_A48C5_CORE_SHA256 = 'e433e7f051cd265bcd2350c396f8aa1365905197e815b101767d12aa1be625ec'

_A48C5_EXPECTED_CASES = {
    'normal-unprimed': (18, 18, 576, 3, 1170, 576, 36),
    'normal-primed': (18, 18, 576, 3, 1170, 576, 36),
    'missing-lesion-unprimed': (5, 1, 496, 3, 993, 496, 31),
    'missing-lesion-primed': (5, 1, 496, 3, 993, 496, 31),
    'dt-zero': (0, 0, 576, 3, 1152, 576, 36),
    'requested-zero': (0, 0, 576, 3, 1152, 576, 36),
    'resource-stop': (512, 0, 576, 3, 1152, 576, 36),
}


class _A48C5TrackingGenerator(np.random.Generator):
    """A real Generator which records the frozen high-level API calls."""

    def __init__(self, state):
        super(_A48C5TrackingGenerator, self).__init__(np.random.PCG64())
        self.bit_generator.state = copy.deepcopy(state)
        self.calls = []

    def integers(self, *args, **kwargs):
        self.calls.append(('integers', tuple(args), dict(kwargs)))
        return super(_A48C5TrackingGenerator, self).integers(*args, **kwargs)

    def random(self, *args, **kwargs):
        self.calls.append(('random', tuple(args), dict(kwargs)))
        return super(_A48C5TrackingGenerator, self).random(*args, **kwargs)


class _A48C5TrackingMutationLedger(dict):
    """Live-only mapping which detects even same-value ledger writes."""

    def __init__(self, values):
        dict.__init__(self, values)
        self.writes = []

    def __setitem__(self, key, value):
        self.writes.append(('set', key, value))
        return dict.__setitem__(self, key, value)

    def __delitem__(self, key):
        self.writes.append(('delete', key))
        return dict.__delitem__(self, key)

    def clear(self):
        self.writes.append(('clear',))
        return dict.clear(self)

    def pop(self, key, *default):
        self.writes.append(('pop', key))
        return dict.pop(self, key, *default)

    def popitem(self):
        self.writes.append(('popitem',))
        return dict.popitem(self)

    def setdefault(self, key, default=None):
        self.writes.append(('setdefault', key, default))
        return dict.setdefault(self, key, default)

    def update(self, *args, **kwargs):
        self.writes.append(('update',))
        return dict.update(self, *args, **kwargs)

    def __deepcopy__(self, memo):
        values = copy.deepcopy(dict(self), memo)
        result = _A48C5TrackingMutationLedger(values)
        memo[id(self)] = result
        return result


def _a48c5_tracking_ledger(cell):
    ledger = _A48C5TrackingMutationLedger(cell.mutation_events)
    cell.mutation_events = ledger
    return ledger


def _a48c5_world_for_case(case, track_rng=False, track_ledger=False):
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(case['state']),
    )
    if bool(case.get('drop_lesions', False)):
        world.cells[0].genome_lesions = []
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
    if track_rng:
        world.rng = _A48C5TrackingGenerator(
            world.rng.bit_generator.state,
        )
    if track_ledger:
        _a48c5_tracking_ledger(world.cells[0])
    return world


def _a48c5_cases(seed=12301):
    """Mutation-free starts with both PCG64 cache states and zero work."""
    if int(seed) != 12301:
        raise AssertionError('A4.8c5 fixed fixture seed must remain 12301')
    c4_cases = {case['label']: case for case in _a48c4_cases()}
    definitions = (
        ('normal-unprimed', 'row0-normal', False),
        ('normal-primed', 'row0-normal', True),
        ('missing-lesion-unprimed', 'row2-missing-lesion', False),
        ('missing-lesion-primed', 'row2-missing-lesion', True),
        ('dt-zero', 'dt-zero', False),
        ('requested-zero', 'requested-zero', True),
        ('resource-stop', 'resource-stop', False),
    )
    cases = []
    for offset, (label, source_label, primed) in enumerate(definitions):
        source_case = c4_cases[source_label]
        world = _a48c4_world_for_case(source_case)
        world.config.mutation = False
        world.rng = np.random.Generator(np.random.PCG64(seed + offset))
        if primed:
            world.rng.integers(0, int(a4.g2.ALPHABET_SIZE))
        rng_state = copy.deepcopy(world.rng.bit_generator.state)
        if int(rng_state['has_uint32']) != int(bool(primed)):
            raise AssertionError(label + ' PCG64 cache priming drifted')
        cell = world.cells[0]
        cell.mutation_events['a48c5_extra_sentinel'] = 301 + offset
        state = v3.pickle_clone(world.state_dict())
        provisional = {
            'state': state,
            'drop_lesions': bool(source_case.get('drop_lesions', False)),
        }
        direct = _a48c5_world_for_case(provisional, track_rng=True)
        before = copy.deepcopy(direct.cells[0])
        direct_ledger = _a48c5_tracking_ledger(direct.cells[0])
        direct.cells[0]._replicate_genome(
            direct, source_case['dt'], direct.config,
        )
        after = direct.cells[0]
        if (direct.rng.calls != [('integers', (0, 1), {})]
                or direct.rng.bit_generator.state != rng_state
                or direct_ledger.writes
                or tuple(direct_ledger.items())
                != tuple(before.mutation_events.items())):
            raise AssertionError(label + ' frozen selection/ledger drifted')
        capacity_values = _a48c2_capacity_for_transition(before, after)
        capacity = a4.GPU068A4Config(**capacity_values)

        source = _a48c5_world_for_case(provisional)
        _, _, binding = _paid_replication_binding(
            [source.cells[0]], source.config, capacity,
        )
        plan = a44._paid_replication_elongation_numpy(
            binding, source_case['dt'], source.config,
            allow_template_start=True,
        )
        _assert_paid_replication_cpu_parity(
            plan, [before], [after], 'a48c5.fixture.' + label,
        )
        template_length = len(before.genomes[0])
        values = (
            int(plan.requested_symbols[0]),
            int(plan.append_count[0]),
            template_length,
            int(capacity.max_sequences), int(capacity.max_symbols),
            int(capacity.max_sequence_symbols),
            int(capacity.max_proteins_per_cell),
        )
        if (values != _A48C5_EXPECTED_CASES[label]
                or not bool(plan.template_start_events[0])
                or bool(plan.completion_events[0])
                or int(plan.selected_template_indices[0]) != 0
                or int(plan.substitution_events[0]) != 0
                or float(plan.last_effective_error_rate[0]) <= 0.0):
            raise AssertionError(label + ' deterministic start plan drifted')
        cases.append({
            'label': label, 'state': state,
            'drop_lesions': provisional['drop_lesions'],
            'cell_id': int(cell.cell_id),
            'dt': float(source_case['dt']),
            'capacity': capacity,
            'capacity_values': capacity_values,
            'template_length': template_length,
            'requested': int(plan.requested_symbols[0]),
            'append': int(plan.append_count[0]),
            'effective_error': float(plan.last_effective_error_rate[0]),
            'rng_before_state': rng_state,
            'rng_after_state': copy.deepcopy(
                direct.rng.bit_generator.state,
            ),
            'plan_state': plan.state_dict(),
            'mutation_before': tuple(before.mutation_events.items()),
            'mutation_after': tuple(after.mutation_events.items()),
        })
    return cases


def _a48c5_assert_start_oracle(expected_world, expected_cell,
                                actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c5.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or float(expected_world.dissipated_energy)
            != float(actual_world.dissipated_energy)
            or len(actual_cell.genomes) != 1
            or actual_cell.replication_template is None
            or len(actual_cell.replication_copy)
            >= len(actual_cell.replication_template)
            or not np.array_equal(
                actual_cell.replication_template,
                actual_cell.genomes[0],
            )
            or actual_cell.replication_template is actual_cell.genomes[0]
            or np.shares_memory(
                actual_cell.replication_template, actual_cell.genomes[0],
            )):
        raise AssertionError(label + ' start topology/PCG64 differs')


def _a48c5_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c5.Hybrid066WorldA4ReplicationMutationFreeStart(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def test_a48c5_mutation_free_inactive_start_atomic_commit_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c5')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveStart(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_candidate = candidate
            return candidate

    details = []
    cases = _a48c5_cases()
    for case in cases:
        devices = ['cpu']
        if (case['label'] in (
                'normal-unprimed', 'normal-primed',
                'missing-lesion-unprimed', 'missing-lesion-primed')
                and torch.cuda.is_available()):
            devices.append('cuda')
        for device in devices:
            direct = _a48c5_world_for_case(
                case, track_rng=True, track_ledger=True,
            )
            direct_cell = direct.cells[0]
            direct_ledger = direct_cell.mutation_events
            direct_copy_before = direct_cell.replication_copy
            direct_stable = _a48c4_success_stable_identity(
                direct, direct_cell,
            )
            direct_cell._replicate_genome(
                direct, case['dt'], direct.config,
            )
            if (_a48c4_success_stable_identity(direct, direct_cell)
                    != direct_stable
                    or direct_cell.replication_copy is direct_copy_before
                    or direct.rng.calls != [('integers', (0, 1), {})]
                    or direct.rng.bit_generator.state
                    != case['rng_before_state']
                    or direct_ledger.writes
                    or tuple(direct_ledger.items())
                    != case['mutation_before']):
                raise AssertionError(
                    case['label']
                    + ' frozen selection/ledger identity drifted'
                )

            world = _a48c5_world_for_case(
                case, track_rng=True, track_ledger=True,
            )
            cell = world.cells[0]
            live_ledger = cell.mutation_events
            stable_before = _a48c4_success_stable_identity(world, cell)
            old_copy = cell.replication_copy
            pools_before = np.asarray(cell.pools, np.float64).copy()
            scheduler = ObserveStart(case['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                candidate = scheduler.observed_candidate
                _a48c5_assert_start_oracle(
                    direct, direct_cell, world, cell,
                    'a48c5.oracle.%s.%s' % (device, case['label']),
                )
                plan = candidate.plan
                evidence = candidate.evidence
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                if (_a48c4_success_stable_identity(world, cell)
                        != stable_before
                        or cell.replication_copy is old_copy
                        or cell.replication_template
                        is candidate.candidate_cell.replication_template
                        or np.shares_memory(
                            cell.replication_template,
                            candidate.candidate_cell.replication_template,
                        )
                        or cell.replication_copy
                        is candidate.candidate_cell.replication_copy
                        or not np.array_equal(
                            cell.replication_template,
                            candidate.candidate_cell.replication_template,
                        )
                        or cell.replication_copy
                        != candidate.candidate_cell.replication_copy):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' success identity/nonalias differs'
                    )
                paid = {
                    int(a4.a3.POOL_NUCLEOTIDE), int(a4.a3.POOL_ATP),
                }
                unchanged = [
                    index for index in range(int(a4.a3.POOL_COUNT))
                    if index not in paid
                ]
                if (not np.array_equal(
                        np.asarray(cell.pools[unchanged], np.float64).view(
                            np.uint64),
                        pools_before[unchanged].view(np.uint64),
                        )
                        or live_ledger.writes
                        or cell.mutation_events is not live_ledger
                        or tuple(live_ledger.items())
                        != case['mutation_before']
                        or tuple(candidate.candidate_cell.mutation_events.items())
                        != case['mutation_before']
                        or not isinstance(
                            candidate.candidate_cell.mutation_events,
                            _A48C5TrackingMutationLedger,
                        )
                        or candidate.candidate_cell.mutation_events.writes
                        or world.rng.calls
                        or world.rng.bit_generator.state
                        != case['rng_before_state']
                        or candidate.live_rng is not world.rng
                        or not candidate.consumed
                        or int(plan.requested_symbols[0]) != case['requested']
                        or int(plan.append_count[0]) != case['append']
                        or int(plan.substitution_events[0]) != 0
                        or float(plan.last_effective_error_rate[0])
                        != case['effective_error']
                        or float(cell.last_effective_error_rate)
                        != case['effective_error']
                        or int(evidence.low) != 0
                        or int(evidence.high) != 1
                        or int(evidence.selected_index) != 0
                        or int(evidence.call_count) != 1
                        or evidence.rng_before_state
                        != case['rng_before_state']
                        or evidence.rng_after_state
                        != case['rng_before_state']
                        or candidate.rng_before_state
                        != case['rng_before_state']
                        or candidate.rng_after_state
                        != case['rng_before_state']
                        or metadata.get('authority') != (
                            'A4.8c5-resident-mutation-free-template-start-'
                            'plan-atomic-cpu-cell-pcg64-commit'
                        )
                        or metadata.get('branch')
                        != 'inactive-template-start-deterministic-selection'
                        or metadata.get('device') != device
                        or metadata.get('mutation_enabled') is not False
                        or int(metadata.get('amount', -1)) != case['append']
                        or int(metadata.get('requested_symbols', -1))
                        != case['requested']
                        or int(metadata.get('substitution_events', -1)) != 0
                        or int(metadata.get('rng_call_count', -1)) != 1
                        or metadata.get('work_performed') is not True):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' plan/evidence/receipt/ledger differs'
                    )
                fresh = candidate.fresh_binding
                if (int(fresh.ragged.sequence_count) != 3
                        or int(fresh.ragged.symbol_count)
                        != case['capacity_values']['max_symbols']
                        or int(fresh.ragged.genome_counts[0]) != 1
                        or not bool(fresh.ragged.replication_active[0])
                        or int(fresh.state.genome_count[0]) != 1):
                    raise AssertionError(
                        case['label'] + ' fresh Q/S/genome topology differs'
                    )
                details.append('%s:%s:%d/%d' % (
                    device, case['label'], case['requested'], case['append'],
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 selection-only actual integers(0,1), primed/unprimed '
            'state-neutral PCG64, new template/copy nonalias, lesion/fallback, '
            'paid pools, zero-write ledger, effective-error telemetry, '
            'Q+2/S+T+A; zero-append starts remain work=true: '
            + ', '.join(details))


def test_a48c5_candidate_cpu_cuda_selection_purity_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c5')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    cases = _a48c5_cases()

    # Exercise the c5 private high-level selection primitive itself for both
    # PCG64 cache states and both normal/missing-lesion sources.
    selection_details = []
    for case in cases[:4]:
        generator = _A48C5TrackingGenerator(case['rng_before_state'])
        selected = a48c5._formal066_single_template_selection(generator)
        if (int(selected) != 0
                or generator.calls != [('integers', (0, 1), {})]
                or generator.bit_generator.state
                != case['rng_before_state']):
            raise AssertionError(
                case['label'] + ' c5 selection primitive drifted'
            )
        selection_details.append(case['label'])

    case = cases[0]
    reference_plan = None
    reference_state = None
    reference_rng = None
    details = []
    for device in devices:
        direct = _a48c5_world_for_case(case)
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        world = _a48c5_world_for_case(
            case, track_rng=True, track_ledger=True,
        )
        cell = world.cells[0]
        live_ledger = cell.mutation_events
        old_copy = cell.replication_copy
        scheduler = a48c5.A4ReplicationMutationFreeStartEventScheduler(
            case['capacity'], device,
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, case['dt'],
        )
        before_world = v3.pickle_clone(world.state_dict())
        before_ids = _a48c_identity_snapshot(world, cell)
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = (
                scheduler._prepare_replication_mutation_free_start_candidate(
                    world, cell, case['dt'], world.config,
                )
            )
            v3.assert_recursive_close(
                before_world, world.state_dict(), 0.0, 0.0,
                'a48c5.prepare_purity.' + device,
            )
            _, record = scheduler._record(cell)
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng
                    or world.rng.calls
                    or live_ledger.writes
                    or float(world.dissipated_energy) != before_energy
                    or _a48c_identity_snapshot(world, cell) != before_ids
                    or any(entry['event'] == 'replication_cpu'
                           for entry in record['events'])):
                raise AssertionError(device + ' prepare crossed claim/purity')
            prepared = candidate.candidate_cell
            evidence = candidate.evidence
            if (prepared is cell
                    or prepared.pools is cell.pools
                    or prepared.genomes is cell.genomes
                    or prepared.replication_template is None
                    or prepared.replication_template is prepared.genomes[0]
                    or np.shares_memory(
                        prepared.replication_template, prepared.genomes[0],
                    )
                    or prepared.replication_copy is old_copy
                    or prepared.mutation_events is cell.mutation_events
                    or candidate.source_binding is candidate.fresh_binding
                    or candidate.plan is candidate.host_replay
                    or candidate.plan is candidate.resident_plan
                    or candidate.live_rng is not world.rng
                    or candidate.rng_before_state != before_rng
                    or candidate.rng_after_state != before_rng
                    or evidence.rng_before_state != before_rng
                    or evidence.rng_after_state != before_rng
                    or int(evidence.call_count) != 1):
                raise AssertionError(device + ' candidate authority/alias differs')
            if (candidate.evidence.state_dict() != candidate.evidence_state
                    or a48c5._host_mutation_free_start_artifacts(
                        candidate.evidence, candidate.host_replay,
                        candidate.plan,
                    ) != candidate.host_artifacts
                    or a48c5._resident_mutation_free_start_artifacts(
                        candidate.resident_binding,
                        candidate.resident_plan, device,
                    ) != candidate.resident_artifacts
                    or not a48._state_arrays_bit_exact(
                        candidate.plan.state_dict(), case['plan_state'],
                    )
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_plan.to_numpy().state_dict(),
                        candidate.plan.state_dict(),
                    )
                    or not a48c4._start_plan_states_bit_exact(
                        candidate.plan, candidate.host_replay,
                    )):
                raise AssertionError(device + ' evidence/plan association differs')
            a48c5._mutation_free_start_plan_scope(
                candidate.source_binding, candidate.plan,
                case['dt'], world.config, evidence=candidate.evidence,
            )
            independent = a44._paid_replication_elongation_numpy(
                candidate.source_binding, case['dt'], world.config,
                allow_template_start=True,
            )
            if not a48c4._start_plan_states_bit_exact(
                    candidate.host_replay, independent):
                raise AssertionError(device + ' independent NumPy plan differs')
            _a48b_assert_cell_matches(
                direct.cells[0], prepared,
                'a48c5.candidate.' + device,
                atol=a48c5.REPLICATION_ORACLE_ATOL,
            )
            a4._require_translation_binding(candidate.fresh_binding)
            if (not isinstance(
                    candidate.fresh_binding.ragged.symbols, np.ndarray)
                    or not isinstance(
                        candidate.fresh_binding.cache.fingerprints,
                        np.ndarray,
                    )
                    or np.shares_memory(
                        candidate.fresh_binding.ragged.symbols,
                        candidate.source_binding.ragged.symbols,
                    )):
                raise AssertionError(device + ' fresh binding/cache differs')

            private_template = prepared.replication_template
            private_copy = prepared.replication_copy
            scheduler._commit_replication_mutation_free_start_candidate(
                world, cell, case['dt'], world.config, candidate,
            )
            _a48c5_assert_start_oracle(
                direct, direct.cells[0], world, cell,
                'a48c5.commit.' + device,
            )
            if (cell.replication_template is private_template
                    or np.shares_memory(
                        cell.replication_template, private_template,
                    )
                    or cell.replication_copy is private_copy
                    or not np.array_equal(
                        cell.replication_template, private_template,
                    )
                    or cell.replication_copy != private_copy
                    or cell.mutation_events is not live_ledger
                    or live_ledger.writes
                    or not isinstance(
                        prepared.mutation_events,
                        _A48C5TrackingMutationLedger,
                    )
                    or prepared.mutation_events.writes
                    or tuple(live_ledger.items())
                    != case['mutation_before']):
                raise AssertionError(
                    device + ' publish alias/ledger write differs'
                )
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48c5.A4ReplicationMutationFreeStartCommitError,
                lambda: scheduler._commit_replication_mutation_free_start_candidate(
                    world, cell, case['dt'], world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48c5.one_shot.' + device,
            )
            if (not candidate.consumed
                    or world.rng is not before_rng_object
                    or world.rng.bit_generator.state != committed_rng
                    or world.rng.calls
                    or live_ledger.writes):
                raise AssertionError(device + ' one-shot authority differs')
            if reference_plan is None:
                reference_plan = candidate.plan.clone()
                reference_state = v3.pickle_clone(cell.state_dict())
                reference_rng = copy.deepcopy(committed_rng)
            else:
                if not a48c4._start_plan_states_bit_exact(
                        candidate.plan, reference_plan):
                    raise AssertionError('CPU/CUDA resident plans differ')
                v3.assert_recursive_close(
                    reference_state, cell.state_dict(),
                    a48c5.REPLICATION_ORACLE_ATOL, 0.0,
                    'a48c5.cpu_cuda_state',
                )
                if committed_rng != reference_rng:
                    raise AssertionError('CPU/CUDA committed PCG64 differs')
            pointer_count = sum(
                len(values) for values in
                candidate.resident_artifacts['data_ptrs'].values()
            )
            details.append('%s:%d-pointers' % (device, pointer_count))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('private selection primitive %s; CPU/CUDA prepare purity, '
            'resident private-plan device/pointer/version/content, independent '
            'NumPy oracle, fresh evidence/binding/cache, zero-write ledger, '
            'same live Generator and one-shot PASS: %s' % (
                '/'.join(selection_details), '/'.join(details),
            ))


def _a48c5_assert_preclaim_failure(state, cell_id, dt, capacity,
                                    scheduler_type=None, device='cpu',
                                    label='scope'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    cell = next(
        item for item in world.cells if int(item.cell_id) == int(cell_id)
    )
    scheduler_type = (
        scheduler_type
        or a48c5.A4ReplicationMutationFreeStartEventScheduler
    )
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(world.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng_object = world.rng
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    before_receipts = len(scheduler._receipts)
    try:
        _assert_raises(
            (a48c5.A4ReplicationMutationFreeStartCommitError,
             a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, dt, world.config,
            ),
        )
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c5.preclaim.' + label,
        )
        _, record = scheduler._record(cell)
        if (world.rng is not before_rng_object
                or world.rng.bit_generator.state != before_rng
                or float(world.dissipated_energy) != before_energy
                or _a48c_identity_snapshot(world, cell) != before_ids
                or len(scheduler._receipts) != before_receipts
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c5_capacity_trust_rollback_scope_and_exact_once():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c5 trust tests')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    resident_devices = ['cpu']
    if torch.cuda.is_available():
        resident_devices.append('cuda')
    base = _a48c5_cases()[0]

    capacity_details = []
    for label, key in (
            ('Q', 'max_sequences'), ('S', 'max_symbols'),
            ('W', 'max_sequence_symbols'),
            ('P', 'max_proteins_per_cell')):
        exact = a4.GPU068A4Config(**base['capacity_values'])
        direct = _a48c5_world_for_case(base)
        direct.cells[0]._replicate_genome(
            direct, base['dt'], direct.config,
        )
        for device in resident_devices:
            world = _a48c5_world_for_case(base)
            cell = world.cells[0]
            scheduler = a48c5.A4ReplicationMutationFreeStartEventScheduler(
                exact, device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, base['dt'], world.config,
                )
                _a48c5_assert_start_oracle(
                    direct, direct.cells[0], world, cell,
                    'a48c5.capacity.%s.%s' % (label, device),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append('%s@%s' % (label, device))
        short = dict(base['capacity_values'])
        short[key] -= 1
        _a48c5_assert_preclaim_failure(
            base['state'], base['cell_id'], base['dt'],
            a4.GPU068A4Config(**short),
            label='capacity_' + label + '_one_short',
        )
    del scheduler, world, cell, direct
    gc.collect()

    trust_details = []

    def assert_tamper_failure(scheduler_type, label, devices=('cpu',),
                              require_isolation=False):
        for device in devices:
            world = _a48c5_world_for_case(base)
            cell = world.cells[0]
            scheduler = scheduler_type(base['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            before_world = v3.pickle_clone(world.state_dict())
            before_objects = _a48c_identity_snapshot(world, cell)
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_config_object = id(world.config)
            before_receipts = len(scheduler._receipts)
            try:
                _assert_raises(
                    (a48c5.A4ReplicationMutationFreeStartCommitError,
                     a4.A4Error, a3s.A3SchedulerError),
                    lambda: scheduler.cpu_replication(
                        world, cell, base['dt'], world.config,
                    ),
                )
                expected_world = getattr(
                    scheduler, 'expected_world_state', before_world,
                )
                expected_rng = getattr(
                    scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, world.state_dict(), 0.0, 0.0,
                    'a48c5.trust.%s.%s' % (device, label),
                )
                _, record = scheduler._record(cell)
                if (world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(world, cell)
                        != before_objects
                        or id(world.config) != before_config_object
                        or len(scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim' % (device, label)
                    )
                if (require_isolation
                        and not bool(getattr(scheduler, 'isolated', False))):
                    raise AssertionError(label + ' retained alias was not isolated')
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
            del scheduler, cell, world
            gc.collect()

    class TamperPackedSource(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.source_binding.ragged.symbols
            self.isolated = (
                retained is not cell.genomes[0]
                and not np.shares_memory(retained, cell.genomes[0])
            )
            retained[0] ^= np.uint8(1)
            return candidate

    class TamperSourceLesion(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.source_binding.ragged.genome_lesions[0] = np.nextafter(
                candidate.source_binding.ragged.genome_lesions[0], np.inf,
            )
            return candidate

    class TamperEvidence(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(candidate.evidence, 'selected_index', 1)
            return candidate

    class TamperEvidenceSnapshot(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.evidence_state['selected_index'] = 1
            return candidate

    class TamperEvidenceConfigDigest(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(candidate.evidence, 'config_sha256', '0' * 64)
            return candidate

    class TamperHostOracle(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.template_storage_symbols[0] += 1
            return candidate

    class TamperReadbackPlan(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.template_start_events[0] = False
            return candidate

    class TamperResidentRaggedData(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentStateData(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_NUCLEOTIDE
            ].add_(1e-8)
            return candidate

    class TamperResidentCacheData(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class TamperResidentPlanData(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.template_storage_symbols.data[0].add_(1)
            return candidate

    class SwapResidentTensorPointer(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.template_storage_symbols = (
                candidate.resident_plan.template_storage_symbols.clone()
            )
            return candidate

    class SwapResidentWholeMember(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(
                candidate.resident_binding, 'ragged',
                candidate.resident_binding.ragged.clone(),
            )
            return candidate

    class TamperResidentPrivateSnapshot(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_artifacts['data_ptrs']['plan'][
                'template_storage_symbols'
            ] += 1
            return candidate

    class TamperHostPrivateSnapshot(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_artifacts['plan']['data_ptrs'][
                'template_storage_symbols'
            ] += 1
            return candidate

    class MoveResidentTensorDevice(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            tensor = candidate.resident_binding.ragged.symbols
            target = 'cuda' if tensor.device.type == 'cpu' else 'cpu'
            candidate.resident_binding.ragged.symbols = tensor.to(target)
            return candidate

    class TamperCandidateTemplate(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_template[0] ^= np.uint8(1)
            return candidate

    class AliasCandidateTemplate(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_template = (
                candidate.candidate_cell.genomes[0]
            )
            return candidate

    class SwapCandidateCopyOuter(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.replication_copy = list(
                candidate.candidate_cell.replication_copy
            )
            return candidate

    class MutateUnpackedCandidate(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            retained = candidate.candidate_cell.last_repair_flux
            self.isolated = (
                retained is not cell.last_repair_flux
                and not np.shares_memory(retained, cell.last_repair_flux)
            )
            retained[0] += 0.125
            return candidate

    class TamperFreshBinding(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class TamperSchedulerCapacityDevice(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = 'cuda' if self.a4_device == 'cpu' else 'cpu'
            return candidate

    class TamperLiveConfig(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                float(world.config.mutation_rate), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveSource(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.genomes[0][0] ^= np.uint8(1)
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveRng(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperCandidateRng(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _replication_mutation_free_start_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_state,
            )
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    for scheduler_type, label, isolation in (
            (TamperPackedSource, 'packed_source', True),
            (TamperSourceLesion, 'source_lesion', False),
            (TamperEvidence, 'selection_evidence', False),
            (TamperEvidenceSnapshot, 'selection_evidence_snapshot', False),
            (TamperEvidenceConfigDigest, 'selection_config_digest', False),
            (TamperHostOracle, 'host_oracle_start_topology', False),
            (TamperReadbackPlan, 'readback_plan_start_flag', False),
            (SwapResidentTensorPointer, 'resident_pointer', False),
            (SwapResidentWholeMember, 'resident_whole_member', False),
            (TamperResidentPrivateSnapshot, 'resident_private', False),
            (TamperHostPrivateSnapshot, 'host_private', False),
            (TamperCandidateTemplate, 'candidate_template', False),
            (AliasCandidateTemplate, 'candidate_template_alias', False),
            (SwapCandidateCopyOuter, 'candidate_copy_outer', False),
            (MutateUnpackedCandidate, 'candidate_deep_array', True),
            (TamperFreshBinding, 'fresh_binding', False),
            (TamperSchedulerCapacityDevice, 'scheduler_config_device', False),
            (TamperLiveConfig, 'live_config', False),
            (TamperLiveSource, 'live_source_genome', False),
            (TamperLiveRng, 'live_rng', False),
            (TamperCandidateRng, 'candidate_rng', False)):
        assert_tamper_failure(
            scheduler_type, label, require_isolation=isolation,
        )
    for scheduler_type, label in (
            (TamperResidentRaggedData, 'resident_ragged_data'),
            (TamperResidentStateData, 'resident_state_data'),
            (TamperResidentCacheData, 'resident_cache_data'),
            (TamperResidentPlanData, 'resident_plan_data')):
        assert_tamper_failure(
            scheduler_type, label, devices=resident_devices,
        )
    if torch.cuda.is_available():
        assert_tamper_failure(
            MoveResidentTensorDevice, 'resident_device',
            devices=resident_devices,
        )

    world = _a48c5_world_for_case(base)
    cell = world.cells[0]
    scheduler = a48c5.A4ReplicationMutationFreeStartEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, cell, base['dt'])
    before = v3.pickle_clone(world.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = (
            scheduler._prepare_replication_mutation_free_start_candidate(
                world, cell, base['dt'], world.config,
            )
        )
        wrong_inputs = (
            ('cell', copy.deepcopy(cell), base['dt'], world.config),
            ('dt', cell, np.nextafter(base['dt'], np.inf), world.config),
            ('config', cell, base['dt'], copy.deepcopy(world.config)),
        )
        for label, wrong_cell, wrong_dt, wrong_config in wrong_inputs:
            _assert_raises(
                a48c5.A4ReplicationMutationFreeStartCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler._commit_replication_mutation_free_start_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError('wrong ' + label + ' consumed candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c5.wrong_cell_dt_config',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong inputs changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)
    del candidate, scheduler, world, cell
    gc.collect()

    scope_cases = []

    def add_scope(label, mutate, dt=base['dt']):
        scoped = _a48c5_world_for_case(base)
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scoped.last_step_material_residual = 0.0
        scope_cases.append((
            label, v3.pickle_clone(scoped.state_dict()),
            int(scoped_cell.cell_id), float(dt),
        ))

    add_scope('disabled', lambda world, cell: setattr(
        world.config, 'genome_replication', False))
    add_scope('negative_atp', lambda world, cell: cell.pools.__setitem__(
        a4.a3.POOL_ATP, -1e-12))

    def make_no_genome(world, cell):
        cell.genomes = []
        cell.genome_lesions = []
        cell.replication_template = None
        cell.replication_copy = []
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = 0.0
        cell._refresh_gene_cache()

    add_scope('no_genome', make_no_genome)

    def make_two_genomes(world, cell):
        cell.genomes = [
            np.asarray(cell.genomes[0], np.uint8).copy(),
            np.asarray(cell.genomes[0], np.uint8).copy(),
        ]
        cell.genome_lesions = [0.1, 0.2]
        cell._refresh_gene_cache()

    add_scope('two_genomes', make_two_genomes)

    def remove_replicase(world, cell):
        world.config.external_replicase = False
        for fingerprint, spec in list(cell.gene_specs.items()):
            if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
                cell.proteins.pop(fingerprint, None)
        cell._sync_protein_pool()

    add_scope('replicase_gate', remove_replicase)

    def make_start_completion(world, cell):
        cell.genomes = [np.asarray((3,), dtype=np.uint8)]
        cell.genome_lesions = [0.2]
        cell.replication_template = None
        cell.replication_copy = []
        cell.replication_template_lesion = 0.0
        cell.replication_fractional = 0.0
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 0.75
        cell.pools[a4.a3.POOL_ATP] = 0.75
        cell._refresh_gene_cache()

    add_scope('same_call_completion', make_start_completion, dt=100.0)
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for label, state, cell_id, dt in scope_cases:
            _a48c5_assert_preclaim_failure(
                state, cell_id, dt, a4.GPU068A4Config(),
                label='scope_' + label,
            )

        # The direct fixture helper intentionally rejects dead cells before
        # it can create an event context.  Establish the legal alive context
        # first, then make the cell dead and prove c5 itself stays preclaim.
        dead = _a48c5_world_for_case(base)
        dead_cell = dead.cells[0]
        dead_scheduler = (
            a48c5.A4ReplicationMutationFreeStartEventScheduler(
                base['capacity'], 'cpu',
            )
        )
        _a48c_begin_replication_direct(
            dead_scheduler, dead, dead_cell, base['dt'],
        )
        dead_cell.alive = False
        dead_before = v3.pickle_clone(dead.state_dict())
        dead_ids = _a48c_identity_snapshot(dead, dead_cell)
        dead_rng = copy.deepcopy(dead.rng.bit_generator.state)
        _assert_raises(
            a48c5.A4ReplicationMutationFreeStartCommitError,
            lambda: dead_scheduler.cpu_replication(
                dead, dead_cell, base['dt'], dead.config,
            ),
        )
        v3.assert_recursive_close(
            dead_before, dead.state_dict(), 0.0, 0.0,
            'a48c5.scope_dead',
        )
        _, dead_record = dead_scheduler._record(dead_cell)
        if (dead.rng.bit_generator.state != dead_rng
                or _a48c_identity_snapshot(dead, dead_cell) != dead_ids
                or any(entry['event'] == 'replication_cpu'
                       for entry in dead_record['events'])):
            raise AssertionError('dead scope crossed preclaim boundary')
        _a48_abort_direct(dead_scheduler, dead_cell)
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('excluded c5 scope used CPU fallback')

    duplicate = _a48c5_world_for_case(base)
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = (
        a48c5.A4ReplicationMutationFreeStartEventScheduler(
            base['capacity'], 'cpu',
        )
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, base['dt'],
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, base['dt'], duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, base['dt'], duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate.state_dict(), 0.0, 0.0,
        'a48c5.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate start changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = _a48c5_world_for_case(base)
    order_cell = order.cells[0]
    order_scheduler = a48c5.A4ReplicationMutationFreeStartEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, base['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, base['dt'], order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order.state_dict(), 0.0, 0.0,
        'a48c5.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order start changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(
            a48c5.A4ReplicationMutationFreeStartEventScheduler):
        def _publish_replication_mutation_free_start_candidate(
                self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_mutation_free_start_candidate(
                world, cell, candidate,
            )
            if cell.replication_template is None:
                raise AssertionError('publish seam ran before template creation')
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.replication_template[0] ^= np.uint8(1)
            cell.replication_copy.append(7)
            cell.replication_copy = list(cell.replication_copy)
            cell.replication_template_lesion += 0.25
            cell.replication_fractional += 0.5
            cell.last_replication_symbols += 2
            cell.last_effective_error_rate += 0.125
            cell.cumulative_proofreading_atp += 0.5
            cell.mutation_events['a48c5_extra_sentinel'] += 1
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c5 publish failure')

    rollback = _a48c5_world_for_case(base)
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(base['capacity'], 'cpu')
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, base['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    old_copy_object = rollback_cell.replication_copy
    old_pools_object = rollback_cell.pools
    old_events_object = rollback_cell.mutation_events
    rollback_rng_object = rollback.rng
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, base['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c5 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c5.publish_rollback',
    )
    if (rollback_cell.replication_template is not None
            or rollback_cell.replication_copy is not old_copy_object
            or rollback_cell.replication_copy != []
            or rollback_cell.pools is not old_pools_object
            or rollback_cell.mutation_events is not old_events_object
            or rollback.rng is not rollback_rng_object
            or rollback.rng.bit_generator.state != rollback_rng
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids):
        raise AssertionError(
            'None-aware c5 rollback lost source identity/state'
        )
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if entry['event'] != 'replication_cpu' or receipt['status'] != 'aborted':
        raise AssertionError('publish failure did not leave aborted receipt')
    return ('Q/S/W/P exact CPU/CUDA + each one-short; %d source/evidence/'
            'plan/pointer/device/.data/candidate/fresh/live trust checks; '
            'wrong inputs; %d no-fallback scopes; duplicate-before-reclass/'
            'order; None-template + old-copy/pools/ledger/RNG identity '
            'rollback PASS (%s)' % (
                len(trust_details), len(scope_cases) + 1,
                '/'.join(capacity_details),
            ))


def test_a48c5_inherited_c4_c3_c2_c1_save_clone_successor_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c5')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    delegated_cases = []
    c1_world, c1_cells, _, c1_dt = _a45_substitution_fixture(seed=12401)
    _a48c_reduce_world(c1_world, [c1_cells[0]])
    delegated_cases.append({
        'label': 'c1-active-noncompletion',
        'state': v3.pickle_clone(c1_world.state_dict()),
        'dt': float(c1_dt), 'capacity': a4.GPU068A4Config(),
        'authority': 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
    })

    c2_state, _, c2_dt = _a48c2_completion_case(seed=12402, row=0)
    c2_before_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_before = copy.deepcopy(c2_before_world.cells[0])
    c2_after_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after_world.cells[0]._replicate_genome(
        c2_after_world, c2_dt, c2_after_world.config,
    )
    c2_capacity = a4.GPU068A4Config(**_a48c2_capacity_for_transition(
        c2_before, c2_after_world.cells[0],
    ))
    delegated_cases.append({
        'label': 'c2-mutation-free-completion',
        'state': c2_state, 'dt': float(c2_dt),
        'capacity': c2_capacity,
        'authority': (
            'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit'
        ),
    })

    c3_case = _a48c3_completion_mutation_cases()[0]
    delegated_cases.append({
        'label': 'c3-mutation-completion',
        'state': c3_case['state'], 'dt': float(c3_case['dt']),
        'capacity': c3_case['integration_capacity'],
        'authority': (
            'A4.8c3-resident-completion-mutation-plan-atomic-'
            'cpu-cell-rng-commit'
        ),
    })

    c4_case = _a48c4_cases()[0]
    delegated_cases.append({
        'label': 'c4-mutation-start',
        'state': c4_case['state'], 'dt': float(c4_case['dt']),
        'capacity': c4_case['capacity'],
        'authority': (
            'A4.8c4-resident-template-start-plan-atomic-'
            'cpu-cell-pcg64-commit'
        ),
    })

    delegated = []
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in delegated_cases:
            for device in devices:
                direct = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(case['state']),
                )
                world = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(case['state']),
                )
                direct.cells[0]._replicate_genome(
                    direct, case['dt'], direct.config,
                )
                scheduler = (
                    a48c5.A4ReplicationMutationFreeStartEventScheduler(
                        case['capacity'], device,
                    )
                )
                _a48c_begin_replication_direct(
                    scheduler, world, world.cells[0], case['dt'],
                )
                try:
                    scheduler.cpu_replication(
                        world, world.cells[0], case['dt'], world.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], world.cells[0],
                        'a48c5.delegate.%s.%s' % (device, case['label']),
                        atol=a48c5.REPLICATION_ORACLE_ATOL,
                    )
                    entry = _a48c_replication_entry(
                        scheduler, world.cells[0],
                    )
                    if (direct.rng.bit_generator.state
                            != world.rng.bit_generator.state
                            or float(direct.dissipated_energy)
                            != float(world.dissipated_energy)
                            or entry['metadata'].get('authority')
                            != case['authority']):
                        raise AssertionError(
                            device + '/' + case['label']
                            + ' inherited authority differs'
                        )
                    delegated.append(device + ':' + case['label'])
                finally:
                    _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('an inherited c5 branch used frozen CPU fallback')

    own = _a48c5_cases()[0]
    direct = _a48c5_world_for_case(own)
    direct.cells[0]._replicate_genome(direct, own['dt'], direct.config)
    world = _a48c5_world_for_case(own)
    scheduler = a48c5.A4ReplicationMutationFreeStartEventScheduler(
        own['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, world.cells[0], own['dt'])
    original_c4 = a48c4.A4ReplicationStartEventScheduler.cpu_replication

    def c4_dispatch_bomb(*args, **kwargs):
        raise AssertionError('owned c5 start delegated to c4')

    a48c4.A4ReplicationStartEventScheduler.cpu_replication = c4_dispatch_bomb
    try:
        scheduler.cpu_replication(
            world, world.cells[0], own['dt'], world.config,
        )
        _a48c5_assert_start_oracle(
            direct, direct.cells[0], world, world.cells[0],
            'a48c5.owned_no_c4_dispatch',
        )
    finally:
        a48c4.A4ReplicationStartEventScheduler.cpu_replication = original_c4
        _a48_abort_direct(scheduler, world.cells[0])

    roomy = a4.GPU068A4Config(
        max_cells=1, max_sequences=4, max_symbols=2048,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )
    successor_details = []
    persisted = None
    persisted_cpu = None
    for device in devices:
        cpu = _a48c5_world_for_case(own)
        hybrid = _a48c5_hybrid_from_state(own['state'], roomy, device)
        cpu.step(own['dt'])
        original_c4 = a48c4.A4ReplicationStartEventScheduler.cpu_replication
        a48c4.A4ReplicationStartEventScheduler.cpu_replication = c4_dispatch_bomb
        try:
            receipt = hybrid.step(own['dt'])
        finally:
            a48c4.A4ReplicationStartEventScheduler.cpu_replication = original_c4
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c5.successor.' + device,
        )
        record = receipt['cells'][0]
        names = [entry['event'] for entry in record['events']]
        by_name = {entry['event']: entry for entry in record['events']}
        if (names.count('replication_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1
                or not names.index('translation_cpu')
                < names.index('replication_cpu')
                < names.index('surface_assembly')
                < names.index('genome_hydrolysis_cpu_rng')
                < names.index('motion')
                or by_name['replication_cpu']['metadata'].get('authority')
                != ('A4.8c5-resident-mutation-free-template-start-plan-'
                    'atomic-cpu-cell-pcg64-commit')
                or by_name['replication_cpu']['metadata'].get(
                    'work_performed') is not True):
            raise AssertionError(device + ' c5 successor order differs')
        successor_details.append(device)
        if device == 'cpu':
            persisted = hybrid
            persisted_cpu = cpu

    twin = persisted.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c5.pkl')
        persisted.save(path)
        restored = (
            a48c5.Hybrid066WorldA4ReplicationMutationFreeStart.load(path)
        )
        for value in (persisted, twin, restored):
            if (type(value)
                    is not a48c5.Hybrid066WorldA4ReplicationMutationFreeStart
                    or type(value.scheduler)
                    is not a48c5.A4ReplicationMutationFreeStartEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != roomy
                    or value.scheduler.replication_mutation_free_start_integration_state()
                    != {
                        'schema': a48c5.SCHEMA_VERSION,
                        'config': a48._config_state(roomy),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c5 save/clone authority was lost')
        expected = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(persisted_cpu.state_dict()),
        )
        expected.step(own['dt'])
        for value in (persisted, twin, restored):
            value.step(own['dt'])
            v3._assert_world_pair(
                expected, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c5.persisted_successor',
            )

    active = _a48c5_hybrid_from_state(own['state'], roomy, 'cpu')
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active',
            '_a4_replication_start_commit_active',
            '_a4_replication_mutation_free_start_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    expected_status = (
        'a4.8c5-mutation-free-inactive-one-genome-template-start-'
        'noncompletion-resident-plan-selection-only-pcg64-atomic-commit-'
        'a4.8c4-c3-c2-c1-branches-inherited-other-inactive-early-noop-'
        'fail-closed'
    )
    signature = inspect.signature(
        a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication,
    )
    prior_names = '\n'.join(fn.__name__ for fn in TESTS[:70])
    c5_names = tuple(fn.__name__ for fn in TESTS[70:74])
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationMutationFreeStartCommitCandidate'
            in set(a48c5.__all__)
            or '_A4SingleTemplateSelectionEvidence' in set(a48c5.__all__)
            or not issubclass(
                a48c5.A4ReplicationMutationFreeStartEventScheduler,
                a48c4.A4ReplicationStartEventScheduler,
            )
            or not issubclass(
                a48c5.Hybrid066WorldA4ReplicationMutationFreeStart,
                a48c4.Hybrid066WorldA4ReplicationStart,
            )
            or a48c5.PORT_STATUS.get('genome_replication') != expected_status
            or a48c5.FULL_GPU_WORLD_STEP is not False
            or a48c5.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) < 74
            or c5_names != (
                'test_a48c5_mutation_free_inactive_start_atomic_commit_formal066_oracle',
                'test_a48c5_candidate_cpu_cuda_selection_purity_and_fresh_binding',
                'test_a48c5_capacity_trust_rollback_scope_and_exact_once',
                'test_a48c5_inherited_c4_c3_c2_c1_save_clone_successor_and_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A48C5_TEST_NAMES_SHA256):
        raise AssertionError('A4.8c5 API/scope/70-test authority differs')
    expected_hashes = dict(PROMOTED_A3_SHA256)
    expected_hashes.update(_A48C5_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_start_mutation_free_integration.py'
    ] = _A48C5_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('inactive mutation=true c4 + active c1/c2/c3 CPU/CUDA '
            'delegates %s; owned start no c4/frozen fallback; save/load/clone + '
            'c1/c2/c3/c4/c5 active reject; translation<replication<surface<'
            'hydrolysis<motion successor %s; first70/A3/A4.5b/c1-c5 hash/'
            'full_gpu=false PASS' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A48C6_TEST_NAMES_SHA256 = (
    '8abdcb0f35d8362f8ae76753045ea625f066d5e82b66afe80753e0344ee62499'
)

_A48C6_PRIOR_CORE_SHA256 = dict(_A48C5_PRIOR_CORE_SHA256)
_A48C6_PRIOR_CORE_SHA256[
    'src/0_6_8/'
    'SOMA_CELL_0_6_8_gpu_a4_replication_start_mutation_free_integration.py'
] = _A48C5_CORE_SHA256

_A48C6_CORE_SHA256 = (
    '6dd747d9fb1451b9ddcbc790b4e695108763975313dda3d241ce982e383cc8d8'
)

_A48C6_EXPECTED_CASES = {
    'normal-unprimed': (4083, 576, 576, 3, 1152, 576, 36),
    'normal-primed': (4083, 576, 576, 3, 1152, 576, 36),
    'missing-lesion-unprimed': (5972, 496, 496, 3, 992, 496, 31),
    'missing-lesion-primed': (5972, 496, 496, 3, 992, 496, 31),
}


def _a48c6_world_for_case(case, track_rng=False, track_ledger=False):
    world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(case['state']),
    )
    if bool(case.get('drop_lesions', False)):
        world.cells[0].genome_lesions = []
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
    if track_rng:
        world.rng = _A48C5TrackingGenerator(
            world.rng.bit_generator.state,
        )
    if track_ledger:
        _a48c5_tracking_ledger(world.cells[0])
    return world


def _a48c6_synthetic_active_cell(source):
    synthetic = copy.deepcopy(source)
    synthetic.replication_template = np.asarray(
        source.genomes[0], dtype=np.uint8,
    ).copy()
    synthetic.replication_template_lesion = (
        float(source.genome_lesions[0]) if source.genome_lesions else 0.0
    )
    synthetic.replication_copy = []
    synthetic.replication_fractional = 0.0
    return synthetic


def _a48c6_cases(seed=12501):
    """Fixed same-call completions over both PCG64 cache states."""
    if int(seed) != 12501:
        raise AssertionError('A4.8c6 fixed fixture seed must remain 12501')
    c5_cases = {case['label']: case for case in _a48c5_cases()[:4]}
    definitions = (
        ('normal-unprimed', False),
        ('normal-primed', True),
        ('missing-lesion-unprimed', False),
        ('missing-lesion-primed', True),
    )
    cases = []
    for offset, (label, primed) in enumerate(definitions):
        base = c5_cases[label]
        world = _a48c5_world_for_case(base)
        cell = world.cells[0]
        cell.pools[a4.a3.POOL_NUCLEOTIDE] = 2.0
        cell.pools[a4.a3.POOL_ATP] = 2.0
        cell.mutation_events['a48c6_extra_sentinel'] = 401 + offset
        world.rng = np.random.Generator(np.random.PCG64(seed + offset))
        if primed:
            world.rng.integers(0, int(a4.g2.ALPHABET_SIZE))
        rng_state = copy.deepcopy(world.rng.bit_generator.state)
        if int(rng_state['has_uint32']) != int(bool(primed)):
            raise AssertionError(label + ' PCG64 cache priming drifted')
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
        state = v3.pickle_clone(world.state_dict())
        provisional = {
            'state': state,
            'drop_lesions': bool(base.get('drop_lesions', False)),
        }

        direct = _a48c6_world_for_case(provisional, track_rng=True)
        before = copy.deepcopy(direct.cells[0])
        material_before = float(direct.cells[0].material_mass())
        direct.cells[0]._replicate_genome(direct, 100.0, direct.config)
        after = direct.cells[0]
        if (direct.rng.calls != [('integers', (0, 1), {})]
                or direct.rng.bit_generator.state != rng_state
                or len(after.genomes) != 2
                or after.replication_template is not None
                or after.replication_copy
                or tuple(before.mutation_events.items())
                != tuple(after.mutation_events.items())):
            raise AssertionError(label + ' frozen completion semantics drifted')

        synthetic = _a48c6_synthetic_active_cell(before)
        capacity_values = _a48c2_capacity_for_transition(synthetic, after)
        capacity = a4.GPU068A4Config(**capacity_values)
        source_world = _a48c6_world_for_case(provisional)
        source = source_world.cells[0]
        source_ragged, _, source_binding = _paid_replication_binding(
            [source], source_world.config, capacity,
        )
        synthetic_ragged, _, synthetic_binding = _paid_replication_binding(
            [synthetic], source_world.config, capacity,
        )
        plan = a44.paid_replication_completion_plan(
            synthetic_binding, 100.0, source_world.config,
        )
        _assert_completion_cpu_parity(
            plan, [synthetic], [after], 'a48c6.fixture.' + label,
        )
        final_ragged = a4.FullFidelityA4GenomeAdapter(
            capacity,
        ).pack_cells([after])
        template_length = len(before.genomes[0])
        values = (
            int(plan.requested_symbols[0]), int(plan.append_count[0]),
            template_length, int(capacity.max_sequences),
            int(capacity.max_symbols), int(capacity.max_sequence_symbols),
            int(capacity.max_proteins_per_cell),
        )
        if (values != _A48C6_EXPECTED_CASES[label]
                or int(source_ragged.sequence_count) != 1
                or int(source_ragged.symbol_count) != template_length
                or int(synthetic_ragged.sequence_count) != 3
                or int(synthetic_ragged.symbol_count) != 2 * template_length
                or int(final_ragged.sequence_count) != 2
                or int(final_ragged.symbol_count) != 2 * template_length
                or bool(plan.template_start_events[0])
                or not bool(plan.completion_events[0])
                or int(plan.substitution_events[0]) != 0
                or int(plan.topology_sequence_deltas[0]) != -1
                or int(plan.topology_symbol_deltas[0]) != 0
                or int(plan.replication_cycle_deltas[0]) != 1
                or not np.array_equal(
                    plan.completed_symbols[0, :template_length],
                    before.genomes[0],
                )):
            raise AssertionError(label + ' fixed synthetic/final plan drifted')
        v3.assert_recursive_close(
            material_before, float(after.material_mass()),
            atol=3e-12, rtol=0.0, path='a48c6.material.' + label,
        )
        cases.append({
            'label': label, 'state': state,
            'drop_lesions': provisional['drop_lesions'],
            'cell_id': int(cell.cell_id), 'dt': 100.0,
            'capacity': capacity, 'capacity_values': capacity_values,
            'template_length': template_length,
            'requested': int(plan.requested_symbols[0]),
            'append': int(plan.append_count[0]),
            'effective_error': float(plan.last_effective_error_rate[0]),
            'new_lesion': float(plan.new_genome_lesions[0]),
            'rng_before_state': rng_state,
            'rng_after_state': copy.deepcopy(
                direct.rng.bit_generator.state,
            ),
            'plan_state': plan.state_dict(),
            'synthetic_state': synthetic.state_dict(),
            'mutation_before': tuple(before.mutation_events.items()),
            'mutation_after': tuple(after.mutation_events.items()),
            'source_lesions': len(before.genome_lesions),
            'final_lesions': len(after.genome_lesions),
        })
    return cases


def _a48c6_assert_completion_oracle(expected_world, expected_cell,
                                     actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c6.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or float(expected_world.dissipated_energy)
            != float(actual_world.dissipated_energy)
            or len(actual_cell.genomes) != 2
            or actual_cell.replication_template is not None
            or actual_cell.replication_copy
            or float(actual_cell.replication_template_lesion) != 0.0
            or float(actual_cell.replication_fractional) != 0.0
            or not np.array_equal(
                actual_cell.genomes[-1], actual_cell.genomes[0],
            )
            or actual_cell.genomes[-1] is actual_cell.genomes[0]
            or np.shares_memory(
                actual_cell.genomes[-1], actual_cell.genomes[0],
            )):
        raise AssertionError(label + ' completion/topology/PCG64 differs')


def _a48c6_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c6.Hybrid066WorldA4ReplicationStartCompletion(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def test_a48c6_mutation_free_inactive_start_completion_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c6')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveCompletion(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_candidate = candidate
            return candidate

    details = []
    for case in _a48c6_cases():
        devices = ['cpu']
        if torch.cuda.is_available():
            devices.append('cuda')
        for device in devices:
            direct = _a48c6_world_for_case(case, track_rng=True)
            direct_cell = direct.cells[0]
            direct_ledger = direct_cell.mutation_events
            direct_mutation_before = tuple(
                direct_cell.mutation_events.items()
            )
            direct_cell._replicate_genome(
                direct, case['dt'], direct.config,
            )
            if (direct.rng.calls != [('integers', (0, 1), {})]
                    or direct.rng.bit_generator.state
                    != case['rng_before_state']
                    or direct_cell.mutation_events is not direct_ledger
                    or tuple(direct_cell.mutation_events.items())
                    != direct_mutation_before):
                raise AssertionError(
                    case['label'] + ' direct selection/value oracle drifted'
                )

            world = _a48c6_world_for_case(
                case, track_rng=True, track_ledger=True,
            )
            cell = world.cells[0]
            live_ledger = cell.mutation_events
            before_ids = _a48c_identity_snapshot(world, cell)
            old_copy = cell.replication_copy
            old_gene_spec_ids = tuple(
                (int(key), id(value))
                for key, value in cell.gene_specs.items()
            )
            pools_before = np.asarray(cell.pools, np.float64).copy()
            energy_before = float(world.dissipated_energy)
            scheduler = ObserveCompletion(case['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                candidate = scheduler.observed_candidate
                plan = candidate.plan
                evidence = candidate.evidence
                _a48c6_assert_completion_oracle(
                    direct, direct_cell, world, cell,
                    'a48c6.oracle.%s.%s' % (device, case['label']),
                )
                after_ids = _a48c_identity_snapshot(world, cell)
                for key in (
                        'rng', 'pools', 'genomes', 'lesions', 'events',
                        'proteins', 'damaged', 'gene_specs'):
                    if after_ids[key] != before_ids[key]:
                        raise AssertionError(
                            '%s/%s changed %s identity' % (
                                device, case['label'], key,
                            )
                        )
                if (after_ids['genome_items'][:-1]
                        != before_ids['genome_items']
                        or cell.replication_copy is old_copy
                        or cell.replication_copy
                        is candidate.candidate_cell.replication_copy
                        or cell.genomes[-1]
                        is candidate.candidate_cell.genomes[-1]
                        or np.shares_memory(
                            cell.genomes[-1],
                            candidate.candidate_cell.genomes[-1],
                        )
                        or tuple(
                            (int(key), id(value))
                            for key, value in cell.gene_specs.items()
                        ) == old_gene_spec_ids):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' final topology/nonalias/cache refresh differs'
                    )
                paid = {
                    int(a4.a3.POOL_NUCLEOTIDE), int(a4.a3.POOL_ATP),
                }
                unchanged = [
                    index for index in range(int(a4.a3.POOL_COUNT))
                    if index not in paid
                ]
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                tlen = case['template_length']
                if (not np.array_equal(
                        np.asarray(cell.pools[unchanged], np.float64).view(
                            np.uint64,
                        ),
                        pools_before[unchanged].view(np.uint64),
                        )
                        or live_ledger.writes
                        or cell.mutation_events is not live_ledger
                        or tuple(live_ledger.items())
                        != case['mutation_before']
                        or world.rng.calls
                        or world.rng.bit_generator.state
                        != case['rng_before_state']
                        or world.rng is not candidate.live_rng
                        or float(world.dissipated_energy) != energy_before
                        or int(plan.requested_symbols[0])
                        != case['requested']
                        or int(plan.append_count[0]) != tlen
                        or int(plan.completed_lengths[0]) != tlen
                        or bool(plan.template_start_events[0])
                        or not bool(plan.completion_events[0])
                        or int(plan.substitution_events[0]) != 0
                        or float(plan.last_effective_error_rate[0])
                        != case['effective_error']
                        or float(plan.new_genome_lesions[0])
                        != case['new_lesion']
                        or int(evidence.low) != 0
                        or int(evidence.high) != 1
                        or int(evidence.selected_index) != 0
                        or int(evidence.call_count) != 1
                        or evidence.rng_before_state
                        != case['rng_before_state']
                        or evidence.rng_after_state
                        != case['rng_before_state']
                        or candidate.rng_before_state
                        != case['rng_before_state']
                        or candidate.rng_after_state
                        != case['rng_before_state']
                        or not candidate.consumed
                        or metadata.get('authority') != (
                            'A4.8c6-resident-mutation-free-start-'
                            'completion-plan-selection-only-pcg64-atomic-'
                            'commit'
                        )
                        or metadata.get('branch') != (
                            'inactive-template-start-completion-'
                            'deterministic'
                        )
                        or metadata.get('mutation_enabled') is not False
                        or metadata.get('device') != device
                        or int(metadata.get('amount', -1)) != tlen
                        or int(metadata.get('template_start_events', 0)) != 1
                        or int(metadata.get('completion_events', 0)) != 1
                        or int(metadata.get('requested_symbols', -1))
                        != case['requested']
                        or int(metadata.get('substitution_events', -1)) != 0
                        or int(metadata.get('rng_call_count', -1)) != 1
                        or metadata.get('work_performed') is not True):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' plan/evidence/receipt/ledger differs'
                    )
                source = candidate.source_binding.ragged
                synthetic = candidate.synthetic_binding.ragged
                final = candidate.fresh_binding.ragged
                if (int(source.sequence_count) != 1
                        or int(source.symbol_count) != tlen
                        or int(synthetic.sequence_count) != 3
                        or int(synthetic.symbol_count) != 2 * tlen
                        or int(final.sequence_count) != 2
                        or int(final.symbol_count) != 2 * tlen
                        or int(final.lesion_count)
                        != case['final_lesions']
                        or int(candidate.fresh_binding.state.genome_count[0])
                        != 2
                        or int(candidate.fresh_binding.state.
                               genome_material_symbols[0]) != 2 * tlen):
                    raise AssertionError(
                        case['label']
                        + ' source/synthetic/final Q/S/material differs'
                    )
                details.append('%s:%s:%d' % (
                    device, case['label'], tlen,
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 normal/missing-lesion x primed/unprimed; actual '
            'selection-only integers(0,1), state-neutral PCG64, direct final '
            'ledger identity/order/value plus integration no-write publish, '
            'paid completion/lesion/cycle/cache/novel age and '
            'source Q1/S=T -> synthetic Q3/S=2T -> final Q2/S=2T: '
            + ', '.join(details))


def test_a48c6_candidate_cpu_cuda_synthetic_plan_purity_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c6')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    # The c6 evidence must replay the literal high-level selection for all
    # four source/cache combinations before any resident completion work.
    selection_details = []
    for case in _a48c6_cases():
        generator = _A48C5TrackingGenerator(case['rng_before_state'])
        if (a48c5._formal066_single_template_selection(generator) != 0
                or generator.calls != [('integers', (0, 1), {})]
                or generator.bit_generator.state
                != case['rng_before_state']):
            raise AssertionError(case['label'] + ' selection primitive drifted')
        selection_details.append(case['label'])

    case = _a48c6_cases()[0]
    reference_plan = None
    reference_state = None
    reference_rng = None
    details = []
    for device in devices:
        direct = _a48c6_world_for_case(case)
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        world = _a48c6_world_for_case(
            case, track_rng=True, track_ledger=True,
        )
        cell = world.cells[0]
        live_ledger = cell.mutation_events
        scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
            case['capacity'], device,
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, case['dt'],
        )
        before_world = v3.pickle_clone(world.state_dict())
        before_ids = _a48c_identity_snapshot(world, cell)
        before_rng_object = world.rng
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = (
                scheduler._prepare_replication_start_completion_candidate(
                    world, cell, case['dt'], world.config,
                )
            )
            v3.assert_recursive_close(
                before_world, world.state_dict(), 0.0, 0.0,
                'a48c6.prepare_purity.' + device,
            )
            _, record = scheduler._record(cell)
            if (world.rng is not before_rng_object
                    or world.rng.bit_generator.state != before_rng
                    or world.rng.calls
                    or live_ledger.writes
                    or float(world.dissipated_energy) != before_energy
                    or _a48c_identity_snapshot(world, cell) != before_ids
                    or any(entry['event'] == 'replication_cpu'
                           for entry in record['events'])):
                raise AssertionError(device + ' prepare crossed claim/purity')

            synthetic = candidate.synthetic_cell
            prepared = candidate.candidate_cell
            tlen = case['template_length']
            if (synthetic is cell
                    or synthetic.pools is cell.pools
                    or synthetic.genomes is cell.genomes
                    or synthetic.replication_template is None
                    or synthetic.replication_template
                    is synthetic.genomes[0]
                    or np.shares_memory(
                        synthetic.replication_template,
                        synthetic.genomes[0],
                    )
                    or synthetic.replication_copy is cell.replication_copy
                    or prepared is cell or prepared is synthetic
                    or prepared.pools is cell.pools
                    or prepared.genomes is cell.genomes
                    or prepared.genomes[-1] is cell.genomes[0]
                    or np.shares_memory(
                        prepared.genomes[-1], cell.genomes[0],
                    )
                    or candidate.source_binding
                    is candidate.synthetic_binding
                    or candidate.synthetic_binding
                    is candidate.fresh_binding
                    or candidate.plan is candidate.host_replay
                    or candidate.plan is candidate.resident_plan
                    or candidate.live_rng is not world.rng
                    or candidate.rng_before_state != before_rng
                    or candidate.rng_after_state != before_rng
                    or candidate.evidence.rng_before_state != before_rng
                    or candidate.evidence.rng_after_state != before_rng
                    or int(candidate.evidence.call_count) != 1):
                raise AssertionError(device + ' candidate authority/alias differs')

            independent = a44.paid_replication_completion_plan(
                candidate.synthetic_binding, case['dt'], world.config,
            )
            if (candidate.evidence.state_dict()
                    != candidate.evidence_state
                    or a48c._host_binding_artifact_snapshot(
                        candidate.source_binding,
                    ) != candidate.source_artifacts
                    or a48c._host_binding_artifact_snapshot(
                        candidate.synthetic_binding,
                    ) != candidate.synthetic_artifacts
                    or a48c._host_binding_artifact_snapshot(
                        candidate.fresh_binding,
                    ) != candidate.fresh_artifacts
                    or a48c._resident_artifact_snapshot(
                        candidate.resident_binding, None,
                        candidate.resident_plan, device,
                    ) != candidate.resident_artifacts
                    or a48c6._host_completion_artifacts(
                        candidate.evidence, candidate.host_replay,
                        candidate.plan,
                    ) != candidate.host_plan_artifacts
                    or not a48c._plan_states_bit_exact(
                        candidate.plan, candidate.host_replay,
                    )
                    or not a48c._plan_states_bit_exact(
                        candidate.plan, independent,
                    )
                    or not a48c._plan_states_bit_exact(
                        candidate.resident_plan.to_numpy(), candidate.plan,
                    )):
                raise AssertionError(device + ' retained artifact association differs')
            a48c6._start_completion_plan_scope(
                candidate.source_binding, candidate.synthetic_binding,
                candidate.plan, case['dt'], world.config,
                candidate.evidence,
            )
            _assert_completion_cpu_parity(
                candidate.plan, [synthetic], [prepared],
                'a48c6.candidate.' + device,
            )
            _a48b_assert_cell_matches(
                direct.cells[0], prepared,
                'a48c6.candidate_direct.' + device,
                atol=a48c6.REPLICATION_ORACLE_ATOL,
            )
            if (int(candidate.source_binding.ragged.sequence_count) != 1
                    or int(candidate.synthetic_binding.ragged.sequence_count)
                    != 3
                    or int(candidate.synthetic_binding.ragged.symbol_count)
                    != 2 * tlen
                    or int(candidate.fresh_binding.ragged.sequence_count) != 2
                    or int(candidate.fresh_binding.ragged.symbol_count)
                    != 2 * tlen):
                raise AssertionError(device + ' source/synthetic/fresh Q/S differs')

            private_genome = prepared.genomes[-1]
            private_copy = prepared.replication_copy
            scheduler._commit_replication_start_completion_candidate(
                world, cell, case['dt'], world.config, candidate,
            )
            _a48c6_assert_completion_oracle(
                direct, direct.cells[0], world, cell,
                'a48c6.commit.' + device,
            )
            if (cell.genomes[-1] is private_genome
                    or np.shares_memory(cell.genomes[-1], private_genome)
                    or cell.replication_copy is private_copy
                    or cell.mutation_events is not live_ledger
                    or live_ledger.writes
                    or tuple(live_ledger.items())
                    != case['mutation_before']):
                raise AssertionError(device + ' publish alias/ledger differs')
            committed = v3.pickle_clone(cell.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48c6.A4ReplicationStartCompletionCommitError,
                lambda: scheduler._commit_replication_start_completion_candidate(
                    world, cell, case['dt'], world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, cell.state_dict(), 0.0, 0.0,
                'a48c6.one_shot.' + device,
            )
            if (not candidate.consumed
                    or world.rng is not before_rng_object
                    or world.rng.bit_generator.state != committed_rng
                    or world.rng.calls or live_ledger.writes):
                raise AssertionError(device + ' one-shot authority differs')
            if reference_plan is None:
                reference_plan = candidate.plan.clone()
                reference_state = v3.pickle_clone(cell.state_dict())
                reference_rng = copy.deepcopy(committed_rng)
            else:
                if not a48c._plan_states_bit_exact(
                        candidate.plan, reference_plan):
                    raise AssertionError('CPU/CUDA completion plans differ')
                v3.assert_recursive_close(
                    reference_state, cell.state_dict(),
                    a48c6.REPLICATION_ORACLE_ATOL, 0.0,
                    'a48c6.cpu_cuda_state',
                )
                if committed_rng != reference_rng:
                    raise AssertionError('CPU/CUDA committed PCG64 differs')
            pointer_count = sum(
                len(values) for values in
                candidate.resident_artifacts['data_ptrs'].values()
            )
            details.append('%s:%d-pointers' % (device, pointer_count))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('selection primitive %s; CPU/CUDA prepare purity, original->'
            'synthetic binding proof, public resident completion plan/'
            'independent NumPy oracle, device pointer/version/content, final '
            'fresh binding, same Generator and one-shot PASS: %s' % (
                '/'.join(selection_details), '/'.join(details),
            ))


def _a48c6_assert_preclaim_failure(
        state, cell_id, dt, capacity, drop_lesions=False,
        scheduler_type=None, device='cpu', label='scope'):
    case = {'state': state, 'drop_lesions': bool(drop_lesions)}
    world = _a48c6_world_for_case(case)
    cell = next(
        item for item in world.cells if int(item.cell_id) == int(cell_id)
    )
    scheduler_type = (
        scheduler_type
        or a48c6.A4ReplicationStartCompletionEventScheduler
    )
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(scheduler, world, cell, dt)
    before = v3.pickle_clone(world.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng_object = world.rng
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    before_receipts = len(scheduler._receipts)
    try:
        _assert_raises(
            (a48c6.A4ReplicationStartCompletionCommitError,
             a48c5.A4ReplicationMutationFreeStartCommitError,
             a48c4.A4ReplicationStartCommitError,
             a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, dt, world.config,
            ),
        )
        expected_world = getattr(
            scheduler, 'expected_world_state', before,
        )
        expected_rng = getattr(
            scheduler, 'expected_rng_state', before_rng,
        )
        v3.assert_recursive_close(
            expected_world, world.state_dict(), 0.0, 0.0,
            'a48c6.preclaim.' + label,
        )
        _, record = scheduler._record(cell)
        if (world.rng is not before_rng_object
                or world.rng.bit_generator.state != expected_rng
                or float(world.dissipated_energy) != before_energy
                or _a48c_identity_snapshot(world, cell) != before_ids
                or len(scheduler._receipts) != before_receipts
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c6_capacity_trust_scope_rollback_and_exact_once():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c6 trust tests')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    base = _a48c6_cases()[0]

    capacity_details = []
    direct = _a48c6_world_for_case(base)
    direct.cells[0]._replicate_genome(direct, base['dt'], direct.config)
    for label, key in (
            ('Q', 'max_sequences'), ('S', 'max_symbols'),
            ('W', 'max_sequence_symbols'),
            ('P', 'max_proteins_per_cell')):
        exact = a4.GPU068A4Config(**base['capacity_values'])
        for device in devices:
            world = _a48c6_world_for_case(base)
            cell = world.cells[0]
            scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
                exact, device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, base['dt'], world.config,
                )
                _a48c6_assert_completion_oracle(
                    direct, direct.cells[0], world, cell,
                    'a48c6.capacity.%s.%s' % (label, device),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append('%s@%s' % (label, device))
        short = dict(base['capacity_values'])
        short[key] -= 1
        _a48c6_assert_preclaim_failure(
            base['state'], base['cell_id'], base['dt'],
            a4.GPU068A4Config(**short),
            drop_lesions=base['drop_lesions'],
            label='capacity_' + label + '_one_short',
        )
    del scheduler, world, cell, direct
    gc.collect()

    trust_details = []

    def assert_tamper_failure(scheduler_type, label, test_devices=('cpu',)):
        for device in test_devices:
            world = _a48c6_world_for_case(base)
            cell = world.cells[0]
            scheduler = scheduler_type(base['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            before_world = v3.pickle_clone(world.state_dict())
            before_ids = _a48c_identity_snapshot(world, cell)
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_receipts = len(scheduler._receipts)
            try:
                _assert_raises(
                    (a48c6.A4ReplicationStartCompletionCommitError,
                     a4.A4Error, a3s.A3SchedulerError),
                    lambda: scheduler.cpu_replication(
                        world, cell, base['dt'], world.config,
                    ),
                )
                expected_world = getattr(
                    scheduler, 'expected_world_state', before_world,
                )
                expected_rng = getattr(
                    scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, world.state_dict(), 0.0, 0.0,
                    'a48c6.trust.%s.%s' % (device, label),
                )
                _, record = scheduler._record(cell)
                if (world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(world, cell) != before_ids
                        or len(scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim' % (device, label)
                    )
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
            del scheduler, cell, world
            gc.collect()

    class TamperSource(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.source_binding.ragged.symbols[0] ^= np.uint8(1)
            return candidate

    class TamperEvidence(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            object.__setattr__(candidate.evidence, 'selected_index', 1)
            return candidate

    class TamperSyntheticCell(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.synthetic_cell.replication_template[0] ^= np.uint8(1)
            return candidate

    class TamperSyntheticBinding(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.synthetic_binding.ragged.symbols[0] ^= np.uint8(1)
            return candidate

    class TamperHostOracle(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.completed_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperReadbackPlan(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.completed_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperResidentSource(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentPlan(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.completed_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    class SwapResidentPointer(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.completed_symbols = (
                candidate.resident_plan.completed_symbols.clone()
            )
            return candidate

    class TamperCandidate(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.genomes[-1][0] ^= np.uint8(1)
            return candidate

    class AliasCandidateGenome(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.genomes[-1] = cell.genomes[0]
            return candidate

    class TamperFreshBinding(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class TamperSchedulerConfig(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = 'cuda' if self.a4_device == 'cpu' else 'cpu'
            return candidate

    class TamperLiveConfig(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                float(world.config.mutation_rate), np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveSource(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.genomes[0][0] ^= np.uint8(1)
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveRng(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperCandidateRng(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _replication_start_completion_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_state,
            )
            generator.random()
            candidate.rng_after_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    for scheduler_type, label in (
            (TamperSource, 'source_binding'),
            (TamperEvidence, 'selection_evidence'),
            (TamperSyntheticCell, 'synthetic_cell'),
            (TamperSyntheticBinding, 'synthetic_binding'),
            (TamperHostOracle, 'host_oracle'),
            (TamperReadbackPlan, 'readback_plan'),
            (SwapResidentPointer, 'resident_pointer'),
            (TamperCandidate, 'candidate_cell'),
            (AliasCandidateGenome, 'candidate_alias'),
            (TamperFreshBinding, 'fresh_binding'),
            (TamperSchedulerConfig, 'scheduler_config_device'),
            (TamperLiveConfig, 'live_config'),
            (TamperLiveSource, 'live_source'),
            (TamperLiveRng, 'live_rng'),
            (TamperCandidateRng, 'candidate_rng')):
        assert_tamper_failure(scheduler_type, label)
    for scheduler_type, label in (
            (TamperResidentSource, 'resident_source_data'),
            (TamperResidentPlan, 'resident_plan_data')):
        assert_tamper_failure(
            scheduler_type, label, test_devices=tuple(devices),
        )

    # Wrong live associations must neither consume nor publish the candidate.
    world = _a48c6_world_for_case(base)
    cell = world.cells[0]
    scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, cell, base['dt'])
    before = v3.pickle_clone(world.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = scheduler._prepare_replication_start_completion_candidate(
            world, cell, base['dt'], world.config,
        )
        wrong_inputs = (
            ('cell', copy.deepcopy(cell), base['dt'], world.config),
            ('dt', cell, np.nextafter(base['dt'], np.inf), world.config),
            ('config', cell, base['dt'], copy.deepcopy(world.config)),
        )
        for label, wrong_cell, wrong_dt, wrong_config in wrong_inputs:
            _assert_raises(
                a48c6.A4ReplicationStartCompletionCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler._commit_replication_start_completion_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError('wrong ' + label + ' consumed candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c6.wrong_cell_dt_config',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong inputs changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)
    del candidate, scheduler, world, cell
    gc.collect()

    scope_cases = []

    def add_scope(label, mutate, dt=base['dt']):
        scoped = _a48c6_world_for_case(base)
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scoped.last_step_material_residual = 0.0
        scope_cases.append((
            label, v3.pickle_clone(scoped.state_dict()),
            int(scoped_cell.cell_id), float(dt),
        ))

    add_scope('disabled', lambda world, cell: setattr(
        world.config, 'genome_replication', False))
    add_scope('negative_atp', lambda world, cell: cell.pools.__setitem__(
        a4.a3.POOL_ATP, -1e-12))

    def no_genome(world, cell):
        cell.genomes = []
        cell.genome_lesions = []
        cell._refresh_gene_cache()

    add_scope('no_genome', no_genome)

    def two_genomes(world, cell):
        cell.genomes.append(np.asarray(cell.genomes[0], np.uint8).copy())
        cell.genome_lesions.append(0.2)
        cell._refresh_gene_cache()

    add_scope('two_genomes', two_genomes)

    def remove_replicase(world, cell):
        world.config.external_replicase = False
        for fingerprint, spec in list(cell.gene_specs.items()):
            if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
                cell.proteins.pop(fingerprint, None)
        cell._sync_protein_pool()

    add_scope('replicase_gate', remove_replicase)
    add_scope('mutation_enabled_completion', lambda world, cell: setattr(
        world.config, 'mutation', True))

    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for label, state, cell_id, dt in scope_cases:
            _a48c6_assert_preclaim_failure(
                state, cell_id, dt, a4.GPU068A4Config(),
                label='scope_' + label,
            )

        dead = _a48c6_world_for_case(base)
        dead_cell = dead.cells[0]
        dead_scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
            base['capacity'], 'cpu',
        )
        _a48c_begin_replication_direct(
            dead_scheduler, dead, dead_cell, base['dt'],
        )
        dead_cell.alive = False
        dead_before = v3.pickle_clone(dead.state_dict())
        dead_ids = _a48c_identity_snapshot(dead, dead_cell)
        dead_rng = copy.deepcopy(dead.rng.bit_generator.state)
        _assert_raises(
            a48c6.A4ReplicationStartCompletionCommitError,
            lambda: dead_scheduler.cpu_replication(
                dead, dead_cell, base['dt'], dead.config,
            ),
        )
        v3.assert_recursive_close(
            dead_before, dead.state_dict(), 0.0, 0.0,
            'a48c6.scope_dead',
        )
        _, dead_record = dead_scheduler._record(dead_cell)
        if (dead.rng.bit_generator.state != dead_rng
                or _a48c_identity_snapshot(dead, dead_cell) != dead_ids
                or any(entry['event'] == 'replication_cpu'
                       for entry in dead_record['events'])):
            raise AssertionError('dead scope crossed preclaim boundary')
        _a48_abort_direct(dead_scheduler, dead_cell)
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('excluded c6 scope used CPU fallback')

    duplicate = _a48c6_world_for_case(base)
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, base['dt'],
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, base['dt'], duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, base['dt'], duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate.state_dict(), 0.0, 0.0,
        'a48c6.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate completion changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = _a48c6_world_for_case(base)
    order_cell = order.cells[0]
    order_scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, base['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, base['dt'], order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order.state_dict(), 0.0, 0.0,
        'a48c6.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order completion changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(
            a48c6.A4ReplicationStartCompletionEventScheduler):
        def _publish_replication_start_completion_candidate(
                self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_start_completion_candidate(
                world, cell, candidate,
            )
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.genomes[0][0] ^= np.uint8(1)
            cell.genomes[-1][0] ^= np.uint8(1)
            cell.genome_lesions[-1] += 0.25
            cell.replication_template = np.asarray((1,), np.uint8)
            cell.replication_copy.append(7)
            cell.replication_copy = list(cell.replication_copy)
            cell.replication_cycles += 3
            cell.last_replication_symbols += 2
            cell.last_effective_error_rate += 0.125
            cell.cumulative_proofreading_atp += 0.5
            first_spec = next(iter(cell.gene_specs.values()))
            first_spec['parameter'] = int(first_spec['parameter']) + 1
            cell.mutation_events['a48c6_extra_sentinel'] += 1
            cell.novel_path_first_age = -3.0
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c6 publish failure')

    rollback = _a48c6_world_for_case(base)
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(base['capacity'], 'cpu')
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, base['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    old_copy_object = rollback_cell.replication_copy
    old_pools_object = rollback_cell.pools
    old_genomes_object = rollback_cell.genomes
    old_lesions_object = rollback_cell.genome_lesions
    old_events_object = rollback_cell.mutation_events
    old_gene_specs_object = rollback_cell.gene_specs
    rollback_rng_object = rollback.rng
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, base['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c6 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c6.publish_rollback',
    )
    if (rollback_cell.replication_template is not None
            or rollback_cell.replication_copy is not old_copy_object
            or rollback_cell.replication_copy != []
            or rollback_cell.pools is not old_pools_object
            or rollback_cell.genomes is not old_genomes_object
            or rollback_cell.genome_lesions is not old_lesions_object
            or rollback_cell.mutation_events is not old_events_object
            or rollback_cell.gene_specs is not old_gene_specs_object
            or rollback.rng is not rollback_rng_object
            or rollback.rng.bit_generator.state != rollback_rng
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids):
        raise AssertionError(
            'c6 rollback lost inactive source identity/state'
        )
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if entry['event'] != 'replication_cpu' or receipt['status'] != 'aborted':
        raise AssertionError('publish failure did not leave aborted receipt')
    return ('Q/S/W/P exact CPU/CUDA + each one-short (synthetic S=2T '
            'minimum); %d source/evidence/synthetic/host/resident/final/live '
            'trust checks; wrong inputs; %d no-fallback scopes; duplicate/'
            'order; None-template + old-copy/pools/genomes/lesions/cache/'
            'ledger/RNG identity rollback PASS (%s)' % (
                len(trust_details), len(scope_cases) + 1,
                '/'.join(capacity_details),
            ))


def test_a48c6_inherited_c5_c4_c3_c2_c1_save_clone_successor_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c6')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    delegated_cases = []
    c1_world, c1_cells, _, c1_dt = _a45_substitution_fixture(seed=12601)
    _a48c_reduce_world(c1_world, [c1_cells[0]])
    delegated_cases.append({
        'label': 'c1-active-noncompletion',
        'state': v3.pickle_clone(c1_world.state_dict()),
        'dt': float(c1_dt), 'capacity': a4.GPU068A4Config(),
        'authority': 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'drop_lesions': False,
    })

    c2_state, _, c2_dt = _a48c2_completion_case(seed=12602, row=0)
    c2_before_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after_world = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after_world.cells[0]._replicate_genome(
        c2_after_world, c2_dt, c2_after_world.config,
    )
    c2_capacity = a4.GPU068A4Config(**_a48c2_capacity_for_transition(
        c2_before_world.cells[0], c2_after_world.cells[0],
    ))
    delegated_cases.append({
        'label': 'c2-mutation-free-completion',
        'state': c2_state, 'dt': float(c2_dt),
        'capacity': c2_capacity,
        'authority': (
            'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit'
        ),
        'drop_lesions': False,
    })

    c3_case = _a48c3_completion_mutation_cases()[0]
    delegated_cases.append({
        'label': 'c3-mutation-completion',
        'state': c3_case['state'], 'dt': float(c3_case['dt']),
        'capacity': c3_case['integration_capacity'],
        'authority': (
            'A4.8c3-resident-completion-mutation-plan-atomic-'
            'cpu-cell-rng-commit'
        ),
        'drop_lesions': False,
    })

    c4_case = _a48c4_cases()[0]
    delegated_cases.append({
        'label': 'c4-mutation-start-noncompletion',
        'state': c4_case['state'], 'dt': float(c4_case['dt']),
        'capacity': c4_case['capacity'],
        'authority': (
            'A4.8c4-resident-template-start-plan-atomic-'
            'cpu-cell-pcg64-commit'
        ),
        'drop_lesions': bool(c4_case.get('drop_lesions', False)),
    })

    c5_case = _a48c5_cases()[0]
    delegated_cases.append({
        'label': 'c5-mutation-free-start-noncompletion',
        'state': c5_case['state'], 'dt': float(c5_case['dt']),
        'capacity': c5_case['capacity'],
        'authority': (
            'A4.8c5-resident-mutation-free-template-start-plan-'
            'atomic-cpu-cell-pcg64-commit'
        ),
        'drop_lesions': bool(c5_case.get('drop_lesions', False)),
    })

    delegated = []
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in delegated_cases:
            for device in devices:
                provisional = {
                    'state': case['state'],
                    'drop_lesions': case['drop_lesions'],
                }
                direct = _a48c6_world_for_case(provisional)
                world = _a48c6_world_for_case(provisional)
                direct.cells[0]._replicate_genome(
                    direct, case['dt'], direct.config,
                )
                scheduler = (
                    a48c6.A4ReplicationStartCompletionEventScheduler(
                        case['capacity'], device,
                    )
                )
                _a48c_begin_replication_direct(
                    scheduler, world, world.cells[0], case['dt'],
                )
                try:
                    scheduler.cpu_replication(
                        world, world.cells[0], case['dt'], world.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], world.cells[0],
                        'a48c6.delegate.%s.%s' % (
                            device, case['label'],
                        ),
                        atol=a48c6.REPLICATION_ORACLE_ATOL,
                    )
                    entry = _a48c_replication_entry(
                        scheduler, world.cells[0],
                    )
                    if (direct.rng.bit_generator.state
                            != world.rng.bit_generator.state
                            or float(direct.dissipated_energy)
                            != float(world.dissipated_energy)
                            or entry['metadata'].get('authority')
                            != case['authority']):
                        raise AssertionError(
                            device + '/' + case['label']
                            + ' inherited authority differs'
                        )
                    delegated.append(device + ':' + case['label'])
                finally:
                    _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('an inherited c6 branch used CPU fallback')

    own = _a48c6_cases()[0]
    direct = _a48c6_world_for_case(own)
    direct.cells[0]._replicate_genome(direct, own['dt'], direct.config)
    world = _a48c6_world_for_case(own)
    scheduler = a48c6.A4ReplicationStartCompletionEventScheduler(
        own['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(scheduler, world, world.cells[0], own['dt'])
    original_c5 = (
        a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication
    )

    def c5_dispatch_bomb(*args, **kwargs):
        raise AssertionError('owned c6 completion delegated to c5')

    a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication = (
        c5_dispatch_bomb
    )
    try:
        scheduler.cpu_replication(
            world, world.cells[0], own['dt'], world.config,
        )
        _a48c6_assert_completion_oracle(
            direct, direct.cells[0], world, world.cells[0],
            'a48c6.owned_no_c5_dispatch',
        )
    finally:
        a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication = (
            original_c5
        )
        _a48_abort_direct(scheduler, world.cells[0])

    # Formal066 world.step clamps its biology substep to 0.1.  Use a legal
    # one-symbol genome here so the full scheduler path reaches c6 in that
    # one substep; the fixed T=576/496 scientific matrix remains above.
    successor_world = _a48c6_world_for_case(own)
    successor_cell = successor_world.cells[0]
    successor_cell.genomes = [np.asarray((3,), dtype=np.uint8)]
    successor_cell.genome_lesions = [0.2]
    successor_cell.replication_template = None
    successor_cell.replication_template_lesion = 0.0
    successor_cell.replication_copy = []
    successor_cell.replication_fractional = 0.0
    successor_cell.pools[a4.a3.POOL_NUCLEOTIDE] = 10.0
    successor_cell.pools[a4.a3.POOL_ATP] = 10.0
    successor_cell.age = 0.0
    successor_cell.novel_path_first_age = None
    successor_cell._refresh_gene_cache()
    successor_world.initial_total_material = successor_world.total_material()
    successor_world.last_step_material_residual = 0.0
    successor_state = v3.pickle_clone(successor_world.state_dict())
    successor_dt = 0.1
    probe = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(successor_state),
    )
    probe.cells[0]._replicate_genome(probe, successor_dt, probe.config)
    if (len(probe.cells[0].genomes) != 2
            or probe.cells[0].replication_template is not None):
        raise AssertionError('A4.8c6 full-step completion probe drifted')
    roomy = a4.GPU068A4Config(
        max_cells=4, max_sequences=12, max_symbols=8192,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )

    seed_hybrid = _a48c6_hybrid_from_state(
        successor_state, roomy, 'cpu',
    )
    twin = seed_hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c6.pkl')
        seed_hybrid.save(path)
        restored = a48c6.Hybrid066WorldA4ReplicationStartCompletion.load(path)
        for value in (seed_hybrid, twin, restored):
            if (type(value)
                    is not a48c6.Hybrid066WorldA4ReplicationStartCompletion
                    or type(value.scheduler)
                    is not a48c6.A4ReplicationStartCompletionEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != roomy
                    or value.scheduler.replication_start_completion_integration_state()
                    != {
                        'schema': a48c6.SCHEMA_VERSION,
                        'config': a48._config_state(roomy),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c6 save/clone authority was lost')
        expected = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(successor_state),
        )
        expected.step(successor_dt)
        successor_details = []
        for index, value in enumerate((seed_hybrid, twin, restored)):
            original_c5 = (
                a48c5.A4ReplicationMutationFreeStartEventScheduler.
                cpu_replication
            )
            a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication = (
                c5_dispatch_bomb
            )
            try:
                receipt = value.step(successor_dt)
            finally:
                a48c5.A4ReplicationMutationFreeStartEventScheduler.cpu_replication = (
                    original_c5
                )
            v3._assert_world_pair(
                expected, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c6.successor.%d' % index,
            )
            record = receipt['cells'][0]
            names = [entry['event'] for entry in record['events']]
            by_name = {entry['event']: entry for entry in record['events']}
            if (names.count('replication_cpu') != 1
                    or names.count('genome_hydrolysis_cpu_rng') != 1
                    or not names.index('translation_cpu')
                    < names.index('replication_cpu')
                    < names.index('surface_assembly')
                    < names.index('genome_hydrolysis_cpu_rng')
                    < names.index('motion')
                    or by_name['replication_cpu']['metadata'].get('authority')
                    != (
                        'A4.8c6-resident-mutation-free-start-completion-'
                        'plan-selection-only-pcg64-atomic-commit'
                    )
                    or by_name['replication_cpu']['metadata'].get(
                        'work_performed') is not True):
                raise AssertionError('c6 successor order differs')
            successor_details.append(str(index))

    active = _a48c6_hybrid_from_state(successor_state, roomy, 'cpu')
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active',
            '_a4_replication_start_commit_active',
            '_a4_replication_mutation_free_start_commit_active',
            '_a4_replication_start_completion_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    expected_status = (
        'a4.8c6-mutation-free-inactive-one-genome-template-start-'
        'same-call-completion-synthetic-active-public-plan-selection-only-'
        'pcg64-resident-atomic-commit-a4.8c5-c4-c3-c2-c1-inherited-'
        'other-inactive-early-noop-fail-closed'
    )
    signature = inspect.signature(
        a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication,
    )
    prior_names = '\n'.join(fn.__name__ for fn in TESTS[:74])
    c6_names = tuple(fn.__name__ for fn in TESTS[74:78])
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationStartCompletionCommitCandidate'
            in set(a48c6.__all__)
            or '_A4SingleTemplateCompletionSelectionEvidence'
            in set(a48c6.__all__)
            or not issubclass(
                a48c6.A4ReplicationStartCompletionEventScheduler,
                a48c5.A4ReplicationMutationFreeStartEventScheduler,
            )
            or not issubclass(
                a48c6.Hybrid066WorldA4ReplicationStartCompletion,
                a48c5.Hybrid066WorldA4ReplicationMutationFreeStart,
            )
            or a48c6.PORT_STATUS.get('genome_replication')
            != expected_status
            or a48c6.FULL_GPU_WORLD_STEP is not False
            or a48c6.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) < 78
            or c6_names != (
                'test_a48c6_mutation_free_inactive_start_completion_formal066_oracle',
                'test_a48c6_candidate_cpu_cuda_synthetic_plan_purity_and_fresh_binding',
                'test_a48c6_capacity_trust_scope_rollback_and_exact_once',
                'test_a48c6_inherited_c5_c4_c3_c2_c1_save_clone_successor_and_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A48C6_TEST_NAMES_SHA256):
        raise AssertionError('A4.8c6 API/scope/74-test authority differs')
    expected_hashes = dict(PROMOTED_A3_SHA256)
    expected_hashes.update(_A48C6_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_integration.py'
    ] = _A48C6_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('active c1/c2/c3 + inactive mutation c4 + mutation-free '
            'noncompletion c5 CPU/CUDA delegates %s; owned completion no c5/'
            'frozen fallback; pre-step save/load/clone successor %s; c1-c6 '
            'active/pending reject; first74/A3/A4.5b/c1-c6 hash/'
            'full_gpu=false PASS' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A48C7_TEST_NAMES_SHA256 = (
    '11575e2e6642db726cd4d4da633ed3a4a6d6eaccae195ffe1bc7d681cb9c33f7'
)

_A48C7_TEST_AST_SHA256 = (
    'a73f0fceadedc8029704574d9861831d033720fe5f59777bfabc4c4c59ecd087'
)

_A48C7_PRIOR_CORE_SHA256 = dict(_A48C6_PRIOR_CORE_SHA256)
_A48C7_PRIOR_CORE_SHA256[
    'src/0_6_8/'
    'SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_integration.py'
] = _A48C6_CORE_SHA256

# Filled from the audited final c7 bytes before canonical insertion.
_A48C7_CORE_SHA256 = (
    '91c1c8ae7e3f623100353d2e51a8a12b96454d2e08de262031c1d97b8f5de7fb'
)

_A48C7_EXPECTED_CASES = {
    'deletion-refund': {
        'requested': 4083, 'template_length': 576, 'final_length': 574,
        'substitution': 179, 'structural': (0, 2, 0, 0, 0),
        'padding': 0, 'material_delta': -2,
        'capacity': (1, 3, 1152, 576, 46), 'rng_call_count': 763,
        'rng_before_cache': (0, 0),
        'rng_after_cache': (1, 1669040376),
        'new_lesion': 10.941200030393253,
    },
    'expansion': {
        'requested': 4083, 'template_length': 576, 'final_length': 592,
        'substitution': 194, 'structural': (0, 0, 16, 0, 1),
        'padding': 0, 'material_delta': 16,
        'capacity': (1, 3, 1168, 592, 42), 'rng_call_count': 781,
        'rng_before_cache': (0, 0),
        'rng_after_cache': (1, 4106076383),
        'new_lesion': 11.277114991615834,
    },
    'padding-budget-trim': {
        'requested': 166, 'template_length': 3, 'final_length': 3,
        'substitution': 0, 'structural': (3, 0, 0, 1, 0),
        'padding': 26, 'material_delta': 0,
        'capacity': (1, 3, 6, 3, 31), 'rng_call_count': 14,
        'rng_before_cache': (0, 0),
        'rng_after_cache': (0, 2655235611),
        'new_lesion': 0.181072,
    },
    'missing-lesion-primed': {
        'requested': 5972, 'template_length': 496, 'final_length': 496,
        'substitution': 133, 'structural': (0, 0, 0, 0, 1),
        'padding': 0, 'material_delta': 0,
        'capacity': (1, 3, 992, 496, 41), 'rng_call_count': 638,
        'rng_before_cache': (1, 925098552),
        'rng_after_cache': (1, 183994996),
        'new_lesion': 8.21351039154056,
    },
}


def _a48c7_world_for_case(case, track_rng=False, track_ledger=False):
    return _a48c6_world_for_case(
        case, track_rng=track_rng, track_ledger=track_ledger,
    )


def _a48c7_cases():
    """Four fixed continuous selection/append/structural PCG64 chains."""
    bases = {case['label']: case for case in _a48c6_cases()}
    definitions = (
        ('deletion-refund', 'normal-unprimed', 13039, False, False),
        ('expansion', 'normal-unprimed', 13122, False, False),
        ('padding-budget-trim', 'normal-unprimed', 15335, False, True),
        (
            'missing-lesion-primed', 'missing-lesion-unprimed',
            16001, True, False,
        ),
    )
    result = []
    structural_names = tuple(a44.STRUCTURAL_EVENT_NAMES)
    for offset, (label, base_label, seed, primed, short) in enumerate(
            definitions):
        base = bases[base_label]
        world = _a48c6_world_for_case(base)
        cell = world.cells[0]
        world.config.mutation = True
        if short:
            cell.genomes = [np.asarray((3, 4, 5), dtype=np.uint8)]
            cell.genome_lesions = [0.2]
            cell.pools[a4.a3.POOL_NUCLEOTIDE] = (
                3.0 * float(a4.g2.MONOMER_MASS)
            )
            cell.pools[a4.a3.POOL_ATP] = 2.0
            cell._refresh_gene_cache()
        else:
            cell.pools[a4.a3.POOL_NUCLEOTIDE] = 2.0
            cell.pools[a4.a3.POOL_ATP] = 2.0
        cell.mutation_events['a48c7_extra_sentinel'] = 501 + offset
        world.rng = np.random.Generator(np.random.PCG64(seed))
        if primed:
            world.rng.integers(0, int(a4.g2.ALPHABET_SIZE) - 1)
        world.initial_total_material = world.total_material()
        world.last_step_material_residual = 0.0
        state = v3.pickle_clone(world.state_dict())
        provisional = {
            'state': state,
            'drop_lesions': bool(base.get('drop_lesions', False)),
        }

        direct = _a48c7_world_for_case(provisional, track_rng=True)
        before = copy.deepcopy(direct.cells[0])
        mutation_before = copy.deepcopy(before.mutation_events)
        rng_before = copy.deepcopy(direct.rng.bit_generator.state)
        direct.cells[0]._replicate_genome(direct, 100.0, direct.config)
        after = direct.cells[0]

        selection = _A48C5TrackingGenerator(rng_before)
        if (a48c5._formal066_single_template_selection(selection) != 0
                or selection.calls != [('integers', (0, 1), {})]):
            raise AssertionError(label + ' literal selection call drifted')
        rng_after_selection = copy.deepcopy(
            selection.bit_generator.state,
        )
        synthetic = _a48c6_synthetic_active_cell(before)
        capacity_values = _a48c2_capacity_for_transition(
            synthetic, after,
        )
        capacity = a4.GPU068A4Config(**capacity_values)
        packed_world = _a48c7_world_for_case(provisional)
        source_ragged, _, source_binding = _paid_replication_binding(
            [packed_world.cells[0]], packed_world.config, capacity,
        )
        synthetic_ragged, _, synthetic_binding = _paid_replication_binding(
            [synthetic], packed_world.config, capacity,
        )
        tape = a44.prepare_completion_mutation_rng_tape(
            synthetic_binding, 100.0, packed_world.config,
            rng_after_selection,
        )
        plan = a44.paid_replication_completion_mutation_plan(
            synthetic_binding, 100.0, packed_world.config, tape,
        )
        final_ragged = a4.FullFidelityA4GenomeAdapter(
            capacity,
        ).pack_cells([after])

        # Independent frozen replay begins after selection from the synthetic
        # active state.  Its exact high-level call signatures must equal the
        # tail of the one-call inactive Formal066 trace.
        replay_world = _a48c7_world_for_case(provisional)
        replay_world.rng = _A48C5TrackingGenerator(rng_after_selection)
        replay_cell = copy.deepcopy(synthetic)
        replay_world.cells = [replay_cell]
        replay_cell._replicate_genome(
            replay_world, 100.0, replay_world.config,
        )
        _a48b_assert_cell_matches(
            after, replay_cell, 'a48c7.fixture.replay.' + label,
            atol=a48c7.REPLICATION_ORACLE_ATOL,
        )

        expected = _A48C7_EXPECTED_CASES[label]
        structural = tuple(
            int(after.mutation_events[name])
            - int(mutation_before[name])
            for name in structural_names
        )
        substitution = (
            int(after.mutation_events['substitution'])
            - int(mutation_before['substitution'])
        )
        capacity_tuple = tuple(
            int(capacity_values[name]) for name in (
                'max_cells', 'max_sequences', 'max_symbols',
                'max_sequence_symbols', 'max_proteins_per_cell',
            )
        )
        before_cache = (
            int(rng_before['has_uint32']), int(rng_before['uinteger']),
        )
        rng_after = copy.deepcopy(direct.rng.bit_generator.state)
        after_cache = (
            int(rng_after['has_uint32']), int(rng_after['uinteger']),
        )
        template_length = len(before.genomes[0])
        final_length = len(after.genomes[-1])
        values = {
            'requested': int(plan.requested_symbols[0]),
            'template_length': template_length,
            'final_length': final_length,
            'substitution': substitution,
            'structural': structural,
            'padding': int(tape.padding_count[0]),
            'material_delta': int(plan.material_delta_symbols[0]),
            'capacity': capacity_tuple,
            'rng_call_count': len(direct.rng.calls),
            'rng_before_cache': before_cache,
            'rng_after_cache': after_cache,
        }
        if (values != {
                key: expected[key] for key in values
                }
                or abs(
                    float(after.genome_lesions[-1])
                    - float(expected['new_lesion'])
                ) > 1e-15
                or direct.rng.calls[:1]
                != [('integers', (0, 1), {})]
                or direct.rng.calls[1:] != replay_world.rng.calls
                or tape.rng_before_state != rng_after_selection
                or tape.rng_after_state != rng_after
                or len(direct.rng.calls)
                != 1 + _a48c3_completion_mutation_rng_call_count(tape)
                or int(source_ragged.sequence_count) != 1
                or int(source_ragged.symbol_count) != template_length
                or int(synthetic_ragged.sequence_count) != 3
                or int(synthetic_ragged.symbol_count) != 2 * template_length
                or int(final_ragged.sequence_count) != 2
                or int(final_ragged.symbol_count)
                != template_length + final_length
                or not np.array_equal(
                    plan.final_symbols[0, :final_length],
                    after.genomes[-1],
                )):
            raise AssertionError(label + ' fixed c7 fixture drifted')

        result.append({
            'label': label, 'state': state,
            'drop_lesions': provisional['drop_lesions'],
            'cell_id': int(cell.cell_id), 'dt': 100.0,
            'capacity': capacity, 'capacity_values': capacity_values,
            'template_length': template_length,
            'final_length': final_length,
            'requested': int(plan.requested_symbols[0]),
            'substitution': substitution,
            'structural': structural,
            'padding': int(tape.padding_count[0]),
            'material_delta': int(plan.material_delta_symbols[0]),
            'rng_call_count': len(direct.rng.calls),
            'rng_before_state': rng_before,
            'rng_after_selection_state': rng_after_selection,
            'rng_after_state': rng_after,
            'tape_state': tape.state_dict(),
            'plan_state': plan.state_dict(),
            'mutation_before': tuple(mutation_before.items()),
            'mutation_after': tuple(after.mutation_events.items()),
            'source_lesions': len(before.genome_lesions),
            'final_lesions': len(after.genome_lesions),
            'new_lesion': float(after.genome_lesions[-1]),
        })
    return result


def _a48c7_assert_completion_oracle(expected_world, expected_cell,
                                     actual_world, actual_cell, label):
    _a48b_assert_cell_matches(
        expected_cell, actual_cell, label,
        atol=a48c7.REPLICATION_ORACLE_ATOL,
    )
    if (expected_world.rng.bit_generator.state
            != actual_world.rng.bit_generator.state
            or float(expected_world.dissipated_energy)
            != float(actual_world.dissipated_energy)
            or len(actual_cell.genomes) != 2
            or actual_cell.replication_template is not None
            or actual_cell.replication_copy
            or float(actual_cell.replication_template_lesion) != 0.0
            or float(actual_cell.replication_fractional) != 0.0
            or actual_cell.genomes[-1] is actual_cell.genomes[0]
            or np.shares_memory(
                actual_cell.genomes[-1], actual_cell.genomes[0],
            )):
        raise AssertionError(label + ' completion/topology/PCG64 differs')


def _a48c7_hybrid_from_state(state, capacity=None, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c7.Hybrid066WorldA4ReplicationStartCompletionMutation(
        world, backend=backend,
        a4_config=capacity or a4.GPU068A4Config(),
        a4_device=device,
    )


def _a48c7_prior_test_ast_sha256(functions):
    ast_module = __import__('ast')
    payload = '\n'.join(
        ast_module.dump(
            ast_module.parse(inspect.getsource(function)).body[0],
            include_attributes=False,
        )
        for function in functions
    )
    return hashlib.sha256(payload.encode('utf-8')).hexdigest()


def test_a48c7_mutation_enabled_inactive_start_completion_formal066_oracle():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c7')
    if _REQUIRE_CUDA and not torch.cuda.is_available():
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    class ObserveStartCompletionMutation(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.observed_candidate = candidate
            return candidate

    details = []
    structural_names = tuple(a44.STRUCTURAL_EVENT_NAMES)
    for case in _a48c7_cases():
        devices = ['cpu']
        if torch.cuda.is_available():
            devices.append('cuda')
        for device in devices:
            direct = _a48c7_world_for_case(case, track_rng=True)
            direct_cell = direct.cells[0]
            direct_before = copy.deepcopy(direct_cell)
            direct_cell._replicate_genome(
                direct, case['dt'], direct.config,
            )

            selection = _A48C5TrackingGenerator(
                case['rng_before_state'],
            )
            a48c5._formal066_single_template_selection(selection)
            synthetic = _a48c6_synthetic_active_cell(direct_before)
            replay = _a48c7_world_for_case(case)
            replay.rng = _A48C5TrackingGenerator(
                selection.bit_generator.state,
            )
            replay_cell = copy.deepcopy(synthetic)
            replay.cells = [replay_cell]
            replay_cell._replicate_genome(
                replay, case['dt'], replay.config,
            )
            if (direct.rng.calls[:1]
                    != [('integers', (0, 1), {})]
                    or direct.rng.calls[1:] != replay.rng.calls
                    or selection.bit_generator.state
                    != case['rng_after_selection_state']
                    or len(direct.rng.calls) != case['rng_call_count']):
                raise AssertionError(
                    case['label'] + ' direct continuous call order differs'
                )

            world = _a48c7_world_for_case(
                case, track_rng=True, track_ledger=True,
            )
            cell = world.cells[0]
            ledger = cell.mutation_events
            before_ids = _a48c_identity_snapshot(world, cell)
            old_copy = cell.replication_copy
            pools_before = np.asarray(cell.pools, np.float64).copy()
            energy_before = float(world.dissipated_energy)
            scheduler = ObserveStartCompletionMutation(
                case['capacity'], device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                candidate = scheduler.observed_candidate
                evidence = candidate.evidence
                tape = candidate.tape
                plan = candidate.plan
                label = 'a48c7.oracle.%s.%s' % (
                    device, case['label'],
                )
                _a48c7_assert_completion_oracle(
                    direct, direct_cell, world, cell, label,
                )
                after_ids = _a48c_identity_snapshot(world, cell)
                for key in (
                        'rng', 'pools', 'genomes', 'lesions', 'events',
                        'proteins', 'damaged', 'gene_specs'):
                    if after_ids[key] != before_ids[key]:
                        raise AssertionError(
                            '%s changed %s identity' % (label, key)
                        )
                if (after_ids['genome_items'][:-1]
                        != before_ids['genome_items']
                        or cell.replication_copy is old_copy
                        or cell.replication_copy
                        is candidate.candidate_cell.replication_copy
                        or cell.genomes[-1]
                        is candidate.candidate_cell.genomes[-1]
                        or np.shares_memory(
                            cell.genomes[-1],
                            candidate.candidate_cell.genomes[-1],
                        )
                        or np.shares_memory(
                            cell.genomes[-1],
                            candidate.synthetic_cell.replication_template,
                        )):
                    raise AssertionError(label + ' final topology aliases')

                expected_structural = [
                    int(value) for value in case['structural']
                ]
                paid = {
                    int(a4.a3.POOL_NUCLEOTIDE),
                    int(a4.a3.POOL_ATP),
                }
                unpaid = [
                    index for index in range(int(a4.a3.POOL_COUNT))
                    if index not in paid
                ]
                metadata = _a48c_replication_entry(
                    scheduler, cell,
                )['metadata']
                expected_capacity = {
                    'Q': 3,
                    'S': int(case['capacity_values']['max_symbols']),
                    'W': int(
                        case['capacity_values']['max_sequence_symbols']
                    ),
                    'P': int(
                        case['capacity_values']['max_proteins_per_cell']
                    ),
                }
                final_length = case['final_length']
                if (cell.mutation_events is not ledger
                        or tuple(ledger.items())
                        != case['mutation_after']
                        or tuple(ledger) != tuple(
                            key for key, _ in case['mutation_before']
                        )
                        or not np.array_equal(
                            np.asarray(
                                cell.pools[unpaid], np.float64,
                            ).view(np.uint64),
                            pools_before[unpaid].view(np.uint64),
                        )
                        or world.rng.calls
                        or world.rng.bit_generator.state
                        != case['rng_after_state']
                        or world.rng is not candidate.live_rng
                        or float(world.dissipated_energy) != energy_before
                        or evidence.low != 0 or evidence.high != 1
                        or evidence.selected_index != 0
                        or evidence.call_count != 1
                        or evidence.rng_before_state
                        != case['rng_before_state']
                        or evidence.rng_after_state
                        != case['rng_after_selection_state']
                        or candidate.rng_after_selection_state
                        != tape.rng_before_state
                        or tape.rng_after_state
                        != candidate.rng_after_state
                        or candidate.rng_after_state
                        != case['rng_after_state']
                        or not a48._state_arrays_bit_exact(
                            tape.state_dict(), case['tape_state'],
                        )
                        or int(plan.requested_symbols[0])
                        != case['requested']
                        or int(plan.append_count[0])
                        != case['template_length']
                        or int(plan.final_lengths[0]) != final_length
                        or int(plan.substitution_events[0])
                        != case['substitution']
                        or tuple(
                            int(value) for value in
                            plan.structural_event_counts[0]
                        ) != case['structural']
                        or int(tape.padding_count[0]) != case['padding']
                        or int(plan.material_delta_symbols[0])
                        != case['material_delta']
                        or abs(
                            float(plan.new_genome_lesions[0])
                            - case['new_lesion']
                        ) > a48c7.REPLICATION_ORACLE_ATOL
                        or not np.array_equal(
                            plan.final_symbols[0, :final_length],
                            cell.genomes[-1],
                        )
                        or candidate.transaction_capacity
                        != expected_capacity
                        or metadata.get('authority') != (
                            'A4.8c7-resident-mutation-enabled-start-'
                            'completion-final-plan-pcg64-chain-atomic-commit'
                        )
                        or metadata.get('branch') != (
                            'inactive-template-start-completion-mutation'
                        )
                        or metadata.get('mutation_enabled') is not True
                        or metadata.get('device') != device
                        or int(metadata.get('template_start_events', 0)) != 1
                        or int(metadata.get('completion_events', 0)) != 1
                        or int(metadata.get('substitution_events', -1))
                        != case['substitution']
                        or metadata.get('structural_event_counts')
                        != expected_structural
                        or int(metadata.get('material_delta_symbols', 999))
                        != case['material_delta']
                        or int(metadata.get('final_length', -1))
                        != final_length
                        or int(metadata.get('rng_call_count', -1))
                        != case['rng_call_count']
                        or metadata.get('rng_schedule_sha256')
                        != candidate.rng_chain_sha256
                        or metadata.get('selection_schedule_sha256')
                        != evidence.schedule_sha256
                        or metadata.get(
                            'completion_mutation_schedule_sha256'
                        ) != tape.schedule_sha256
                        or metadata.get('transaction_capacity')
                        != expected_capacity
                        or not candidate.consumed):
                    raise AssertionError(
                        label + ' plan/chain/ledger/receipt differs'
                    )

                source = candidate.source_binding.ragged
                transient = candidate.synthetic_binding.ragged
                final = candidate.fresh_binding.ragged
                tlen = case['template_length']
                if (int(source.sequence_count) != 1
                        or int(source.symbol_count) != tlen
                        or int(transient.sequence_count) != 3
                        or int(transient.symbol_count) != 2 * tlen
                        or int(final.sequence_count) != 2
                        or int(final.symbol_count) != tlen + final_length
                        or int(final.lesion_count) != case['final_lesions']):
                    raise AssertionError(label + ' Q/S/lesion topology differs')
                details.append('%s:%s:%d/%d' % (
                    device, case['label'], final_length,
                    case['rng_call_count'],
                ))
            finally:
                _a48_abort_direct(scheduler, cell)
    return ('Formal066 deletion/refund, expansion, short padding+budget '
            'trim, missing-lesion primed; literal selection then independent '
            'active replay exact calls/state; resident final polymer/pools/'
            'lesion/counters/cache/ledger/QSWP receipt: '
            + ', '.join(details))


def test_a48c7_candidate_cpu_cuda_synthetic_tape_plan_purity_and_fresh_binding():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c7')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    case = next(
        item for item in _a48c7_cases() if item['label'] == 'expansion'
    )
    reference = None
    details = []
    for device in devices:
        direct = _a48c7_world_for_case(case)
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        world = _a48c7_world_for_case(
            case, track_rng=True, track_ledger=True,
        )
        cell = world.cells[0]
        scheduler = (
            a48c7.A4ReplicationStartCompletionMutationEventScheduler(
                case['capacity'], device,
            )
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, case['dt'],
        )
        before = v3.pickle_clone(world.state_dict())
        before_ids = _a48c_identity_snapshot(world, cell)
        before_rng = copy.deepcopy(world.rng.bit_generator.state)
        before_energy = float(world.dissipated_energy)
        try:
            candidate = (
                scheduler.
                _prepare_replication_start_completion_mutation_candidate(
                    world, cell, case['dt'], world.config,
                )
            )
            v3.assert_recursive_close(
                before, world.state_dict(), 0.0, 0.0,
                'a48c7.prepare_purity.' + device,
            )
            _, record = scheduler._record(cell)
            if (world.rng.bit_generator.state != before_rng
                    or world.rng.calls
                    or float(world.dissipated_energy) != before_energy
                    or _a48c_identity_snapshot(world, cell) != before_ids
                    or any(entry['event'] == 'replication_cpu'
                           for entry in record['events'])):
                raise AssertionError(device + ' prepare crossed claim/purity')

            if (candidate.candidate_cell is cell
                    or candidate.synthetic_cell is cell
                    or candidate.synthetic_cell.pools is cell.pools
                    or candidate.candidate_cell.pools is cell.pools
                    or candidate.synthetic_cell.genomes is cell.genomes
                    or candidate.candidate_cell.genomes is cell.genomes
                    or candidate.synthetic_cell.replication_template
                    is cell.genomes[0]
                    or np.shares_memory(
                        candidate.synthetic_cell.replication_template,
                        cell.genomes[0],
                    )
                    or np.shares_memory(
                        candidate.synthetic_cell.replication_template,
                        candidate.synthetic_cell.genomes[0],
                    )
                    or np.shares_memory(
                        candidate.candidate_cell.genomes[-1],
                        candidate.synthetic_cell.replication_template,
                    )
                    or candidate.plan is candidate.host_replay
                    or candidate.plan is candidate.resident_plan
                    or candidate.tape is candidate.resident_tape
                    or candidate.source_binding
                    is candidate.synthetic_binding
                    or candidate.synthetic_binding
                    is candidate.fresh_binding
                    or candidate.live_rng is not world.rng):
                raise AssertionError(device + ' candidate alias/authority differs')

            a48c7._validate_single_template_completion_mutation_selection_evidence(
                candidate.evidence, candidate.source_binding,
                candidate.synthetic_binding, case['dt'], world.config,
            )
            a44.validate_a4_completion_mutation_rng_tape(
                candidate.tape, candidate.synthetic_binding,
                case['dt'], world.config,
            )
            if (candidate.evidence.rng_after_state
                    != candidate.tape.rng_before_state
                    or candidate.tape.rng_after_state
                    != candidate.rng_after_state
                    or a48c7._completion_mutation_rng_chain_sha256(
                        candidate.evidence, candidate.tape,
                    ) != candidate.rng_chain_sha256
                    or a48c3._resident_completion_mutation_artifacts(
                        candidate.resident_binding,
                        candidate.resident_tape,
                        candidate.resident_plan,
                        device,
                    ) != candidate.resident_artifacts
                    or a48c3._host_completion_mutation_artifacts(
                        candidate.tape, candidate.host_replay,
                        candidate.plan,
                    ) != candidate.host_artifacts
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_tape.to_numpy().state_dict(),
                        candidate.tape.state_dict(),
                    )
                    or not a48._state_arrays_bit_exact(
                        candidate.resident_plan.to_numpy().state_dict(),
                        candidate.plan.state_dict(),
                    )):
                raise AssertionError(device + ' tape/plan artifact chain differs')

            a48c3._completion_mutation_plan_semantic_match(
                candidate.plan, candidate.host_replay,
            )
            oracle_mean = np.float64(
                candidate.host_replay.genome_lesion_mean_after[0]
            )
            one_ulp = candidate.plan.clone()
            one_ulp.genome_lesion_mean_after[0] = np.nextafter(
                oracle_mean, np.float64(np.inf),
            )
            a48c3._completion_mutation_plan_semantic_match(
                one_ulp, candidate.host_replay,
            )
            two_ulp = candidate.plan.clone()
            two_ulp.genome_lesion_mean_after[0] = np.nextafter(
                np.nextafter(oracle_mean, np.float64(np.inf)),
                np.float64(np.inf),
            )
            _assert_raises(
                a48c3.A4ReplicationCompletionMutationCommitError,
                lambda: a48c3._completion_mutation_plan_semantic_match(
                    two_ulp, candidate.host_replay,
                ),
            )
            _a48b_assert_cell_matches(
                direct.cells[0], candidate.candidate_cell,
                'a48c7.candidate.' + device,
                atol=a48c7.REPLICATION_ORACLE_ATOL,
            )
            a4._require_translation_binding(candidate.fresh_binding)
            if (not isinstance(
                    candidate.fresh_binding.ragged.symbols, np.ndarray)
                    or not isinstance(
                        candidate.fresh_binding.cache.fingerprints,
                        np.ndarray)
                    or np.shares_memory(
                        candidate.fresh_binding.ragged.symbols,
                        candidate.source_binding.ragged.symbols,
                    )
                    or np.shares_memory(
                        candidate.fresh_binding.ragged.symbols,
                        candidate.synthetic_binding.ragged.symbols,
                    )):
                raise AssertionError(device + ' fresh host binding aliases')

            stable = (
                candidate.tape.state_dict(), candidate.plan.state_dict(),
                candidate.rng_chain_sha256,
            )
            if reference is None:
                reference = stable
            else:
                if (not a48._state_arrays_bit_exact(
                        reference[0], stable[0])
                        or not a48._state_arrays_bit_exact(
                            reference[1], stable[1])
                        or reference[2] != stable[2]):
                    raise AssertionError(
                        device + ' differs from CPU candidate authority'
                    )

            scheduler._commit_replication_start_completion_mutation_candidate(
                world, cell, case['dt'], world.config, candidate,
            )
            _a48c7_assert_completion_oracle(
                direct, direct.cells[0], world, cell,
                'a48c7.commit.' + device,
            )
            committed = v3.pickle_clone(world.state_dict())
            committed_rng = copy.deepcopy(world.rng.bit_generator.state)
            _assert_raises(
                a48c7.A4ReplicationStartCompletionMutationCommitError,
                lambda:
                scheduler._commit_replication_start_completion_mutation_candidate(
                    world, cell, case['dt'], world.config, candidate,
                ),
            )
            v3.assert_recursive_close(
                committed, world.state_dict(), 0.0, 0.0,
                'a48c7.one_shot.' + device,
            )
            if world.rng.bit_generator.state != committed_rng:
                raise AssertionError(device + ' one-shot rejection changed RNG')
            pointer_count = sum(
                len(values) for values in
                candidate.resident_artifacts['data_ptrs'].values()
            )
            details.append('%s:%d-pointers' % (device, pointer_count))
        finally:
            _a48_abort_direct(scheduler, cell)
    return ('CPU/CUDA prepare purity; source->synthetic->selection/tape->'
            'NumPy oracle/Torch resident plan->fresh binding association; '
            'explicit pointer/version/readback, 1-ULP accept/2-ULP reject, '
            'deep nonalias and one-shot PASS: ' + '/'.join(details))


def _a48c7_assert_preclaim_failure(case, capacity, scheduler_type=None,
                                    device='cpu', label='scope'):
    world = _a48c7_world_for_case(case)
    cell = next(
        item for item in world.cells
        if int(item.cell_id) == int(case['cell_id'])
    )
    scheduler_type = (
        scheduler_type
        or a48c7.A4ReplicationStartCompletionMutationEventScheduler
    )
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(
        scheduler, world, cell, case['dt'],
    )
    before = v3.pickle_clone(world.state_dict())
    before_ids = _a48c_identity_snapshot(world, cell)
    before_rng_object = world.rng
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    before_energy = float(world.dissipated_energy)
    before_receipts = len(scheduler._receipts)
    try:
        _assert_raises(
            (a48c7.A4ReplicationStartCompletionMutationCommitError,
             a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, case['dt'], world.config,
            ),
        )
        expected_world = getattr(
            scheduler, 'expected_world_state', before,
        )
        expected_rng = getattr(
            scheduler, 'expected_rng_state', before_rng,
        )
        v3.assert_recursive_close(
            expected_world, world.state_dict(), 0.0, 0.0,
            'a48c7.preclaim.' + label,
        )
        _, record = scheduler._record(cell)
        if (world.rng is not before_rng_object
                or world.rng.bit_generator.state != expected_rng
                or float(world.dissipated_energy) != before_energy
                or _a48c_identity_snapshot(world, cell) != before_ids
                or len(scheduler._receipts) != before_receipts
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(label + ' crossed preclaim boundary')
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c7_capacity_trust_scope_rollback_and_exact_once():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c7 trust tests')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    cases = {case['label']: case for case in _a48c7_cases()}

    axes = (
        ('Q', 'max_sequences', 'deletion-refund'),
        ('S', 'max_symbols', 'expansion'),
        ('W', 'max_sequence_symbols', 'expansion'),
        ('P', 'max_proteins_per_cell', 'deletion-refund'),
    )
    capacity_details = []
    for axis, key, case_label in axes:
        case = cases[case_label]
        exact = a4.GPU068A4Config(**case['capacity_values'])
        direct = _a48c7_world_for_case(case)
        direct.cells[0]._replicate_genome(
            direct, case['dt'], direct.config,
        )
        for device in devices:
            world = _a48c7_world_for_case(case)
            cell = world.cells[0]
            scheduler = (
                a48c7.A4ReplicationStartCompletionMutationEventScheduler(
                    exact, device,
                )
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            try:
                scheduler.cpu_replication(
                    world, cell, case['dt'], world.config,
                )
                _a48c7_assert_completion_oracle(
                    direct, direct.cells[0], world, cell,
                    'a48c7.capacity.%s.%s' % (axis, device),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append('%s@%s' % (axis, device))
        short = dict(case['capacity_values'])
        short[key] -= 1
        _a48c7_assert_preclaim_failure(
            case, a4.GPU068A4Config(**short),
            label='capacity_%s_one_short' % axis,
        )
    del scheduler, world, cell, direct
    gc.collect()

    base = cases['deletion-refund']
    trust_details = []

    def assert_tamper_failure(scheduler_type, label,
                              test_devices=('cpu',)):
        for device in test_devices:
            world = _a48c7_world_for_case(base)
            cell = world.cells[0]
            scheduler = scheduler_type(base['capacity'], device)
            _a48c_begin_replication_direct(
                scheduler, world, cell, base['dt'],
            )
            before = v3.pickle_clone(world.state_dict())
            before_ids = _a48c_identity_snapshot(world, cell)
            before_rng = copy.deepcopy(world.rng.bit_generator.state)
            before_receipts = len(scheduler._receipts)
            try:
                _assert_raises(
                    (a48c7.A4ReplicationStartCompletionMutationCommitError,
                     a4.A4Error, a3s.A3SchedulerError),
                    lambda: scheduler.cpu_replication(
                        world, cell, base['dt'], world.config,
                    ),
                )
                expected_world = getattr(
                    scheduler, 'expected_world_state', before,
                )
                expected_rng = getattr(
                    scheduler, 'expected_rng_state', before_rng,
                )
                v3.assert_recursive_close(
                    expected_world, world.state_dict(), 0.0, 0.0,
                    'a48c7.trust.%s.%s' % (device, label),
                )
                _, record = scheduler._record(cell)
                if (world.rng.bit_generator.state != expected_rng
                        or _a48c_identity_snapshot(world, cell)
                        != before_ids
                        or len(scheduler._receipts) != before_receipts
                        or any(entry['event'] == 'replication_cpu'
                               for entry in record['events'])):
                    raise AssertionError(
                        '%s/%s tamper crossed preclaim' % (device, label)
                    )
                trust_details.append(device + ':' + label)
            finally:
                _a48_abort_direct(scheduler, cell)
            del scheduler, cell, world
            gc.collect()

    class TamperSource(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.source_binding.ragged.symbols[0] ^= np.uint8(1)
            return candidate

    class TamperEvidence(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.evidence.selected_index = 1
            return candidate

    class AliasSyntheticTemplate(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.synthetic_cell.replication_template = (
                candidate.synthetic_cell.genomes[0][::]
            )
            return candidate

    class TamperSyntheticBinding(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.synthetic_binding.ragged.symbols[0] ^= np.uint8(1)
            return candidate

    class TamperTape(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.tape.append_uniform_draws[0, 0] = np.nextafter(
                candidate.tape.append_uniform_draws[0, 0], np.inf,
            )
            return candidate

    class TamperHostReplay(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.host_replay.final_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperReadbackPlan(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.plan.final_symbols[0, 0] ^= np.uint8(1)
            return candidate

    class TamperResidentSource(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.ragged.symbols.data[0].bitwise_xor_(1)
            return candidate

    class TamperResidentState(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.state.pools.data[
                0, a4.a3.POOL_NUCLEOTIDE
            ].add_(1e-8)
            return candidate

    class TamperResidentCache(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_binding.cache.copy_numbers.data[0].add_(1)
            return candidate

    class TamperResidentTape(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_tape.append_uniform_draws.data[0, 0].add_(0.125)
            return candidate

    class TamperResidentPlan(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.final_symbols.data[0, 0].bitwise_xor_(1)
            return candidate

    class SwapResidentPointer(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_plan.final_symbols = (
                candidate.resident_plan.final_symbols.clone()
            )
            return candidate

    class TamperResidentArtifacts(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.resident_artifacts['data_ptrs']['plan'][
                'final_symbols'
            ] += 1
            return candidate

    class TamperCandidate(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.genomes[-1][0] ^= np.uint8(1)
            return candidate

    class AliasFinalView(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.candidate_cell.genomes[-1] = (
                candidate.candidate_cell.genomes[-1][::]
            )
            return candidate

    class TamperFreshBinding(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.fresh_binding.ragged.symbols[-1] ^= np.uint8(1)
            return candidate

    class TamperCapacity(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.transaction_capacity['S'] += 1
            return candidate

    class TamperChainDigest(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            candidate.rng_chain_sha256 = '0' * 64
            return candidate

    class TamperSchedulerConfig(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            self.a4_config = a4.GPU068A4Config(
                max_cells=1, max_sequences=1, max_symbols=1,
                max_sequence_symbols=1, max_proteins_per_cell=1,
            )
            self.a4_device = (
                'cuda' if self.a4_device == 'cpu' else 'cpu'
            )
            return candidate

    class TamperLiveConfig(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.config.mutation_rate = float(np.nextafter(
                world.config.mutation_rate, np.inf,
            ))
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveSource(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            cell.genomes[0][0] ^= np.uint8(1)
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperLiveRng(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            world.rng.random()
            self.expected_rng_state = copy.deepcopy(
                world.rng.bit_generator.state,
            )
            self.expected_world_state = v3.pickle_clone(world.state_dict())
            return candidate

    class TamperCandidateRng(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _replication_start_completion_mutation_candidate_ready(
                self, world, cell, dt, config, candidate):
            generator = np.random.Generator(np.random.PCG64())
            generator.bit_generator.state = copy.deepcopy(
                candidate.rng_after_selection_state,
            )
            generator.random()
            candidate.rng_after_selection_state = copy.deepcopy(
                generator.bit_generator.state,
            )
            return candidate

    for scheduler_type, label in (
            (TamperSource, 'source_binding'),
            (TamperEvidence, 'selection_evidence'),
            (AliasSyntheticTemplate, 'synthetic_template_view_alias'),
            (TamperSyntheticBinding, 'synthetic_binding'),
            (TamperTape, 'host_tape'),
            (TamperHostReplay, 'host_oracle'),
            (TamperReadbackPlan, 'readback_plan'),
            (SwapResidentPointer, 'resident_pointer'),
            (TamperResidentArtifacts, 'resident_private_artifacts'),
            (TamperCandidate, 'candidate_cell'),
            (AliasFinalView, 'final_view_alias'),
            (TamperFreshBinding, 'fresh_binding'),
            (TamperCapacity, 'transaction_capacity'),
            (TamperChainDigest, 'rng_chain_digest'),
            (TamperSchedulerConfig, 'scheduler_config_device'),
            (TamperLiveConfig, 'live_config'),
            (TamperLiveSource, 'live_source'),
            (TamperLiveRng, 'live_rng'),
            (TamperCandidateRng, 'candidate_rng')):
        assert_tamper_failure(scheduler_type, label)
    for scheduler_type, label in (
            (TamperResidentSource, 'resident_source_data'),
            (TamperResidentState, 'resident_state_data'),
            (TamperResidentCache, 'resident_cache_data'),
            (TamperResidentTape, 'resident_tape_data'),
            (TamperResidentPlan, 'resident_plan_data')):
        assert_tamper_failure(
            scheduler_type, label, test_devices=tuple(devices),
        )

    # Wrong live associations cannot consume one prepared candidate.
    world = _a48c7_world_for_case(base)
    cell = world.cells[0]
    scheduler = a48c7.A4ReplicationStartCompletionMutationEventScheduler(
        base['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        scheduler, world, cell, base['dt'],
    )
    before = v3.pickle_clone(world.state_dict())
    before_rng = copy.deepcopy(world.rng.bit_generator.state)
    try:
        candidate = (
            scheduler.
            _prepare_replication_start_completion_mutation_candidate(
                world, cell, base['dt'], world.config,
            )
        )
        for label, wrong_cell, wrong_dt, wrong_config in (
                ('cell', copy.deepcopy(cell), base['dt'], world.config),
                ('dt', cell, np.nextafter(base['dt'], np.inf), world.config),
                ('config', cell, base['dt'], copy.deepcopy(world.config))):
            _assert_raises(
                a48c7.A4ReplicationStartCompletionMutationCommitError,
                lambda wrong_cell=wrong_cell, wrong_dt=wrong_dt,
                       wrong_config=wrong_config:
                scheduler.
                _commit_replication_start_completion_mutation_candidate(
                    world, wrong_cell, wrong_dt, wrong_config, candidate,
                ),
            )
            if candidate.consumed:
                raise AssertionError('wrong ' + label + ' consumed candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c7.wrong_cell_dt_config',
        )
        if world.rng.bit_generator.state != before_rng:
            raise AssertionError('wrong inputs changed RNG')
    finally:
        _a48_abort_direct(scheduler, cell)
    del candidate, scheduler, world, cell
    gc.collect()

    scope_cases = []

    def add_scope(label, mutate):
        scoped = _a48c7_world_for_case(base)
        scoped_cell = scoped.cells[0]
        mutate(scoped, scoped_cell)
        scoped.initial_total_material = scoped.total_material()
        scoped.last_step_material_residual = 0.0
        scope_cases.append({
            'label': label,
            'state': v3.pickle_clone(scoped.state_dict()),
            'drop_lesions': False,
            'cell_id': int(scoped_cell.cell_id),
            'dt': base['dt'],
        })

    add_scope('disabled', lambda world, cell: setattr(
        world.config, 'genome_replication', False))
    add_scope('negative_atp', lambda world, cell: cell.pools.__setitem__(
        a4.a3.POOL_ATP, -1e-12))

    def no_genome(world, cell):
        cell.genomes = []
        cell.genome_lesions = []
        cell._refresh_gene_cache()

    add_scope('no_genome', no_genome)

    def empty_genome(world, cell):
        cell.genomes = [np.zeros((0,), dtype=np.uint8)]
        cell.genome_lesions = [0.0]
        cell._refresh_gene_cache()

    add_scope('empty_genome', empty_genome)

    def two_genomes(world, cell):
        cell.genomes.append(np.asarray(cell.genomes[0], np.uint8).copy())
        cell.genome_lesions.append(0.2)
        cell._refresh_gene_cache()

    add_scope('two_genomes', two_genomes)

    def no_replicase(world, cell):
        world.config.external_replicase = False
        for fingerprint, spec in list(cell.gene_specs.items()):
            if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
                cell.proteins.pop(fingerprint, None)
        cell._sync_protein_pool()

    add_scope('replicase_gate', no_replicase)
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for scoped in scope_cases:
            _a48c7_assert_preclaim_failure(
                scoped, a4.GPU068A4Config(),
                label='scope_' + scoped['label'],
            )

        dead = _a48c7_world_for_case(base)
        dead_cell = dead.cells[0]
        dead_scheduler = (
            a48c7.A4ReplicationStartCompletionMutationEventScheduler(
                base['capacity'], 'cpu',
            )
        )
        _a48c_begin_replication_direct(
            dead_scheduler, dead, dead_cell, base['dt'],
        )
        dead_cell.alive = False
        dead_before = v3.pickle_clone(dead.state_dict())
        dead_ids = _a48c_identity_snapshot(dead, dead_cell)
        dead_rng = copy.deepcopy(dead.rng.bit_generator.state)
        _assert_raises(
            a48c7.A4ReplicationStartCompletionMutationCommitError,
            lambda: dead_scheduler.cpu_replication(
                dead, dead_cell, base['dt'], dead.config,
            ),
        )
        v3.assert_recursive_close(
            dead_before, dead.state_dict(), 0.0, 0.0,
            'a48c7.scope_dead',
        )
        if (dead.rng.bit_generator.state != dead_rng
                or _a48c_identity_snapshot(dead, dead_cell) != dead_ids):
            raise AssertionError('dead scope changed biology/RNG')
        _a48_abort_direct(dead_scheduler, dead_cell)
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('excluded c7 scope used CPU fallback')

    duplicate = _a48c7_world_for_case(base)
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = (
        a48c7.A4ReplicationStartCompletionMutationEventScheduler(
            base['capacity'], 'cpu',
        )
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell, base['dt'],
    )
    duplicate_scheduler.cpu_replication(
        duplicate, duplicate_cell, base['dt'], duplicate.config,
    )
    duplicate_before = v3.pickle_clone(duplicate.state_dict())
    duplicate_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, base['dt'], duplicate.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate.state_dict(), 0.0, 0.0,
        'a48c7.duplicate',
    )
    if duplicate.rng.bit_generator.state != duplicate_rng:
        raise AssertionError('duplicate c7 event changed RNG')
    _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = _a48c7_world_for_case(base)
    order_cell = order.cells[0]
    order_scheduler = (
        a48c7.A4ReplicationStartCompletionMutationEventScheduler(
            base['capacity'], 'cpu',
        )
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, base['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order_scheduler.cpu_replication(
            order, order_cell, base['dt'], order.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order.state_dict(), 0.0, 0.0,
        'a48c7.out_of_order',
    )
    if order.rng.bit_generator.state != order_rng:
        raise AssertionError('out-of-order c7 event changed RNG')
    _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(
            a48c7.A4ReplicationStartCompletionMutationEventScheduler):
        def _publish_replication_start_completion_mutation_candidate(
                self, world, cell, candidate):
            super(FailAfterPublish, self)._publish_replication_start_completion_mutation_candidate(
                world, cell, candidate,
            )
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.genomes[0][0] ^= np.uint8(1)
            cell.genomes[-1][0] ^= np.uint8(1)
            cell.genome_lesions[-1] += 0.25
            cell.replication_template = np.asarray((1,), np.uint8)
            cell.replication_copy.append(7)
            cell.replication_copy = list(cell.replication_copy)
            cell.mutation_events['a48c7_extra_sentinel'] += 1
            cell.proteins[-901] = 0.25
            cell.damaged_proteins[-902] = 0.5
            first = next(iter(cell.gene_specs.values()))
            first['parameter'] = int(first['parameter']) + 1
            cell.replication_cycles += 3
            cell.last_replication_symbols += 2
            cell.last_effective_error_rate += 0.125
            cell.cumulative_proofreading_atp += 0.5
            cell.novel_path_first_age = -9.0
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c7 publish failure')

    rollback = _a48c7_world_for_case(base)
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(base['capacity'], 'cpu')
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, base['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    rollback_spec_ids = tuple(
        (key, id(spec)) for key, spec in rollback_cell.gene_specs.items()
    )
    old_copy = rollback_cell.replication_copy
    rollback_rng_object = rollback.rng
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, base['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c7 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c7.publish_rollback',
    )
    if (rollback.rng is not rollback_rng_object
            or rollback.rng.bit_generator.state != rollback_rng
            or _a48c_identity_snapshot(rollback, rollback_cell)
            != rollback_ids
            or rollback_cell.replication_template is not None
            or rollback_cell.replication_copy is not old_copy
            or tuple(
                (key, id(spec))
                for key, spec in rollback_cell.gene_specs.items()
            ) != rollback_spec_ids):
        raise AssertionError(
            'c7 rollback lost source/nested/copy/RNG identity'
        )
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if entry['event'] != 'replication_cpu' or receipt['status'] != 'aborted':
        raise AssertionError('publish failure did not leave aborted receipt')
    return ('Q/S/W/P exact CPU/CUDA and each one-short; %d source/'
            'selection/synthetic/tape/oracle/resident/public/private/view/'
            'candidate/live trust checks; wrong inputs; %d no-fallback '
            'ordinary scopes; duplicate/order; full inactive None/old-copy/'
            'nested ledger/cache/RNG rollback PASS (%s)' % (
                len(trust_details), len(scope_cases) + 1,
                '/'.join(capacity_details),
            ))


def test_a48c7_inherited_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c7')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')

    delegated_cases = []
    c1_world, c1_cells, c1_capacity, c1_dt = (
        _a45_substitution_fixture(seed=13601)
    )
    _a48c_reduce_world(c1_world, [c1_cells[0]])
    delegated_cases.append({
        'label': 'c1-active-noncompletion',
        'state': v3.pickle_clone(c1_world.state_dict()),
        'dt': float(c1_dt), 'capacity': c1_capacity,
        'authority': 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'drop_lesions': False,
    })

    c2_state, _, c2_dt = _a48c2_completion_case(seed=13602, row=0)
    c2_before = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after.cells[0]._replicate_genome(
        c2_after, c2_dt, c2_after.config,
    )
    c2_capacity = a4.GPU068A4Config(**_a48c2_capacity_for_transition(
        c2_before.cells[0], c2_after.cells[0],
    ))
    delegated_cases.append({
        'label': 'c2-mutation-free-active-completion',
        'state': c2_state, 'dt': float(c2_dt),
        'capacity': c2_capacity,
        'authority': (
            'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit'
        ),
        'drop_lesions': False,
    })

    c3_case = _a48c3_completion_mutation_cases()[0]
    delegated_cases.append({
        'label': 'c3-mutation-active-completion',
        'state': c3_case['state'], 'dt': float(c3_case['dt']),
        'capacity': c3_case['integration_capacity'],
        'authority': (
            'A4.8c3-resident-completion-mutation-plan-atomic-'
            'cpu-cell-rng-commit'
        ),
        'drop_lesions': False,
    })

    c4_case = _a48c4_cases()[0]
    delegated_cases.append({
        'label': 'c4-mutation-start-noncompletion',
        'state': c4_case['state'], 'dt': float(c4_case['dt']),
        'capacity': c4_case['capacity'],
        'authority': (
            'A4.8c4-resident-template-start-plan-atomic-'
            'cpu-cell-pcg64-commit'
        ),
        'drop_lesions': bool(c4_case.get('drop_lesions', False)),
    })

    c5_case = _a48c5_cases()[0]
    delegated_cases.append({
        'label': 'c5-mutation-free-start-noncompletion',
        'state': c5_case['state'], 'dt': float(c5_case['dt']),
        'capacity': c5_case['capacity'],
        'authority': (
            'A4.8c5-resident-mutation-free-template-start-plan-'
            'atomic-cpu-cell-pcg64-commit'
        ),
        'drop_lesions': bool(c5_case.get('drop_lesions', False)),
    })

    c6_case = _a48c6_cases()[0]
    delegated_cases.append({
        'label': 'c6-mutation-free-start-completion',
        'state': c6_case['state'], 'dt': float(c6_case['dt']),
        'capacity': c6_case['capacity'],
        'authority': (
            'A4.8c6-resident-mutation-free-start-completion-plan-'
            'selection-only-pcg64-atomic-commit'
        ),
        'drop_lesions': bool(c6_case.get('drop_lesions', False)),
    })

    delegated = []
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in delegated_cases:
            for device in devices:
                direct = _a48c7_world_for_case(case)
                world = _a48c7_world_for_case(case)
                direct.cells[0]._replicate_genome(
                    direct, case['dt'], direct.config,
                )
                scheduler = (
                    a48c7.
                    A4ReplicationStartCompletionMutationEventScheduler(
                        case['capacity'], device,
                    )
                )
                _a48c_begin_replication_direct(
                    scheduler, world, world.cells[0], case['dt'],
                )
                try:
                    scheduler.cpu_replication(
                        world, world.cells[0], case['dt'], world.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], world.cells[0],
                        'a48c7.delegate.%s.%s' % (
                            device, case['label'],
                        ),
                        atol=a48c7.REPLICATION_ORACLE_ATOL,
                    )
                    metadata = _a48c_replication_entry(
                        scheduler, world.cells[0],
                    )['metadata']
                    if (direct.rng.bit_generator.state
                            != world.rng.bit_generator.state
                            or float(direct.dissipated_energy)
                            != float(world.dissipated_energy)
                            or metadata.get('authority')
                            != case['authority']):
                        raise AssertionError(
                            device + '/' + case['label']
                            + ' inherited authority differs'
                        )
                    delegated.append(device + ':' + case['label'])
                finally:
                    _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('a c1-c6 delegate used CPU fallback')

    own = _a48c7_cases()[0]
    direct = _a48c7_world_for_case(own)
    direct.cells[0]._replicate_genome(
        direct, own['dt'], direct.config,
    )
    world = _a48c7_world_for_case(own)
    scheduler = (
        a48c7.A4ReplicationStartCompletionMutationEventScheduler(
            own['capacity'], 'cpu',
        )
    )
    _a48c_begin_replication_direct(
        scheduler, world, world.cells[0], own['dt'],
    )
    original_c6 = (
        a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication
    )

    def c6_dispatch_bomb(*args, **kwargs):
        raise AssertionError('owned c7 completion delegated to c6')

    a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication = (
        c6_dispatch_bomb
    )
    try:
        scheduler.cpu_replication(
            world, world.cells[0], own['dt'], world.config,
        )
        _a48c7_assert_completion_oracle(
            direct, direct.cells[0], world, world.cells[0],
            'a48c7.owned_no_c6_dispatch',
        )
    finally:
        a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication = (
            original_c6
        )
        _a48_abort_direct(scheduler, world.cells[0])

    # Full Formal066 step clamps biology to 0.1.  A one-symbol genome with
    # ample pools reaches c7 and then continues through hydrolysis/motion.
    successor_world = _a48c7_world_for_case(own)
    successor_cell = successor_world.cells[0]
    successor_cell.genomes = [np.asarray((3,), dtype=np.uint8)]
    successor_cell.genome_lesions = [0.2]
    successor_cell.replication_template = None
    successor_cell.replication_template_lesion = 0.0
    successor_cell.replication_copy = []
    successor_cell.replication_fractional = 0.0
    successor_cell.pools[a4.a3.POOL_NUCLEOTIDE] = 10.0
    successor_cell.pools[a4.a3.POOL_ATP] = 10.0
    successor_cell.age = 0.0
    successor_cell.novel_path_first_age = None
    successor_cell._refresh_gene_cache()
    successor_world.initial_total_material = successor_world.total_material()
    successor_world.last_step_material_residual = 0.0
    successor_state = v3.pickle_clone(successor_world.state_dict())
    successor_dt = 0.1
    probe = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(successor_state),
    )
    probe.cells[0]._replicate_genome(
        probe, successor_dt, probe.config,
    )
    if (len(probe.cells[0].genomes) != 2
            or probe.cells[0].replication_template is not None):
        raise AssertionError('A4.8c7 successor completion probe drifted')
    roomy = a4.GPU068A4Config(
        max_cells=4, max_sequences=12, max_symbols=8192,
        max_sequence_symbols=a4.MAX_FROZEN_GENOME_SYMBOLS,
        max_proteins_per_cell=64,
    )

    successor_details = []
    for device in devices:
        expected = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(successor_state),
        )
        hybrid = _a48c7_hybrid_from_state(
            successor_state, roomy, device,
        )
        expected.step(successor_dt)
        original_c6 = (
            a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication
        )
        a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication = (
            c6_dispatch_bomb
        )
        try:
            receipt = hybrid.step(successor_dt)
        finally:
            a48c6.A4ReplicationStartCompletionEventScheduler.cpu_replication = (
                original_c6
            )
        v3._assert_world_pair(
            expected, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c7.successor.' + device,
        )
        record = receipt['cells'][0]
        names = [entry['event'] for entry in record['events']]
        by_name = {entry['event']: entry for entry in record['events']}
        if (names.count('replication_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1
                or not names.index('translation_cpu')
                < names.index('replication_cpu')
                < names.index('surface_assembly')
                < names.index('genome_hydrolysis_cpu_rng')
                < names.index('motion')
                or by_name['replication_cpu']['metadata'].get('authority')
                != (
                    'A4.8c7-resident-mutation-enabled-start-completion-'
                    'final-plan-pcg64-chain-atomic-commit'
                )):
            raise AssertionError(device + ' c7 successor order differs')
        successor_details.append(device)

    seed_hybrid = _a48c7_hybrid_from_state(
        successor_state, roomy, 'cpu',
    )
    twin = seed_hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c7.pkl')
        seed_hybrid.save(path)
        restored = (
            a48c7.Hybrid066WorldA4ReplicationStartCompletionMutation.load(
                path,
            )
        )
        for value in (seed_hybrid, twin, restored):
            if (type(value) is not (
                    a48c7.Hybrid066WorldA4ReplicationStartCompletionMutation)
                    or type(value.scheduler) is not (
                        a48c7.
                        A4ReplicationStartCompletionMutationEventScheduler)
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config != roomy
                    or value.scheduler.
                    replication_start_completion_mutation_integration_state()
                    != {
                        'schema': a48c7.SCHEMA_VERSION,
                        'config': a48._config_state(roomy),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c7 save/clone authority was lost')
        expected = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(successor_state),
        )
        expected.step(successor_dt)
        for index, value in enumerate((seed_hybrid, twin, restored)):
            receipt = value.step(successor_dt)
            v3._assert_world_pair(
                expected, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c7.persisted_successor.%d' % index,
            )
            metadata = {
                entry['event']: entry['metadata']
                for entry in receipt['cells'][0]['events']
            }
            if metadata['replication_cpu'].get('authority') != (
                    'A4.8c7-resident-mutation-enabled-start-completion-'
                    'final-plan-pcg64-chain-atomic-commit'):
                raise AssertionError('persisted c7 authority differs')

    active = _a48c7_hybrid_from_state(
        successor_state, roomy, 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active',
            '_a4_replication_start_commit_active',
            '_a4_replication_mutation_free_start_commit_active',
            '_a4_replication_start_completion_commit_active',
            '_a4_replication_start_completion_mutation_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    expected_status = (
        'a4.8c7-mutation-enabled-inactive-one-genome-template-start-'
        'same-call-completion-selection-to-completion-mutation-tape-'
        'resident-final-plan-atomic-commit-a4.8c6-c5-c4-c3-c2-c1-'
        'inherited-other-inactive-early-noop-fail-closed'
    )
    signature = inspect.signature(
        a48c7.
        A4ReplicationStartCompletionMutationEventScheduler.cpu_replication,
    )
    prior_names = '\n'.join(fn.__name__ for fn in TESTS[:78])
    c7_names = tuple(fn.__name__ for fn in TESTS[78:82])
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or '_A4ReplicationStartCompletionMutationCommitCandidate'
            in set(a48c7.__all__)
            or '_A4SingleTemplateCompletionMutationSelectionEvidence'
            in set(a48c7.__all__)
            or not issubclass(
                a48c7.
                A4ReplicationStartCompletionMutationEventScheduler,
                a48c6.A4ReplicationStartCompletionEventScheduler,
            )
            or not issubclass(
                a48c7.Hybrid066WorldA4ReplicationStartCompletionMutation,
                a48c6.Hybrid066WorldA4ReplicationStartCompletion,
            )
            or a48c7.PORT_STATUS.get('genome_replication')
            != expected_status
            or a48c7.FULL_GPU_WORLD_STEP is not False
            or a48c7.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) != 82
            or c7_names != (
                'test_a48c7_mutation_enabled_inactive_start_completion_formal066_oracle',
                'test_a48c7_candidate_cpu_cuda_synthetic_tape_plan_purity_and_fresh_binding',
                'test_a48c7_capacity_trust_scope_rollback_and_exact_once',
                'test_a48c7_inherited_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority',
            )
            or hashlib.sha256(
                prior_names.encode('utf-8')
            ).hexdigest() != _A48C7_TEST_NAMES_SHA256
            or _a48c7_prior_test_ast_sha256(TESTS[:78])
            != _A48C7_TEST_AST_SHA256):
        raise AssertionError(
            'A4.8c7 API/scope/first78 name+function-AST authority differs'
        )
    expected_hashes = dict(PROMOTED_A3_SHA256)
    expected_hashes.update(_A48C7_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_mutation_integration.py'
    ] = _A48C7_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('c1-c6 CPU/CUDA delegates %s; owned c7 no c6/frozen '
            'fallback; CPU/CUDA successor %s; save/load/clone exact type; '
            'c1-c7 active/pending save reject; first78 names/function-AST + '
            'A3/pure/c1-c7 hashes/full_gpu=false PASS' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A48C8_PRIOR_TEST_NAMES_SHA256 = (
    '964ff04eb573a433526cd03d2d4e5ff9a1a0668f68bee605c67f1d1775fe6550'
)

_A48C8_PRIOR_TEST_AST_SHA256 = (
    'bbd38a881e60433c0f748a2074ac418c9759517116a79c1cb3e71477efb85386'
)

_A48C8_CORE_SHA256 = (
    '4bfe5ef02c20bb435319c4e6d59520fe863a61d7995d836e7fd159576f6ab36f'
)

_A48C8_AUTHORITY = (
    'A4.8c8-resident-pre-active-ordinary-early-noop-atomic-commit'
)

_A48C8_BRANCH_CODES = {
    'disabled': a48c8.BRANCH_DISABLED,
    'no-genome': a48c8.BRANCH_NO_GENOME,
    'replicase-gate': a48c8.BRANCH_REPLICASE_GATE,
    'inactive-multi-genome': a48c8.BRANCH_INACTIVE_MULTI_GENOME,
}


def _a48c8_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.8c8')
    devices = ['cpu']
    if torch.cuda.is_available():
        devices.append('cuda')
    elif _REQUIRE_CUDA:
        raise AssertionError('CUDA required but unavailable; fallback forbidden')
    return devices


def _a48c8_capacity_values(cell):
    sequences = list(cell.genomes)
    if cell.replication_template is not None:
        sequences.extend((cell.replication_template, cell.replication_copy))
    gene_keys = set(int(value) for value in cell.gene_specs)
    protein_capacity = max(
        len(gene_keys.union(int(value) for value in cell.proteins)),
        len(gene_keys.union(
            int(value) for value in cell.damaged_proteins
        )),
        1,
    )
    return {
        'max_cells': 1,
        'max_sequences': max(len(sequences), 1),
        'max_symbols': max(sum(len(value) for value in sequences), 1),
        'max_sequence_symbols': max(
            [1] + [len(value) for value in sequences]
        ),
        'max_proteins_per_cell': protein_capacity,
    }


def _a48c8_make_world(branch, seed=17201, mutation=False,
                       prime_uint32=False):
    if branch not in _A48C8_BRANCH_CODES:
        raise AssertionError('unknown A4.8c8 fixture branch')
    world, cells, _, _ = _a45b_template_start_fixture(
        seed=seed, prime_uint32=False,
    )
    cell = copy.deepcopy(cells[0])
    _a48c_reduce_world(world, [cell])
    world.config.genome_replication = True
    world.config.external_replicase = False
    world.config.mutation = bool(mutation)
    cell.replication_template = None
    cell.replication_template_lesion = 0.0
    cell.replication_copy = []
    cell.replication_fractional = 0.0
    cell.replication_cycles = 31
    cell.last_replication_symbols = 701
    cell.last_effective_error_rate = 0.375
    cell.cumulative_proofreading_atp = 0.625
    cell.mutation_events['a48c8_sentinel'] = 811
    cell.novel_path_first_age = None

    if branch in ('disabled', 'no-genome'):
        cell.genomes = []
        cell.genome_lesions = []
        cell._refresh_gene_cache()
    if branch == 'disabled':
        # The overlap with no-genome fixes the first frozen branch.
        world.config.genome_replication = False
    elif branch == 'no-genome':
        # Empty complete-genome count precedes the replicase gate.
        world.config.external_replicase = False
    elif branch in ('replicase-gate', 'inactive-multi-genome'):
        if not cell.genomes or len(cell.genomes[0]) == 0:
            raise AssertionError('A4.8c8 multi fixture lost its source genome')
        cell.genomes.append(np.asarray(cell.genomes[0], np.uint8).copy())
        cell.genome_lesions.append(0.25)
        cell._refresh_gene_cache()
        if branch == 'replicase-gate':
            # The overlap with two genomes fixes gate-before-multi order.
            for fingerprint, spec in list(cell.gene_specs.items()):
                if int(spec['role']) == int(a4.g2.ROLE_REPLICASE):
                    cell.proteins.pop(fingerprint, None)
            cell._sync_protein_pool()
            if cell.role_activity(a4.g2.ROLE_REPLICASE) > 1e-6:
                raise AssertionError('A4.8c8 gate fixture retains replicase')
        else:
            world.config.external_replicase = True

    if prime_uint32:
        world.rng.integers(0, 7)
    if int(world.rng.bit_generator.state['has_uint32']) != int(prime_uint32):
        raise AssertionError('A4.8c8 PCG64 cache fixture differs')
    world.initial_total_material = world.total_material()
    world.last_step_material_residual = 0.0
    capacity_values = _a48c8_capacity_values(cell)
    return world, cell, a4.GPU068A4Config(**capacity_values), 0.1


def _a48c8_case(branch, seed=17201, mutation=False,
                 prime_uint32=False):
    world, cell, capacity, dt = _a48c8_make_world(
        branch, seed=seed, mutation=mutation,
        prime_uint32=prime_uint32,
    )
    return {
        'label': branch,
        'branch_code': int(_A48C8_BRANCH_CODES[branch]),
        'state': v3.pickle_clone(world.state_dict()),
        'cell_id': int(cell.cell_id),
        'dt': float(dt),
        'capacity': capacity,
        'capacity_values': _a48c8_capacity_values(cell),
        'mutation': bool(mutation),
        'prime_uint32': bool(prime_uint32),
    }


def _a48c8_world_for_case(case):
    return a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(case['state']),
    )


def _a48c8_hybrid_from_state(state, capacity, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a48c8.Hybrid066WorldA4ReplicationEarlyNoop(
        world, backend=backend, a4_config=capacity, a4_device=device,
    )


def _a48c8_assert_only_last_zero(before, identities, rng_state,
                                 energy, world, cell, label):
    expected = v3.pickle_clone(before)
    expected['last_replication_symbols'] = 0
    v3.assert_recursive_close(
        expected, cell.state_dict(), 0.0, 0.0, label + '.state',
    )
    if (_a48c_identity_snapshot(world, cell) != identities
            or world.rng.bit_generator.state != rng_state
            or not a48c._float64_bits_equal(
                np.asarray([world.dissipated_energy], np.float64),
                np.asarray([energy], np.float64),
            )):
        raise AssertionError(label + ' mutable identity/RNG/energy differs')


def test_a48c8_pre_active_noop_branch_order_formal066_oracle():
    details = []
    for branch_index, branch in enumerate(_A48C8_BRANCH_CODES):
        for mutation in (False, True):
            for primed in (False, True):
                world, cell, capacity, dt = _a48c8_make_world(
                    branch, seed=17210 + 20 * branch_index
                    + 4 * int(mutation) + int(primed),
                    mutation=mutation, prime_uint32=primed,
                )
                before = v3.pickle_clone(cell.state_dict())
                identities = _a48c_identity_snapshot(world, cell)
                rng_state = copy.deepcopy(world.rng.bit_generator.state)
                energy = float(world.dissipated_energy)
                cell._replicate_genome(world, dt, world.config)
                label = 'a48c8.formal.%s.%d.%d' % (
                    branch, int(mutation), int(primed),
                )
                _a48c8_assert_only_last_zero(
                    before, identities, rng_state, energy,
                    world, cell, label,
                )

                source = a4.a3.s66.Formal066World.from_state(
                    v3.pickle_clone(_a48c8_make_world(
                        branch, seed=17210 + 20 * branch_index
                        + 4 * int(mutation) + int(primed),
                        mutation=mutation, prime_uint32=primed,
                    )[0].state_dict()),
                )
                source_cell = source.cells[0]
                source_before = v3.pickle_clone(source.state_dict())
                ragged, state, binding = _paid_replication_binding(
                    [source_cell], source.config, capacity,
                )
                plan = a48c8.paid_replication_early_noop_numpy(
                    binding, dt, source.config,
                )
                code = int(_A48C8_BRANCH_CODES[branch])
                if (int(plan.cell_count) != 1
                        or not bool(plan.scope_valid[0])
                        or int(plan.scope_error_code[0]) != a48c8.SCOPE_OK
                        or int(plan.branch_code[0]) != code
                        or int(plan.genome_count[0])
                        != len(source_cell.genomes)
                        or bool(plan.replication_active[0])
                        or bool(plan.has_empty_complete_genome[0])
                        or bool(plan.negative_atp[0])
                        or int(plan.last_replication_symbols_after[0]) != 0
                        or int(plan.rng_call_count[0]) != 0
                        or bool(plan.mutation_enabled) != bool(mutation)):
                    raise AssertionError(label + ' NumPy descriptor differs')
                if (branch == 'replicase-gate'
                        and float(plan.replicase_activity[0]) > 1e-6):
                    raise AssertionError(label + ' gate activity differs')
                if (branch == 'inactive-multi-genome'
                        and float(plan.replicase_activity[0]) <= 1e-6):
                    raise AssertionError(label + ' multi activity differs')
                v3.assert_recursive_close(
                    source_before, source.state_dict(), 0.0, 0.0,
                    label + '.plan_purity',
                )
                details.append('%s:m%d:p%d' % (
                    branch, int(mutation), int(primed),
                ))
    return ('16 direct Formal066 rows fix disabled->no-genome->replicase-'
            'gate->inactive-multi order; only last-symbol zero, PCG64/'
            'identity exact; NumPy plan pure: ' + '/'.join(details))


def _a48c8_assert_plan_row(plan, case, label):
    code = int(case['branch_code'])
    if (int(plan.cell_count) != 1
            or not bool(plan.cell_mask[0])
            or not bool(plan.scope_valid[0])
            or int(plan.scope_error_code[0]) != a48c8.SCOPE_OK
            or int(plan.branch_code[0]) != code
            or int(plan.last_replication_symbols_after[0]) != 0
            or int(plan.rng_call_count[0]) != 0
            or a48c8.BRANCH_NAMES.get(code) != case['label']):
        raise AssertionError(label + ' early-noop plan row differs')


_A48C8_PLAN_STATE_KEYS = (
    'schema_version', 'cell_capacity', 'cell_count',
    'source_provenance', 'dt_hex', 'config_sha256',
    'genome_replication_enabled', 'external_replicase_enabled',
    'mutation_enabled', 'cell_ids', 'cell_mask', 'scope_valid',
    'scope_error_code', 'branch_code', 'replicase_activity',
    'genome_count', 'replication_active',
    'has_empty_complete_genome', 'negative_atp',
    'last_replication_symbols_after', 'rng_call_count',
)


def _a48c8_capacity_for_cells(cells, spare_cells=0):
    sequences = []
    protein_capacity = 1
    for cell in cells:
        cell_sequences = list(cell.genomes)
        if cell.replication_template is not None:
            cell_sequences.extend((
                cell.replication_template, cell.replication_copy,
            ))
        sequences.extend(cell_sequences)
        gene_keys = set(int(value) for value in cell.gene_specs)
        protein_capacity = max(
            protein_capacity,
            len(gene_keys.union(int(value) for value in cell.proteins)),
            len(gene_keys.union(
                int(value) for value in cell.damaged_proteins
            )),
        )
    return a4.GPU068A4Config(
        max_cells=max(len(cells) + int(spare_cells), 1),
        max_sequences=max(len(sequences), 1),
        max_symbols=max(sum(len(value) for value in sequences), 1),
        max_sequence_symbols=max(
            [1] + [len(value) for value in sequences]
        ),
        max_proteins_per_cell=protein_capacity,
    )


def _a48c8_mixed_plan_fixture(seed=17340):
    no_world, no_genome, _, dt = _a48c8_make_world(
        'no-genome', seed=seed,
    )
    _, gate, _, _ = _a48c8_make_world(
        'replicase-gate', seed=seed + 1,
    )
    source_world, inactive_multi, _, _ = _a48c8_make_world(
        'inactive-multi-genome', seed=seed + 2,
    )
    normal = copy.deepcopy(inactive_multi)
    normal.genomes.pop()
    normal.genome_lesions.pop()
    normal._refresh_gene_cache()
    empty = copy.deepcopy(normal)
    empty.genomes = [np.zeros((0,), dtype=np.uint8)]
    empty.genome_lesions = [0.0]
    empty._refresh_gene_cache()
    negative = copy.deepcopy(normal)
    negative.pools[a4.a3.POOL_ATP] = -1e-12
    cells = [no_genome, gate, inactive_multi, normal, empty, negative]
    for index, cell in enumerate(cells):
        cell.cell_id = 48380 + index
    config = copy.deepcopy(source_world.config)
    config.genome_replication = True
    config.external_replicase = False
    config.mutation = False
    capacity = _a48c8_capacity_for_cells(cells, spare_cells=2)
    ragged, state, binding = _paid_replication_binding(
        cells, config, capacity,
    )
    # Keep this local world only as a config authority; no planner may mutate
    # either it or the six independently constructed source cells.
    no_world.config = config
    return no_world, cells, capacity, float(dt), ragged, state, binding


def _a48c8_assert_plan_schema_roundtrip(plan, devices, label):
    if tuple(plan.state_dict()) != _A48C8_PLAN_STATE_KEYS:
        raise AssertionError(label + ' state_dict schema differs')
    plan.validate()
    original = plan.state_dict()
    host_clone = plan.clone()
    a48c8._plan_semantic_match(host_clone, plan)
    for name in a48c8._PLAN_ARRAY_FIELDS:
        left = np.asarray(getattr(plan, name))
        right = np.asarray(getattr(host_clone, name))
        if (left is right or np.shares_memory(left, right)):
            raise AssertionError(label + ' host clone aliases ' + name)
    detached_state = plan.state_dict()
    detached_state['branch_code'][0] = a48c8.BRANCH_NONE
    if int(plan.branch_code[0]) == a48c8.BRANCH_NONE:
        raise AssertionError(label + ' state_dict aliases plan storage')
    for device in devices:
        resident = plan.to_torch(device)
        resident.validate()
        readback = resident.to_numpy()
        a48c8._plan_semantic_match(readback, plan)
        resident_clone = resident.clone()
        resident_clone.validate()
        if any(
                resident.data_ptrs()[name]
                == resident_clone.data_ptrs()[name]
                for name in a48c8._PLAN_ARRAY_FIELDS):
            raise AssertionError(
                label + '/' + device + ' resident clone aliases storage'
            )
        a48c8._plan_semantic_match(resident_clone.to_numpy(), plan)
        roundtrip = readback.to_torch(device).to_numpy()
        a48c8._plan_semantic_match(roundtrip, plan)
        resident_state = resident.state_dict()
        if tuple(resident_state) != _A48C8_PLAN_STATE_KEYS:
            raise AssertionError(
                label + '/' + device + ' resident state schema differs'
            )
        resident_state['branch_code'][0].fill_(a48c8.BRANCH_NONE)
        a48c8._plan_semantic_match(resident.to_numpy(), plan)
    a48c8._plan_semantic_match(
        a48c8.A4ReplicationEarlyNoopPlan(**original), plan,
    )


def _a48c8_replicase_boundary_fixture(target, seed):
    world, source_cells, capacity, dt = _paid_replication_b_fixture(
        seed=seed,
    )
    cell = copy.deepcopy(source_cells[0])
    _a48c_reduce_world(world, [cell])
    world.config.external_replicase = False
    ragged, state, initial = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    specs = initial.cache.materialize_gene_specs_host()[0]
    replicase_positions = []
    donor_positions = []
    for position in range(int(state.active_count[0])):
        fingerprint = int(state.active_fingerprints[0, position])
        spec = specs.get(fingerprint)
        if (spec is not None
                and int(spec['role']) == int(a4.a3.ROLE_REPLICASE)):
            replicase_positions.append(position)
        else:
            donor_positions.append(position)
    if len(replicase_positions) != 1 or not donor_positions:
        raise AssertionError('A4.8c8 boundary protein shape drifted')
    rep_position = replicase_positions[0]
    donor_position = donor_positions[0]
    rep_fingerprint = int(state.active_fingerprints[0, rep_position])
    donor_fingerprint = int(state.active_fingerprints[0, donor_position])
    efficiency = float(specs[rep_fingerprint]['efficiency'])
    pools = state.pools[0]
    volume = max(
        0.20,
        (float(state.radius[0]) / float(a4.s5.BASE_RADIUS)) ** 2,
    )
    concentration = float(pools[a4.a3.POOL_AGGREGATE] / volume)
    active = max(0.0, float(pools[a4.a3.POOL_CATALYST]))
    damaged = float(pools[a4.a3.POOL_DAMAGED_PROTEIN])
    aggregate = float(pools[a4.a3.POOL_AGGREGATE])
    functional = active / max(1e-9, active + damaged + aggregate)
    toxicity = 1.0 / (1.0 + 3.6 * concentration)
    proteostasis = float(np.clip(functional * toxicity, 0.02, 1.0))
    genome_factor = 1.0 / (
        1.0 + 0.85 * float(state.genome_lesion_mean[0])
    )
    target_mass = (
        (float(target) * 0.040 / (proteostasis * genome_factor))
        / efficiency
    )
    if np.float64(target) < np.float64(1e-6):
        # Preserve the requested derived nextafter control despite the final
        # grouped mass*efficiency/proteostasis rounding in Formal066.
        target_mass = float(np.nextafter(
            np.float64(target_mass), np.float64(np.inf),
        ))
    old_mass = float(cell.proteins[rep_fingerprint])
    transfer = old_mass - target_mass
    cell.proteins[rep_fingerprint] = target_mass
    cell.proteins[donor_fingerprint] = (
        float(cell.proteins[donor_fingerprint]) + transfer
    )
    cell._sync_protein_pool()
    ragged, state, binding = _paid_replication_binding(
        [cell], world.config, capacity,
    )
    observed = a44._numpy_replicase(
        binding, 0, binding.cache.materialize_gene_specs_host()[0],
    )
    if not a44._numpy_fp64_comparison_boundary(observed, 1e-6):
        raise AssertionError('A4.8c8 live replicase missed fp64 guard')
    world.initial_total_material = world.total_material()
    world.last_step_material_residual = 0.0
    return world, cell, capacity, float(dt), binding, float(observed)


def _a48c8_tamper_candidate(candidate, kind):
    if kind == 'host_ragged_content':
        candidate.source_binding.ragged.symbols[0] ^= np.uint8(1)
    elif kind == 'host_state_content':
        candidate.source_binding.state.pools[0, a4.a3.POOL_ATP] += 1e-6
    elif kind == 'host_cache_content':
        candidate.source_binding.cache.payloads[0, 0] ^= np.uint8(1)
    elif kind == 'host_plan_content':
        candidate.host_plan.branch_code[0] = a48c8.BRANCH_DISABLED
    elif kind == 'resident_ragged_data':
        candidate.resident_binding.ragged.symbols.data.view(-1)[0].bitwise_xor_(1)
    elif kind == 'resident_state_data':
        candidate.resident_binding.state.pools.data[
            0, a4.a3.POOL_ATP
        ].add_(1e-6)
    elif kind == 'resident_cache_data':
        candidate.resident_binding.cache.payloads.data.view(-1)[0].bitwise_xor_(1)
    elif kind == 'resident_plan_data':
        candidate.resident_plan.branch_code.data[0].fill_(
            a48c8.BRANCH_DISABLED
        )
    elif kind == 'readback_plan_content':
        candidate.plan.branch_code[0] = a48c8.BRANCH_DISABLED
    elif kind == 'candidate_branch_scalar':
        candidate.branch_code = a48c8.BRANCH_DISABLED
    elif kind == 'candidate_scope_scalar':
        candidate.scope_error_code = a48c8.SCOPE_NOT_EARLY_NOOP
    else:
        raise AssertionError('unknown A4.8c8 candidate tamper')


def _a48c8_assert_candidate_tamper(case, device, kind):
    world = _a48c8_world_for_case(case)
    cell = world.cells[0]
    scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
        case['capacity'], device,
    )
    _a48c_begin_replication_direct(
        scheduler, world, cell, case['dt'],
    )
    before = v3.pickle_clone(cell.state_dict())
    identities = _a48c_identity_snapshot(world, cell)
    rng_state = copy.deepcopy(world.rng.bit_generator.state)
    energy = float(world.dissipated_energy)
    try:
        candidate = scheduler._prepare_replication_early_noop_candidate(
            world, cell, case['dt'], world.config,
        )
        _a48c8_tamper_candidate(candidate, kind)
        _assert_raises(
            a48c8.A4ReplicationEarlyNoopCommitError,
            lambda: scheduler._commit_replication_early_noop_candidate(
                world, cell, case['dt'], world.config, candidate,
            ),
        )
        if candidate.consumed:
            raise AssertionError(kind + ' consumed rejected candidate')
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0,
            'a48c8.tamper.%s.%s' % (device, kind),
        )
        _, record = scheduler._record(cell)
        if (world.rng.bit_generator.state != rng_state
                or _a48c_identity_snapshot(world, cell) != identities
                or float(world.dissipated_energy) != energy
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(
                '%s/%s crossed the A4.8c8 claim boundary' % (
                    device, kind,
                )
            )
    finally:
        _a48_abort_direct(scheduler, cell)


def test_a48c8_candidate_cpu_cuda_resident_classifier_purity_and_fresh_binding():
    devices = _a48c8_devices()
    mixed_world, mixed_cells, mixed_capacity, mixed_dt, mixed_ragged, \
        mixed_state, mixed_binding = _a48c8_mixed_plan_fixture()
    mixed_world_before = v3.pickle_clone(mixed_world.state_dict())
    mixed_cells_before = [
        v3.pickle_clone(cell.state_dict()) for cell in mixed_cells
    ]
    mixed_ragged_before = mixed_ragged.state_dict()
    mixed_state_before = mixed_state.state_dict()
    mixed_cache_before = mixed_binding.cache.state_dict()
    mixed_plan = a48c8.paid_replication_early_noop_numpy(
        mixed_binding, mixed_dt, mixed_world.config,
    )
    expected_branch = np.asarray((2, 3, 4, 0, 0, 0), np.int64)
    expected_error = np.asarray((0, 0, 0, 1, 2, 3), np.int64)
    if (int(mixed_plan.cell_count) != 6
            or int(mixed_plan.cell_capacity) != 8
            or not np.array_equal(
                mixed_plan.branch_code[:6], expected_branch,
            )
            or not np.array_equal(
                mixed_plan.scope_error_code[:6], expected_error,
            )
            or not np.array_equal(
                mixed_plan.scope_valid[:6],
                np.asarray((True, True, True, False, False, False)),
            )
            or np.any(mixed_plan.cell_mask[6:])
            or np.any(mixed_plan.branch_code[6:])
            or np.any(mixed_plan.scope_error_code[6:])):
        raise AssertionError('A4.8c8 enabled six-row matrix differs')
    _a48c8_assert_plan_schema_roundtrip(
        mixed_plan, devices, 'a48c8.mixed',
    )
    for device in devices:
        mixed_resident_binding = a4.bind_a4_translation(
            mixed_ragged.to_torch(device=device),
            mixed_state.to_torch(device=device),
        )
        resident_inputs = (
            ('ragged', mixed_resident_binding.ragged, a4._ARRAY_FIELDS),
            ('state', mixed_resident_binding.state,
             a4._TRANSLATION_ARRAY_FIELDS),
            ('cache', mixed_resident_binding.cache,
             a4._GENE_ARRAY_FIELDS),
        )
        resident_input_snapshots = {}
        for input_label, batch, names in resident_inputs:
            resident_input_snapshots[input_label] = {
                'pointers': {
                    name: int(getattr(batch, name).data_ptr())
                    for name in names
                },
                'versions': {
                    name: int(getattr(batch, name)._version)
                    for name in names
                },
                'content': batch.to_numpy().state_dict(),
            }
        mixed_resident = a48c8.paid_replication_early_noop_torch(
            mixed_resident_binding, mixed_dt, mixed_world.config,
        )
        a48c8._plan_semantic_match(
            mixed_resident.to_numpy(), mixed_plan,
        )
        for input_label, batch, names in resident_inputs:
            expected_input = resident_input_snapshots[input_label]
            if ({
                    name: int(getattr(batch, name).data_ptr())
                    for name in names
                    } != expected_input['pointers']
                    or {
                        name: int(getattr(batch, name)._version)
                        for name in names
                    } != expected_input['versions']):
                raise AssertionError(
                    'a48c8.mixed.%s.%s pointer/version changed' % (
                        device, input_label,
                    )
                )
            v3.assert_recursive_close(
                expected_input['content'], batch.to_numpy().state_dict(),
                0.0, 0.0,
                'a48c8.mixed.%s.%s.content' % (
                    device, input_label,
                ),
            )

    disabled_cells = [
        copy.deepcopy(mixed_cells[0]), copy.deepcopy(mixed_cells[2]),
    ]
    for index, cell in enumerate(disabled_cells):
        cell.cell_id = 48420 + index
    disabled_config = copy.deepcopy(mixed_world.config)
    disabled_config.genome_replication = False
    disabled_capacity = _a48c8_capacity_for_cells(
        disabled_cells, spare_cells=1,
    )
    disabled_ragged, disabled_state, disabled_binding = (
        _paid_replication_binding(
            disabled_cells, disabled_config, disabled_capacity,
        )
    )
    disabled_plan = a48c8.paid_replication_early_noop_numpy(
        disabled_binding, mixed_dt, disabled_config,
    )
    if (not np.array_equal(
            disabled_plan.branch_code[:2],
            np.asarray((1, 1), np.int64),
            )
            or not np.array_equal(
                disabled_plan.scope_valid[:2],
                np.asarray((True, True)),
            )
            or np.any(disabled_plan.scope_error_code[:2])):
        raise AssertionError('A4.8c8 disabled priority batch differs')
    for device in devices:
        disabled_resident_binding = a4.bind_a4_translation(
            disabled_ragged.to_torch(device=device),
            disabled_state.to_torch(device=device),
        )
        disabled_resident = a48c8.paid_replication_early_noop_torch(
            disabled_resident_binding, mixed_dt, disabled_config,
        )
        a48c8._plan_semantic_match(
            disabled_resident.to_numpy(), disabled_plan,
        )

    malformed_dtype = mixed_plan.clone()
    malformed_dtype.branch_code = np.asarray(
        malformed_dtype.branch_code, dtype=np.int32,
    )
    _assert_raises(
        a48c8.A4ReplicationEarlyNoopCommitError,
        malformed_dtype.clone,
    )
    malformed_relation = mixed_plan.clone()
    malformed_relation.scope_valid[0] = False
    _assert_raises(
        a48c8.A4ReplicationEarlyNoopCommitError,
        malformed_relation.clone,
    )
    malformed_schema = mixed_plan.clone()
    malformed_schema.schema_version += '-tampered'
    _assert_raises(
        a48c8.A4ReplicationEarlyNoopCommitError,
        malformed_schema.validate,
    )

    v3.assert_recursive_close(
        mixed_world_before, mixed_world.state_dict(), 0.0, 0.0,
        'a48c8.mixed.world_purity',
    )
    for index, (before, cell) in enumerate(zip(
            mixed_cells_before, mixed_cells)):
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0,
            'a48c8.mixed.cell[%d]' % index,
        )
    for label, before, after in (
            ('ragged', mixed_ragged_before, mixed_ragged.state_dict()),
            ('state', mixed_state_before, mixed_state.state_dict()),
            ('cache', mixed_cache_before, mixed_binding.cache.state_dict())):
        v3.assert_recursive_close(
            before, after, 0.0, 0.0,
            'a48c8.mixed.' + label,
        )

    boundary_details = []
    threshold = np.float64(1e-6)
    boundary_targets = (
        ('below', np.nextafter(threshold, np.float64(0.0))),
        ('exact', threshold),
        ('above', np.nextafter(threshold, np.float64(np.inf))),
    )
    for index, (boundary_label, target) in enumerate(boundary_targets):
        boundary_world, boundary_cell, boundary_capacity, boundary_dt, \
            boundary_binding, observed = _a48c8_replicase_boundary_fixture(
                target, 17360 + index,
            )
        if np.float64(observed) != np.float64(target):
            raise AssertionError(
                'A4.8c8 %s replicase nextafter control differs: %s != %s' % (
                    boundary_label, observed, target,
                )
            )
        boundary_plan = a48c8.paid_replication_early_noop_numpy(
            boundary_binding, boundary_dt, boundary_world.config,
        )
        if (bool(boundary_plan.scope_valid[0])
                or int(boundary_plan.scope_error_code[0])
                != a48c8.SCOPE_FP64_COMPARISON_BOUNDARY
                or int(boundary_plan.branch_code[0])
                != a48c8.BRANCH_NONE):
            raise AssertionError(
                'A4.8c8 %s NumPy boundary row differs' % boundary_label
            )
        for device in devices:
            resident_binding = a4.bind_a4_translation(
                boundary_binding.ragged.to_torch(device=device),
                boundary_binding.state.to_torch(device=device),
            )
            resident_plan = a48c8.paid_replication_early_noop_torch(
                resident_binding, boundary_dt, boundary_world.config,
            )
            readback = resident_plan.to_numpy()
            a48c8._plan_semantic_match(readback, boundary_plan)
            if (bool(readback.scope_valid[0])
                    or int(readback.scope_error_code[0])
                    != a48c8.SCOPE_FP64_COMPARISON_BOUNDARY
                    or int(readback.branch_code[0])
                    != a48c8.BRANCH_NONE):
                raise AssertionError(
                    'A4.8c8 %s/%s boundary row differs' % (
                        device, boundary_label,
                    )
                )
            boundary_case = {
                'state': v3.pickle_clone(boundary_world.state_dict()),
                'cell_id': int(boundary_cell.cell_id),
                'dt': boundary_dt, 'capacity': boundary_capacity,
            }
            _a48c8_assert_preclaim_failure(
                boundary_case, boundary_capacity, device,
                'replicase_boundary_' + boundary_label,
            )
        boundary_details.append(
            '%s:%s' % (boundary_label, float(observed).hex())
        )

    cases = [
        _a48c8_case(
            branch, seed=17300 + index,
            mutation=bool(index % 2), prime_uint32=bool((index + 1) % 2),
        )
        for index, branch in enumerate(_A48C8_BRANCH_CODES)
    ]
    details = []
    for case in cases:
        for device in devices:
            direct = _a48c8_world_for_case(case)
            direct.cells[0]._replicate_genome(
                direct, case['dt'], direct.config,
            )
            world = _a48c8_world_for_case(case)
            cell = world.cells[0]
            scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
                case['capacity'], device,
            )
            _a48c_begin_replication_direct(
                scheduler, world, cell, case['dt'],
            )
            before = v3.pickle_clone(cell.state_dict())
            identities = _a48c_identity_snapshot(world, cell)
            rng_state = copy.deepcopy(world.rng.bit_generator.state)
            energy = float(world.dissipated_energy)
            try:
                candidate = (
                    scheduler._prepare_replication_early_noop_candidate(
                        world, cell, case['dt'], world.config,
                    )
                )
                v3.assert_recursive_close(
                    before, cell.state_dict(), 0.0, 0.0,
                    'a48c8.prepare.%s.%s' % (device, case['label']),
                )
                if (world.rng.bit_generator.state != rng_state
                        or _a48c_identity_snapshot(world, cell) != identities
                        or float(world.dissipated_energy) != energy):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' prepare changed live authority'
                    )
                _a48c8_assert_plan_row(
                    candidate.host_plan, case,
                    'a48c8.host.%s.%s' % (device, case['label']),
                )
                _a48c8_assert_plan_row(
                    candidate.plan, case,
                    'a48c8.readback.%s.%s' % (device, case['label']),
                )
                resident = candidate.resident_plan
                for name in a48c8._PLAN_ARRAY_FIELDS:
                    value = getattr(resident, name)
                    if (not isinstance(value, torch.Tensor)
                            or value.device.type != torch.device(device).type):
                        raise AssertionError(
                            device + ' resident plan escaped device: ' + name
                        )
                a48c8._plan_semantic_match(
                    resident.to_numpy(), candidate.host_plan,
                )
                if (candidate.branch_code != case['branch_code']
                        or candidate.scope_error_code != a48c8.SCOPE_OK
                        or candidate.live_rng is not world.rng
                        or candidate.rng_state != rng_state):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' candidate association differs'
                    )
                scheduler._commit_replication_early_noop_candidate(
                    world, cell, case['dt'], world.config, candidate,
                )
                _a48c8_assert_only_last_zero(
                    before, identities, rng_state, energy,
                    world, cell,
                    'a48c8.commit.%s.%s' % (device, case['label']),
                )
                v3.assert_recursive_close(
                    direct.cells[0].state_dict(), cell.state_dict(),
                    0.0, 0.0,
                    'a48c8.direct.%s.%s' % (device, case['label']),
                )
                entry = _a48c_replication_entry(scheduler, cell)
                metadata = entry['metadata']
                if (entry['status'] != 'executed'
                        or metadata.get('authority') != _A48C8_AUTHORITY
                        or metadata.get('branch') != case['label']
                        or metadata.get('device') != device
                        or metadata.get('amount') != 0
                        or metadata.get('work_performed') is not False
                        or metadata.get('rng_call_count') != 0
                        or metadata.get('last_replication_symbols') != 0
                        or not isinstance(
                            metadata.get('classifier_sha256'), str,
                        )
                        or len(metadata['classifier_sha256']) != 64):
                    raise AssertionError(
                        device + '/' + case['label']
                        + ' receipt metadata differs'
                    )
                committed = v3.pickle_clone(cell.state_dict())
                _assert_raises(
                    a48c8.A4ReplicationEarlyNoopCommitError,
                    lambda: scheduler.
                    _commit_replication_early_noop_candidate(
                        world, cell, case['dt'], world.config, candidate,
                    ),
                )
                v3.assert_recursive_close(
                    committed, cell.state_dict(), 0.0, 0.0,
                    'a48c8.one_shot.%s.%s' % (device, case['label']),
                )
                details.append(device + ':' + case['label'])
            finally:
                _a48_abort_direct(scheduler, cell)

    trust_case = _a48c8_case(
        'inactive-multi-genome', seed=17390,
        mutation=True, prime_uint32=True,
    )
    host_kinds = (
        'host_ragged_content', 'host_state_content',
        'host_cache_content', 'host_plan_content',
        'readback_plan_content', 'candidate_branch_scalar',
        'candidate_scope_scalar',
    )
    resident_kinds = (
        'resident_ragged_data', 'resident_state_data',
        'resident_cache_data', 'resident_plan_data',
    )
    for kind in host_kinds:
        _a48c8_assert_candidate_tamper(trust_case, 'cpu', kind)
    for device in devices:
        for kind in resident_kinds:
            _a48c8_assert_candidate_tamper(trust_case, device, kind)
    return ('enabled six-row branches 2/3/4/0/0/0 errors 0/0/0/1/2/3; '
            'disabled two-row branch1; plan clone/to_numpy/to_torch/state '
            'schema exact and dtype/relation/schema malformed fail closed; '
            'replicase nextafter/exact guard %s; NumPy/Torch candidate and '
            'resident readback commit exact %s; host ragged/state/cache/plan '
            '+readback/scalars and resident ragged/state/cache/plan .data '
            'tamper fail closed (%d checks)' % (
                '/'.join(boundary_details), '/'.join(details),
                len(host_kinds) + len(devices) * len(resident_kinds),
            ))


def _a48c8_assert_preclaim_failure(case, capacity, device, label,
                                   scheduler_type=None, mutator=None):
    world = _a48c8_world_for_case(case)
    cell = world.cells[0]
    if mutator is not None:
        mutator(world, cell)
    scheduler_type = (
        scheduler_type or a48c8.A4ReplicationEarlyNoopEventScheduler
    )
    scheduler = scheduler_type(capacity, device)
    _a48c_begin_replication_direct(
        scheduler, world, cell, case['dt'],
    )
    before = v3.pickle_clone(cell.state_dict())
    identities = _a48c_identity_snapshot(world, cell)
    rng_state = copy.deepcopy(world.rng.bit_generator.state)
    energy = float(world.dissipated_energy)
    try:
        _assert_raises(
            (a4.A4Error, a3s.A3SchedulerError),
            lambda: scheduler.cpu_replication(
                world, cell, case['dt'], world.config,
            ),
        )
        v3.assert_recursive_close(
            before, cell.state_dict(), 0.0, 0.0,
            'a48c8.preclaim.%s.%s' % (device, label),
        )
        _, record = scheduler._record(cell)
        if (world.rng.bit_generator.state != rng_state
                or _a48c_identity_snapshot(world, cell) != identities
                or float(world.dissipated_energy) != energy
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(
                device + '/' + label + ' changed state before claim'
            )
    finally:
        _a48_abort_direct(scheduler, cell)


def _a48c8_assert_candidate_association_reject(case, kind):
    world = _a48c8_world_for_case(case)
    cell = world.cells[0]
    scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
        case['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        scheduler, world, cell, case['dt'],
    )
    original_device = scheduler.a4_device
    candidate = None
    other_world = None
    try:
        candidate = scheduler._prepare_replication_early_noop_candidate(
            world, cell, case['dt'], world.config,
        )
        commit_world = world
        commit_cell = cell
        commit_dt = case['dt']
        commit_config = world.config
        if kind == 'wrong_cell':
            commit_cell = copy.deepcopy(cell)
            commit_cell.cell_id = int(cell.cell_id) + 100000
        elif kind == 'wrong_world_cell':
            other_world = _a48c8_world_for_case(case)
            commit_world = other_world
            commit_cell = other_world.cells[0]
            commit_config = other_world.config
        elif kind == 'wrong_dt':
            commit_dt = float(np.nextafter(
                np.float64(case['dt']), np.float64(np.inf),
            ))
        elif kind == 'wrong_rng':
            world.rng.random()
        elif kind == 'wrong_device':
            scheduler.a4_device = 'cuda'
        else:
            raise AssertionError('unknown A4.8c8 association fixture')
        before = v3.pickle_clone(world.state_dict())
        identities = _a48c_identity_snapshot(world, cell)
        rng_state = copy.deepcopy(world.rng.bit_generator.state)
        other_before = (
            None if other_world is None
            else v3.pickle_clone(other_world.state_dict())
        )
        _assert_raises(
            a48c8.A4ReplicationEarlyNoopCommitError,
            lambda: scheduler._commit_replication_early_noop_candidate(
                commit_world, commit_cell, commit_dt,
                commit_config, candidate,
            ),
        )
        if candidate.consumed:
            raise AssertionError(kind + ' consumed rejected candidate')
        v3.assert_recursive_close(
            before, world.state_dict(), 0.0, 0.0,
            'a48c8.association.' + kind,
        )
        if other_world is not None:
            v3.assert_recursive_close(
                other_before, other_world.state_dict(), 0.0, 0.0,
                'a48c8.association.other.' + kind,
            )
        _, record = scheduler._record(cell)
        if (_a48c_identity_snapshot(world, cell) != identities
                or world.rng.bit_generator.state != rng_state
                or any(entry['event'] == 'replication_cpu'
                       for entry in record['events'])):
            raise AssertionError(kind + ' crossed the A4.8c8 claim boundary')
    finally:
        scheduler.a4_device = original_device
        _a48_abort_direct(scheduler, cell)


def _a48c8_exclusion_case(kind, seed, mutation=False):
    world, cells, _, _ = _a45b_template_start_fixture(
        seed=seed, prime_uint32=False,
    )
    cell = copy.deepcopy(cells[0])
    _a48c_reduce_world(world, [cell])
    world.config.genome_replication = True
    world.config.external_replicase = True
    world.config.mutation = bool(mutation)
    cell.replication_template = None
    cell.replication_template_lesion = 0.0
    cell.replication_copy = []
    cell.replication_fractional = 0.0
    cell.last_replication_symbols = 733
    if kind == 'empty-genome':
        cell.genomes = [np.zeros((0,), dtype=np.uint8)]
        cell.genome_lesions = [0.0]
        cell._refresh_gene_cache()
    elif kind == 'negative-atp':
        cell.pools[a4.a3.POOL_ATP] = -1e-12
    else:
        raise AssertionError('unknown A4.8c8 exclusion fixture')
    world.initial_total_material = world.total_material()
    world.last_step_material_residual = 0.0
    capacity_values = _a48c8_capacity_values(cell)
    # Empty complete genomes still require one representable zero-length row.
    capacity_values['max_sequences'] = max(
        capacity_values['max_sequences'], 2,
    )
    capacity_values['max_symbols'] = max(capacity_values['max_symbols'], 2)
    return {
        'label': kind + ('-mutation' if mutation else '-deterministic'),
        'state': v3.pickle_clone(world.state_dict()),
        'cell_id': int(cell.cell_id), 'dt': 0.1,
        'capacity': a4.GPU068A4Config(**capacity_values),
    }


def test_a48c8_capacity_trust_exclusions_rollback_and_exact_once():
    devices = _a48c8_devices()
    capacity_case = _a48c8_case(
        'inactive-multi-genome', seed=17401,
        mutation=True, prime_uint32=True,
    )
    values = dict(capacity_case['capacity_values'])
    exact_tuple = tuple(values[name] for name in (
        'max_cells', 'max_sequences', 'max_symbols',
        'max_sequence_symbols', 'max_proteins_per_cell',
    ))
    if (exact_tuple[0] != 1 or exact_tuple[1] != 2
            or exact_tuple[2] <= 0 or exact_tuple[3] <= 0
            or exact_tuple[4] <= 1):
        raise AssertionError('A4.8c8 exact source capacity drifted')
    capacity_details = []
    for device in devices:
        direct = _a48c8_world_for_case(capacity_case)
        direct.cells[0]._replicate_genome(
            direct, capacity_case['dt'], direct.config,
        )
        world = _a48c8_world_for_case(capacity_case)
        cell = world.cells[0]
        scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
            a4.GPU068A4Config(**values), device,
        )
        _a48c_begin_replication_direct(
            scheduler, world, cell, capacity_case['dt'],
        )
        try:
            scheduler.cpu_replication(
                world, cell, capacity_case['dt'], world.config,
            )
            v3.assert_recursive_close(
                direct.cells[0].state_dict(), cell.state_dict(),
                0.0, 0.0, 'a48c8.capacity_exact.' + device,
            )
        finally:
            _a48_abort_direct(scheduler, cell)
        for axis, key in (
                ('Q', 'max_sequences'), ('S', 'max_symbols'),
                ('W', 'max_sequence_symbols'),
                ('P', 'max_proteins_per_cell')):
            short = dict(values)
            short[key] -= 1
            _a48c8_assert_preclaim_failure(
                capacity_case, a4.GPU068A4Config(**short), device,
                'capacity_' + axis,
            )
            capacity_details.append(device + ':' + axis)

    exclusions = (
        _a48c8_exclusion_case('empty-genome', 17420, mutation=False),
        _a48c8_exclusion_case('empty-genome', 17421, mutation=True),
        _a48c8_exclusion_case('negative-atp', 17422, mutation=False),
    )
    # These direct probes prove why they must not be relabelled ordinary no-op.
    empty_off = _a48c8_world_for_case(exclusions[0])
    empty_before_cycles = int(empty_off.cells[0].replication_cycles)
    empty_off.cells[0]._replicate_genome(
        empty_off, exclusions[0]['dt'], empty_off.config,
    )
    if (len(empty_off.cells[0].genomes) != 2
            or int(empty_off.cells[0].replication_cycles)
            != empty_before_cycles + 1):
        raise AssertionError('empty deterministic genome became a no-op')
    empty_on = _a48c8_world_for_case(exclusions[1])
    empty_rng = copy.deepcopy(empty_on.rng.bit_generator.state)
    empty_on.cells[0]._replicate_genome(
        empty_on, exclusions[1]['dt'], empty_on.config,
    )
    if (len(empty_on.cells[0].genomes) != 2
            or len(empty_on.cells[0].genomes[-1]) == 0
            or empty_on.rng.bit_generator.state == empty_rng):
        raise AssertionError('empty mutation genome became a no-op')
    negative = _a48c8_world_for_case(exclusions[2])
    negative.cells[0]._replicate_genome(
        negative, exclusions[2]['dt'], negative.config,
    )
    if (negative.cells[0].replication_template is None
            or not float(negative.cells[0].replication_fractional) < 0.0):
        raise AssertionError('negative ATP became a no-op')

    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in exclusions:
            for device in devices:
                _a48c8_assert_preclaim_failure(
                    case, case['capacity'], device,
                    'scope_' + case['label'],
                )
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('an A4.8c8 exclusion used CPU fallback')

    trust_case = _a48c8_case(
        'inactive-multi-genome', seed=17424,
        mutation=True, prime_uint32=True,
    )

    def stale_cache_nested(world, cell):
        fingerprint = next(iter(cell.gene_specs))
        cell.gene_specs[fingerprint]['parameter'] = (
            int(cell.gene_specs[fingerprint]['parameter']) + 1
        )

    def malformed_config_bool(world, cell):
        world.config.genome_replication = 0

    def malformed_source_copy(world, cell):
        cell.replication_copy = tuple(cell.replication_copy)

    for label, mutator in (
            ('stale_cache_nested', stale_cache_nested),
            ('malformed_config_bool', malformed_config_bool),
            ('malformed_source_copy', malformed_source_copy)):
        _a48c8_assert_preclaim_failure(
            trust_case, trust_case['capacity'], 'cpu', label,
            mutator=mutator,
        )

    association_kinds = (
        'wrong_cell', 'wrong_world_cell', 'wrong_dt',
        'wrong_rng', 'wrong_device',
    )
    for kind in association_kinds:
        _a48c8_assert_candidate_association_reject(trust_case, kind)

    nested_flags = (
        '_a4_replication_commit_active',
        '_a4_replication_completion_commit_active',
        '_a4_replication_completion_mutation_commit_active',
        '_a4_replication_start_commit_active',
        '_a4_replication_mutation_free_start_commit_active',
        '_a4_replication_start_completion_commit_active',
        '_a4_replication_start_completion_mutation_commit_active',
        '_a4_replication_early_noop_commit_active',
    )
    for flag in nested_flags:
        nested_world = _a48c8_world_for_case(trust_case)
        nested_cell = nested_world.cells[0]
        nested_scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
            trust_case['capacity'], 'cpu',
        )
        _a48c_begin_replication_direct(
            nested_scheduler, nested_world, nested_cell, trust_case['dt'],
        )
        before = v3.pickle_clone(nested_world.state_dict())
        identities = _a48c_identity_snapshot(nested_world, nested_cell)
        rng_state = copy.deepcopy(nested_world.rng.bit_generator.state)
        setattr(nested_scheduler, flag, True)
        try:
            _assert_raises(
                a3s.A3SchedulerProtocolError,
                lambda: nested_scheduler.cpu_replication(
                    nested_world, nested_cell, trust_case['dt'],
                    nested_world.config,
                ),
            )
            v3.assert_recursive_close(
                before, nested_world.state_dict(), 0.0, 0.0,
                'a48c8.nested.' + flag,
            )
            _, nested_record = nested_scheduler._record(nested_cell)
            if (_a48c_identity_snapshot(nested_world, nested_cell)
                    != identities
                    or nested_world.rng.bit_generator.state != rng_state
                    or any(entry['event'] == 'replication_cpu'
                           for entry in nested_record['events'])):
                raise AssertionError(flag + ' crossed the nested guard')
        finally:
            setattr(nested_scheduler, flag, False)
            _a48_abort_direct(nested_scheduler, nested_cell)

    # A dead cell is excluded from direct c8 entry; the full scheduler marks
    # the event skipped before any replication backend runs.
    dead_case = _a48c8_case('disabled', seed=17430)
    dead = _a48c8_world_for_case(dead_case)
    dead_cell = dead.cells[0]
    dead_scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
        dead_case['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        dead_scheduler, dead, dead_cell, dead_case['dt'],
    )
    dead_cell.alive = False
    dead_before = v3.pickle_clone(dead.state_dict())
    dead_ids = _a48c_identity_snapshot(dead, dead_cell)
    dead_rng = copy.deepcopy(dead.rng.bit_generator.state)
    try:
        _assert_raises(
            a48c8.A4ReplicationEarlyNoopCommitError,
            lambda: dead_scheduler.cpu_replication(
                dead, dead_cell, dead_case['dt'], dead.config,
            ),
        )
        v3.assert_recursive_close(
            dead_before, dead.state_dict(), 0.0, 0.0,
            'a48c8.dead_direct',
        )
        if (_a48c_identity_snapshot(dead, dead_cell) != dead_ids
                or dead.rng.bit_generator.state != dead_rng):
            raise AssertionError('dead direct exclusion changed authority')
    finally:
        _a48_abort_direct(dead_scheduler, dead_cell)

    dead_world = _a48c8_world_for_case(dead_case)
    dead_world.cells[0].alive = False
    dead_world.initial_total_material = dead_world.total_material()
    dead_state = v3.pickle_clone(dead_world.state_dict())
    dead_hybrid = _a48c8_hybrid_from_state(
        dead_state, dead_case['capacity'], 'cpu',
    )
    dead_receipt = dead_hybrid.step(dead_case['dt'])
    dead_entries = [
        entry for entry in dead_receipt['cells'][0]['events']
        if entry['event'] == 'replication_cpu'
    ]
    if (len(dead_entries) != 1
            or dead_entries[0]['status'] != 'skipped'
            or dead_entries[0]['metadata'].get('reason')
            != 'dead_before_metabolism'
            or dead_entries[0]['metadata'].get('authority') is not None):
        raise AssertionError('dead world replication skip differs')

    duplicate = _a48c8_world_for_case(capacity_case)
    duplicate_cell = duplicate.cells[0]
    duplicate_scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
        capacity_case['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        duplicate_scheduler, duplicate, duplicate_cell,
        capacity_case['dt'],
    )
    try:
        duplicate_scheduler.cpu_replication(
            duplicate, duplicate_cell, capacity_case['dt'], duplicate.config,
        )
        committed = v3.pickle_clone(duplicate.state_dict())
        committed_ids = _a48c_identity_snapshot(duplicate, duplicate_cell)
        committed_rng = copy.deepcopy(duplicate.rng.bit_generator.state)
        _assert_raises(
            a3s.A3DuplicateEventError,
            lambda: duplicate_scheduler.cpu_replication(
                duplicate, duplicate_cell,
                capacity_case['dt'], duplicate.config,
            ),
        )
        v3.assert_recursive_close(
            committed, duplicate.state_dict(), 0.0, 0.0,
            'a48c8.duplicate',
        )
        if (_a48c_identity_snapshot(duplicate, duplicate_cell)
                != committed_ids
                or duplicate.rng.bit_generator.state != committed_rng):
            raise AssertionError('duplicate c8 event changed authority')
    finally:
        _a48_abort_direct(duplicate_scheduler, duplicate_cell)

    order = _a48c8_world_for_case(capacity_case)
    order_cell = order.cells[0]
    order_scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
        capacity_case['capacity'], 'cpu',
    )
    _a48b_begin_translation_direct(
        order_scheduler, order, order_cell, capacity_case['dt'],
    )
    order_before = v3.pickle_clone(order.state_dict())
    order_ids = _a48c_identity_snapshot(order, order_cell)
    order_rng = copy.deepcopy(order.rng.bit_generator.state)
    try:
        _assert_raises(
            a3s.A3EventOrderError,
            lambda: order_scheduler.cpu_replication(
                order, order_cell, capacity_case['dt'], order.config,
            ),
        )
        v3.assert_recursive_close(
            order_before, order.state_dict(), 0.0, 0.0,
            'a48c8.out_of_order',
        )
        if (_a48c_identity_snapshot(order, order_cell) != order_ids
                or order.rng.bit_generator.state != order_rng):
            raise AssertionError('out-of-order c8 event changed authority')
    finally:
        _a48_abort_direct(order_scheduler, order_cell)

    class FailAfterPublish(a48c8.A4ReplicationEarlyNoopEventScheduler):
        def _publish_replication_early_noop_candidate(
                self, world, cell, candidate):
            super(
                FailAfterPublish, self,
            )._publish_replication_early_noop_candidate(
                world, cell, candidate,
            )
            cell.last_replication_symbols = 991
            cell.pools[a4.a3.POOL_ATP] += 0.125
            cell.genomes[0][0] ^= np.uint8(1)
            cell.genome_lesions.append(0.75)
            cell.mutation_events['a48c8_sentinel'] += 1
            cell.proteins[next(iter(cell.proteins))] += 0.25
            first = next(iter(cell.gene_specs))
            cell.gene_specs[first]['promoter'] += 0.25
            cell.age += 2.0
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.8c8 publish failure')

    rollback = _a48c8_world_for_case(capacity_case)
    rollback_cell = rollback.cells[0]
    rollback_scheduler = FailAfterPublish(
        capacity_case['capacity'], 'cpu',
    )
    _a48c_begin_replication_direct(
        rollback_scheduler, rollback, rollback_cell, capacity_case['dt'],
    )
    rollback_before = v3.pickle_clone(rollback.state_dict())
    rollback_ids = _a48c_identity_snapshot(rollback, rollback_cell)
    rollback_rng = copy.deepcopy(rollback.rng.bit_generator.state)
    caught = None
    try:
        rollback_scheduler.cpu_replication(
            rollback, rollback_cell, capacity_case['dt'], rollback.config,
        )
    except RuntimeError as error:
        caught = error
    if caught is None:
        raise AssertionError('injected A4.8c8 publish failure did not escape')
    v3.assert_recursive_close(
        rollback_before, rollback.state_dict(), 0.0, 0.0,
        'a48c8.publish_rollback',
    )
    if (_a48c_identity_snapshot(rollback, rollback_cell) != rollback_ids
            or rollback.rng.bit_generator.state != rollback_rng):
        raise AssertionError('A4.8c8 rollback lost identity/RNG')
    entry = _a48c_replication_entry(rollback_scheduler, rollback_cell)
    receipt = _a48_abort_direct(
        rollback_scheduler, rollback_cell, caught,
    )
    if (entry['event'] != 'replication_cpu'
            or entry['metadata'].get('authority') != _A48C8_AUTHORITY
            or receipt['status'] != 'aborted'):
        raise AssertionError('A4.8c8 publish failure receipt differs')

    return ('exact C/Q/S/W/P %s; each one-short %s; empty mutation-off/on '
            'and negative ATP direct non-noop but preclaim fail closed; '
            'stale nested cache/malformed bool config/source copy; wrong '
            'cell/world/dt/RNG/device association; c1-c8 nested guards; dead '
            'world skip; duplicate/order; bounded identity/RNG publish rollback' % (
                '/'.join(str(value) for value in exact_tuple),
                '/'.join(capacity_details),
            ))


def _a48c8_delegated_cases():
    cases = []
    c1_world, c1_cells, c1_capacity, c1_dt = (
        _a45_substitution_fixture(seed=17501)
    )
    _a48c_reduce_world(c1_world, [c1_cells[0]])
    cases.append({
        'label': 'c1-active-noncompletion',
        'state': v3.pickle_clone(c1_world.state_dict()),
        'dt': float(c1_dt), 'capacity': c1_capacity,
        'authority': 'A4.8c1-resident-plan-atomic-cpu-cell-rng-commit',
        'drop_lesions': False,
    })

    c2_state, _, c2_dt = _a48c2_completion_case(seed=17502, row=0)
    c2_before = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(c2_state),
    )
    c2_after.cells[0]._replicate_genome(
        c2_after, c2_dt, c2_after.config,
    )
    cases.append({
        'label': 'c2-mutation-free-active-completion',
        'state': c2_state, 'dt': float(c2_dt),
        'capacity': a4.GPU068A4Config(**_a48c2_capacity_for_transition(
            c2_before.cells[0], c2_after.cells[0],
        )),
        'authority': (
            'A4.8c2-resident-completion-plan-atomic-cpu-cell-commit'
        ),
        'drop_lesions': False,
    })

    c3 = _a48c3_completion_mutation_cases()[0]
    cases.append({
        'label': 'c3-mutation-active-completion',
        'state': c3['state'], 'dt': float(c3['dt']),
        'capacity': c3['integration_capacity'],
        'authority': (
            'A4.8c3-resident-completion-mutation-plan-atomic-'
            'cpu-cell-rng-commit'
        ),
        'drop_lesions': False,
    })

    for label, source, authority in (
            (
                'c4-mutation-start-noncompletion', _a48c4_cases()[0],
                'A4.8c4-resident-template-start-plan-atomic-'
                'cpu-cell-pcg64-commit',
            ),
            (
                'c5-mutation-free-start-noncompletion', _a48c5_cases()[0],
                'A4.8c5-resident-mutation-free-template-start-plan-'
                'atomic-cpu-cell-pcg64-commit',
            ),
            (
                'c6-mutation-free-start-completion', _a48c6_cases()[0],
                'A4.8c6-resident-mutation-free-start-completion-plan-'
                'selection-only-pcg64-atomic-commit',
            ),
            (
                'c7-mutation-start-completion', _a48c7_cases()[0],
                'A4.8c7-resident-mutation-enabled-start-completion-'
                'final-plan-pcg64-chain-atomic-commit',
            )):
        cases.append({
            'label': label, 'state': source['state'],
            'dt': float(source['dt']), 'capacity': source['capacity'],
            'authority': authority,
            'drop_lesions': bool(source.get('drop_lesions', False)),
        })
    return cases


def test_a48c8_inherited_c7_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority():
    devices = _a48c8_devices()
    delegated = []
    legacy_called = []
    original_legacy = a48b.A4TranslationEventScheduler.cpu_replication

    def legacy_bomb(*args, **kwargs):
        legacy_called.append(True)
        raise AssertionError('legacy CPU replication fallback was called')

    a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
    try:
        for case in _a48c8_delegated_cases():
            for device in devices:
                direct = _a48c7_world_for_case(case)
                world = _a48c7_world_for_case(case)
                direct.cells[0]._replicate_genome(
                    direct, case['dt'], direct.config,
                )
                scheduler = a48c8.A4ReplicationEarlyNoopEventScheduler(
                    case['capacity'], device,
                )
                _a48c_begin_replication_direct(
                    scheduler, world, world.cells[0], case['dt'],
                )
                try:
                    scheduler.cpu_replication(
                        world, world.cells[0], case['dt'], world.config,
                    )
                    _a48b_assert_cell_matches(
                        direct.cells[0], world.cells[0],
                        'a48c8.delegate.%s.%s' % (
                            device, case['label'],
                        ),
                        atol=a48c8.REPLICATION_ORACLE_ATOL,
                    )
                    metadata = _a48c_replication_entry(
                        scheduler, world.cells[0],
                    )['metadata']
                    if (direct.rng.bit_generator.state
                            != world.rng.bit_generator.state
                            or float(direct.dissipated_energy)
                            != float(world.dissipated_energy)
                            or metadata.get('authority')
                            != case['authority']):
                        raise AssertionError(
                            device + '/' + case['label']
                            + ' inherited authority differs'
                        )
                    delegated.append(device + ':' + case['label'])
                finally:
                    _a48_abort_direct(scheduler, world.cells[0])
    finally:
        a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
    if legacy_called:
        raise AssertionError('a c1-c7 delegate used frozen CPU fallback')

    successor_case = _a48c8_case(
        'disabled', seed=17540, mutation=True, prime_uint32=True,
    )
    successor_details = []
    for device in devices:
        expected = _a48c8_world_for_case(successor_case)
        hybrid = _a48c8_hybrid_from_state(
            successor_case['state'], successor_case['capacity'], device,
        )
        expected.step(successor_case['dt'])
        original_legacy = a48b.A4TranslationEventScheduler.cpu_replication
        a48b.A4TranslationEventScheduler.cpu_replication = legacy_bomb
        try:
            receipt = hybrid.step(successor_case['dt'])
        finally:
            a48b.A4TranslationEventScheduler.cpu_replication = original_legacy
        v3._assert_world_pair(
            expected, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL,
            label='a48c8.successor.' + device,
        )
        record = receipt['cells'][0]
        names = [entry['event'] for entry in record['events']]
        by_name = {entry['event']: entry for entry in record['events']}
        if (names.count('replication_cpu') != 1
                or names.count('genome_hydrolysis_cpu_rng') != 1
                or not names.index('translation_cpu')
                < names.index('replication_cpu')
                < names.index('surface_assembly')
                < names.index('genome_hydrolysis_cpu_rng')
                < names.index('motion')
                or by_name['replication_cpu']['status'] != 'executed'
                or by_name['replication_cpu']['metadata'].get('authority')
                != _A48C8_AUTHORITY
                or by_name['replication_cpu']['metadata'].get('branch')
                != 'disabled'):
            raise AssertionError(device + ' c8 successor order differs')
        successor_details.append(device)
    if legacy_called:
        raise AssertionError('owned A4.8c8 successor used CPU fallback')

    seed_hybrid = _a48c8_hybrid_from_state(
        successor_case['state'], successor_case['capacity'], 'cpu',
    )
    twin = seed_hybrid.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a48c8.pkl')
        seed_hybrid.save(path)
        restored = a48c8.Hybrid066WorldA4ReplicationEarlyNoop.load(path)
        for value in (seed_hybrid, twin, restored):
            if (type(value)
                    is not a48c8.Hybrid066WorldA4ReplicationEarlyNoop
                    or type(value.scheduler)
                    is not a48c8.A4ReplicationEarlyNoopEventScheduler
                    or value.scheduler.a4_device != 'cpu'
                    or value.scheduler.a4_config
                    != successor_case['capacity']
                    or value.scheduler.
                    replication_early_noop_integration_state() != {
                        'schema': a48c8.INTEGRATION_SCHEMA_VERSION,
                        'config': a48._config_state(
                            successor_case['capacity'],
                        ),
                        'device': 'cpu',
                    }):
                raise AssertionError('A4.8c8 save/clone authority was lost')
        for index, value in enumerate((seed_hybrid, twin, restored)):
            expected = _a48c8_world_for_case(successor_case)
            expected.step(successor_case['dt'])
            receipt = value.step(successor_case['dt'])
            v3._assert_world_pair(
                expected, value, state_atol=v3.WORLD_FP64_ATOL,
                ledger_atol=v3.LEDGER_ATOL,
                label='a48c8.persisted.%d' % index,
            )
            metadata = {
                entry['event']: entry['metadata']
                for entry in receipt['cells'][0]['events']
            }
            if metadata['replication_cpu'].get('authority') != _A48C8_AUTHORITY:
                raise AssertionError('persisted c8 authority differs')

    active = _a48c8_hybrid_from_state(
        successor_case['state'], successor_case['capacity'], 'cpu',
    )
    active.scheduler.begin_step(active.world, active.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, active.clone)
    active.scheduler.abort_step(RuntimeError('expected pending-save rejection'))
    for flag in (
            '_a4_replication_commit_active',
            '_a4_replication_completion_commit_active',
            '_a4_replication_completion_mutation_commit_active',
            '_a4_replication_start_commit_active',
            '_a4_replication_mutation_free_start_commit_active',
            '_a4_replication_start_completion_commit_active',
            '_a4_replication_start_completion_mutation_commit_active',
            '_a4_replication_early_noop_commit_active'):
        setattr(active.scheduler, flag, True)
        try:
            _assert_raises(a3s.A3SchedulerProtocolError, active.state_dict)
        finally:
            setattr(active.scheduler, flag, False)

    signature = inspect.signature(
        a48c8.A4ReplicationEarlyNoopEventScheduler.cpu_replication,
    )
    c8_names = tuple(fn.__name__ for fn in A48C8_TESTS)
    expected_status = (
        'a4.8c8-canonical-valid-alive-nonempty-nonnegative-atp-ordered-'
        'disabled-no-genome-replicase-gate-inactive-multi-genome-'
        'resident-early-noop-last-symbol-only-atomic-commit-plus-a4.8c7-'
        'c6-c5-c4-c3-c2-c1-inherited-normal-work'
    )
    if (tuple(signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or not callable(a48c8.paid_replication_early_noop_numpy)
            or not callable(a48c8.paid_replication_early_noop_torch)
            or not issubclass(
                a48c8.A4ReplicationEarlyNoopEventScheduler,
                a48c7.A4ReplicationStartCompletionMutationEventScheduler,
            )
            or not issubclass(
                a48c8.Hybrid066WorldA4ReplicationEarlyNoop,
                a48c7.Hybrid066WorldA4ReplicationStartCompletionMutation,
            )
            or a48c8.PORT_STATUS.get('genome_replication')
            != expected_status
            or a48c8.FULL_GPU_WORLD_STEP is not False
            or a48c8.PORT_STATUS.get('full_gpu_world_step') is not False
            or len(TESTS) != 82
            or len(A48C8_TESTS) != 4
            or len(VALIDATION_TESTS) != 86
            or c8_names != (
                'test_a48c8_pre_active_noop_branch_order_formal066_oracle',
                'test_a48c8_candidate_cpu_cuda_resident_classifier_purity_and_fresh_binding',
                'test_a48c8_capacity_trust_exclusions_rollback_and_exact_once',
                'test_a48c8_inherited_c7_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority',
            )
            or hashlib.sha256(
                '\n'.join(fn.__name__ for fn in TESTS).encode('utf-8')
            ).hexdigest() != _A48C8_PRIOR_TEST_NAMES_SHA256
            or _a48c7_prior_test_ast_sha256(TESTS)
            != _A48C8_PRIOR_TEST_AST_SHA256):
        raise AssertionError(
            'A4.8c8 API/scope/frozen82/total86 authority differs'
        )
    expected_hashes = dict(_A48C7_PRIOR_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_mutation_integration.py'
    ] = _A48C7_CORE_SHA256
    expected_hashes[
        'src/0_6_8/'
        'SOMA_CELL_0_6_8_gpu_a4_replication_early_noop_integration.py'
    ] = _A48C8_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    return ('c1-c7 CPU/CUDA delegates %s; owned successor %s; save/load/'
            'clone exact; c1-c8 active/pending save reject; frozen82 names/'
            'function-AST + total86 + A3/pure/c1-c8 hashes/full_gpu=false' % (
                '/'.join(delegated), '/'.join(successor_details),
            ))


_A49A_CORE_SHA256 = (
    '03d689830b71dced3531d8d0a289faaecf3b22553cb5c51f9e5cb6071536982a'
)

_A49A_PRIOR_86_NAMES_SHA256 = (
    '43671c2a9c328c932260a8f80bdce2995454cd9a4a28fa81a508d841293e1d38'
)

_A49A_PRIOR_86_AST_SHA256 = (
    '23dccb9ac7115c260562ba2e77abfe194b6fb80646e49ed003ec6ce1b5dc1df5'
)

_A49A_FROZEN_CORE_SHA256 = {
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3.py': (
        '6ca66378702dabd5a388553ca92279376deae5ba8c39bea9bdbe3488aa41b4f6'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a3_scheduler.py': (
        '5e207f68fd0d9aa674ab054dc43d62247e84c1f3c61fc6c7b455960ff84413aa'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4.py': (
        '84ba52d6f6185e298ae10358c2a44172f049cef21bc6add5d2ed4d6d7aabbb8e'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication.py': (
        'c4e27520b8665ba036322e9b1e23ccec280b40ad5d19016ba523bd4bf3dd1e0a'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_hydrolysis.py': (
        '210a64f4906309aaeb2fb9ef2e363f1f07d104e830334f11cdbc26d6941b7019'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_integration.py': (
        'f380293f66d2ca78ce2b927105be38a785181938516fcf00b842b8f9c24f7cb0'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_translation_integration.py': (
        'ec86f855287d20fcb300daa47b1b5a615720b18642b999251c3e2a499b99bc20'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_integration.py': (
        'fa9787a6dfc59a7767fcc00095ad052f7afe77877eef78b6e02e992015b2bbef'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_integration.py': (
        '6bf4c979b524c176c2e72b1630b14f630638cdd7eb883bdeeb48208e9ba5b416'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_completion_mutation_integration.py': (
        '9e34a63322e717f646f225588abd3f908e335c5527bc3068bd357d438f73057c'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_integration.py': (
        '10f3e357e1b2466245ba10add7b22599b4a2853abf2c7bb5cd1f247dc022fd3d'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_mutation_free_integration.py': (
        'e433e7f051cd265bcd2350c396f8aa1365905197e815b101767d12aa1be625ec'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_integration.py': (
        '6dd747d9fb1451b9ddcbc790b4e695108763975313dda3d241ce982e383cc8d8'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_start_completion_mutation_integration.py': (
        '91c1c8ae7e3f623100353d2e51a8a12b96454d2e08de262031c1d97b8f5de7fb'
    ),
    'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_replication_early_noop_integration.py': (
        '4bfe5ef02c20bb435319c4e6d59520fe863a61d7995d836e7fd159576f6ab36f'
    ),
}

_A49A_PUBLIC_API = (
    'BUILD', 'BUILD_ID', 'BUILD_LONG', 'SCHEMA_VERSION',
    'FULL_GPU_WORLD_STEP', 'COHERENT', 'CPU_NEWER', 'INVALID',
    'PORT_STATUS', 'A4ResidentArenaError', 'A4ResidentArenaScopeError',
    'A4ResidentArenaEpochs', 'A4ResidentArenaOwner',
)


def _a49a_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.9a')
    devices = ['cpu']
    if _REQUIRE_CUDA:
        if not torch.cuda.is_available():
            raise AssertionError(
                'CUDA required for A4.9a but unavailable; fallback forbidden'
            )
        devices.append('cuda')
    return devices


def _a49a_fixture(seed=7401):
    world, cells = _fixture_cells(seed=seed)
    capacity = _a48c8_capacity_for_cells(cells)
    observed = (
        int(capacity.max_cells), int(capacity.max_sequences),
        int(capacity.max_symbols), int(capacity.max_sequence_symbols),
        int(capacity.max_proteins_per_cell),
    )
    if observed != (4, 10, 340, 48, 32):
        raise AssertionError(
            'A4.9a heterogeneous fixture C/Q/S/W/P drifted: %r' %
            (observed,)
        )
    return world, cells, capacity


def _a49a_host_binding(world, capacity):
    adapter = a4.FullFidelityA4GenomeAdapter(capacity)
    ragged = adapter.pack_cells(tuple(world.cells))
    state = a4.pack_a4_translation_state(
        tuple(world.cells), ragged, world.config, capacity,
    )
    return a4.bind_a4_translation(ragged, state)


def _a49a_expect_error(fn, kinds=None):
    kinds = kinds or (
        a49a.A4ResidentArenaError, a49a.A4ResidentArenaScopeError,
    )
    try:
        fn()
    except kinds as exc:
        return exc
    raise AssertionError('expected A4.9a fail-closed exception')


def _a49a_assert_snapshot_matches(world, capacity, snapshot, label):
    host = _a49a_host_binding(world, capacity)
    v3.assert_recursive_close(
        host.ragged.state_dict(), snapshot.ragged.state_dict(),
        0.0, 0.0, label + '.ragged',
    )
    v3.assert_recursive_close(
        host.state.state_dict(), snapshot.state.state_dict(),
        0.0, 0.0, label + '.state',
    )
    v3.assert_recursive_close(
        host.cache.state_dict(), snapshot.cache.state_dict(),
        0.0, 0.0, label + '.cache',
    )
    if (a4._ragged_translation_provenance(snapshot.ragged)
            != host.state.source_provenance
            or a4._gene_cache_provenance(snapshot.cache)
            != a4._gene_cache_provenance(host.cache)):
        raise AssertionError(label + ' resident provenance differs')
    return host


def _a49a_signature_sets(signature):
    records = tuple(signature.tensor_records)
    if len(records) != 50:
        raise AssertionError('A4.9a resident signature is not exactly 50 tensors')
    groups = {name: 0 for name in ('ragged', 'state', 'cache')}
    object_ids = set()
    storage = set()
    nonempty_storage_count = 0
    for record in records:
        group, name, object_id, pointer, version, device, dtype, shape = record
        if group not in groups or not name or int(object_id) <= 0:
            raise AssertionError('malformed A4.9a tensor record')
        if (int(pointer) < 0 or int(version) < 0
                or not device or not dtype):
            raise AssertionError('invalid A4.9a pointer/version/device record')
        if any(int(value) < 0 for value in shape):
            raise AssertionError('negative A4.9a tensor dimension')
        zero_sized = any(int(value) == 0 for value in shape)
        if int(pointer) == 0 and not zero_sized:
            raise AssertionError('nonempty A4.9a tensor has null storage')
        groups[group] += 1
        object_ids.add(int(object_id))
        if int(pointer) != 0:
            storage.add((str(device), int(pointer)))
            nonempty_storage_count += 1
    if groups != {'ragged': 11, 'state': 30, 'cache': 9}:
        raise AssertionError('A4.9a tensor group counts differ: %r' % groups)
    if (len(object_ids) != 50
            or len(storage) != nonempty_storage_count):
        raise AssertionError('A4.9a resident tensors alias each other')
    return object_ids, storage


def _a49a_assert_world_unchanged(before, rng_object, rng_state, world, label):
    v3.assert_recursive_close(
        before, world.state_dict(), 0.0, 0.0, label + '.world',
    )
    if (world.rng is not rng_object
            or world.rng.bit_generator.state != rng_state):
        raise AssertionError(label + ' changed CPU world RNG identity/state')


def _a49a_full_rebuild(owner, world):
    expected_id = owner.arena_id
    expected_lifecycle = owner.lifecycle
    expected_epochs = owner.epochs
    old_signature = owner._arena.resident_signature
    candidate = owner.prepare_rebuild(world)
    _, old_storage = _a49a_signature_sets(old_signature)
    _, new_storage = _a49a_signature_sets(candidate._arena.resident_signature)
    if old_storage.intersection(new_storage):
        raise AssertionError('A4.9a rebuild candidate aliases old storage')
    owner.swap_rebuild(
        candidate, world, expected_id, expected_lifecycle, expected_epochs,
    )
    if (owner.lifecycle != a49a.COHERENT
            or owner.arena_id == expected_id
            or candidate._consumed is not True):
        raise AssertionError('A4.9a rebuild did not publish exactly once')
    return candidate


def test_a49a_fresh_world_arena_lease_pointer_stability_and_devices():
    details = []
    for device in _a49a_devices():
        world, cells, capacity = _a49a_fixture(seed=18100)
        before = v3.pickle_clone(world.state_dict())
        rng_object = world.rng
        rng_state = v3.pickle_clone(world.rng.bit_generator.state)
        owner = a49a.A4ResidentArenaOwner.from_cpu(
            world, capacity, device,
        )
        if (owner.lifecycle != a49a.COHERENT
                or owner.epochs != a49a.A4ResidentArenaEpochs(1, 1, 1, 1, 1)):
            raise AssertionError(device + ' fresh A4.9a lifecycle/epochs differ')
        first = owner._validation_snapshot(world)
        _a49a_assert_snapshot_matches(
            world, capacity, first, 'a49a.fresh.' + device,
        )
        objects, storage = _a49a_signature_sets(first.resident_signature)
        if (first.device != device
                or first.lifecycle != a49a.COHERENT
                or int(first.storage_generation) != 1
                or int(first.cache.entry_count) != 6
                or int(first.cache.entry_capacity) != 21
                or int(first.ragged.cell_count) != 4
                or int(first.ragged.sequence_count) != 10
                or int(first.ragged.symbol_count) != 340):
            raise AssertionError(device + ' fresh A4.9a fixture metadata differs')

        for lease_index in range(2):
            lease = owner._open_read_lease(world)
            with lease as active:
                binding = active._binding_once()
                if ((id(binding), id(binding.ragged), id(binding.state),
                     id(binding.cache))
                        != tuple(first.resident_signature.binding_object_ids)):
                    raise AssertionError(device + ' lease binding identity differs')
                _a49a_expect_error(active._binding_once)
                if (set(binding.ragged.data_ptrs().values()).difference(
                        pointer for resident_device, pointer in storage
                        if torch.device(str(resident_device)).type
                        == torch.device(str(device)).type)):
                    raise AssertionError(device + ' lease ragged pointer differs')
            _a49a_expect_error(lease.__enter__)
            if owner.lifecycle != a49a.COHERENT:
                raise AssertionError(device + ' closed lease changed lifecycle')

        second = owner._validation_snapshot(world)
        if (second.resident_signature != first.resident_signature
                or second.arena_id != first.arena_id
                or second.epochs != first.epochs
                or owner.audit_cpu(world) != a49a.COHERENT):
            raise AssertionError(device + ' repeated view changed immutable arena')
        second_objects, second_storage = _a49a_signature_sets(
            second.resident_signature,
        )
        if objects != second_objects or storage != second_storage:
            raise AssertionError(device + ' resident pointer lifetime changed')
        detached_epochs = owner.epochs
        if detached_epochs is owner._epochs:
            raise AssertionError('owner epochs property leaked canonical object')
        _a49a_expect_error(
            lambda: setattr(detached_epochs, 'state_epoch', 99),
            kinds=(Exception,),
        )
        if owner.epochs != a49a.A4ResidentArenaEpochs(1, 1, 1, 1, 1):
            raise AssertionError('detached epoch mutation reached owner')
        _a49a_assert_world_unchanged(
            before, rng_object, rng_state, world, 'a49a.fresh.' + device,
        )
        details.append('%s:50ptr/K21' % device)
    return ('world-wide fresh shadow exact; two one-shot leases retain 50 '
            'ragged/state/cache pointers and CPU/RNG on %s' % '/'.join(details))


def test_a49a_cpu_newer_epochs_dirty_domains_and_separate_rebuild():
    expected = {
        'S': a49a.A4ResidentArenaEpochs(1, 1, 2, 1, 1),
        'R': a49a.A4ResidentArenaEpochs(1, 2, 2, 2, 1),
        'C': a49a.A4ResidentArenaEpochs(1, 1, 1, 2, 1),
        'M': a49a.A4ResidentArenaEpochs(2, 2, 2, 2, 1),
    }
    details = []
    for index, domain in enumerate(('S', 'R', 'C', 'M')):
        world, cells, capacity = _a49a_fixture(seed=18200 + index)
        rng_object = world.rng
        rng_state = v3.pickle_clone(world.rng.bit_generator.state)
        owner = a49a.A4ResidentArenaOwner.from_cpu(
            world, capacity, 'cpu',
        )
        original = owner._validation_snapshot(world)
        original_records = tuple(original.resident_signature.tensor_records)

        if domain == 'S':
            cells[1].pools[0] = np.nextafter(
                float(cells[1].pools[0]), np.inf,
            )
        elif domain == 'R':
            cells[1].genomes[0][0] = np.uint8(
                (int(cells[1].genomes[0][0]) + 1) % 8
            )
            cells[1]._refresh_gene_cache()
        elif domain == 'C':
            cells[1].gene_specs = {}
        else:
            world.cells[0], world.cells[1] = world.cells[1], world.cells[0]

        lifecycle = owner.audit_cpu(world)
        expected_lifecycle = (
            a49a.INVALID if domain == 'M' else a49a.CPU_NEWER
        )
        if lifecycle != expected_lifecycle or owner.epochs != expected[domain]:
            raise AssertionError(
                '%s drift lifecycle/epochs differ: %s %r' % (
                    domain, lifecycle, owner.epochs,
                )
            )
        once = owner.epochs
        if owner.audit_cpu(world) != expected_lifecycle or owner.epochs != once:
            raise AssertionError(domain + ' repeated audit was not idempotent')
        if tuple(owner._arena.resident_signature.tensor_records) != original_records:
            raise AssertionError(domain + ' CPU drift changed old storage')
        _a49a_expect_error(lambda: owner._validation_snapshot(world))
        _a49a_expect_error(lambda: owner._open_read_lease(world))

        if domain == 'C':
            # A stale caller cache cannot be rebuilt as resident authority.
            before_failed = (
                owner.arena_id, owner.lifecycle, owner.epochs,
                tuple(owner._arena.resident_signature.tensor_records),
            )
            _a49a_expect_error(lambda: owner.prepare_rebuild(world))
            after_failed = (
                owner.arena_id, owner.lifecycle, owner.epochs,
                tuple(owner._arena.resident_signature.tensor_records),
            )
            if after_failed != before_failed:
                raise AssertionError('failed stale-cache rebuild changed owner')
            cells[1]._refresh_gene_cache()
            if owner.audit_cpu(world) != a49a.CPU_NEWER:
                raise AssertionError('refreshed CPU cache unexpectedly recohered')
            if owner.epochs != a49a.A4ResidentArenaEpochs(1, 1, 1, 3, 1):
                raise AssertionError('CPU cache refresh epoch did not advance once')

        candidate = _a49a_full_rebuild(owner, world)
        rebuilt = owner._validation_snapshot(world)
        _a49a_assert_snapshot_matches(
            world, capacity, rebuilt, 'a49a.rebuild.' + domain,
        )
        if (int(rebuilt.storage_generation) != 2
                or rebuilt.epochs.cache_source_ragged_epoch
                != rebuilt.epochs.ragged_epoch
                or tuple(rebuilt.resident_signature.tensor_records)
                == original_records):
            raise AssertionError(domain + ' rebuild did not use a fresh generation')
        _a49a_expect_error(
            lambda: owner.swap_rebuild(
                candidate, world, owner.arena_id, owner.lifecycle,
                owner.epochs,
            )
        )
        if (world.rng is not rng_object
                or world.rng.bit_generator.state != rng_state):
            raise AssertionError(domain + ' audit/rebuild changed CPU RNG')
        details.append('%s:%s' % (domain, '/'.join(map(str, (
            rebuilt.epochs.membership_epoch,
            rebuilt.epochs.ragged_epoch,
            rebuilt.epochs.state_epoch,
            rebuilt.epochs.cache_epoch,
        )))))

    # The actual A3 unpack path rebinds lesions, pools and protein mappings even
    # when their values are equal.  The identity-aware audit must advance R/S/C
    # once, and only once.
    world, cells, capacity = _a49a_fixture(seed=18220)
    owner = a49a.A4ResidentArenaOwner.from_cpu(world, capacity, 'cpu')
    packed = a4.a3.pack_a3_cell(cells[2], model_config=world.config)
    a4.a3.unpack_a3_cell(packed, cells[2])
    if (owner.audit_cpu(world) != a49a.CPU_NEWER
            or owner.epochs != a49a.A4ResidentArenaEpochs(1, 2, 2, 2, 1)):
        raise AssertionError('real A3 unpack did not stale R/S/C together')
    unpack_epochs = owner.epochs
    if owner.audit_cpu(world) != a49a.CPU_NEWER or owner.epochs != unpack_epochs:
        raise AssertionError('real A3 unpack drift advanced more than once')

    # The c8-only last-symbol field is outside M/R/S/C and must not dirty 9a.
    world, cells, capacity = _a49a_fixture(seed=18221)
    owner = a49a.A4ResidentArenaOwner.from_cpu(world, capacity, 'cpu')
    telemetry_signature = owner._arena.resident_signature
    cells[0].last_replication_symbols = 987654321
    if (owner.audit_cpu(world) != a49a.COHERENT
            or owner.epochs != a49a.A4ResidentArenaEpochs(1, 1, 1, 1, 1)
            or owner._arena.resident_signature != telemetry_signature):
        raise AssertionError('out-of-shadow c8 telemetry dirtied A4.9a')
    return ('S/R/C/M one-way epochs + separate nonalias rebuild %s; real '
            'A3 unpack stales R/S/C once; c8 last-symbol remains coherent' %
            '/'.join(details))


def test_a49a_capacity_creation_seals_leases_candidates_and_scope_fail_closed():
    world, cells, exact = _a49a_fixture(seed=18300)
    before = v3.pickle_clone(world.state_dict())
    rng_object = world.rng
    rng_state = v3.pickle_clone(world.rng.bit_generator.state)
    values = {
        'max_cells': int(exact.max_cells),
        'max_sequences': int(exact.max_sequences),
        'max_symbols': int(exact.max_symbols),
        'max_sequence_symbols': int(exact.max_sequence_symbols),
        'max_proteins_per_cell': int(exact.max_proteins_per_cell),
    }
    shorts = {
        'C': ('max_cells', 3),
        'Q': ('max_sequences', 9),
        'S': ('max_symbols', 339),
        'W': ('max_sequence_symbols', 47),
        'P': ('max_proteins_per_cell', 31),
    }
    upload_calls = []
    original_to_torch = a4.A4RaggedGenomeBatch.to_torch

    def upload_bomb(*args, **kwargs):
        upload_calls.append(True)
        raise AssertionError('one-short capacity reached resident upload')

    a4.A4RaggedGenomeBatch.to_torch = upload_bomb
    try:
        for axis, (field, short_value) in shorts.items():
            candidate_values = dict(values)
            candidate_values[field] = short_value
            config = a4.GPU068A4Config(**candidate_values)
            _a49a_expect_error(
                lambda config=config: a49a.A4ResidentArenaOwner.from_cpu(
                    world, config, 'cpu',
                )
            )
    finally:
        a4.A4RaggedGenomeBatch.to_torch = original_to_torch
    if upload_calls:
        raise AssertionError('capacity preflight allocated resident storage')
    _a49a_assert_world_unchanged(
        before, rng_object, rng_state, world, 'a49a.capacity',
    )

    def tamper_arena_id(owner):
        owner._arena.arena_id += ':tampered'

    def tamper_source(owner):
        object.__setattr__(owner._arena.source, 'ragged_digest', '0' * 64)

    def tamper_expected_digest(owner):
        owner._arena.expected_cache_digest = '1' * 64

    def tamper_config(owner):
        owner._arena.config_state = tuple(reversed(owner._arena.config_state))

    def tamper_storage_generation(owner):
        owner._arena.storage_generation += 1

    def tamper_owner_epochs(owner):
        owner._epochs = a49a.A4ResidentArenaEpochs(9, 9, 9, 9, 9)

    def tamper_arena_epochs(owner):
        owner._arena.epochs = a49a.A4ResidentArenaEpochs(1, 2, 1, 1, 1)

    def tamper_lifecycle(owner):
        owner._arena.lifecycle = a49a.CPU_NEWER

    def tamper_last_observed(owner):
        object.__setattr__(owner._last_observed, 'state_digest', '2' * 64)

    metadata_tampers = (
        ('arena_id', tamper_arena_id),
        ('source', tamper_source),
        ('expected_digest', tamper_expected_digest),
        ('config', tamper_config),
        ('storage_generation', tamper_storage_generation),
        ('owner_epochs', tamper_owner_epochs),
        ('arena_epochs', tamper_arena_epochs),
        ('lifecycle', tamper_lifecycle),
        ('last_observed', tamper_last_observed),
    )
    tamper_details = []
    for index, (label, mutate) in enumerate(metadata_tampers):
        test_world, _, capacity = _a49a_fixture(seed=18320 + index)
        test_before = v3.pickle_clone(test_world.state_dict())
        test_rng = test_world.rng
        test_rng_state = v3.pickle_clone(test_rng.bit_generator.state)
        owner = a49a.A4ResidentArenaOwner.from_cpu(
            test_world, capacity, 'cpu',
        )
        mutate(owner)
        _a49a_expect_error(lambda: owner.audit_cpu(test_world))
        if owner.lifecycle != a49a.INVALID:
            raise AssertionError(label + ' metadata tamper did not invalidate')
        _a49a_assert_world_unchanged(
            test_before, test_rng, test_rng_state, test_world,
            'a49a.tamper.' + label,
        )
        tamper_details.append(label)

    # Pointer/version checks and the explicit content seal independently catch
    # ordinary writes, storage replacement, and Tensor.data version bypass.
    for index, mode in enumerate(('ordinary', 'pointer', 'data')):
        test_world, _, capacity = _a49a_fixture(seed=18350 + index)
        owner = a49a.A4ResidentArenaOwner.from_cpu(
            test_world, capacity, 'cpu',
        )
        tensor = owner._arena.binding.ragged.symbols
        if mode == 'ordinary':
            tensor[0] = (int(tensor[0]) + 1) % 8
        elif mode == 'pointer':
            owner._arena.binding.ragged.symbols = tensor.clone()
        else:
            tensor.data[0] = (int(tensor.data[0]) + 1) % 8
        _a49a_expect_error(lambda: owner.audit_cpu(test_world))
        if owner.lifecycle != a49a.INVALID:
            raise AssertionError(mode + ' tensor tamper did not invalidate')

    # A coordinated Tensor.data + expected digest + source attestation + inner
    # seal rewrite must still fail against the owner's detached outer snapshot.
    # Exercise the derived-cache relation explicitly.
    coforge_world, _, coforge_capacity = _a49a_fixture(seed=18353)
    coforge_owner = a49a.A4ResidentArenaOwner.from_cpu(
        coforge_world, coforge_capacity, 'cpu',
    )
    coforge_tensor = coforge_owner._arena.binding.cache.copy_numbers
    coforge_tensor.data[0] = int(coforge_tensor.data[0]) + 1
    coforge_digest = a4._gene_cache_provenance(
        coforge_owner._arena.binding.cache.to_numpy(),
    )
    if coforge_digest == coforge_owner._arena.source.derived_cache_digest:
        raise AssertionError('active cache coforge did not change its digest')
    coforge_owner._arena.expected_cache_digest = coforge_digest
    coforge_seal = coforge_owner._arena_seal
    object.__setattr__(
        coforge_owner._arena.source, 'derived_cache_digest', coforge_digest,
    )
    object.__setattr__(
        coforge_seal, 'expected_digests',
        (coforge_seal.expected_digests[0],
         coforge_seal.expected_digests[1], coforge_digest),
    )
    object.__setattr__(
        coforge_seal, 'source_values',
        a49a._source_identity_values(coforge_owner._arena.source),
    )
    if (a49a._require_arena_creation_seal(
            coforge_owner._arena, coforge_seal)
            is not coforge_owner._arena):
        raise AssertionError('active cache inner coforge was incomplete')
    _a49a_expect_error(lambda: coforge_owner.audit_cpu(coforge_world))
    if coforge_owner.lifecycle != a49a.INVALID:
        raise AssertionError('active cache digest/seal coforge stayed valid')

    # Equal-value current-seal copies are non-authoritative records and remain
    # legal.  They must not let a stale former guard seal be coforged with the
    # live arena epoch object behind the current seal's association.
    epoch_world, _, epoch_capacity = _a49a_fixture(seed=18354)
    epoch_before = v3.pickle_clone(epoch_world.state_dict())
    epoch_rng = epoch_world.rng
    epoch_rng_state = v3.pickle_clone(epoch_rng.bit_generator.state)
    epoch_owner = a49a.A4ResidentArenaOwner.from_cpu(
        epoch_world, epoch_capacity, 'cpu',
    )
    stale_guard_seal = epoch_owner._arena_seal
    epoch_owner._arena_seal = copy.copy(stale_guard_seal)
    if epoch_owner.lifecycle != a49a.COHERENT:
        raise AssertionError('equal current arena-seal copy changed authority')
    forged_epochs = a49a.A4ResidentArenaEpochs(9, 9, 9, 9, 9)
    epoch_owner._arena.epochs = forged_epochs
    object.__setattr__(stale_guard_seal, 'epochs_ref', forged_epochs)
    object.__setattr__(
        stale_guard_seal, 'epochs_values', (9, 9, 9, 9, 9),
    )
    if (a49a._require_arena_creation_seal(
            epoch_owner._arena, stale_guard_seal)
            is not epoch_owner._arena):
        raise AssertionError('stale guard inner epoch coforge was incomplete')
    if (epoch_owner.lifecycle != a49a.INVALID
            or epoch_owner.epochs
            != a49a.A4ResidentArenaEpochs(2, 2, 2, 2, 1)):
        raise AssertionError('stale-seal epoch coforge did not fail closed')
    invalid_epochs = epoch_owner.epochs
    if (epoch_owner.audit_cpu(epoch_world) != a49a.INVALID
            or epoch_owner.epochs != invalid_epochs):
        raise AssertionError('epoch coforge invalidation advanced twice')
    _a49a_assert_world_unchanged(
        epoch_before, epoch_rng, epoch_rng_state, epoch_world,
        'a49a.active.epoch-coforge',
    )

    # Issued lease A becomes stale when B is issued; neither serial nor token
    # may be replayed A -> B -> A, and a binding is consumable exactly once.
    world, _, capacity = _a49a_fixture(seed=18360)
    owner = a49a.A4ResidentArenaOwner.from_cpu(world, capacity, 'cpu')
    lease_a = owner._open_read_lease(world)
    lease_b = owner._open_read_lease(world)
    _a49a_expect_error(lease_a.__enter__)
    with lease_b as active:
        active._binding_once()
        _a49a_expect_error(active._binding_once)
        active._consumed = False
        _a49a_expect_error(active._binding_once)
    _a49a_expect_error(lease_a.__enter__)
    _a49a_expect_error(lease_b.__enter__)

    other_world, _, other_capacity = _a49a_fixture(seed=18361)
    other = a49a.A4ResidentArenaOwner.from_cpu(
        other_world, other_capacity, 'cpu',
    )
    cross = owner._open_read_lease(world)
    _a49a_expect_error(lambda: other._activate_lease(cross))
    with cross as active:
        active._binding_once()

    # A .data mutation inside a lease must be found at close and must always
    # clear the active token, even while the generation becomes INVALID.
    lease_world, _, lease_capacity = _a49a_fixture(seed=18362)
    lease_owner = a49a.A4ResidentArenaOwner.from_cpu(
        lease_world, lease_capacity, 'cpu',
    )
    in_lease = lease_owner._open_read_lease(lease_world)

    def mutate_inside_lease():
        with in_lease as active:
            binding = active._binding_once()
            tensor = binding.ragged.symbols
            tensor.data[0] = (int(tensor.data[0]) + 1) % 8

    _a49a_expect_error(mutate_inside_lease)
    if (lease_owner.lifecycle != a49a.INVALID
            or lease_owner._active_lease_token is not None
            or lease_owner._issued_lease_token is not None
            or int(lease_owner._arena.active_leases) != 0):
        raise AssertionError('failed lease close retained active authority')

    # An issued lease is also stale across CPU drift and a complete A->B swap.
    stale_world, stale_cells, stale_capacity = _a49a_fixture(seed=18363)
    stale_owner = a49a.A4ResidentArenaOwner.from_cpu(
        stale_world, stale_capacity, 'cpu',
    )
    stale_lease = stale_owner._open_read_lease(stale_world)
    stale_cells[0].pools[0] = np.nextafter(
        float(stale_cells[0].pools[0]), np.inf,
    )
    if stale_owner.audit_cpu(stale_world) != a49a.CPU_NEWER:
        raise AssertionError('lease drift did not stale owner')
    _a49a_expect_error(stale_lease.__enter__)
    _a49a_full_rebuild(stale_owner, stale_world)
    _a49a_expect_error(stale_lease.__enter__)

    # Candidate creation seals reject an equal-value epoch-object rebind, an
    # old-arena alias, and a CPU source changed after preparation.
    candidate_modes = ('epochs_equal_rebind', 'old_alias', 'cpu_freshness')
    for index, mode in enumerate(candidate_modes):
        candidate_world, candidate_cells, candidate_capacity = (
            _a49a_fixture(seed=18370 + index)
        )
        candidate_owner = a49a.A4ResidentArenaOwner.from_cpu(
            candidate_world, candidate_capacity, 'cpu',
        )
        candidate_cells[0].pools[0] = np.nextafter(
            float(candidate_cells[0].pools[0]), np.inf,
        )
        candidate_owner.audit_cpu(candidate_world)
        expected_id = candidate_owner.arena_id
        expected_lifecycle = candidate_owner.lifecycle
        expected_epochs = candidate_owner.epochs
        old_signature = candidate_owner._arena.resident_signature
        candidate = candidate_owner.prepare_rebuild(candidate_world)
        if mode == 'epochs_equal_rebind':
            candidate._expected_epochs = copy.deepcopy(
                candidate._expected_epochs,
            )
        elif mode == 'old_alias':
            candidate._arena = candidate_owner._arena
        else:
            candidate_cells[0].pools[1] = np.nextafter(
                float(candidate_cells[0].pools[1]), np.inf,
            )
        _a49a_expect_error(
            lambda: candidate_owner.swap_rebuild(
                candidate, candidate_world, expected_id,
                expected_lifecycle, expected_epochs,
            )
        )
        if (candidate_owner.arena_id != expected_id
                or candidate_owner._arena.resident_signature != old_signature
                or candidate_owner.lifecycle == a49a.COHERENT):
            raise AssertionError(mode + ' failed candidate changed old owner')

    # The same content/expected/source/inner-seal coforge on a private rebuild
    # must fail against its outer candidate snapshot before CAS publication.
    coforge_world, coforge_cells, coforge_capacity = _a49a_fixture(seed=18373)
    coforge_owner = a49a.A4ResidentArenaOwner.from_cpu(
        coforge_world, coforge_capacity, 'cpu',
    )
    coforge_cells[0].pools[0] = np.nextafter(
        float(coforge_cells[0].pools[0]), np.inf,
    )
    coforge_owner.audit_cpu(coforge_world)
    coforge_id = coforge_owner.arena_id
    coforge_lifecycle = coforge_owner.lifecycle
    coforge_epochs = coforge_owner.epochs
    coforge_signature = coforge_owner._arena.resident_signature
    coforge_candidate = coforge_owner.prepare_rebuild(coforge_world)
    coforge_before = v3.pickle_clone(coforge_world.state_dict())
    coforge_rng = coforge_world.rng
    coforge_rng_state = v3.pickle_clone(coforge_rng.bit_generator.state)
    coforge_tensor = coforge_candidate._arena.binding.ragged.symbols
    coforge_tensor.data[0] = (int(coforge_tensor.data[0]) + 1) % 8
    coforge_digest = a4._ragged_translation_provenance(
        coforge_candidate._arena.binding.ragged.to_numpy(),
    )
    if coforge_digest == coforge_candidate._arena.source.ragged_digest:
        raise AssertionError('candidate ragged coforge did not change its digest')
    coforge_candidate._arena.expected_ragged_digest = coforge_digest
    coforge_seal = coforge_candidate._seal.arena_seal
    object.__setattr__(
        coforge_candidate._arena.source, 'ragged_digest', coforge_digest,
    )
    object.__setattr__(
        coforge_seal, 'expected_digests',
        (coforge_digest, coforge_seal.expected_digests[1],
         coforge_seal.expected_digests[2]),
    )
    object.__setattr__(
        coforge_seal, 'source_values',
        a49a._source_identity_values(coforge_candidate._arena.source),
    )
    if (a49a._require_arena_creation_seal(
            coforge_candidate._arena, coforge_seal)
            is not coforge_candidate._arena):
        raise AssertionError('candidate ragged inner coforge was incomplete')
    _a49a_expect_error(
        lambda: coforge_owner.swap_rebuild(
            coforge_candidate, coforge_world, coforge_id,
            coforge_lifecycle, coforge_epochs,
        )
    )
    if (coforge_owner.arena_id != coforge_id
            or coforge_owner._arena.resident_signature != coforge_signature
            or coforge_owner.lifecycle != coforge_lifecycle
            or coforge_candidate._consumed):
        raise AssertionError('candidate digest/seal coforge changed old owner')
    _a49a_assert_world_unchanged(
        coforge_before, coforge_rng, coforge_rng_state, coforge_world,
        'a49a.candidate.coforge',
    )

    # Replace every resident object in a prepared candidate with fresh cloned
    # ragged/state storage, its freshly derived cache, binding and signature.
    # Even a completely self-consistent inner arena/seal rewrite must not pass
    # the candidate's detached outer creation snapshot.
    binding_world, binding_cells, binding_capacity = _a49a_fixture(seed=18374)
    binding_owner = a49a.A4ResidentArenaOwner.from_cpu(
        binding_world, binding_capacity, 'cpu',
    )
    binding_cells[0].pools[0] = np.nextafter(
        float(binding_cells[0].pools[0]), np.inf,
    )
    binding_owner.audit_cpu(binding_world)
    binding_id = binding_owner.arena_id
    binding_lifecycle = binding_owner.lifecycle
    binding_epochs = binding_owner.epochs
    binding_signature = binding_owner._arena.resident_signature
    _, owner_storage = _a49a_signature_sets(binding_signature)
    binding_candidate = binding_owner.prepare_rebuild(binding_world)
    _, candidate_storage = _a49a_signature_sets(
        binding_candidate._arena.resident_signature,
    )
    binding_before = v3.pickle_clone(binding_world.state_dict())
    binding_rng = binding_world.rng
    binding_rng_state = v3.pickle_clone(binding_rng.bit_generator.state)

    fresh_ragged = binding_candidate._arena.binding.ragged.clone()
    fresh_state = binding_candidate._arena.binding.state.clone()
    fresh_binding = a4.bind_a4_translation(fresh_ragged, fresh_state)
    fresh_signature = a49a._resident_signature(fresh_binding)
    _, fresh_storage = _a49a_signature_sets(fresh_signature)
    if (fresh_storage.intersection(owner_storage)
            or fresh_storage.intersection(candidate_storage)):
        raise AssertionError('candidate fresh binding aliases prior storage')
    fresh_source = copy.copy(binding_candidate._arena.source)
    fresh_digests = (
        a4._ragged_translation_provenance(fresh_ragged.to_numpy()),
        a4._translation_state_provenance(fresh_state.to_numpy()),
        a4._gene_cache_provenance(fresh_binding.cache.to_numpy()),
    )
    binding_candidate._arena.binding = fresh_binding
    binding_candidate._arena.resident_signature = fresh_signature
    binding_candidate._arena.source = fresh_source
    (binding_candidate._arena.expected_ragged_digest,
     binding_candidate._arena.expected_state_digest,
     binding_candidate._arena.expected_cache_digest) = fresh_digests
    binding_seal = binding_candidate._seal.arena_seal
    object.__setattr__(binding_seal, 'binding_ref', fresh_binding)
    object.__setattr__(binding_seal, 'resident_signature_ref', fresh_signature)
    object.__setattr__(binding_seal, 'source_ref', fresh_source)
    object.__setattr__(
        binding_seal, 'source_values',
        a49a._source_identity_values(fresh_source),
    )
    object.__setattr__(
        binding_seal, 'resident_signature_values',
        (tuple(fresh_signature.binding_object_ids),
         tuple(fresh_signature.tensor_records)),
    )
    object.__setattr__(binding_seal, 'expected_digests', fresh_digests)
    if (a49a._require_arena_creation_seal(
            binding_candidate._arena, binding_seal)
            is not binding_candidate._arena):
        raise AssertionError('candidate fresh-binding inner coforge incomplete')
    _a49a_expect_error(
        lambda: binding_owner.swap_rebuild(
            binding_candidate, binding_world, binding_id,
            binding_lifecycle, binding_epochs,
        )
    )
    if (binding_owner.arena_id != binding_id
            or binding_owner.lifecycle != binding_lifecycle
            or binding_owner._arena.resident_signature != binding_signature
            or binding_candidate._consumed):
        raise AssertionError('candidate binding coforge changed old owner')
    _, after_owner_storage = _a49a_signature_sets(
        binding_owner._arena.resident_signature,
    )
    if after_owner_storage != owner_storage:
        raise AssertionError('candidate binding coforge changed old pointers')
    _a49a_assert_world_unchanged(
        binding_before, binding_rng, binding_rng_state, binding_world,
        'a49a.candidate.binding-coforge',
    )

    # A4.9a exposes no successful device-dirty/D2H/publish/serialization path.
    forbidden_public = ('write', 'dirty', 'd2h', 'publish', 'sync', 'state_dict',
                        'from_state', 'clone', 'to_cpu')
    public_names = tuple(a49a.__all__)
    if any(any(token in name.lower() for token in forbidden_public)
           for name in public_names):
        raise AssertionError('A4.9a public API leaks resident authority')
    scope_world, _, scope_capacity = _a49a_fixture(seed=18380)
    scope_owner = a49a.A4ResidentArenaOwner.from_cpu(
        scope_world, scope_capacity, 'cpu',
    )
    for operation in (
            lambda: pickle.dumps(scope_owner),
            lambda: pickle.dumps(scope_owner._arena),
            lambda: copy.copy(scope_owner),
            lambda: copy.deepcopy(scope_owner)):
        _a49a_expect_error(
            operation, kinds=(a49a.A4ResidentArenaScopeError,),
        )
    if (a49a.PORT_STATUS.get('resident_device_writes')
            != 'not-implemented-scope-error'
            or a49a.PORT_STATUS.get('resident_d2h_publish')
            != 'not-implemented-scope-error'):
        raise AssertionError('A4.9a unsupported resident authority drifted')
    return ('exact C/Q/S/W/P and each one-short pre-upload; %d metadata/'
            'source/digest + 3 tensor tamper + 4 outer-snapshot coforge; lease '
            'serial/token/local-rearm/one-shot/in-close data; candidate '
            'equal-epoch/alias/freshness; no successful public D2H publish/'
            'CPU overwrite path' %
            len(tamper_details))


def test_a49a_fresh_reconstruction_c8_successor_frozen86_and_authority():
    case = _a48c8_case(
        'disabled', seed=18400, mutation=True, prime_uint32=True,
    )
    source = _a48c8_hybrid_from_state(
        case['state'], case['capacity'], 'cpu',
    )
    twin = source.clone()
    with tempfile.TemporaryDirectory() as directory:
        path = os.path.join(directory, 'a49a_cpu_authority.pkl')
        source.save(path)
        restored = a48c8.Hybrid066WorldA4ReplicationEarlyNoop.load(path)

    wrappers = (source, twin, restored)
    owners = [
        a49a.A4ResidentArenaOwner.from_cpu(
            wrapper.world, case['capacity'], 'cpu',
        )
        for wrapper in wrappers
    ]
    snapshots = [
        owner._validation_snapshot(wrapper.world)
        for owner, wrapper in zip(owners, wrappers)
    ]
    arena_ids = {snapshot.arena_id for snapshot in snapshots}
    if len(arena_ids) != len(snapshots):
        raise AssertionError('load/clone reused an A4.9a arena ID')
    all_storage = []
    all_objects = []
    for index, (wrapper, owner, snapshot) in enumerate(
            zip(wrappers, owners, snapshots)):
        _a49a_assert_snapshot_matches(
            wrapper.world, case['capacity'], snapshot,
            'a49a.persisted.%d' % index,
        )
        objects, storage = _a49a_signature_sets(
            snapshot.resident_signature,
        )
        all_objects.append(objects)
        all_storage.append(storage)
        if (owner.epochs != a49a.A4ResidentArenaEpochs(1, 1, 1, 1, 1)
                or int(snapshot.storage_generation) != 1):
            raise AssertionError('fresh reconstructed arena did not reset namespace')
    for left in range(len(snapshots)):
        for right in range(left + 1, len(snapshots)):
            if (all_storage[left].intersection(all_storage[right])
                    or all_objects[left].intersection(all_objects[right])):
                raise AssertionError('load/clone A4.9a arenas alias resident tensors')
            v3.assert_recursive_close(
                snapshots[left].ragged.state_dict(),
                snapshots[right].ragged.state_dict(),
                0.0, 0.0, 'a49a.persisted.ragged.%d.%d' % (left, right),
            )
            v3.assert_recursive_close(
                snapshots[left].state.state_dict(),
                snapshots[right].state.state_dict(),
                0.0, 0.0, 'a49a.persisted.state.%d.%d' % (left, right),
            )
            v3.assert_recursive_close(
                snapshots[left].cache.state_dict(),
                snapshots[right].cache.state_dict(),
                0.0, 0.0, 'a49a.persisted.cache.%d.%d' % (left, right),
            )

    # The shadow is external to the scheduler.  A complete successor step must
    # retain the exact c8 event/RNG authority; its ordinary CPU object recast
    # invalidates the old membership-bound arena and requires a fresh rebuild.
    expected = source
    shadow = twin
    shadow_owner = owners[1]
    expected_receipt = expected.step(case['dt'])
    shadow_receipt = shadow.step(case['dt'])
    v3._assert_world_pair(
        expected.world, shadow, state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL, label='a49a.c8.successor',
    )
    v3.assert_recursive_close(
        expected_receipt, shadow_receipt, 0.0, 0.0,
        'a49a.c8.successor.receipt',
    )
    names = [entry['event'] for entry in shadow_receipt['cells'][0]['events']]
    ordinals = [entry['ordinal'] for entry in shadow_receipt['cells'][0]['events']]
    if (tuple(names) != tuple(a3s.CELL_EVENT_ORDER)
            or len(ordinals) != len(set(ordinals))
            or names.count('translation_cpu') != 1
            or names.count('replication_cpu') != 1
            or names.count('genome_hydrolysis_cpu_rng') != 1):
        raise AssertionError('A4.9a shadow changed c8 successor event order')
    if shadow_owner.audit_cpu(shadow.world) != a49a.INVALID:
        raise AssertionError('full CPU successor did not invalidate old membership')
    step_before = v3.pickle_clone(shadow.world.state_dict())
    step_rng = shadow.world.rng
    step_rng_state = v3.pickle_clone(step_rng.bit_generator.state)
    _a49a_full_rebuild(shadow_owner, shadow.world)
    _a49a_assert_world_unchanged(
        step_before, step_rng, step_rng_state, shadow.world,
        'a49a.c8.post_step_rebuild',
    )

    # Pending scheduler serialization remains a CPU-world guard; the external
    # arena neither weakens it nor becomes part of the durable state.
    pending = restored
    pending_owner = owners[2]
    pending.scheduler.begin_step(pending.world, pending.world.cells)
    _assert_raises(a3s.A3SchedulerProtocolError, pending.clone)
    if pending_owner.audit_cpu(pending.world) != a49a.COHERENT:
        raise AssertionError('pending scheduler alone dirtied external shadow')
    pending.scheduler.abort_step(RuntimeError('expected A4.9a pending guard'))

    # Runtime owner/candidate/lease state is never durable biology.
    lease = pending_owner._open_read_lease(pending.world)
    _a49a_expect_error(
        lambda: pickle.dumps(lease),
        kinds=(a49a.A4ResidentArenaScopeError,),
    )
    with lease as active:
        active._binding_once()
    pending.world.cells[0].pools[0] = np.nextafter(
        float(pending.world.cells[0].pools[0]), np.inf,
    )
    pending_owner.audit_cpu(pending.world)
    candidate = pending_owner.prepare_rebuild(pending.world)
    _a49a_expect_error(
        lambda: pickle.dumps(candidate),
        kinds=(a49a.A4ResidentArenaScopeError,),
    )

    prior_names = '\n'.join(fn.__name__ for fn in VALIDATION_TESTS)
    new_names = tuple(fn.__name__ for fn in A49A_TESTS)
    if (tuple(a49a.__all__) != _A49A_PUBLIC_API
            or a49a.FULL_GPU_WORLD_STEP is not False
            or a49a.PORT_STATUS.get('full_gpu_world_step') is not False
            or a48c8.FULL_GPU_WORLD_STEP is not False
            or len(TESTS) != 82
            or len(A48C8_TESTS) != 4
            or len(VALIDATION_TESTS) != 86
            or len(A49A_TESTS) != 4
            or len(A49A_VALIDATION_TESTS) != 90
            or new_names != (
                'test_a49a_fresh_world_arena_lease_pointer_stability_and_devices',
                'test_a49a_cpu_newer_epochs_dirty_domains_and_separate_rebuild',
                'test_a49a_capacity_creation_seals_leases_candidates_and_scope_fail_closed',
                'test_a49a_fresh_reconstruction_c8_successor_frozen86_and_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A49A_PRIOR_86_NAMES_SHA256
            or _a48c7_prior_test_ast_sha256(VALIDATION_TESTS)
            != _A49A_PRIOR_86_AST_SHA256):
        raise AssertionError(
            'A4.9a API/scope/frozen86/total90 authority differs'
        )
    for relative, expected_hash in _A49A_FROZEN_CORE_SHA256.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    core_path = os.path.join(
        ROOT, 'src', '0_6_8',
        'SOMA_CELL_0_6_8_gpu_a4_resident_arena.py',
    )
    if _sha256(core_path) != _A49A_CORE_SHA256:
        raise AssertionError('A4.9a core differs from preregistered authority')
    return ('CPU save/load/clone rebuild three fresh nonalias arenas; c8 '
            'successor event/RNG exact then membership-invalid/full rebuild; '
            'pending and arena serialization guards; frozen86 AST/names + '
            'A3/pure/c1-c8/core hashes + total90/full_gpu=false')


_A49B_CORE_SHA256 = (
    'be8cd45b0dc3edd32987688bcb2641121c4fdf6498a9624a8ae8b208b3b3eb31'
)

_A49B_PREREG_SHA256 = (
    'df9927d1db7b15015d57d0402f0f7941e9d0eddda7075bbddd7ddbcec901d9c7'
)

_A49B_PRIOR_90_NAMES_SHA256 = (
    '8e795a1e9872c66d57845bd27fdabeb00d97f3c11d0e359d76fc2b048ffc6123'
)

_A49B_PRIOR_90_AST_SHA256 = (
    'a0de8a1e916ceeb3e7d07050334319ac968d3d44f52f70b0651bcf4faa90c4ca'
)

_A49B_PUBLIC_API = (
    'BUILD', 'BUILD_ID', 'BUILD_LONG', 'SCHEMA_VERSION',
    'SELECTED_PLAN_SCHEMA_VERSION', 'SAVE_VERSION',
    'FULL_GPU_WORLD_STEP', 'TRANSLATION_ORACLE_ATOL',
    'COHERENT', 'CPU_NEWER', 'INVALID', 'PORT_STATUS',
    'A4ResidentTranslationCommitError', 'A4SelectedTranslationPlan',
    'paid_translation_selected_numpy', 'paid_translation_selected_torch',
    'paid_translation_selected', 'A4ResidentTranslationEventScheduler',
    'Hybrid066WorldA4ResidentTranslation',
)


def _a49b_devices():
    if torch is None:
        raise AssertionError('PyTorch is required for A4.9b')
    if _REQUIRE_CUDA:
        if not torch.cuda.is_available():
            raise AssertionError(
                'CUDA required for A4.9b but unavailable; fallback forbidden'
            )
        return ('cuda',)
    return ('cpu',)


def _a49b_hybrid_from_state(state, capacity, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a49b.Hybrid066WorldA4ResidentTranslation(
        world, backend=backend, a4_config=capacity, a4_device=device,
    )


def _a49b_begin_translation_direct(scheduler, world, cell, dt):
    scheduler._bind_resident_world(world)
    return _a48b_begin_translation_direct(
        scheduler, world, cell, dt,
    )


def _a49b_plan_pointer_set(plan):
    pointers = plan.data_ptrs()
    if len(pointers) != 31 or len(set(pointers.values())) != 31:
        raise AssertionError('A4.9b selected plan is not 31 fresh tensors')
    if (int(plan.target_mask._version) != 0
            or any(int(getattr(plan.state_after, name)._version) != 0
                   for name in a4._TRANSLATION_ARRAY_FIELDS)):
        raise AssertionError('A4.9b selected plan tensors are not version zero')
    return {
        (str(getattr(plan.state_after, 'pools').device), int(pointer))
        for pointer in pointers.values() if int(pointer) != 0
    }


def _a49b_world_cell_identities(world):
    return tuple(
        (id(cell), id(cell.pools), id(cell.proteins),
         id(cell.damaged_proteins), id(cell.gene_specs),
         tuple((int(key), id(value))
               for key, value in cell.gene_specs.items()))
        for cell in world.cells
    )


def test_a49b_selected_plan_formal066_cpu_cuda_isolation():
    source, _, capacity, source_dt = _paid_translation_fixture(seed=18500)
    source_state = v3.pickle_clone(source.state_dict())
    cases = []
    for label, index in (
            ('rich_first', 0), ('atp_reserve_middle', 1),
            ('mineral_exhaustion_middle', 2), ('no_translator_last', 3)):
        world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(source_state),
        )
        cases.append((label, world, index, source_dt))

    disabled = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    disabled.config.gene_expression = False
    cases.append(('disabled', disabled, 0, source_dt))

    no_genome = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    empty = no_genome.cells[2]
    empty.genomes = []
    empty.genome_lesions = []
    empty.replication_template = None
    empty.replication_copy = []
    empty.replication_template_lesion = 0.0
    empty.replication_fractional = 0.0
    empty._refresh_gene_cache()
    cases.append(('no_genome', no_genome, 2, source_dt))

    zero_dt = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    cases.append(('eligible_dt0', zero_dt, 0, 0.0))

    residual = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(source_state),
    )
    residual.cells[0].pools[a4.a3.POOL_FUEL] = 9.936293309191388e-08
    cases.append(('signed_residual', residual, 0, 0.001))

    details = []
    for case_index, (label, world, index, dt) in enumerate(cases):
        cells = world.cells
        target = cells[index]
        roles = {int(spec['role']) for spec in target.gene_specs.values()}
        if target.genomes and (int(a4.g2.ROLE_GENERIC) not in roles
                               or len(roles) < 2):
            raise AssertionError(label + ' lost generic/non-generic genes')
        if label == 'eligible_dt0':
            translator = float(target.role_activity(a4.g2.ROLE_TRANSLATOR))
            if world.config.external_translator:
                translator += 0.75
            if (translator <= 1e-5 or not target.genomes
                    or not target.gene_specs):
                raise AssertionError('dt0 fixture does not reach weight gate')
        expected = [copy.deepcopy(cell) for cell in cells]
        expected_sync = a48b._translation_sync_performed(
            _a48_binding(world, target, capacity),
        )
        damaged_before = sum(float(value) for value in
                             expected[index].damaged_proteins.values())
        expected[index].translate(dt, world.config)
        if (label == 'rich_first'
                and sum(float(value) for value in
                        expected[index].damaged_proteins.values())
                <= damaged_before):
            raise AssertionError('rich fixture did not produce misfolded mass')

        before = v3.pickle_clone(world.state_dict())
        identities = _a49b_world_cell_identities(world)
        rng_object = world.rng
        rng_state = v3.pickle_clone(world.rng.bit_generator.state)
        binding = _a49a_host_binding(world, capacity)
        binding_before = (
            v3.pickle_clone(binding.ragged.state_dict()),
            v3.pickle_clone(binding.state.state_dict()),
            v3.pickle_clone(binding.cache.state_dict()),
        )
        numpy_plan = a49b.paid_translation_selected_numpy(
            binding, dt, index, int(target.cell_id),
        )
        if (numpy_plan.validate() is not numpy_plan
                or int(np.count_nonzero(numpy_plan.target_mask)) != 1
                or not bool(numpy_plan.target_mask[index])
                or numpy_plan.target_index != index
                or numpy_plan.target_cell_id != int(target.cell_id)):
            raise AssertionError(label + ' NumPy selector differs')
        _assert_translation_matches_cells(
            numpy_plan.state_after, expected,
            'a49b.numpy.' + label, atol=a49b.TRANSLATION_ORACLE_ATOL,
        )
        a49b._selected_output_scope(
            binding.state, numpy_plan.state_after, index,
        )

        for device in _a49b_devices():
            resident = a4.bind_a4_translation(
                binding.ragged.to_torch(device=device),
                binding.state.to_torch(device=device),
            )
            source_records = a49a._resident_signature(resident).tensor_records
            source_storage = {
                (str(record[5]), int(record[3])) for record in source_records
                if int(record[3]) != 0
            }
            torch_plan = a49b.paid_translation_selected(
                resident, dt, index, int(target.cell_id),
            )
            if torch_plan.validate() is not torch_plan:
                raise AssertionError(label + '/' + device + ' plan invalid')
            plan_storage = _a49b_plan_pointer_set(torch_plan)
            if plan_storage.intersection(source_storage):
                raise AssertionError(label + '/' + device + ' plan aliases source')
            host_plan = torch_plan.to_numpy()
            a49b._selected_states_match(
                host_plan.state_after, numpy_plan.state_after, index,
            )
            a49b._selected_output_scope(
                binding.state, host_plan.state_after, index,
            )
            _assert_translation_matches_cells(
                host_plan.state_after, expected,
                'a49b.%s.%s' % (device, label),
                atol=a49b.TRANSLATION_ORACLE_ATOL,
            )
            details.append('%s:%s:%s' % (
                device, label, 'sync' if expected_sync else 'early',
            ))

        v3.assert_recursive_close(
            binding_before[0], binding.ragged.state_dict(), 0.0, 0.0,
            'a49b.%s.source.ragged' % label,
        )
        v3.assert_recursive_close(
            binding_before[1], binding.state.state_dict(), 0.0, 0.0,
            'a49b.%s.source.state' % label,
        )
        v3.assert_recursive_close(
            binding_before[2], binding.cache.state_dict(), 0.0, 0.0,
            'a49b.%s.source.cache' % label,
        )
        _a49a_assert_world_unchanged(
            before, rng_object, rng_state, world,
            'a49b.selected.' + label,
        )
        if identities != _a49b_world_cell_identities(world):
            raise AssertionError(label + ' selected plan changed CPU identities')

    residual_value = cases[-1][1].cells[0].pools[a4.a3.POOL_FUEL]
    if residual_value != 9.936293309191388e-08:
        raise AssertionError('pure signed-residual source was mutated')
    return ('8 Formal066 branches across first/middle/last target; selected '
            'NumPy/%s fp64 target <=2e-12, discrete/order exact, 30-array '
            'non-target isolation, fresh version-0 31 tensors, source/RNG/'
            'identity unchanged: %s' % (
                '/'.join(_a49b_devices()), '/'.join(details),
            ))


def test_a49b_rank5_fresh_generation_epochs_nonalias_and_capacity():
    class CaptureScheduler(a49b.A4ResidentTranslationEventScheduler):
        def __init__(self, *args, **kwargs):
            super(CaptureScheduler, self).__init__(*args, **kwargs)
            self.prepared_seen = None
            self.finalized_seen = None

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            return prepared

        def _resident_translation_finalized_ready(
                self, world, cell, prepared, finalized):
            self.finalized_seen = finalized
            return finalized

    generation_details = []
    for device in _a49b_devices():
        world, cells, capacity, dt = _paid_translation_fixture(seed=18600)
        scheduler = CaptureScheduler(capacity, device)
        scheduler._bind_resident_world(world)
        owner = scheduler._resident_owner_for_rank5(world)
        initial_arena = owner._arena
        initial_epochs = owner.epochs
        initial_generation = int(owner._storage_generation)

        # Exercise the real pre-rank-5 A3 unpack identity churn.  This is an
        # R/S/C CPU_NEWER source, so the transaction must first publish a
        # separate full rebuild and only then its selected translation arena.
        packed = a4.a3.pack_a3_cell(cells[1], model_config=world.config)
        a4.a3.unpack_a3_cell(packed, cells[1])
        before = v3.pickle_clone(world.state_dict())
        before_ids = _a49b_world_cell_identities(world)
        rng_object = world.rng
        rng_state = v3.pickle_clone(world.rng.bit_generator.state)
        energy_object = world.dissipated_energy
        list_object = world.cells
        target = cells[2]
        expected = copy.deepcopy(target)
        expected.translate(dt, world.config)
        target_pool = target.pools
        target_active = target.proteins
        target_damaged = target.damaged_proteins
        target_gene_specs = target.gene_specs
        target_nested = {
            int(key): id(value) for key, value in target.gene_specs.items()
        }

        _a48b_begin_translation_direct(scheduler, world, target, dt)
        try:
            scheduler.cpu_translation(world, target, dt, world.config)
            prepared = scheduler.prepared_seen
            finalized = scheduler.finalized_seen
            if prepared is None or finalized is None:
                raise AssertionError(device + ' capture seams were not reached')
            old_arena = prepared.old_arena_ref
            old_objects, old_storage = _a49a_signature_sets(
                old_arena.resident_signature,
            )
            candidate_objects, candidate_storage = _a49a_signature_sets(
                prepared.candidate_signature,
            )
            plan_storage = _a49b_plan_pointer_set(prepared.selected_plan)
            if (old_objects.intersection(candidate_objects)
                    or old_storage.intersection(candidate_storage)
                    or plan_storage.intersection(candidate_storage)):
                raise AssertionError(device + ' old/plan/candidate storage aliases')
            records = tuple(prepared.candidate_signature.tensor_records)
            if any(int(record[4]) != 0 for record in records):
                raise AssertionError(device + ' candidate tensor version differs')
            expected_epochs = a49a._advance_epochs(
                prepared.old_epochs, frozenset(('S',)), cache_built=False,
            )
            if (initial_arena.retired is not True
                    or old_arena.retired is not True
                    or old_arena.lifecycle != a49a.INVALID
                    or owner.arena_id != prepared.expected_arena_id
                    or int(owner._storage_generation)
                    != int(prepared.old_storage_generation) + 1
                    or int(prepared.old_storage_generation)
                    != initial_generation + 1
                    or owner.epochs != expected_epochs
                    or owner.lifecycle != a49b.COHERENT
                    or owner._arena.binding is not prepared.candidate_binding
                    or owner._arena.resident_signature
                    != prepared.candidate_signature
                    or not prepared.consumed
                    or not finalized.consumed):
                raise AssertionError(device + ' fresh generation/CAS differs')
            if (prepared.old_epochs.membership_epoch
                    != initial_epochs.membership_epoch
                    or prepared.old_epochs.ragged_epoch
                    != initial_epochs.ragged_epoch + 1
                    or prepared.old_epochs.state_epoch
                    != initial_epochs.state_epoch + 1
                    or prepared.old_epochs.cache_epoch
                    != initial_epochs.cache_epoch + 1
                    or prepared.old_epochs.cache_source_ragged_epoch
                    != prepared.old_epochs.ragged_epoch):
                raise AssertionError(device + ' pre-rank5 rebuild epochs differ')
            _a48b_assert_cell_matches(
                expected, target, 'a49b.%s.rank5.commit' % device,
                atol=a49b.TRANSLATION_ORACLE_ATOL,
            )
            _a49a_assert_snapshot_matches(
                world, capacity, owner._validation_snapshot(world),
                'a49b.%s.rank5.full_candidate' % device,
            )
            if (target.pools is not target_pool
                    or not prepared.sync_performed
                    or target.proteins is target_active
                    or target.damaged_proteins is target_damaged
                    or target.gene_specs is not target_gene_specs
                    or {int(key): id(value)
                        for key, value in target.gene_specs.items()}
                    != target_nested
                    or world.cells is not list_object
                    or world.rng is not rng_object
                    or world.rng.bit_generator.state != rng_state
                    or world.dissipated_energy is not energy_object):
                raise AssertionError(device + ' target identity/RNG/energy differs')
            after_ids = _a49b_world_cell_identities(world)
            for index in range(len(cells)):
                if index != 2 and after_ids[index] != before_ids[index]:
                    raise AssertionError(device + ' changed non-target identity')
                if index != 2:
                    v3.assert_recursive_close(
                        before['cells'][index], world.state_dict()['cells'][index],
                        0.0, 0.0,
                        'a49b.%s.non_target[%d]' % (device, index),
                    )
            first_signature = owner._arena.resident_signature
            first_storage_generation = int(owner._storage_generation)
            first_epochs = owner.epochs
        finally:
            _a48_abort_direct(scheduler, target)

        # A valid no-op is still one logical translation invocation: it must
        # publish a fresh nonalias generation and advance S exactly once.
        noop_target = cells[3]
        noop_before = copy.deepcopy(noop_target)
        noop_before.translate(0.0, world.config)
        noop_active = noop_target.proteins
        noop_damaged = noop_target.damaged_proteins
        scheduler.prepared_seen = None
        scheduler.finalized_seen = None
        _a48b_begin_translation_direct(scheduler, world, noop_target, 0.0)
        try:
            scheduler.cpu_translation(
                world, noop_target, 0.0, world.config,
            )
            second = scheduler.prepared_seen
            if second is None:
                raise AssertionError(device + ' no-op candidate missing')
            first_objects, first_storage = _a49a_signature_sets(
                first_signature,
            )
            second_objects, second_storage = _a49a_signature_sets(
                second.candidate_signature,
            )
            second_plan_storage = _a49b_plan_pointer_set(
                second.selected_plan,
            )
            if (first_objects.intersection(second_objects)
                    or first_storage.intersection(second_storage)
                    or second_plan_storage.intersection(second_storage)
                    or any(int(record[4]) != 0 for record in
                           second.candidate_signature.tensor_records)
                    or int(owner._storage_generation)
                    != first_storage_generation + 1
                    or owner.epochs != a49a._advance_epochs(
                        first_epochs, frozenset(('S',)), cache_built=False,
                    )
                    or owner.lifecycle != a49b.COHERENT
                    or second.sync_performed
                    or noop_target.proteins is not noop_active
                    or noop_target.damaged_proteins is not noop_damaged
                    or second.old_arena_ref.retired is not True):
                raise AssertionError(device + ' successive no-op generation differs')
            _a48b_assert_cell_matches(
                noop_before, noop_target, 'a49b.%s.noop' % device,
                atol=a49b.TRANSLATION_ORACLE_ATOL,
            )
            _a49a_assert_snapshot_matches(
                world, capacity, owner._validation_snapshot(world),
                'a49b.%s.noop.full_candidate' % device,
            )
            second_signature = owner._arena.resident_signature
            second_storage_generation = int(owner._storage_generation)
            second_epochs = owner.epochs
        finally:
            _a48_abort_direct(scheduler, noop_target)

        # Distinguish an eligible gate-after zero-work call from the early
        # no-translator branch above.  Both advance S/generation, but only the
        # gate-after branch replaces both protein dictionary outer objects.
        eligible = cells[0]
        translator = float(eligible.role_activity(a4.g2.ROLE_TRANSLATOR))
        if world.config.external_translator:
            translator += 0.75
        if translator <= 1e-5 or not eligible.genomes or not eligible.gene_specs:
            raise AssertionError(device + ' eligible dt0 fixture missed gate')
        eligible_expected = copy.deepcopy(eligible)
        eligible_expected.translate(0.0, world.config)
        eligible_active = eligible.proteins
        eligible_damaged = eligible.damaged_proteins
        scheduler.prepared_seen = None
        scheduler.finalized_seen = None
        _a48b_begin_translation_direct(scheduler, world, eligible, 0.0)
        try:
            scheduler.cpu_translation(world, eligible, 0.0, world.config)
            third = scheduler.prepared_seen
            second_objects, second_storage = _a49a_signature_sets(
                second_signature,
            )
            third_objects, third_storage = _a49a_signature_sets(
                third.candidate_signature,
            )
            third_plan_storage = _a49b_plan_pointer_set(third.selected_plan)
            if (not third.sync_performed
                    or eligible.proteins is eligible_active
                    or eligible.damaged_proteins is eligible_damaged
                    or second_objects.intersection(third_objects)
                    or second_storage.intersection(third_storage)
                    or first_objects.intersection(third_objects)
                    or first_storage.intersection(third_storage)
                    or third_plan_storage.intersection(third_storage)
                    or any(int(record[4]) != 0 for record in
                           third.candidate_signature.tensor_records)
                    or int(owner._storage_generation)
                    != second_storage_generation + 1
                    or owner.epochs != a49a._advance_epochs(
                        second_epochs, frozenset(('S',)), cache_built=False,
                    )
                    or third.old_arena_ref.retired is not True
                    or owner.lifecycle != a49b.COHERENT):
                raise AssertionError(
                    device + ' eligible dt0 generation/identity differs'
                )
            _a48b_assert_cell_matches(
                eligible_expected, eligible, 'a49b.%s.eligible_dt0' % device,
                atol=a49b.TRANSLATION_ORACLE_ATOL,
            )
            _a49a_assert_snapshot_matches(
                world, capacity, owner._validation_snapshot(world),
                'a49b.%s.eligible_dt0.full_candidate' % device,
            )
            generation_details.append(
                '%s:g%d>%d>%d>%d:S%d' % (
                    device, initial_generation,
                    first_storage_generation, second_storage_generation,
                    int(owner._storage_generation),
                    int(owner.epochs.state_epoch),
                )
            )
        finally:
            _a48_abort_direct(scheduler, eligible)

    capacity_details = []
    for device in _a49b_devices():
        exact_world, exact_cells, exact = _a49a_fixture(seed=18650)
        exact_scheduler = a49b.A4ResidentTranslationEventScheduler(
            exact, device,
        )
        exact_cell = exact_cells[1]
        _a49b_begin_translation_direct(
            exact_scheduler, exact_world, exact_cell, 0.1,
        )
        try:
            exact_scheduler.cpu_translation(
                exact_world, exact_cell, 0.1, exact_world.config,
            )
        finally:
            _a48_abort_direct(exact_scheduler, exact_cell)

        values = {
            'max_cells': int(exact.max_cells),
            'max_sequences': int(exact.max_sequences),
            'max_symbols': int(exact.max_symbols),
            'max_sequence_symbols': int(exact.max_sequence_symbols),
            'max_proteins_per_cell': int(exact.max_proteins_per_cell),
        }
        for axis, field in (
                ('C', 'max_cells'), ('Q', 'max_sequences'),
                ('S', 'max_symbols'), ('W', 'max_sequence_symbols'),
                ('P', 'max_proteins_per_cell')):
            short_values = dict(values)
            short_values[field] -= 1
            short = a4.GPU068A4Config(**short_values)
            world, cells, _ = _a49a_fixture(seed=18660)
            cell = cells[1]
            scheduler = a49b.A4ResidentTranslationEventScheduler(
                short, device,
            )
            before = v3.pickle_clone(world.state_dict())
            rng = world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            _a49b_begin_translation_direct(scheduler, world, cell, 0.1)
            try:
                _assert_raises(
                    (a4.A4CapacityError,
                     a49a.A4ResidentArenaError,
                     a49b.A4ResidentTranslationCommitError),
                    lambda: scheduler.cpu_translation(
                        world, cell, 0.1, world.config,
                    ),
                )
                _, record = scheduler._record(cell)
                if any(entry['event'] == 'translation_cpu'
                       for entry in record['events']):
                    raise AssertionError(axis + ' one-short crossed claim')
                _a49a_assert_world_unchanged(
                    before, rng, rng_state, world,
                    'a49b.capacity.%s.%s' % (device, axis),
                )
            finally:
                _a48_abort_direct(scheduler, cell)
            capacity_details.append(device + ':' + axis)

    return ('pre-rank5 real R/S/C CPU_NEWER full rebuild, success/no-op fresh '
            'COHERENT S+1 generation CAS, old retired, plan31/candidate50/'
            'successive50 nonalias, target/non-target identity exact %s; '
            'exact C/Q/S/W/P and one-short preclaim %s' % (
                '/'.join(generation_details), '/'.join(capacity_details),
            ))


def test_a49b_fail_closed_rollback_cas_leases_duplicate_and_order():
    class TamperPrepared(a49b.A4ResidentTranslationEventScheduler):
        def __init__(self, *args, **kwargs):
            self.mode = kwargs.pop('mode')
            self.prepared_seen = None
            super(TamperPrepared, self).__init__(*args, **kwargs)

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            if self.mode == 'plan_version':
                prepared.selected_plan.state_after.pools[0, 0] += 1.0
            elif self.mode == 'candidate_data':
                prepared.candidate_binding.state.pools.data[0, 0] += 1.0
            elif self.mode == 'candidate_pointer':
                object.__setattr__(
                    prepared.candidate_binding, 'state',
                    prepared.candidate_binding.state.clone(),
                )
            elif self.mode == 'host_oracle':
                prepared.numpy_oracle.state_after.pools[0, 0] += 1.0
            elif self.mode == 'diagnostic_host':
                prepared.diagnostic_state.pools[0, 0] += 1.0
            elif self.mode == 'prepared_seal':
                prepared.expected_storage_generation += 1
            elif self.mode == 'rng_snapshot':
                changed = copy.deepcopy(prepared.rng_state)
                changed['state']['state'] = (
                    int(changed['state']['state']) + 1
                ) % (1 << 128)
                prepared.rng_state = changed
            elif self.mode == 'old_data_trust':
                tensor = self._a49b_owner._arena.binding.ragged.symbols
                tensor.data[0] = (int(tensor.data[0]) + 1) % 8
            else:
                raise AssertionError('unknown A4.9b tamper mode')
            return prepared

    class FailAfterPublish(a49b.A4ResidentTranslationEventScheduler):
        prepared_seen = None

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            return prepared

        def _publish_resident_translation_candidate(
                self, world, cell, prepared):
            super(FailAfterPublish, self)._publish_resident_translation_candidate(
                world, cell, prepared,
            )
            cell.pools[a4.a3.POOL_FUEL] += 1.0
            cell.proteins[999991] = 1.0
            cell.damaged_proteins[999992] = 2.0
            cell.last_translation += 1.0
            cell.last_quiescence += 1.0
            fingerprint = next(iter(cell.gene_specs))
            cell.gene_specs[fingerprint]['promoter'] += 0.125
            cell.gene_specs = copy.deepcopy(cell.gene_specs)
            world.dissipated_energy += 1.0
            world.rng.random()
            raise RuntimeError('injected A4.9b post-publish failure')

    class FailFinalArmRngEnergy(a49b.A4ResidentTranslationEventScheduler):
        prepared_seen = None

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            return prepared

        def _resident_translation_finalized_ready(
                self, world, cell, prepared, finalized):
            world.rng.random()
            world.dissipated_energy += 1.0
            return finalized

    class FailRollbackAttestation(FailAfterPublish):
        def _rollback_resident_translation_publish(
                self, world, cell, snapshot):
            super(FailRollbackAttestation, self)._rollback_resident_translation_publish(
                world, cell, snapshot,
            )
            cell.pools[a4.a3.POOL_FUEL] = np.nextafter(
                float(cell.pools[a4.a3.POOL_FUEL]), np.inf,
            )

    class TamperLiveSource(a49b.A4ResidentTranslationEventScheduler):
        def __init__(self, *args, **kwargs):
            self.mode = kwargs.pop('mode')
            self.injected_state = None
            self.injected_identities = None
            self.injected_rng = None
            self.injected_rng_state = None
            super(TamperLiveSource, self).__init__(*args, **kwargs)

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            if self.mode == 'cpu_value':
                cell.pools[a4.a3.POOL_FUEL] = np.nextafter(
                    float(cell.pools[a4.a3.POOL_FUEL]), np.inf,
                )
            elif self.mode == 'cpu_identity':
                cell.pools = np.asarray(
                    cell.pools, dtype=np.float64,
                ).copy()
            elif self.mode == 'live_rng':
                world.rng.random()
            else:
                raise AssertionError('unknown live-source tamper')
            self.injected_state = v3.pickle_clone(world.state_dict())
            self.injected_identities = _a49b_world_cell_identities(world)
            self.injected_rng = world.rng
            self.injected_rng_state = v3.pickle_clone(
                world.rng.bit_generator.state,
            )
            return prepared

    preclaim_details = []
    for device in _a49b_devices():
        for mode in (
                'plan_version', 'candidate_data', 'candidate_pointer',
                'host_oracle', 'diagnostic_host', 'prepared_seal',
                'rng_snapshot',
                'old_data_trust'):
            world, cells, capacity, dt = _paid_translation_fixture(seed=18700)
            cell = cells[0]
            scheduler = TamperPrepared(capacity, device, mode=mode)
            scheduler._bind_resident_world(world)
            owner = scheduler._resident_owner_for_rank5(world)
            old_arena = owner._arena
            old_epochs = owner.epochs
            old_signature = old_arena.resident_signature
            before = v3.pickle_clone(world.state_dict())
            identities = _a49b_world_cell_identities(world)
            rng = world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            _a48b_begin_translation_direct(scheduler, world, cell, dt)
            try:
                _assert_raises(
                    (a49a.A4ResidentArenaError,
                     a49b.A4ResidentTranslationCommitError),
                    lambda: scheduler.cpu_translation(
                        world, cell, dt, world.config,
                    ),
                )
                _, record = scheduler._record(cell)
                if any(entry['event'] == 'translation_cpu'
                       for entry in record['events']):
                    raise AssertionError(mode + ' preclaim tamper was claimed')
                _a49a_assert_world_unchanged(
                    before, rng, rng_state, world,
                    'a49b.preclaim.' + mode,
                )
                if identities != _a49b_world_cell_identities(world):
                    raise AssertionError(mode + ' changed CPU identities')
                if mode == 'old_data_trust':
                    if owner.lifecycle != a49b.INVALID:
                        raise AssertionError('old resident trust stayed usable')
                    _assert_raises(
                        a49b.A4ResidentTranslationCommitError,
                        lambda: owner._cohere_rank5(world),
                    )
                elif (owner.lifecycle != a49b.COHERENT
                      or owner._arena is not old_arena
                      or owner.epochs != old_epochs
                      or owner._arena.resident_signature != old_signature
                      or owner._tx_active
                      or scheduler.prepared_seen is None
                      or not scheduler.prepared_seen.consumed):
                    raise AssertionError(mode + ' changed trustworthy old owner')
                preclaim_details.append(device + ':' + mode)
            finally:
                _a48_abort_direct(scheduler, cell)

        def change_owner_epoch(owner):
            value = owner._epochs
            owner._epochs = a49a.A4ResidentArenaEpochs(
                value.membership_epoch, value.ragged_epoch,
                value.state_epoch + 1, value.cache_epoch,
                value.cache_source_ragged_epoch,
            )

        def change_cache_source_epoch(owner):
            value = owner._epochs
            owner._epochs = a49a.A4ResidentArenaEpochs(
                value.membership_epoch, value.ragged_epoch + 1,
                value.state_epoch, value.cache_epoch,
                value.cache_source_ragged_epoch,
            )

        def change_storage_generation(owner):
            owner._storage_generation += 1

        for label, mutate in (
                ('owner_epoch', change_owner_epoch),
                ('cache_source_epoch', change_cache_source_epoch),
                ('storage_generation', change_storage_generation)):
            world, cells, capacity, dt = _paid_translation_fixture(seed=18710)
            cell = cells[0]
            scheduler = a49b.A4ResidentTranslationEventScheduler(
                capacity, device,
            )
            scheduler._bind_resident_world(world)
            owner = scheduler._resident_owner_for_rank5(world)
            before = v3.pickle_clone(world.state_dict())
            identities = _a49b_world_cell_identities(world)
            rng = world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            mutate(owner)
            _a48b_begin_translation_direct(scheduler, world, cell, dt)
            try:
                _assert_raises(
                    (a49a.A4ResidentArenaError,
                     a49b.A4ResidentTranslationCommitError),
                    lambda: scheduler.cpu_translation(
                        world, cell, dt, world.config,
                    ),
                )
                _, record = scheduler._record(cell)
                if (any(entry['event'] == 'translation_cpu'
                        for entry in record['events'])
                        or owner.lifecycle != a49b.INVALID
                        or identities != _a49b_world_cell_identities(world)):
                    raise AssertionError(label + ' trust tamper was not isolated')
                _a49a_assert_world_unchanged(
                    before, rng, rng_state, world,
                    'a49b.owner_trust.' + label,
                )
                _assert_raises(
                    a49b.A4ResidentTranslationCommitError,
                    lambda: owner._cohere_rank5(world),
                )
                preclaim_details.append(device + ':' + label + ':INVALID')
            finally:
                _a48_abort_direct(scheduler, cell)

        for mode in ('cpu_value', 'cpu_identity', 'live_rng'):
            world, cells, capacity, dt = _paid_translation_fixture(seed=18715)
            cell = cells[0]
            scheduler = TamperLiveSource(capacity, device, mode=mode)
            scheduler._bind_resident_world(world)
            owner = scheduler._resident_owner_for_rank5(world)
            old_arena = owner._arena
            old_epochs = owner.epochs
            _a48b_begin_translation_direct(scheduler, world, cell, dt)
            try:
                _assert_raises(
                    a49b.A4ResidentTranslationCommitError,
                    lambda: scheduler.cpu_translation(
                        world, cell, dt, world.config,
                    ),
                )
                _, record = scheduler._record(cell)
                if (any(entry['event'] == 'translation_cpu'
                        for entry in record['events'])
                        or scheduler.injected_state is None):
                    raise AssertionError(mode + ' crossed the claim boundary')
                v3.assert_recursive_close(
                    scheduler.injected_state, world.state_dict(), 0.0, 0.0,
                    'a49b.live_source.' + mode,
                )
                if (scheduler.injected_identities
                        != _a49b_world_cell_identities(world)
                        or world.rng is not scheduler.injected_rng
                        or world.rng.bit_generator.state
                        != scheduler.injected_rng_state):
                    raise AssertionError(mode + ' had post-detection mutation')
                if mode == 'live_rng':
                    if (owner.lifecycle != a49b.COHERENT
                            or owner._arena is not old_arena
                            or owner.epochs != old_epochs):
                        raise AssertionError('RNG-only rejection dirtied owner')
                elif owner.lifecycle != a49b.INVALID:
                    raise AssertionError(mode + ' CPU trust stayed usable')
                preclaim_details.append(device + ':' + mode)
            finally:
                _a48_abort_direct(scheduler, cell)

        # Caller gene_specs must be an exact materialization of the derived
        # cache.  A missing single cache entry and one-short cell capacity are
        # both rejected before a lease, candidate, claim, or fallback exists.
        cache_world, cache_cells, cache_capacity, dt = (
            _paid_translation_fixture(seed=18718)
        )
        exact_cache_owner = a49b._A4ResidentTranslationOwner.from_cpu(
            cache_world, cache_capacity, device,
        )
        exact_cache_owner._validation_snapshot(cache_world)
        missing_world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(cache_world.state_dict()),
        )
        missing_cell = missing_world.cells[0]
        missing_cell.gene_specs.pop(next(reversed(missing_cell.gene_specs)))
        missing_before = v3.pickle_clone(missing_world.state_dict())
        missing_ids = _a49b_world_cell_identities(missing_world)
        missing_rng = missing_world.rng
        missing_rng_state = v3.pickle_clone(
            missing_rng.bit_generator.state,
        )
        missing = a49b.A4ResidentTranslationEventScheduler(
            cache_capacity, device,
        )
        _a49b_begin_translation_direct(
            missing, missing_world, missing_cell, dt,
        )
        try:
            _assert_raises(
                (a49a.A4ResidentArenaError,
                 a49b.A4ResidentTranslationCommitError),
                lambda: missing.cpu_translation(
                    missing_world, missing_cell, dt, missing_world.config,
                ),
            )
            _, record = missing._record(missing_cell)
            if (missing._a49b_owner is not None
                    or any(entry['event'] == 'translation_cpu'
                           for entry in record['events'])
                    or missing_ids != _a49b_world_cell_identities(
                        missing_world)):
                raise AssertionError('cache missing-one crossed preflight')
            _a49a_assert_world_unchanged(
                missing_before, missing_rng, missing_rng_state,
                missing_world, 'a49b.cache_missing_one',
            )
        finally:
            _a48_abort_direct(missing, missing_cell)

        short_values = {
            'max_cells': int(cache_capacity.max_cells) - 1,
            'max_sequences': int(cache_capacity.max_sequences),
            'max_symbols': int(cache_capacity.max_symbols),
            'max_sequence_symbols': int(
                cache_capacity.max_sequence_symbols
            ),
            'max_proteins_per_cell': int(
                cache_capacity.max_proteins_per_cell
            ),
        }
        short_cells = a4.GPU068A4Config(**short_values)
        count_world = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(cache_world.state_dict()),
        )
        count_cell = count_world.cells[0]
        count_before = v3.pickle_clone(count_world.state_dict())
        count_rng = count_world.rng
        count_rng_state = v3.pickle_clone(count_rng.bit_generator.state)
        fallback_calls = []

        class NoCapacityFallback(
                a49b.A4ResidentTranslationEventScheduler):
            def _publish_resident_translation_candidate(
                    self, world, cell, prepared):
                fallback_calls.append('resident_publish')
                raise AssertionError(
                    'one-short capacity reached resident publisher'
                )

            def _publish_translation_candidate(
                    self, world, cell, prepared):
                fallback_calls.append('legacy_publish')
                raise AssertionError(
                    'one-short capacity reached translation publisher'
                )

        count_scheduler = NoCapacityFallback(short_cells, device)
        original_builder = (
            a49b._A4ResidentTranslationOwner._build_prepared_storage
        )
        original_legacy_translation = (
            a48b.A4TranslationEventScheduler.cpu_translation
        )

        def capacity_fallback_bomb(*args, **kwargs):
            fallback_calls.append('builder/fallback')
            raise AssertionError(
                'one-short capacity clipped, grew, or fell back'
            )

        a49b._A4ResidentTranslationOwner._build_prepared_storage = (
            capacity_fallback_bomb
        )
        a48b.A4TranslationEventScheduler.cpu_translation = (
            capacity_fallback_bomb
        )
        _a49b_begin_translation_direct(
            count_scheduler, count_world, count_cell, dt,
        )
        try:
            _assert_raises(
                (a49a.A4ResidentArenaError,
                 a49b.A4ResidentTranslationCommitError),
                lambda: count_scheduler.cpu_translation(
                    count_world, count_cell, dt, count_world.config,
                ),
            )
            _, record = count_scheduler._record(count_cell)
            if (count_scheduler._a49b_owner is not None
                    or fallback_calls
                    or any(entry['event'] == 'translation_cpu'
                           for entry in record['events'])):
                raise AssertionError(
                    'cell-count one-short was claimed/clipped/fell back'
                )
            _a49a_assert_world_unchanged(
                count_before, count_rng, count_rng_state, count_world,
                'a49b.cell_count_one_short',
            )
        finally:
            a49b._A4ResidentTranslationOwner._build_prepared_storage = (
                original_builder
            )
            a48b.A4TranslationEventScheduler.cpu_translation = (
                original_legacy_translation
            )
            _a48_abort_direct(count_scheduler, count_cell)
        preclaim_details.extend((
            device + ':cache_exact/missing_one',
            device + ':cell_count_exact/one_short',
        ))

        invalid_world, invalid_cells, invalid_capacity, dt = (
            _paid_translation_fixture(seed=18719)
        )
        invalid_cell = invalid_cells[0]
        invalid_scheduler = a49b.A4ResidentTranslationEventScheduler(
            invalid_capacity, device,
        )
        invalid_scheduler._bind_resident_world(invalid_world)
        invalid_owner = invalid_scheduler._resident_owner_for_rank5(
            invalid_world,
        )
        invalid_owner.invalidate('validation preexisting INVALID')
        invalid_arena = invalid_owner._arena
        invalid_epochs = invalid_owner.epochs
        invalid_before = v3.pickle_clone(invalid_world.state_dict())
        invalid_rng = invalid_world.rng
        invalid_rng_state = v3.pickle_clone(
            invalid_rng.bit_generator.state,
        )
        _a48b_begin_translation_direct(
            invalid_scheduler, invalid_world, invalid_cell, dt,
        )
        try:
            _assert_raises(
                a49b.A4ResidentTranslationCommitError,
                lambda: invalid_scheduler.cpu_translation(
                    invalid_world, invalid_cell, dt, invalid_world.config,
                ),
            )
            _, record = invalid_scheduler._record(invalid_cell)
            if (any(entry['event'] == 'translation_cpu'
                    for entry in record['events'])
                    or invalid_owner.lifecycle != a49b.INVALID
                    or invalid_owner._arena is not invalid_arena
                    or invalid_owner.epochs != invalid_epochs):
                raise AssertionError('preexisting INVALID was auto-rebuilt')
            _a49a_assert_world_unchanged(
                invalid_before, invalid_rng, invalid_rng_state,
                invalid_world, 'a49b.preexisting_invalid',
            )
        finally:
            _a48_abort_direct(invalid_scheduler, invalid_cell)
        preclaim_details.append(device + ':preexisting_INVALID')

        # Issued/open/consumed translation leases are private, one-shot, and
        # cannot be replayed on another owner.
        lease_world, _, lease_capacity, _ = _paid_translation_fixture(
            seed=18720,
        )
        other_world, _, other_capacity, _ = _paid_translation_fixture(
            seed=18721,
        )
        owner = a49b._A4ResidentTranslationOwner.from_cpu(
            lease_world, lease_capacity, device,
        )
        other = a49b._A4ResidentTranslationOwner.from_cpu(
            other_world, other_capacity, device,
        )
        stale = owner._open_translation_lease(lease_world)
        _assert_raises(
            a49b.A4ResidentTranslationCommitError,
            lambda: other._activate_translation_lease(stale),
        )
        owner._abort_preclaim(lease_world)
        _assert_raises(a49b.A4ResidentTranslationCommitError, stale.__enter__)
        consumed = owner._open_translation_lease(lease_world)
        try:
            with consumed as active:
                active._binding_once()
                _assert_raises(
                    a49b.A4ResidentTranslationCommitError,
                    active._binding_once,
                )
                active._consumed = False
                _assert_raises(
                    a49b.A4ResidentTranslationCommitError,
                    active._binding_once,
                )
                raise RuntimeError('intentional preprepared lease abort')
        except RuntimeError:
            pass
        _assert_raises(
            a49b.A4ResidentTranslationCommitError, consumed.__enter__,
        )
        if owner.lifecycle != a49b.COHERENT or owner._tx_active:
            raise AssertionError(device + ' lease abort changed old authority')

    rollback_details = []
    for device in _a49b_devices():
        for failure_type, label in (
                (FailAfterPublish, 'post_publish'),
                (FailFinalArmRngEnergy, 'final_arm_rng_energy')):
            world, cells, capacity, dt = _paid_translation_fixture(seed=18740)
            cell = cells[0]
            scheduler = failure_type(capacity, device)
            scheduler._bind_resident_world(world)
            owner = scheduler._resident_owner_for_rank5(world)
            old_arena = owner._arena
            old_id = owner.arena_id
            old_epochs = owner.epochs
            old_signature = old_arena.resident_signature
            before = v3.pickle_clone(world.state_dict())
            identities = _a49b_world_cell_identities(world)
            rng = world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            energy = world.dissipated_energy
            _a48b_begin_translation_direct(scheduler, world, cell, dt)
            caught = None
            try:
                scheduler.cpu_translation(world, cell, dt, world.config)
            except (RuntimeError,
                    a49b.A4ResidentTranslationCommitError) as exc:
                caught = exc
            if caught is None:
                raise AssertionError(label + ' injected failure escaped')
            _a49a_assert_world_unchanged(
                before, rng, rng_state, world,
                'a49b.rollback.' + label,
            )
            if (identities != _a49b_world_cell_identities(world)
                    or world.dissipated_energy is not energy
                    or owner._arena is not old_arena
                    or owner.arena_id != old_id
                    or owner.epochs != old_epochs
                    or owner._arena.resident_signature != old_signature
                    or owner.lifecycle != a49b.COHERENT
                    or old_arena.retired
                    or owner._tx_active
                    or scheduler.prepared_seen is None
                    or not scheduler.prepared_seen.consumed):
                raise AssertionError(label + ' rollback identity/CAS differs')
            entry = _a48b_translation_entry(scheduler, cell)
            if ('amount' in entry['metadata']
                    or entry['metadata'].get('rng_draw_count') is not None):
                raise AssertionError(label + ' annotated before final arm')
            receipt = _a48_abort_direct(scheduler, cell, caught)
            if receipt['status'] != 'aborted':
                raise AssertionError(label + ' did not abort claimed step')
            rollback_details.append(device + ':' + label)

        mismatch_world, mismatch_cells, capacity, dt = (
            _paid_translation_fixture(seed=18760)
        )
        mismatch_cell = mismatch_cells[0]
        mismatch = FailRollbackAttestation(capacity, device)
        mismatch._bind_resident_world(mismatch_world)
        mismatch_owner = mismatch._resident_owner_for_rank5(mismatch_world)
        mismatch_rng = mismatch_world.rng
        mismatch_rng_state = v3.pickle_clone(
            mismatch_rng.bit_generator.state,
        )
        mismatch_energy = mismatch_world.dissipated_energy
        _a48b_begin_translation_direct(
            mismatch, mismatch_world, mismatch_cell, dt,
        )
        caught = None
        try:
            mismatch.cpu_translation(
                mismatch_world, mismatch_cell, dt, mismatch_world.config,
            )
        except a49b.A4ResidentTranslationCommitError as exc:
            caught = exc
        if (caught is None
                or mismatch_owner.lifecycle != a49b.INVALID
                or mismatch_owner._tx_active
                or mismatch_world.rng is not mismatch_rng
                or mismatch_world.rng.bit_generator.state
                != mismatch_rng_state
                or mismatch_world.dissipated_energy is not mismatch_energy):
            raise AssertionError(
                device + ' rollback-attestation mismatch was not isolated'
            )
        _a48b_translation_entry(mismatch, mismatch_cell)
        mismatch_receipt = _a48_abort_direct(
            mismatch, mismatch_cell, caught,
        )
        if mismatch_receipt['status'] != 'aborted':
            raise AssertionError('rollback mismatch did not abort step')
        rollback_details.append(device + ':rollback_mismatch_INVALID')

    # Public selector/type identity checks fail without any resident claim.
    public_world, public_cells, public_capacity, dt = (
        _paid_translation_fixture(seed=18770)
    )
    host = _a49a_host_binding(public_world, public_capacity)
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: a49b.paid_translation_selected_numpy(
            host, dt, -1, int(public_cells[0].cell_id),
        ),
    )
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: a49b.paid_translation_selected_numpy(
            host, dt, 0, int(public_cells[0].cell_id) + 1,
        ),
    )
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: a49b.paid_translation_selected_numpy(
            host, dt, True, int(public_cells[0].cell_id),
        ),
    )
    resident = a4.bind_a4_translation(
        host.ragged.to_torch(device='cpu'),
        host.state.to_torch(device='cpu'),
    )
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: a49b.paid_translation_selected_numpy(
            resident, dt, 0, int(public_cells[0].cell_id),
        ),
    )
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: a49b.paid_translation_selected_torch(
            host, dt, 0, int(public_cells[0].cell_id),
        ),
    )

    wrong_input_details = []
    for label in ('world', 'cell', 'config', 'dt'):
        world, cells, capacity, dt = _paid_translation_fixture(seed=18775)
        cell = cells[0]
        scheduler = a49b.A4ResidentTranslationEventScheduler(
            capacity, 'cpu',
        )
        _a49b_begin_translation_direct(scheduler, world, cell, dt)
        before = v3.pickle_clone(world.state_dict())
        rng = world.rng
        rng_state = v3.pickle_clone(rng.bit_generator.state)
        if label == 'world':
            foreign_world = a4.a3.s66.Formal066World.from_state(
                v3.pickle_clone(world.state_dict()),
            )
            operation = lambda: scheduler.cpu_translation(
                foreign_world, cell, dt, foreign_world.config,
            )
        elif label == 'cell':
            foreign_cell = copy.deepcopy(cell)
            operation = lambda: scheduler.cpu_translation(
                world, foreign_cell, dt, world.config,
            )
        elif label == 'config':
            foreign_config = copy.deepcopy(world.config)
            operation = lambda: scheduler.cpu_translation(
                world, cell, dt, foreign_config,
            )
        else:
            wrong_dt = np.nextafter(dt, np.inf)
            operation = lambda: scheduler.cpu_translation(
                world, cell, wrong_dt, world.config,
            )
        try:
            _assert_raises(
                (a3s.A3SchedulerProtocolError,
                 a49b.A4ResidentTranslationCommitError),
                operation,
            )
            _, record = scheduler._record(cell)
            if (scheduler._a49b_owner is not None
                    or any(entry['event'] == 'translation_cpu'
                           for entry in record['events'])):
                raise AssertionError(label + ' input crossed claim/owner')
            _a49a_assert_world_unchanged(
                before, rng, rng_state, world,
                'a49b.wrong_input.' + label,
            )
            wrong_input_details.append(label)
        finally:
            _a48_abort_direct(scheduler, cell)

    runtime_seal_details = []
    for device in _a49b_devices():
        for label in ('device', 'config_rebind', 'config_value'):
            world, cells, capacity, dt = _paid_translation_fixture(seed=18776)
            cell = cells[0]
            scheduler = a49b.A4ResidentTranslationEventScheduler(
                capacity, device,
            )
            scheduler._bind_resident_world(world)
            owner = scheduler._resident_owner_for_rank5(world)
            old_arena = owner._arena
            old_epochs = owner.epochs
            old_signature = old_arena.resident_signature
            before = v3.pickle_clone(world.state_dict())
            rng = world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            if label == 'device':
                scheduler.a4_device = (
                    'cuda' if device == 'cpu' else 'cpu'
                )
            elif label == 'config_rebind':
                scheduler.a4_config = copy.deepcopy(capacity)
            else:
                object.__setattr__(
                    scheduler.a4_config, 'max_symbols',
                    int(scheduler.a4_config.max_symbols) + 1,
                )
            _a48b_begin_translation_direct(scheduler, world, cell, dt)
            try:
                _assert_raises(
                    a49b.A4ResidentTranslationCommitError,
                    lambda: scheduler.cpu_translation(
                        world, cell, dt, world.config,
                    ),
                )
                _, record = scheduler._record(cell)
                if (any(entry['event'] == 'translation_cpu'
                        for entry in record['events'])
                        or owner._lifecycle != a49b.COHERENT
                        or owner._arena is not old_arena
                        or a49a._epoch_values(owner._epochs)
                        != a49a._epoch_values(old_epochs)
                        or owner._arena.resident_signature != old_signature):
                    raise AssertionError(label + ' runtime seal crossed claim')
                _a49a_assert_world_unchanged(
                    before, rng, rng_state, world,
                    'a49b.scheduler_seal.' + label,
                )
                runtime_seal_details.append(device + ':scheduler_' + label)
            finally:
                _a48_abort_direct(scheduler, cell)

        def backend_config_rebind(hybrid):
            hybrid.backend.config = copy.deepcopy(hybrid.backend.config)

        def backend_config_value(hybrid):
            object.__setattr__(
                hybrid.backend.config, 'max_particles',
                int(hybrid.backend.config.max_particles) + 1,
            )

        def backend_device(hybrid):
            hybrid.backend.device = torch.device(
                'cuda' if device == 'cpu' else 'cpu'
            )

        def backend_dtype(hybrid):
            hybrid.backend.dtype = torch.float32

        def backend_name(hybrid):
            hybrid.scheduler._backend_name = 'tampered-backend-name'

        def backend_world(hybrid):
            hybrid.world._soma068a3_backend = object()

        for label, mutate in (
                ('config_rebind', backend_config_rebind),
                ('config_value', backend_config_value),
                ('device', backend_device), ('dtype', backend_dtype),
                ('name', backend_name), ('world', backend_world)):
            source = v3.make_world(seed=18777, cells=1)
            hybrid = _a49b_hybrid_from_state(
                source.state_dict(), capacity, device,
            )
            owner = hybrid.scheduler._resident_owner_for_rank5(hybrid.world)
            old_arena = owner._arena
            old_epochs = owner.epochs
            old_signature = old_arena.resident_signature
            before = v3.pickle_clone(hybrid.world.state_dict())
            rng = hybrid.world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            mutate(hybrid)
            _assert_raises(
                a49b.A4ResidentTranslationCommitError, hybrid.summary,
            )
            _assert_raises(
                a49b.A4ResidentTranslationCommitError,
                lambda: hybrid.step(0.1),
            )
            if (owner._lifecycle != a49b.COHERENT
                    or owner._arena is not old_arena
                    or a49a._epoch_values(owner._epochs)
                    != a49a._epoch_values(old_epochs)
                    or owner._arena.resident_signature != old_signature
                    or hybrid.scheduler.active):
                raise AssertionError(label + ' backend seal dirtied authority')
            _a49a_assert_world_unchanged(
                before, rng, rng_state, hybrid.world,
                'a49b.backend_seal.' + label,
            )
            runtime_seal_details.append(device + ':backend_' + label)

    # Exactly-once and exact-rank guards are inherited scheduler authority,
    # and neither a duplicate nor an early call can allocate a new arena.
    duplicate_world, duplicate_cells, capacity, dt = (
        _paid_translation_fixture(seed=18780)
    )
    duplicate_cell = duplicate_cells[0]
    duplicate = a49b.A4ResidentTranslationEventScheduler(capacity, 'cpu')
    _a49b_begin_translation_direct(
        duplicate, duplicate_world, duplicate_cell, dt,
    )
    duplicate.cpu_translation(
        duplicate_world, duplicate_cell, dt, duplicate_world.config,
    )
    duplicate_before = v3.pickle_clone(duplicate_world.state_dict())
    duplicate_owner = duplicate._a49b_owner
    duplicate_arena = duplicate_owner._arena
    duplicate_epochs = duplicate_owner.epochs
    _assert_raises(
        a3s.A3DuplicateEventError,
        lambda: duplicate.cpu_translation(
            duplicate_world, duplicate_cell, dt, duplicate_world.config,
        ),
    )
    v3.assert_recursive_close(
        duplicate_before, duplicate_world.state_dict(), 0.0, 0.0,
        'a49b.duplicate',
    )
    if (duplicate_owner._arena is not duplicate_arena
            or duplicate_owner.epochs != duplicate_epochs):
        raise AssertionError('duplicate translation changed fresh owner')
    _a48_abort_direct(duplicate, duplicate_cell)

    order_world, order_cells, capacity, dt = _paid_translation_fixture(
        seed=18781,
    )
    order_cell = order_cells[0]
    order = a49b.A4ResidentTranslationEventScheduler(capacity, 'cpu')
    order._bind_resident_world(order_world)
    order.begin_step(order_world, [order_cell])
    metabolism_rank = a3s.WORLD_EVENT_ORDER.index('cell_metabolism_loop')
    for event in a3s.WORLD_EVENT_ORDER[:metabolism_rank]:
        order.claim_world(event, status='skipped')
    order.claim(order_cell, 'surface_exchange', status='skipped')
    order.reserve_metabolism_dispatch(order_world, order_cell, dt)
    order.begin_a3_metabolism(order_world, order_cell, dt)
    order.claim(order_cell, 'gene_refresh', status='skipped')
    order_before = v3.pickle_clone(order_world.state_dict())
    _assert_raises(
        a3s.A3EventOrderError,
        lambda: order.cpu_translation(
            order_world, order_cell, dt, order_world.config,
        ),
    )
    v3.assert_recursive_close(
        order_before, order_world.state_dict(), 0.0, 0.0,
        'a49b.out_of_order',
    )
    if order._a49b_owner is not None:
        raise AssertionError('out-of-order translation constructed an owner')
    _a48_abort_direct(order, order_cell)

    return ('preclaim plan/version/candidate pointer/.data seals and old-trust '
            'INVALID isolation %s; stale/cross-owner/consumed lease rejection; '
            'postpublish and final-arm RNG/energy failure exact CPU identity/'
            'RNG/old-COHERENT rollback %s; selector/device and wrong %s fail closed; '
            'scheduler/backend creation seals prebiology %s; '
            'duplicate/out-of-order exactly-once' % (
                '/'.join(preclaim_details), '/'.join(rollback_details),
                '/'.join(wrong_input_details), '/'.join(runtime_seal_details),
            ))


def test_a49b_world_lockstep_persistence_a5_boundary_frozen90_authority():
    capacity = a4.GPU068A4Config()
    legacy_calls = []
    original_a48b = a48b.A4TranslationEventScheduler.cpu_translation
    original_a48a = a48.A4HydrolysisEventScheduler.cpu_translation

    def legacy_bomb(*args, **kwargs):
        legacy_calls.append(True)
        raise AssertionError('legacy A4.8b/CPU translation bridge was called')

    def run_lockstep(label, state, steps, device):
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(state),
        )
        hybrid = _a49b_hybrid_from_state(state, capacity, device)
        original_container = hybrid.world.cells
        original_members = tuple(hybrid.world.cells)
        receipts = []
        for step_index in range(int(steps)):
            cpu.step(0.1)
            receipt = hybrid.step(0.1)
            receipts.append(receipt)
            if (hybrid.world.cells is not original_container
                    or tuple(hybrid.world.cells) != original_members):
                raise AssertionError(
                    '%s step %d did not normalize same-membership list' % (
                        label, step_index,
                    )
                )
            if (hybrid.scheduler._a49b_owner is None
                    or hybrid.scheduler._a49b_owner.lifecycle
                    != a49b.COHERENT):
                raise AssertionError(label + ' lost coherent resident owner')
            previous_motion = None
            for record in receipt['cells']:
                names = [entry['event'] for entry in record['events']]
                by_name = {
                    entry['event']: entry for entry in record['events']
                }
                if (tuple(names) != tuple(a3s.CELL_EVENT_ORDER)
                        or names.count('translation_cpu') != 1
                        or names.count('replication_cpu') != 1
                        or not names.index('maintenance')
                        < names.index('translation_cpu')
                        < names.index('replication_cpu')
                        < names.index('genome_hydrolysis_cpu_rng')
                        < names.index('motion')
                        < names.index('actual_split_cpu')
                        < names.index('death_release_cpu')
                        or by_name['translation_cpu']['metadata'].get(
                            'authority')
                        != ('A4.9b-world-resident-selected-paid-'
                            'translation-atomic-CAS')
                        or by_name['translation_cpu']['metadata'].get(
                            'rng_draw_count') != 0):
                    raise AssertionError(label + ' c8/A5 event order differs')
                if ('two_cell_interleave' in label
                        and by_name['genome_hydrolysis_cpu_rng'][
                            'metadata'].get('eligible_genome_count', 0) < 1):
                    raise AssertionError(
                        label + ' did not execute eligible hydrolysis RNG work'
                    )
                translation = by_name['translation_cpu']['ordinal']
                motion = by_name['motion']['ordinal']
                if previous_motion is not None and previous_motion >= translation:
                    raise AssertionError(label + ' inter-cell order was batched')
                previous_motion = motion
            world_names = [
                entry['event'] for entry in receipt['world_events']
            ]
            if tuple(world_names) != tuple(a3s.WORLD_EVENT_ORDER):
                raise AssertionError(label + ' world HGT/washout order differs')
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL, label=label,
        )
        return hybrid, receipts

    lockstep_details = []
    a48b.A4TranslationEventScheduler.cpu_translation = legacy_bomb
    a48.A4HydrolysisEventScheduler.cpu_translation = legacy_bomb
    try:
        for device in _a49b_devices():
            for steps, seed in ((1, 18800), (10, 18801)):
                source = v3.make_world(seed=seed, cells=1)
                hybrid, _ = run_lockstep(
                    'a49b.%s.lockstep_%d' % (device, steps),
                    source.state_dict(), steps, device,
                )
                lockstep_details.append(
                    '%s:%d:g%d' % (
                        device, steps,
                        int(hybrid.scheduler._a49b_owner._storage_generation),
                    )
                )

            stressed = v3.make_world(seed=18802, cells=1)
            stressed.config.external_translator = True
            stressed.config.protein_repair = True
            stressed.config.quiescence = True
            stressed.config.quiescence_effector = True
            cell = stressed.cells[0]
            cell.current_stress = 1.5
            cell.damage_trace[:] = 0.9
            cell.membrane_oxidation[:] = np.linspace(
                0.3, 1.8, len(cell.membrane_oxidation),
            )
            cell.pools[a4.a3.POOL_REACTIVE] += 0.16
            cell.pools[a4.a3.POOL_AGGREGATE] += 0.07
            cell.pools[a4.a3.POOL_ATP] += 0.5
            cell.behavioural_quiescence = 0.6
            cell._sync_damage_pool()
            run_lockstep(
                'a49b.%s.translation_stress' % device,
                stressed.state_dict(), 2, device,
            )

            interleave = v3.make_world(seed=18803, cells=2)
            interleave.config.endogenous_damage = False
            for cell in interleave.cells:
                cell.genome_lesions = [
                    0.5 / 0.00065 for _ in cell.genomes
                ]
                cell._refresh_gene_cache()
            run_lockstep(
                'a49b.%s.two_cell_interleave' % device,
                interleave.state_dict(), 1, device,
            )
    finally:
        a48b.A4TranslationEventScheduler.cpu_translation = original_a48b
        a48.A4HydrolysisEventScheduler.cpu_translation = original_a48a
    if legacy_calls:
        raise AssertionError('A4.9b used a frozen translation bridge')

    # Dynamically cover the successful Hybrid.step normalization branch with
    # a real member change.  One living cell commits rank-5 translation while
    # its already-dead peer is released later in the same integrated step.
    # The wrapper must restore only the original list object, never its stale
    # membership.  The next rank-5 audit must then STOP as INVALID.
    member_source = v3.make_world(seed=18804, cells=2)
    member_source.cells[1].alive = False
    member_source.cells[1].death_reason = 'a49b_integrated_death'
    member_state = member_source.state_dict()
    member_cpu = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(member_state),
    )
    member_hybrid = _a49b_hybrid_from_state(
        member_state, capacity, 'cpu',
    )
    member_container = member_hybrid.world.cells
    member_objects = tuple(member_hybrid.world.cells)
    dead_id = int(member_objects[1].cell_id)
    member_cpu.step(0.1)
    member_receipt = member_hybrid.step(0.1)
    member_owner = member_hybrid.scheduler._a49b_owner
    if member_owner is None:
        raise AssertionError('integrated death did not commit rank-5 owner')
    member_arena = member_owner._arena
    member_signature = member_arena.resident_signature
    member_generation = int(member_owner._storage_generation)
    member_readback = v3.pickle_clone(
        a49a._attest_resident(
            member_arena, member_owner._arena_seal, readback=True,
        )
    )
    dead_records = [
        record for record in member_receipt['cells']
        if int(record['cell_id']) == dead_id
    ]
    dead_events = [] if len(dead_records) != 1 else [
        event for event in dead_records[0]['events']
        if event['event'] == 'death_release_cpu'
        and event['status'] == 'executed'
    ]
    if (member_receipt['status'] != 'complete'
            or member_hybrid.world.cells is not member_container
            or tuple(member_hybrid.world.cells) == member_objects
            or len(member_hybrid.world.cells) != 1
            or len(dead_events) != 1
            or member_owner.lifecycle != a49b.COHERENT):
        raise AssertionError(
            'successful Hybrid.step hid or misreceipted actual membership drift'
        )
    v3._assert_world_pair(
        member_cpu, member_hybrid,
        state_atol=v3.WORLD_FP64_ATOL,
        ledger_atol=v3.LEDGER_ATOL,
        label='a49b.integrated_member_normalization',
    )
    _assert_raises(
        a49b.A4ResidentTranslationCommitError,
        lambda: member_hybrid.step(0.1),
    )
    aborted_member_receipt = member_hybrid.scheduler.last_receipt
    member_after_abort = a49a._attest_resident(
        member_arena, member_owner._arena_seal, readback=True,
    )
    v3.assert_recursive_close(
        member_readback, member_after_abort, 0.0, 0.0,
        'a49b.integrated_member_invalid_resident50',
    )
    if (aborted_member_receipt is None
            or aborted_member_receipt['status'] != 'aborted'
            or member_owner.lifecycle != a49b.INVALID
            or member_owner._arena is not member_arena
            or member_owner._arena.resident_signature != member_signature
            or int(member_owner._storage_generation) != member_generation):
        raise AssertionError(
            'next rank-5 audit auto-rebuilt actual membership drift'
        )

    class SerializationCapture(a49b.A4ResidentTranslationEventScheduler):
        prepared_seen = None
        finalized_seen = None

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            return prepared

        def _resident_translation_finalized_ready(
                self, world, cell, prepared, finalized):
            self.finalized_seen = finalized
            return finalized

    serial_world, serial_cells, serial_capacity, serial_dt = (
        _paid_translation_fixture(seed=18810)
    )
    serial_cell = serial_cells[0]
    serial_scheduler = SerializationCapture(serial_capacity, 'cpu')
    _a49b_begin_translation_direct(
        serial_scheduler, serial_world, serial_cell, serial_dt,
    )
    try:
        serial_scheduler.cpu_translation(
            serial_world, serial_cell, serial_dt, serial_world.config,
        )
        for artifact in (
                serial_scheduler.prepared_seen,
                serial_scheduler.finalized_seen,
                serial_scheduler.prepared_seen.selected_plan,
                serial_scheduler._a49b_owner):
            _assert_raises(
                (a49a.A4ResidentArenaScopeError,
                 a49b.A4ResidentTranslationCommitError),
                lambda artifact=artifact: pickle.dumps(artifact),
            )
    finally:
        _a48_abort_direct(serial_scheduler, serial_cell)

    persistence_details = []
    for device in _a49b_devices():
        persisted_source = v3.make_world(seed=18820, cells=1)
        persisted = _a49b_hybrid_from_state(
            persisted_source.state_dict(), capacity, device,
        )
        persisted.step(0.1)
        durable = v3.pickle_clone(persisted.world.state_dict())
        durable_payload = persisted.state_dict()
        v3.assert_recursive_close(
            durable, durable_payload['cpu_world'], 0.0, 0.0,
            'a49b.%s.durable_cpu_payload' % device,
        )
        canonical_durable = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(durable),
        ).state_dict()
        # Frozen Formal066.from_state canonicalizes this fixture's pool[4] by
        # exactly one half-epsilon.  Characterize that inherited behavior;
        # never weaken the raw durable payload or clone/load comparisons.
        raw_pools = np.asarray(
            durable['cells'][0]['pools'], dtype=np.float64,
        )
        canonical_pools = np.asarray(
            canonical_durable['cells'][0]['pools'], dtype=np.float64,
        )
        differing_pool_indices = tuple(
            int(index) for index in np.flatnonzero(
                raw_pools != canonical_pools
            )
        )
        expected_ulp = float(2.0 ** -53)
        inherited_ulp = float(abs(raw_pools[4] - canonical_pools[4]))
        if (raw_pools.shape != canonical_pools.shape
                or differing_pool_indices != (4,)
                or inherited_ulp != expected_ulp
                or durable['rng_state'] != canonical_durable['rng_state']):
            raise AssertionError(
                'frozen Formal066 from_state canonicalization changed'
            )
        canonical_proof = v3.pickle_clone(durable)
        canonical_proof['cells'][0]['pools'][4] = canonical_pools[4]
        v3.assert_recursive_close(
            canonical_proof, canonical_durable, 0.0, 0.0,
            'a49b.%s.inherited_from_state_1ulp' % device,
        )
        inherited_c8 = _a48c8_hybrid_from_state(
            durable, capacity, device,
        )
        v3.assert_recursive_close(
            canonical_durable, inherited_c8.world.state_dict(), 0.0, 0.0,
            'a49b.%s.inherited_c8_canonical' % device,
        )

        integration_payloads = (
            durable_payload.get('a4_resident_translation'),
            durable_payload.get('scheduler', {}).get(
                'a4_resident_translation'
            ),
        )
        if any(not isinstance(value, Mapping)
               or set(value) != {'schema', 'config', 'device'}
               for value in integration_payloads):
            raise AssertionError(
                'durable resident-translation payload exposed runtime state'
            )

        forbidden_runtime_keys = frozenset((
            'owner', 'owner_token', 'arena', 'arena_id', 'arena_ref',
            'epoch', 'epochs', 'pointer', 'pointers', 'seal', 'guard',
            'lease', 'selected_plan', 'prepared', 'prepared_storage',
            'finalized', 'resident_signature', 'storage_generation',
            'candidate_binding', 'candidate_signature', 'old_arena_ref',
        ))

        def runtime_keys(value, path='state'):
            found = []
            if isinstance(value, Mapping):
                for key, item in value.items():
                    key_text = str(key).lower()
                    child = path + '.' + str(key)
                    if (key_text in forbidden_runtime_keys
                            or key_text.startswith('_a49b_')):
                        found.append(child)
                    found.extend(runtime_keys(item, child))
            elif isinstance(value, (list, tuple)):
                for index, item in enumerate(value):
                    found.extend(runtime_keys(
                        item, '%s[%d]' % (path, index),
                    ))
            return found

        non_cpu_payload = {
            key: value for key, value in durable_payload.items()
            if key != 'cpu_world'
        }
        leaked = runtime_keys(non_cpu_payload)
        if leaked:
            raise AssertionError(
                'durable payload serialized resident runtime: %r' % leaked
            )
        twin = persisted.clone()
        with tempfile.TemporaryDirectory() as directory:
            path = os.path.join(directory, 'a49b.pkl')
            persisted.save(path)
            restored = a49b.Hybrid066WorldA4ResidentTranslation.load(path)
        for value in (twin, restored):
            if (type(value) is not a49b.Hybrid066WorldA4ResidentTranslation
                    or type(value.scheduler)
                    is not a49b.A4ResidentTranslationEventScheduler
                    or value.scheduler._a49b_owner is not None
                    or value.scheduler._a49b_world is not value.world
                    or value.scheduler._a49b_cells is not value.world.cells
                    or value.scheduler.a4_device != device
                    or value.scheduler.a4_config != capacity):
                raise AssertionError(device + ' save/clone authority differs')
            v3.assert_recursive_close(
                canonical_durable, value.world.state_dict(), 0.0, 0.0,
                'a49b.%s.persisted_cpu' % device,
            )

        continuations = (persisted, twin, restored)
        for value in continuations:
            value.step(0.1)
        for value in continuations[1:]:
            v3.assert_recursive_close(
                continuations[0].world.state_dict(),
                value.world.state_dict(), 0.0, 0.0,
                'a49b.%s.persisted_continuation' % device,
            )
        owners = [value.scheduler._a49b_owner for value in continuations]
        if len({id(owner._owner_token) for owner in owners}) != 3:
            raise AssertionError(device + ' save/clone reused owner token')
        arena_ids = {owner.arena_id for owner in owners}
        if len(arena_ids) != 3:
            raise AssertionError(device + ' save/clone reused arena ID')
        storage_sets = [
            _a49a_signature_sets(owner._arena.resident_signature)[1]
            for owner in owners
        ]
        if any(storage_sets[left].intersection(storage_sets[right])
               for left in range(3) for right in range(left + 1, 3)):
            raise AssertionError(device + ' save/clone arenas alias storage')
        persistence_details.append(
            device + ':fresh3/inherited-from_state-1ULP'
        )

        pending = _a49b_hybrid_from_state(
            v3.make_world(seed=18821, cells=1).state_dict(),
            capacity, device,
        )
        pending.scheduler.begin_step(pending.world, pending.world.cells)
        _assert_raises(a3s.A3SchedulerProtocolError, pending.state_dict)
        _assert_raises(a3s.A3SchedulerProtocolError, pending.clone)
        pending.scheduler.abort_step(
            RuntimeError('expected A4.9b active-save rejection'),
        )
        pending.scheduler._resident_owner_for_rank5(pending.world)
        lease = pending.scheduler._a49b_owner._open_translation_lease(
            pending.world,
        )
        _assert_raises(a3s.A3SchedulerProtocolError, pending.state_dict)
        _assert_raises(a3s.A3SchedulerProtocolError, pending.clone)
        _assert_raises(
            a49b.A4ResidentTranslationCommitError,
            lambda: pickle.dumps(lease),
        )
        pending.scheduler._a49b_owner._abort_preclaim(pending.world)

    # A5 stays CPU-authoritative.  Exercise each topology/genome boundary
    # directly after a paid translation and prove that it never flushes or
    # edits the immutable resident tensors.  The next audit must instead make
    # the old owner unusable, and rank-5 must not silently rebuild it.
    def assert_a5_boundary(
            label, source, mutate, changed,
            expected_lifecycle=a49b.INVALID):
        hybrid = _a49b_hybrid_from_state(
            source.state_dict(), capacity, 'cpu',
        )
        hybrid.step(0.1)
        owner = hybrid.scheduler._a49b_owner
        old_arena = owner._arena
        old_signature = old_arena.resident_signature
        old_readback = v3.pickle_clone(
            a49a._attest_resident(
                old_arena, owner._arena_seal, readback=True,
            )
        )
        # This is the explicit, public A3 hand-off to the frozen CPU world,
        # not an error fallback.  It restores the raw split/death/washout APIs
        # while deliberately retaining the old resident owner for the audit.
        detached = a3s.detach_backend_from_world_a3(hybrid.world)
        if detached is not hybrid.world:
            raise AssertionError(label + ' raw CPU detach changed the world')
        cpu = a4.a3.s66.Formal066World.from_state(
            v3.pickle_clone(hybrid.world.state_dict()),
        )
        before = v3.pickle_clone(hybrid.world.state_dict())
        cpu_result = mutate(cpu)
        hybrid_result = mutate(hybrid.world)
        if cpu_result != hybrid_result or not changed(before, hybrid.world):
            raise AssertionError(label + ' did not reach its A5 boundary')
        # The raw attached world and its frozen Formal066.from_state twin can
        # differ by the inherited one-ULP canonicalization pinned above.  Use
        # the frozen world lockstep bound while keeping resident tensors exact.
        v3._assert_world_pair(
            cpu, hybrid, state_atol=v3.WORLD_FP64_ATOL,
            ledger_atol=v3.LEDGER_ATOL, label='a49b.a5.' + label,
        )
        after_readback = a49a._attest_resident(
            old_arena, owner._arena_seal, readback=True,
        )
        v3.assert_recursive_close(
            old_readback, after_readback, 0.0, 0.0,
            'a49b.a5.%s.old_resident50' % label,
        )
        old_generation = int(owner._storage_generation)
        if (owner._arena is not old_arena
                or old_arena.resident_signature != old_signature
                or owner.audit_cpu(hybrid.world) != expected_lifecycle
                or owner._arena is not old_arena
                or old_arena.resident_signature != old_signature):
            raise AssertionError(
                label + ' did not invalidate the unchanged old arena'
            )
        after_audit = a49a._attest_resident(
            old_arena, owner._arena_seal, readback=True,
        )
        v3.assert_recursive_close(
            old_readback, after_audit, 0.0, 0.0,
            'a49b.a5.%s.invalid_resident50' % label,
        )
        if expected_lifecycle == a49b.INVALID:
            _assert_raises(
                a49b.A4ResidentTranslationCommitError,
                lambda: owner._cohere_rank5(hybrid.world),
            )
            return label + ':INVALID/no-flush'
        old_objects, old_storage = _a49a_signature_sets(old_signature)
        rebuilt_owner = owner._cohere_rank5(hybrid.world)
        rebuilt = owner._arena
        new_objects, new_storage = _a49a_signature_sets(
            rebuilt.resident_signature,
        )
        if (expected_lifecycle != a49b.CPU_NEWER
                or rebuilt_owner is not owner
                or rebuilt is old_arena
                or owner._arena is not rebuilt
                or owner.lifecycle != a49b.COHERENT
                or int(owner._storage_generation) != old_generation + 1
                or old_arena.retired is not True
                or old_objects.intersection(new_objects)
                or old_storage.intersection(new_storage)):
            raise AssertionError(
                label + ' explicit rank-5 rebuild did not publish fresh arena'
            )
        old_after_rebuild = a49a._resident_readback(old_arena.binding)[:3]
        v3.assert_recursive_close(
            old_readback, old_after_rebuild, 0.0, 0.0,
            'a49b.a5.%s.retired_resident50' % label,
        )
        return label + ':CPU_NEWER/explicit-fresh-rebuild'

    def force_split(world):
        parent = world.cells[0]
        if len(parent.genomes) < 2:
            parent.genomes.append(parent.genomes[0].copy())
            parent.genome_lesions.append(0.0)
            parent._refresh_gene_cache()
        parent.division_progress = 1.0
        before_count = len(world.cells)
        world._handle_divisions_and_deaths()
        return len(world.cells) - before_count

    def force_death(world):
        victim = world.cells[0]
        victim.alive = False
        victim.death_reason = 'a49b_validation_death'
        before_count = len(world.cells)
        world._handle_divisions_and_deaths()
        return before_count - len(world.cells)

    def force_washout(world):
        world.age = float(world.eco66_next_washout)
        before_count = len(world.cells)
        world._washout()
        return before_count - len(world.cells)

    def force_hgt(world):
        recipient = world.cells[0]
        sequence = recipient.genomes[0][:a4.g2.GENE_SPAN].copy()
        fragment = world.edna.add_fragment(
            sequence, recipient.pos, origin_lineage=987,
            origin_cell=654, origin_hash='a49b-hgt', mobile=True,
        )
        world.edna.fragments.remove(fragment)
        world.edna.uptaken_fragments += 1
        world.hgt_attempts += 1
        recipient.hgt_attempts += 1
        integrated = recipient.integrate_fragment(
            fragment, world, force=True,
        )
        if integrated:
            world.edna.integrated_fragments += 1
            world.hgt_integrations += 1
        return bool(integrated)

    def force_reorder(world):
        before = tuple(int(cell.cell_id) for cell in world.cells)
        world.cells = list(reversed(world.cells))
        return before != tuple(int(cell.cell_id) for cell in world.cells)

    def force_alive_drift(world):
        world.cells[0].alive = False
        world.cells[0].death_reason = 'a49b_validation_alive_drift'
        return not world.cells[0].alive

    membership_changed = lambda before, world: (
        len(before['cells']) != len(world.cells)
    )
    genome_changed = lambda before, world: (
        len(before['cells'][0]['genomes'][0])
        != len(world.cells[0].genomes[0])
    )
    order_changed = lambda before, world: (
        tuple(int(item['cell_id']) for item in before['cells'])
        != tuple(int(cell.cell_id) for cell in world.cells)
    )
    alive_changed = lambda before, world: (
        bool(before['cells'][0]['alive']) != bool(world.cells[0].alive)
    )

    washout_source = v3.make_world(seed=18842, cells=4)
    washout_source.config.eco66_washout = True
    a5_details = (
        assert_a5_boundary(
            'split', v3.make_world(seed=18840, cells=2),
            force_split, membership_changed,
        ),
        assert_a5_boundary(
            'death', v3.make_world(seed=18841, cells=2),
            force_death, membership_changed,
        ),
        assert_a5_boundary(
            'washout', washout_source,
            force_washout, membership_changed,
        ),
        assert_a5_boundary(
            'hgt', v3.make_world(seed=18843, cells=2),
            force_hgt, genome_changed, a49b.CPU_NEWER,
        ),
        assert_a5_boundary(
            'reorder', v3.make_world(seed=18844, cells=2),
            force_reorder, order_changed,
        ),
        assert_a5_boundary(
            'alive_drift', v3.make_world(seed=18845, cells=2),
            force_alive_drift, alive_changed,
        ),
    )

    prior_names = '\n'.join(
        fn.__name__ for fn in A49A_VALIDATION_TESTS
    )
    new_names = tuple(fn.__name__ for fn in A49B_TESTS)
    expected_hashes = dict(_A49A_FROZEN_CORE_SHA256)
    expected_hashes[
        'src/0_6_8/SOMA_CELL_0_6_8_gpu_a4_resident_arena.py'
    ] = _A49A_CORE_SHA256
    for relative, expected_hash in expected_hashes.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')
    core_path = os.path.join(
        ROOT, 'src', '0_6_8',
        'SOMA_CELL_0_6_8_gpu_a4_resident_translation_integration.py',
    )
    prereg_path = os.path.join(
        ROOT, 'planning', 'SOMA_CELL_0_6_8_GPU_A4_PREREGISTRATION_JA.md',
    )
    scheduler_signature = inspect.signature(
        a49b.A4ResidentTranslationEventScheduler.cpu_translation,
    )
    if (tuple(a49b.__all__) != _A49B_PUBLIC_API
            or tuple(scheduler_signature.parameters)
            != ('self', 'world', 'cell', 'dt', 'config')
            or not issubclass(
                a49b.A4ResidentTranslationEventScheduler,
                a48c8.A4ReplicationEarlyNoopEventScheduler,
            )
            or not issubclass(
                a49b.Hybrid066WorldA4ResidentTranslation,
                a48c8.Hybrid066WorldA4ReplicationEarlyNoop,
            )
            or a49b.FULL_GPU_WORLD_STEP is not False
            or a49b.PORT_STATUS.get('full_gpu_world_step') is not False
            or 'RESIDENT_NEWER' in tuple(a49b.__all__)
            or len(SOURCE_PATHS) != 29
            or len(A49A_VALIDATION_TESTS) != 90
            or len(A49B_TESTS) != 4
            or len(A49B_VALIDATION_TESTS) != 94
            or new_names != (
                'test_a49b_selected_plan_formal066_cpu_cuda_isolation',
                'test_a49b_rank5_fresh_generation_epochs_nonalias_and_capacity',
                'test_a49b_fail_closed_rollback_cas_leases_duplicate_and_order',
                'test_a49b_world_lockstep_persistence_a5_boundary_frozen90_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A49B_PRIOR_90_NAMES_SHA256
            or _a48c7_prior_test_ast_sha256(A49A_VALIDATION_TESTS)
            != _A49B_PRIOR_90_AST_SHA256
            or _sha256(prereg_path) != _A49B_PREREG_SHA256
            or _sha256(core_path) != _A49B_CORE_SHA256):
        raise AssertionError(
            'A4.9b API/prereg/core/frozen90/total94 authority differs'
        )
    return ('1/10-step + 2-step translation stress + 2-cell interleave '
            'CPU/c8 event/receipt/PCG64/material lockstep with no legacy '
            'translation %s; save/load/clone fresh nonalias owners %s; '
            'active/pending/nonserialization reject; committed-CPU A5 '
            'split/death/washout/reorder/alive M-domain drift makes unchanged '
            'old 50-tensor arenas INVALID without auto-rebuild, while HGT '
            'makes CPU_NEWER then explicitly rebuilds a fresh arena %s; '
            'frozen90 names/AST + A3/pure/c1-c8/A4.9a hashes + total94/'
            'full_gpu=false' % (
                '/'.join(lockstep_details),
                '/'.join(persistence_details),
                '/'.join(a5_details),
            ))


_A49C_CORE_SHA256 = (
    'b983a19c0b204fd643c2f98ec5078375665db1dc8ae8d555ea05ae764942eb86'
)

_A49C_PREREG_SHA256 = (
    'df9927d1db7b15015d57d0402f0f7941e9d0eddda7075bbddd7ddbcec901d9c7'
)

_A49C_PREREG_PREFIX_BYTES = 143281
_A49C_PREREG_PREFIX_SHA256 = (
    'd1dd7f0ee7ba56508cbc1eccd7ffcb3487d5242d0d3f16839e0031e9dc231334'
)

_A49C_PRIOR_94_NAMES_SHA256 = (
    '9f1e611f7cc918ba77c20bc38d83df5129007b3629dcbe279737d57590dd1347'
)

_A49C_PRIOR_94_AST_SHA256 = (
    '2a6095e7c360ea7cb1e12a9120cc9b17ec96f9e09462880056abd563ef728c44'
)

_A49C_PUBLIC_API = (
    'BUILD', 'BUILD_ID', 'BUILD_LONG', 'SCHEMA_VERSION',
    'SUPPLEMENT_SCHEMA_VERSION', 'SELECTED_PLAN_SCHEMA_VERSION',
    'SAVE_VERSION', 'FULL_GPU_WORLD_STEP', 'MAINTENANCE_ORACLE_ATOL',
    'PORT_STATUS', 'COHERENT', 'CPU_NEWER', 'INVALID',
    'A4ResidentMaintenanceFoundationError',
    'A4MaintenanceSupplementBatch', 'A4MaintenanceBinding',
    'A4SelectedMaintenancePlan', 'pack_a4_maintenance_supplement',
    'bind_a4_maintenance', 'paid_maintenance_selected_numpy',
    'paid_maintenance_selected_torch', 'paid_maintenance_selected',
    'A4ResidentMaintenanceFoundationScheduler',
    'Hybrid066WorldA4ResidentMaintenanceFoundation',
)


def _a49c_devices():
    return tuple(_a49a_devices())


def _a49c_fixture(seed=18900):
    world, cells, capacity, _ = _paid_translation_fixture(seed=seed)
    width = int(a49c.a3.MEMBRANE_SEGMENTS * a49c.a3.CHANNEL_COUNT)
    for index, cell in enumerate(cells):
        cell.transporters = (
            np.arange(width, dtype=np.float64).reshape(
                a49c.a3.MEMBRANE_SEGMENTS, a49c.a3.CHANNEL_COUNT,
            ) * np.float64((index + 1) * 1.0e-5)
        )
        if hasattr(cell, 'maintenance_shortfall'):
            delattr(cell, 'maintenance_shortfall')
    cells[1].maintenance_shortfall = 0.0
    cells[3].maintenance_shortfall = 0.321
    cells[0].pools[a49c.a3.POOL_ATP] = 5.0
    cells[1].pools[a49c.a3.POOL_ATP] = 1.0e-9
    cells[2].pools[a49c.a3.POOL_ATP] = 0.0
    cells[3].pools[a49c.a3.POOL_ATP] = 0.4
    template = np.asarray(cells[2].genomes[0], dtype=np.uint8).copy()
    cells[2].replication_template = template.copy()
    cells[2].replication_copy = [int(value) for value in template[:11]]
    cells[2].replication_template_lesion = 0.25
    cells[2].replication_fractional = 0.5
    cells[2]._refresh_gene_cache()
    return world, cells, capacity


def _a49c_hybrid_from_state(state, capacity, device='cpu'):
    world = a4.a3.s66.Formal066World.from_state(v3.pickle_clone(state))
    backend = a4.a3.TorchKernelBackendA3(
        v3._a3_config(device=device, precision='float64'),
    )
    return a49c.Hybrid066WorldA4ResidentMaintenanceFoundation(
        world, backend=backend, a4_config=capacity, a4_device=device,
    )


def _a49c_host_array(value):
    if a4._is_tensor(value):
        return value.detach().cpu().numpy()
    return np.asarray(value)


def _a49c_exact_array(left, right):
    left = _a49c_host_array(left)
    right = _a49c_host_array(right)
    return (left.dtype == right.dtype and left.shape == right.shape
            and left.tobytes() == right.tobytes())


def _a49c_binding_snapshot(binding):
    return tuple(
        _a49c_host_array(value).copy()
        for value in a49c._binding_arrays(binding)
    )


def _a49c_assert_binding_snapshot(snapshot, binding, label):
    arrays = a49c._binding_arrays(binding)
    if len(snapshot) != 52 or len(arrays) != 52:
        raise AssertionError(label + ' composite array count differs')
    for index, (before, after) in enumerate(zip(snapshot, arrays)):
        if not _a49c_exact_array(before, after):
            raise AssertionError(
                '%s composite array[%d] changed' % (label, index)
            )


def _a49c_assert_binding52(binding, label):
    if binding.validate() is not binding:
        raise AssertionError(label + ' binding validate identity differs')
    arrays = tuple(a49c._binding_arrays(binding))
    if len(arrays) != 52 or len({id(value) for value in arrays}) != 52:
        raise AssertionError(label + ' binding is not exactly 52 objects')
    storage = tuple(a49c._array_storage_descriptor(value) for value in arrays)
    if a4._is_tensor(arrays[0]):
        pointers = binding.data_ptrs()
        if (len(pointers) != 52 or len(set(pointers.values())) != 52
                or any(int(value._version) != 0 for value in arrays)):
            raise AssertionError(
                label + ' binding pointers/version-zero differ'
            )
    else:
        for index, left in enumerate(arrays):
            if any(np.shares_memory(np.asarray(left), np.asarray(right))
                   for right in arrays[index + 1:]):
                raise AssertionError(label + ' NumPy binding aliases storage')
    return {tuple(value) for value in storage}


def _a49c_assert_plan33(plan, source, label):
    if plan.validate() is not plan:
        raise AssertionError(label + ' plan validate identity differs')
    outputs = tuple(a49c._plan_arrays(plan))
    if len(outputs) != 33 or len({id(value) for value in outputs}) != 33:
        raise AssertionError(label + ' plan is not exactly 33 objects')
    source_arrays = tuple(a49c._binding_arrays(source))
    if a4._is_tensor(outputs[0]):
        pointers = plan.data_ptrs()
        source_pointers = source.data_ptrs()
        if (len(pointers) != 33 or len(set(pointers.values())) != 33
                or set(pointers.values()).intersection(
                    source_pointers.values())
                or any(int(value._version) != 0 for value in outputs)):
            raise AssertionError(label + ' Torch plan freshness differs')
    else:
        for index, left in enumerate(outputs):
            if (any(np.shares_memory(np.asarray(left), np.asarray(right))
                    for right in outputs[index + 1:])
                    or any(np.shares_memory(
                        np.asarray(left), np.asarray(right),
                    ) for right in source_arrays)):
                raise AssertionError(label + ' NumPy plan aliases storage')
    return outputs


def _a49c_expect_error(fn, kinds=None):
    kinds = kinds or (
        a49c.A4ResidentMaintenanceFoundationError,
        a49b.A4ResidentTranslationCommitError,
        a49a.A4ResidentArenaError,
        a49a.A4ResidentArenaScopeError,
        a4.A4CapacityError,
        TypeError, ValueError,
    )
    try:
        fn()
    except kinds as exc:
        return exc
    raise AssertionError('expected A4.9c fail-closed exception')


def _a49c_cpu_identities(world):
    return tuple(
        (id(cell), id(cell.pools), id(cell.transporters),
         hasattr(cell, 'maintenance_shortfall'),
         id(getattr(cell, 'maintenance_shortfall', None)))
        for cell in world.cells
    )


def _a49c_owner(world, capacity, device='cpu'):
    return a49c._A4ResidentMaintenanceFoundationOwner.from_cpu(
        world, capacity, device,
    )


def test_a49c_selected_maintenance_plan_formal066_cpu_cuda_isolation():
    world, cells, capacity = _a49c_fixture(seed=18900)
    before = v3.pickle_clone(world.state_dict())
    identities = _a49c_cpu_identities(world)
    rng = world.rng
    rng_state = v3.pickle_clone(rng.bit_generator.state)
    supplement = a49c.pack_a4_maintenance_supplement(world, capacity)
    binding = a49c.bind_a4_maintenance(supplement)
    _a49c_assert_binding52(binding, 'a49c.public.host52')
    source_snapshot = _a49c_binding_snapshot(binding)
    source_presence = tuple(getattr(supplement, '_presence'))
    if (source_presence != (False, True, False, True)
            or supplement.transporters.shape != (
                int(capacity.max_cells), int(a49c.a3.MEMBRANE_SEGMENTS),
                int(a49c.a3.CHANNEL_COUNT),
            )
            or supplement.maintenance_shortfall.shape
            != (int(capacity.max_cells),)):
        raise AssertionError('A4.9c heterogeneous supplement shape/presence differs')

    cases = (
        ('rich_first', 0, 0.137),
        ('poor_middle', 1, 0.137),
        ('zero_partial_middle', 2, 0.137),
        ('prior_shortfall_last', 3, 0.137),
        ('zero_dt', 2, 0.0),
    )
    details = []
    for label, index, dt in cases:
        target = cells[index]
        oracle = a4.a3.maintenance_numpy(
            a4.a3.pack_a3_cell(
                copy.deepcopy(target), model_config=world.config,
            ),
            dt,
        )
        numpy_plan = a49c.paid_maintenance_selected_numpy(
            binding, dt, index, int(target.cell_id),
        )
        _a49c_assert_plan33(
            numpy_plan, binding, 'a49c.numpy.' + label,
        )
        if (numpy_plan.target_index != index
                or numpy_plan.target_cell_id != int(target.cell_id)
                or numpy_plan.dt_hex != float(dt).hex()
                or int(np.count_nonzero(numpy_plan.target_mask)) != 1
                or not bool(numpy_plan.target_mask[index])
                or tuple(getattr(
                    numpy_plan.supplement_after, '_presence', (),
                )) != source_presence
                or hasattr(target, 'maintenance_shortfall')
                != source_presence[index]):
            raise AssertionError(label + ' selector/presence differs')
        a49c._require_selected_scope(
            binding, numpy_plan.state_after,
            numpy_plan.supplement_after, index,
        )
        np_atp = float(
            numpy_plan.state_after.pools[index, a49c.a3.POOL_ATP]
        )
        np_shortfall = float(
            numpy_plan.supplement_after.maintenance_shortfall[index]
        )
        if (abs(np_atp - float(oracle.pools[a49c.a3.POOL_ATP]))
                > a49c.MAINTENANCE_ORACLE_ATOL
                or abs(np_shortfall - float(oracle.maintenance_shortfall))
                > a49c.MAINTENANCE_ORACLE_ATOL):
            raise AssertionError(label + ' NumPy Formal066 oracle differs')

        for device in _a49c_devices():
            resident = binding.to_torch(device)
            source_storage = _a49c_assert_binding52(
                resident, 'a49c.%s.%s.source52' % (device, label),
            )
            torch_plan = a49c.paid_maintenance_selected(
                resident, dt, index, int(target.cell_id),
            )
            _a49c_assert_plan33(
                torch_plan, resident,
                'a49c.%s.%s.plan33' % (device, label),
            )
            host_plan = torch_plan.to_numpy()
            _a49c_assert_plan33(
                host_plan, binding,
                'a49c.%s.%s.host33' % (device, label),
            )
            if (abs(float(host_plan.state_after.pools[
                        index, a49c.a3.POOL_ATP])
                    - float(oracle.pools[a49c.a3.POOL_ATP]))
                    > a49c.MAINTENANCE_ORACLE_ATOL
                    or abs(float(
                        host_plan.supplement_after.maintenance_shortfall[index]
                    ) - float(oracle.maintenance_shortfall))
                    > a49c.MAINTENANCE_ORACLE_ATOL):
                raise AssertionError(
                    '%s/%s Torch Formal066 oracle differs' % (label, device)
                )
            for name in a4._TRANSLATION_ARRAY_FIELDS:
                if not _a49c_exact_array(
                        getattr(host_plan.state_after, name),
                        getattr(numpy_plan.state_after, name)):
                    raise AssertionError(
                        '%s/%s state field differs: %s' % (
                            label, device, name,
                        )
                    )
            if (not _a49c_exact_array(
                    host_plan.supplement_after.transporters,
                    numpy_plan.supplement_after.transporters)
                    or not _a49c_exact_array(
                        host_plan.supplement_after.maintenance_shortfall,
                        numpy_plan.supplement_after.maintenance_shortfall)):
                raise AssertionError(label + '/' + device + ' sidecar differs')
            if len(source_storage) != 52:
                raise AssertionError(label + '/' + device + ' source52 differs')
            details.append('%s:%s' % (device, label))

        _a49c_assert_binding_snapshot(
            source_snapshot, binding, 'a49c.source.' + label,
        )
        _a49a_assert_world_unchanged(
            before, rng, rng_state, world, 'a49c.pure.' + label,
        )
        if identities != _a49c_cpu_identities(world):
            raise AssertionError(label + ' changed CPU field identities')

    implicit = a49c.pack_a4_maintenance_supplement(world)
    explicit_default = a49c.pack_a4_maintenance_supplement(
        world, a4.GPU068A4Config(),
    )
    if (implicit.source_provenance == explicit_default.source_provenance
            or a49c.bind_a4_maintenance(implicit).validate() is None
            or a49c.bind_a4_maintenance(explicit_default).validate() is None):
        raise AssertionError('implicit/explicit config provenance differs')

    foreign = a4.a3.s66.Formal066World.from_state(
        v3.pickle_clone(world.state_dict()),
    )
    foreign_supplement = a49c.pack_a4_maintenance_supplement(
        foreign, capacity,
    )
    if foreign_supplement.source_provenance == supplement.source_provenance:
        raise AssertionError('equal-valued foreign world reused provenance')
    _a49c_expect_error(lambda: copy.copy(foreign_supplement))
    _a49c_expect_error(lambda: pickle.loads(pickle.dumps(foreign_supplement)))
    _a49c_expect_error(foreign_supplement.to_numpy)
    _a49c_expect_error(
        lambda: foreign_supplement.to_torch('cpu'),
    )
    alternate = a49c.pack_a4_maintenance_supplement(world, capacity)
    object.__setattr__(
        alternate, '_base_binding_ref',
        getattr(foreign_supplement, '_base_binding_ref'),
    )
    _a49c_expect_error(lambda: a49c.bind_a4_maintenance(alternate))

    return ('heterogeneous first/middle/last rich/poor/zero/prior/dt0; '
            'complete+partial-copy tension and complete-only DNA direct A3 '
            'oracle exact; NumPy/%s joint52 + fresh33 version-zero nonalias; '
            'presence/source/CPU/RNG immutable; implicit-explicit/equal-world/'
            'copy/alternate-base fail closed: %s' % (
                '/'.join(_a49c_devices()), '/'.join(details),
            ))


def test_a49c_composite52_shadow_pointers_coherence_rebuild_and_capacity():
    tail_world, _, tail_capacity = _a49a_fixture(seed=19000)
    tail_world.cells = list(tail_world.cells[:2])
    tail_supplement = a49c.pack_a4_maintenance_supplement(
        tail_world, tail_capacity,
    )
    tail_binding = a49c.bind_a4_maintenance(tail_supplement)
    _a49c_assert_binding52(tail_binding, 'a49c.tail.host52')
    if (tail_supplement.cell_count != 2
            or tail_supplement.cell_capacity != 4
            or np.any(tail_supplement.transporters[2:].view(np.uint64) != 0)
            or np.any(
                tail_supplement.maintenance_shortfall[2:].view(np.uint64)
                != 0)):
        raise AssertionError('A4.9c two-array unused tail is not bitwise zero')

    world, cells, capacity = _a49a_fixture(seed=19001)
    owner = _a49c_owner(world, capacity, 'cpu')
    initial_binding = owner._maintenance_binding
    initial_storage = _a49c_assert_binding52(
        initial_binding, 'a49c.owner.initial52',
    )
    initial_snapshot = _a49c_binding_snapshot(initial_binding)
    expected = a49c.bind_a4_maintenance(
        a49c.pack_a4_maintenance_supplement(world, capacity)
    ).to_torch('cpu')
    for index, (left, right) in enumerate(zip(
            a49c._binding_arrays(initial_binding),
            a49c._binding_arrays(expected))):
        if not _a49c_exact_array(left, right):
            raise AssertionError(
                'initial resident52 readback[%d] differs' % index
            )
    if (owner.lifecycle != a49c.COHERENT
            or owner.epochs
            != a49a.A4ResidentArenaEpochs(1, 1, 1, 1, 1)
            or int(owner._storage_generation) != 1):
        raise AssertionError('initial composite lifecycle/epochs differ')

    old_binding = owner._maintenance_binding
    old_snapshot = _a49c_binding_snapshot(old_binding)
    old_generation = int(owner._storage_generation)
    old_epochs = owner.epochs
    cells[0].transporters[0, 0] = np.nextafter(
        float(cells[0].transporters[0, 0]), np.inf,
    )
    if owner.audit_cpu(world) != a49c.CPU_NEWER:
        raise AssertionError('sidecar-only drift did not become CPU_NEWER')
    dirty_epochs = owner.epochs
    if (dirty_epochs != a49a._advance_epochs(
            old_epochs, frozenset(('S',)), cache_built=False)):
        raise AssertionError('sidecar-only drift did not advance S once')
    owner.audit_cpu(world)
    if owner.epochs != dirty_epochs:
        raise AssertionError('repeated sidecar audit double-counted S')
    expected_id = owner.arena_id
    expected_lifecycle = owner.lifecycle
    candidate = owner.prepare_rebuild(world)
    candidate_storage = _a49c_assert_binding52(
        candidate.new_binding_ref, 'a49c.owner.rebuild52',
    )
    if initial_storage.intersection(candidate_storage):
        raise AssertionError('rebuild candidate52 aliases old generation')
    owner.swap_rebuild(
        candidate, world, expected_id, expected_lifecycle, dirty_epochs,
    )
    if (owner.lifecycle != a49c.COHERENT
            or int(owner._storage_generation) != old_generation + 1
            or owner.epochs != dirty_epochs
            or owner._maintenance_binding is old_binding
            or not candidate.consumed):
        raise AssertionError('full composite rebuild CAS differs')
    _a49c_assert_binding_snapshot(
        old_snapshot, old_binding, 'a49c.rebuild.retired52',
    )

    simultaneous_epochs = owner.epochs
    cells[1].pools[a49c.a3.POOL_ATP] = np.nextafter(
        float(cells[1].pools[a49c.a3.POOL_ATP]), np.inf,
    )
    cells[1].transporters[0, 1] = np.nextafter(
        float(cells[1].transporters[0, 1]), np.inf,
    )
    if (owner.audit_cpu(world) != a49c.CPU_NEWER
            or owner.epochs != a49a._advance_epochs(
                simultaneous_epochs, frozenset(('S',)),
                cache_built=False,
            )):
        raise AssertionError('base+sidecar simultaneous drift double-counted S')
    expected_id = owner.arena_id
    expected_epochs = owner.epochs
    candidate = owner.prepare_rebuild(world)
    owner.swap_rebuild(
        candidate, world, expected_id, a49c.CPU_NEWER,
        expected_epochs,
    )

    presence_world, presence_cells, presence_capacity = _a49a_fixture(
        seed=19002,
    )
    if hasattr(presence_cells[0], 'maintenance_shortfall'):
        delattr(presence_cells[0], 'maintenance_shortfall')
    presence_owner = _a49c_owner(
        presence_world, presence_capacity, 'cpu',
    )
    presence_epochs = presence_owner.epochs
    presence_cells[0].maintenance_shortfall = 0.0
    if (presence_owner.audit_cpu(presence_world) != a49c.CPU_NEWER
            or presence_owner.epochs != a49a._advance_epochs(
                presence_epochs, frozenset(('S',)),
                cache_built=False,
            )):
        raise AssertionError('absent-to-explicit-zero presence drift was hidden')

    capacity_details = []
    for device in _a49c_devices():
        exact_world, _, exact = _a49a_fixture(seed=19010)
        exact_owner = _a49c_owner(exact_world, exact, device)
        _a49c_assert_binding52(
            exact_owner._maintenance_binding,
            'a49c.%s.capacity.exact52' % device,
        )
        values = {
            'max_cells': int(exact.max_cells),
            'max_sequences': int(exact.max_sequences),
            'max_symbols': int(exact.max_symbols),
            'max_sequence_symbols': int(exact.max_sequence_symbols),
            'max_proteins_per_cell': int(exact.max_proteins_per_cell),
        }
        for axis, field in (
                ('C', 'max_cells'), ('Q', 'max_sequences'),
                ('S', 'max_symbols'), ('W', 'max_sequence_symbols'),
                ('P', 'max_proteins_per_cell')):
            short_values = dict(values)
            short_values[field] -= 1
            short = a4.GPU068A4Config(**short_values)
            short_world, _, _ = _a49a_fixture(seed=19011)
            before = v3.pickle_clone(short_world.state_dict())
            rng = short_world.rng
            rng_state = v3.pickle_clone(rng.bit_generator.state)
            _a49c_expect_error(
                lambda short_world=short_world, short=short, device=device:
                _a49c_owner(short_world, short, device),
            )
            _a49a_assert_world_unchanged(
                before, rng, rng_state, short_world,
                'a49c.%s.capacity.%s' % (device, axis),
            )
            capacity_details.append(device + ':' + axis)

    class CaptureScheduler(a49c.A4ResidentMaintenanceFoundationScheduler):
        def __init__(self, *args, **kwargs):
            self.prepared_seen = None
            self.finalized_seen = None
            super(CaptureScheduler, self).__init__(*args, **kwargs)

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            return prepared

        def _resident_translation_finalized_ready(
                self, world, cell, prepared, finalized):
            self.finalized_seen = finalized
            return finalized

    carry_details = []
    for device in _a49c_devices():
        carry_world, carry_cells, carry_capacity, carry_dt = (
            _paid_translation_fixture(seed=19020)
        )
        scheduler = CaptureScheduler(carry_capacity, device)
        scheduler._bind_resident_world(carry_world)
        carry_owner = scheduler._resident_owner_for_rank5(carry_world)
        branches = (
            ('rich', carry_cells[0], carry_dt, True),
            ('early', carry_cells[3], carry_dt, False),
            ('eligible_dt0', carry_cells[0], 0.0, True),
        )
        for label, cell, dt, expected_sync in branches:
            scheduler.prepared_seen = None
            scheduler.finalized_seen = None
            before_binding = carry_owner._maintenance_binding
            before_snapshot = _a49c_binding_snapshot(before_binding)
            before_storage = _a49c_assert_binding52(
                before_binding, 'a49c.%s.%s.old52' % (device, label),
            )
            before_generation = int(carry_owner._storage_generation)
            before_epochs = carry_owner.epochs
            sidecar_before = (
                _a49c_host_array(
                    before_binding.supplement.transporters,
                ).copy(),
                _a49c_host_array(
                    before_binding.supplement.maintenance_shortfall,
                ).copy(),
            )
            _a49b_begin_translation_direct(
                scheduler, carry_world, cell, dt,
            )
            try:
                scheduler.cpu_translation(
                    carry_world, cell, dt, carry_world.config,
                )
                prepared = scheduler.prepared_seen
                finalized = scheduler.finalized_seen
                after_binding = carry_owner._maintenance_binding
                after_storage = _a49c_assert_binding52(
                    after_binding,
                    'a49c.%s.%s.new52' % (device, label),
                )
                if (prepared is None or finalized is None
                        or bool(prepared.sync_performed) is not expected_sync
                        or before_storage.intersection(after_storage)
                        or int(carry_owner._storage_generation)
                        != before_generation + 1
                        or carry_owner.epochs != a49a._advance_epochs(
                            before_epochs, frozenset(('S',)),
                            cache_built=False,
                        )
                        or carry_owner.lifecycle != a49c.COHERENT
                        or after_binding is before_binding
                        or not prepared.consumed or not finalized.consumed
                        or not _a49c_exact_array(
                            sidecar_before[0],
                            after_binding.supplement.transporters)
                        or not _a49c_exact_array(
                            sidecar_before[1],
                            after_binding.supplement.maintenance_shortfall)):
                    raise AssertionError(
                        '%s/%s translation sidecar carry differs' % (
                            device, label,
                        )
                    )
                _a49c_assert_binding_snapshot(
                    before_snapshot, before_binding,
                    'a49c.%s.%s.retired52' % (device, label),
                )
                carry_details.append('%s:%s' % (device, label))
            finally:
                _a48_abort_direct(scheduler, cell)

    return ('two-array shape/tail and resident52 exact; sidecar-only and '
            'base+sidecar S-once/idempotent full CAS; presence drift; exact '
            'C/Q/S/W/P one-short %s; rich/early/no-op A4.9b carry fresh52 '
            'sidecar-bit-exact generation/S +1 %s' % (
                '/'.join(capacity_details), '/'.join(carry_details),
            ))


def test_a49c_plan_and_arena_seals_leases_tamper_scope_fail_closed():
    gc.collect()
    if any(len(registry) != 0 for registry in (
            a49c._SUPPLEMENT_REGISTRY, a49c._BINDING_REGISTRY,
            a49c._PLAN_REGISTRY)):
        raise AssertionError('A4.9c weak creation registries retained artifacts')

    def transient_artifacts():
        world, cells, capacity = _a49c_fixture(seed=19100)
        supplement = a49c.pack_a4_maintenance_supplement(world, capacity)
        binding = a49c.bind_a4_maintenance(supplement)
        a49c.paid_maintenance_selected_numpy(
            binding, 0.1, 0, int(cells[0].cell_id),
        )

    transient_artifacts()
    gc.collect()
    if any(len(registry) != 0 for registry in (
            a49c._SUPPLEMENT_REGISTRY, a49c._BINDING_REGISTRY,
            a49c._PLAN_REGISTRY)):
        raise AssertionError('A4.9c weak creation registries did not release')

    world, cells, capacity = _a49c_fixture(seed=19101)
    supplement = a49c.pack_a4_maintenance_supplement(world, capacity)
    binding = a49c.bind_a4_maintenance(supplement)
    plan = a49c.paid_maintenance_selected_numpy(
        binding, 0.1, 0, int(cells[0].cell_id),
    )
    for bad_dt in (-1.0, float('nan'), float('inf'), True, '0.1'):
        _a49c_expect_error(
            lambda bad_dt=bad_dt: a49c.paid_maintenance_selected_numpy(
                binding, bad_dt, 0, int(cells[0].cell_id),
            )
        )
    for bad_index in (-1, len(cells), True, 1.0):
        _a49c_expect_error(
            lambda bad_index=bad_index: a49c.paid_maintenance_selected_numpy(
                binding, 0.1, bad_index, int(cells[0].cell_id),
            )
        )
    _a49c_expect_error(
        lambda: a49c.paid_maintenance_selected_numpy(
            binding, 0.1, 0, int(cells[0].cell_id) + 1,
        )
    )
    resident = binding.to_torch('cpu')
    _a49c_expect_error(
        lambda: a49c.paid_maintenance_selected_numpy(
            resident, 0.1, 0, int(cells[0].cell_id),
        )
    )
    _a49c_expect_error(
        lambda: a49c.paid_maintenance_selected_torch(
            binding, 0.1, 0, int(cells[0].cell_id),
        )
    )
    for artifact in (supplement, binding, plan):
        _a49c_expect_error(lambda artifact=artifact: pickle.dumps(artifact))
        for forbidden in ('apply', 'publish', 'swap', 'state_dict'):
            if hasattr(artifact, forbidden):
                raise AssertionError(
                    '%s exposed forbidden %s' % (
                        type(artifact).__name__, forbidden,
                    )
                )
    _a49c_expect_error(
        lambda: a49c.A4MaintenanceSupplementBatch(),
    )
    _a49c_expect_error(lambda: a49c.A4MaintenanceBinding())
    _a49c_expect_error(lambda: a49c.A4SelectedMaintenancePlan())

    for offset, bad in enumerate((
            True, np.bool_(False), '0.0', np.asarray([0.0]), [0.0],
            None, -1.0, float('nan'), float('inf'))):
        bad_world, bad_cells, bad_capacity = _a49c_fixture(
            seed=19110 + offset,
        )
        bad_cells[0].maintenance_shortfall = bad
        _a49c_expect_error(
            lambda bad_world=bad_world, bad_capacity=bad_capacity:
            a49c.pack_a4_maintenance_supplement(
                bad_world, bad_capacity,
            )
        )

    alias_world, _, alias_capacity = _a49c_fixture(seed=19120)
    alias_source = a49c.bind_a4_maintenance(
        a49c.pack_a4_maintenance_supplement(
            alias_world, alias_capacity,
        )
    )
    alias_copy = alias_source.to_numpy()
    object.__setattr__(
        alias_copy.translation.state, 'membrane',
        alias_source.translation.state.membrane,
    )
    _a49c_expect_error(alias_copy.validate)

    source_modes = (
        ('ragged_counts', lambda value:
         value.translation.ragged.genome_counts.data.__setitem__(
             0, value.translation.ragged.genome_counts.data[0] + 1)),
        ('ragged_offsets', lambda value:
         value.translation.ragged.sequence_offsets.data.__setitem__(
             1, value.translation.ragged.sequence_offsets.data[1] + 1)),
        ('state', lambda value:
         value.translation.state.pools.data.__setitem__(
             (0, 0), value.translation.state.pools.data[0, 0] + 1.0)),
        ('cache', lambda value:
         value.translation.cache.copy_numbers.data.__setitem__(
             0, value.translation.cache.copy_numbers.data[0] + 1)),
        ('transporters', lambda value:
         value.supplement.transporters.data.__setitem__(
             (0, 0, 0), value.supplement.transporters.data[0, 0, 0] + 1.0)),
        ('shortfall', lambda value:
         value.supplement.maintenance_shortfall.data.__setitem__(
             0, value.supplement.maintenance_shortfall.data[0] + 1.0)),
    )
    tamper_details = []
    for offset, (label, mutate) in enumerate(source_modes):
        tamper_world, _, tamper_capacity = _a49c_fixture(
            seed=19130 + offset,
        )
        tampered = a49c.bind_a4_maintenance(
            a49c.pack_a4_maintenance_supplement(
                tamper_world, tamper_capacity,
            )
        ).to_torch('cpu')
        mutate(tampered)
        _a49c_expect_error(tampered.validate)
        tamper_details.append('source:' + label)

    plan_modes = (
        ('mask', lambda value: value.target_mask.data.__setitem__(
            0, ~value.target_mask.data[0])),
        ('state', lambda value: value.state_after.pools.data.__setitem__(
            (0, 0), value.state_after.pools.data[0, 0] + 1.0)),
        ('transporters', lambda value:
         value.supplement_after.transporters.data.__setitem__(
             (0, 0, 0),
             value.supplement_after.transporters.data[0, 0, 0] + 1.0)),
        ('shortfall', lambda value:
         value.supplement_after.maintenance_shortfall.data.__setitem__(
             0,
             value.supplement_after.maintenance_shortfall.data[0] + 1.0)),
    )
    for offset, (label, mutate) in enumerate(plan_modes):
        tamper_world, tamper_cells, tamper_capacity = _a49c_fixture(
            seed=19140 + offset,
        )
        source = a49c.bind_a4_maintenance(
            a49c.pack_a4_maintenance_supplement(
                tamper_world, tamper_capacity,
            )
        ).to_torch('cpu')
        tampered_plan = a49c.paid_maintenance_selected(
            source, 0.1, 0, int(tamper_cells[0].cell_id),
        )
        mutate(tampered_plan)
        _a49c_expect_error(tampered_plan.validate)
        tamper_details.append('plan:' + label)

    lease_world, _, lease_capacity = _a49a_fixture(seed=19150)
    lease_owner = _a49c_owner(lease_world, lease_capacity, 'cpu')
    lease_a = lease_owner._open_read_lease(lease_world)
    lease_b = lease_owner._open_read_lease(lease_world)
    _a49c_expect_error(lease_a.__enter__)
    with lease_b as active:
        leased = active._binding_once()
        if leased is not lease_owner._maintenance_binding:
            raise AssertionError('read lease did not return composite52')
        _a49c_expect_error(active._binding_once)
    _a49c_expect_error(lease_b.__enter__)
    other_world, _, other_capacity = _a49a_fixture(seed=19151)
    other_owner = _a49c_owner(other_world, other_capacity, 'cpu')
    cross = lease_owner._open_read_lease(lease_world)
    _a49c_expect_error(lambda: other_owner._activate_lease(cross))
    with cross as active:
        active._binding_once()

    for offset, field in enumerate(('transporters', 'shortfall')):
        active_world, _, active_capacity = _a49a_fixture(
            seed=19160 + offset,
        )
        active_owner = _a49c_owner(active_world, active_capacity, 'cpu')
        source = active_owner._maintenance_source
        if field == 'transporters':
            source.transporters[0, 0, 0] += 7.0
        else:
            source.maintenance_shortfall[0] += 7.0
        _a49c_expect_error(active_owner._guard_or_invalidate)
        if active_owner.lifecycle != a49c.INVALID:
            raise AssertionError(field + ' active observation stayed usable')
        tamper_details.append('active-observation:' + field)

    candidate_modes = ('source_transporters', 'source_shortfall',
                       'binding_base', 'binding_sidecar')
    for offset, mode in enumerate(candidate_modes):
        candidate_world, candidate_cells, candidate_capacity = _a49a_fixture(
            seed=19170 + offset,
        )
        candidate_owner = _a49c_owner(
            candidate_world, candidate_capacity, 'cpu',
        )
        candidate_cells[0].transporters[0, 0] = np.nextafter(
            float(candidate_cells[0].transporters[0, 0]), np.inf,
        )
        candidate_owner.audit_cpu(candidate_world)
        expected_id = candidate_owner.arena_id
        expected_epochs = candidate_owner.epochs
        candidate = candidate_owner.prepare_rebuild(candidate_world)
        if mode == 'source_transporters':
            candidate.source_ref.transporters[0, 0, 0] += 7.0
        elif mode == 'source_shortfall':
            candidate.source_ref.maintenance_shortfall[0] += 7.0
        elif mode == 'binding_base':
            candidate.new_binding_ref.translation.ragged.genome_counts.data[0] += 1
        else:
            candidate.new_binding_ref.supplement.transporters.data[0, 0, 0] += 1.0
        _a49c_expect_error(lambda: candidate_owner.swap_rebuild(
            candidate, candidate_world, expected_id,
            a49c.CPU_NEWER, expected_epochs,
        ))
        if (candidate_owner.lifecycle != a49c.INVALID
                or candidate_owner._maintenance_rebuild_candidate is not None):
            raise AssertionError(mode + ' candidate was not quarantined')
        tamper_details.append('candidate:' + mode)

    ordinary_world, ordinary_cells, ordinary_capacity = _a49a_fixture(
        seed=19180,
    )
    ordinary_owner = _a49c_owner(
        ordinary_world, ordinary_capacity, 'cpu',
    )
    ordinary_cells[0].transporters[0, 0] = np.nextafter(
        float(ordinary_cells[0].transporters[0, 0]), np.inf,
    )
    ordinary_owner.audit_cpu(ordinary_world)
    old_binding = ordinary_owner._maintenance_binding
    old_snapshot = _a49c_binding_snapshot(old_binding)
    old_generation = int(ordinary_owner._storage_generation)
    candidate = ordinary_owner.prepare_rebuild(ordinary_world)
    _a49c_expect_error(lambda: ordinary_owner.swap_rebuild(
        candidate, ordinary_world, ordinary_owner.arena_id + '-wrong',
        a49c.CPU_NEWER, ordinary_owner.epochs,
    ))
    if (ordinary_owner.lifecycle != a49c.CPU_NEWER
            or ordinary_owner._maintenance_rebuild_candidate is not None
            or int(ordinary_owner._storage_generation) != old_generation):
        raise AssertionError('ordinary wrong CAS invalidated old composite')
    _a49c_assert_binding_snapshot(
        old_snapshot, old_binding, 'a49c.ordinary-cas.old52',
    )

    class TranslationTamper(a49c.A4ResidentMaintenanceFoundationScheduler):
        def __init__(self, *args, **kwargs):
            self.mode = kwargs.pop('mode')
            self.prepared_seen = None
            super(TranslationTamper, self).__init__(*args, **kwargs)

        def _resident_translation_candidate_ready(
                self, world, cell, dt, config, prepared):
            self.prepared_seen = prepared
            if self.mode == 'prepared_base':
                prepared._a49c_candidate_binding.translation.ragged.genome_counts.data[0] += 1
            elif self.mode == 'prepared_sidecar':
                prepared._a49c_candidate_binding.supplement.transporters.data[0, 0, 0] += 1.0
            elif self.mode == 'ordinary':
                raise RuntimeError('ordinary preclaim hook failure')
            return prepared

        def _publish_resident_translation_candidate(
                self, world, cell, prepared):
            super(TranslationTamper, self)._publish_resident_translation_candidate(
                world, cell, prepared,
            )
            if self.mode == 'publisher_sidecar':
                prepared._a49c_candidate_binding.supplement.transporters.data[0, 0, 0] += 1.0
                raise RuntimeError('publisher sidecar trust failure')

        def _resident_translation_finalized_ready(
                self, world, cell, prepared, finalized):
            if self.mode == 'finalized_base':
                finalized._a49c_new_binding.translation.cache.copy_numbers.data[0] += 1
            elif self.mode == 'finalized_sidecar':
                finalized._a49c_new_binding.supplement.maintenance_shortfall.data[0] += 1.0
            return finalized

    for offset, mode in enumerate((
            'prepared_base', 'prepared_sidecar', 'ordinary',
            'publisher_sidecar', 'finalized_base', 'finalized_sidecar')):
        tx_world, tx_cells, tx_capacity, tx_dt = _paid_translation_fixture(
            seed=19190 + offset,
        )
        cell = tx_cells[0]
        scheduler = TranslationTamper(tx_capacity, 'cpu', mode=mode)
        scheduler._bind_resident_world(tx_world)
        tx_owner = scheduler._resident_owner_for_rank5(tx_world)
        old_arena = tx_owner._arena
        old_binding = tx_owner._maintenance_binding
        old_snapshot = _a49c_binding_snapshot(old_binding)
        old_generation = int(tx_owner._storage_generation)
        before = v3.pickle_clone(tx_world.state_dict())
        rng = tx_world.rng
        rng_state = v3.pickle_clone(rng.bit_generator.state)
        _a49b_begin_translation_direct(scheduler, tx_world, cell, tx_dt)
        caught = None
        try:
            scheduler.cpu_translation(tx_world, cell, tx_dt, tx_world.config)
        except BaseException as exc:
            caught = exc
        if caught is None:
            raise AssertionError(mode + ' injected translation failure escaped')
        _a49a_assert_world_unchanged(
            before, rng, rng_state, tx_world,
            'a49c.translation.' + mode,
        )
        expected_lifecycle = (
            a49c.COHERENT if mode == 'ordinary' else a49c.INVALID
        )
        if (tx_owner.lifecycle != expected_lifecycle
                or tx_owner._arena is not old_arena
                or tx_owner._maintenance_binding is not old_binding
                or int(tx_owner._storage_generation) != old_generation
                or tx_owner._tx_active
                or tx_owner._tx_prepared is not None
                or tx_owner._maintenance_phase != 'idle'):
            raise AssertionError(mode + ' rollback/quarantine differs')
        _a49c_assert_binding_snapshot(
            old_snapshot, old_binding, 'a49c.translation.' + mode,
        )
        _, record = scheduler._record(cell)
        claims = [entry for entry in record['events']
                  if entry['event'] == 'translation_cpu']
        if mode.startswith('prepared') or mode == 'ordinary':
            if claims:
                raise AssertionError(mode + ' crossed preclaim boundary')
        elif (len(claims) != 1 or 'amount' in claims[0]['metadata']
              or 'rng_draw_count' in claims[0]['metadata']):
            raise AssertionError(mode + ' claim was annotated before rollback')
        _a48_abort_direct(scheduler, cell, caught)
        tamper_details.append('translation:' + mode)

    for offset, mode in enumerate(('build', 'clone', 'seal')):
        fail_world, fail_cells, fail_capacity, fail_dt = (
            _paid_translation_fixture(seed=19210 + offset)
        )
        cell = fail_cells[0]
        scheduler = a49c.A4ResidentMaintenanceFoundationScheduler(
            fail_capacity, 'cpu',
        )
        scheduler._bind_resident_world(fail_world)
        fail_owner = scheduler._resident_owner_for_rank5(fail_world)
        old_binding = fail_owner._maintenance_binding
        old_snapshot = _a49c_binding_snapshot(old_binding)
        old_generation = int(fail_owner._storage_generation)
        original_make = a49c._make_resident_maintenance_binding
        original_clone = torch.Tensor.clone
        target_pointers = set(old_binding.supplement.data_ptrs().values())

        def make_failure(*args, **kwargs):
            raise RuntimeError('injected sidecar build failure')

        def seal_failure(*args, **kwargs):
            built = original_make(*args, **kwargs)
            object.__setattr__(
                built.supplement, '_content_digests', ('tamper', 'tamper'),
            )
            return built

        def clone_failure(tensor, *args, **kwargs):
            if int(tensor.data_ptr()) in target_pointers:
                raise RuntimeError('injected sidecar clone failure')
            return original_clone(tensor, *args, **kwargs)

        if mode == 'build':
            a49c._make_resident_maintenance_binding = make_failure
        elif mode == 'seal':
            a49c._make_resident_maintenance_binding = seal_failure
        else:
            torch.Tensor.clone = clone_failure
        _a49b_begin_translation_direct(
            scheduler, fail_world, cell, fail_dt,
        )
        caught = None
        try:
            scheduler.cpu_translation(
                fail_world, cell, fail_dt, fail_world.config,
            )
        except BaseException as exc:
            caught = exc
        finally:
            a49c._make_resident_maintenance_binding = original_make
            torch.Tensor.clone = original_clone
        if (caught is None or fail_owner.lifecycle != a49c.COHERENT
                or fail_owner._maintenance_binding is not old_binding
                or int(fail_owner._storage_generation) != old_generation
                or fail_owner._tx_active
                or fail_owner._tx_prepared is not None
                or fail_owner._maintenance_phase != 'idle'):
            raise AssertionError(mode + ' failure stranded transaction')
        _a49c_assert_binding_snapshot(
            old_snapshot, old_binding, 'a49c.sidecar-failure.' + mode,
        )
        _a48_abort_direct(scheduler, cell, caught)
        tamper_details.append('sidecar-failure:' + mode)

    return ('strict selector/dt/scalar/device/artifact scope; weak registries '
            'release; NumPy alias and source/plan .data seals; composite '
            'leases; active/candidate observation/base/sidecar quarantine; '
            'ordinary CAS preserves old52; prepared/publisher/finalized '
            'trust INVALID vs ordinary abort; build/clone/seal tx neutral: %s' %
            '/'.join(tamper_details))


def test_a49c_reconstruction_a49b_successor_frozen94_authority():
    capacity = a4.GPU068A4Config()

    def a49b_from_state(state, device):
        return _a49b_hybrid_from_state(state, capacity, device)

    def run_lockstep(label, state, steps, device):
        inherited = a49b_from_state(state, device)
        successor = _a49c_hybrid_from_state(state, capacity, device)
        original_container = successor.world.cells
        original_members = tuple(successor.world.cells)
        last_generation = 0
        for step_index in range(int(steps)):
            inherited_receipt = inherited.step(0.1)
            successor_receipt = successor.step(0.1)
            v3.assert_recursive_close(
                inherited_receipt, successor_receipt, 0.0, 0.0,
                '%s.receipt[%d]' % (label, step_index),
            )
            v3.assert_recursive_close(
                inherited.world.state_dict(),
                successor.world.state_dict(),
                v3.WORLD_FP64_ATOL, 0.0,
                '%s.world[%d]' % (label, step_index),
            )
            if (successor.world.cells is not original_container
                    or tuple(successor.world.cells) != original_members):
                raise AssertionError(
                    '%s step %d did not normalize same membership' % (
                        label, step_index,
                    )
                )
            owner = successor.scheduler._a49b_owner
            if owner is None or owner.lifecycle != a49c.COHERENT:
                raise AssertionError(label + ' did not construct rank5 owner')
            if int(owner._storage_generation) <= last_generation:
                raise AssertionError(label + ' storage generation did not advance')
            last_generation = int(owner._storage_generation)
            for record in successor_receipt['cells']:
                names = [entry['event'] for entry in record['events']]
                by_name = {
                    entry['event']: entry for entry in record['events']
                }
                if (tuple(names) != tuple(a3s.CELL_EVENT_ORDER)
                        or names.count('maintenance') != 1
                        or names.count('translation_cpu') != 1
                        or names.index('maintenance')
                        >= names.index('translation_cpu')
                        or by_name['translation_cpu']['metadata'].get(
                            'authority')
                        != ('A4.9b-world-resident-selected-paid-'
                            'translation-atomic-CAS')
                        or by_name['translation_cpu']['metadata'].get(
                            'rng_draw_count') != 0):
                    raise AssertionError(label + ' event authority/order differs')
            if tuple(
                    entry['event']
                    for entry in successor_receipt['world_events']) != tuple(
                        a3s.WORLD_EVENT_ORDER):
                raise AssertionError(label + ' world event order differs')

            # Rank-5 is followed by CPU transporter/repair work.  The shadow
            # is therefore audited as CPU_NEWER at the boundary and rebuilt
            # at the next rank-5 preclaim; a repeated audit is idempotent.
            epoch_before = owner.epochs
            if owner.audit_cpu(successor.world) != a49c.CPU_NEWER:
                raise AssertionError(label + ' end-step drift was not observed')
            observed_epochs = owner.epochs
            if (observed_epochs.state_epoch
                    != epoch_before.state_epoch + 1):
                raise AssertionError(label + ' end-step S epoch differs')
            owner.audit_cpu(successor.world)
            if owner.epochs != observed_epochs:
                raise AssertionError(label + ' repeated end-step audit advanced')
        return inherited, successor, last_generation

    lockstep_details = []
    for device in _a49c_devices():
        for steps, seed in ((1, 19300), (10, 19301)):
            source = v3.make_world(seed=seed, cells=1)
            _, successor, generation = run_lockstep(
                'a49c.%s.lockstep_%d' % (device, steps),
                source.state_dict(), steps, device,
            )
            lockstep_details.append(
                '%s:%d:g%d' % (device, steps, generation)
            )
        two = v3.make_world(seed=19302, cells=2)
        two.config.endogenous_damage = False
        for cell in two.cells:
            cell.genome_lesions = [
                0.5 / 0.00065 for _ in cell.genomes
            ]
            cell._refresh_gene_cache()
        run_lockstep(
            'a49c.%s.two_cell' % device,
            two.state_dict(), 1, device,
        )

    persistence_details = []
    for device in _a49c_devices():
        persisted_source = v3.make_world(seed=19310, cells=1)
        persisted = _a49c_hybrid_from_state(
            persisted_source.state_dict(), capacity, device,
        )
        persisted.step(0.1)
        durable = persisted.state_dict()
        expected_keys = {
            'schema', 'config', 'device', 'foundation', 'port_status',
        }
        top_status = durable.get('a4_resident_maintenance_foundation')
        scheduler_status = durable.get('scheduler', {}).get(
            'a4_resident_maintenance_foundation'
        )
        if (not isinstance(top_status, Mapping)
                or not isinstance(scheduler_status, Mapping)
                or set(top_status) != expected_keys
                or set(scheduler_status) != expected_keys
                or top_status != scheduler_status
                or top_status['port_status'] != a49c.PORT_STATUS):
            raise AssertionError(device + ' canonical foundation status differs')
        for value in (top_status, scheduler_status):
            if any(key in value for key in (
                    'supplement', 'binding', 'owner', 'arena', 'candidate',
                    'lease', 'epochs', 'pointers', 'seal', 'guard')):
                raise AssertionError(device + ' saved runtime artifact')
        pickle.dumps(durable, protocol=pickle.HIGHEST_PROTOCOL)

        twin = persisted.clone()
        with tempfile.TemporaryDirectory() as directory:
            path = os.path.join(directory, 'a49c.pkl')
            persisted.save(path)
            restored = (
                a49c.Hybrid066WorldA4ResidentMaintenanceFoundation.load(
                    path,
                )
            )
        for value in (twin, restored):
            if (type(value)
                    is not a49c.Hybrid066WorldA4ResidentMaintenanceFoundation
                    or type(value.scheduler)
                    is not a49c.A4ResidentMaintenanceFoundationScheduler
                    or value.scheduler._a49b_owner is not None
                    or value.scheduler._a49b_world is not value.world
                    or value.scheduler._a49b_cells is not value.world.cells
                    or value.scheduler.a4_device != device
                    or value.scheduler.a4_config != capacity):
                raise AssertionError(device + ' reconstruction authority differs')
        v3.assert_recursive_close(
            twin.world.state_dict(), restored.world.state_dict(),
            0.0, 0.0, 'a49c.%s.clone-load' % device,
        )
        v3.assert_recursive_close(
            persisted.world.state_dict(), twin.world.state_dict(),
            v3.WORLD_FP64_ATOL, 0.0,
            'a49c.%s.source-clone' % device,
        )

        wrappers = (persisted, twin, restored)
        owners = []
        storage = []
        arena_ids = set()
        for index, value in enumerate(wrappers):
            owner = value.scheduler._resident_owner_for_rank5(value.world)
            owners.append(owner)
            storage.append(_a49c_assert_binding52(
                owner._maintenance_binding,
                'a49c.%s.persisted%d' % (device, index),
            ))
            arena_ids.add(owner.arena_id)
        if len(arena_ids) != 3:
            raise AssertionError(device + ' reconstruction reused arena ID')
        for left in range(3):
            for right in range(left + 1, 3):
                if storage[left].intersection(storage[right]):
                    raise AssertionError(
                        device + ' reconstructed composite52 aliases'
                    )

        twin_owner = owners[1]
        twin.world.cells[0].transporters[0, 0] = np.nextafter(
            float(twin.world.cells[0].transporters[0, 0]), np.inf,
        )
        twin_owner.audit_cpu(twin.world)
        pending = twin_owner.prepare_rebuild(twin.world)
        _a49c_expect_error(twin.state_dict)
        twin_owner._discard_composite_rebuild(pending)
        restored_owner = owners[2]
        lease = restored_owner._open_read_lease(restored.world)
        _a49c_expect_error(restored.state_dict)
        with lease as active:
            active._binding_once()
        _a49c_expect_error(lambda: pickle.dumps(restored_owner))
        persistence_details.append(device + ':fresh3x52')

    bad_state = a49c.A4ResidentMaintenanceFoundationScheduler(
        capacity, 'cpu',
    ).state_dict()
    bad_state = copy.deepcopy(bad_state)
    bad_state['a4_resident_maintenance_foundation']['supplement'] = {}
    _a49c_expect_error(
        lambda: a49c.A4ResidentMaintenanceFoundationScheduler.from_state(
            bad_state,
        )
    )

    member_source = v3.make_world(seed=19320, cells=2)
    member_source.cells[1].alive = False
    member_source.cells[1].death_reason = 'a49c_integrated_death'
    member = _a49c_hybrid_from_state(
        member_source.state_dict(), capacity, 'cpu',
    )
    member_container = member.world.cells
    original_members = tuple(member.world.cells)
    receipt = member.step(0.1)
    member_owner = member.scheduler._a49b_owner
    old_binding = member_owner._maintenance_binding
    old_snapshot = _a49c_binding_snapshot(old_binding)
    old_generation = int(member_owner._storage_generation)
    if (receipt['status'] != 'complete'
            or member.world.cells is not member_container
            or tuple(member.world.cells) == original_members
            or len(member.world.cells) != 1):
        raise AssertionError('successful membership normalization was hidden')
    _a49c_expect_error(lambda: member.step(0.1))
    if (member_owner.lifecycle != a49c.INVALID
            or member_owner._maintenance_binding is not old_binding
            or int(member_owner._storage_generation) != old_generation):
        raise AssertionError('actual membership drift auto-rebuilt composite')
    _a49c_assert_binding_snapshot(
        old_snapshot, old_binding, 'a49c.a5.no-flush52',
    )

    prior_names = '\n'.join(
        fn.__name__ for fn in A49B_VALIDATION_TESTS
    )
    new_names = tuple(fn.__name__ for fn in A49C_TESTS)
    prereg_path = os.path.join(
        ROOT, 'planning', 'SOMA_CELL_0_6_8_GPU_A4_PREREGISTRATION_JA.md',
    )
    with open(prereg_path, 'rb') as handle:
        preregistration = handle.read()
    suffix = preregistration[_A49C_PREREG_PREFIX_BYTES:]
    public_signatures = (
        tuple(inspect.signature(
            a49c.pack_a4_maintenance_supplement,
        ).parameters),
        tuple(inspect.signature(a49c.bind_a4_maintenance).parameters),
        tuple(inspect.signature(
            a49c.paid_maintenance_selected_numpy,
        ).parameters),
        tuple(inspect.signature(
            a49c.paid_maintenance_selected_torch,
        ).parameters),
        tuple(inspect.signature(
            a49c.paid_maintenance_selected,
        ).parameters),
    )
    if (tuple(a49c.__all__) != _A49C_PUBLIC_API
            or public_signatures != (
                ('world', 'config'), ('supplement',),
                ('binding', 'dt', 'target_index', 'target_cell_id'),
                ('binding', 'dt', 'target_index', 'target_cell_id'),
                ('binding', 'dt', 'target_index', 'target_cell_id'),
            )
            or tuple(a49c.A4MaintenanceSupplementBatch.__dataclass_fields__)
            != ('schema_version', 'cell_capacity', 'cell_count',
                'source_provenance', 'transporters',
                'maintenance_shortfall')
            or tuple(a49c.A4MaintenanceBinding.__dataclass_fields__)
            != ('translation', 'supplement')
            or tuple(a49c.A4SelectedMaintenancePlan.__dataclass_fields__)
            != ('schema_version', 'target_index', 'target_cell_id',
                'dt_hex', 'target_mask', 'source_provenance',
                'state_after', 'supplement_after')
            or not issubclass(
                a49c.A4ResidentMaintenanceFoundationScheduler,
                a49b.A4ResidentTranslationEventScheduler)
            or not issubclass(
                a49c.Hybrid066WorldA4ResidentMaintenanceFoundation,
                a49b.Hybrid066WorldA4ResidentTranslation)
            or a49c.A4ResidentMaintenanceFoundationScheduler.cpu_translation
            is not a49b.A4ResidentTranslationEventScheduler.cpu_translation
            or a49c.FULL_GPU_WORLD_STEP is not False
            or a49c.PORT_STATUS.get('resident_device_writes') is not True
            or a49c.PORT_STATUS.get('resident_d2h_publish') is not True
            or a49c.PORT_STATUS.get('resident_device_writes_scope')
            != 'inherited-a4.9b-selected-translation-only'
            or a49c.PORT_STATUS.get('resident_d2h_publish_scope')
            != 'inherited-a4.9b-selected-translation-only'
            or a49c.PORT_STATUS.get('selected_maintenance')
            != 'pure-plan-only'
            or any(a49c.PORT_STATUS.get(key) is not False for key in (
                'maintenance_resident_device_writes',
                'maintenance_resident_d2h_publish',
                'maintenance_live_commit',
                'maintenance_resident_biology_authority',
                'full_gpu_world_step'))):
        raise AssertionError('A4.9c public API/status authority differs')

    if (len(SOURCE_PATHS) != 29
            or len(A49C_SOURCE_PATHS) != 30
            or A49C_SOURCE_PATHS[:-1] != SOURCE_PATHS
            or A49C_SOURCE_PATHS[-1] != A49C_NEW_MODULE_PATH
            or len(A49B_VALIDATION_TESTS) != 94
            or len(A49C_TESTS) != 4
            or len(A49C_VALIDATION_TESTS) != 98
            or new_names != (
                'test_a49c_selected_maintenance_plan_formal066_cpu_cuda_isolation',
                'test_a49c_composite52_shadow_pointers_coherence_rebuild_and_capacity',
                'test_a49c_plan_and_arena_seals_leases_tamper_scope_fail_closed',
                'test_a49c_reconstruction_a49b_successor_frozen94_authority',
            )
            or hashlib.sha256(prior_names.encode('utf-8')).hexdigest()
            != _A49C_PRIOR_94_NAMES_SHA256
            or _a48c7_prior_test_ast_sha256(A49B_VALIDATION_TESTS)
            != _A49C_PRIOR_94_AST_SHA256
            or hashlib.sha256(preregistration).hexdigest()
            != _A49C_PREREG_SHA256
            or hashlib.sha256(preregistration[
                :_A49C_PREREG_PREFIX_BYTES]).hexdigest()
            != _A49C_PREREG_PREFIX_SHA256
            or not suffix.startswith('\n## A4.9c追加仮説'.encode('utf-8'))
            or _sha256(os.path.join(
                ROOT, *A49C_NEW_MODULE_PATH.split('/'),
            )) != _A49C_CORE_SHA256
            or _sha256(os.path.join(
                ROOT, 'src', '0_6_8',
                'SOMA_CELL_0_6_8_gpu_a4_resident_arena.py',
            )) != _A49A_CORE_SHA256
            or _sha256(os.path.join(
                ROOT, 'src', '0_6_8',
                'SOMA_CELL_0_6_8_gpu_a4_resident_translation_integration.py',
            )) != _A49B_CORE_SHA256):
        raise AssertionError('A4.9c frozen94/prefix/source/hash wiring differs')

    for relative, expected_hash in _A49A_FROZEN_CORE_SHA256.items():
        if _sha256(os.path.join(ROOT, *relative.split('/'))) != expected_hash:
            raise AssertionError(relative + ' changed from frozen authority')

    return ('A49b-vs-A49c 1/10-step and 2-cell CPU/event/receipt/PCG64 '
            'lockstep with end-step CPU_NEWER/next-rank5 rebuild %s; '
            'save/load/clone ownerless then three fresh nonalias52 %s; '
            'pending/lease save reject, exact status, actual membership A5 '
            'no-flush INVALID; public24, frozen94 AST/names, prefix/whole, '
            'SOURCE_PATHS29/A49C30, total98, frozen cores' % (
                '/'.join(lockstep_details),
                '/'.join(persistence_details),
            ))


TESTS = (
    test_api_scope,
    test_source_hash_inputs_present,
    test_ragged_batch_roundtrip_lossless,
    test_numpy_torch_fp64_roundtrip_and_deep_clone,
    test_ragged_schema_rejects_corruption_atomically,
    test_capacity_exact_and_plus_one_atomic,
    test_cell_identity_order_and_duplicate_rejected,
    test_explicit_device_residency_checksum_smoke,
    test_a4_gene_decode_frozen_oracle,
    test_a4_gene_decode_numpy_torch_devices,
    test_a4_gene_decode_capacity_exact_and_plus_one,
    test_a4_gene_cache_schema_and_decode_nonmutation,
    test_a4_paid_translation_cpu_oracle_ledger_and_order,
    test_a4_paid_translation_numpy_torch_devices_nonmutation,
    test_a4_paid_translation_full_mro_signals_and_behavioural_quiescence,
    test_a4_paid_translation_early_gate_capacity_and_schema,
    test_a3_focused_regression,
    test_a44_paid_replication_formal066_cpu_oracle_and_nonmutation,
    test_a44_requested_zero_and_exact_resource_boundaries,
    test_a44_paid_replication_numpy_torch_devices_and_scope_readback,
    test_a44_capacity_scope_fail_closed_and_a3_authority,
    test_a44b_feature_matrix_formal066_cpu_oracle,
    test_a44b_proof_ledger_resource_error_and_nonmutation,
    test_a44b_numpy_torch_deterministic_devices_and_pointers,
    test_a44b_capacity_scope_and_a3_authority,
    test_a45_substitution_rng_tape_formal066_oracle,
    test_a45_substitution_rng_tape_numpy_torch_devices,
    test_a45_substitution_rng_tape_fail_closed_and_authority,
    test_a45b_template_start_formal066_oracle_and_rng_order,
    test_a45b_template_start_exact_capacity_and_zero_dt,
    test_a45b_template_start_numpy_torch_trust_and_scope,
    test_a46a_completion_formal066_oracle_and_conceptual_poststate,
    test_a46a_completion_numpy_torch_devices_and_nonmutation,
    test_a46a_completion_scope_schema_and_a3_authority,
    test_a46b1_completion_mutation_rng_tape_formal066_oracle,
    test_a46b1_completion_mutation_rng_tape_torch_roundtrip,
    test_a46b1_completion_mutation_rng_tape_fail_closed_and_authority,
    test_a46b2_completion_mutation_apply_formal066_oracle,
    test_a46b2_completion_mutation_numpy_torch_devices,
    test_a46b2_completion_mutation_fail_closed_and_authority,
    test_a47a_hydrolysis_literal_boundaries_and_zero_probability_draw,
    test_a47a_hydrolysis_formal066_hit_miss_hit_oracle_and_purity,
    test_a47a_hydrolysis_torch_trust_scope_and_a3_authority,
    test_a47b_hydrolysis_apply_formal066_oracle_and_noop,
    test_a47b_hydrolysis_numpy_torch_devices_and_purity,
    test_a47b_hydrolysis_capacity_trust_rollback_and_authority,
    test_a48a_atomic_commit_formal066_hit_nohit_dt0,
    test_a48a_candidate_cpu_cuda_purity_fresh_binding_and_one_shot,
    test_a48a_fail_closed_capacity_trust_order_and_publish_rollback,
    test_a48a_world_interleave_lockstep_save_clone_and_a3_authority,
    test_a48b_translation_atomic_commit_formal066_oracle_and_gates,
    test_a48b_translation_candidate_cpu_cuda_commit_purity_and_fresh_binding,
    test_a48b_translation_capacity_trust_rollback_and_exact_once,
    test_a48b_world_lockstep_stress_interleave_save_clone_and_a3_authority,
    test_a48c1_active_noncompletion_atomic_commit_formal066_oracle,
    test_a48c1_active_noncompletion_candidate_cpu_cuda_purity_and_rng,
    test_a48c1_active_noncompletion_capacity_trust_rollback_scope_and_exact_once,
    test_a48c1_world_lockstep_interleave_save_clone_successor_and_authority,
    test_a48c2_mutation_free_completion_atomic_commit_formal066_oracle,
    test_a48c2_completion_candidate_cpu_cuda_purity_and_fresh_binding,
    test_a48c2_completion_capacity_trust_rollback_scope_and_exact_once,
    test_a48c2_inherited_c1_save_clone_successor_and_authority,
    test_a48c3_completion_mutation_atomic_commit_formal066_oracle,
    test_a48c3_completion_mutation_candidate_cpu_cuda_purity_rng_and_fresh_binding,
    test_a48c3_completion_mutation_capacity_trust_rollback_scope_and_exact_once,
    test_a48c3_inherited_c2_c1_save_clone_successor_and_authority,
    test_a48c4_inactive_start_noncompletion_atomic_commit_formal066_oracle,
    test_a48c4_candidate_cpu_cuda_purity_rng_and_fresh_binding,
    test_a48c4_capacity_trust_rollback_scope_and_exact_once,
    test_a48c4_active_delegate_save_clone_successor_and_authority,
    test_a48c5_mutation_free_inactive_start_atomic_commit_formal066_oracle,
    test_a48c5_candidate_cpu_cuda_selection_purity_and_fresh_binding,
    test_a48c5_capacity_trust_rollback_scope_and_exact_once,
    test_a48c5_inherited_c4_c3_c2_c1_save_clone_successor_and_authority,
    test_a48c6_mutation_free_inactive_start_completion_formal066_oracle,
    test_a48c6_candidate_cpu_cuda_synthetic_plan_purity_and_fresh_binding,
    test_a48c6_capacity_trust_scope_rollback_and_exact_once,
    test_a48c6_inherited_c5_c4_c3_c2_c1_save_clone_successor_and_authority,
    test_a48c7_mutation_enabled_inactive_start_completion_formal066_oracle,
    test_a48c7_candidate_cpu_cuda_synthetic_tape_plan_purity_and_fresh_binding,
    test_a48c7_capacity_trust_scope_rollback_and_exact_once,
    test_a48c7_inherited_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority,
)

A48C8_TESTS = (
    test_a48c8_pre_active_noop_branch_order_formal066_oracle,
    test_a48c8_candidate_cpu_cuda_resident_classifier_purity_and_fresh_binding,
    test_a48c8_capacity_trust_exclusions_rollback_and_exact_once,
    test_a48c8_inherited_c7_c6_c5_c4_c3_c2_c1_save_clone_successor_and_authority,
)

VALIDATION_TESTS = TESTS + A48C8_TESTS

A49A_TESTS = (
    test_a49a_fresh_world_arena_lease_pointer_stability_and_devices,
    test_a49a_cpu_newer_epochs_dirty_domains_and_separate_rebuild,
    test_a49a_capacity_creation_seals_leases_candidates_and_scope_fail_closed,
    test_a49a_fresh_reconstruction_c8_successor_frozen86_and_authority,
)

A49A_VALIDATION_TESTS = VALIDATION_TESTS + A49A_TESTS

A49B_TESTS = (
    test_a49b_selected_plan_formal066_cpu_cuda_isolation,
    test_a49b_rank5_fresh_generation_epochs_nonalias_and_capacity,
    test_a49b_fail_closed_rollback_cas_leases_duplicate_and_order,
    test_a49b_world_lockstep_persistence_a5_boundary_frozen90_authority,
)

A49B_VALIDATION_TESTS = A49A_VALIDATION_TESTS + A49B_TESTS

A49C_TESTS = (
    test_a49c_selected_maintenance_plan_formal066_cpu_cuda_isolation,
    test_a49c_composite52_shadow_pointers_coherence_rebuild_and_capacity,
    test_a49c_plan_and_arena_seals_leases_tamper_scope_fail_closed,
    test_a49c_reconstruction_a49b_successor_frozen94_authority,
)

A49C_VALIDATION_TESTS = A49B_VALIDATION_TESTS + A49C_TESTS


def run_all(write=False, output_dir=None):
    rows = []
    started = time.time()
    for fn in A49C_VALIDATION_TESTS:
        then = time.perf_counter()
        try:
            detail = fn()
            status = 'PASS'
            error = ''
        except Exception as exc:
            detail = ''
            status = 'FAIL'
            error = '%s: %s' % (type(exc).__name__, exc)
        row = {
            'test': fn.__name__, 'status': status,
            'detail': detail, 'error': error,
            'seconds': time.perf_counter() - then,
        }
        rows.append(row)
        print('%s: %s - %s' % (status, fn.__name__, detail or error))
    passed = sum(row['status'] == 'PASS' for row in rows)
    failed = sum(row['status'] == 'FAIL' for row in rows)
    elapsed = time.time() - started
    payload = {
        'build': a49c.BUILD,
        'schema': {
            'ragged_genome': a4.SCHEMA_VERSION,
            'gene_cache': a4.GENE_CACHE_SCHEMA_VERSION,
            'translation_state': a4.TRANSLATION_SCHEMA_VERSION,
            'paid_replication_elongation': a44.SCHEMA_VERSION,
            'substitution_rng_tape': a44.RNG_TAPE_SCHEMA_VERSION,
            'completion_mutation_rng_tape': (
                a44.COMPLETION_MUTATION_RNG_TAPE_SCHEMA_VERSION
            ),
            'completion_mutation_plan': (
                a44.COMPLETION_MUTATION_PLAN_SCHEMA_VERSION
            ),
            'genome_hydrolysis_rng_tape': a47.SCHEMA_VERSION,
            'genome_hydrolysis_deletion_plan': (
                a47.DELETION_PLAN_SCHEMA_VERSION
            ),
            'genome_hydrolysis_atomic_commit': a48.SCHEMA_VERSION,
            'paid_translation_atomic_commit': a48b.SCHEMA_VERSION,
            'active_noncompletion_replication_atomic_commit': (
                a48c.SCHEMA_VERSION
            ),
            'mutation_free_active_completion_atomic_commit': (
                a48c2.SCHEMA_VERSION
            ),
            'mutation_enabled_active_completion_atomic_commit': (
                a48c3.SCHEMA_VERSION
            ),
            'mutation_enabled_inactive_start_atomic_commit': (
                a48c4.SCHEMA_VERSION
            ),
            'single_template_selection_evidence': (
                a48c5.SELECTION_EVIDENCE_SCHEMA_VERSION
            ),
            'mutation_free_inactive_start_atomic_commit': (
                a48c5.SCHEMA_VERSION
            ),
            'single_template_completion_selection_evidence': (
                a48c6.SELECTION_EVIDENCE_SCHEMA_VERSION
            ),
            'mutation_free_inactive_start_completion_atomic_commit': (
                a48c6.SCHEMA_VERSION
            ),
            'single_template_completion_mutation_selection_evidence': (
                a48c7.SELECTION_EVIDENCE_SCHEMA_VERSION
            ),
            'mutation_enabled_inactive_start_completion_atomic_commit': (
                a48c7.SCHEMA_VERSION
            ),
            'replication_early_noop_plan': a48c8.SCHEMA_VERSION,
            'replication_early_noop_atomic_commit': (
                a48c8.INTEGRATION_SCHEMA_VERSION
            ),
            'resident_world_arena_shadow': a49a.SCHEMA_VERSION,
            'selected_translation_plan': (
                a49b.SELECTED_PLAN_SCHEMA_VERSION
            ),
            'resident_selected_translation': a49b.SCHEMA_VERSION,
            'maintenance_supplement': a49c.SUPPLEMENT_SCHEMA_VERSION,
            'selected_maintenance_plan': (
                a49c.SELECTED_PLAN_SCHEMA_VERSION
            ),
            'resident_maintenance_foundation': a49c.SCHEMA_VERSION,
        },
        'development_slice': (
            'A4.9c-composite52-maintenance-shadow-foundation-and-'
            'disposable-pure-selected-maintenance-plan'
        ),
        'promoted_baseline_unchanged': 'SOMA-CELL 0.6.8-GPU A3',
        'full_gpu_world_step': False,
        'resident_biology_authority': (
            a49c.PORT_STATUS['resident_biology_authority']
        ),
        'resident_device_writes': (
            a49c.PORT_STATUS['resident_device_writes']
        ),
        'resident_device_writes_scope': (
            a49c.PORT_STATUS['resident_device_writes_scope']
        ),
        'resident_d2h_publish': (
            a49c.PORT_STATUS['resident_d2h_publish']
        ),
        'resident_d2h_publish_scope': (
            a49c.PORT_STATUS['resident_d2h_publish_scope']
        ),
        'selected_maintenance': a49c.PORT_STATUS['selected_maintenance'],
        'maintenance_resident_device_writes': (
            a49c.PORT_STATUS['maintenance_resident_device_writes']
        ),
        'maintenance_resident_d2h_publish': (
            a49c.PORT_STATUS['maintenance_resident_d2h_publish']
        ),
        'maintenance_live_commit': (
            a49c.PORT_STATUS['maintenance_live_commit']
        ),
        'maintenance_resident_biology_authority': (
            a49c.PORT_STATUS['maintenance_resident_biology_authority']
        ),
        'frozen_prior_test_count': 94,
        'frozen_prior_test_names_sha256': _A49C_PRIOR_94_NAMES_SHA256,
        'frozen_prior_test_ast_sha256': _A49C_PRIOR_94_AST_SHA256,
        'resident_arena_core_sha256': _A49A_CORE_SHA256,
        'resident_translation_core_sha256': _A49B_CORE_SHA256,
        'resident_maintenance_foundation_core_sha256': _A49C_CORE_SHA256,
        'a49c_preregistration_prefix_bytes': _A49C_PREREG_PREFIX_BYTES,
        'a49c_preregistration_prefix_sha256': (
            _A49C_PREREG_PREFIX_SHA256
        ),
        'a49c_preregistration_sha256': _A49C_PREREG_SHA256,
        'fixed_fixture_capacity': {
            'C': 4, 'Q': 10, 'S': 340, 'W': 48, 'P': 32, 'K': 21,
        },
        'require_cuda': bool(_REQUIRE_CUDA),
        'source_sha256_map': a49c_source_sha256_map(),
        'passed': passed, 'failed': failed, 'total': len(rows),
        'elapsed_seconds': elapsed, 'rows': rows,
    }
    if write:
        output_dir = os.path.abspath(output_dir or HERE)
        os.makedirs(output_dir, exist_ok=True)
        with open(os.path.join(output_dir, RESULT_JSON), 'w', encoding='utf-8') as handle:
            json.dump(payload, handle, indent=2, sort_keys=True)
            handle.write('\n')
        with open(os.path.join(output_dir, RESULT_CSV), 'w', newline='', encoding='utf-8') as handle:
            writer = csv.DictWriter(
                handle, fieldnames=('test', 'status', 'detail', 'error', 'seconds'),
            )
            writer.writeheader()
            writer.writerows(rows)
        lines = [
            '%s VALIDATION' % a49c.BUILD,
            '%d PASS / %d FAIL / %d TOTAL' % (passed, failed, len(rows)),
            'elapsed %.6fs' % elapsed,
            '',
        ]
        lines.extend('%s: %s - %s' % (
            row['status'], row['test'], row['detail'] or row['error'],
        ) for row in rows)
        with open(os.path.join(output_dir, RESULT_TXT), 'w', encoding='utf-8') as handle:
            handle.write('\n'.join(lines) + '\n')
    return payload


def main(argv=None):
    global _REQUIRE_CUDA
    parser = argparse.ArgumentParser()
    parser.add_argument('--require-cuda', action='store_true')
    parser.add_argument('--write', action='store_true')
    parser.add_argument('--output-dir')
    args = parser.parse_args(argv)
    _REQUIRE_CUDA = bool(args.require_cuda)
    payload = run_all(write=args.write, output_dir=args.output_dir)
    return 0 if payload['failed'] == 0 else 1


if __name__ == '__main__':
    raise SystemExit(main())
